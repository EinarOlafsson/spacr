================================================================================
THE MONTAGE KNOWS ITS OWN RUN, AND SAYS HOW IT CHOSE
================================================================================

Status:    filed 2026-08-18.
Requested: 2026-08-18, from the cells-behind-a-gene feature --

  "the coefficient table on the screen was not read from a run folder ... it is
   true ... but that is because it was not externally loaded it was run in the
   application so the application should know where the run folder is given
   that it just created it. this information should all be saved is whatever
   form in the run. for this feature there should also be more settings ...
   for each well that is pulled it should get its own tab that is only closed
   when an x in the top left of the tab (which states the well and gRNA) is
   clicked ... how is the infered classefication score calculated here? please
   discribe that to me and if appropreate give the user some settings to
   influence how stringent this equation is."

Related: 131 built this feature. 154 G / 153 are the run-is-loaded work. 116 is
         per-run state. 99 resolves hits back to cells.

--------------------------------------------------------------------------------
A. THE APPLICATION FORGETS A FOLDER IT CREATED ITSELF
--------------------------------------------------------------------------------

CONFIRMED 2026-08-18. `ml.py:5954` writes `regression_data.csv` into
`res_folder`, so the run knows exactly where it is. And:

    grep -n "res_folder|run_folder|_folder" spacr/qt/widgets/regression_results.py
    -> NOTHING

The results panel never receives it. So `results_provider` has nothing to
return for a LIVE run, and the montage reports RESULTS_WITHOUT_A_FOLDER
(`cell_montage_view.py:660`) -- a sentence that is literally true and
practically absurd: the application is telling the user it cannot find a folder
it created ten seconds earlier.

THE FIX IS NOT A BETTER MESSAGE. The run must carry its own folder, and every
view that needs it asks the run rather than the widget that happens to hold the
table. Per the request: "this information should all be saved in whatever form
in the run" -- so it is written into the run, not merely passed in memory, and a
run reloaded from disk answers the same question the same way. 154 G already
made a finished run the LOADED run and gave a run on disk first-class status;
this is the same object gaining the one attribute it was missing.

ALSO: the sentence should say "the LOADED coefficient table", which is what the
user calls it. "The coefficient table on screen" describes a widget; the user is
thinking about a run.

WHAT MUST STILL BE SAID: a table genuinely pasted or opened from a bare CSV has
no run folder, and that case keeps its message. The bug is that a run made in
the application takes the same branch as a stray CSV.

--------------------------------------------------------------------------------
B. HOW THE SCORE IS CALCULATED -- WRITE IT WHERE THE USER CAN READ IT
--------------------------------------------------------------------------------

Asked directly, so it is recorded here in full and must also appear ON SCREEN.
`cell_montage.score_window` (spacr/cell_montage.py:380):

    baseline  = median per-object score over EVERY object supplied
                (the whole screen, not the gene's wells)
    target    = baseline + effect          <- SCORE_TARGET_RULE
    scale     = 1.4826 * MAD of the same screen-wide scores
    window    = target +/- half_widths * scale     (half_widths = 1.0)
    "closest" = smallest |score - target|

WHY `baseline + effect` IS THE TARGET: under the well-level model a well scores
`baseline + fraction * effect`, so a well made ENTIRELY of cells carrying the
guide would score `baseline + effect` -- which is the score one such cell
implies. It is an inference from a pooled fit, not a measurement of that cell.

HOW MANY COME FROM EACH WELL: `round(n_objects_in_well * guide_fraction)`
(`objects_to_show`), the expected number carrying the guide. 200 cells at 15%
gives 30. A well rounding to zero is REPORTED as a zero-contribution well rather
than vanishing. Halves round away from zero, not banker's rounding, so the
caption's "round(n x fraction)" is reconstructable.

DEGENERATE CASES ALREADY HANDLED: MAD of zero falls back to the plain standard
deviation, and if that is zero too the window is infinite and SAYS it is
degenerate rather than admitting nothing.

THE ONE-WIDTH RULE IS DELIBERATE AND MUST SURVIVE ANY SETTING ADDED HERE.
`WINDOW_HALF_WIDTHS` is one number for the whole screen and every coefficient,
because "a window recomputed per gene is a window that can be tuned until the
pictures look right, and nothing in the output would show that it had been."

--------------------------------------------------------------------------------
C. THE SETTINGS, AND THE ONE RULE THEY MAY NOT BREAK
--------------------------------------------------------------------------------

"give the user some settings to influence how stringent this equation is."

  * WINDOW HALF-WIDTH (`half_widths`, default 1.0 robust scales). The direct
    stringency control. Already an argument and already written into the
    caption when overridden -- surface it.
  * BASELINE SOURCE: the screen median (default) or THE FITTED INTERCEPT, which
    `score_window` already accepts as `baseline=` and records as 'given'. The
    intercept is the model's own answer and is arguably the better one; offer
    both and say which is in force.
  * SCORE COLUMN (`pred` by default) -- a screen with more than one classifier
    output has more than one candidate.
  * MAX OBJECTS (`MAX_OBJECTS`, 300) with its measured cost visible: the merged
    source is priced by FIELDS TOUCHED, not crops cut -- 300 crops cost 11.43
    ms each over 30 fields against 2.58 ms over 6.
  * PER-GUIDE OR SUMMED for a gene with several guides
    (`select_montage_per_guide` exists).

EVERY ONE OF THESE CHANGES WHICH CELLS A READER IS LOOKING AT, so every one is
written into the caption, and a non-default value says so ON the montage.
THE SETTING IS PER SCREEN, NEVER PER GENE. A per-gene width is the tuning the
module was built to prevent; if a per-gene control is ever wanted it must record
itself so loudly that a figure made that way cannot be mistaken for one that
was not.

--------------------------------------------------------------------------------
D. A TAB PER WELL, CLOSED ONLY BY ITS OWN X
--------------------------------------------------------------------------------

"for each well that is pulled it should get its own tab that is only closed when
an x in the top left of the tab (which states the well and gRNA) is clicked."

  * ONE TAB PER WELL, not one montage of everything. The well is the unit the
    fraction and the count are defined on, so it is the unit a user checks.
  * THE TAB'S LABEL NAMES THE WELL AND THE gRNA, both, because a gene with
    several guides pulls the same well more than once and two tabs called
    `p1_r3_c7` are indistinguishable.
  * THE X IS THE ONLY WAY IT CLOSES. It must survive selecting another
    coefficient, re-sorting the table and re-running -- comparing two genes'
    cells side by side is the whole point, and a tab that closes itself when
    the selection moves makes that impossible.
  * BOUNDED, and the bound stated: tabs hold pixmaps, so say what the limit is
    rather than letting a user discover it by filling memory.

--------------------------------------------------------------------------------
E. WHAT MUST BE TRUE FOR IT TO WORK -- THE CONTRACT, AS THE MAINTAINER STATES IT
--------------------------------------------------------------------------------

  1. The database tables -- AT MINIMUM `png_list` -- can be merged with the
     classification scores.
  2. The measurements database sits in:

         folder/measurements/measurements.db
         folder/data/      <- the exported PNGs
         folder/merged/    <- the arrays crops can be cut from

TWO ROUTES TO PIXELS, AND THEY NEED DIFFERENT THINGS. State the requirement,
check it up front, and name what is missing instead of failing later:

  ROUTE 1 -- LIVE CROP FROM `merged/`. Needs IMAGE CHANNELS, THE MASK ARRAY, and
  A BOUNDING BOX OR OBJECT ID. With an object id the crop follows the object's
  own mask; that is the better picture and the one worth defaulting to.

  ROUTE 2 -- COORDINATES FROM A TABLE, then crop from the merged arrays. Needs
  THE TABLE AND THE CHANNELS. BOUNDING-BOX CROPS ONLY -- there is no mask in
  this route, so an object-shaped crop cannot be offered and must not appear as
  a choice that silently does something else.

`crops.resolve_crop_source` and `cell_montage.resolve_montage_crop_source`
already choose between the PNGs and `merged/` AND say which they chose. This
adds the two routes' own requirements to that answer, so a user missing a
channel list is told THAT rather than being told there is no source.

--------------------------------------------------------------------------------
HOW TO KNOW IT WORKED
--------------------------------------------------------------------------------

* Run a regression in the application, click a gene, and the montage appears --
  no "Load run…", no folder to point at. The run knew where it was.
* The same run, reopened from disk in a later session, behaves identically.
* Each well is its own tab, labelled with well AND gRNA, and stays open until
  its x is clicked -- including after another coefficient is selected.
* The montage states the target, the baseline and its source, the window in
  robust scales, and `round(n x fraction)` for each well, in words a reader can
  check the arithmetic from.
* Changing the half-width changes which cells appear AND changes the caption.
* A screen missing the channel list is told the channel list is missing.


================================================================================
F. THE PATHS MUST SURVIVE THE FOLDER MOVING MACHINE
================================================================================

Requested: 2026-08-18 -- "there is logic in spacr, make sure that if a source
           folder moves computer the paths still work as long as the folder
           structure i described is upheld. please find this logic and see if
           it will hold in this context. it may need to be generalized further."

THE LOGIC EXISTS, IN TWO PLACES, WITH TWO DIFFERENT STRATEGIES:

  * `utils.correct_paths(df, base_path, folder='data')` -- splits a path on
    `/data/` and re-anchors the remainder under the new root. Used for
    `png_path`.
  * `crops.MergedCropSource.resolve_path` -- if the recorded path does not
    exist, retries `<merged_root>/<basename>`. Used for `merged_path` /
    `path_name`.

And the anchor itself is already derivable and already correct:
`cell_montage_view.experiment_root(db_path)` walks up from
`<folder>/measurements/measurements.db` to `<folder>`, which is exactly the
structure the request describes. So the ROOT is not the problem.

MEASURED 2026-08-18, four cases through the real `correct_paths`:

    OK    same-OS move        /old/home/exp1/data/plate1/a.png
                           -> /mnt/newdisk/exp1/data/plate1/a.png

    FAIL  Windows path read on Linux
                              C:\lab\exp1\data\plate1\a.png
                           -> unchanged

    WRONG nested `data/` twice
                              /old/data/exp1/data/plate1/a.png
                           -> /mnt/newdisk/exp1/data/exp1

    FAIL  no `data/` component
                              /old/home/exp1/crops/plate1/a.png
                           -> unchanged

SO THE ANSWER TO THE QUESTION IS: IT HOLDS FOR THE CASE ASKED ABOUT AND FAILS
TWO OTHERS, ONE OF THEM SILENTLY.

1. THE SEPARATOR IS HARD-CODED. `path.split(f'/{folder}/')` cannot match
   `\data\`, and `os.path.basename` on Linux returns the WHOLE of
   `C:\lab\exp1\merged\x.npy` because it knows nothing of backslashes. A
   database written on Windows and opened on Linux -- or on a share mounted
   both ways -- re-anchors through neither route. Both need a
   separator-agnostic split: normalise `\` to `/` before matching, and take the
   basename after the last separator of EITHER kind.

2. THE THIRD CASE IS THE DANGEROUS ONE, because it does not fail. `split` takes
   the FIRST `/data/`, so an experiment whose old root itself contained a
   `data` folder produced `/mnt/newdisk/exp1/data/exp1` -- a path that has
   silently lost `plate1/a.png` and now names a directory. SPLIT ON THE LAST
   OCCURRENCE, not the first: the anchor is the one nearest the file.

3. `base_path not in path` IS A SUBSTRING TEST standing in for "is this already
   re-anchored". It answers wrongly whenever the old path happens to contain
   the new root as a substring, and it is not what the question means. Compare
   resolved prefixes.

4. GENERALISE IT TO THE FOLDER, NOT THE COLUMN. `correct_paths` re-anchors
   `png_path` only, while this feature needs `path_name` and `merged_path` too,
   and the structure the request states has TWO anchor folders (`data/` and
   `merged/`) under one root. One function should take the experiment root and
   re-anchor every path-bearing column against whichever anchor its own path
   contains -- with the anchor found from the RIGHT, per point 2.

5. AND IT MUST SAY WHEN IT CANNOT. A path with no recognisable anchor is
   returned unchanged today and fails later as a missing file, somewhere with
   less context. Count them and name one, the way the merge names its dropped
   columns: "31 of 1,204 crops could not be re-anchored under <root>; the first
   is <path>". A silent pass-through is how case 3 stayed invisible.

HOW TO KNOW IT WORKED

* A run folder written on machine A, moved to machine B at a different path,
  opens and shows its montage with no setting changed -- the case asked for.
* The same with a database whose paths were written on Windows.
* An old root containing its own `data/` folder re-anchors to the file, not to
  a directory. This one is a regression test for a silent corruption.
* Paths that cannot be re-anchored are counted and one is named.

--------------------------------------------------------------------------------
2026-08-18 -- SECTION A DONE. THE RUN CARRIES ITS FOLDER.
--------------------------------------------------------------------------------

WHAT SHIPPED (A only; B-F are untouched by this pass)

  * `RegressionResultsPanel.run_folder()` and `.run_name()`. The panel held
    the folder all along, in `_path` -- and `_path` is a CSV when a run is
    opened off disk and a DIRECTORY when `_on_pipeline_result` hands
    `res_folder` straight over, so every caller had to guess which it was
    holding, and every caller guessed differently. Both are answered here,
    once. `""` for a table with no run behind it, which is a different
    situation and keeps its own sentence.
  * `app_screen._results_source_path` -- the montage's provider -- ASKS THE
    RUN. The Runs tab's `loaded_run_folder()` first, the panel second (for
    results opened with "Load results…" without a run row). It now always
    returns a FOLDER.
  * A RUN RELOADED FROM DISK ANSWERS THE SAME WAY, and needs nothing written
    to make that true: the folder is not a fact carried beside the run, it IS
    the run. Which is what "this information should all be saved in whatever
    form in the run" comes to once the run is a folder -- no manifest, no
    sidecar, and nothing that can disagree with where the file actually is.

TWO BUGS FOUND ON THE WAY, BOTH FIXED, BOTH FROM THE SAME AMBIGUITY

  * `cell_montage.load_fraction_frame` resolves a DIRECTORY to
    `<dir>/regression_data.csv` and takes a FILE as the fraction CSV itself.
    A run opened off disk left `_path` as `<run>/results.csv`, so the montage
    read the COEFFICIENT table as the fraction table and failed on a missing
    `fraction` column -- a confusing error for a run that had everything it
    needed. Returning the folder is what fixes it.
  * `app_screen._scan_source_frame` ran `dirname` over `_path`
    unconditionally. Right for the CSV, WRONG for the directory a live run
    leaves: it climbed to `results/` and looked for `regression_data.csv`
    beside the sibling runs, where there is none -- so the Measurements tab
    silently found no source frame after a live run. Now asks
    `_results_source_path()`.

WHAT REMAINS IN A

  * THE SENTENCE. `cell_montage_view.RESULTS_WITHOUT_A_FOLDER` should say
    "the LOADED coefficient table" -- what the user calls it -- rather than
    "the coefficient table on screen", which describes a widget. NOT CHANGED
    HERE: `cell_montage_view.py` belongs to another agent this session. It is
    a one-string change and it is in `handoffs`.
  * With the provider fixed, that branch should now be reached only by a
    table genuinely opened from a bare CSV, which is the case the sentence is
    FOR. Worth re-reading it once both halves have landed.



================================================================================
2026-08-18 -- B, C, D, E AND F SHIPPED. (A is recorded above by its own agent.)
================================================================================

WHAT LANDED

B -- THE ARITHMETIC IS ON THE MONTAGE. `MontagePlan.arithmetic()` prints the
    whole sum with the real numbers: where the baseline came from and over how
    many objects, `baseline + effect` worked through, `1.4826 x MAD`, the
    bounds, `round(n x fraction) = k -> j shown` for every well, and a total
    whose terms are printed beside it (`total: 4 + 1 + 4 = 9`). A degenerate
    window says so instead of printing a width of zero. Over 40 wells the list
    elides AND CARRIES ITS OWN COUNT rather than trailing off
    (`MontagePlan.WELL_LINES`). It is inside `caption()`, so the saved figure
    has it too.

C -- FIVE SETTINGS, ALL SURFACED, ALL WRITTEN INTO THE CAPTION.
    Window half-width, baseline source (screen median | THE FITTED INTERCEPT),
    score column, MAX_OBJECTS and per-guide/summed are on a second controls
    row. `MontagePlan.settings_line()` prints every one of them WHETHER OR NOT
    it is a default, and a non-default adds a second line naming what changed
    and saying the montage is not comparable to one made with the defaults.
    THE SETTINGS ARE PER SCREEN: they live on the view, not per coefficient,
    and `test_the_settings_are_per_screen_and_not_per_gene` holds it.
    `score_window(baseline_label=...)` is new -- a montage centred on the
    intercept says "the model's fitted intercept" instead of "given", which
    named no source at all. A table with NO Intercept row is told so and the
    button greys; falling back to the median would caption one baseline under
    a control reading the other.

D -- ONE TAB PER WELL. `_WellTab`, keyed on
    `(coefficient, level, guides, well)`, labelled with the WELL AND THE gRNA,
    carrying its OWN caption (well, count, window, settings, the inference
    notice) so it is safe beside another gene's. Tab 0 is a non-closable
    Summary -- no thumbnails, it is where the arithmetic and every refusal are
    read. THE BOUND IS `MAX_WELL_TABS = 12` and it is STATED on the montage;
    when it is reached the new wells are refused BY NAME and no tab is ever
    closed for the user.

E -- THE TWO ROUTES, CHECKED UP FRONT. `montage_route_requirements` ->
    `RouteRequirements(route, shapes, missing, assumed, detail)`, attached to
    `CropSourceChoice.requirements`. `merged-mask` (channels + mask plane +
    object id) offers both shapes; `merged-bbox` (coordinates, no mask) offers
    BOUNDING BOXES ONLY and the object-shaped entry is DISABLED with the
    reason; the PNG route greys the whole shape control ("cut when the run
    wrote them"). A run whose measurements.db records no `png_dims` is told
    THE CHANNEL LIST IS MISSING and which planes were assumed -- it still
    draws.

F -- THE PATHS. New in `spacr/crops.py`: `normalise_separators`,
    `basename_any`, `path_components`, `path_is_under`, `reanchor_path`,
    `ReanchorReport`, `reanchor_frame`, `PATH_ANCHORS`, `PATH_COLUMNS`.
    `utils.correct_paths` delegates to them; `PngCropSource.resolve` and
    `MergedCropSource.resolve_path` use them too, so there is ONE re-anchor.
    All four measured cases now behave:

        OK     same-OS move
        OK     C:\lab\exp1\data\plate1\a.png read on Linux
        OK     /old/data/exp1/data/plate1/a.png -> <root>/data/plate1/a.png
               (the anchor is found from the RIGHT -- this was the silent one)
        SAID   no anchor: unchanged, but COUNTED and one NAMED

    `reanchor_frame` re-anchors `png_path`, `path_name` AND `merged_path`
    against `data/` and `merged/` in one pass, and the montage loader runs it
    per database against `experiment_root(db)`; its report becomes a caption
    line. `tests/test_paths_survive_a_move.py` is the named regression test
    for the silent corruption.

FIVE THINGS THE TEST SUITE WAS GREEN THROUGH AND DRIVING THE WIDGET FOUND

  1. THE X WAS ON THE RIGHT. Qt puts the close button wherever
     `SH_TabBar_CloseButtonPosition` says, and on this style that is the
     right. The request asks for the top LEFT. The automatic button is now
     removed and ours placed on the left -- and it closes BY WIDGET, not by
     the index it was created at, which moves whenever an earlier tab closes.
  2. TWO TABS WITH THE SAME LABEL. The guide-level coefficient `GRA14_1` and
     the GENE `GRA14` shown one guide at a time both open a tab for `GRA14_1`
     in the same well. Different effect, different window, different cells,
     identical label. Now `GRA14_1` vs `GRA14_1 (of GRA14)`, with a
     last-resort `#2` suffix if two ever still collide.
  3. A TAB THIS RUN NO LONGER FILLS KEPT ITS OLD THUMBNAILS. Narrow the cap,
     re-run, and the wells that dropped out still showed the previous
     montage under a summary describing the new settings. The tab is not
     closed -- only its x does that -- it is emptied and says why.
  4. THE WIDGET'S MINIMUM WIDTH WENT TO 1346 px. The tab lives in the left
     half of the figures splitter, which starts at 780. Two rows of controls
     with their prose spelled out in the widgets did it. The words moved to
     the tooltips (where instruction 113 wants them anyway) and the combos
     elide: 503 px now, BELOW the 978 it was before this work.
  5. The empty-tab message sat IN the grid, so anything counting thumbnails
     counted it. It is a label above the scroll area now; the "no crop"
     placeholders stay in the grid because they ARE one per object.

TAKEN OVER FROM A's AGENT, AS ASKED: `RESULTS_WITHOUT_A_FOLDER` and the
"no fitted effect" sentence now say "the LOADED coefficient table".

WORTH NOT REDISCOVERING

  * `channels_from_settings` ALWAYS returns channels -- it defaults to
    `{r:2, g:1, b:0}`. So "the spec has channels" is NOT the same question as
    "a channel list exists", and the second is the one the user is asking.
    `resolve_montage_crop_source` reads `measurements.db` itself to answer it.
  * `MergedCropSource.spec_for` REQUIRES `object_label`; a bounding box alone
    is not enough for it. That is why `merged-bbox` reports the missing id
    rather than pretending the route is complete.
  * Widening the score window does NOT show more cells. The count rule caps
    each well at `round(n x fraction)` whatever the window admits, so only
    NARROWING is visible. A test written the other way proves nothing.

NOT DONE

  * The tab bound is a count, not a memory budget. 12 tabs x 300 objects is
    ~130 MB of pixmaps in the worst case; a real budget would count pixmaps.
  * `plot.py:6155` re-anchors the activation folder with
    `folder=settings['activation_mode']`, which is neither `data` nor
    `merged`. It goes through the fixed `correct_paths` and is correct, but
    it is a third anchor name that `PATH_ANCHORS` does not know about --
    worth folding in if that folder ever moves too.

--------------------------------------------------------------------------------
2026-08-20 -- CLOSED. Every section above is marked SHIPPED with what it did.
The items under "NOT DONE" are recorded scope decisions with their reasons,
not outstanding work; anything among them that is later wanted is a new
instruction rather than this one reopened.
--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
2026-08-26 -- RE-AUDIT. Every criterion re-checked against the code; TWO NAMES
             IN THE PROSE ABOVE HAVE SINCE MOVED. The behaviour is unchanged.
--------------------------------------------------------------------------------

Checked by driving `select_montage` and `reanchor_path`/`reanchor_frame`, and by
running tests/test_the_montage_needs_no_run_folder.py,
tests/test_paths_survive_a_move.py, tests/test_crop_paths_survive_a_move.py and
tests/test_cell_montage.py (131 tests, green). The caption prints the target,
the baseline and its source, the window in robust scales and
`round(n x fraction)` per well; a narrower half-width changes both the caption
and the selection; `montage_route_requirements(..., channels_declared=False)`
names the missing channel list; the tab close button is placed on
`QTabBar.LeftSide`; all four re-anchor cases behave and an unanchorable path is
counted and named.

WHAT THE PROSE ABOVE NOW MISNAMES, corrected here rather than rewritten there,
so the record of what was done stays readable:

  * SECTION C says `MAX_OBJECTS, 300`. It is 2000 (cell_montage.py). The
    number was raised after this file was written; the caption prints whatever
    it is ("cap 2,000 objects"), so nothing downstream depended on the 300.
  * The 2026-08-18 SECTION A note names `cell_montage.load_fraction_frame`.
    That function is now `cell_montage.read_well_guide_fractions`, with the
    same folder-or-CSV resolution the note describes.
