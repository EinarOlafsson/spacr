================================================================================
THE MODEL SUMMARY SURVIVES BEING LOADED FROM DISK
================================================================================

Status:    filed 2026-08-18.
Requested: 2026-08-18 --

  "i get: 'No summary: this panel was opened from a results table on disk
   rather than from a run, so the fitted model is not here. Re-run to see it.'
   dor the run, also eaven if this was loaded from disk i should still have
   access to the summary."

Related: 132/144 are the model box. 146 is the Runs tab. 139 C established that
         saved and visible are the same event -- this is the same principle
         applied to text instead of pictures.

--------------------------------------------------------------------------------
A. THE SUMMARY IS ALREADY ON DISK. NOTHING READS IT BACK.
--------------------------------------------------------------------------------

FOUND 2026-08-18. `spacr/ml.py:6270` already does this:

    save_summary_to_file(model, file_path=f'{res_folder}/mode_summary.csv')

So a run DOES write `model.summary().as_text()` into its results folder. The
panel simply never looks: `regression_results.summary_text(model)` returns the
"not here, re-run" sentence the moment `model is None`, and there is no path
that consults the run folder.

THE FIX IS TO READ THE FILE THAT IS ALREADY BEING WRITTEN. A panel opened from
a results table knows its folder -- that is how it found `results.csv` -- so
the summary is one `open()` away. Nothing needs recomputing and no fit needs
repeating, which is what makes "Re-run to see it" the wrong advice as well as
an unhelpful one: it asks for minutes of GPU time to recover a file already
sitting beside the table.

TWO DEFECTS IN THE FILE ITSELF, to fix while here, under the standing rule
that the format is corrected going forward and old content is migrated:

  * IT IS CALLED `mode_summary.csv`. "mode" is a typo for "model", and the
    contents are plain text, not CSV -- `save_summary_to_file`'s own docstring
    says so: "Despite the default 'summary.csv' name, the content is the
    statsmodels text summary, never CSV." A name that does not follow the file
    is a path nobody can open, which is the same rule the figure work landed
    on.
  * WRITE `model_summary.txt` GOING FORWARD, and READ BOTH -- old runs on disk
    keep working, and their summaries appear where they always should have.

--------------------------------------------------------------------------------
B. "OPENED FROM DISK" IS SOMETIMES A FALSE EXPLANATION
--------------------------------------------------------------------------------

The maintainer says they get this "dor the run" -- for a live run, not only for
a table loaded from disk. That is possible, and the code says how:

    regression_results.py ~853-871
        except PanelUnavailable as error:  -> clear_diagnostics(...); return
        except Exception as error:         -> clear_diagnostics(...); return
        self._model = model                <- only reached if neither fired

`self._model` is assigned AFTER the diagnostics are computed. If the QC context
cannot be built -- an unsupported backend, a fit the diagnostics do not cover,
any exception at all -- the function returns before the assignment, and the
model is discarded even though the run produced one. The Summary tab then says
the panel "was opened from a results table on disk", WHICH IS NOT TRUE, and
sends the user to re-run something that just ran.

TWO THINGS WRONG AND BOTH MATTER:

  1. THE MODEL SHOULD BE KEPT REGARDLESS. It is the fit; the diagnostics are a
     view of it. Store it FIRST, then attempt the diagnostics, so a failure in
     the view cannot destroy the thing being viewed.
  2. A MESSAGE MUST NOT ASSERT A CAUSE IT HAS NOT CHECKED. "No summary" is
     honest; "because this panel was opened from a results table on disk" is a
     story, and it is told with equal confidence whether or not it happened.
     Distinguish the cases and say the true one -- there is no summary because
     the panel was loaded from disk, or because the diagnostics failed (naming
     the error), or because the backend has none (already handled correctly at
     the next branch down, which is the model to follow).

--------------------------------------------------------------------------------
C. WHAT "ACCESS TO THE SUMMARY" SHOULD MEAN
--------------------------------------------------------------------------------

The request is for the summary to be there, not for a placeholder that explains
itself better. So:

  * loaded from disk with a summary file present -> the summary, with a line
    saying it was read from the run rather than recomputed, because a reader
    pasting it into a methods section should know it is the run's own;
  * loaded from disk with NO summary file (an older run) -> say exactly that,
    naming the folder looked in;
  * a live run -> the live model's summary, as today.

Every path ends in either a summary or a true sentence about why not.

--------------------------------------------------------------------------------
HOW TO KNOW IT WORKED
--------------------------------------------------------------------------------

* Run a regression, close the panel, re-open the results table from disk: the
  Summary tab holds the same text it held during the run, byte for byte.
* A run whose diagnostics fail still shows its summary, and the reason the
  diagnostics failed is stated where the diagnostics would have been.
* No message claims the panel was opened from disk unless it was.
* An old run folder holding `mode_summary.csv` still shows its summary; new
  runs write `model_summary.txt`.

--------------------------------------------------------------------------------
2026-08-18 -- SHIPPED, ALL OF A AND B AND C. Commit cab06a6b.
--------------------------------------------------------------------------------

WHAT SHIPPED

A. THE FILE IS READ BACK. `regression_results.find_summary_file(path)` takes a
   results CSV, a run folder, or a parent of one and returns the summary
   beside the table that was actually chosen. `summary_text` grew two keyword
   arguments -- `path=` and `reason=` -- and with no live model it reads the
   file and prefixes it with

       Read from <path>: this is the run's own summary, written when it was
       fitted, not recomputed here.

   Nothing is recomputed and no fit is repeated. Driven through the real
   AppScreen: Load run… -> the Results tab -> the Summary tab holds the run's
   own text.

   THE PROOF THAT IT IS READ AND NOT RE-RENDERED, worth keeping because it
   cost ten minutes to interpret: a fresh `model.summary().as_text()` differs
   from the file in exactly one place, the `Time:` field in the header. An
   equality test against a re-render therefore FAILS even when the feature is
   perfect. Compare against the bytes on disk, not against a new render.

A'. THE NAME. `spacr.ml.SUMMARY_FILENAME` is `model_summary.txt` and
   `SUMMARY_FILENAMES` is `(model_summary.txt, mode_summary.csv,
   summary.csv)` -- newest first, all three read. `save_summary_to_file`'s
   default moved to the new name, it makes its parent folder, it returns the
   path it wrote, and it no longer raises: a backend with no `summary()` and
   one whose `summary()` throws both come back `None` rather than taking a
   finished run down over a text file.

A''. ONE DEFECT THE INSTRUCTION DID NOT KNOW ABOUT. The save was INSIDE the
   `if settings['verbose']:` branch, beside the `print`. So a quiet run --
   the normal case -- wrote no summary at all, and "the summary is already on
   disk" was true only for verbose runs. It is written unconditionally now;
   printing stayed behind `verbose`, where it belongs. A test holds the
   indentation relationship, because this is the kind of thing a later edit
   re-introduces silently.

B. THE MODEL IS STORED FIRST. `set_diagnostics` assigns `self._model` before
   it imports regression_qc or builds a context; the three failure branches
   now go through a new private `_clear_diagnostic_views`, which empties the
   tabs and says why WITHOUT discarding the fit. `clear_diagnostics` still
   drops it, because it is called when a NEW TABLE arrives and the old fit has
   nothing to say about that.

   So a fit whose diagnostics cannot be built still shows its summary, and the
   three tabs carry the error that stopped them.

B'. NO MESSAGE ASSERTS A CAUSE IT HAS NOT CHECKED. `_no_model_reason` returns
   only reasons that actually happened:
     * the diagnostics failed and the fit was not kept (naming the error) --
       defensive now, since B makes it unreachable;
     * `NO_MODEL_FROM_THIS_RUN`, when a run of this session handed over its
       settings and no model. `set_run_settings` is called ONLY by the live
       path, which is what makes "from a run" checkable rather than guessed;
     * otherwise `summary_text` decides from what it can see: a path means
       disk, no path means nothing has been fitted yet.

C. `set_frame` refreshes the Summary tab. It did not, so a table opened from
   disk sat under the PREVIOUS run's statsmodels output with nothing saying
   whose it was -- a stale summary is worse than an absent one.

TESTS: tests/test_the_model_summary_survives_being_loaded_from_disk.py, 23 of
them. tests/test_the_regression_summary_is_a_tab.py lost its assertion that a
bare `summary_text(None)` says "results table on disk" -- that was the false
explanation this instruction is about, asserted as a property.

WHAT REMAINS

* NOTHING IN 153. Every path ends in a summary or a sentence that was checked.
* The api-i18n ratchet wants +6 for the public surface added across 153 and
  154 G -- `find_summary_file`, `SweepRunsPanel.loaded_run`,
  `.loaded_run_folder`, `.set_loaded_run`, `.load_run_from_disk`, and
  `CellMontageView.loaded_run_name`. The documented CONSTANTS do not count;
  only functions and methods with docstrings do. Measured, not guessed:
  the counter reads 7214 against a reviewed 7185, and the other 23 are
  another session's.

ONE FINDING WORTH NOT REDISCOVERING

`RegressionResultsPanel._path` is a FOLDER when a live run hands the table
over (`res_folder`) and a CSV FILE PATH when `load()` found it on disk. Every
reader has to handle both -- `find_summary_file` does, and so does
`CellMontageView.loaded_run_name`. It is not a bug worth changing under this
instruction, but it is a trap: `os.path.dirname(panel._path)` is right in one
case and silently wrong in the other.

--------------------------------------------------------------------------------
2026-08-19 -- CLOSED. The work is done; the ratchet is not this file's to bump.
--------------------------------------------------------------------------------

Every path in 153 ends in a summary or a checked sentence, and the Summary
tab now folds it too (168 D). The one loose end recorded above -- the
api-i18n public-surface ratchet -- is NOT bumped here, and deliberately.

Measured 2026-08-19: the counter reads 7633 against a reviewed 7185, which is
+448. The six functions 153 and 154 G added are in there, and so are roughly
forty from this session's sweep, attribution, tile-chrome and folding work --
but the bulk is the concurrent public-API restructuring (typed MaskConfig and
MeasureConfig, curated API navigation). Bumping the number here would absorb
four hundred additions this instruction never made, under a comment claiming
to name what it admitted. The ratchet's own message asks for the opposite:
"name what it admitted in the comment above, and regenerate the target
catalogs".

So it stays for whoever owns that surface, together with the runtime i18n
catalogs, which need regenerating for the seventeen picture-settings tooltips
added under 176 B.
