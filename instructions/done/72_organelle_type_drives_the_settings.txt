================================================================================
ONE "ORGANELLE TYPE" CHOICE SHOULD DRIVE THE REST OF THE ORGANELLE SETTINGS
================================================================================

Status:    done. All six items, including the measure side, which needed
           instruction 76's per-slot types and now has them, and including
           the run journal that item 6 left open.
Requested: 2026-08-10 — "add to instructions to work do a sweep of organelle in measure and mask. the point of this settings class and object class is to generate measurements and capture organelle objects. this is the most diverse set of objects. Best would be if there were the options organelle type with the following categories: Vesicular (vacuoles, autophagosomes, transport vesicles, secretory vesicles, early endosomes, lysosomes), Filamentous (microtubules, F-actin filaments, intermediate filaments), Punctate (peroxisomes, lipid droplets, ribosomes, PML nuclear bodies, vaults), Tubular (smooth endoplasmic reticulum, healthy mitochondrial networks, sorting endosomes, trans-Golgi network), Cisternal (rough endoplasmic reticulum sheets, Golgi stacks, nuclear envelope), Reticular (endoplasmic reticulum meshwork, interconnected mitochondrial reticula), Spherical (nucleus, nucleolus, chloroplasts, swollen mitochondria, macromolecular condensates), Toroidal (condensing/stressed mitochondria, midbodies, nuclear pore complexes), Crescent (phagophores/early autophagosomes, specialized yeast nucleoli) and then set up settings and detection modals for each type with the others not visible or grayed out that do not apply to the chosen organelle. as it is now there are too many options for organelle and to many settings for organelle in mask, so if organelle type is Vesicular then organelle morphology will always be spots (for example make sure this is true dont just take my word for it) and not network. so essentially i want to bundle a bunch of current (and potentially new settings into the categories specified above to make it easier for the user. all settings should still be modifiable by the user but they should be in an organelle advanced settings category, and only the minimum relevant settings in the organelle category."

--------------------------------------------------------------------------------
WHAT THE STATE IS
--------------------------------------------------------------------------------

The maintainer asked to verify rather than assume, so: **Vesicular does NOT
map to `spots`.** By the project's own tooltip at `settings.py:3009`, today's
`organelle_morphology` has four values and the request's Vesicular list is
split across three of them:

    'spots'      punctate (vesicles, lipid droplets)
    'network'    filamentous (mitochondria, ER tubules)
    'irregular'  solid blobby (Golgi, LYSOSOMES)
    'ring'       hollow (ENDOSOMES, AUTOPHAGOSOMES)

Vesicular as listed = vacuoles, autophagosomes, transport vesicles,
secretory vesicles, early endosomes, lysosomes. Of those, autophagosomes and
early endosomes are documented as `ring`, lysosomes as `irregular`, and only
the transport/secretory vesicles as `spots`. A vacuole is hollow and large,
so `ring` as well.

THIS IS THE WHOLE DESIGN PROBLEM IN MINIATURE. "Vesicular" is a CELL-BIOLOGY
category (membrane-bound compartment that carries cargo) while `spots` /
`ring` / `network` / `irregular` is an IMAGE-APPEARANCE category (what the
segmentation algorithm has to find). They do not nest, because the same
biological family looks different at different sizes: a 200 nm transport
vesicle is a diffraction-limited dot, a 2 um vacuole is a visible ring.

So the mapping is not type -> morphology. It is

    (organelle type, expected size relative to the PSF) -> morphology

and the size half is already partly present as `organelle_diameter`. Any
implementation that hard-codes one morphology per type will be wrong for
half the entries in the request's own list, and the failure is silent: the
user picks "Vesicular", gets a spot detector, and their lysosomes come out
as rings of holes.

--------------------------------------------------------------------------------
WHY IT MATTERS
--------------------------------------------------------------------------------

The complaint underneath it is correct and measurable: organelle is the most
over-configured object class in the tool. A user who knows they are imaging
lysosomes should not have to know what a ridge filter is, or which of
`organelle_method`'s seven values are legal for which morphology, or what
`organelle_network_threshold` does when they are not segmenting a network.

The fix is a NAMED PRESET that sets several settings at once and explains
what it set -- not a new hard-coded pipeline. That keeps the request's real
goal (fewer decisions in front of a biologist) without pretending a taxonomy
is a segmentation strategy.

--------------------------------------------------------------------------------
WHAT TO DO
--------------------------------------------------------------------------------

1. INVENTORY FIRST. List every `organelle_*` setting in `settings.py` and
   every place `mask` and `measure` read one. Record how many there are --
   the claim "too many options" should have a number attached before and
   after. Note which are legal in combination: `organelle_method` is already
   validated against `organelle_morphology` and an unsupported pair raises
   before any image loads, so that constraint graph partly exists and should
   be read rather than re-invented.

2. WORK OUT THE REAL MAPPING, type x size -> morphology + method + the
   handful of parameters that matter. Do it as a table in this file, with
   one row per entry in the maintainer's nine categories, and mark the rows
   where size decides. Expect to find that several categories map to more
   than one morphology; say so rather than forcing them.
   Where the evidence is thin, say that too -- "Crescent" and "Toroidal"
   have no dedicated detector today and may reduce to `ring` plus a shape
   filter, which is an honest answer.

3. ADD `organelle_type` as the ONE visible choice, defaulting to whatever
   reproduces today's behaviour so no existing settings CSV changes meaning.
   It must state its default in its tooltip like every other setting
   (instruction 61 closed that at zero missing).

4. PRESET, DO NOT OVERRIDE. Choosing a type fills the advanced settings with
   its recommended values and leaves them editable. A user who then changes
   `organelle_method` keeps that change; the type does not silently reassert
   itself. The GUI should show which values came from the preset -- greying
   out an inapplicable setting is fine, HIDING it is not, because a hidden
   setting that is still in the settings dict is how a run gets a value
   nobody can see (this project already has eleven phantom settings from
   exactly that class of drift -- see instruction 61).

5. SPLIT THE CATEGORY. "Organelle" keeps the minimum: channel, type,
   diameter, the on/off switches a biologist recognises. "Organelle advanced"
   takes the rest. This is the same move instruction 73 makes for the other
   object classes, so do them together or make 73 follow this one's shape.

6. MEASURE SIDE. The request says this is a sweep of organelle "in measure
   and mask". On the measure side the question is which measurements make
   sense per type -- neighbour counts and per-object spacing are meaningful
   for Punctate and Vesicular and meaningless for a single Reticular network
   filling the cell. Instruction 71 item 3 defers to this file for that gate.

--------------------------------------------------------------------------------
HOW TO KNOW IT WORKED
--------------------------------------------------------------------------------

A biologist can pick "lysosomes" from a list and get a working segmentation
without opening advanced settings; the count of settings visible by default
under Organelle has gone down and the number is recorded here; every advanced
setting is still reachable and still editable; and no existing settings CSV
changes meaning on load.

--------------------------------------------------------------------------------
DELIBERATELY NOT DONE
--------------------------------------------------------------------------------

Not started. Related: 73 (the same split for the other object classes, and
the sub-section structure this should adopt), 71 (which measurements to
enable per type), 61 (every setting states its default), 37 (the classify
settings overhaul is the same shape of problem and its scope note is worth
reading first).

--------------------------------------------------------------------------------
2026-08-12 - DONE. THE NUMBER IS 53 -> 6.
--------------------------------------------------------------------------------

(1) INVENTORY, with the number attached as this instruction required.
FIFTY-THREE `organelle_*` settings reached the mask pipeline. After the split:

    Organelle             6 settings   (organelle_type, diameter,
                                        mask_within_cells, min_size,
                                        max_size, remove_border)
    Organelle advanced   48 settings

Total 54, because `organelle_type` is new. Nothing was removed.

The constraint graph already existed and was READ rather than re-invented:
`spacr.object._validate_organelle_settings` maps each morphology to its legal
methods and raises before any image loads.

(2) THE REAL MAPPING. It is (type, size) -> morphology, exactly as this file
predicted, and two of the nine rows split on size:

    type          small (< 15 px)     large (>= 15 px)    method
    ----------    ----------------    ----------------    ----------
    punctate      spots               spots               log
    vesicular     spots               RING                log
    spherical     spots               IRREGULAR           otsu
    filamentous   network             network             ridge
    tubular       network             network             ridge
    reticular     network             network             hysteresis
    cisternal     irregular           irregular           adaptive
    toroidal      ring                ring                dog
    crescent      ring                ring                dog
    custom        -                   -                   -

`RING_RESOLVABLE_PX = 15` is the split point, named and documented rather
than buried in a branch: at ~100 nm/px it is ~1.5 um, an object whose
membrane and lumen are several pixels apart. Below it the PSF fills the
middle and a hollow vesicle is indistinguishable from a solid one.

WHERE THE EVIDENCE IS THIN, THE PRESET SAYS SO, as instructed:
  * toroidal  -- "THERE IS NO TOROID DETECTOR." ring + shape filter; NPCs are
                 below the diffraction limit and belong under punctate.
  * crescent  -- "THERE IS NO CRESCENT DETECTOR." ring with the prominence
                 lowered so an open arc registers and a convex fill so the
                 unclosed side does not leak.
  * cisternal -- no sheet detector; a cisterna in-plane is 'irregular' and
                 edge-on looks filamentous.
  * vesicular -- LYSOSOMES are on the maintainer's own Vesicular list and
                 image as solid blobby. The caveat names them.

Proved rather than asserted: every preset, at eight diameters from 1 to 1000
px, is fed to the REAL validator. None is rejected.

(3) `organelle_type` ADDED, defaulting to 'custom', which recommends nothing.
A settings file written before this existed still yields spots/otsu. Its
tooltip states its default (instruction 61).

(4) PRESET, NOT OVERRIDE. Three tiers, and the ORDER is the design:

        what the USER set     >   what the PRESET advises   >   the DEFAULT

The preset runs FIRST, against the caller's own dict, where the only keys
present are the ones they chose. Running it after the defaults was the first
attempt and was wrong: `organelle_method` was already 'otsu', the preset saw
a set key, kept it, and naming a type did nothing at all.

`verbose` prints what it set, what it KEPT because the user had set it, and
the caveat.

(5) CATEGORY SPLIT, in the house pattern -- both names in the `categories`
literal, membership derived from `organelle_types.BASIC_SETTINGS`, the way
motility's two already work. MOVED, NOT HIDDEN: every advanced setting is
still in the panel and still editable. Both categories are gated on
`organelle_channel`, not just the first.

A BUG FOUND AND FIXED WHILE DOING IT, worth recording because it was
silent: `apply_preset` returns a NEW dict, and every caller does
`_set_organelle_defaults(settings)` without taking the return value. Rebinding
the local name threw forty defaults onto a copy -- the mask panel's 53
organelle settings became 13, with no error. Caught by the category tests,
now pinned by `test_the_defaults_helper_still_fills_in_place`.

Five tests that encoded "there is exactly one Organelle heading" were
REWRITTEN, not deleted, and the replacement is stricter: no key may appear
under both headings, none may fall out of both, and the basic heading must
stay at or under eight settings -- a split that left thirty under the first
heading would satisfy every other test and none of the request.

(6) MEASURE SIDE: not done here. Which measurements are meaningful per type
(neighbour counts mean something for Punctate and nothing for a single
Reticular network filling the cell) is instruction 71 item 3, and it needs
the per-instance types of instruction 76 to be worth wiring.

STATUS: DONE except item 6, which is tracked under 71/76.

--------------------------------------------------------------------------------
ITEM 6: WHICH MEASUREMENTS MEAN SOMETHING FOR WHICH TYPE
--------------------------------------------------------------------------------

It was deferred because it needed per-instance types. Instruction 76 shipped
four independent slots, each with its own type preset, so it is wireable and
is wired.

THE ANSWER, in `spacr.settings.ORGANELLE_OBJECTS_PER_CELL`. What decides it
is not the measurement, it is how many objects of that kind a cell holds:

    many      punctate, vesicular, crescent
    one       filamentous, tubular, reticular, cisternal
    depends   spherical, toroidal
    unknown   custom

`spatial_measurements` counts an object's neighbours and measures how far
apart they are; the within-type half of `object_distances` measures the same
thing pairwise. Both answer "how many, and how clustered", which is the
phenotype for a punctate organelle and is not a question at all for a
reticular one: an ER meshwork is ONE connected object filling the cell, so
its neighbour count is zero, its nearest-neighbour distance is undefined,
and a screen regressing on either would be regressing on whether the
segmentation happened to break the network in two that day.

TWO ROWS SPLIT, and they are named rather than forced -- the same finding as
the type-to-morphology table, where two of nine rows split on size.
"Spherical" holds the nucleus (one per cell) and macromolecular condensates
(dozens); "Toroidal" holds midbodies (one per dividing cell) and nuclear
pores (thousands). The structure decides, not the category, and the caveat
says so.

NOTHING IS SWITCHED OFF. A family the type makes doubtful is still measured
and still written. A number that vanished without being asked to is worse
than one that comes with a caveat, and this project already carries eleven
phantom settings from values nobody could see. What the type buys is that
the run SAYS SO, in the same voice the preset already uses to say what it
set and what is weak about it.

MEASURE NOW OFFERS THE TYPE. It could not answer the question without being
told what it is measuring, and the mask module's copy never reaches a
measure run -- the same gap that silenced `summarize_organelles_by` and with
it every organelle table. Each slot's type defaults to 'custom', which makes
no claim, so a settings file written before this still means exactly what it
meant. It sits beside the mask dimensions in the Measure panel, because the
two answer one question: which plane, and what is on it.

AND THE RUN JOURNAL CARRIES IT NOW, NOT ONLY THE CONSOLE.
`explain_organelle_measurements` printed the sentence and stopped there, so a
measurements.db opened a week later held the count-dependent columns with
nothing beside them saying what they mean for a one-object-per-cell
organelle -- and a batch is only ever read back, never watched.

`measure._record_organelle_caveats(settings, run)` is called inside
`measure_crop`, beside the `RunLedger` the tables about to be written are
accounted on. It asks
`spacr.settings.organelle_measurement_caveats(settings)` and puts each caveat
on `run.log`, so it is stamped with the same run id as the database and comes
back from `spacr.runctx.read_run_log(run_id)` beside it. Once per source
folder, in the parent -- not per field in a worker, which would repeat one
sentence a thousand times in processes whose records are not joined to the
run.

NOTHING IS SWITCHED OFF, which is this instruction's own rule: the doubtful
family is still measured and still written, and it is still silent when there
is nothing to say. What changed is that the sentence outlives the terminal.

STATUS: DONE, item 6 included.

--------------------------------------------------------------------------------
ITEM 5 FOLDED INTO 73
--------------------------------------------------------------------------------

"Organelle" and "Organelle advanced" are still the split this instruction
shipped, and the shared families are now one structure with the other object
classes rather than a second parallel scheme: an organelle's size filters
sit under "Advanced settings / Object filtration / Organelle 1", beside the
same decision taken for cells and nuclei.
