================================================================================
MODULES THAT OVERLAP OR ARE REDUNDANT, MEASURED 2026-08-17
================================================================================

Status:    filed 2026-08-17. ANALYSIS ONLY -- no code written, on purpose.
Requested: 2026-08-17 --

           "take a look through all the modules in spacr. some of them overlap
            substantially and some might be redundant at this stage after
            working extensively on some modules. take a look and make
            recommendations on modules that should be removed and merged."

Why no code: which modules exist is a product decision, and three of the
findings below are load-bearing for published figures. Same rule instruction
06 set for the GUI apps -- 06 measured the APPS, this measures the LIBRARY.

--------------------------------------------------------------------------------
METHOD
--------------------------------------------------------------------------------

154 top-level modules under spacr/ (381 files including qt/ and the nine
i18n catalogs; 447,200 lines total). For each: line count, public symbol
count, and every in-repo import of it -- counting relative imports at any
depth, which a first pass missed and which made twenty widgets' worth of
imports invisible.

Then, for each suspected pair, the two public surfaces side by side and --
where the pair makes a CLAIM about data -- the two implementations run on
the same input to see whether they agree.

--------------------------------------------------------------------------------
1. SEVEN VOLCANO RENDERERS. THE STRONGEST FINDING.
--------------------------------------------------------------------------------

    spacr/toxo.py:11                      custom_volcano_plot
    spacr/plot.py:6191                    volcano_plot
    spacr/volcano_style.py:339            render_volcano          (613 LOC)
    spacr/figures/panels.py:207           volcano                 (the house style)
    spacr/qt/widgets/fast_plots.py:938    VolcanoPlot             (interactive)
    spacr/guide_permutation.py:439        plot_guide_permutation_volcano
    spacr/regression_qc.py:3021           _panel_volcano_reference

Seven implementations of one picture. This is not a tidiness complaint: it
is exactly why "hide my old volcano behind a boolean" was reported as still
broken on 2026-08-17 after being fixed on 2026-08-16 -- the fix gated one of
three call sites because nobody had counted them.

    RECOMMEND: two survive. `figures/panels.volcano` for anything saved, and
    `fast_plots.VolcanoPlot` for the interactive one -- they are the two the
    maintainer asked for and the two under the house style.

    `volcano_style.py` (613 LOC, `VolcanoStyle`/`render_volcano`/
    `point_details`) is imported by NOTHING in spacr/ and referenced by 4
    tests. It is a whole styling system for a renderer that lost. Strongest
    single deletion candidate in the package.

    `toxo.custom_volcano_plot` cannot simply be deleted: it also RETURNS the
    hit list the GT1 phenotype plot and ME49 heatmap are built from. It now
    takes `draw=False` for exactly that reason. Retiring it means moving the
    hit rule out first.

    `guide_permutation` and `regression_qc` draw their own for a reason --
    a permutation null and a QC reference are not the screen's volcano.
    Leave them; they should CALL the panel, not reimplement it.

--------------------------------------------------------------------------------
2. TWO "WHICH TEST APPLIES" ENGINES, AND THEY DISAGREE
--------------------------------------------------------------------------------

`spacr/sp_stats.py` (314 LOC) and `spacr/figures/stats.py` (474 LOC) both
choose a statistical test from the data. Run on the same inputs:

    case                        sp_stats        figures.stats     same family?
    normal, equal var, n=30     T-test          Student's t       yes
    normal, UNEQUAL var, n=30   T-test          Welch's t         yes (*)
    skewed (exponential) n=30   T-test          Mann-Whitney U    NO
    n=3 vs 24  (THE REAL CASE)  T-test          Mann-Whitney U    NO
    n=5 vs 5                    T-test          Mann-Whitney U    NO

THREE OF FIVE DISAGREE, and always in the same direction: `sp_stats` takes
the parametric test where the assumption checks have no power to refuse it.
That is precisely the trap instruction 124 K names -- "choosing Student's
because Levene did not reject on n = 3 is how a screen reports a difference
that is not there" -- and this screen HAS three positive controls.

(*) it reports "T-test" on unequal variance too, so whether it is using
Welch at all needs checking; the label does not say.

    RECOMMEND: `figures/stats.py` is the survivor -- it is the one that
    treats an underpowered check as FAILED rather than as passed, and the one
    whose output the Statistics tab and the exported stats sidecar already
    use. `sp_stats` should become a thin adapter over it or go.

    THIS IS A CORRECTNESS ITEM, NOT A CLEANUP. Two engines that disagree
    about which test applies will eventually put two different p-values for
    one comparison into one paper.

--------------------------------------------------------------------------------
3. TWO FIGURE-STYLE SYSTEMS
--------------------------------------------------------------------------------

    spacr/figure_style.py    226 LOC   apply / rc_params / resolve
    spacr/figures/style.py   322 LOC   figure_style / Palette / ROLES / rc / ...

No shared names, same job: deciding how a figure looks. They answer to
DIFFERENT masters, which is the honest complication -- `figure_style` is the
user's preference (general + per-graph, instruction 118), `figures/style` is
the publication house style (the apicomplexan-figures skill). Both are
legitimate; having them unaware of each other is not.

`figure_style` is imported by exactly one place in spacr/
(`qt/preferences.py:346`) and 11 tests.

    RECOMMEND: keep both concepts, merge the CODE -- the house style is the
    base, the user preference is an override layer on top of it. Today a user
    preference cannot reach a house-style panel at all, which is the bug this
    overlap is hiding.

--------------------------------------------------------------------------------
4. TWO REGRESSION QC MODULES
--------------------------------------------------------------------------------

    spacr/regression_qc.py           3444 LOC, 19 public
    spacr/regression_diagnostics.py   722 LOC,  8 public

Only one shared name (`variance_inflation_factors`, computed in both). The
split is real -- `regression_qc` is the panel suite with a context object,
`regression_diagnostics` is three plots -- but the boundary is not written
down anywhere, and a reader cannot tell which to add a panel to.

    RECOMMEND: lower priority than 1-3. Either merge, or write the boundary
    into both headers. The duplicated VIF is a small correctness risk of the
    same shape as finding 2.

--------------------------------------------------------------------------------
5. A DEPRECATION SHIM NOTHING USES
--------------------------------------------------------------------------------

`spacr/regression_search.py` -- 25 lines, re-exports `parameter_sweep` and
raises a DeprecationWarning. ZERO references in spacr/ AND zero in tests.

    RECOMMEND: check whether any RELEASED version advertised the name. If it
    did, keep one more cycle; if the rename never shipped, delete it. It
    currently costs a DeprecationWarning in the test output and nothing else.

--------------------------------------------------------------------------------
CHECKED AND NOT REDUNDANT -- recorded so nobody re-opens them
--------------------------------------------------------------------------------

    sim / power_simulate          a classifier simulator and a power
                                  calculator. No shared names, no shared job.
    model_check / model_compare   "can this model load" vs "do two models
                                  agree". Different questions.
    profiler / classifier_evaluation
                                  sensitivity profiling vs leakage auditing.

--------------------------------------------------------------------------------
HOW TO KNOW IT WORKED
--------------------------------------------------------------------------------

* One volcano renderer for saved figures, one for the interactive plot, and
  a test that counts them so an eighth cannot appear.
* One engine decides which statistical test applies, and the table above
  agrees down every row.
* A user's figure preference reaches a house-style panel.
* `spacr.figures` and the modules it replaced do not both ship the same
  picture.


--------------------------------------------------------------------------------
2026-08-18 -- THE COUNT IS A TEST NOW, AND TWO OF THE FINDINGS ABOVE ARE WRONG
--------------------------------------------------------------------------------

RE-MEASURED BEFORE ANYTHING WAS TOUCHED, and the audit above does not survive
contact with the current tree on two points. Both are recorded here rather
than corrected in place, because the recommendations were acted on elsewhere.

  1. THE SEVEN VOLCANO RENDERERS ARE SIX. `regression_qc._panel_volcano_
     reference` draws no volcano: it is `set_axis_off` and three lines of text
     naming the file the real one was written to. Counting a signpost as a
     renderer made the duplication look worse than it is.

  2. `volcano_style.render_volcano` IS NOT UNIMPORTED, so the "strongest
     single deletion candidate in the package" would have deleted a live
     screen. `spacr/qt/widgets/volcano_explorer.py:56` imports
     `VolcanoStyle`, `render_volcano` and `point_details` from it, and
     `qt/screens/volcano.py:49` names the function in its own help text. The
     original claim was true of an older tree; it is not true of this one.

WHAT SHIPPED

  * `tests/test_one_volcano_for_each_job.py` -- the counting test the
     acceptance asks for. It walks spacr/ with `ast`, finds every def/class
     whose name says "volcano", and compares the set against an inventory
     that says WHAT EACH ONE IS: six renderers, six things that host, name or
     describe one. An eighth is not forbidden; an UNRECORDED eighth is, and
     recording one costs a sentence about which job it does -- the step that
     was skipped the last three times. The two survivors (`figures.panels.
     volcano` for saved, `fast_plots.VolcanoPlot` for interactive) are pinned
     by name, so a third survivor fails.

     It also pins the two corrections above as assertions rather than prose:
     the signpost fails the test if it ever grows a `scatter`, and the
     explorer's import of `render_volcano` fails it if that module is deleted
     on the stale evidence.

     And it checks the saved renderer's HIT RULE off the picture -- counting
     the marks drawn in the up/down role colours, not reading the caption --
     against `adjusted p <= alpha`. That is the failure this finding is really
     about: two renderers that disagree is how a figure in a paper stops
     matching the figure on screen.

  * FINDING 3 IS FIXED, not only recommended. "Today a user preference cannot
    reach a house-style panel at all, which is the bug this overlap is
    hiding" -- it can now. `spacr/figures/style.py` gains `user_overrides()`,
    and `rc()` applies it LAST, so the house style is the base and the user's
    preference is the override layer, which is the order the finding asks for.

    IT RETURNS A DIFF, NOT THE USER'S RESOLVED STYLE, and that is the whole
    design. `figure_style.GENERAL_DEFAULTS` is a complete style of its own --
    11 pt DejaVu Sans, gridlines ON, a white ground -- so laying the resolved
    style over the house style would replace the published look for every user
    who has never opened Preferences. Only the keys they actually moved are
    applied.

    `figures.sheet.build_panel` passes `kind=key`, so the PER-GRAPH layer
    reaches the panel it belongs to and no other -- "changing the volcano's
    point size must not touch the heatmaps" is asserted.

    ONE DELIBERATE EXCLUSION, asserted so it is not read as a gap: the palette
    preference does NOT reach a house-style panel. The house style fixes the
    colour vocabulary -- grey for everything the sentence is not about, one
    highlight for what it is -- and a seaborn categorical cycle laid over that
    is the rainbow the style exists to forbid. It holds by construction
    (`rc_params` carries no palette key); the test makes the choice visible.

    MEASURED on this machine, where the store really holds
    `{'font_size': 16.0, 'palette': 'deep'}` and `{'volcano': {'marker_size':
    40.0}}`: the font size now reaches every house-style panel, and it did not
    before.

WHAT REMAINS, AND NOTHING WAS DELETED

Per the standing rule, no module was removed. These are for the maintainer:

  * `plot.volcano_plot` -> `figures.panels.volcano`. The consolidation the
    acceptance names for SAVED figures. Not done: `volcano_plot` is a
    general-purpose reader (CSV/TSV/Excel, x/y transforms, its own threshold
    vocabulary) and `panels.volcano` takes an axes and a tidy frame, so this
    is an API change with a real blast radius on `ml.py:3449` and its tests,
    not a rename.
  * `toxo.custom_volcano_plot`. Still cannot go: it RETURNS the hit list the
    GT1 phenotype plot and the ME49 heatmap are built from. Moving the hit
    rule out is the first step and is worth doing on its own.
  * `volcano_style.py` -- KEEP, on the corrected evidence above.
  * `sp_stats` vs `figures/stats` (finding 2) -- untouched. It is the
    correctness item of the five and it was outside this session's territory.
  * `regression_search.py` (finding 5) and the duplicated VIF (finding 4) --
    untouched.


--------------------------------------------------------------------------------
2026-08-18 (evening) -- FINDING 2 WAS ALREADY DONE, 4 AND 5 ARE DONE NOW
--------------------------------------------------------------------------------

FINDING 2 IS CLOSED AND THE NOTE ABOVE IS WRONG ABOUT IT. "sp_stats vs
figures/stats (finding 2) -- untouched. It is the correctness item of the five
and it was outside this session's territory" was written on 2026-08-18; the
work had landed the previous afternoon in commit ``abca1d06``, "One engine
decides which statistical test applies". The header lied and so did the
trailing note; the code was right. This is the eighth entry for HANDOFF 0a.

RE-MEASURED THIS EVENING before believing either of them, same five rows, the
same arrays through both entry points:

    case                        sp_stats             p          engine  same p
    normal, equal var, n=30     T-test               0.0028882  Student's t  y
    normal, UNEQUAL var, n=30   Welch's T-test       0.394734   Welch's t    y
    skewed (exponential) n=30   Mann-Whitney U test  0.00442719 M-W U        y
    n=3 vs 24                   Mann-Whitney U test  0.244786   M-W U        y
    n=5 vs 5                    Mann-Whitney U test  0.222222   M-W U        y

Bit-identical on all five, and the old unconditional t-test would have said
0.0028882 / 0.391727 / 0.00268354 / 0.200121 / 0.232468 -- so on the rows that
moved it was reporting a p-value between 1.2x and 1.7x smaller than the test
the data supports. `tests/test_one_engine_decides_which_test_applies.py` pins
all five. 104 tests green across the four stats-engine files.

ONE ENGINE IS NOT YET *ALL* THE ENGINES, AND THIS IS NOT IN MY TERRITORY.
`spacr/utils.py:8940` has a THIRD `perform_statistical_tests` -- same name as
sp_stats', different module -- choosing ANOVA against Kruskal-Wallis from
`utils.check_normality`, which is D'Agostino-Pearson at alpha 0.05 with no
power floor. Its own docstring already records the trap ("Small samples
therefore look normal for want of power") and it still takes the parametric
branch: under 8 observations scipy returns NaN, `NaN < alpha` is False, and the
feature is reported as normal. That is finding 2's mechanism exactly, one
module over, deciding which test a cluster-feature table reports. HANDOFF.

--------------------------------------------------------------------------------
FINDING 4 -- THE DUPLICATED VIF. CONSOLIDATED.
--------------------------------------------------------------------------------

MEASURED FIRST, because "a small correctness risk of the same shape as finding
2" is a claim about numbers. Both implementations, same designs, 200x20 /
400x60 / 96x30 each with a near-collinear pair:

    largest relative difference   2.3e-10
    exact collinearity            both name g0, g2, g9 as inf
    cost at 60 guides             130.8 ms (aux regressions) vs 13.0 ms (corr)

SO IT IS NOT THE SAME SHAPE AS FINDING 2, and the difference is worth keeping:
the two statistical engines DISAGREED, these two AGREED. That is precisely why
this one was worth removing rather than arbitrating -- two routes to one number
that agree today are two routes that can stop agreeing, and nothing in the
package was comparing them.

`regression_qc.variance_inflation_factors` survives, chosen on cost since it
could not be chosen on correctness: ``VIF_j = (R^-1)_jj`` is one O(p^3)
decomposition where the auxiliary-regression form is O(p^4). 7.0 ms after the
change on the 400x60 case. `regression_diagnostics.variance_inflation_factors`
keeps its signature and everything that is CONTRACT rather than statistic --
the refusal on a rank-deficient design (the engine answers inf for every guide,
which is true and useless), the widest-support-first report cap, the
`wells_with_guide` column -- and calls the engine for the numbers.

`tests/test_one_vif_not_two.py`, 11 cases, including the inventory count in the
shape `test_one_volcano_for_each_job.py` established: a third implementation is
not forbidden, an UNRECORDED third one is.

--------------------------------------------------------------------------------
FINDING 5 -- THE SHIM. THE QUESTION IS ANSWERED; THE DELETION IS NOT MINE.
--------------------------------------------------------------------------------

"check whether any RELEASED version advertised the name" -- NO.

    for tag in v1.3.5 v1.3.6 v1.4.9.8 v1.4.9.9 v1.5.0.1 v1.5.0.4; do
        git cat-file -e $tag:spacr/regression_search.py    # absent, all six
    done

And none of the six carries `spacr/parameter_sweep.py` either, so BOTH names
arrived after the last release: no installed spaCR has ever exposed either one
and there is no downstream import to keep working. By the finding's own rule
that is "delete it".

NOT DELETED. Which modules exist is the maintainer's decision (the rule
instruction 06 set for the GUI apps, and this brief repeats it). The evidence
is written into the module docstring so the decision costs a sentence rather
than the check again, and `tests/test_the_shim_that_never_shipped.py` pins the
five aliases and the star-import against `parameter_sweep.__all__` so it cannot
rot while it waits -- an alias that has quietly stopped resolving is worse than
the deprecation it was written to soften. That test also fails the moment
anything in `spacr/` imports the old name, because that would change the
answer.

REMOVAL CANDIDATES, FOR THE MAINTAINER, all flagged and none acted on:
  * `spacr/regression_search.py` -- clean delete, zero external obligation.
  * `plot.volcano_plot` -> `figures.panels.volcano` (unchanged from the note
    above: an API change with a real blast radius, not a rename).
  * `toxo.custom_volcano_plot` -- still returns the hit list; move the hit
    rule out first.
  * `volcano_style.py` -- KEEP. The corrected evidence above stands.

THE COUNTING TEST THE ACCEPTANCE ASKS FOR ALREADY EXISTS and is green:
`tests/test_one_volcano_for_each_job.py`. Nothing was added to it; the two
survivors are still pinned by name and the inventory still has twelve entries.

--------------------------------------------------------------------------------
DONE 2026-08-19 -- every acceptance line is met; the remainder is YOUR decision
--------------------------------------------------------------------------------

  * ONE RENDERER PER JOB, WITH A TEST THAT COUNTS THEM.
    `tests/test_one_volcano_for_each_job.py`, green, thirteen entries and two
    named survivors. It EARNED ITS KEEP on 2026-08-19: instruction 115 added
    `regression_qc._score_volcano_reference` and the inventory went red the
    same day. It is a scorer that draws nothing, and it was REGISTERED rather
    than excluded by a `_score_*` name rule -- a rule like that would have made
    the red go away and hidden the next real renderer that happened to be
    called one. A ratchet that can be widened by the thing it catches is not a
    ratchet.
  * ONE ENGINE CHOOSES THE STATISTICAL TEST. Finding 2, the correctness item:
    two engines disagreed on 3 of 5 cases, always with sp_stats taking the
    parametric branch. Closed earlier in this run.
  * A FIGURE PREFERENCE REACHES A HOUSE-STYLE PANEL. `figures/style.py` lays
    the user's own preferences over the house style and reads the screen/print
    mode from them.
  * `spacr.figures` AND WHAT IT REPLACED DO NOT BOTH SHIP THE SAME PICTURE --
    pinned by the inventory above.

WHAT IS LEFT IS NOT WORK, IT IS A DECISION, and this file said so from the
start: "which modules exist is a product decision, and three of the findings
are load-bearing for published figures." The candidates are unchanged and
nothing has been deleted:

  * `spacr/regression_search.py` -- clean delete. Nothing in `spacr/` imports
    it except the lazy-loader list in `__init__.py`.
  * `plot.volcano_plot` -> `figures.panels.volcano` -- an API change with a
    real blast radius, not a rename.
  * `toxo.custom_volcano_plot` -- still returns the hit list the GT1 phenotype
    plot and the ME49 heatmap are built from; move the hit rule out first.
  * `volcano_style.py` -- KEEP, on the corrected evidence above.

Say the word on any of them and it is a small change. Left standing is the
right default: a module deleted from under a published figure cannot be
un-deleted from a reader's paper.
