================================================================================
EVERY SETTING AUDITED: IS IT NEEDED, IS IT A DUPLICATE, IS ITS NAME HONEST
================================================================================

Status:    MASK AND MEASURE AUDITED AND THE DECISIONS IMPLEMENTED 2026-09-02.
           The maintainer approved retiring the _size pair, all four renames,
           and a per-object TABLE for the repeated settings. The table is the
           only part not yet built. See instructions/data/364_mask_and_measure_settings_
           audit_2026-09-02.txt for the proposals. Nothing changed yet.
Requested: 2026-09-02, verbatim -- "look through each moduals settings and
           evaluate if any settings are unnecessary, redundant or are not
           intuitive. then propose to remove settings that are unnecessary,
           combine redundant settings, and change the names of non intuitive
           settings. this should be done for all modules, starting with core
           moduals, and all of their settings including any settings in live
           previews, hyperparameter searches or other sub modules. start with
           the mask and measure moduals. after you are done with the moduals
           go on to the preferences in spaCR and spaceout."
Owner:     spacr/settings.py, spacr/validate.py (RETIRED_SETTINGS),
           spacr/qt/screens/preferences.py, spacr/qt/spaceout.py.

PROPOSE, DO NOT DELETE. The maintainer asked to be shown proposals. Each
phase produces a TABLE for review, and nothing is removed or renamed until
that table comes back approved. A setting removed by surprise is a settings
file in the wild that stops loading.

--------------------------------------------------------------------------------
THE SIZE OF IT, MEASURED 2026-09-02
--------------------------------------------------------------------------------

  get_timelapse_settings (mask, the superset)     203
  get_perform_regression_default_settings          87
  get_measure_crop_settings                        65
  get_automated_motility_assay_default_settings    55
  get_train_test_model_settings                    50
  get_analyze_recruitment_default_settings         30
  get_default_generate_activation_map_settings     27
  get_identify_masks_finetune_default_settings     23
  get_analyze_plaque_settings                      22
  get_check_cellpose_models_default_settings       18
  get_train_cellpose_default_settings              16
  get_plot_data_from_csv_default_settings          13
  get_default_apply_cellpose_model_settings        10
  get_default_test_cellpose_model_settings          9
  get_map_barcodes_default_settings                 5
  get_analyze_reads_default_settings                3

Mask alone asks 203 questions. That is the number to hold in mind: a user
who has to answer, or decide not to answer, 203 questions before segmenting
anything has been handed the maintainer's problem instead of a tool.

--------------------------------------------------------------------------------
WHERE THE REDUNDANCY ACTUALLY IS
--------------------------------------------------------------------------------

78 of mask's 203 settings are the SAME 20 QUESTIONS asked once per object
type -- `cell_`, `nucleus_`, `pathogen_`, `organelle_`, `cytoplasm_`:

  CP_prob x4        FT x4              Signal_to_noise x3   area_multiplier x4
  background x3     channel x4         diameter x4          intensity_merge x4
  ... and 12 more shapes

This is the single biggest finding and it is NOT simply redundancy to
delete: a user genuinely may want a different diameter for a nucleus than
for a cell. But 20 shapes x N objects is a presentation problem being
solved by multiplying the vocabulary, and instruction 326 has already
established that the object count must be ARBITRARY -- so this multiplies
without bound as soon as 326 lands. The two items meet here.

  THE LIKELY SHAPE: one question per shape, answered per object in a small
  table or per-object tab, rather than N flat settings per shape. That is a
  presentation change with a settings-file migration behind it, not a
  rename. Propose it as such.

--------------------------------------------------------------------------------
WHAT THE FIRST AUDIT FOUND (mask + measure, 2026-09-02)
--------------------------------------------------------------------------------

NOTHING IS DEAD. All 268 settings across the two modules are read somewhere.
An audit hunting for removable settings would have reported "nothing to do"
and missed every real problem. The axes that matter are REDUNDANCY and
NAMING.

THE ONE THAT IS A BUG, NOT UNTIDINESS: organelle carries BOTH
`_min_size`/`_max_size` and `_min_area`/`_max_area`. They mean the same
thing, and they are read by DIFFERENT code -- the `_size` pair by the batch
organelle mask writer, the `_area` pair by the shared filter the Qt live
preview uses. So tuning the preview and then running the batch applies a
different filter, silently. cell, nucleus and pathogen have only the `_area`
pair; organelle alone answers the question twice.

FOUR RENAMES PROPOSED, all where the tooltip must translate the name before
it can explain the setting: `_FT` -> `_flow_threshold`, `_CP_prob` ->
`_cellprob_threshold`, `_Signal_to_noise` -> `_signal_to_noise`, and
`_min_object_area` -> `_min_split_area` (that last one is not style: it is
the floor below which an object is never SPLIT, and its current name reads
as a variant of `_min_area`, which it is not).

AND A FINDING FROM OUTSIDE THIS ITEM: 59 of the 706 settings in the
generated `setting_api_targets.py` map point at a function that reads no
settings at all -- 30 of them at one guide-permutation CSV writer. That map
is what a setting's "where is this used" API link follows, and instruction
368 proposes building a settings-flow tree on it. It has to be right first.

--------------------------------------------------------------------------------
THE FOUR JUDGEMENTS, AND WHAT EVIDENCE EACH NEEDS
--------------------------------------------------------------------------------

UNNECESSARY -- propose removal. The evidence is not opinion:
  * nothing reads it (grep the package, not just the module);
  * it is read but every caller passes the default;
  * it exposes an implementation detail the user cannot reason about;
  * it duplicates something spaCR can measure or infer.
  A setting that only ever takes its default is a question with one answer.

REDUNDANT -- propose combining. Two settings are redundant when no
combination of their values is meaningful that one setting could not
express. Say which combinations become unreachable, because that is the
cost.

NOT INTUITIVE -- propose a rename. The test is whether the name says what
the user is deciding, in the user's vocabulary. `pathogen_Signal_to_noise`
mixes snake_case with a capitalised term of art; `FT` is an abbreviation of
something; `intensity_merge` does not say what merges with what. Check the
TOOLTIP against the NAME: where the tooltip has to explain what the word
means before it can explain the setting, the word is wrong.

  ONLY 14 TOOLTIPS ARE DEFINED against ~600 settings across all modules.
  Any rename proposal should note where there is no tooltip at all, because
  an unnamed, undocumented setting is the worst case and is invisible in a
  count of "settings that need work".

--------------------------------------------------------------------------------
ORDER OF WORK, AS ASKED
--------------------------------------------------------------------------------

  1. Mask (203)          -- start here; the per-object families are here
  2. Measure (65)
  3. The rest of the core modules: Annotate, Classify, Map Barcodes,
     Regression (87)
  4. Sub-surfaces the maintainer named explicitly and which are easy to
     forget because they are not on the module's main page: LIVE PREVIEW
     settings, HYPERPARAMETER SEARCH settings (train_test_model 50,
     check_cellpose_models 18, identify_masks_finetune 23), and the assay
     sub-modules (motility 55, recruitment 30, plaques 22).
  5. spaCR Preferences
  6. spaceout Preferences

--------------------------------------------------------------------------------
TRAPS
--------------------------------------------------------------------------------

* A REMOVED SETTING GOES IN `RETIRED_SETTINGS` in `spacr/validate.py`, so an
  old settings file loads with a message instead of an error. `corrected_manders`
  is the worked example.
* A RENAMED SETTING IS A REMOVAL PLUS AN ADDITION as far as every saved
  settings file, published tutorial, notebook and reviewed translation
  catalog is concerned. Each rename stales: the notebooks' generated setting
  documentation, `docs/i18n/reviewed/runtime/**` records pinned to the old
  tooltip text, and the tutorial JSON. Budget for that; instruction 337's
  two-word default change staled two reviewed records on its own.
* `settings.categories` MUST list every key, and a key added there without
  being registered in `KEYS_ADDED_BY_REGROUP` fails two tests. This has
  already caught one change this week.
* Do not confuse "unused by the GUI" with "unused". Several settings exist
  for the headless `spacr-run` path and notebooks only.

--------------------------------------------------------------------------------
HOW IT WILL BE CHECKED
--------------------------------------------------------------------------------

* One proposal table per module, reviewed by the maintainer BEFORE any edit:
  setting, judgement, evidence, proposed action.
* After each approved batch: the module's settings count falls, every
  removal is in `RETIRED_SETTINGS`, every rename has a tooltip, and an old
  settings file still loads.
* No setting is left whose tooltip has to define its own name.


--------------------------------------------------------------------------------
DECIDED AND DONE, 2026-09-02
--------------------------------------------------------------------------------

RETIRED, as approved: `organelle_min_size` and `organelle_max_size`. Both are
in `RETIRED_SETTINGS` mapped to their `_area` counterpart, so an old settings
file loads with a message rather than an error. The batch mask writer now
reads the `_area` pair the Qt live preview already used, so tuning the
preview and pressing run finally apply the SAME filter. Mask went 203 -> 201.

  THE DEFAULT CARRIED OVER, NOT RESET. `organelle_min_area` is now 10 -- the
  value the batch run actually used through the retired `_size` setting --
  rather than the 0 the preview had. Real runs are unchanged; it is the
  preview that stops disagreeing with them.

  AND A SECOND COPY WAS FOUND WHILE DOING IT. The mask factory hand-wrote
  `organelle_min_area`/`organelle_max_area` as 0 beside
  `_set_organelle_defaults`, which owns them per organelle type and sets 10.
  That shadowing is what made the preview read 0 while the batch read 10, so
  it is the other half of the same bug and is removed.

RENAMED, all four approved: 90 settings across 26 organelle slots.

    <object>_FT               -> <object>_flow_threshold
    <object>_CP_prob          -> <object>_cellprob_threshold
    <object>_Signal_to_noise  -> <object>_signal_to_noise
    <object>_min_object_area  -> <object>_min_split_area

  Done as a SUFFIX substitution rather than 90 literal renames, because the
  organelle slots are generated and a literal list would miss the next one.
  497 occurrences across 60 files, plus 94 more for the retirement.

  THE TOOLTIPS NOW STATE THE REAL PER-APP DEFAULTS. The surviving
  `organelle_min_area` tooltip said "Default 0 (disabled)" while Mask's
  default is 10 -- a claim that was already wrong and would have stayed wrong
  silently. It now names both, which also removed two entries from the
  tooltip-drift ledger: a variant the tooltip itself explains is not drift.

THE PER-OBJECT TABLE'S MODEL IS BUILT: `spacr/object_settings_table.py`.
`to_table(settings)` reads a flat settings dict as {question: {object:
value}} and `from_table` writes it back. Mask's 113 per-object keys become 55
questions.

  AND THERE IS NO SETTINGS-FILE MIGRATION AFTER ALL, which is the important
  part. This file assumed one; there is none, because the table is a
  PRESENTATION and the stored keys do not change. `from_table(to_table(s), s)
  == s` for both Mask and Measure, so no saved settings file, notebook,
  tutorial or headless `spacr-run` has to move. Storing a nested structure
  instead would have been a file-format change -- the presentation was what
  was wrong, so only the presentation changes.

  `widen(table, 'organellec')` is the operation 326 needs: one call, one new
  column, and the number of QUESTIONS does not move. It starts the new
  organelle where the first one is rather than at a global default nobody
  chose. In the flat vocabulary the same act is twenty new settings that
  every consumer, tooltip and translation catalog has to learn.

  TWO THINGS THE MODEL GETS RIGHT THAT A GUI WOULD OTHERWISE GET WRONG.
  Object prefixes are matched LONGEST FIRST, or `organelleb_min_area` files
  under `organelle` and twenty-six slots collapse into one column -- which no
  assertion about totals would catch. And a question an object does not ask
  is ABSENT from its row rather than present as None: cytoplasm is derived,
  so it has no channel and no diameter, and that is a different fact from a
  diameter nobody has chosen yet.

STILL TO BUILD: the GUI that renders it, and the same treatment for the
remaining modules in this file's order.

--------------------------------------------------------------------------------
2026-09-02: THE TABLE IS BUILT -- MODEL AND VIEW
--------------------------------------------------------------------------------

`spacr/object_settings_table.py` was the model and drew nothing; the view is
now `spacr/qt/widgets/object_settings_grid.py`. One row per question, one
column per object, and the button that widens it.

WHAT IT COSTS THE SETTINGS FILE: nothing, and that is the whole design.
`to_table` reads a flat dict and `from_table` writes it back, so the stored
keys are unchanged and no settings file, notebook, tutorial or `spacr-run`
invocation migrates. `test_showing_a_settings_dict_changes_nothing_in_it`
round-trips Mask's own 201 defaults and checks the TYPE of every value as
well as its value.

TWO THINGS THE VIEW HAD TO GET RIGHT, both of which corrupt a settings file
rather than merely look wrong, and both now pinned:

  * A VALUE KEEPS ITS TYPE. A table hands back strings. `cell_background` is
    an int and `organelle_cellprob_threshold` is a float -- the SAME question
    with two types, which is exactly the case a single "cast to the row's
    type" rule gets wrong. Where the cell being replaced is None there is no
    type to copy, so the same question asked about another object is used
    instead: a diameter is a diameter whether it is a cell's or a nucleus's.
  * A QUESTION AN OBJECT DOES NOT ASK STAYS ABSENT. `cytoplasm` is derived --
    cell minus the rest -- so it has no channel, no diameter and no detection
    method. Those cells are not editable, because a value typed there invents
    a settings key nothing reads. They read as blank rather than as `auto`,
    which is a different claim: `auto` means "work it out" and is what an
    unset diameter says.

WHERE IT MEETS 326. Adding an organelle is `widen()` -- one column, seeded
from the first organelle's answers rather than from a global default nobody
chose. The question count does not move. In the flat vocabulary the same
operation was twenty new settings that every consumer, tooltip table and
translation catalog had to learn, which is why 326's ceiling could not be
raised before this existed. The column headers are NUMBERED ("Organelle 2"),
never lettered, per the maintainer on the same day.

The ceiling is refused out loud rather than hit silently: at 26 the button
says the slots are lettered and the alphabet is what runs out.

STILL TO DO: mounting the grid on Mask's own settings form in place of the
78 flat rows. The widget is finished and tested against Mask's real defaults
(`tests/qt/test_the_per_object_settings_are_one_table.py`, 13 tests), and
the mount is NOT a small edit. Read before starting it, because the seam
looks like a two-line change and is not:

  * THE SEAM IS `_split_rows_by_object` (settings_model.py:2219) and its one
    caller at 8312, which turns a family's rows into one sub-section per
    object. Replacing those children with a single grid section is the
    obvious move.
  * WHAT MAKES IT NOT OBVIOUS is what a "row" is by the time the screen has
    it. `AppScreen` reads each row as `(label, widget)` and then: derives the
    setting key from the widget (`_key_of_field`); decides from that key
    whether the run has an object for it and DEFERS the caption if not;
    hands the key list to `SettingsWidgets.remember_section_rows`; lets the
    settings search index the row by key; and `tests/qt/test_all_module_
    smoke.py` reads every entry as a labelled setting row. A QTableView
    holding 78 keys satisfies none of those five contracts.
  * SO THE REAL WORK is teaching the row machinery about a SPANNING
    COMPOSITE row that owns many keys -- what its key list is, how the
    search reaches a cell, what "hidden because the run has no organelle 3"
    means when the slot is a column rather than a row. That is a design
    change to the most-used screen in the application, and it is worth doing
    deliberately rather than in the same afternoon as the widget.
  * THE WIDGETS THEMSELVES SHOULD SURVIVE THE MOUNT. `collect()` reads
    `self._widgets`, and with it every coercion, canonicalisation and the
    organelle-slot rule in `_organelle_slots_worth_keeping`. The cheap and
    safe shape is therefore a grid that EDITS THE EXISTING WIDGETS and a
    flat row that is hidden rather than deleted -- not a grid that owns the
    values and a widget dict that has lost them.

REGRESSION IS AUDITED, 2026-09-02 -- see
instructions/data/364_regression_settings_audit_2026-09-02.txt. 88 settings,
nothing dead (the same scan that found nothing dead in mask and measure's
268), and six findings. The two worth the maintainer's attention first:

  * `Toxoplasma` and `annotation_source` ARE THE SAME REQUEST, and this is
    not an inference -- `spacr/ml.py`'s own reconciler says so in its
    docstring. Propose retiring the checkbox and keeping the field, with
    `Toxoplasma` accepted as a legacy spelling the way `toxo` already is.
    One case must survive: `Toxoplasma=False` with no field means NO
    annotation, and a careless migration would turn that into the default.
  * NINE SETTINGS SAY "CONTROL" AND MEAN FOUR DIFFERENT THINGS -- an
    identifier, a set of wells, non-targeting gRNAs, and an invasion-assay
    stain baseline. GitHub issues 91 and 92 are both this: a user putting
    well names into the identifier settings. `positive_control` also means a
    COLUMN VALUE in the machine-learning screen and a GENE/gRNA ID in gRNA
    regression -- one key, two meanings, chosen by which family runs.

CLASSIFY IS AUDITED TOO, 2026-09-02 -- see
instructions/data/364_classify_settings_audit_2026-09-02.txt. 114 settings,
nothing dead, seven findings. One of them should not wait for a rename batch:

  * `intermedeate_save`'s TOOLTIP IS FALSE. It tells the user "It currently
    has no effect ... those checkpoints are written regardless of this flag",
    and `spacr/io.py:_save_model` plainly branches on it -- `False` gives an
    empty threshold list, the comment beside it says so, `deep_spacr` passes
    the setting through, and four test files exercise `False`. So the control
    works and its own documentation tells everyone it does not. Anybody who
    wanted to stop the extra checkpoints read that and gave up. It is also
    the only misspelled key in the module.

The rest are the familiar shapes: `cross_validation` and
`cross_validation_enabled` are two switches for one question;
`gradient_accumulation` is a boolean that `gradient_accumulation_steps = 1`
already says; `normalize` and `normalization` are two letters apart and mean
different things at different stages; and `min_max` is the plate heatmap's
colour limits wearing the name of a normalisation method.

Annotate and Map Barcodes are the remaining core modules. Then preferences
and spaceout as the request asks.

ANNOTATE AND MAP BARCODES ARE AUDITED, 2026-09-02 -- see
instructions/data/364_annotate_and_map_barcodes_settings_audit_2026-09-02.txt.
That completes the six core modules. Annotate: 23 settings in its defaults
factory, 31 counting the screen's own table, 44 counting the Cells-tab
picture panel. Map Barcodes: 18 declared, 20 that the pipeline actually
reads, plus 5 in a factory nothing calls. Thirteen findings, and this pair
does NOT look like the first four.

  SOMETHING IS DEAD AT LAST, AND IT SAYS SO ITSELF. `barcodes` and `grna`
  are in `get_map_barcodes_default_settings`, which is called by nothing
  under spacr/. Their own tooltips state it -- "Nothing in the current code
  reads this key ... so setting it has no effect" -- and the grep confirms
  it: `settings['grna']` and `settings.get('grna')` appear nowhere. Both are
  still typed, tooltipped, categorised under Paths, and translated into all
  ten catalogs.

  WHICH MEANS THE "NOTHING IS DEAD" SCAN HAS A HOLE. The file-count scan the
  first four audits used says `grna` is named in 66 files and `barcodes` in
  46 -- every one of them `grna_csv`, `barcodes_column.csv` or the pandas
  column `df['grna']`. It is the same collision `test_dead_settings.py`
  already exempts `nc` and `pc` for. The four earlier "nothing is dead"
  results are probably still right -- those modules' keys are long and
  distinctive -- but they are worth re-checking with the stricter test
  (grep for `settings['<key>']`, not for the bare word) before they are
  relied on.

  AND THE ONE THAT COSTS A USER SOMETHING EVERY SESSION: ANNOTATE HAS TWO
  DEFAULT TABLES THAT DISAGREE ON 15 OF THE 21 KEYS THEY SHARE, TEN OF THEM
  SUBSTANTIVELY. `set_annotate_default_settings`
  is what every tooltip documents; the screen does
  `self._settings = AnnotateSettings()` (annotate.py:2237) and never consults
  it. `outline_sigma` is 4 in the factory and 1.0 on screen, and its tooltip
  says "Default 4." `crop_source` is the serious one: the factory ships
  'png' under a capitalised comment citing instructions 170 and 171 -- "This
  shipped 'auto', which takes the PNG folder whenever one exists ... and
  unaskable when a user wants the other" -- and the dataclass still starts at
  'auto'. The decision landed in the factory and the annotation screen never
  saw it.

  A RELATED ONE THAT IS PURE NAMING: `img_size` (factory, int) and
  `image_size` (screen, tuple) are one question, and the screen's own
  settings-CSV importer looks for `image_size` while the defaults factory
  writes `img_size` -- so a settings file made from spaCR's own defaults
  silently sets no image size.

The rest are the familiar shapes plus one new one. `threshold`'s tooltip
offers quantile codes 'q1'-'q9' that no code in the package parses, so acting
on it raises `ValueError` on a worker thread; the same tooltip pair claims
`>=`/`<=` where `_apply_threshold` is strictly `>`/`<`. `expected_end` is a
window LENGTH whose own docstring has to say "window *length*, not an end
coordinate" twice. `n_jobs`'s shared tooltip tells the Map Barcodes user that
-1 means every core, and `_chunk_worker_count` raises on anything below 1.
`image_type` / `object_array` / `object_type` are three names for "which
object", one per crop source, and the panel already copies one into another.
`cv_group_by` and `object_size` are the first two settings in five modules
that are genuinely UNNECESSARY -- offered by Annotate, never acted on by it.

  THE NEW SHAPE, and it is what let the rest happen: ANNOTATE IS NOT
  REGISTERED AS A SETTINGS MODULE. `settings_model.py:295` returns
  `{"src": "path to images"}` for it, and it is absent from
  `GUI_MODULE_DEFAULTS`, so `test_every_setting_a_module_offers_has_a_
  category` never runs for it. 28 of its 44 keys have no category, 28 have no
  `expected_types` entry -- which is exactly why `edge_image` still ships the
  STRING 'False', a truthy value for a checkbox -- and 7 are declared nowhere
  at all: `object_filters`, `outline_method`, `grid_rows`, `grid_cols`,
  `stored_channel_order`, `display_order`, `display_primaries`. Three of
  those are combo boxes on the form with hand-written setToolTip strings.
  Registering Annotate the way every other module is registered would have
  caught most of this file on its own.

Preferences and spaceout are next, as the request asks, followed by the
sub-surfaces it named: live previews, the hyperparameter searches, and the
assay sub-modules.

--------------------------------------------------------------------------------
2026-09-03: THE PREVIEWS / SEARCHES / ASSAYS AUDIT LANDED, AND ONE FINDING WAS
A LIVE REGRESSION
--------------------------------------------------------------------------------

`instructions/data/364_previews_searches_and_assays_settings_audit_2026-09-02
.txt`: 1,188 lines, 16 findings. Mask live preview 95 rows, Crop preview 27,
Timelapse 14, Motility 11; the three hyperparameter surfaces 23 / 18 / 50 plus
16 run controls; recruitment 30, invasion 37, replication 25, analyze_plaques
22.

INVASION AND REPLICATION ARE CLEAN. 62 settings between them, nothing dead,
no duplicate pair, no tooltip contradicting its code. Recorded as a result,
because "we looked and it was fine" is one.

TWO FINDINGS ACTED ON THE SAME NIGHT, both verified before and after:

  * THE MASK LIVE PREVIEW HAD STOPPED READING MASK. `b7ae412af` renamed four
    settings families by suffix substitution on names beginning with an
    underscore; three tables in `live_preview.py` spell them without one, so
    the substitution walked past them. The panel propagated
    `cell_flow_threshold` OUT and read `cell_FT` back IN. Fixed; a worktree
    at `b7ae412af^` proves the rename broke exactly three tests and the
    fourth was already red.

    I then swept the package for other tables keyed on a bare suffix. The
    remaining `FT` / `CP_prob` / `Signal_to_noise` are a DIFFERENT and
    intentional family -- the standalone apply/test-model submodules use
    unprefixed names, and `FT`'s own tooltip says it is "the counterpart of
    the Mask module's per-object <object>_flow_threshold". So no further
    breakage. It does leave two names for one quantity, which is a
    redundancy finding for this instruction rather than a bug.

  * `expected_types["background"] = str`, so spaCR's own untouched defaults
    failed spaCR's own pre-flight with severity ERROR on three modules, and
    `coerce_expected_types` preserved the string into a numpy comparison.
    Fixed to `(int, float)`.

ONE FINDING DELIBERATELY NOT ACTED ON TONIGHT, and the reason is the shape of
the fix rather than doubt about the finding:

  RECRUITMENT OFFERS THREE SETTINGS IT NEVER READS. Measured inside
  `analyze_recruitment`, its only consumer: `cell_mask_dim`,
  `nucleus_mask_dim` and `pathogen_mask_dim` have ZERO reads each, against
  four each for their `*_chann_dim` twins. The form interleaves the dead
  three with the live three in one section (`settings_model.py:1731-1735`)
  under a hint reading "A wrong index here measures the wrong compartment
  without complaining" -- so half that section does nothing and its tooltips
  describe `measure_crop`.

  WHY IT WAITS: the keys are shipped by `get_analyze_recruitment_default_
  settings` in `spacr/settings.py`, and `recruitment` is in
  `GUI_MODULE_DEFAULTS`, so `test_every_setting_a_module_offers_has_a_
  category` runs for it. Dropping them from the panel alone leaves three
  factory keys uncategorised; dropping them from the factory is a change to
  the other session's file with a blast radius I cannot measure end-to-end
  without running recruitment on real data. Both halves, together, with the
  module exercised -- not one half at one in the morning.

---

2026-09-03, overnight. THE GRID IS BOUND, AND THE RETIREMENT IS FINISHED.

THE MOUNT IS NO LONGER AN OPEN QUESTION. This item warned that mounting the
per-object grid on Mask's form is "a design change to the most-used screen"
and should not be done in the same afternoon as the widget. Reading the seam
properly showed the shape it proposed -- "a grid that EDITS THE EXISTING
WIDGETS" -- is cheap, because every primitive it needs already exists and is
proven in other work:

  * `SettingsWidgets.set_value_for_key` writes a value into the widget for a
    key and handles every widget class. Live Preview's "Propagate settings"
    already uses it.
  * `collect()` reads the other direction.
  * `_set_row_visible(key, False)` hides a row, LABEL AND ALL, and
    `_guard_hidden_rows` keeps it hidden against something else showing it --
    which is 364's "hidden rather than deleted" half, already built for the
    rule that hides an object whose channel names no plane.

`spacr/qt/widgets/object_grid_binding.py` is the write-through binding, with
11 tests. It is NOT mounted on Mask. What remains is genuinely small:

  1. Build the grid into Mask's form as one section.
  2. Seed it, and reseed when a settings file is applied.
  3. Hide the flat per-object rows through a set kept SEPARATE from
     `_hidden_by_the_run` -- that set is recomputed from scratch on every
     `refresh_object_visibility` pass, so grid-hidden rows put into it would
     be shown again on the next pass. Union the two in the pass.
  4. Decide whether the grid is on by default or behind a preference.

Only 4 is a question for the maintainer.

WHAT THE BINDING PINNED THAT IS WORTH KNOWING: reading both sides BEFORE
writing either is what protects values, not the reentrancy guard. The guard
only stops the table being rebuilt under the cursor. They were conflated in
the first draft of the test, which passed against the guard removed.

THE RETIREMENT IN b7ae412af HAD STOPPED HALFWAY, and it left three tests red
on nightly. Same cause as the live-preview aliases fixed last night: the
suffix substitution walked past every spelling that was not its literal.

  * `measure_crop` read `{role}_min_size` while the factory shipped none, so
    the organelle size filter never ran -- silently, because a missing key is
    falsy.
  * Measure's first organelle slot was seeded by hand and never given a size
    floor at all, while slots 2..n got one.
  * `_translate_legacy_setting_keys` never consulted RETIRED_SETTINGS, so
    `spacr-doctor` reported a rename for a file whose value the panel had
    just dropped. It now does, with a suffix rule for the generated slots,
    since the table names only the first one and a run can declare
    organellez.

Fixed in 2120c905f. The two stale test lines were left by the same
substitution; the legacy-CSV one now pins the migration rather than asserting
the pre-migration names.

SEPARATELY: `docs/setting_consumers.json` was stale at HEAD -- every "where
is this setting read?" link pointed at line numbers 14 to 36 lines out.
Confirmed not mine by regenerating at clean HEAD in a worktree. Regenerated
in 1f39dfaf0, on its own so the churn did not bury the fix.

THREE ORGANELLE SETTINGS EXIST IN THE CODE AND NOWHERE ELSE. Found by
sweeping `measure.py` and `io.py` for keys the code reads that no factory
ships -- the generalisation of tonight's `{role}_min_size` bug.

`io.py:1686-1691` reads `organelle_background`, `organelle_signal_to_noise`
and `remove_background_organelle` as optional overrides of the generic
values, with an explicit fallback, under the comment "use organelle-specific
settings when present, otherwise the generic defaults above". No factory
ships any of the three and no form offers them, so the only way to reach them
is to hand-edit a settings CSV.

The other three objects have all three keys each. So an organelle is the one
object whose background subtraction cannot be tuned, while
`organelle_signal_to_noise` MULTIPLIES a background the user cannot see --
and 326 makes organelles first-class and arbitrary in number.

NOT DONE, and the reason is the same as the recruitment finding above: the
keys belong to `spacr/settings.py`, and the fix is only correct if the
shipped defaults equal what the fallback currently produces, which has to be
read out of the calling frame rather than guessed. Behaviour-neutral is
achievable here -- it is a question of confirming the generic value, not of
choosing a new one.

`measure.py` swept clean after tonight's fix. The sweep is worth keeping as a
test -- it is the check that would have caught the retirement stopping
halfway -- but NOT as written: a naive regex over `settings['x']` also
matches inside docstrings (`io.py:3494` names two keys in prose) and matches
WRITES (`io.py:7557` assigns `nr_classes`). Both were false positives here.
It needs to read the AST and count reads only, and to be seeded with the
metadata/annotation keys (`metadata_rules`, `annotation_columns`,
`annotation_values`, `metadata_type_by`) which come from a settings family
outside the per-module factories rather than from nowhere.

---

2026-09-03, 06:20. THE MOUNT IS DONE, and the one question this file said
needed the maintainer is answered by making it a preference.

`Preferences -> Appearance -> "Per-object settings as a table"`, OFF by
default. Clear, `_mount_the_object_grid` returns before touching anything and
the form is exactly what it was. Set, the 78 per-object rows are hidden and
the grid takes their place.

WHAT MADE IT SAFE, and both were found by reading rather than guessing:

  * `Section.add_row` records the pair in `_row_widgets`, and the module
    smoke test takes every entry there to BE a labelled setting with its own
    `settingKey` and linked API help. `add_prose_row` exists for exactly that
    distinction and is what the grid goes in through. This was the blocker I
    had recorded as a reason to wait; it turned out to be a solved problem
    with a method already written for it.
  * `refresh_object_visibility` recomputes its hidden set from scratch on
    every pass, so the grid's keys live in `_hidden_by_the_grid` and are
    unioned in. In `_hidden_by_the_run` they would have reappeared the first
    time a channel was typed -- which is the trap this file predicted.

MEASURED, NOT ASSERTED: `collect()` returns the same keys AND the same values
with the grid on and off, so a settings file written either way is the same
file. The module smoke test, the caption contract, the settings search and
the object-gating suites were all re-run with the grid FORCED ON -- 221 tests
-- because "off by default" is only safe if on is also correct.

WHAT IS LEFT ON THIS ITEM: nothing that is mine. The recruitment finding and
the organelle preprocessing gap above both need `spacr/settings.py`, which is
the other session's file, and both are recorded with the measurement that
found them.

--------------------------------------------------------------------------------
2026-09-04 -- THE RECRUITMENT FINDING IS ACTED ON, AND ITS BLOCKER IS GONE
--------------------------------------------------------------------------------

This file closed with "nothing that is mine": the recruitment finding needed
`spacr/settings.py`, which belonged to the other session under 325. That
session has been retired and the maintainer confirmed ownership of every lane,
so the blocker was administrative rather than technical.

RE-MEASURED BEFORE ACTING, because a finding two days old is a claim:

    reads inside analyze_recruitment, its only consumer
      cell_mask_dim       0        cell_chann_dim       4
      nucleus_mask_dim    0        nucleus_chann_dim    4
      pathogen_mask_dim   0        pathogen_chann_dim   4

    references anywhere in spacr/submodules.py: none of the three

Both halves together, which is what this file said it was waiting for: the
rows come off the form and the keys off
`get_analyze_recruitment_default_settings`.

THE ONE JUDGEMENT THIS NEEDED, and it is the trap 375 records for the same
table: NOT `RETIRED_SETTINGS`. Those three keys are declared by three
factories -- recruitment, `get_measure_crop_settings` and
`set_default_plot_merge_settings` -- and are read in the latter two, where
they name a plane of the merged stack. RETIRED_SETTINGS warns against exactly
this case in its own comment, because naming a key there that `spacr.settings`
still declares warns a user off a setting that works. The retirement is
therefore scoped to recruitment and nothing global changed.

THE HEADING FOLLOWED THE ROWS. "Mask & Channel Mapping" over a section with no
mask rows is the same dishonesty in smaller type, and its blurb promised a
mask index the module does not use. Renamed to "Channel Mapping", and the
existing per-app blurb was rewritten rather than a new shared entry added --
one CHANGED string across nine locales instead of nine new ones, which matters
while 306 is already carrying 2,646 owed translations.

276 passed, 1 skipped across the recruitment, settings-category and
retired-key selection. The skip is a missing pypdf and predates this.

WHAT IS LEFT ON 364 NOW: the organelle preprocessing gap recorded above, and
the remaining smaller modules in this file's own order -- motility 55,
train_test_model 50, activation 27, identify_masks_finetune 23, plaques 22 and
the rest. The two core modules the maintainer named first are done.

--------------------------------------------------------------------------------
2026-09-04 -- MOTILITY AUDITED (55 settings). ONE DEFECT, AND IT IS THE
              CLASSIFY SHAPE AGAIN
--------------------------------------------------------------------------------

See instructions/data/364_motility_settings_audit_2026-09-04.txt. PROPOSALS
only; nothing in the module was changed.

  * NOTHING IS DEAD, and the near-miss is worth recording. Two of the 55 are
    never referenced in `timelapse.py`, which owns the assay. One of them,
    `motility_analysis`, is read twice in `spacr/object.py` as the gate that
    decides whether the assay runs at all -- it reads as dead from the module
    that consumes it and removing it would have taken away the module's own
    switch. Checked before proposing, which is the whole method.

  * `infection_pca_n_clusters` IS the defect, and it is classify's finding 0
    again: not a dead setting but a live one whose own tooltip says it does
    nothing -- "Not currently honoured: the pca/umap/tsne QC always runs
    KMeans with exactly two clusters". Verified in the code rather than taken
    from the tooltip: four KMeans calls, every one hardcoding n_clusters=2,
    and the key read nowhere. It is offered all the same -- factory, type map
    and rendered form row.

    It is also, unlike the recruitment keys, declared by ONLY this factory, so
    it is a genuine candidate for RETIRED_SETTINGS rather than a scoped
    removal. Recommended end: remove it, because a binary infected/uninfected
    call has no cluster count to choose.

  * NO REDUNDANT PAIRS, which is worth saying after mask's 78-of-203. The
    twelve `infection_xgb_*` settings are one classifier's twelve
    hyperparameters, each read once by the model it configures.

  * TWO NAMES RECORDED AND NOT PROPOSED: `zscore_thresh` names the statistic
    rather than the decision, and `straightness_filter` removes tracks rather
    than flagging them. Both are one-line renames with a migration each, not
    worth a settings-file change on their own; recorded so they ride along if
    this module is touched.

REMAINING IN THIS FILE'S ORDER: train_test_model 42, activation 27,
identify_masks_finetune 23, plaques 22, check_cellpose_models 18,
train_cellpose 16, plot_data_from_csv 13, apply_cellpose_model 10,
test_cellpose_model 9, map_barcodes 5, analyze_reads 3. Plus the organelle
preprocessing gap already recorded above.

--------------------------------------------------------------------------------
2026-09-04 -- THE REMAINING ELEVEN MODULES SWEPT. THE MODULE PHASE IS COMPLETE.
--------------------------------------------------------------------------------

The request was "this should be done for all modules, starting with core
moduals". The core modules were audited on 2026-09-02 and motility earlier
tonight; this is the rest of the list, swept in one pass for the two questions
a machine can answer honestly -- is any key never read, and does any tooltip
admit the setting has no effect.

    train_test_model                42 settings    clean
    analyze_recruitment             28 settings    clean (after tonight's fix)
    generate_activation_map         27 settings    clean
    identify_masks_finetune         23 settings    clean
    analyze_plaque                  22 settings    clean
    check_cellpose_models           18 settings    clean
    train_cellpose                  16 settings    clean
    plot_data_from_csv              13 settings    clean
    apply_cellpose_model            10 settings    clean
    test_cellpose_model              9 settings    clean
    map_barcodes                     5 settings    the known dead factory
    analyze_reads                    3 settings    clean

NOTHING NEW WAS FOUND, and that is the result rather than an absence of one.
Ten of the twelve are clean on both questions. The eleventh, map_barcodes, is
the dead factory the 2026-09-02 annotate audit already recorded -- checked
here against that file rather than re-derived, because re-deriving a known
finding and presenting it as new is how a list grows without moving.

  Re-confirmed only because it is cheap: `get_map_barcodes_default_settings`
  is defined at settings.py:1975 and called nowhere. Its `barcodes` and `grna`
  tooltips say so themselves, and the live equivalents `row_csv`,
  `column_csv` and `grna_csv` carry 59 references each.

SO THE MODULE PHASE OF THIS INSTRUCTION IS DONE. Every settings factory in
`spacr/settings.py` has now been audited for dead keys, redundant pairs and
dishonest names. Two defects came out of the whole sweep and both are the same
shape -- a live setting whose own description says it does nothing:

    map_barcodes   `barcodes`, `grna`      known since 2026-09-02
    motility       `infection_pca_n_clusters`   found 2026-09-04

WHAT REMAINS ON 364 is no longer a sweep: the two proposals above wait on the
maintainer, the organelle preprocessing gap is recorded above, and the two
motility renames are parked until that module is touched for another reason.
The preferences and spaceout half was audited on 2026-09-02.


================================================================================
2026-09-04  THE PER-OBJECT TABLE: THREE DEFECTS, ALL MINE, ALL FROM ONE CHANGE
================================================================================

The table was asked to show only the settings "common between all objects".
That was implemented literally, and literally was wrong in three ways that
took a user report and two tests to find. Recording them together because
they are one mistake wearing three faces: I picked a rule that was easy to
state instead of measuring what the rule would select.

FACE ONE -- "ALL" EMPTIED MEASURE COMPLETELY.
Reported as "in measure there are way more per object settings than are shown
in the per object settings pannel, e.g. mask dim, max size". Measured:

    measure     mask_dim   cell, nucleus, pathogen, organelle   (not cytoplasm)
                min_size   cell, nucleus, pathogen, organelle
                max_size   three of the five
                min_area   one
                type       one

Cytoplasm is DERIVED -- it is what is left of a cell once the nucleus and
pathogen are taken out -- so it does not answer most questions an object
answers. Requiring all five objects therefore selected NOTHING, and Measure's
table was empty rather than short. A blank cell already reads as "this object
does not ask", and the model draws it as not-editable, so a question three
objects share was always a perfectly good row.

FACE TWO -- "MORE THAN ONE COLUMN" MADE THE ROWS DEPEND ON THE COLUMN COUNT.
The obvious repair. It is wrong because a new organelle is SEEDED FROM THE
ONE BEFORE IT, so every question only organelle A asked suddenly has two
askers and qualifies. Pressing Add went from 20 rows to 54. Caught by
`test_one_more_organelle_is_one_more_column`, which asserts the question
count does not move -- a test that existed for a different reason and paid
for itself here.

THE RULE THAT HOLDS is KINDS, not columns: every organelle counts once
between them. A question is per-object because DIFFERENT KINDS of object ask
it, and two organelles are the same kind asked twice. `_kind_of` in
`object_settings_grid.py`.

FACE THREE -- THE TABLE HAS TO EARN ITS PLACE, and the filter is not what
decides that. Asked whether per-object settings belong in regression and
classify. Measured:

    mask/timelapse   20 questions x 3 objects      a table
    measure           3 questions x 4 objects      a table
    regression        2 questions x 2 objects      NOT a table
    classify          0                            NOT a table

Regression shares `area_outlier_mads` and `intensity_outlier_mads` between
cell and nucleus. That is genuinely per-object and genuinely four cells, and
replacing a four-row form with a table, a header, an Add button and a resize
grip is a heavier control than the thing it replaces. `MIN_GRID_QUESTIONS`
in `app_screen.py` is that threshold.

THE LESSON, which is the same one 306 recorded from the other direction:
measure what a predicate SELECTS, against the real dictionary, before
believing the sentence that describes it. "Common to all objects" and
"shared between objects" sound like the same rule and differ by every
per-object setting Measure has.

--------------------------------------------------------------------------------
ALSO FIXED, same day, same area
--------------------------------------------------------------------------------

THE SWITCH WAS READ ONCE, WHILE THE PANEL WAS BEING BUILT. So turning the
per-object table on did nothing to a module already open, and turning it off
left the table on screen with the rows it speaks for still hidden. Reported
as "if you have per object table off and open a modual then turn it on it
dosnt go on, same if you have it on". `apply_object_grid_preference` now
mounts or unmounts on demand and Preferences broadcasts to every open screen.
The off direction is the one worth guarding: the flat rows are HIDDEN while
the grid speaks for them, so a table that vanished without releasing them
would leave those settings in no table and no form.

THE BINDING WAS ONE-WAY. The grid wrote through to the widgets -- that is
what makes it safe and it was well tested -- but nothing told the grid when a
widget moved, so a value changed anywhere else (a preset, a settings file, the
Live Preview, a flat row the table does not claim) left the table showing the
old answer while `collect()` returned the new one. Two answers to the same
question is the exact failure the binding exists to prevent. `follow_the_form`
connects the widgets behind the cells; it writes ONE CELL rather than
reseeding, because a reseed is 30 ms on Mask and 100 ms at ten organelles and
would run per keystroke and steal focus from the cell being typed into.

THE CEILING TEST HUNG. 326 raised MAX_ORGANELLES from 26 to 702, and
`test_the_ceiling_is_said_rather_than_hit_silently` pressed Add until it
refused -- 702 full table rebuilds. `next_organelle` also rescanned from slot
1 on every call, which made the walk quadratic. Both fixed: the scan starts
from the slots in use, and the test seeds at the ceiling instead of walking
to it. Its docstring was also still claiming twenty-six was the alphabet
running out, which 326 had made false.
