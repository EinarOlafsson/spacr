================================================================================
RETIRE metadata_type AND consolidate, ONCE IMPORT HAS ACTUALLY REPLACED THEM
================================================================================

Status:    not started, filed 2026-09-02. THE SCHEDULING HALF OF 363, which
           refused to do this in the same change and was right to.
Requested: implied by 363 -- "Once this exists, `metadata_type` and
           `consolidate` are import concerns sitting in Mask's settings, and
           Mask should read a project that is already a project. Retiring
           them is the natural end of this work -- but NOT in the same
           change: they are the only working path today, and removing them
           before the replacement is proven strands every existing user and
           every settings file in the wild. Land the module, migrate, then
           retire."
Owner:     spacr/settings.py and spacr/validate.py (the other session's files
           under 325), with the migration itself in spacr/qt/screens/
           image_import.py, which is mine.
Follows:   363, which is the LAND half and is closed. This is the migrate and
           retire halves, and they are in that order for a reason.

--------------------------------------------------------------------------------
WHY THIS IS A SEPARATE ITEM AND NOT A LOOSE END
--------------------------------------------------------------------------------

363 built `spacr/image_import.py` and its screen: a module that infers a
plate's layout from the filenames, reads OME stage coordinates, stitches
tiles back into fields, and writes a project. That makes `metadata_type` --
"which microscope wrote these names" -- and `consolidate` -- "copy everything
into one folder first" -- into import concerns that Mask should no longer
have to hold.

BUT THEY ARE STILL THE ONLY WORKING PATH FOR AN EXISTING USER. Measured
2026-09-02: `metadata_type` is read at 7 sites and mentioned in 30 files;
`consolidate` is read at 1 and mentioned in 30. Every settings CSV anyone has
saved names them.

--------------------------------------------------------------------------------
THE THREE STEPS, IN ORDER. DO NOT REORDER THEM.
--------------------------------------------------------------------------------

1. PROVE THE REPLACEMENT ON REAL DATA, not on the corpus. The ten synthetic
   acquisitions in `tests/import_corpus.py` are the specification and the new
   module answers all ten -- but a synthetic corpus is written by the same
   person who writes the parser, and that is exactly the shape of evidence
   this step must not rest on. Run the new Import over the acquisitions the
   maintainer actually has, including the ones `metadata_type` was added for,
   and record what it got right and what it did not.

2. MIGRATE. A settings file naming `metadata_type` or `consolidate` must keep
   working, and the user must be told once where the answer now lives. The
   shape is already in the package: `RETIRED_SETTINGS` in
   `spacr/validate.py` maps a withdrawn name either to its replacement
   (`organelle_min_size` -> `organelle_min_area`) or to `""` when there is
   none, and `_check_retired_keys` raises a Problem naming it.

   NEITHER OF THESE HAS A ONE-KEY REPLACEMENT, so both map to `""` -- and the
   message has to do the work instead. "Retired" is not enough for a setting
   somebody is relying on: say that Import now infers it, and that the way to
   get the old behaviour is to run Import once on the folder.

3. RETIRE. Only then. Add both to `RETIRED_SETTINGS` with a comment in the
   style of `corrected_manders`'s -- which says what it used to switch on,
   what replaced it, and why there is no replacement name -- and remove the
   rows from Mask's settings panel.

--------------------------------------------------------------------------------
THE TRAP, RECORDED BECAUSE IT HAS BEEN SPRUNG BEFORE IN THIS FILE'S FAMILY
--------------------------------------------------------------------------------

`RETIRED_SETTINGS` carries its own warning: "NOT HERE: a setting withdrawn
from ONE panel while `spacr.settings` still declares it ... naming one here
would warn a user off a setting that works." So step 3 is not done until the
setting is gone from `spacr.settings` as well. A key that is retired in
`validate.py` and still declared in `settings.py` produces a warning about a
setting that is still read, which is worse than either state alone.

--------------------------------------------------------------------------------
HOW IT WILL BE CHECKED
--------------------------------------------------------------------------------

* A settings CSV naming either key still loads, and the user is told once
  where the answer moved -- asserted on the message, not just on the absence
  of a crash.
* Neither key is read anywhere in `spacr/` afterwards; the 7 + 1 read sites
  measured above are zero.
* Neither key is declared in `spacr.settings` while being named in
  `RETIRED_SETTINGS`.
* Import's own path answers, on the maintainer's real acquisitions, whatever
  `metadata_type` was being set to answer.

--------------------------------------------------------------------------------
2026-09-04 -- STEP 1 RUN ON REAL ACQUISITIONS, AND IT SAYS STOP
--------------------------------------------------------------------------------

Step 1 asks for the replacement to be proven on the maintainer's own data
rather than on `tests/import_corpus.py`, "because a synthetic corpus is
written by the same person who writes the parser". Done, on five acquisitions
from /nas_mnt/carruthers/Einar. Names only were read; nothing on the NAS was
touched, and the sampled names were recreated as empty files locally so the
inference ran on the half `metadata_type` actually decides.

    acquisition                  shape                    old cv    infer_layout
    akt_gsk3_tubulin             plate_well_T..F..C..     150/150   150 placed
    calnexin_20240615_112503     plate_well_T..F..C..     150/150   150 placed
    turboID_20240218_131300      plate_well_T..F..C..     149/150   149 placed
    tsg101_screen                plate_well_T..F..C..     128/150   128 placed
    mitoscreen                   plate#_E07_18.tif          0/150    43 placed

On the four cellvoyager acquisitions the new Import matches the old regex file
for file and derives well, field and channel for every name. That half is
proven.

MITOSCREEN IS THE ONE THAT MATTERS AND IT FAILS. Every name in the sample is
the same token-less shape -- `plate4_F07_18.tif`, i.e. plate, well, and one
trailing number -- and:

  * the old cellvoyager regex matches NONE of it, which is precisely why
    `metadata_type` exists as a setting;
  * the new Import places 43 of 150, and for those it derives only plate and
    well. Field and channel come back empty for the entire acquisition.

So Import today answers the acquisitions `metadata_type='cellvoyager'` already
answered, and does NOT answer the one a user would have to change the setting
for. Retiring `metadata_type` now would take away the only working path for
this shape and replace it with a parse that loses the field and the channel --
exactly what the instruction's own ordering exists to prevent.

STEPS 2 AND 3 ARE NOT STARTED, DELIBERATELY. The file says DO NOT REORDER
THEM, and step 1's answer is no. What step 1 has changed is that the gate is
now a measurement rather than an assumption: the work needed before this
instruction can move is for `infer_layout` to read a trailing-number field on
a token-less `plate_well_field` name, and to say which trailing number is the
field and which the channel when there is only one.

NOT ATTEMPTED HERE, because it is a change to the inference that 363 owns and
it needs the maintainer to say what that trailing number means in this
acquisition -- field, channel, or slice. The names alone do not decide it, and
guessing would put a wrong answer under a setting nobody can override once
`metadata_type` is gone.

--------------------------------------------------------------------------------
2026-09-05 -- STEP 1 RUN FOR `consolidate` TOO, AND IT ALSO SAYS STOP.
NOTHING RETIRED. ONE STALE PIECE OF ADVICE FIXED.
--------------------------------------------------------------------------------

The 2026-09-04 entry ran step 1 for `metadata_type` -- the filename half --
and recorded a no. It did not run step 1 for `consolidate`: the sampled names
were recreated FLAT, and `consolidate` is about the folder tree, not the name.
So the half that decides `consolidate` had never been measured. It has now.

The NAS is not mounted from this session (`/nas_mnt: No such device`), so
this could not be re-run on the maintainer's acquisitions. Measured instead on
the two tree shapes `consolidate`'s OWN tooltip names -- "one plate's images
are split across per-well or per-channel subfolders" -- plus the two shapes
2026-09-04 cared about, nested one folder deep. Both paths were run over the
same trees:

  tree                  consolidate + cellvoyager   spacr.image_import
  per_well              NO MATCH (0/12)             12/12: well, field, chan
  per_channel           NO MATCH (0/8)              8/8 placed, channel is a
                                                    stated question, not a guess
  per_well_cellvoyager  8/8, plate becomes          REFUSES: the well appears
                        'A01_plate1'                in folder AND filename and
                                                    both copies read unplaced
  per_well_mitoscreen   NO MATCH (0/6)              6/6 placed, field/channel
                                                    empty (as 2026-09-04)

WHAT THAT CHANGES. On the layouts `consolidate` exists for, `consolidate` is
not a working path at all: it copies the plate and then the cellvoyager regex
matches nothing it wrote. Import is strictly better on both of them. That is
the argument FOR retiring it -- and it is not enough, because of row three.

ROW THREE IS THE GATE, and it is the same gate as mitoscreen. A per-well tree
whose filenames ALREADY carry full cellvoyager metadata is parsed by
`consolidate` and REFUSED by Import, because the well is named twice -- once
by the folder, once by the filename -- and `infer_layout` reports both copies
as unplaced tokens rather than recognising a token that repeats an axis the
name already gives. `apply_import` refuses a plan with problems, by design.
So retiring `consolidate` today takes away the one path that shape has.

THE TWO GATES ARE NOW ONE SENTENCE, which is the useful thing this measurement
produced. Both blockers are the same missing rule in `infer_layout`: it has no
way to resolve a varying digit slot that carries no marker. Mitoscreen's
trailing number and this tree's duplicated well are the same failure wearing
two hats. Whoever picks up 363's inference next fixes both or neither.

RECOUNTED, because 2026-09-02's numbers have moved and only in the wrong
direction. `metadata_type` is read at 12 sites now, not 7 -- it has since
spread to `io.py` (2), `qt/prerun.py`, `validate.py` (2) and
`qt/widgets/live_preview.py` -- and is mentioned in 92 files. `consolidate` is
still 1 read (`core.py:322`) plus its `setdefault`, in 28 files.

STEPS 2 AND 3 REMAIN NOT STARTED, for both keys, deliberately. `RETIRED_SETTINGS`
was checked and is honest as it stands: no entry in it is also declared in
`spacr.settings.expected_types`, and every named replacement is a live
setting. Adding either key to it today would trip the trap this file records,
because both are still declared AND still read.

WHAT WAS ACTUALLY CHANGED, and it is the only thing that could be changed
without reordering the steps: `spacr/validate.py`'s "no image files found in
src" error advised `consolidate=True` to gather images from subfolders. The
table above says that advice is wrong -- for the per-well and per-channel
trees it names, it buys a full second copy of the plate and still parses
nothing. It now sends a nested tree to Import first and describes
`consolidate` as the older path with the condition under which it helps.
Nothing was withdrawn and no settings file changed meaning.


--------------------------------------------------------------------------------
2026-09-04 — RE-MEASURED. STEP 1 IS MAINTAINER-GATED AND HAS NOT MOVED.
--------------------------------------------------------------------------------

STEP 1 CANNOT BE DONE BY A SESSION. It asks for the new Import to be run over
"the acquisitions the maintainer actually has, including the ones
`metadata_type` was added for". That is not data in the repository, and the
step exists precisely because the synthetic corpus is not evidence -- it was
written by whoever wrote the parser. Steps 2 and 3 are ordered behind it and
this file says not to reorder them, so nothing here is safe to start.

WHAT IS ACTIONABLE HAS BEEN DONE: the premise re-measured, because a number
recorded two days ago and acted on next week is how a plan goes wrong.

    metadata_type   read sites  7 -> 13     mentioned in 30 files (unchanged)
    consolidate     read sites  1 ->  1     mentioned in 31 files (was 30)

THE READ COUNT FOR `metadata_type` NEARLY DOUBLED. That is the opposite
direction from the one this instruction assumes, and it makes the retirement
harder rather than easier: whatever added those sites did so while the plan
of record was to remove the key. Worth understanding before step 3, because
retiring a key that six more places have started reading since the plan was
written is a bigger change than the plan costed.

A RELATED TEST WAS RED AND IS NOW GREEN, and it is worth saying it was not a
375 defect. `test_a_panel_with_no_gated_setting_greys_nothing` asserted Mask
had no gated settings. `custom_regex` has since gained a dependency rule --
it is read only when `metadata_type` is 'custom', or 'auto' with a regex
supplied -- so the test's premise went stale while the behaviour was correct.

Rewritten to assert the property actually worth guarding: the sweep greys THE
RULED SETTING AND NOTHING ELSE. A second test now pins the 'auto' case, which
is the half a tidy-up would get wrong -- `metadata_type`'s own description
says 'auto' renames using `custom_regex` when supplied, so greying the field
there would remove documented behaviour while looking like a cleanup.

NEXT ACTION IS THE MAINTAINER'S: run Import over the real acquisitions, and
say what it got right and what it did not. Until then this item is blocked,
not merely unstarted.
