================================================================================
MAKE THE API PAGE LOOK PROFESSIONAL, CLEAN AND USEFUL
================================================================================

Status:    in progress. 17 short underlines and 181 duplicate object
           descriptions fixed, both VERIFIED by rebuild (2026-08-09).
Requested: 2026-08-08 — "do a sweep of the API page and make sure everything looks as professions and clean and informative and usefull as you can make it"

--------------------------------------------------------------------------------
WHAT THE STATE IS
--------------------------------------------------------------------------------

The API documentation is the target of every tooltip's link: a tooltip
built by `format_tooltip` ends in a link to `/spacr/<module>/index.html`
with an anchor for the setting. So the page is not a separate artefact from
the product -- it is where the in-app help sends people.

What is known about its quality:

  * A contract test (`tests/test_api_documentation_contract.py`) requires
    every public function and class to have a docstring. It was FAILING
    until today, with four public symbols undocumented, which means the
    generated pages had four empty entries.
  * The `docs` workflow is green.
  * Nobody has read the rendered pages end to end and asked whether they
    are good, as opposed to present.

--------------------------------------------------------------------------------
WHY IT MATTERS
--------------------------------------------------------------------------------

This is the page a new user lands on from a tooltip, and the page a
prospective adopter reads before deciding whether the tool is maintained.
For an academic tool both audiences matter and both judge quickly: an API
page with empty entries, inconsistent formatting or a wall of undifferentiated
functions reads as unmaintained regardless of how good the code is.

It is also the destination of instruction 61's link checking. Fixing a
tooltip's link is pointless if it resolves to a page that does not answer
the question.

--------------------------------------------------------------------------------
WHAT TO DO
--------------------------------------------------------------------------------

1. BUILD AND READ IT. Render the docs and go through the pages as a user
   would, from the landing page inward. Note what is confusing, empty or
   ugly before changing anything -- a sweep driven by a list of observed
   problems beats one driven by taste.
2. Landing page first: it should say what spaCR is, who it is for, and
   where to start, above the fold. That page is disproportionately what
   people judge.
3. Consistent docstring shape across the public API -- summary line,
   params, returns, raises. The repository already writes good ones in
   places; make the rest match rather than inventing a new format.
4. Separate the public surface from the internals. A page listing every
   private helper beside the three functions a user actually calls is
   noise, and it is the most common reason generated API docs feel
   unusable.
5. Every module page should say what the module is FOR in a sentence
   before it lists what is in it.
6. Check the rendering itself: broken cross-references, unrendered
   reStructuredText, code blocks that lost their highlighting, tables that
   overflow. These are what "unprofessional" usually means concretely.
7. Coordinate with instruction 61. Its link-resolution test is the thing
   that keeps this page and the tooltips honest with each other, and it
   should be written once, not twice.

--------------------------------------------------------------------------------
HOW TO KNOW IT WORKED
--------------------------------------------------------------------------------

The built docs have no empty entries, no broken cross-references and no
unrendered markup; every module page opens with a sentence saying what it
is for; and following an API link from a tooltip in the running app lands
on a page that answers the question the tooltip raised.

--------------------------------------------------------------------------------
DELIBERATELY NOT DONE
--------------------------------------------------------------------------------

Not started. A caution worth stating: a tutorials effort is running in
parallel (instruction 48, a concurrent Codex session) and touches the same
docs tree. Check what that session has changed before editing docs, or the
two sweeps will conflict.

--------------------------------------------------------------------------------
2026-08-08 - BUILT IT, AND HERE IS THE DEFECT LIST
--------------------------------------------------------------------------------

    python -m sphinx -E -b html --keep-going docs/source /tmp/docsbuild

~8 minutes, 125 module pages, index.html produced. Two things settled
before any read-through:

  CONTENT IS NOT MISSING. A page per module, 125 of them. Whatever is
  wrong with this site is presentation, not absence -- a much better
  problem to start from than the one I expected.

  THE LINKS ARE CLEAN. 1151 tooltip links checked against this build,
  0 dead (instruction 61). So the tooltip -> docs route works end to end.

WHAT THE BUILD ACTUALLY COMPLAINS ABOUT: 231 warnings, 21 errors. Grouped,
worst first, and every one of these is visible on the rendered page:

    ~150  duplicate object description
          database_concurrency (37), remote_execution.R* (27),
          plugins.AppContrib* (21), remote_execution.E* (18),
          classifier_evaluation (11), plugins.SpacrPlugin (8), ...
          The same symbol documented twice, so it appears twice in the
          page and twice in the index. Usually a re-export: the object is
          picked up in its defining module AND wherever it is imported.

     18  ERROR: Unexpected indentation  [docutils]
     17  WARNING: Title underline too short
     11  WARNING: Block quote ends without a blank line
      2  ERROR: Malformed table
      1  ERROR: Undefined substitution referenced

The last five groups are ~49 malformed docstrings, and each one renders as
BROKEN MARKUP on the page -- a table that does not become a table, a code
block that loses its indentation. That is exactly the "unprofessional"
this instruction is about, and it is a finite, addressable list rather
than a matter of taste.

ORDER TO FIX IN:
  1. the ~49 malformed docstrings. Each is one file, one line number, in
     the build log, and each one is visibly wrong on the site.
  2. the duplicate object descriptions. One cause, ~150 warnings: decide
     where a re-exported symbol should be documented and configure
     autoapi to document it once.
  3. THEN read the pages, which is what the original note asks for. Doing
     the read-through first would mean re-reading everything after the
     markup is fixed.

Keep the build log. The line numbers are the work list, and regenerating
it is eight minutes.

--------------------------------------------------------------------------------
2026-08-08 - HOW TO GET THE EXACT LIST (I tried the shortcut; it does not work)
--------------------------------------------------------------------------------

The build names each fault as `api/spacr/<module>/index.rst:<line>`, and
autoapi DELETES those generated .rst files when it finishes -- the workflow
even does `rm -rf docs/source/api` first. So by the time you read the log,
the line numbers point at files that no longer exist.

The shortcut I tried: scan the source for a rule line (`---`) shorter than
the text line above it, since that is what "Title underline too short"
means. It reports 5102 hits against 17 real warnings. Almost all are prose
separators inside ordinary comments that never reach a rendered docstring,
and a handful are inside module docstrings docutils never parses as
titles. Useless as a work list, and fixing from it would be editing
hundreds of files on a guess.

DO THIS INSTEAD -- keep the generated rst:

    python -m sphinx -E -b html --keep-going docs/source /tmp/docsbuild
    # do NOT clean docs/source/api afterwards
    grep -E "Title underline|Unexpected indentation|Malformed table" \
        /tmp/sphinx.log

then open each `docs/source/api/<...>/index.rst` at the reported line. The
generated rst quotes the docstring verbatim, so the offending text is right
there and maps back to its module by path.

The modules with the most faults, from this build, as a starting order:

    anndata_export (4), runctx (3), ome_zarr (3), qt/synthetic (2),
    qt/bridge (2), model_zoo (2), model_compare (2), foreign (2)

That is ~20 of the ~49 in eight files.

--------------------------------------------------------------------------------
2026-08-09 - A SHORTCUT THAT DOES NOT WORK, RECORDED SO NOBODY REPEATS IT
--------------------------------------------------------------------------------

The build log from the first pass was lost in a machine crash, and a
rebuild is ~8 minutes, so I tried to regenerate the defect list cheaply by
running docutils directly over every docstring -- docutils being what
actually emits "Unexpected indentation" and friends.

IT DOES NOT REPRODUCE SPHINX'S FINDINGS, in two separate ways:

  5335 false "Unknown interpreted text role" -- :func:, :class:, :data:,
       :mod:, :meth:, :attr:. Those are SPHINX roles; bare docutils has
       never heard of them. Every one is correct markup.

   ~130 false "Unexpected indentation" / "Block quote ends without a blank
       line" -- these are GOOGLE-STYLE docstrings ("Args:", "Returns:"),
       which `sphinx.ext.napoleon` rewrites into field lists before
       docutils sees them. conf.py line 42 enables napoleon. Bare docutils
       sees the indented block raw and objects.

So the check reported 5466 findings, essentially all of them noise, and I
came close to rewriting 130 correct docstrings to satisfy a tool that was
not modelling the real pipeline.

THE LESSON, which generalises past this task: a cheaper substitute for a
build is only useful if it runs the SAME extensions. Sphinx's own numbers
already account for napoleon and its roles; a bare parser cannot, and its
disagreement is about the parser rather than the docstrings.

USE THE BUILD. It is eight minutes:

    python -m sphinx -E -b html --keep-going docs/source <out>

and its warnings are the authoritative list. Nice it down if the machine is
shared -- there is a concurrent session on this box that has taken ~15
cores for an hour at a time.

--------------------------------------------------------------------------------
2026-08-09 - THE AUTHORITATIVE LIST, AND A MASS EDIT THAT HAD TO BE REVERTED
--------------------------------------------------------------------------------

Rebuilt the docs (8 minutes, niced): 231 warnings, 21 errors. The markup
defects, which is the actionable half:

    18  ERROR    Unexpected indentation
    17  WARNING  Title underline too short
    11  WARNING  Block quote ends without a blank line
     7  WARNING  Definition list ends without a blank line
     6  WARNING  Inline literal start-string without end-string
     4  WARNING  Inline interpreted text or phrase reference
     3  WARNING  Field list ends without a blank line
     2  ERROR    Malformed table
     8  various  undefined substitutions, unknown targets

They are reported against the GENERATED files under docs/source/api/, but
the line numbers match the SOURCE docstrings -- e.g.
anndata_export/index.rst:94 is spacr/anndata_export/__init__.py:94, where

    Where the cell -> nucleus -> pathogen links go, and why not ``obsp``
    -------------------------------------------------------------------

and `The labels the table join drops` (31 chars) sits over 30 dashes. The
underline must match the SOURCE length, and ``obsp`` is 8 source characters
rendering as 4, which is how these get written short.

DO NOT FIX THESE WITH A LINE-BASED REGEX. I tried, and it corrupted 203
files, because `"` is a legal rST underline character -- so a docstring's
closing `"""` matched the underline pattern and was "lengthened" to the
width of the line above it, producing `"""""""""`. Reverted with
`git checkout`; nothing was committed.

Two mistakes worth naming, since either alone would have caught it:
  * it ran over the whole package in one pass instead of one file first;
  * it checked `ast.parse` AFTER writing rather than before, when the check
    is cheap enough to gate every write.

THE SAFE SHAPE, for whoever does this: walk the file with `ast`, take each
docstring's own start and end offsets, edit only INSIDE that span, and
re-parse the file before writing it back. The closing quotes are then
outside the region being touched and cannot be mistaken for content.

--------------------------------------------------------------------------------
2026-08-09 - 17 FIXED AND VERIFIED BY REBUILD
--------------------------------------------------------------------------------

    Title underline too short    17 -> 0
    total warnings              252 -> 235

Exactly 17 removed and nothing added, which is what "verified" should mean
here -- a markup fix that silently trades one warning class for another
would look like progress in the summary line and not be.

(A caution on reading these numbers: `grep -c WARNING` over the log and the
build's own "N warnings" line count different things -- 231 vs 235 on the
same build. Compare like with like, or a clean fix looks like a regression.)

WHAT IS LEFT, from the same build:

    18  ERROR    Unexpected indentation
    11  WARNING  Block quote ends without a blank line
     7  WARNING  Definition list ends without a blank line
     6  WARNING  Inline literal start-string without end-string
     4  WARNING  Inline interpreted text or phrase reference
     3  WARNING  Field list ends without a blank line
     2  ERROR    Malformed table
     8  various  undefined substitutions, unknown targets
   ~150 WARNING  duplicate object description  (one cause, see below)

The duplicates are still the largest group and still one fix: a re-exported
symbol documented both where it is defined and where it is imported. Decide
where each should live and configure autoapi to document it once.

The tool for the rest is `scratchpad/fix_underlines.py`, which is worth
keeping as the PATTERN rather than the script: ast-scoped edits, gated by a
re-parse, run on one file first. The underline fix it performed is the
cheapest of these categories; "Unexpected indentation" and the block quotes
need a human reading each docstring, because the right fix is usually a
blank line in a place only a person can choose.

--------------------------------------------------------------------------------
2026-08-09 - THE DUPLICATES WERE HAND-WRITTEN PAGES, NOT autoapi
--------------------------------------------------------------------------------

    duplicate object description   182 -> 1

The cause was not autoapi's `imported-members` as this file guessed. It was
SIX `.. automodule::` directives in hand-written narrative pages, each
pointing at a module autoapi already documents:

    database_concurrency_audit.rst   spacr.database_concurrency, cli_database
    distributed_execution.rst        spacr.remote_execution
    leakage_audit.rst                spacr.classifier_evaluation
    plugin_sdk.rst                   spacr.plugins
    threading_cancellation_audit.rst spacr.cancellation

Those five files produced 181 of the 182 warnings, and the top offenders in
the count map onto them one for one -- remote_execution 122, plugins 116,
database_concurrency 74.

THE FIX IS `:noindex:` ON EACH, which is exactly what the directive is for:
render the API inline in the narrative page, without claiming to be the
canonical definition of those symbols. Deleting the directives would have
been wrong -- the narrative pages want the API visible in context -- and
excluding the modules from autoapi would have been worse, since the API
reference should list everything.

ONE SURVIVOR, and it is a different cause:

    spacr/mask_io/index.rst:89  spacr.mask_io.DEFAULT_FORMAT

A module-level constant documented twice by autoapi itself. Worth a look,
but it is one warning rather than a class.

STILL OPEN: 18 unexpected indentation, 11 block quotes, 7 definition lists,
6 inline literals, 2 malformed tables, 8 substitutions/targets. Those need a
person per docstring -- the repair is usually a blank line only a reader can
place -- unlike the two classes fixed today, which were mechanical.

--------------------------------------------------------------------------------
2026-08-09 - WHERE IT STANDS, ACROSS THREE BUILDS
--------------------------------------------------------------------------------

                       WARNING   ERROR   duplicates
    baseline               231      21          182
    after 17 underlines    214      21          182
    after :noindex: x6      33      21            1

Warnings are down 86%, and the two fixes account for all of it. The ERROR
count has not moved, and that is the honest state of this task: what is
left needs a person.

    18  Unexpected indentation
     2  Malformed table
     1  Undefined substitution

Plus 32 remaining warnings of the block-quote / definition-list / inline-
literal families.

WHY THE REST IS HAND WORK. Every one of these is "a blank line is missing
in a place that changes the meaning", and which place is a judgement about
what the author meant. A regex attempt at exactly this class corrupted 203
files earlier today. The safe tool -- tools/fix_rst_underlines.py -- works
because "underline shorter than title" has ONE correct repair; these do
not.

NEXT, in order:
  1. the 18 Unexpected indentation errors -- each is one docstring, and
     the build log gives file and line.
  2. the 2 malformed tables, which are usually a column-width row that
     stopped matching after an edit.
  3. THEN read the rendered pages, which is what the original task asked
     for and which is now worth doing, because the noise that would have
     buried a real judgement is gone.

--------------------------------------------------------------------------------
2026-08-09 - "UNEXPECTED INDENTATION" HAS ONE CAUSE, AND IT IS FIXABLE
--------------------------------------------------------------------------------

It is not arbitrary bad markup. Every instance is a reStructuredText
DEFINITION LIST whose term wrapped onto a second line:

    ``ErrorPolicy``, ``resolve_error_policy``, ``SkipRecord``, ``SKIPPED``,
    ``ON_ERROR_STOP``, ``ON_ERROR_SKIP``, ``ON_ERROR_RETRY``
        The S9 machinery.

rST requires the term to be ONE line. A wrapped term is an ordinary
paragraph, so the indented definition under it is "unexpected", and the
whole entry renders as body text instead of a definition list. The fix is
to join the term onto one long line -- long lines being correct here, since
this is prose, not code.

FIXED: 4. Three in `spacr/runctx.py` by hand, one in `spacr/data_manager.py`
by `scratchpad/fix_defterms.py`, which joins wrapped terms and KEEPS the
change only when re-parsing shows the error count actually dropped and no
new error appeared.

WHY THE TOOL ONLY GOT ONE, recorded so nobody assumes it is broken:
`ast.get_docstring` returns the CLEANED docstring -- dedented, escapes
processed -- so for any docstring indented inside a class or function the
text does not appear verbatim in the source and the replace no-ops. It
works for module-level docstrings at column 0 and nothing else. Fixing that
properly means slicing the source by the node's own offsets, which is what
`tools/fix_rst_underlines.py` already does and what this tool should adopt
if anyone extends it.

REMAINING, each one docstring, each the same wrapped-term shape:

    spacr/artifacts.py            module
    spacr/deep_spacr.py           train_model
    spacr/qt/__init__.py          module
    spacr/qt/ai/__init__.py       module
    spacr/qt/ai/prompts.py        module
    spacr/qt/ai/providers.py      module
    spacr/qt/bridge.py            PipelineWorker  (2)
    spacr/qt/hf_download.py       _HFDownloadWorker
    spacr/qt/linked_selection.py  LinkedSelection
    spacr/qt/regex_detect.py      auto_detect_regex
    spacr/qt/settings_diff.py     module
    spacr/qt/synthetic.py         module, generate_mask_demo
    spacr/utils.py                (see build log)

That list came from running docutils per docstring with napoleon-style ones
EXCLUDED -- Google-style "Args:" docstrings are napoleon's job and were the
false positives that made the earlier bare-docutils sweep useless.

--------------------------------------------------------------------------------
2026-08-09 - THE TOOL NOW SLICES BY OFFSETS, AND STILL REFUSES MOST FILES
--------------------------------------------------------------------------------

`tools/fix_rst_def_terms.py` was rewritten to edit by LINE NUMBER inside
each docstring's own span, which fixes the silent no-op described above --
it no longer depends on `ast.get_docstring` text matching the source.

It still declines almost every file, and the reason is worth knowing before
someone "fixes" the gate: its join loop is GREEDY. Scanning top-down, it
joins the first candidate pair it sees, which can consume a line that
belonged to the pair that actually mattered. On `spacr/artifacts.py` it
finds 2 joins, the total message count drops 17 -> 16, and the
"Unexpected indentation" count stays at 1 -- so the verification gate
discards the whole edit, correctly.

That is the gate working as designed. An unverified version of this tool
would have written a change that looked plausible and fixed nothing, and
its line-based ancestor corrupted 203 files.

FOR WHOEVER FINISHES THIS: the ~15 remaining are one edit each and take
about a minute apiece by hand -- find the wrapped term, join it onto one
line. That is cheaper than teaching the tool to choose between candidate
joins, which is a search problem for a payoff of fifteen edits. The list of
files and symbols is in the previous section.

--------------------------------------------------------------------------------
2026-08-09 - "UNEXPECTED INDENTATION" IS TWO CAUSES, NOT ONE
--------------------------------------------------------------------------------

An earlier note here said every instance is a wrapped definition-list term.
That was true of the four fixed in `runctx.py` and `data_manager.py` and is
NOT true of the rest. Looking at four more:

    spacr/qt/ai/prompts.py     a bullet list with NO BLANK LINE after the
                               paragraph introducing it. rST requires one;
                               without it the bullets are an indented block.
                               FIXED -- 1 -> 0 errors.
    spacr/qt/settings_diff.py  a definition whose continuation is indented
                               DEEPER than the definition body
    spacr/qt/ai/providers.py   aligned continuation lines under a term
    spacr/qt/synthetic.py      a bullet whose continuation is indented

So the class splits roughly in two: MISSING BLANK LINE before a list, and
CONTINUATION LINES aligned by eye rather than by rST's rules. The first is
a one-character fix; the second needs someone to decide what the author
meant, because de-indenting changes which text belongs to which item.

The method that works for both, and is worth reusing: parse the docstring
with docutils, count "Unexpected indentation", apply the candidate fix,
count again, and keep it ONLY if the count dropped. That gate is what
distinguishes a fix from a plausible-looking edit, and it is cheap --
`errs(doc)` is four lines.

--------------------------------------------------------------------------------
2026-08-09 - THE AUTOMATABLE PART OF THIS CLASS IS EXHAUSTED AT 16 DOCSTRINGS
--------------------------------------------------------------------------------

Ran the missing-blank-line fix across every docstring in the package, gated
on the error count dropping. It fixed ZERO beyond `prompts.py`, which
confirms the split recorded above: that variant was one instance, and the
rest are continuation lines aligned by eye.

Remaining, 16 docstrings, 19 errors (each verified per-docstring with
docutils, napoleon-style excluded):

    3x  spacr/timelapse.py            _infection_qc_pca_clustering
    2x  spacr/qt/bridge.py            PipelineWorker
    1x  spacr/deep_spacr.py           train_model
    1x  spacr/qt/__init__.py          <module>
    1x  spacr/qt/ai/__init__.py       <module>
    1x  spacr/qt/ai/providers.py      <module>
    1x  spacr/qt/hf_download.py       _HFDownloadWorker
    1x  spacr/qt/linked_selection.py  LinkedSelection
    1x  spacr/qt/regex_detect.py      auto_detect_regex
    1x  spacr/qt/settings_diff.py     <module>
    1x  spacr/qt/synthetic.py         <module>, generate_mask_demo
    1x  spacr/qt/tutorial/engine.py   Step
    1x  spacr/spacrops.py             StitchedMultiAligner
    1x  spacr/timelapse.py            _infection_qc_xgboost
    1x  spacr/utils.py                FocalLossWithLogits

EACH NEEDS A DECISION, not a rule. The text is indented to line up under
something -- a term, a bullet, an argument name -- and rST reads that
alignment as a nested block. De-indenting fixes the error and changes which
text belongs to which item, so the right repair depends on what the author
meant. That is why the gated sweep declines them rather than guessing.

The workflow that makes this quick, about a minute each:

    err = count "Unexpected indentation" in the docstring
    try the candidate edit
    keep it only if the count dropped

`spacr/qt/ai/prompts.py` is the worked example, and `runctx.py` the other
shape (a wrapped definition term joined onto one line).
