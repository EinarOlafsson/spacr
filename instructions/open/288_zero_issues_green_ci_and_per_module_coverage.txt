================================================================================
ZERO ACTIONABLE ISSUES, GREEN GITHUB CI, AND 100% PER-MODULE COVERAGE
================================================================================

Status:    OPEN, filed 2026-08-27. RELEASE-BLOCKING.
Owner:     the complete spaCR source tree, tests and GitHub workflows.
Precedes:  the version, main-branch and history publication actions.
           (These were item 253's, deleted 2026-09-02 at the
           maintainer's instruction.)
Requested: 2026-08-28 — "get the library to 100% test coverage"

--------------------------------------------------------------------------------
WHAT WAS ASKED
--------------------------------------------------------------------------------

Fix all GitHub issues, make every GitHub test pass, and bring every spaCR
module to 100% test coverage.

--------------------------------------------------------------------------------
MEASURED 2026-08-28 — THE CURRENT RELEASE TREE IS NOT CLOSE TO THIS CONTRACT
--------------------------------------------------------------------------------

The first frozen-tree audit found:

  * 490 shipped ``spacr/**/*.py`` modules, while the only saved per-file
    baseline covers 302 rows, omits 200 current modules and retains 12 deleted
    ones;
  * that 2026-08-10 statement-only baseline is 121,662 / 136,889 statements
    (88.876%), with 62 rows at 100%, 240 below and two at zero; it contains no
    branch measurement;
  * 304 prohibited ``# pragma: no cover`` occurrences across 104 tracked
    modules;
  * no required workflow invokes coverage, publishes a per-module artifact or
    runs a negative ratchet test, and neither ``main`` nor ``nightly`` has a
    required branch-protection check configured;
  * 46,877 collected tests on the current tree;
  * the current GitHub head is red: the Slow job has four Measure-demo
    failures and the docs job reports stale reviewed runtime source. The
    Measure crash was reproduced from a clean installed wheel, not only from
    the checkout.

The local ``.coverage`` file is not a baseline: it contains only
``spacr/qt/hidpi.py``. Regenerate evidence from a frozen tree rather than
merging it with that partial file.

--------------------------------------------------------------------------------
MEASURED 2026-08-29 — EXACT CURRENT INVENTORY AND HONEST CARRY-FORWARD
--------------------------------------------------------------------------------

The package boundary is now counted from the same ``setuptools.find_packages``
discovery used by ``setup.py``, rather than treating every Python helper under
``spacr/`` as a wheel module:

  * 486 Python modules are shipped across nine discovered packages;
  * the source tree contains 491 ``spacr/**/*.py`` files. The five that are not
    distributed are the four
    ``spacr/resources/home/versions/_generators/*.py`` scripts and
    ``spacr/resources/icons/backup_icons/_generators/``'s icon generator;
  * coverage.py's static analysis of those 486 current files finds 196,438
    statements and 59,950 branches, plus 726 currently excluded lines;
  * the prohibited-exclusion count remains 304 ``# pragma: no cover``
    occurrences across 104 shipped modules.

There is still no honest current whole-library coverage percentage. The best
frozen statement-only run (2026-08-26) measured 473 non-empty source files at
174,864 / 192,775 statements (90.709%), but 178 current shipped modules have
changed since that snapshot and 16 are new. Only 292 current shipped modules
are byte-for-byte identical to a row in that report; their defensible carried
forward result is 72,317 / 78,671 statements (91.923%), with 137 modules at
100%. Branches were not enabled in that run, so it supplies no branch
numerator and must not be presented as satisfying this instruction.

The first current branch-measured slice now has nine modules at exactly 100%:
437 / 437 statements and 170 / 170 branches. Behavioral tests closed real
gaps in ``regression_families``, ``annotation_umap_qc``, ``well_scope`` and
``figures.spread``; the other five verified modules are ``cli_leakage``,
``cli_plugins``, ``column_groups``, ``image_colors`` and ``training_basis``.
This is a measured gain and a reusable starting slice, not a substitute for
the missing frozen whole-library branch run.

--------------------------------------------------------------------------------
THE CONTRACT
--------------------------------------------------------------------------------

1. GITHUB ISSUES

Read every open issue against the current code. Close an issue only when its
acceptance criteria are implemented and verified, or when the issue is a
demonstrable duplicate/obsolete report with that evidence recorded. Do not
roll back a later implementation to match an older issue. An issue that needs
external information remains explicitly open rather than being declared fixed.

2. ONE GREEN COMMIT

Every required GitHub workflow and every job in those workflows must complete
successfully on the same final commit. A cancelled, skipped-by-error, stale-
branch or superseded run is not a passing badge. Local tests are diagnostics;
the GitHub run is the release evidence.

3. 100% FOR EACH MODULE

Measure statement and branch coverage per source module on a frozen worktree.
Every module reaches 100%; one heavily tested module cannot average away an
untested one. Tests exercise observable behavior and contain meaningful
assertions. Do not use ``# pragma: no cover``, omit files from configuration,
or delete reachable code merely to alter the denominator. Truly dead code may
be removed only after proving it is unreachable and unneeded.

The renewed request says "the library", so the denominator is every shipped
Python module in the distributions, not only ``spacr/`` files that happen to
be imported by the default test command. Package discovery, the coverage
inventory and the wheel/sdist contents must agree. A shipped module missing
from the inventory is a failure, not zero work hidden outside the report.

4. KEEP THE RESULT RATCHETED

CI fails when a new module lacks a coverage row, when any existing module
drops below 100%, or when a prohibited coverage exclusion appears. The report
artifact identifies uncovered lines and branches by module.

--------------------------------------------------------------------------------
HOW TO KNOW IT WORKED
--------------------------------------------------------------------------------

  * GitHub reports no open actionable issue for the spaCR repository, with a
    recorded disposition and verification link for every issue examined;
  * all required workflows are green on one SHA that equals the final nightly
    head, including every supported Python, operating-system, packaging,
    installer, documentation, integration, network, GPU and slow-test job;
  * a clean frozen-worktree coverage run reports 100% statements and branches
    for every spaCR module, with no omitted module and no ``pragma: no cover``;
  * tests fail after deliberately adding an uncovered executable line or a new
    unlisted module, proving the per-module guard is active;
  * the README badge resolves to the successful required workflow rather than
    a cancelled or non-required run;
  * publication may proceed only after all of the above remain true on the
    commit proposed for publication.

THE FAILING-TEST COUNT, RESOLVED (2026-08-30)
---------------------------------------------
The green-CI clause was being read against ~421 failing tests. Running each
failing FILE on its own showed what that number actually was:

    160 files with failures in the full run
    146 pass alone            -- isolation artifacts, not defects
     14 fail alone            -- the real list

Of the 14, nine are now green and each is written up in its own commit:

  test_instruction_index.py        stale generated index during an in-flight
                                   instruction move; rebuilt
  test_test_suite_hygiene.py       four assertion-free tests of mine, given
                                   real assertions rather than a ratchet entry
  test_cov_2_ome_zarr.py           asserted an optional extra was ABSENT, which
                                   is a fact about the machine; now stated
  ...search_strip / launch probes  child interpreters built an environment from
                                   scratch and lost CONDA_PREFIX, so Qt could
                                   not find its platform plugin (tests/child_env.py)
  ...backdrop_is_rounded           a real defect; see 310 section 14
  ...hotkey_map_is_the_whole_map   a real gap; Q was bound and on no map
  ...window_comes_before_dialog    a real regression; five stylesheet
                                   compositions per launch, see 314
  test_registration_seams.py       the restyle mutated the registry the tests
                                   were observing
  test_the_name_is_spaCR.py        my own module mis-cased the project name

FIVE REMAIN, AND THEY ARE ONE ITEM, NOT FIVE
  tests/qt/test_external_i18n_catalogs.py
  tests/qt/test_i18n_caption_ratchet.py      (partly; my captions are clear)
  tests/test_documentation_i18n.py
  tests/test_api_i18n_extractor.py           (partly; my dunders are clear)
  -- all say the same thing: the public documentation inventory grew by 82
  symbols and the nine locale catalogs have not been regenerated for them.

    public API surface   8694, ratchet at 8612   (+82)
    documentation keys   8813, ratchet at 8731   (+82)

Eighteen of the 82 are mine (`spacr.resource_log`). The rest are the
field-browser/quarantine and flowview work, whose source AND catalogs are
uncommitted in the shared tree right now. Closing this means running
tools/build_documentation_i18n.py over nine locales and reviewing the diff,
which would overwrite that in-flight work -- so it belongs to whoever lands
the last of those modules, not to the coverage lane. The numbers above are
what that change has to move.

NOT counted as failures: the two `test_i18n_caption_ratchet` cases still red
are the generated field-browser strings ('Active merged copy', 'Segmentation
QC field browser', ...) from the same uncommitted work.

THE BIGGEST SINGLE UNLOCK IS NOT MORE TESTS (2026-08-30)
---------------------------------------------------------
24% of the remaining coverage gap is in modules that import a dependency
setup.py DECLARES AS REQUIRED and this environment does not have.

`dependencies = [...]` at setup.py:167 is what `install_requires` is set from,
so these are not extras. Nine of its 60 entries are not importable here; one
(win10toast) is correctly absent because it is Windows-only. The other eight:

    pyfixest>=0.40.1,<1 ; python_version >= "3.10"
    glum>=3.1.2,<4      ; python_version >= "3.10"
    gpytorch>=1.11,<2   ; python_version >= "3.10"
    vispy>=0.14,<1.0
    umap-learn>=0.5.11,<1.0
    biopython>=1.80,<2.0
    gputil>=1.4.0,<2.0
    nvidia-ml-py>=11.450.51,<14

What their absence costs, measured against the batched run:

    total missing arcs in spacr/            5566
      in modules importing an absent dep    1328   (24%)

        598  vispy      spacr/qt/widgets/fractal_travel.py (595) is the single
                        largest gap in the package and it is a vispy widget
        273  pyfixest   spacr/ml.py -- the absorbing regression backend
        217  GPUtil     spacr/qt/screens/app_screen.py
        216  umap       settings_model (111), utils (48), timelapse (42),
                        hyperparam (15)
         14  pynvml     spacr/qt/widgets/home.py
         10  Bio        spacr/sequencing.py

These are not untested because nobody wrote tests. Several DO have tests, sitting
behind `@pytest.mark.skipif(not _HAS_GLUM, ...)` and its siblings, which skip
silently and count as passes. A reader of the suite output cannot tell them
from tests that ran.

NOT DONE, AND DELIBERATELY. `pip install` of these eight would touch the user's
conda base environment: umap-learn pulls numba, gpytorch pulls linear_operator,
and both pin against numpy/scipy versions the rest of a working scientific
stack depends on. Restoring a declared dependency set is defensible; doing it
unattended to a machine somebody works on is not, and a broken numpy would cost
far more than 1328 arcs are worth. The command, for whoever decides:

    pip install 'pyfixest>=0.40.1,<1' 'glum>=3.1.2,<4' 'gpytorch>=1.11,<2' \
                'vispy>=0.14,<1.0' 'umap-learn>=0.5.11,<1.0' \
                'biopython>=1.80,<2.0' 'gputil>=1.4.0,<2.0' \
                'nvidia-ml-py>=11.450.51,<14'

Two things follow either way. Instruction 308 is about setup.py declaring every
dependency on every OS and Python; this is the other direction -- the
environment the suite runs in does not match what setup.py declares, and
nothing in CI notices. And any per-module coverage target has to say which
environment it is measured in, because on this one eight modules cannot reach
100% however many tests are written.

    REFINED, SAME CONCLUSION. The figure above was reached with a regex over
    the source, which also matched prose inside docstrings -- it scored "a",
    "the", "disk" and "everybody" as missing packages and briefly read 56%.
    Re-run with an `ast` parse of the real import statements:

        total missing arcs in spacr/                     5566
        in modules importing an absent package     1279   (23%)

          598  vispy       fractal_travel.py (595)     -- REQUIRED
          288  catboost    ml.py (273)                 -- extra
          179  anndata     anndata_export/ (179)       -- extra
          130  trackastra  timelapse_preview.py (88)   -- extra
           74  jax         power_model.py (74)         -- extra
           10  (other)

    So it is not only the eight required ones: optional extras gate another
    ~680 arcs, and tests/test_anndata_export.py is skipped in its ENTIRETY --
    every one of its assertions, on the largest single non-Qt module in this
    list. That skip is legitimate; the point is that the suite's output says
    "1 skipped" and a reader has no way to learn what that cost.

    23-24% either way. The recommendation does not change, and neither does
    the reason for not acting on it unattended.

A FULL DISK IS THE OTHER WAY A COVERAGE RUN LIES (2026-08-30)
--------------------------------------------------------------
Caught with 405 MB left on `/` while the second batched run was writing into
it. The consumer was /tmp/pytest-of-olafsson: 19 GB across 101 abandoned
per-run temp directories, from this session's runs and the other lane's
together.

pytest keeps the last three runs' `tmp_path` trees on purpose, so a developer
can look at what a failing test wrote. It keeps them PER RUN, and a suite that
is run a hundred times in a night is a hundred trees. Nothing removes them.

Two things worth remembering.

  * `fuser -m <dir>` does not answer "is this directory in use". The -m means
    "the filesystem this path is on", so it reported all 101 as busy and the
    first cleanup removed nothing. Matching on the run counter -- keep the
    newest 25, drop the rest -- is what worked.

  * A coverage run that dies on ENOSPC mid-batch loses that batch's data the
    same way a lost xdist worker does, and reports a lower number without
    saying why. The disk is therefore part of the measurement, not part of
    the environment.

Freed 19 GB; `/` went from 405 MB to 21 GB and the run continued. Worth a
line in whatever ends up being the standing instruction for these runs: check
the disk before starting one, and clear /tmp/pytest-of-olafsson down to the
newest handful.

UNCOMMITTED TEST DELETIONS IN THE SHARED TREE (2026-08-30, unresolved)
-----------------------------------------------------------------------
Noticed while checking this lane's git state, NOT acted on. The working tree
has 19 deleted test files that are not this lane's work:

  tests/flowview/  (14 files)   -- deleted alongside spacr/flowview/ itself,
                                   which is coherent: the module is gone.
  tests/test_installed_import_path.py               5 tests
  tests/test_known_bug_contracts.py                 4 tests
  tests/test_optional_dependency_python_markers.py  4 tests
  tests/test_pyinstaller_spec.py                    4 tests  (spacr.spec is
                                                    also gone, so consistent)
  tests/test_timelapse_model_feature_schema.py      3 tests

The flowview set and the pyinstaller one follow their targets out of the tree.
The other three do not obviously: `07_known_bugs_not_fixed.txt`, which
test_known_bug_contracts.py checks, is still there.

That is 20 contracts leaving the suite in an uncommitted state, in a directory
this lane owns. Left exactly as found -- it is somebody's work in progress and
reverting it would destroy it. Recorded because a deletion that lands with no
note reads later as a test that never existed, and because any coverage figure
measured after it is measured against a smaller suite.

WHOEVER LANDS THIS: say in the commit why each file went. Three of them name
targets that are still in the tree.

DO NOT UNION TWO COVERAGE RUNS TAKEN ON DIFFERENT COMMITS (2026-08-30)
------------------------------------------------------------------------
Tempting arithmetic, tried and discarded. Two batched runs exist, one of them
missing a batch, so the obvious move is to take the better of the two per
module and call the result a floor. It reads 98.68%, and it is wrong.

The reason is section 7's trap wearing a different hat. An arc is a PAIR OF
LINE NUMBERS. Between the two measured commits, 22 source files changed, so
run 1's arcs for those files name lines that are now something else -- and
counting them as covered credits the newer file for whatever the older one
executed. The 0.74 points that separated the union from the measured figure
were almost entirely that.

Restricted to files IDENTICAL in both measured revisions, the union is 98.06%
of 248,695 arcs, and the two runs differ by:

    arcs run 1 had and run 2 did not     46
    arcs run 2 had and run 1 did not     66

112 arcs out of 248,695 -- 0.045%. THE MEASUREMENT IS REPRODUCIBLE. Two runs
of the same suite on the same source agree to within a twentieth of a percent,
which is worth knowing in both directions: a coverage change smaller than
about 0.05% is noise, and a change larger than that is real and has a cause
worth naming.

So the rule is: quote one run, on one commit, and say which. Combine runs only
across files whose bytes are identical, and say that is what you did.

PROGRESS NOTE, 2026-09-01 (Qt lane)
-----------------------------------
Pragma count in spacr/qt: 88 -> 33. Fifty-five sites resolved, each one
either DRIVEN with the line verified EXECUTED via coverage JSON, or
DELETED with the premise it rested on pinned by a test.

What the sweep actually found is worth recording, because it changes how
much a `# pragma: no cover` is worth as evidence:

  THE COMMENTS WERE WRONG MORE OFTEN THAN THE CODE.

  * Three sites claimed "JobRunner always returns True today". It
    returns False whenever it is unthreaded and the work or the
    completion callback raises -- which is exactly how those panels are
    built in tests.
  * One claimed "60 doublings passes any finite limit". The walk covers
    step * 2**59, so a small enough step against a large enough limit
    exhausts it, and an infinite limit exhausts it outright.
  * TWO marked lines that were covered all along, by tests that had been
    driving them for as long as they existed.
  * One import-time stylesheet guard was labelled "decoration is not
    load-bearing" -- true at runtime, backwards at import, where the
    failure is not a missing background but the module not importing and
    taking down whatever imports it.
  * Fourteen carried no reason at all.

AND THE TESTS WERE NOT MUCH BETTER. The hygiene suite's
assertion-free ratchet was already failing when this lane started; it is
green now, thirty-four tests later. Two of those were not merely missing
an assertion -- they were testing something other than their name:

  * test_arrays_are_found_and_drawn drew nothing, because
    plot_images_and_arrays only reaches plt.subplots with one continuous
    image AND one label mask, and the test built two folders of the same
    random array.
  * test_a_verdict_is_drawn_onto_the_axis passed a STRING where
    draw_verdict reads verdict.level, so it raised AttributeError and was
    swallowed by the very guard the two tests below it exercise.

Four failure-swallowing skips are gone, and one order-dependent failure
(a laptop-mode test that asserted on caplog after another launch had
already reconfigured logging) is fixed.

Eight stale section assertions from the 2026-08-31 Home restructure are
fixed as well. Two of those were self-contradictory rather than merely
stale: test_feature_dictionary_panel asserted SECTION_EXPLORE IS in
SECTIONS while test_registration_seams asserts it is NOT, so one of the
pair had to fail whichever way the code went.

THE RULE THIS LANE RAN ON, since it caught something every time: after
writing a test for a named line, MEASURE that line. Five tests in this
batch passed without touching their target -- an editable install's
MetaPathFinder beating sys.path, a module-level function patched as
though it were a method, a guard sitting behind an earlier refusal that
was never satisfied. Every one of them was green.

STILL OPEN in this lane: the full tests/qt run segfaults at 34% in a
single process. Pre-existing and already tracked here; the affected
files pass when run directly.

ORDER-DEPENDENT FAILURES AND THE SEGFAULT, measured 2026-09-01
--------------------------------------------------------------
Both are real and both are INTERACTIONS, not single bad files. Recorded
with the evidence so the next attempt does not start from zero.

THE SEGFAULT. `tests/qt` segfaults partway through a single process --
at 34% on one run and 28% on the next, so the position moves with the
content rather than sitting on one test. Every one of the 21 files in a
subset that segfaulted was then run ON ITS OWN: none segfaulted, and
only one failed at all. So no file is individually responsible.

ORDER-DEPENDENT FAILURES. Seven tests in the
`-k "plugin or registration or section"` selection fail there and pass
when their files are run together. The obvious suspects were checked and
cleared: `SECTIONS` is derived live from `APPS`; `SECTION_NOTES` is
rebuilt by `_refresh_sections`; the conftest fixture restores APPS,
APP_FACTORIES, APP_STAGE, APP_META and the side tables and calls
`_refresh_sections`; and `unregister_app` refreshes too. The registry
mechanics are sound, so the leak is somewhere else.

WHAT WAS FIXED RATHER THAN RECORDED. Two order-dependent failures were
genuine and are gone: a Qt-warning test whose own file left the root
sink's LEVEL raised, and a laptop-mode test that asserted on caplog
after another launch had reconfigured logging. Both passed alone and
failed in a run. The pattern is worth naming -- a test that depends on
global state must pin it on the way IN as well as restoring it on the
way out, or a test running BEFORE it decides the answer.

--------------------------------------------------------------------------------
A MECHANISM FOR THE SEGFAULT, FOUND AND REPRODUCED (2026-09-01)
--------------------------------------------------------------------------------

A test that leaves a PARENTLESS Qt object owning a RUNNING TIMER for the
garbage collector to free will crash a LATER test's event loop.

REPRODUCED, both directions, in about a minute:

    pytest tests/qt/test_the_console_keeps_the_ai_error_analysis.py \
           tests/qt/test_track_previews.py::test_completion_handler_runs_on_the_gui_thread
    -> Fatal Python error: Segmentation fault
       Current thread ... qt_compat.py:160 in exec
                          wait_signal.py:58 in wait
                          test_track_previews.py:584

Each file passes ALONE. The faulting frame is inside `qapp.exec()` in the
SECOND file, which is why "every file was run on its own and none segfaulted"
came back clean and told us nothing: the guilty file is not the one that dies.

WHY. A `ConsolePanel` built in a test has no parent, so nothing owns it and
Python frees it whenever the collector next runs -- which can be in the middle
of another test's event dispatch. It owns `_WorkingDots`, a QTimer that is
still RUNNING. Destroying a Qt object with a live timer from inside event
dispatch is the crash. This is the same defect `spacr/qt/gc_policy.py` was
written for on the application side, one layer out: an object destroyed at a
moment nobody chose. THE SUITE DOES NOT INSTALL THAT POLICY, so tests remain
exposed to it.

THE FIX IS PER-FIXTURE AND CHEAP: stop the timer, then `close()`,
`setParent(None)`, `deleteLater()`, `processEvents()`. Stopping the timer
FIRST is load-bearing -- `deleteLater` has not run by the time the fixture
returns. Applied to `test_the_console_keeps_the_ai_error_analysis.py` and
`test_the_issue_preview_can_diagnose.py`; both crashes went away.

WHAT TO DO NEXT, in order:

1. Audit every `tests/qt` fixture that constructs a widget with no parent and
   returns it without teardown. `grep -l "= ConsolePanel()\|Panel(\|Dialog("
   tests/qt` is the starting set. Each is a candidate for the same crash.

2. DO NOT add a between-tests `gc.collect()` fixture. It was tried on
   2026-09-01 and it is the wrong answer twice over:

   * MEASURED NOT TO WORK. With the per-fixture teardown reverted and the
     collect fixture in place, the pair still dumped core. Raising it from
     `gc.collect(1)` to a full `gc.collect()` turned the crash into a plain
     failure but did not remove it. Whatever retains the panel is not a cycle
     the collector frees at that point.
   * AGAINST A DELIBERATE INVARIANT ALREADY WRITTEN DOWN HERE.
     `tests/qt/test_retained_widget_tree.py::
     test_no_boundary_fixture_forces_python_collection_over_live_qt_wrappers`
     exists to forbid exactly this, and calls itself "the CI segfault guard:
     ownership cleanup must stay on Qt's side". Its AST check inspects two
     named fixtures by name, so a third one slipped past it -- which is worth
     fixing on its own: the guard should check every autouse fixture in both
     conftests, or it only forbids what it happens to have been pointed at.

   The per-fixture teardown is therefore the fix, not a stopgap for it.

3. Only then re-measure whether the full-suite segfault is gone. Do NOT
   conclude it is fixed from a single clean run: the position moved between
   runs before, so absence on one run is not evidence.

NOTE the second symptom of the same cause: the same pairing can produce a
FAILURE rather than a crash (`test_the_issue_preview_can_diagnose.py` +
`test_track_previews.py` failed the track test without dying). So an
order-dependent FAILURE in `tests/qt` is worth checking against this mechanism
before it is assumed to be leaked global state.

--------------------------------------------------------------------------------
2026-09-02 — SEQUENCING IS 100.00% ON CURRENT SOURCE
--------------------------------------------------------------------------------

The stale batched11 report predated the final sequencing guard removal, so it
was not used as proof. A fresh branch-coverage measurement on current
``nightly`` ran the 278 sequencing-focused tests under the 115 GiB process-tree
guard, then appended the two source-specific test files that own barcode
mismatch and filter-column resolution. Result: 309 passed, one external real-
library test skipped because ``SPACR_GRNA_LIBRARY`` was unset, and
``spacr/sequencing.py`` measured 577/577 statements and 186/186 branches:
100.00%, with zero missing lines and zero partial branches.

This closes the sequencing module's part of 288. It does not change the
whole-repository percentage or claim that the remaining modules are current.

--------------------------------------------------------------------------------
2026-09-02 — TIMELAPSE IS 100.00% ON CURRENT SOURCE
--------------------------------------------------------------------------------

A fresh branch-coverage measurement on current ``nightly`` ran 868 non-Qt,
timelapse-focused tests under the 115 GiB process-tree guard: 840 passed and
28 optional-btrack tests skipped because that backend is not installed.  That
first selection measured 17 missing statements and 14 missing branch arcs.
The owning non-Qt contract/helper files were then appended to the same
coverage data in a second guarded run (216 passed).  No production or test
change was needed: those paths already had direct owners which the first
focused selection had not collected.

Final current-source result for ``spacr/timelapse.py``: 3330/3330 statements
and 1140/1140 branches, 100.00%, with zero missing lines and zero partial
branches.  This closes the timelapse module's part of 288; it does not make a
whole-repository coverage claim.

--------------------------------------------------------------------------------
2026-09-02 — PLOT IS 100.00% ON CURRENT SOURCE
--------------------------------------------------------------------------------

A fresh branch-coverage measurement on current ``nightly`` started with 41
direct plot test files (849 passed, one documented default-level case skipped)
and appended the lightweight non-Qt owners of the remaining shared figure
helpers.  The final five items were a supplied-title branch and the
per-bin clustered-model failure fallback.  Two direct residual tests now own
those behaviours, and both tests were watched fail against deliberately
broken source before the source was restored.

Final current-source result for ``spacr/plot.py``: 3041/3041 statements and
1144/1144 branches, 100.00%, with zero missing lines and zero partial branches.
This closes the plot module's part of 288; it does not make a whole-repository
coverage claim.

--------------------------------------------------------------------------------
2026-09-02 — SPACROPS IS 100.00% ON CURRENT SOURCE
--------------------------------------------------------------------------------

A fresh guarded run of the 18 direct non-Qt spacrops test files passed all 506
tests.  ``spacr/spacrops.py`` measured 2099/2099 statements and 844/844
branches, 100.00%, with zero missing lines and zero partial branches.  No
production or test change was needed.  This closes the spacrops module's part
of 288; it does not make a whole-repository coverage claim.

--------------------------------------------------------------------------------
2026-09-02 — DEEP_SPACR IS 100.00% ON CURRENT SOURCE
--------------------------------------------------------------------------------

A guarded run of the 45 direct non-Qt deep-spacr test files passed 1176 tests
and exposed 12 GPU-host-only test-isolation failures: three test modules said
they forced CPU by patching ``torch.cuda.is_available`` but did not clear the
shared accelerator result already cached during collection.  Their autouse
fixtures now pin both parts of that premise.  All 69 tests in the affected
modules pass together on this CUDA-equipped host.

The owning multiclass inference test was appended to the current-source
coverage data after the fixture repair.  Final result for
``spacr/deep_spacr.py``: 1831/1831 statements and 704/704 branches, 100.00%,
with zero missing lines and zero partial branches.  This closes the deep_spacr
module's part of 288; it does not make a whole-repository coverage claim.

--------------------------------------------------------------------------------
2026-09-02 — HYPERPARAM IS 100.00% ON CURRENT SOURCE
--------------------------------------------------------------------------------

The frozen report's six-item estimate predated the Activation-search API.  A
fresh guarded run of the 14 direct non-Qt hyperparameter files passed all 574
tests and exposed 32 statements plus 16 arcs in that new API.  Ten direct
residual tests now own its parameter translation, mask and randomisation
scores, unavailable-criterion diagnostics, grid route, model/source guards,
merged-array selection, unnormalised tar route and preloaded-data route.  A
deliberate seven-point source mutation made eight of those tests fail before
the source was restored; the restored suite passes 10/10.

Final current-source result for ``spacr/hyperparam.py``: 1489/1489 statements
and 610/610 branches, 100.00%, with zero missing lines and zero partial
branches.  This closes the hyperparam module's part of 288; it does not make a
whole-repository coverage claim.

--------------------------------------------------------------------------------
2026-09-02 — UTILS IS 100.00% ON CURRENT SOURCE
--------------------------------------------------------------------------------

The frozen report predated several current ``utils.py`` changes, so this close
was re-measured from scratch under the 115 GiB process-tree guard.  Four clean
coverage batches exercised the direct utility tests and the database, model,
mask, metadata, plotting and pipeline callers that own shared utility paths:
3417 tests passed and eight optional-AnnData tests skipped because that extra
is not installed.  A deliberately broader exploratory batch had exposed two
unrelated ``measure.py`` failures; that file was excluded from the clean proof,
so no failing run contributes to this result.

Two direct residual tests now own lazy-module reset/introspection and the rule
that a NULL measurement stamp is legacy 2-D/pixel data and cannot silently
accept 3-D rows.  Each test was watched fail against a deliberate source
mutation before the source was restored.  Final current-source result for
``spacr/utils.py``: 4824/4824 statements and 1906/1906 branches, 100.00%, with
zero missing lines and zero partial branches.  This closes the utils module's
part of 288; it does not make a whole-repository coverage claim.

---

2026-09-03. WHERE THE tests/qt SEGFAULT ACTUALLY IS, which matters because it
is what stops a whole-suite run and therefore a whole-repository number.

Ran 125 of the 993 `tests/qt` files in one process -- the ones matching
preview / setting / organelle / mask / object. It reached 82% and then died:

    Fatal Python error: Segmentation fault
    Current thread ...:
      File ".../pytestqt/plugin.py", line 220 in _process_events
      File ".../pytestqt/plugin.py", line 179 in pytest_runtest_setup

THE CRASH IS NOT IN A TEST. It is in pytest-qt's `_process_events`, called
from `pytest_runtest_setup` -- that is, BETWEEN tests, while draining the
event queue before the next one starts. A queued event is being delivered to
a C++ object that an earlier test has already destroyed, so the test that
dies is not the test that caused it and bisecting by failure point will name
the wrong file.

WHAT THAT RULES OUT AND IN. It is not a memory ceiling: RAM sat at 14-15 GiB
of 125 for the whole run. It is not one bad test, or the run would die at the
same count every time. It is accumulated state -- a widget deleted while
something it was connected to still has a queued event, most likely a
`QTimer` or a `deleteLater` that outlives its test.

THE TRACTABLE NEXT STEP, in order of cost:

  1. Run with `-p no:cacheprovider -x` over the same 125 files and record
     WHICH file the count reaches. Repeat three times: a moving boundary
     confirms accumulation and rules out a single culprit.
  2. The suspects are widgets that start a timer in `__init__` and are added
     with `qtbot.addWidget` -- `addWidget` closes and schedules deletion but
     does not stop a running timer. `AmbientWidget`, the preview panels and
     the benchmark controller all own timers.
  3. A `qtbot`-level autouse fixture that stops every child `QTimer` before
     teardown would test the hypothesis without changing any test.

NOT ATTEMPTED TONIGHT: this is a test-infrastructure change affecting every
Qt test, and the diagnosis above is worth more than a guess at the fix.

CORRECTION, same night, after running the control. THE TIMER HYPOTHESIS ABOVE
IS NOT SUPPORTED, and the segfault is not reliably reproducible.

Three runs of the same 125 files:

  1. no guard, while several other Qt processes were running: SEGFAULT at 82%
  2. with the timer guard, machine otherwise quiet: completed, 11m01s
  3. NO GUARD, machine otherwise quiet: completed, 11m12s

Run 3 is the control I should have run before writing the diagnosis. It
completed with byte-identical results to run 2 -- 13 failed, 2933 passed, 24
skipped -- so the guard changed nothing and the difference between run 1 and
the rest is not the guard.

WHAT THE EVIDENCE ACTUALLY SUPPORTS: the crash is load-dependent and
intermittent. It happened once, while this session was running other
offscreen Qt processes against the same repository. It did not happen twice
on a quiet machine. The stack in `_process_events` is still where it died,
and is still consistent with an event reaching a destroyed object -- but a
stack from one crash is not a cause, and I presented it as one.

SO THE USABLE FINDING IS THE OPPOSITE OF WHAT I WROTE: the suite is not
blocked. 125 of the 993 `tests/qt` files run to completion in eleven minutes
on a quiet machine. Whoever picks up the whole-repository number should
budget for an intermittent crash under concurrent load and re-run, rather
than treat the segfault as a wall to be dismantled first.

AND THE 13 FAILURES ARE REAL AND WERE INVISIBLE. Not because of the segfault
-- the run completes -- but because nobody had run these 125 files together.
Every one of them predates tonight. They are listed and being worked through
separately; several were tests asserting behaviour that a later instruction
deliberately replaced.

--------------------------------------------------------------------------------
2026-09-03 -- THE DOCS JOB, WALKED FORWARD THREE GATES, AND WHY THE FOURTH IS
              NOT TO BE FIRED YET
--------------------------------------------------------------------------------

`docs` had failed on every push. Three separate defects were in the AUDIT
rather than in anything it audits, and each hid the next.

  1. A PROTECTED LITERAL, EATEN BY ITS OWN DISAMBIGUATOR. `RUNNING` is a value
     the API reports and sits in `_PROTECTED_TERMS` beside QUEUED, FAILED,
     SKIPPED and SUCCESS. The sense transforms run case-insensitively -- they
     must, "running" being ordinary English almost everywhere it appears -- so
     the expansion to "executing" destroyed the literal and `_syntax_preserved`
     refused the document. Guarded on the case, because the case IS the
     distinction. NOT by masking every protected term: `_CONTEXT_HARD_PROTECT_RE`
     deliberately leaves product names visible so a phrase can repair grammar
     around them, and one of the run transforms lists `GUI` among its own
     alternatives.

  2. A FALSE POSITIVE IN THE CHECKER. Emphasis was counted with a pattern that
     excludes newlines, so `*present in every table this\nrun writes*` did not
     count while the same span on one line did. Expanding "run" pulled it onto
     one line, the count went 1 -> 2, and a document with every literal intact
     was rejected for having been REFLOWED. Lines inside a paragraph are now
     joined before counting; a blank line is not, because a paragraph break
     really does end inline markup. Mutation-tested: dropped emphasis, added
     emphasis, dropped strong and cross-paragraph asterisks all still fail.

  3. SEVEN STALE REVIEWED RECORDS. Both audits abort on the FIRST, so the total
     had never been known. Measured: 7 of 4,339 (de 1, hi 2, is 4). The larger
     numbers a first pass gives are wrong in instructive ways -- 35 name symbols
     no longer in the public API, which the audit already skips on purpose, and
     43 were already retired. Retired, not deleted, per the mechanism at
     build_documentation_i18n.py:5798.

The audit now RUNS TO COMPLETION and reports findings instead of dying with a
traceback. That is the difference between a broken gate and a working one.

--------------------------------------------------------------------------------
WHAT IT REPORTS, AND WHY `--sources-only` IS STILL "ONCE, AT THE END"
--------------------------------------------------------------------------------

199 findings over 67 symbols, every one in `en/`: the English API manifest is
behind current source. The obvious fix is the regeneration HANDOFF already
lists as owed, `tools/build_documentation_i18n.py --sources-only`, which needs
neither OpenCC nor a translation model and writes exactly one file.

IT WAS NOT RUN, and the measurement is the reason:

    stored en.json symbols                        8 943
    current source symbols                        9 405
    entries whose text differs                      510
    in source but MISSING from the manifest         472
    in the manifest but gone from source             10

8,943 is the same number the last audit recorded, so the manifest has not
moved while the source grew by 472 symbols. Regenerating it would admit those
472 to the gate at once, and NONE of them has a translation in any of the nine
locales -- trading a 199-finding English staleness for a nine-locale deficit
several times larger, and one that cannot be closed without the model and a
reviewer. That is the same trap the sibling tool sprang on 2026-09-03, when
`build_i18n_catalogs.py --sources-only` took its file from 8 failures to 10 for
precisely this reason, at smaller scale.

The API surface is also still moving, and this session moved it: the dock
rewrite added `spacr/qt/widgets/dock.py`. HANDOFF's "the API ratchet, once, at
the end -- it moves while agents run" is the correct sequencing and this
measurement is the evidence for it. Regenerate when the surface is frozen, and
budget the nine-locale pass that has to follow it.

--------------------------------------------------------------------------------
STATE OF THE THREE WORKFLOWS ON nightly
--------------------------------------------------------------------------------

    compat-matrix   green
    tests           long; not the blocker examined here
    docs            red, now on a REPORTED finding rather than a crash

Also fixed toward this item today, and recorded in their own commits: the 21
F821 undefined names that failed the repo-wide fatal lint gate (`ruff check
spacr tests --select E9,F63,F7,F82` now passes), and the two i18n tests that
were asserting opposite things about the README language picker.

--------------------------------------------------------------------------------
2026-09-04 -- A DIALOG CLIPPING TEST THAT FAILS ONLY IN COMPANY
--------------------------------------------------------------------------------

Found by a 707-test i18n sweep run for an unrelated reason, and recorded here
because suite-level order dependence is this item's territory rather than 350's.

    tests/qt/test_the_text_fits_sweep_dialogs.py
        ::test_no_dialog_caption_is_cut_off[BarcodeRegexDialog-de-2.0]

    alone                                    PASSES
    with its own file (61 tests)             PASSES
    inside `-k "i18n or translat or caption or language"` (707 tests)   FAILS

MEASURED BOTH WAYS rather than inferred, and it is the same shape as
instructions 345 and 346 -- a test that is green by itself and red in company,
which means the suite is carrying state between tests. 350's clipping method is
not in question here: it passes when it is allowed to measure a settled layout.

WHY IT MATTERS TO 288 SPECIFICALLY. This item's own blocker is that `tests/qt`
has no clean full-suite signal, and its section on the crash records widgets
outliving their tests. A caption that measures as clipped only when 700 other
tests have run first is consistent with that: font scale, style or a leaked
top-level widget surviving into this test's measurement. It is a THIRD symptom
of the same suspected cause, after the crash and the deceleration.

NOT CHASED HERE, deliberately -- this was found while closing 306/316 work and
chasing it now would abandon the release gates for it. What the next attempt
should do is bisect the 707 selection rather than read the test: run it with
`-p no:randomly` against halves of the selection until the smallest set that
reproduces it is named, then look at what that set leaves behind.
