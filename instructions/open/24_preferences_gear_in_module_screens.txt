================================================================================
A PREFERENCES GEAR BESIDE COPY CONSOLE ON THE MODULE SCREENS
================================================================================

Status:    REOPENED 2026-08-09 -- the button works, the ICON does not
           render. Original status:    done 2026-08-08 - gear on every AppScreen right of Copy console, routed through the MainWindow's own dialog.
Requested: 2026-08-06 - "add a preferences button in the mask, measure, classify moduals to the right of copy console (att it as a clickable gear icon)"

--------------------------------------------------------------------------------
WHAT THE STATE IS
--------------------------------------------------------------------------------

Not started. The module screens are built by AppScreen
(spacr/qt/screens/app_screen.py); "Copy console" lives on the console panel
(spacr/qt/widgets/console_panel.py). Find which of the two owns the row
before adding to it.

--------------------------------------------------------------------------------
WHY IT MATTERS
--------------------------------------------------------------------------------

Preferences is currently reachable only from the menu bar, which on macOS is
the menu that is currently broken (instruction 23). A control on the screen
the user is already looking at does not depend on that being fixed.

--------------------------------------------------------------------------------
WHAT TO DO
--------------------------------------------------------------------------------

1. Add a QToolButton with the gear icon from the existing iconset - do not
   introduce a new glyph - immediately right of Copy console.
2. Open the SAME dialog the menu opens: `PreferencesDialog(self).exec()`,
   via spacr.qt.dialogs.detach_from_window_manager (already applied inside
   PreferencesDialog, so just calling it is enough).
3. DECIDED 2026-08-06 by the user: put it on the SHARED row, so every
   module screen gets it rather than only mask/measure/classify. A gear on
   three of sixteen screens is its own kind of confusing.
4. Tooltip, and an accessible name. It is an icon-only button.

--------------------------------------------------------------------------------
HOW TO KNOW IT WORKED
--------------------------------------------------------------------------------

The button appears on the named screens, and clicking it opens the same
dialog as the menu item. A test that finds it by objectName and asserts the
dialog type it opens.

--------------------------------------------------------------------------------
2026-08-09 - REOPENED: THE BUTTON IS THERE, THE GEAR IS NOT
--------------------------------------------------------------------------------

  "if i click to the right of copy console i get the preferences but i
   cannot see the gear that should represent the button."

So the hit area and the wiring are correct and only the ICON is missing --
which is a narrower bug than it sounds and probably one of:

  * the gear glyph is not in the bundled icon set under the name the
    button asks for, so `themed_pixmap` returns None and the button
    renders empty;
  * it is being re-inked to the background colour and is invisible
    rather than absent -- check against BOTH themes, since a glyph that
    vanishes on dark may be fine on light;
  * the icon is set but the button has no visible size, so it paints into
    zero pixels.

Distinguish them before fixing: `themed_qimage` returning None is a
different bug from a correct image drawn in the wrong colour. Note the icon
cache added on 2026-08-09 stores re-inked icons on disk under
~/.spacr/icons -- if the icon was ever cached wrong it will stay wrong, so
clear that directory when testing.
