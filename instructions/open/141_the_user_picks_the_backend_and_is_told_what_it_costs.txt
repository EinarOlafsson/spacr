================================================================================
THE USER PICKS THE BACKEND, AND IS TOLD WHAT EACH ONE IS
================================================================================

Status:    filed 2026-08-18.
Requested: 2026-08-18 --

  "i would like to add anything and everything that could speed up any of the
   regression steps, not just the mixed model ... so the user will choose
   Regression type and backend (i think this is the correct term?) (default to
   statmodels) and if the backend uses GPU it should state that so cuML (GPU),
   Statmodels (CPU), etc. the options that are available via that method
   should be choosable the others grayed out, the text box should discribe all
   of the packages that are available and what they do, briefly and linkt the
   the API for each."

Related: 132 is the model text box this extends. 140 is the cost the box has
         to state. 106 is the greying rule.

--------------------------------------------------------------------------------
THE DEPENDENCY TEST, RUN FIRST BECAUSE IT WAS ASKED FOR FIRST
--------------------------------------------------------------------------------

"first test if adding any of those dependencies causes any problems".

MEASURED 2026-08-18 with `pip install --dry-run --report` against the real
environment, comparing every resolved version with what is installed:

    package        new packages   changes to existing
    pymer4                  1     NONE
    gpytorch                2     NONE
    numpyro                 4     NONE
    glum                    5     NONE
    linearmodels            6     NONE
    pyfixest               12     NONE

EVERY ONE IS PURELY ADDITIVE. Not one of them upgrades or downgrades numpy,
scipy, pandas, torch or scikit-learn, which was the risk worth testing -- a
resolver that quietly moves numpy under a package doing image analysis is how
a working install stops working.

AND pymer4 NO LONGER NEEDS R. Checked on PyPI: pymer4 0.9.2, `requires_python
>=3.10`, rpy2 NOT among its requirements, and it adds exactly ONE package to
this environment. The maintainer asked specifically whether lme4 could be had
without installing or interfacing with R; on this version, yes.

GPU is present and works: torch 2.9.1+cu128, CUDA available, RTX 3090, 25.3 GB.

--------------------------------------------------------------------------------
A. TWO CONTROLS, NOT ONE
--------------------------------------------------------------------------------

`regression_type` says WHAT is fitted -- ols, mixed, lasso, rra and the rest.
A new `regression_backend` says WHO fits it. They are independent: the same
mixed model can be fitted by statsmodels or by lme4, and the answer should be
the same while the time is not.

DEFAULT `statsmodels`. It is what every existing result was produced with, it
needs no new dependency, and a default that changes the numbers under a user
is not a default.

--------------------------------------------------------------------------------
B. WHAT THE BACKENDS ARE, AND WHAT EACH IS FOR
--------------------------------------------------------------------------------

The box describes each briefly and links its API. Measured or stated cost,
never "may be faster".

  statsmodels (CPU)   The default. Inference-first: coefficients, standard
                      errors, p-values and the diagnostics the QC suite reads.
                      Generic dense linear algebra, which is why `mixed` is
                      slow -- measured at 54x OLS on 40 genes and 67x on 80,
                      the ratio RISING with screen size (instruction 140).
                      https://www.statsmodels.org/stable/api.html

  pymer4 / lme4 (CPU) The reference implementation for mixed models. Uses a
                      sparse Cholesky over the nested structure rather than
                      dense algebra, which is an ALGORITHMIC win rather than a
                      hardware one. Version 0.9.2 needs no R.
                      https://eshinjolly.com/pymer4/

  torch (GPU)         spaCR already depends on torch and already fits a model
                      with it (`spacr.power_model`). The mixed model's profiled
                      REML objective can be written directly and optimised on
                      the GPU. MEASURED on this machine: the dense Cholesky
                      that dominates each iteration at q=1212 takes 204 ms on
                      the CPU and 7.69 ms on the RTX 3090 -- 27x, on the
                      operation the fit spends its time in.
                      https://docs.pytorch.org/docs/stable/index.html

  cuML (GPU)          RAPIDS' GPU ridge / lasso / elastic-net, near drop-in
                      for scikit-learn. Speeds the PENALISED backends. It has
                      no mixed model, so it does not touch that bottleneck --
                      say so rather than letting a user pick it and wait.
                      https://docs.rapids.ai/api/cuml/stable/

  pyfixest (CPU)      Absorbs high-dimensional FIXED effects by alternating
                      projections instead of building dummy columns. Directly
                      relevant here: `rowID + columnID` are real columns in the
                      design today, and absorbing them shrinks the problem
                      before any backend runs -- so this helps EVERY model,
                      which is what "not just the mixed model" asks for.
                      https://py-econometrics.github.io/pyfixest/

  glum (CPU)          Fast GLMs. Helps `poisson`, `logit`, `probit`,
                      `quasi_binomial`. https://glum.readthedocs.io/

  numpyro (GPU)       Bayesian, NUTS on the GPU. Gives posteriors rather than
                      point estimates plus SEs -- a DIFFERENT answer, not a
                      faster version of the same one, and the box must say so.
                      https://num.pyro.ai/

  gpytorch (GPU)      A linear mixed model IS a Gaussian process with a linear
                      kernel plus one kernel per nesting level.
                      https://docs.gpytorch.ai/

--------------------------------------------------------------------------------
C. THE LABEL SAYS GPU OR CPU, AND WHAT IS MISSING IS GREYED OUT
--------------------------------------------------------------------------------

Each entry reads "statsmodels (CPU)", "cuML (GPU)". A GPU backend on a machine
with no CUDA is greyed out and SAYS THAT, per instruction 106 -- disabled with
the reason on it, never absent and never silently falling back to the CPU
under a user who chose the GPU on purpose.

A backend that cannot fit the chosen `regression_type` is greyed out for that
reason instead: "cuML has no mixed model". So the two controls constrain each
other in both directions, and the box says which.

EVERY ONE OF THESE IS OPTIONAL. They go in extras, not core -- the dependency
test above says they are additive, not that a microscopy user should download
all of them. A backend whose package is not installed is greyed out with the
pip command that would provide it.

--------------------------------------------------------------------------------
D. THE ANSWER MUST NOT CHANGE
--------------------------------------------------------------------------------

The point of a backend is speed, so a backend that returns different numbers
is a bug, not a trade-off. Any backend added here needs a test that fits the
SAME screen through it and through statsmodels and asserts the coefficients
agree to a stated tolerance -- and where it cannot agree by construction
(numpyro's posteriors, cuML's penalised paths), the box says what differs.

--------------------------------------------------------------------------------
HOW TO KNOW IT WORKED
--------------------------------------------------------------------------------

* `regression_backend` exists, defaults to statsmodels, and an old settings
  CSV still loads.
* Every entry is labelled (CPU) or (GPU).
* A missing package, a missing GPU and an incompatible regression_type each
  grey the entry out with their own reason.
* The box describes every backend and links its API.
* A mixed fit through pymer4 or torch agrees with statsmodels on the same
  screen, and the run says which backend produced it.

--------------------------------------------------------------------------------
2026-08-18 -- THE BOX IS ON SCREEN. The panel half shipped.
--------------------------------------------------------------------------------

AUDITED FIRST, and the header of this file was right about the model layer and
silent about the only thing left. What was already true and was left alone:
REGRESSION_BACKENDS (8 entries, every label (CPU)/(GPU), package / pip / API
url / greying reason), the `regression_backend` setting defaulting to
'statsmodels (CPU)' with an old CSV still loading, `spacr/mixed_gpu.py`,
`ml.py::_require_backend`, and the panel placement (a3944e21).

WHAT WAS ACTUALLY MISSING, measured on the built panel before touching
anything:

    regression_backend was a PLAIN QComboBox with all eight labels and EVERY
    ONE OF THEM ENABLED.

`describe_backends` had been written and tested and NOTHING IN spacr/qt called
it -- confirmed by grep and by driving the widget -- so no description and no
API link was ever on screen; and no entry was greyed for any reason, so a user
could pick `numpyro (GPU)` (no package here, no fit routed through it) and
hear nothing until `_require_backend` refused the run. Handoff 0b exactly.

SHIPPED

* `settings_model._RegressionBackendField` -- the combo plus a QTextBrowser.
  It is the field widget for `regression_backend`, so it needed no change
  outside this territory. Entries are greyed from `backend_menu()`; the box
  is `describe_backends(..., compact=True)`; the links are real anchors and
  `setOpenExternalLinks(True)` opens them.
* It FOLLOWS `regression_type` (SettingsWidgets._refresh_regression_backend),
  because an entry greyed for one family is choosable for another -- cuML is
  refused under `mixed` and offered under `lasso`.
* `backend_status` gained `short_reason`: the same refusal at combo-entry
  length. THE REASON IS IN THREE PLACES because two of them are missable --
  in the entry's own text ("cuML (GPU)  --  no mixed model; fits lasso,
  ridge, elasticnet"), in full on the entry tooltip, and in the box, which is
  on screen whether or not the popup is open.
* `describe_backends(..., compact=True)`. HOW IT FITS INSTRUCTION 135: the
  full text is 3,101 characters, about ninety wrapped lines in a settings
  field. Compact writes the SELECTED backend out in full -- summary, measured
  cost, and the DIFFERENT ANSWER caveat where there is one -- and gives the
  other seven one line each: label, API link, first sentence, or the refusal.
  1,220 characters, and the box has a 168 px ceiling and scrolls, so the page
  is the same length whichever backend is chosen and whatever the machine has
  to say about the rest.
* A SELECTION THE NEW TYPE CANNOT FIT IS KEPT, not silently re-pointed at
  statsmodels. The box leads with "This run will be refused." and the
  sentence `_require_backend` will print. Re-pointing it would be the silent
  fallback C forbids, one step earlier than the fit.
* `backend_status` now says NOT INSTALLED alongside NOT WIRED UP instead of
  returning at the first of them. The unimplemented test came first, so on
  any machine without the extras -- which is every machine, they are extras --
  the pip command C asks for was shown by nothing, ever.

VERIFIED ON THIS MACHINE, through AppScreen("regression"), shown, with
isVisible() (isVisibleTo() is True inside a collapsed section and would have
proved nothing):

    incompatible regression_type   cuML under `mixed`      -> greyed, own reason
    missing package                torch, probe forced     -> greyed, "pip install torch"
    missing CUDA device            torch, probe forced     -> greyed, own reason
    all eight labels + all eight API URLs                  -> in the box, as anchors

`tests/qt/test_the_backend_box_is_on_screen.py`, 12 tests, all of which fail
without this change. `tests/test_mixed_gpu_backend.py` is 36 green, including
the mixed fit agreeing through torch and statsmodels.

WHAT IS TRUE AND SHOULD NOT BE FORGOTTEN

ONLY statsmodels AND torch FIT ANYTHING. pymer4, cuML, pyfixest, glum,
numpyro and gpytorch are INVENTORY ONLY: described so the plan is visible,
greyed out as not wired up, and on this machine not installed either -- both
sentences now on the entry, with the pip command. So the last acceptance line
("a mixed fit through pymer4 or torch agrees with statsmodels") is satisfied
by torch alone, and no package was installed to get there. The next agent to
wire one of the six needs D's agreement test, not just a call site.

FOUND IN PASSING, FIXED HERE:
`tests/qt/test_regression_panel_is_one_page.py::test_neither_box_needs_a_
horizontal_scrollbar` had been RED since instruction 138 landed. It asserted
"the widget does not soft-wrap, so the text arrives pre-wrapped", and both
halves stopped being true when `_wrap_block` began emitting one logical line
per paragraph and the box was set to WidgetWidth. Rewritten to pin the
invariant that survives: a line that must NOT break -- a formula -- is never
wider than explainer_width().

FOUND IN PASSING, NOT MINE TO FIX (outside this territory):
`spacr/qt/screens/cell_montage_view.py::CellMontageView` puts hover help on
its editable fields; `tests/test_tooltips_are_on_the_setting_not_the_field.py
::test_no_screen_puts_hover_help_on_an_editable_field` is red because of it
and was red before this work. The fix is one line -- call
`settings_model.retarget_field_tooltips(self)` at the end of its __init__.


================================================================================
G. THE SIX ARE NOT BROKEN, THEY ARE NOT WIRED -- AND THE ASK WAS WIDER
================================================================================

Asked 2026-08-18: "for the regression backends, only statsmodels and torch GPU
are available (even for lasso, mixed, ridge) etcetera. I dont know if this just
hasnt been implemented yet or if something is wrong with the implementation."

NOTHING IS WRONG. MEASURED through `backend_status` for five types:

    ols       statsmodels                pyfixest would fit it -- NOT ROUTED
    mixed     statsmodels, torch         pymer4/numpyro/gpytorch -- NOT ROUTED
    lasso     statsmodels                cuML would fit it -- NOT ROUTED
    ridge     statsmodels                cuML would fit it -- NOT ROUTED
    poisson   statsmodels                pyfixest, glum -- NOT ROUTED

The greying distinguishes the two cases correctly, which is what section C asked
for: "cannot fit regression_type='lasso'; it fits mixed" is a genuine capability
mismatch, and "is described here but spaCR does not route any fit through it
yet" is the honest admission. Only `torch` was ever wired, and only for `mixed`.

SO THE REMAINING WORK IS THE WIDER HALF OF THE ORIGINAL REQUEST, which asked for
"anything and everything that could speed up any of the regression steps, NOT
JUST THE MIXED MODEL" -- and the mixed model is the only thing that got faster.

THE DEPENDENCY QUESTION IS ALREADY ANSWERED AND THE ANSWER WAS YES. The
`pip install --dry-run --report` comparison at the top of this file measured all
six as PURELY ADDITIVE: not one upgrades or downgrades numpy, scipy, pandas,
torch or scikit-learn. The maintainer's condition -- "first test if adding any of
those dependencies causes any problems, if they dont then i would like to add
anything and everything" -- is met.

WIRE THEM IN THIS ORDER, WHICH IS BY HOW MUCH OF THE SCREEN THEY SPEED UP:

  1. pyfixest -- ABSORBS rowID/columnID INSTEAD OF BUILDING DUMMY COLUMNS.
     They are real columns in the design today, so absorbing them shrinks the
     problem before ANY backend runs. This is the one that helps every model,
     which is precisely what "not just the mixed model" asked for, and it is
     first for that reason rather than because it is the fastest single fit.
  2. cuML -- GPU lasso / ridge / elasticnet, near drop-in for scikit-learn,
     which is what the penalised types already use.
  3. glum -- fast GLMs: glm, poisson, logit, probit, quasi_binomial.
  4. pymer4 / lme4 -- the reference mixed-model implementation, a sparse
     Cholesky over the nested structure rather than dense algebra. An
     ALGORITHMIC win, and the best-tested LMM code that exists.
  5. numpyro / gpytorch -- last, because numpyro returns posteriors rather than
     point estimates plus SEs, which is a DIFFERENT answer and not a faster
     version of the same one. Section B already says the box must state that.

SECTION D IS NOT OPTIONAL FOR ANY OF THEM. "A backend that returns different
numbers is a bug, not a trade-off." Each one lands with a test that fits the
SAME screen through it and through statsmodels and asserts the coefficients
agree to a stated tolerance -- the torch backend set the precedent at 2e-5 on
the variance components. Where a backend CANNOT agree by construction
(numpyro's posteriors, cuML's penalised paths), the box says what differs, and
that sentence is part of the work rather than a follow-up.

AND EACH ONE'S SPEEDUP IS MEASURED, NOT CLAIMED, on a real screen, the way
torch's 25x end-to-end and 27x per-Cholesky were. A backend offered with an
unmeasured "may be faster" is the thing section B forbids.
