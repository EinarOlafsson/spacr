================================================================================
CLAUDE SHOWS AS A CONTRIBUTOR ON THE spaCR GITHUB PAGE
================================================================================

Status:    OPEN — the complete 2026-08-30 current-history rewrite is pushed
           and verified, and GitHub's REST/graph indexes list only the two
           human contributors. The separately cached homepage/sidebar still
           lists Claude and github-actions[bot]; GitHub Support must rebuild
           that server-owned cache and its immutable pull-request refs.
Requested: 2026-08-08 — "remove claude as a contributoa on the spacr github page"
           2026-08-08 — approving the rewrite, on being told it "changes
           every commit hash after the earliest rewritten one": "i want you
           to do this: changes every commit hash after the earliest
           rewritten one / but do this last on the instructions list"
           2026-08-12 — SCOPE WIDENED: "add to instructions remove claude and
           github-actions[bot] from all commits so they are no longer
           contributers"

--------------------------------------------------------------------------------
WHAT THE STATE IS
--------------------------------------------------------------------------------

Measured on `nightly`, last 400 commits:

    author  = olafsson / Einar work / Einar Olafsson    (all Einar)
    author  = github-actions[bot]                       (1)
    NO commit is authored or committed by Claude.

    38 commits carry a `Co-Authored-By: Claude ...` TRAILER.

That trailer is the whole cause. GitHub reads `Co-Authored-By` and credits
the named account in the contributors list and in per-commit attribution,
exactly as if it were a second author.

Going forward this is already prevented: the standing rule for this repo is
that no commit carries a Co-Authored-By trailer and the maintainer is sole
author. The 38 are historical, from before that rule.

--------------------------------------------------------------------------------
WHY IT MATTERS
--------------------------------------------------------------------------------

The contributors list is what a visitor reads as "who wrote this". For an
academic tool, authorship is not decoration -- it is the thing citation and
credit attach to, and a tool that appears to have a corporate co-author
invites questions the maintainer should not have to answer.

--------------------------------------------------------------------------------
WHAT TO DO
--------------------------------------------------------------------------------

There is no way to remove a contributor through the GitHub UI. Attribution
is computed from commits, so it changes only when the commits do.

OPTION A -- rewrite history. The only fix that removes the existing 38.

    git filter-repo --message-callback \\
        'return re.sub(rb"\\n*Co-[Aa]uthored-[Bb]y: Claude[^\\n]*", b"", message)'

    then force-push every affected branch, and re-push tags.

    THIS IS DESTRUCTIVE AND PUBLIC. Every commit hash after the earliest
    rewritten commit changes, which means:
      * open pull requests against the old hashes break;
      * anyone with a clone or fork must re-clone or hard-reset;
      * a Codex session is working in this repo concurrently and would be
        rebasing onto history that no longer exists.
    DO NOT RUN IT without the maintainer saying so, and coordinate the
    timing with whoever else has the repo checked out.

    APPROVED. This is the option to take. See the ordering rule below
    before doing it.

OPTION B -- NOT CHOSEN. leave history, stop the bleeding. Already true: no new commit
    adds the trailer. The 38 stay, and Claude stays in the list.

RECOMMENDED SEQUENCE
    1. Confirm the count against the DEFAULT branch too, not just nightly --
       `git log --grep='Co-Authored-By: Claude' -i --oneline | wc -l` on
       `main`/`master`. Contributor lists are computed from the default
       branch, so the number that matters may differ from 38.
    2. Get an explicit yes for the rewrite, and a time when no one else is
       working in the repo.
    3. Take a full backup clone first (`git clone --mirror`).
    4. Rewrite, force-push, then confirm the contributors page updated --
       GitHub can take a while and sometimes needs a support request for a
       cached contributor entry.

--------------------------------------------------------------------------------
HOW TO KNOW IT WORKED
--------------------------------------------------------------------------------

`git log --grep='Co-Authored-By: Claude' -i --oneline | wc -l` returns 0 on
the default branch, and the repository's contributors page lists only the
maintainer once GitHub recomputes.

--------------------------------------------------------------------------------
DELIBERATELY NOT DONE
--------------------------------------------------------------------------------

Not started, and not to be started unprompted. Rewriting published history
is not reversible for anyone who has already cloned, and this session has a
concurrent Codex session in the same repository. The measurement above is
free and was done; the rewrite is a decision, not a task.

--------------------------------------------------------------------------------
2026-08-08 - APPROVED, AND WHY IT GOES LAST
--------------------------------------------------------------------------------

The rewrite is authorised. The ordering is part of the instruction, not a
preference, and the reason is mechanical rather than cautious:

REWRITING CHANGES EVERY COMMIT HASH AFTER THE EARLIEST REWRITTEN COMMIT.
Any work committed before the rewrite is rewritten with it; any work
committed after it is built on the new history. Doing it in the middle
means every remaining task is done twice -- once on the old hashes, once
after a forced reset -- and anyone else in the repository has to re-clone
in the middle of their own work.

So it is the last thing that happens to this repository, and NOTHING should
be committed after it until the maintainer has re-cloned.

BEFORE RUNNING IT, in this order:

  1. Every other instruction in `open/` is finished or explicitly closed.
     That includes 48, the tutorials task, which belongs to a CONCURRENT
     CODEX SESSION -- its work has to be landed and pushed first, or the
     rewrite discards it or strands that session on dead hashes.
  2. Confirm the count on the DEFAULT branch, not just nightly. GitHub
     computes contributors from the default branch, so the number that
     matters may not be the 38 measured here:
         git log --grep='Co-Authored-By: Claude' -i --oneline | wc -l
  3. Full backup: `git clone --mirror` to somewhere off this disk. The
     rewrite is not reversible from the working copy.
  4. Confirm no open pull requests, or that their authors know.
  5. Rewrite, force-push every branch AND tags.
  6. Verify the count is 0 on the default branch, then check the
     contributors page. GitHub caches it; a stale entry can need a support
     request, so a page that still shows Claude an hour later is not a
     failed rewrite.

WHAT COULD STILL STOP IT. If, when the time comes, there are open PRs or
another session is mid-work, say so and ask rather than proceeding -- the
approval is for the rewrite, not for doing it at a moment that costs
somebody else their work.

--------------------------------------------------------------------------------
2026-08-12 - SCOPE WIDENED TO github-actions[bot], AND MEASURED
--------------------------------------------------------------------------------

Requested: "add to instructions remove claude and github-actions[bot] from
all commits so they are no longer contributers"

MEASURED ON THE FULL HISTORY, not the last 400:

    olafsson              1587      Einar
    Einar work             955      Einar
    Einar Olafsson         112      Einar
    EinarOlafsson           44      Einar
    SpaCr                   26      Einar
    Lophorinus              82      SOMEBODY ELSE -- DO NOT TOUCH
    jak18015                18      SOMEBODY ELSE -- DO NOT TOUCH
    github-actions[bot]      6      to be rewritten

    Co-Authored-By trailers 39      to be stripped

THE TWO HUMAN CO-AUTHORS ARE NOT IN SCOPE. `Lophorinus` and `jak18015` are
real contributors with 100 commits between them. The request names Claude and
the bot; a rewrite that swept up "everyone who is not Einar" would erase two
people's authorship, which is the opposite of what this instruction is for.
Whoever runs this must filter on the two named identities and nothing else.

WHAT THE BOT'S SIX COMMITS ARE: `chore(release): add spaCR <version>
installers`, produced by the release workflows. They are real commits with
real content; only the authorship is wrong.

AND THE REWRITE ALONE WILL NOT HOLD. Three places configure that identity:

    .github/workflows/online-installers.yml:250-251
    .github/workflows/release.yml:98-99
    .github/workflows/release.yml:274-275

Each does `git config user.name "github-actions[bot]"`. Rewriting the six
historical commits without changing these means the NEXT release puts the bot
straight back in the contributors list, and the rewrite has to be done again
-- after the hashes have already been published once. Change the workflows in
the same change, or the work is temporary.

The maintainer's own identity is the right replacement there:
`Einar Olafsson <einar.olafsson@gmail.com>`, which is what every other commit
in this repository uses.

ORDER, unchanged: this whole instruction is still LAST. It rewrites every
commit hash after the earliest rewritten one, so anything unpushed or
unmerged at that moment is stranded. The workflow-file change is the one part
that could safely land earlier -- it only affects FUTURE commits -- and doing
it early is worth considering precisely because it stops the six from
becoming seven.

--------------------------------------------------------------------------------
2026-08-15 - FINAL EXECUTION RECORD
--------------------------------------------------------------------------------

The pre-rewrite audit found 38 Claude co-author trailers and seven commits
authored by github-actions[bot] across all remote branch and tag history. (A
39th trailer exists only in a local stash and is not a published ref.) Future release
commit configuration already uses Einar Olafsson <einar.olafsson@gmail.com>;
the remaining workflow reference to github-actions[bot] is an actor guard, not
an author configuration. There are no open pull requests and neither main nor
nightly is protected against the approved force-push.

The rewrite changes only the two named attribution sources: strip the Claude
trailers and replace the exact bot author/committer identity with the
maintainer's identity. Lophorinus (82 commits at the audit) and jak18015 (22)
are explicit invariants and must remain unchanged. Branch-tip trees are also
recorded before the rewrite and compared afterward, proving content did not
change while commit identities did.

This record is deliberately committed immediately before the external mirror
backup and rewrite. The instruction forbids any commit afterward, so rewritten
SHAs, backup location, pushed ref counts and post-rewrite CI run IDs belong in
the operator's final report rather than in a self-contradictory later commit.

--------------------------------------------------------------------------------
2026-08-25 -- RE-AUDIT: THE REWRITE HELD. THE ACCEPTANCE GREP DID NOT.
--------------------------------------------------------------------------------

MEASURED AGAINST `origin/main`, which is the default branch and the only ref
GitHub computes contributors from:

    Co-Authored-By trailers                    0
    commits authored by github-actions[bot]    0
    Lophorinus                                82   preserved, as required
    jak18015                                  18   preserved, as required

Counted with `%(trailers:key=Co-Authored-By,valueonly)` over every commit --
git's own trailer parser -- not with a text search. `origin/nightly` is the
same. So the rewrite ran, was pushed, and did not touch the two human
co-authors.

THE GREP IN "HOW TO KNOW IT WORKED" IS WRONG, and returns 1 on a clean
repository. `git log --grep='Co-Authored-By: Claude' -i` matches anywhere in
the message, and the closeout commit 65ed5332 is titled

    "Claude shows as a GitHub contributor: 38 Co-Authored-By trailers, ..."

so the commit that RECORDS the fix is the one the check reports. GitHub reads
the trailer, not the prose, so that hit credits nobody. THE CHECK THAT
ACTUALLY MEASURES IT, and returns 0:

    git log origin/main \
        --format='%(trailers:key=Co-Authored-By,valueonly)' \
      | grep -c . || true

    git log origin/main --format='%an %cn' | grep -c 'github-actions\[bot\]'

STILL DIRTY, LOCALLY ONLY: this checkout's local `main` is still at the
PRE-rewrite tip (a843db1c) and carries all 38 trailers, as do roughly forty
stale local `ci-*` / `codex-*` topic branches. None of that reaches GitHub --
`origin/main` is clean -- but pushing local `main` would republish the 38.
Reset it to `origin/main` before anyone uses it.

--------------------------------------------------------------------------------
2026-08-29 -- REOPENED: SIX BOT COMMITTERS SURVIVED, AND THE SIDEBAR IS STALE
--------------------------------------------------------------------------------

A fresh audit used the object IDs returned by ``git ls-remote --heads --tags``
rather than stale local topic/archive refs. Current remote branches and tags
contain no Claude author, committer or Co-Authored-By trailer, and no commit is
authored by github-actions[bot]. However, six commits reachable from both
``main`` and ``nightly`` still have this exact committer identity:

    github-actions <github-actions@github.com>

The six commit IDs are ``ba68b3c7``, ``25479878``, ``3681d262``, ``ce8ce1e7``,
``695ad72d`` and ``513321df``. They are three duplicated 2025-04-11
``Auto-build docs`` events. Their authors were rewritten to EinarOlafsson, but
their committers were not. GitHub's commit REST response maps that committer
email to ``invalid-email-address``, not to ``github-actions[bot]``, and the
contributors API counts author email rather than committer text. These six are
therefore not the standard contributors-API source, but the request to remove
the Actions identity from all current commits still includes them.

A fresh mirror also exposed the more likely sidebar source: GitHub's read-only
``refs/pull/*`` namespace still retains 38 real Claude Co-Authored-By trailers
and seven commits actually mapped to ``github-actions[bot]``. They survive
under pull-request refs 94, 95, 96, 97, 99, 100, 102, 103, 104, 105, 106, 107
and 109 even though no current branch or tag reaches them. Repository owners
cannot force-push or delete those server-owned refs.

GitHub's current data layers disagree in a way a browser refresh cannot fix:

  * ``GET /repos/EinarOlafsson/spacr/contributors`` and
    ``/stats/contributors`` return only EinarOlafsson and jak18015;
  * the public ``contributors_list`` homepage fragment still returns four
    entries: EinarOlafsson, jak18015, claude and github-actions[bot], even with
    a cache-busting query and ``Cache-Control: no-cache``.

WHAT REMAINS, still last in repository order:

  1. after every product change is committed and pushed, make another verified
     off-repository mirror backup;
  2. rewrite only the six exact github-actions committer identities across all
     current remote branches and release tags, preserving every tree and both
     human contributors, then force-push with exact old-ref leases;
  3. verify all reachable remote author, committer and trailer identities;
  4. ask GitHub Support to detach/reindex the stale pull-request attribution
     and rebuild the repository homepage contributors cache, because the
     authoritative contributors API is already clean while the separately
     rendered sidebar fragment is not.

Do not attempt an empty commit, branch toggle, visibility toggle or browser
cache clear as a substitute. None removes the six reachable committer records,
and none provides a supported invalidation of GitHub's server-side sidebar
fragment.

The public contributors graph and both contributor REST endpoints have now
been checked and contain only EinarOlafsson and jak18015. The four-name result
is isolated to the public homepage fragment, so it is a GitHub-side indexing
problem after the current-history cleanup, not local browser cache.

--------------------------------------------------------------------------------
2026-08-30 -- COMPLETE CURRENT-HISTORY REWRITE; GITHUB SIDEBAR SUPPORT BLOCKER
--------------------------------------------------------------------------------

The maintainer explicitly moved this task forward and asked for the rewrite
immediately. A complete off-workspace recovery bundle was made first:

    /mnt/firecuda2/codex/backups/
      spacr-pre-contributor-rewrite-2026-08-30-4ba64525.bundle
    SHA-256:
      c725d3be6a307dc5457bde475680e79aacc66719985ee801e1f4b25c61bc51bf

The bundle contains all nine owner-writable branch/tag refs. The rewrite
changed attribution only: all 4,587 rewritten commit trees were byte-identical
to their predecessors, while the exact forbidden author, committer and trailer
identities fell to zero. The two real contributors were preserved.

The verified public tips after the atomic ref swap are:

    main       dc108c67541ea1adb1f55cd36d7e5225f311acd4
    nightly    3f6334a6b766289f9387a0f09a37666ff94c5ee3

Seven owner-writable release/example tags were rewritten in the same atomic
operation. The temporary staging tag was deleted and the main ruleset's
non-fast-forward/deletion protection was restored afterward.

Post-rewrite data at 2026-08-30 02:18 UTC proves the remaining disagreement is
inside GitHub rather than in a local clone or browser:

  * the contributor REST endpoint returns only EinarOlafsson (2,959) and
    jak18015 (18);
  * ``graphs/contributors-data`` returns only EinarOlafsson (2,909) and
    jak18015 (18);
  * ``/_sidebar`` and a clean unauthenticated Chrome render still return four:
    EinarOlafsson, claude, jak18015 and github-actions[bot];
  * the stale sidebar ETag ``W/\"2b96760e16e8d49bcae2a37652d21a1d\"`` did
    not change during the 02:02--02:18 UTC observation window.

A read-only audit of every advertised ref found the obsolete attribution only
in GitHub-owned refs the repository owner cannot update or delete:

    refs/pull/{94,95,96,97,99,100,102,103,104,105,106,107,109}/head

Their union retains 38 historical Claude co-author trailers and seven
github-actions[bot]-authored commits. Every owner-writable branch and tag is
clean. Therefore rewriting again, pushing an empty commit, toggling the default
branch or clearing a browser cache would not address the remaining state.

ONLY REMAINING ACCEPTANCE ACTION: submit the prepared evidence to GitHub
Support and have GitHub invalidate/rebuild the repository Contributors sidebar
cache, preserving EinarOlafsson and jak18015. Close this item only when the
public homepage widget itself reports two contributors.
