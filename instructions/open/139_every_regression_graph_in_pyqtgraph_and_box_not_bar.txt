================================================================================
EVERY REGRESSION GRAPH IN PYQTGRAPH, AND A BOXPLOT WHERE THERE WAS A BAR
================================================================================

Status:    filed 2026-08-18.
Requested: 2026-08-18 --

  "in the regression modual all of the generated graphs should be generated
   with pyqtgraph, not matplotlib, and the bargraphs with jutter plot
   backgrounds should be boxplots with jutter in the same style as the graph
   skill in the repo that is based on sebastian louridos papers"

Related: 129 put every INTERACTIVE plot in pyqtgraph, a tab each. This is the
         GENERATED half -- the figures a run writes to disk. 136 is the house
         style those figures have to be drawn in. 115 wants every diagnostic
         saved and scored.

--------------------------------------------------------------------------------
A. THE GENERATED GRAPHS MOVE TO PYQTGRAPH
--------------------------------------------------------------------------------

MEASURED 2026-08-18, matplotlib figures still produced on the regression path:

    spacr/regression_diagnostics.py    8
    spacr/toxo.py                      7
    spacr/ml.py                        5
    spacr/guide_permutation.py         1
                                      21

129 already put the interactive plots in pyqtgraph -- the volcano, the Q-Q,
the p-histogram, the control panel, the guide agreement, the effect ranking
and the three residual panels are `spacr/qt/widgets/fast_plots.py`. So the
same screen currently draws each plot TWICE, in two libraries, from two code
paths: one for the tab and one for the PDF. That is the real cost, and it is
not only duplication -- the two can and do disagree, which is how a figure in
a paper stops matching the figure on screen.

WHAT MOVING MEANS, and it needs stating because pyqtgraph is a screen library:
the generated file is a RENDER of the pyqtgraph scene, not a second drawing of
the same data. `FastPlot.snapshot` already photographs a live widget for the
grid; the export path needs the vector equivalent, at print resolution.

TWO THINGS THAT MUST NOT BE LOST, both already true of the matplotlib path:
  * every save goes through `spacr.plot.save_figure`, which honours the
    user's figure-format preference. A literal '.pdf' is a complaint this
    project has had twice.
  * a headless run writes its figures. The regression module runs from
    `spacr-run` and from a notebook with no GUI, and pyqtgraph needs a
    QApplication -- so the export has to work offscreen or say plainly that
    it cannot, per instruction 106. A run that silently stops writing
    figures when there is no display is the worst outcome here.

--------------------------------------------------------------------------------
B. A BOXPLOT WITH JITTER, NOT A BAR WITH JITTER
--------------------------------------------------------------------------------

"the bargraphs with jutter plot backgrounds should be boxplots with jutter".

THIS IS A STATISTICAL CORRECTION, not a preference, and that is why it is
worth doing rather than styling around. A bar drawn at a mean with points
behind it shows ONE number and hides the shape of the data: two groups with
the same mean and completely different spreads draw the same bar. A boxplot
shows the median, the quartiles and the whiskers, so the reader sees the
distribution the points already imply -- and the jitter stays, because the
box summarises and the points are the evidence.

WHERE IT IS: `spacrGraph` / `create_grouped_plot` in `spacr/plot.py` default
to `graph_type='bar'`, and `spacr/settings.py` defaults `graph_type` to
'jitter', 'jitter_bar' and 'violin' in three different modules. `jitter_box`
already exists as an option -- so this is largely changing which one is the
DEFAULT and making sure the box is drawn to the house rules, not writing a
new plot type.

THE STYLE IS THE ONE IN THE REPO: `spacr/figures/style.py`, which implements
`.claude/skills/apicomplexan-figures`, derived from Waldman et al. Cell 2020
and Giuliano et al. Nature Microbiology 2024 -- the Lourido-lab figures the
request names. Its rules apply here directly:
  * everything is grey except what the sentence is about; a box per group in
    a different colour is a rainbow, not an argument;
  * thin spines, small type, no gridlines;
  * the points are the data, so they carry the ink, and the box is a
    reference over them rather than a filled block competing with them.

--------------------------------------------------------------------------------
HOW TO KNOW IT WORKED
--------------------------------------------------------------------------------

* A regression run produces no matplotlib figure on its own path.
* Every generated figure matches the tab it corresponds to, because both come
  from one scene.
* `spacr-run regression` on a headless machine still writes its figures, or
  says why it cannot.
* Every save still honours the figure-format preference.
* A grouped comparison draws a box with jitter by default; the mean-bar is
  still reachable for anyone who wants it.
* The box obeys the house style: one highlighted group against grey.


================================================================================
C. EVERY GRAPH IS BOTH SAVED AND VISIBLE IN THE SOFTWARE
================================================================================

Requested: 2026-08-18 -- "all graphs should be sable and observable in the
           software, currently several graphs are saved but I cannot see them
           in the software"

THE CAUSE, FOUND 2026-08-18. A figure reaches the GUI by exactly ONE route:
`spacr/qt/bridge.py` installs `_capture_show`, which replaces
`matplotlib.pyplot.show` and emits every open figure through
`worker.figure_ready` when a module calls `plt.show()`.

So the rule the application actually runs on is:

    a figure is visible in spaCR if and only if somebody calls plt.show()

WHICH IS NOT THE RULE ANYBODY WROTE DOWN, and the regression module breaks it
in the largest place it could. Counted:

    spacr/ml.py                  10 plt.show()
    spacr/toxo.py                 5 plt.show()
    spacr/regression_qc.py        0 plt.show()   <- the ~19-panel QC suite
    spacr/regression_diagnostics.py  0 plt.show()

`regression_qc` writes one file per panel plus a combined
`regression_qc_report.pdf` with `fig.savefig(...)` and never shows anything.
Every one of those panels is on disk and none of them is in the application.
That is the report, exactly.

WHAT TO DO, and NOT by sprinkling `plt.show()` around:

  * A FIGURE IS REGISTERED, NOT SHOWN. `plt.show()` as the delivery mechanism
    is why this happened -- it couples "the user can see it" to a call that
    exists for a completely different reason, and a module that correctly
    saves-and-closes gets punished for it. There should be one function that
    says "here is a figure, it belongs to this run, keep it", and both the
    file and the gallery should come from it.
  * SAVED AND VISIBLE ARE THE SAME EVENT. Anything worth writing to the run
    folder is worth a tile; anything not worth a tile should not be written.
    If a panel is deliberately file-only, it says so where it is written.
  * THE QC SUITE IS THE TEST CASE. Nineteen panels plus the combined report;
    when this works, all twenty are in the gallery under the run that made
    them, and clicking one opens it.
  * THIS SURVIVES THE MOVE TO PYQTGRAPH (section A). Whatever draws the
    figure, the register-it step is the same, so do this first: it is the
    half that fixes the reported bug, and it makes the pyqtgraph move a
    change of renderer rather than a change of plumbing.

HOW TO KNOW IT WORKED: after a regression run with `regression_qc` on, the
number of figures in the gallery equals the number of image files in the run
folder, and every one of them opens.


--------------------------------------------------------------------------------
2026-08-18 -- SECTION C IS DONE. THE ADOPTION, NOT ONLY THE MECHANISM.
--------------------------------------------------------------------------------

WHAT SHIPPED

`spacr/figure_sink.publish` was already here and `regression_qc` already used
it; the other half of the path did not, so the reported bug was still live.
Now converted, and the whole regression path audited rather than the one file
that was named:

  * `regression_diagnostics._finish` published instead of `fig.savefig`. Its
    design, residual and inference panels were on disk and in no gallery.
  * `regression_diagnostics.write_diagnostic_suite` no longer defaults to
    `formats=("pdf", "png")`. That default was wrong twice over: each entry
    RE-RAN the whole panel, and the pair ignored the figure-format preference
    entirely, so two files (and two tiles) came out of one picture in a format
    the user had not chosen. `None` now means "the preference decides", one
    file per panel.
  * The suite's manifest is keyed by what was WRITTEN. `save_figure` rewrites
    the extension to the preference, so with PNG selected every key named a
    `.pdf` that is not there -- "saved but I cannot see it" wearing a
    different hat. Same fix in `regression_qc_report`, which recorded the
    requested path rather than `_save`'s return.
  * `ml._write_regression_sheet` published. `regression_figure.pdf` is THE
    publication figure of a run and it was written and closed in the next
    breath, so no `plt.show()` ever walked past it.
  * `ml.generate_ml_scores` publishes its four figures. `plot_permutation`,
    `plot_feature_importance` and `shap_analysis` all RETURN a figure and none
    of them shows it.

A SECOND BUG THE dpi WORK UNCOVERED. `_finish` hard-coded `dpi=200` and
applied it only when the path ended `.png`, so the resolution preference
reached none of these panels -- and a PNG written to a `.pdf` name is a file
no viewer opens. `dpi` and `fmt` default to None now, which is what lets the
preference through.

A THIRD, FOUND IN PASSING AND FIXED: `generate_ml_scores` called
`save_figure(figs[1], ...)` unconditionally, and `ml_analysis` returns
`feature_importance_fig = None` for every model without `feature_importances_`
-- logistic regression and HistGradientBoostingClassifier, two of the offered
`model_type_ml` values. `None.savefig` took the whole scoring run down after
the model had been fitted and every object scored. `publish(None, ...)` now
writes nothing, announces nothing and returns None.

ONE PICTURE, ONE TILE. There are two routes into the gallery now -- `show`
and `publish` -- and the acceptance is a COUNT, so a double is as wrong as a
miss. `qt/bridge.py` de-duplicates across both routes on `id(fig)` AND a
`_spacr_emitted` attribute, and it needs both: an id alone is reused by the
next figure after a close, and dropping THAT loses a picture; the attribute
alone outlives the run, so a figure cached across two runs would appear only
in the first. The sequence that made this necessary is real --
`generate_ml_scores` saves the plate heatmap that `plot_plates(verbose=True)`
has already shown.

ACCEPTANCE, MEASURED. On a real OLS fit through `regression_qc_report`:
19 panels + 1 combined report = 20 files on disk, 20 figures announced, under
BOTH the PNG and the PDF preference, and every file opens as the format its
name claims (checked by magic bytes, not by extension).

WHAT REMAINS ON THIS INSTRUCTION

  * Section A. See the note below it for the recount.
  * `spacr/guide_permutation.py:574` still writes with a bare `fig.savefig`,
    with `dpi=600` applied to PNG only -- the same two bugs `_finish` had. It
    is one line and it was left alone only because that file was outside this
    session's territory. HANDOFF.
  * `ml._show_well_distributions` and `ml._show_house_style_panels` take a
    `plot` flag; with it off they save without showing. Left as-is
    deliberately: the user asked not to be shown plots, and overriding that
    to satisfy a count would be the wrong reading of "saved and visible are
    the same event". Worth a decision rather than a guess.


--------------------------------------------------------------------------------
2026-08-18 -- SECTION A RECOUNTED. THE NUMBER IN THE HEADER IS HALF THE TRUTH.
--------------------------------------------------------------------------------

THE ORIGINAL 21 COUNTED FUNCTIONS IN FOUR MODULES and missed the two biggest
producers on the path. Counted the way that matters -- MATPLOTLIB FIGURES ONE
REGRESSION RUN PRODUCES, which is what a user sees and what the gallery holds:

    spacr/regression_qc.py            20   19 panels + the combined page
                                           (23 attempted; 4 are skipped on a
                                           Gaussian fit, measured)
    spacr/ml.py                       12   7 house-style panels (SHEET_ORDER)
                                           + 2 well distributions
                                           + 1 plate heatmap
                                           + 1 regression_figure sheet
                                           + 1 cell_min_threshold
    spacr/toxo.py                      5   volcano, 2 GO enrichment,
                                           phenotypes, heatmaps
    spacr/regression_diagnostics.py    3   design, residual, inference; each
                                           is one multi-panel figure
    spacr/guide_permutation.py         1   the permutation null
                                    ----
                                      41

The seven house-style panels and the QC suite are the bulk of it and neither
appears in the header's list. `spacr/figures/` is matplotlib too.

NOTHING WAS CONVERTED, and the reason is a territory boundary rather than a
judgement about the work: the export the section describes ("the generated
file is a RENDER of the pyqtgraph scene ... the vector equivalent, at print
resolution") already exists as `FastPlot.export` / `FastPlot.snapshot` in
`spacr/qt/widgets/fast_plots.py`, which this session did not own and which
another session was editing at the same time. Doing it from outside that file
would mean a second exporter, which is the duplication the section exists to
end.

TWO MEASUREMENTS TO SAVE THE NEXT ATTEMPT THE EXPERIMENT:

  * THE HEADLESS HALF WORKS. With `QT_QPA_PLATFORM=offscreen` and a
    QApplication, `pyqtgraph.exporters.ImageExporter` writes a PNG and a
    `QPdfWriter` + `scene().render(painter)` writes a real vector PDF, on this
    machine, with no display -- 16 kB and 9.9 kB respectively. So "a run that
    silently stops writing figures when there is no display" is avoidable;
    what it costs is importing PySide6 from `spacr-run` and from a notebook,
    which is the decision to make rather than the risk to fear.
  * THAT GUARANTEE IS NOW PINNED BY A TEST, deliberately written BEFORE the
    renderer changes: `test_a_headless_run_still_writes_its_regression_figures`
    asserts there is no QApplication, no sink, and twenty QC files on disk
    that each open. It fails the moment a conversion makes a figure depend on
    a display.

THE ORDER TO DO IT IN, largest value first, from the recount: the 20-figure QC
suite is the biggest single block but the least duplicated -- the tabs do not
draw those panels, so converting it buys no agreement, only a renderer change.
The DUPLICATED ones are the seven house-style panels, which are exactly
`SHEET_ORDER` and exactly the seven `fast_plots` classes 129 shipped: volcano,
effect_rank, effect_distribution, controls, agreement, p_histogram, qq. That
is where "the same screen draws each plot twice in two libraries" is literally
true, and it is seven figures rather than forty-one. START THERE.


--------------------------------------------------------------------------------
2026-08-18 -- SECTION C FOLLOW-UP: `fmt='pdf'` WAS A DEFAULT PRETENDING TO BE
              A REQUEST
--------------------------------------------------------------------------------

Found by a house-style test failing after the section C work, and it is a
second bug of the same family rather than a consequence of that work.

`regression_qc_report(..., fmt=...)` did TWO wrong things with one line.

  * It DEFAULTED to 'pdf'. A hard default is not a preference, so a user who
    had chosen PNG got PDFs from the whole nineteen-panel suite. `fmt` is None
    now, and None means the preference decides.
  * It baked the format into the file NAME and then threw the request away.
    `path = f"{name}.{fmt}"` was handed to `_save`, which called
    `save_figure` WITHOUT a format -- so `save_figure` used the preference,
    rewrote the extension, and the caller's explicit `fmt='png'` produced a
    PDF. `save_figure`'s own contract says an explicit format wins; it never
    got one. The request is passed through now, and the extension is left to
    whatever is actually written.

The two failure modes are opposite and they were both live: the default
overrode a preference that should have won, and an explicit request was
ignored by the one call that should have honoured it.

`tests/test_regression_qc.py`'s `_save` spy was updated to match -- it keys on
the STEM rather than on `regression_qc_report.pdf`, because a path with the
extension already on it is exactly the bug.


--------------------------------------------------------------------------------
2026-08-18 (evening) -- THE LAST BARE savefig ON THE REGRESSION PATH IS GONE
--------------------------------------------------------------------------------

The item the section C note handed off: "`spacr/guide_permutation.py:574` still
writes with a bare `fig.savefig`, with `dpi=600` applied to PNG only -- the
same two bugs `_finish` had." Done.

`plot_guide_permutation_volcano` now publishes through
`spacr.figure_sink.publish` and takes `fmt=None` / `dpi=None`. The permutation
null is the only picture a nonparametric run draws of its OWN null
distribution, which makes it the worst figure in the package to write to disk
and leave there.

THE dpi MEASUREMENT, on the number `savefig` is handed rather than on the file:
with the resolution preference at 331 the old line passed 600 for a PNG and
None for a PDF. It is 331 for both now. The old conditional was wrong in two
different ways at once and each hid the other -- a hard-coded number that
overrode the preference, and a None that meant matplotlib rasterised the
scatter cloud at the figure's own 100.

ONE THING DELIBERATELY NOT COLLAPSED, AND IT IS A HANDOFF, NOT AN OVERSIGHT.
`ml.py:4568` asks for this picture twice:

    for suffix in ('pdf', 'png'):
        paths[key] = plot_guide_permutation_volcano(..., f'{stem}.{suffix}')

Letting the format preference rewrite both extensions -- the convention
everywhere else, and the one `save_figure`'s docstring states -- puts both
calls on ONE destination. One of the two files a run has always written
disappears, and the gallery gets two tiles for one file, which fails this
section's own acceptance because that acceptance is a COUNT of tiles against
files. So `_named_format` reads a suffix that names a real figure format as a
request and passes it through as `fmt`; a bare stem, or a name like
`guide_permutation_min_2.5_wells`, leaves `fmt=None` and the preference
decides.

    THE FIX ml.py NEEDS: drop the `for suffix` loop, one call, one key
    `plot_min_{threshold}`. It is exactly the fix `write_diagnostic_suite`
    got when its `formats=("pdf","png")` default was removed. The moment it
    lands the preference takes over here with no further change in this file.
    ml.py was outside this session's territory.

`plt.close` is kept rather than `publish(close=True)`: `close=True` calls
`fig.clf()`, which clears a figure without releasing it, and this one comes
from `plt.subplots`, so pyplot holds a reference. A sweep over four thresholds
and two outcomes draws eight.

THE GREP THIS SECTION ASKS FOR, done with `ast` over all 381 files rather than
with a text search, because three modules on this path DISCUSS `savefig` in
prose and a grep that counts a docstring is a check nobody can keep green.
21 `.savefig(` call sites remain in `spacr/`; NONE is on the regression path:

    plot.py:232                  save_figure itself -- the one that should be
    classifier_evaluation.py     1642, 1667
    gui_elements.py              6512
    illumination.py              1326
    qt/screens/app_screen.py     4711
    qt/screens/hyperparam.py     275
    qt/widgets/figure_queue.py   154, 222, 1591
    qt/widgets/figure_settings.py 1193
    report.py                    2401, 2406
    sequencing.py                1101
    sequencing_qc.py             1211
    spacr_cellpose.py            435
    spacrops.py                  950, 1148
    surrogate.py                 879, 913
    volcano_style.py             387

Every one of those is outside this instruction and outside this session's
territory, and each is the same two bugs waiting: no format preference, no
resolution preference, no tile. `volcano_style.py:387` is the interesting one
-- it is a live screen's renderer (see 127's correction) writing its own file.

`tests/test_the_permutation_null_is_published_too.py`, 12 cases, 9 of which
fail against the old savefig line (checked by putting it back). The last one
keeps the grep above as an assertion over the five regression modules.
