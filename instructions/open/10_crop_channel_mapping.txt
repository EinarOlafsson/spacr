================================================================================
CROP CHANNEL MAPPING: png_dims BECOMES AN EXPLICIT {r,g,b} DICT
================================================================================

Status:    in progress — pipeline done and verified, crop-source parity NOT done
Requested: 2026-08-06 — "when the user specifies png_dims as 0,1,2 that should mean that; 0=blue, 1=green, and 2=red ... it would actually be better to change the setting so the user explicitly states what is red what is green and what is blue ... the setting would change from a list to a dict"

--------------------------------------------------------------------------------
WHAT THE STATE IS
--------------------------------------------------------------------------------

DONE and verified end to end:
  * spacr/crops.py — resolve_png_channel_mapping, build_png_channels,
    png_dims_to_channel_mapping, PNG_COLOR_KEYS,
    DEFAULT_PNG_CHANNEL_MAPPING = {'r': 2, 'g': 1, 'b': 0}.
  * CROP_FORMAT_DECLARED_RGB = 3 is CROP_FORMAT_CURRENT. Reversal on read is
    decided by _FORMAT_IS_DECLARED_ORDER (1:True, 2:False, 3:True), NOT by
    `fmt != as_format` — formats 1 and 3 hold identical bytes.
  * migrate_crop_folder now REPAIRS format-2 folders (was: created them).
  * spacr/measure.py — crop path builds planes in file order.
  * spacr/settings.py — png_channel_mapping added, png_dims deprecated but
    still translated.
  * spacr/qt/widgets/channel_mapping.py — three R/G/B fields, wired into
    settings_model.py at _widget_for / _read_widget / set_value_for_key.
  * generate_object_dataset now stamps its output folder (separate bug: it
    wrote correct pixels into an unmarked folder, so read_crop_png reversed
    a correct file — measured PIL (234,4,46) vs read_crop_png (46,4,234)).

NOT DONE, and it is the blocker:
  * MergedCropSource / crops.png_view still return planes in `channels`
    order while read_crop_png now returns FILE order. The two crop sources
    therefore disagree.
  * Failing: tests/test_crop_source_wiring.py (7), tests/test_crop_format.py
    (8, the staging/CLI paths), tests/test_crops.py (2),
    tests/test_crop_dtype_boundaries.py (1).

Nothing is committed.

--------------------------------------------------------------------------------
WHY IT MATTERS
--------------------------------------------------------------------------------

The reported symptom: nuclei render red and parasites blue in the annotator
and in any external viewer. Cause: commit 341f4462 (2026-07-26) deliberately
moved png_dims[0] from the file's blue slot to its red slot. Channel 0 is the
405 line, so that put the nuclear stain in red. migrate_crop_folder then
rewrote existing folders into that state, so pre-07-26 data that was migrated
is reversed on disk too.

Until the crop-source parity is restored, training-tar and UMAP thumbnail
generation can disagree with the PNG folder. DO NOT run a training-data
export off this tree until it is.

--------------------------------------------------------------------------------
WHAT TO DO
--------------------------------------------------------------------------------

1. spacr/crops.py — make png_view / MergedCropSource honour the declared
   mapping so a crop cut from merged/*.npy and one read from a PNG are the
   same array. That is the contract tests/test_crops.py ::
   test_png_source_and_merged_source_agree_on_the_same_object asserts.
2. Finish converting tests/test_crop_format.py. The remaining ones are the
   staging/watermark and `python -m spacr.crops` CLI paths. Pattern already
   applied: _reversed_folder()/_write_reversed()/_mark_reversed() build a
   format-2 folder (the migrator's only real input); "not converted yet" is
   CROP_FORMAT_RGB, "converted" is CROP_FORMAT_DECLARED_RGB.
3. tests/test_crop_source_wiring.py — same treatment.
4. Update skill/INVARIANTS.md: the crop-format rule there still describes the
   pre-inversion behaviour.
5. Tell the user how to repair the crops they made on 2026-08-05:
   migrate_crop_folder on the format-2 folders.

--------------------------------------------------------------------------------
HOW TO KNOW IT WORKED
--------------------------------------------------------------------------------

QT_QPA_PLATFORM=offscreen python -m pytest tests/test_crop_format.py \
      tests/test_crops.py tests/test_crop_source_wiring.py \
      tests/test_crop_dtype_boundaries.py tests/test_generate_object_dataset.py \
      -q -p no:randomly

green. And the end-to-end probe: a stack with ch0=60000, ch1=20000, ch2=5000
written through the measure crop path must give file R,G,B = [19, 78, 234] —
the 405 plane in BLUE — for both the default mapping and a legacy
png_dims=[0,1,2].

--------------------------------------------------------------------------------
DELIBERATELY NOT DONE
--------------------------------------------------------------------------------

Reverting 341f4462 outright. The format needs to stay versioned: crops
written in the 07-26..08-06 window exist and are marked format 2, and a
reader has to keep telling them apart from format 1. Format 3 was added
rather than reusing 2 so that a folder's marker never lies about which
writer made it.
