================================================================================
THE VERSION BUMP AND GETTING ACTIONS GREEN
================================================================================

Status:   OPEN — release action explicitly held by the maintainer; merged with
          blockers 288, 304 and terminal item 253.
Current:  ``python packaging/release.py version`` reports 1.5.0.4.

--------------------------------------------------------------------------------
THE BUMP
--------------------------------------------------------------------------------

    python packaging/release.py bump 1.5.0.5

Rules that already apply and are easy to get wrong:
  * bump the PATCH after a batch of work, never per commit;
  * smallest increment that is honest;
  * verify from a CLEAN worktree before pushing, not from this one -- this
    tree has had several agents in it and an uncommitted file would ship.

    git worktree add --detach /tmp/spacr-verify HEAD
    cd /tmp/spacr-verify && python -m pytest -q -m "not slow and not heavy"

--------------------------------------------------------------------------------
ACTIONS
--------------------------------------------------------------------------------

The blocking push workflows are:

  tests.yml           fast/minimum, marker suites and quality gate
  compat-matrix.yml   package artifacts and supported OS/Python cells
  docs.yml            localization audit and Sphinx build

``online-installers.yml`` has its own explicit dispatch path, and the
publication-only ``release.yml`` runs from ``main`` after the approved version
change. Neither is a substitute for one green nightly SHA.

What is known about the state of them:

  * The rewritten remote baseline ``3f6334a6`` is not the release candidate:
    its tests run failed Fast, Minimum dependencies and both Qt shards while
    Integration, Slow, GPU, Network, NAS and quality passed.
  * The isolated post-rewrite integration tree contains the repairs. Exact
    local selectors have passed Integration (349), GPU (86), Network (38,
    including btrack), and NAS (1). Fast, Minimum dependencies and both Qt
    shards still have to pass on the final consolidated tree, followed by the
    required GitHub workflows on that same pushed SHA.
  * The old modal-QDialog hang is fixed and guarded. A future silent Qt timeout
    should still be investigated as a modal or process-teardown problem rather
    than counted as an ordinary assertion failure.

--------------------------------------------------------------------------------
ORDER
--------------------------------------------------------------------------------

Finish 288, consolidate every concurrent lane, and make one nightly SHA green
first. Then wait for the maintainer's explicit 1.5.0.5 go-ahead. Only after
that approval may the bump, main update, tag, package publication or Zenodo
release path run.
