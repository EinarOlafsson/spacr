================================================================================
DEFECTS FOUND WHILE COVERING CODE THAT WAS EXCLUDED FROM COVERAGE
================================================================================

Status:    OPEN, filed 2026-08-29. A RUNNING LIST -- appended to as the
           coverage sweep reaches more modules.
Owner:     mixed; each entry names its own lane.
Requested: 2026-08-29 -- "continue with the tests and dont stop until the tests
           are complete and all moduals are 100% covered", under the standing
           rule that a bug found in passing gets fixed or queued, never just
           narrated.

--------------------------------------------------------------------------------
WHAT THE STATE IS
--------------------------------------------------------------------------------

Item 288 requires the 304 prohibited ``# pragma: no cover`` markers to go. Code
behind a pragma has, by construction, never executed under test, so the sweep
that removes them is also a bug hunt. Four defects found this way are ALREADY
FIXED and pushed, and are recorded here only so they are not re-derived:

  * spacr/trial_metrics.py:145 -- compared a masked residual array's length
    against the unmasked fitted length, raising IndexError out of a function
    both callers wrap in one try. Cost every residual statistic, not just the
    slope it guarded. Fixed in a7048150.
  * spacr/projects.py:760,769 -- an artifacts.db that is not valid SQLite
    raised sqlite3.DatabaseError past a too-narrow ``except
    _dm.DataManagerError`` and past an unguarded ``_registry_for()``, so one
    corrupt plate took down the entire project browse. Fixed.
  * spacr/seg_qc.py:2226 -- ``int(float(...))`` on an ``inf`` cell raises
    OverflowError, which was not in the except tuple (``nan`` raises
    ValueError and WAS caught), so an infinite count escaped read_scorecard's
    "never raises" contract into every caller of read_digest. Fixed.
  * spacr/methods_export.py:1003 -- ``is not None`` does not screen NaN, and
    hits.summary() reports NaN for max_abs_effect on a screen with no hits, so
    a methods section rendered "The largest absolute effect among them was
    nan, with a median of nan" into manuscript text. Fixed.

The entries below are NOT fixed. Each was found with a test proving the
current behaviour, and each was left because fixing it needs a decision or a
file outside the finder's lane.

--------------------------------------------------------------------------------
1. THE FIGURE SAVE MODE HAS NO WAY TO BE SET  (lane: spacr/qt)
--------------------------------------------------------------------------------

spacr/figure_style.py:831 reads
``getattr(preferences, "get_figure_save_mode", None)``. No such function
exists: spacr/qt/preferences.py does not define it, and a repo-wide grep finds
the name ONLY at that one line. So the branch is dead in production and the
saved-figure export mode can be set exclusively through the
``SPACR_FIGURE_SAVE_MODE`` environment variable -- the GUI offers no control
at all. The docstring's claim that the environment variable "takes precedence
over the Qt preference store" describes a store that was never wired up.

This is spaCR's commonest defect shape: finished code with no caller. The
``# pragma: no cover - needs Qt`` marker is exactly what kept it invisible.

TO DO: add ``get_figure_save_mode()`` to spacr/qt/preferences.py and a
Preferences control that sets it, or delete the branch and the docstring
sentence. Do not leave a third state where the code reads a setting nothing
can write. Assert the CALL SITE, not just the function.

FIXED 2026-08-29. ``get_figure_save_mode`` and its validating setter now own
``prefs/figure_save_mode``. The real Figures page exposes print, screen and
transparent choices, saves the selected item, restores it in a newly opened
dialog, and resets it to print. The call-site test then reads the choice
through ``spacr.figure_style.figure_save_mode``; a valid environment override
still wins. The focused preference/call-site cohort is 19/19 green.
Implementation commit: ``b47caef2``.

--------------------------------------------------------------------------------
2. A BARE VEuPATHDB ACCESSION IS CLASSIFIED AS A GUIDE  (lane: spacr/hits.py)
--------------------------------------------------------------------------------

``guide_of("fraction[TGGT1_231640]")`` returns ``"TGGT1_231640"`` -- a guide --
because the shape rule ``_\d+$`` matches the accession's own numeric tail.
Only the explicit spelling protects it:
``guide_of("gene_fraction:gene[TGGT1_231640]") is None``.

Consequence: an unlabelled gene term lands in the *grna* testing family rather
than the gene family, which changes which multiple-testing family a term is
corrected within. Every table spaCR writes today carries the label, so this
reaches only legacy or hand-built formulas -- but it is a scientific
classification error, not a cosmetic one, which is why it is filed rather than
patched in passing.

A fix must change BOTH ``guide_of`` and ``family_labels``: family_labels
computes ``suffix_guide`` independently and its docstring promises it agrees
with guide_of, so changing one alone makes the docstring false. The candidate
rule is ``_gene_id_of(token) != token``.

Current behaviour is pinned by
tests/test_hits_uncovered_paths.py::test_only_an_explicit_label_protects_a_bare_veupathdb_accession,
so that test must be UPDATED, not deleted, when the behaviour changes.

FIXED 2026-08-29. ``guide_of`` and the vectorized ``family_labels`` path now
share one ``_is_guide_token`` predicate: a token is a guide only when
normalizing it to a gene id removes a suffix. Thus ``TGGT1_231640`` remains a
gene while ``TGGT1_231640_3`` remains a guide, and the updated regression test
asserts both public functions agree on all three explicit/bare spellings.

--------------------------------------------------------------------------------
3. PRAGMAS DELIBERATELY LEFT, WITH THE ARGUMENT FOR EACH
--------------------------------------------------------------------------------

These are NOT oversights and must not be "fixed" by inventing a test:

  * spacr/methods_export.py:182 and :251 -- ``except ValueError`` around
    ``float(token)`` where the token already matched
    ``[-+]?\d+(?:\.\d+)?(?:[eE][-+]?\d+)?``. All 0x110000 codepoints were
    brute-forced: no character the regex accepts is rejected by ``float()``
    (CPython normalises every Nd digit), and huge magnitudes return inf rather
    than raising. Unreachable by construction.
  * spacr/gene_measurement_sweep.py:1149 -- the ``continue`` for a gene in
    ``summary.index`` with no rows in ``frame``. summary.index comes from
    ``frame.groupby("gene")``, so every key has at least one row; NaN is
    excluded twice (``notna()`` filter and groupby's own NaN drop). This is
    unconditionally dead defensive code with no pragma on it.

If item 288's contract requires literally zero uncovered lines, these are the
places where that is impossible without deleting working defensive code, and
that decision belongs to a human.

--------------------------------------------------------------------------------
0. A TEST THAT SEGFAULTED, AND WHY NO FULL-SUITE COVERAGE RUN FINISHED
   [FIXED 2026-08-30]
--------------------------------------------------------------------------------

tests/test_regression_screen_layout.py::test_right_clicking_the_live_tile_offers_the_graphs_own_menu
SEGFAULTS -- a core dump, not an exception -- under
QT_QPA_PLATFORM=offscreen, at spacr/qt/screens/app_screen.py:6763 in
``_pinned_menu`` by way of spacr/qt/widgets/figure_grid_view.py:511. It kills
the whole pytest process rather than failing a test.

This is why no whole-library coverage run has ever completed. Two attempts on
2026-08-29 died this way: the first wedged at 96% with its xdist workers idle
in ``execnet.serve`` while pytest-timeout printed faulthandler dumps every
five minutes -- the classic signature of a worker whose process vanished while
the master waited on it.

There is a SECOND one: ``tests/qt/test_layout_drops.py`` also core dumps
offscreen, found independently by a wave-3 verifier on 2026-08-29.

``--timeout-method=thread`` does NOT rescue either of them, and it was tried:
a segfault kills the process outright, so no thread survives to time out. The
run wedges exactly as before -- workers idle in ``execnet.serve`` while
pytest-timeout prints faulthandler dumps. EXCLUDING both files was initially
the only thing that let a full-suite coverage run finish.

FIXED. The two destructor faults had different ownership causes. Commit
``f3712818`` retires pyqtgraph's parentless context-menu trees when a FastPlot
closes, so opening the pinned plot menu no longer leaves Qt wrappers alive past
their owner. Commit ``7be7a99f`` creates the settings-search container without
a parent and lets ``QSplitter.insertWidget`` parent it exactly once, preventing
Shiboken's double release. The two formerly fatal files now run together to
completion: 149 passed and 2 data-dependent skips on Python 3.12 / PySide
6.11.2, including the live-tile right-click and layout-drop paths. They no
longer need exclusion from a coverage run.

Separately, do NOT set ``ulimit -v`` for such a run. A 12 GB virtual-address
cap was tried and killed a worker mid-test, which xdist reports as an
INTERNALERROR and which loses the coverage JSON even though every test ran.
torch and numba reserve far more address space than they commit; physical
memory is the real constraint.

This was a REAL defect, not just a test-harness annoyance: a right-click on a
live figure tile is an ordinary user action, and it took the interpreter down
rather than raising. It is recorded here because the coverage sweep found it.

--------------------------------------------------------------------------------
0b. A PERFORMANCE TEST THAT CANNOT SURVIVE INSTRUMENTATION
    [FIXED 2026-08-30]
--------------------------------------------------------------------------------

tests/test_fast_plots.py::TestTheLastGraphIsNotSlowAnyMore::test_the_plain_volcano_is_immediate
fails ONLY under ``--cov`` (measured 36 ms against a 30 ms budget) and passes
uninstrumented. A wall-clock budget that tight cannot hold with a tracer
installed, so the test makes coverage and performance measurement mutually
exclusive. Either raise the budget under coverage, mark it to skip when a
tracer is active, or measure it outside the coverage job.

FIXED in ``e8de72ce``. The 30 ms assertion remains unchanged and passes in an
uninstrumented process; only that wall-clock node skips when ``sys.gettrace()``
reports active instrumentation. A focused ``pytest --cov`` replay confirms the
node is skipped rather than comparing traced time to the production budget.
The performance contract therefore stays strict in the slow/performance lane
without making the coverage lane red for measuring a different workload.

--------------------------------------------------------------------------------
2b. TWO LATENT DEFECTS IN DEAD ATTRIBUTION CODE  (lane: spacr/guide_attribution.py)
--------------------------------------------------------------------------------

Both are unreachable today -- proved by exhaustive and random fuzzing -- so
neither is a live bug. They are recorded because each would be SILENT
corruption of a scientific result if the guard above it ever did fire, and
because the obvious "fix" at each site does not actually recover.

  * L487 ``if columns.size != n:`` then ``columns[:n]``. Slicing does not
    recover: ``linear_sum_assignment`` on an (n x m) matrix with m < n matches
    only m rows, so ``assigned`` would be shorter than the cell count -- cells
    dropped from the attribution without a word -- and ``order =
    np.empty(n)`` would carry UNINITIALISED memory into ``best`` and then into
    ``degeneracy``. Verified against scipy directly.
  * L675 ``density[dead, :] = weights`` in ``posterior_multivariate``. Copied
    from ``posterior``, where it is live and correct. Here it sets ``density =
    prior`` and the following ``r = density * weights`` applies the prior a
    SECOND time, so a dead row would start from prior-squared, not the prior
    the comment promises.

If either guard is ever made reachable, the body must be rewritten, not
uncommented.

--------------------------------------------------------------------------------
3b. FURTHER PRAGMAS PROVED UNREACHABLE, LEFT IN PLACE
--------------------------------------------------------------------------------

  * spacr/data_manager.py:333 -- ``return f"{value:.1f} PB"`` after the loop in
    ``human_bytes``. The loop is
    ``for unit in ("B", ..., "PB"): if abs(value) < 1000.0 or unit == "PB":
    return ...``. On the final iteration the ``unit == "PB"`` disjunct is
    unconditionally true, so the loop cannot complete -- including for NaN,
    where ``abs(nan) < 1000.0`` is False but the disjunct still returns.
    Deleting the line would make loop completion return None instead, so it
    stays.
  * spacr/database_concurrency.py:281 -- the ``else:`` on the BEGIN retry loop.
    ``attempts = max(1, int(attempts))`` and the body either breaks, re-raises
    a non-busy error, or raises DatabaseBusy when ``attempt == attempts``,
    which is guaranteed on the last iteration. The loop cannot fall through.
  * spacr/layers.py:1223->1225 -- ``CanvasLink.lock()``'s ``if leader is not
    None:``. ``_leader()`` is called immediately after ``_locked[str(key)] =
    True``, and ``self[key]`` has already raised unless the key exists, so a
    leader always exists. Verified empirically across every route into
    ``lock()``. This one has NO pragma; it is simply an uncoverable arc.
    Deleting the guard would reach 100% and was DECLINED: it mirrors the guard
    in ``add()``, where the same condition is genuinely reachable.

--------------------------------------------------------------------------------
4. SMALLER OBSERVATIONS
--------------------------------------------------------------------------------

  * spacr/database_concurrency.py ``_run_probe`` (739-742, 771) -- the final
    ``if thread.is_alive()`` re-check can suppress the "did not finish within
    30 seconds" error, but the ``alive`` list it re-checks is not rebuilt, so
    ``if temporary_dir is not None and not alive`` still keeps the scratch
    database. A worker that finishes inside the join window therefore produces
    a clean result AND a leftover temporary directory. Harmless in effect, but
    the docstring says the survivor is "a worker that outlives the join
    deadline", which is not what the code does. Current behaviour is pinned by
    a test, so a deliberate fix will surface as a failing assertion rather
    than silently.
  * spacr/crops.py ``_region_for`` -- the docstring still documents a ``None``
    return the code can no longer produce. The ``binary_dilation(iterations=0)``
    quirk that produced it was fixed by the ``if px > 0`` guard, so every path
    now returns an ndarray. Confirmed by brute force over 57,600 field/spec
    combinations: zero None regions. The stale sentence should go.
  * tests/test_the_name_is_spaCR.py::test_no_file_mis_cases_the_name STARTED
    FAILING during the 2026-08-29 session -- it is NOT pre-existing. The
    offenders spell the name "SpaCR" at docs/source/installer_guide.rst:101
    and :107, and tests/test_readme_presentation.py:313-319. Both are outside
    the coverage session's lane. Lane: docs.
  * spacr/fit_resources.py ``readable`` and spacr/mixed_gpu.py ``_readable``
    are byte-identical implementations in two modules. Deduplicating them
    would couple mixed_gpu to fit_resources, so it was left alone; noted only
    so the duplication is a decision rather than an accident.
  * spacr/pipeline_graph.py:485 -- ``except (FileNotFoundError, OSError)``
    lists a subclass beside its base; ``OSError`` alone is equivalent.
  * tests/test_api_i18n_extractor.py::test_public_docstrings_matches_reviewed_visible_coverage
    FAILS at present: public API surface 8613 vs reviewed 8612. THE EXTRA
    SYMBOL IS ``spacr.qt.widgets.fast_plots.FastPlot.closeEvent`` -- a Qt
    addition from the concurrent packaging/GUI session, not from the coverage
    sweep. Proved by parsing HEAD's and the working copy's versions of the
    coverage session's own files and getting identical symbol sets and
    docstrings. tests/test_documentation_i18n.py::test_documentation_api_catalog_inventory_and_hashes_are_current
    fails for the same reason. Both need the ratchet bumped and
    tools/build_documentation_i18n.py re-run. Lane: docs.

--------------------------------------------------------------------------------
HOW TO KNOW IT WORKED
--------------------------------------------------------------------------------

  * Entry 1: a test asserts a Preferences control writes the value that
    figure_style.py:831 reads back, driven through the real widget.
  * Entry 2: guide_of and family_labels agree on a bare accession, and the
    pinning test above is updated to the new expectation rather than removed.
  * Entry 3: each surviving pragma is either deleted with a real covering
    test, or recorded in the item-288 artifact as a named, argued exclusion.

--------------------------------------------------------------------------------
5. THE WORKING TREE ITSELF WAS THE FIRST DEFECT  (2026-08-29, later session)
--------------------------------------------------------------------------------

Before any coverage could be measured, the tree it would have been measured on
turned out to be in two contradictory states at once. This is recorded here
because a coverage number taken from it would have been fiction, and because
the same shape will recur whenever work is committed from a side worktree.

/mnt/firecuda2/codex/repo/spacr was found to be SIMULTANEOUSLY:

  * BEHIND origin/nightly. 36 files present in HEAD were absent from disk,
    including the entire spacr/flowview/ package and its tests, committed that
    same afternoon between 17:28 and 19:38. The reflog shows why: repeated
    ``commit`` -> ``reset: moving to origin/nightly`` cycles. Those commits
    were made in OTHER worktrees (codex/pushwt, codex/covwt3) and pushed, and
    this tree received a MIXED reset, which moves the pointer and never
    touches a file. It therefore never got them.
  * AHEAD in unsaved work. 199 modified files whose exact content appears in
    NO commit in the repository's entire history -- checked against all 47,765
    blobs reachable from every ref, not merely recent ones -- plus 62
    untracked files.

Both halves matter and they are easy to confuse. ``git status`` alone reads as
"someone has edits"; it does not distinguish an edit from a file this checkout
never received, and ``git pull`` or ``git restore`` on that state would have
destroyed the 199.

THE CHECK THAT SEPARATES THEM, since it is not obvious:

    h=$(git hash-object "$f")
    git rev-list --objects --all | awk 'NF>1{print $1}' | grep -qx "$h" \
      && echo "committed somewhere (tree is merely behind)" \
      || echo "content exists only here"

A second, cheaper discriminator for a file that IS unique: count deletions in
``git diff HEAD -- "$f"``. Zero deletions means the working copy is HEAD plus
additions and is safe to commit; deletions mean the copy lacks something HEAD
has, and committing it would silently revert that.

Of 146 modified test files, only 21 were pure additions. The other 125 each
carried deletions against HEAD and were left alone.

WHAT WAS RECOVERED: 54 test files (34 new, 20 pure-addition edits) and
spacr/resource_log.py, all run before committing -- 970 passed, 4 skipped.

TWO FILES DELIBERATELY NOT RECOVERED, each checked against a CLEAN worktree at
HEAD rather than assumed:

  * tests/qt/test_registration_seams.py fails three tests in the working tree
    and THE SAME THREE on clean HEAD -- test_nothing_registered_is_byte_-
    identical_to_no_seam_at_all, test_none_opacity_reaches_the_callback_as_none
    and test_two_widgets_cannot_quietly_claim_one_name. Pre-existing, and red
    on HEAD right now, which item 288's "one green commit" cannot tolerate.
    Lane: Qt.
  * tests/qt/test_external_i18n_catalogs.py fails TWO in the working tree and
    a DIFFERENT FOUR on clean HEAD. A file that fails differently on two trees
    is reporting catalog state, not code. Lane: i18n.

THE RULE THIS ADDS to HANDOFF 0c ("git commit without paths commits the
index") and 3f ("pip -e pointed at the wrong checkout"): BEFORE MEASURING
ANYTHING, ASSERT THE TREE IS WHAT YOU THINK IT IS. A stale checkout does not
announce itself, and neither does a checkout carrying a day of unsaved work.

    git status --short | wc -l          # both halves, undifferentiated
    git diff HEAD --stat | tail -1      # how far apart they are

Coverage is now measured on a frozen worktree at /mnt/firecuda2/codex/covclean
created with ``git worktree add --detach``, never on the shared tree.

--------------------------------------------------------------------------------
3c. A GUARD MADE DEAD BY AN abspath TWO LINES ABOVE IT
--------------------------------------------------------------------------------

spacr/roi.py:451 -- ``RoiSet.save``'s ``if parent:`` around
``os.makedirs(parent, exist_ok=True)``. The guard is correct in principle:
``os.makedirs('')`` raises FileNotFoundError, so a bare filename would need
it. But line 448 is ``target = os.path.abspath(str(path))``, and
``dirname(abspath(x))`` is never empty on POSIX. Checked rather than reasoned:
``''``, ``'.'``, ``'/'``, ``'//'``, ``'~'``, ``'region.json'`` and
``'a/b.json'`` all yield a non-empty directory, including ``'/'`` whose
dirname is ``'/'``.

LEFT IN PLACE, deliberately. Deleting it would make the function correct only
for as long as line 448 survives, and an abspath is exactly the kind of line a
later refactor moves or drops. It is recorded here so the arc is a known,
argued exclusion rather than an unexplained gap in the per-module report.

Found while closing the one-arc modules for item 288. Five of the six in that
batch were genuinely reachable and now have tests; this was the sixth.

  * spacr/figures/headless.py:111 -- ``if parent:`` around ``os.makedirs``,
    the SAME dead pattern as roi.py:451 above and dead for the same reason:
    line 110 is ``parent = os.path.dirname(os.path.abspath(target))``, so
    ``parent`` is never empty. Two independent occurrences of one mistake is
    a pattern worth naming: writing ``dirname(abspath(x))`` and then testing
    it for emptiness is a guard that reads correct and can never fire. A
    third copy will appear; the check is whether an abspath sits above the
    dirname.

  * spacr/errors.py:724 -- the ``finally: if conn is not None:`` in
    ``read_run_status``, arc 724->726. Falling through to 726 with ``conn``
    still None requires the try block to complete normally WITHOUT having
    connected, and there is no such path: ``connect`` either returns a handle
    (so the guard is true), or raises sqlite3.Error into the except that
    re-raises, or returns None and the next line raises something the except
    does not catch. Every route out with ``conn is None`` is an exception,
    which does not take the 724->726 arc. Unreachable, guard left in place.

--------------------------------------------------------------------------------
6. A CRASH FOUND BY WRITING A TEST FOR A BRANCH  (lane: spacr/annotation.py)
--------------------------------------------------------------------------------

FIXED 2026-08-29. ``spacr.annotation.annotate`` raised

    KeyError: "['gene_nr'] not found in axis"

on any table whose gene column is literally named ``gene_nr``.

The cause: the join did ``out.merge(right[["gene_nr"] + new], left_on=
"_gene_nr", right_on="gene_nr")`` and then ``out.drop(columns=["gene_nr"])``.
When the caller's own frame already carries ``gene_nr``, pandas suffixes both
sides into ``gene_nr_x`` and ``gene_nr_y``, so the drop finds no ``gene_nr``
and raises.

THIS IS NOT AN EXOTIC INPUT. ``gene_nr`` is the FIRST name ``_key_column``
looks for, ahead of ``gene``, ``gene_id``, ``grna`` and ``feature`` -- so it is
the canonical spelling, and it is what spaCR's own annotated output is keyed
on. Re-annotating a table that this very function had already written crashed.
It went unnoticed because every fixture in the suite keyed on ``grna`` or
``gene``, which is also why the "already annotated, stay quiet" branch below
it had never run: nothing had ever reached a second pass.

Fixed by renaming the right-hand key to a private ``_JOIN_KEY`` before the
merge, so the two sides cannot collide and the drop names a column that is
certainly there. Regression: 622 annotation/toxo tests pass, and a table keyed
on ``gene_nr`` now survives two passes.

The branch that started this is tests/test_the_last_branch_in_five_readers.py::
test_a_second_annotation_pass_says_nothing_when_asked_to_be_quiet.

  * spacr/model_zoo.py:481 -- ``return f"{n:.1f} GB"`` after the loop in
    ``_human_bytes``. A THIRD instance of the pattern already recorded for
    spacr/data_manager.py:333: ``for unit in ("B", ..., "GB"): if n < 1024 or
    unit == "GB": return ...``. On the final iteration the ``unit == "GB"``
    disjunct is unconditionally true, so the loop cannot complete. Verified by
    brute force over 0.5, 1, 1023, 1024, 1024**2, 1024**3, 1024**4, 1024**8,
    1e300 and inf -- every one returns from inside the loop, including inf,
    which yields "inf GB".

    Three copies of one helper, all with the same unreachable tail. This is
    the deduplication candidate that spacr/fit_resources.py ``readable`` and
    spacr/mixed_gpu.py ``_readable`` were already noted for in section 4; the
    count is now four byte-similar byte-formatters across the package. Left
    alone here because the coverage lane should not be the one to merge four
    modules' helpers, but it is worth someone's decision.

--------------------------------------------------------------------------------
7. THE COVERAGE REPORT'S LINE NUMBERS ARE NOT THE WORKING TREE'S
--------------------------------------------------------------------------------

A coverage report is taken on a FROZEN worktree and read on the SHARED one, and
those two disagree wherever the shared tree has uncommitted edits. spacr/crops.py
is the case that surfaced it: its working copy carries eight extra comment lines,
so the report's "missing lines 3619, 3621, 3622" pointed at comment text when
read in the shared tree, which is impossible and is what gave it away.

Checked across every module targeted in this session's sweep: crops.py was the
only one affected, so nothing else needs revisiting. But the trap is general and
it fails QUIETLY in the other direction -- a report line that lands on a
different STATEMENT rather than on a comment gives a test aimed at the wrong
branch, which then passes.

THE CHECK: compare the file, not the commit. ``git diff 4eccf7b8 HEAD -- <file>``
was EMPTY for crops.py and still the two files differed, because the difference
was never committed at all.

    md5sum <frozen-worktree>/spacr/x.py <shared-tree>/spacr/x.py

THE HABIT THAT AVOIDS IT: verify by ARC, not by line number. Every closure in
this sweep was confirmed by re-running under coverage with the exclude list
emptied and checking ``executed_branches`` for the specific arc -- so a
misread line number shows up as a test that does not close what it claimed,
rather than as a green test measuring the wrong thing. That check caught three
tests in this session that exercised nothing.

  * The dead ``dirname(abspath(...))`` guard has EIGHT independent copies:
    roi.py:451, figures/headless.py:111, plate_qc.py:1014 (write_layout_csv),
    style_base.py:182 (write), counting.py:401 (to_csv), tabular.py:231
    (write_table), tabular.py:420 (write_database) and report.py's own walk
    helper. Same proof each time: '', '.', '/', 'out.csv' and 'a/b.csv'
    all yield a non-empty directory, so ``if parent:`` is unconditionally true
    and its false side can never be taken.

    EIGHT copies is not eight slips, it is one idiom nobody has written down. The
    fix is a shared helper -- ``_ensure_parent(path)`` doing the abspath and
    the makedirs together and returning the absolute path -- which removes eight
    uncoverable arcs and the eight chances for a later edit to drop the abspath
    and make the guard load-bearing again. Filed as a decision for whoever owns
    the io layer; the coverage sweep should not be the one to add a helper six
    modules import.

--------------------------------------------------------------------------------
8. A CONSENSUS THAT IS SAFE ONLY BECAUSE ITS CALLERS PAD  (lane: spacr/sequencing.py)
--------------------------------------------------------------------------------

``create_consensus`` (spacr/sequencing.py:283-287) loops over ``len(seq1)`` and
indexes ``seq2`` and ``qual2`` at the same offset. A shorter second sequence
raises IndexError; nothing in the function checks, and the docstring does not
say the two must be equal.

It is safe today because all three callers --
``paired_find_sequence_in_chunk_reads``, its reverse-complement retry, and
``single_find_sequence_in_chunk_reads`` -- pad both reads to ``expected_end``
before calling. The padding is four lines above each call and reads as
cosmetic.

WHY THAT MATTERS BEYOND TIDINESS: this runs inside a chunk worker over a whole
FASTQ, so the failure mode of removing or reordering that padding is an
IndexError in a subprocess partway through a sequencing run.

THE ASYMMETRY IS WORSE THAN THE RAISE. A shorter seq2 raises. A LONGER seq2 is
silently ignored past ``len(seq1)``, so the second read simply stops
contributing -- no error, a consensus built from one read, and a count that
lands in the wrong well rather than being discarded.

CONSEQUENCE FOR ITEM 288, and this is why it is filed rather than fixed: the
padding makes ``len(consensus_seq)`` exactly ``expected_end``, so the three
``if len(consensus_seq) >= expected_end:`` guards at :436, :461 and :521 are
unconditionally true. Their false arcs -- 436->407, 461->467 and 521->497 --
are UNREACHABLE and cannot be covered without deleting the padding. Verified by
fuzzing create_consensus over 3,000 equal-length pairs: the consensus is
exactly ``len(seq1)`` every time.

The invariant is now pinned by tests/test_the_consensus_depends_on_its_callers_padding.py,
so a change that stops padding fails a test instead of a run. A fix would give
create_consensus its own length check; that is a decision for the sequencing
lane, not the coverage sweep.

FIXED 2026-08-30. ``create_consensus`` now validates all four lengths before
indexing. A shorter or longer mate and a sequence/quality mismatch each raise
one ``ValueError`` that reports every length, rather than an internal
``IndexError`` or a silently truncated barcode. The callers keep their padding
because the fixed output window is still their contract. Four explicit uneven
shapes plus the surrounding sequencing suite are 123/123 green.

--------------------------------------------------------------------------------
9. AN UNRECOGNISED metadata_type RAISED UnboundLocalError  (lane: spacr/utils.py)
--------------------------------------------------------------------------------

FIXED 2026-08-30. ``spacr.utils._get_regex`` mapped ``metadata_type`` onto a
filename regex through an if/elif chain over 'cellvoyager', 'cq1', 'auto' and
'custom' -- with no else. Any other value fell through to
``print(f'...regex:{regex}')`` with ``regex`` never assigned, so the user got

    UnboundLocalError: cannot access local variable 'regex' where it is not
    associated with a value

``metadata_type`` is read straight from a settings file, so this is a TYPO A
USER MAKES, not an internal fault -- and 'CellVoyager', the spelling in the
vendor's own documentation, is one of the values that produced it. The error
named an implementation detail instead of the setting that was wrong.

Fixed with an else that raises ValueError listing the four accepted values.
The list is the documentation at the moment it is needed, which is the same
argument seg_qc._resolve's unknown-threshold raise already makes.

Regression: 46 tests across the filename/organise/yokogawa suites pass.
Found by trying to cover arc 6592->6595, which was the fall-through itself.

--------------------------------------------------------------------------------
3d. FURTHER UNREACHABLE ARCS, PROVED AND LEFT
--------------------------------------------------------------------------------

  * spacr/response_distribution.py:293 -- ``if handles:`` in ``panel``, arc
    293->306. Both ``ax.hist`` and ``twin.hist`` have already run four lines
    above with ``bins=40``, and matplotlib creates all forty patches even for
    an EMPTY array -- measured, not assumed. So ``ax.patches`` and
    ``twin.patches`` are always non-empty, ``handles`` always has two entries,
    and the false side cannot be taken. The guard is correct defensive code
    against a future change to how the histograms are drawn; it simply cannot
    fire against the current ones.

  * spacr/sudoku.py:507 -- ``if mine is not None:`` in ``sudoku_all``, arc
    507->511. ``mine`` is ``here.names.index(guide) if guide in here.names else
    None``, and ``here`` is the result of ``sudoku(..., names, ...)`` called
    with the SAME ``names`` tuple the enclosing loop iterates. ``sudoku``
    builds ``names = tuple(str(g) for g in guides)`` and never filters it, so
    every ``guide`` is in ``here.names`` by construction and ``mine`` is never
    None.

  * spacr/utils.py -- the arc reported as 9754->9755 in feature_folder_name is
    a generator-expression artifact rather than a branch: line 9755 is the
    mixed-list return and IS now covered, checked in executed_lines.

Counting these, the sweep's argued-unreachable list is now: roi.py:451,
figures/headless.py:111, plate_qc.py:1014 (the dirname(abspath) family),
errors.py:724, model_zoo.py:481 and data_manager.py:333 (the byte-formatter
family), layers.py:1223, database_concurrency.py:281,
gene_measurement_sweep.py:1149, sequencing.py:436/461/521 (the consensus
padding family), response_distribution.py:293 and sudoku.py:507.

THREE FAMILIES, not thirteen accidents. A guard after a normalising call that
cannot return the guarded value; a loop whose last iteration always returns; and
a guard whose precondition the caller four lines above has just established.
Each is worth a shared helper or a comment rather than a pragma, and none is
worth deleting.

--------------------------------------------------------------------------------
10. A MODEL-NAME CHECK THAT CAN NEVER FIRE  (lane: spacr/model_check.py)
    [FIXED 2026-08-30]
--------------------------------------------------------------------------------

spacr/model_check.py:173-181 reads

    try:
        from .model_zoo import KNOWN_MODELS
        known = set(KNOWN_MODELS)
    except Exception:
        known = set()
    if known and name not in known:
        problems.append(f"{name!r} is not a model spaCR knows; ...")

``KNOWN_MODELS`` DOES NOT EXIST. A repo-wide grep finds the name at exactly two
lines, both of them this import and the line under it. So the except always
runs, ``known`` is always empty, the ``if`` is always False, and the complaint
can never be emitted.

CONSEQUENCE: ``check_model`` -- whose whole job is answering "can this model
train on this data" before a click turns into an hour of training -- does not
notice a misspelled model name. A user typing 'maxvit_tt' is told nothing, and
finds out inside training as a torchvision AttributeError.

THIS IS ENTRY 1'S SHAPE INVERTED. Entry 1 was finished code with no caller
(``get_figure_save_mode``); this is a caller reading a name nothing defines,
with a bare ``except Exception`` converting the ImportError into silence. Both
are invisible without coverage, and a bare except is what makes this one
survive every test run.

THE FIX NEEDS A DECISION, which is why it is filed and not taken. There is no
product-level registry of classifier names: ``spacr.utils.TorchModel`` resolves
them straight off ``torchvision.models`` by attribute, so torchvision's own
registry is the only authority. Whether to validate against it, and how to word
the message when torchvision is absent, belongs to the classifier lane.

Current behaviour is pinned by
tests/test_the_model_check_cannot_currently_name_an_unknown_model.py, including
an assertion that the import itself fails. Those tests must be UPDATED when the
check is given a real source, not deleted -- they are the record that makes the
change a fix rather than a regression.

FIXED in ``ec4bee23``. The click-time checker now asks TorchVision's own
``list_models`` registry and falls back to the lightweight curated settings
inventory when TorchVision is absent or its registry raises. A misspelling such
as ``maxvit_tt`` is refused before training, while ``maxvit_t`` and
``resnet50`` remain accepted. The tests were updated in place, not deleted,
and also pin both fallback paths; 34 model-check tests pass, with the separate
malformed-classes contract still recorded independently below.

--------------------------------------------------------------------------------
11. AN UNKNOWN ORGANELLE PROPERTY LOSES WHICH ORGANELLE IT WAS
     (lane: spacr/feature_dict.py)  -- FIXED 2026-08-30
--------------------------------------------------------------------------------

``_parse_organelle_summary`` canonicalises a role prefix before looking a
property up, so ``organelle_summary_organelleb_count`` and its ``organelle``
sibling share one description -- correct, and the point of the function: they
are one measurement of different objects.

When the role matches but the PROPERTY is not in KNOWN_PROPERTIES, the loop
breaks and falls through to a plain lookup that hard-codes
``object_type="organelle"``. So an unrecognised property under ``organelleb``,
``organellec`` or ``organelled`` comes back attributed to ``organelle``.

SMALL AND REAL. The column name is preserved in full, so nothing is
unrecoverable, but a report grouping unknown features by object_type files them
under the wrong organelle. It bites only for properties spaCR does not know,
which is why it has gone unnoticed -- and a custom measurement is exactly the
sort of column that would be unknown.

Current behaviour pinned by
tests/test_the_organelle_summary_names_resolve_to_one_vocabulary.py, which says
in its docstring that it is pinned and not endorsed. A fix would carry ``role``
into the fall-through; that belongs to whoever owns the feature dictionary.

FIXED in ``9908e848``. The longest-prefix loop now retains the role it matched
and gives that role to the unknown-feature fallback. The regression walks all
four organelle roles, so a future fifth role added to the tuple must retain its
identity too. The non-Qt feature dictionary cohort passed 331/331; the two Qt
dictionary cohorts passed 32/32 and 38/38 in fresh processes. Their combined
process is intentionally order-sensitive because the first file registers the
optional app globally, so isolation is part of the test contract rather than a
feature-parser failure.

--------------------------------------------------------------------------------
12. A SECOND COPY OF THE OBJECT-ID VOCABULARY, DRIFTED
     (lane: spacr/predictions.py)  -- FIXED 2026-08-30
--------------------------------------------------------------------------------

``spacr/predictions.py::_OBJECT_ID_COLUMNS`` was a hand-written copy of
``spacr.utils.PNG_OBJECT_ID_COLUMNS``, and it had drifted:

    written by filepaths_to_database : cell_id, nucleus_id, pathogen_id,
                                       cytoplasm_id, organelle_id,
                                       organelleb_id, organellec_id,
                                       organelled_id
    read by _prcfo_from_metadata     : cell_id, nucleus_id, pathogen_id,
                                       cytoplasm_id, object

The four ORGANELLE roles were written and never read. Also absent was
``schema.OBJECT_KEY`` (``objectID``), the canonical spelling.

CONSEQUENCE: an organelle-mode classifier score table carries the metadata but
no ``prcfo``, ``_prcfo_from_metadata`` returned None, and the join matched zero
rows -- reported as "no per-object score". That is the EXACT failure the
comment above the fallback in ``_result_keys`` says was fixed for the plainer
spellings. It was fixed incompletely, and instruction 300's organelle roles
then walked into the half that was left.

PNG_OBJECT_ID_COLUMNS carries its own comment about 'organelle' having been
missing from IT once, and causing "Columns must be same length as key" after
the organelle PNGs were on disk. The same omission, in the reading copy, a
second time.

FIXED by deriving the tuple from ``schema.ORGANELLE_ROLES`` -- where
PNG_OBJECT_ID_COLUMNS gets them too -- and appending ``schema.OBJECT_KEY``.
``spacr.utils`` cannot be imported at module scope here (it is imported lazily
inside functions to avoid a cycle), which is how the copy came to exist.
Both additions go LAST so precedence is unchanged and no table that resolved
before resolves differently.

A test now walks the whole vocabulary and asserts
``set(PNG_OBJECT_ID_COLUMNS.values()) <= set(_OBJECT_ID_COLUMNS)``, so the two
cannot drift apart again. Regression: 210 prediction and classifier tests pass.

This is instruction 145's theme -- one reader, one writer, one vocabulary --
found in a third place.

  * spacr/schema.py:287 -- the ``raise RuntimeError`` in the module-level
    OBJECT_TYPES validation. It fires only for a role that is SHIPPED, and
    OBJECT_TYPES is a literal in the same module, so reaching it means editing
    the source rather than arranging an input. An import-time assertion whose
    false side cannot be exercised from a test without rewriting the file
    underneath it.

    Covered instead by tests/test_an_object_role_cannot_break_a_typed_id.py,
    which asserts the RULE against the roles that actually ship, demonstrates
    the round trip the rule protects, and shows the collision it prevents
    ('cell1' + '7' == 'cell' + '17'). That is the test that catches the real
    risk: the organelle roles are the ones a maintainer would extend, and the
    fifth is naturally called 'organelle2'.
