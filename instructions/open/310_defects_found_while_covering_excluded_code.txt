================================================================================
DEFECTS FOUND WHILE COVERING CODE THAT WAS EXCLUDED FROM COVERAGE
================================================================================

Status:    OPEN, filed 2026-08-29. A RUNNING LIST -- appended to as the
           coverage sweep reaches more modules.
Owner:     mixed; each entry names its own lane.
Requested: 2026-08-29 -- "continue with the tests and dont stop until the tests
           are complete and all moduals are 100% covered", under the standing
           rule that a bug found in passing gets fixed or queued, never just
           narrated.

--------------------------------------------------------------------------------
WHAT THE STATE IS
--------------------------------------------------------------------------------

Item 288 requires the 304 prohibited ``# pragma: no cover`` markers to go. Code
behind a pragma has, by construction, never executed under test, so the sweep
that removes them is also a bug hunt. Four defects found this way are ALREADY
FIXED and pushed, and are recorded here only so they are not re-derived:

  * spacr/trial_metrics.py:145 -- compared a masked residual array's length
    against the unmasked fitted length, raising IndexError out of a function
    both callers wrap in one try. Cost every residual statistic, not just the
    slope it guarded. Fixed in a7048150.
  * spacr/projects.py:760,769 -- an artifacts.db that is not valid SQLite
    raised sqlite3.DatabaseError past a too-narrow ``except
    _dm.DataManagerError`` and past an unguarded ``_registry_for()``, so one
    corrupt plate took down the entire project browse. Fixed.
  * spacr/seg_qc.py:2226 -- ``int(float(...))`` on an ``inf`` cell raises
    OverflowError, which was not in the except tuple (``nan`` raises
    ValueError and WAS caught), so an infinite count escaped read_scorecard's
    "never raises" contract into every caller of read_digest. Fixed.
  * spacr/methods_export.py:1003 -- ``is not None`` does not screen NaN, and
    hits.summary() reports NaN for max_abs_effect on a screen with no hits, so
    a methods section rendered "The largest absolute effect among them was
    nan, with a median of nan" into manuscript text. Fixed.

The entries below are NOT fixed. Each was found with a test proving the
current behaviour, and each was left because fixing it needs a decision or a
file outside the finder's lane.

--------------------------------------------------------------------------------
1. THE FIGURE SAVE MODE HAS NO WAY TO BE SET  (lane: spacr/qt)
--------------------------------------------------------------------------------

FIXED 2026-08-29 in b47caef2. ``spacr.qt.preferences`` now owns validated
``get_figure_save_mode`` / ``set_figure_save_mode`` accessors and a real
Figure save mode combo in the Preferences dialog. Saving that dialog writes
the value that ``spacr.figure_style.figure_save_mode`` reads; a valid
``SPACR_FIGURE_SAVE_MODE`` still takes precedence for command-line and
notebook runs.

The regression drives the real widget and then asserts both the production
resolver and ``saved_figure_appearance`` see its value:
``tests/qt/test_figure_save_mode_preferences.py``. Rechecked against current
nightly on 2026-08-30 together with the figure-style save-path tests: 41
passed.

--------------------------------------------------------------------------------
2. A BARE VEuPATHDB ACCESSION IS CLASSIFIED AS A GUIDE  (lane: spacr/hits.py)
--------------------------------------------------------------------------------

``guide_of("fraction[TGGT1_231640]")`` returns ``"TGGT1_231640"`` -- a guide --
because the shape rule ``_\d+$`` matches the accession's own numeric tail.
Only the explicit spelling protects it:
``guide_of("gene_fraction:gene[TGGT1_231640]") is None``.

Consequence: an unlabelled gene term lands in the *grna* testing family rather
than the gene family, which changes which multiple-testing family a term is
corrected within. Every table spaCR writes today carries the label, so this
reaches only legacy or hand-built formulas -- but it is a scientific
classification error, not a cosmetic one, which is why it is filed rather than
patched in passing.

A fix must change BOTH ``guide_of`` and ``family_labels``: family_labels
computes ``suffix_guide`` independently and its docstring promises it agrees
with guide_of, so changing one alone makes the docstring false. The candidate
rule is ``_gene_id_of(token) != token``.

Current behaviour is pinned by
tests/test_hits_uncovered_paths.py::test_only_an_explicit_label_protects_a_bare_veupathdb_accession,
so that test must be UPDATED, not deleted, when the behaviour changes.

FIXED 2026-08-29. ``guide_of`` and the vectorized ``family_labels`` path now
share one ``_is_guide_token`` predicate: a token is a guide only when
normalizing it to a gene id removes a suffix. Thus ``TGGT1_231640`` remains a
gene while ``TGGT1_231640_3`` remains a guide, and the updated regression test
asserts both public functions agree on all three explicit/bare spellings.

--------------------------------------------------------------------------------
3. PRAGMAS DELIBERATELY LEFT, WITH THE ARGUMENT FOR EACH
--------------------------------------------------------------------------------

These are NOT oversights and must not be "fixed" by inventing a test:

  * spacr/methods_export.py:182 and :251 -- ``except ValueError`` around
    ``float(token)`` where the token already matched
    ``[-+]?\d+(?:\.\d+)?(?:[eE][-+]?\d+)?``. All 0x110000 codepoints were
    brute-forced: no character the regex accepts is rejected by ``float()``
    (CPython normalises every Nd digit), and huge magnitudes return inf rather
    than raising. Unreachable by construction.
  * spacr/gene_measurement_sweep.py:1149 -- the ``continue`` for a gene in
    ``summary.index`` with no rows in ``frame``. summary.index comes from
    ``frame.groupby("gene")``, so every key has at least one row; NaN is
    excluded twice (``notna()`` filter and groupby's own NaN drop). This is
    unconditionally dead defensive code with no pragma on it.

If item 288's contract requires literally zero uncovered lines, these are the
places where that is impossible without deleting working defensive code, and
that decision belongs to a human.

--------------------------------------------------------------------------------
0. A TEST THAT SEGFAULTS, AND WHY NO FULL-SUITE COVERAGE RUN FINISHES
--------------------------------------------------------------------------------

tests/test_regression_screen_layout.py::test_right_clicking_the_live_tile_offers_the_graphs_own_menu
SEGFAULTS -- a core dump, not an exception -- under
QT_QPA_PLATFORM=offscreen, at spacr/qt/screens/app_screen.py:6763 in
``_pinned_menu`` by way of spacr/qt/widgets/figure_grid_view.py:511. It kills
the whole pytest process rather than failing a test.

This is why no whole-library coverage run has ever completed. Two attempts on
2026-08-29 died this way: the first wedged at 96% with its xdist workers idle
in ``execnet.serve`` while pytest-timeout printed faulthandler dumps every
five minutes -- the classic signature of a worker whose process vanished while
the master waited on it.

There is a SECOND one: ``tests/qt/test_layout_drops.py`` also core dumps
offscreen, found independently by a wave-3 verifier on 2026-08-29.

``--timeout-method=thread`` does NOT rescue either of them, and it was tried:
a segfault kills the process outright, so no thread survives to time out. The
run wedges exactly as before -- workers idle in ``execnet.serve`` while
pytest-timeout prints faulthandler dumps. EXCLUDING both files is currently
the only thing that lets a full-suite coverage run finish.

Separately, do NOT set ``ulimit -v`` for such a run. A 12 GB virtual-address
cap was tried and killed a worker mid-test, which xdist reports as an
INTERNALERROR and which loses the coverage JSON even though every test ran.
torch and numba reserve far more address space than they commit; physical
memory is the real constraint.

This is a REAL defect, not just a test-harness annoyance: a right-click on a
live figure tile is an ordinary user action, and it takes the interpreter down
rather than raising. It is filed here because it was found by the coverage
sweep, but it belongs to whoever owns the figure grid.

--------------------------------------------------------------------------------
0b. A PERFORMANCE TEST THAT CANNOT SURVIVE INSTRUMENTATION
--------------------------------------------------------------------------------

tests/test_fast_plots.py::TestTheLastGraphIsNotSlowAnyMore::test_the_plain_volcano_is_immediate
fails ONLY under ``--cov`` (measured 36 ms against a 30 ms budget) and passes
uninstrumented. A wall-clock budget that tight cannot hold with a tracer
installed, so the test makes coverage and performance measurement mutually
exclusive. Either raise the budget under coverage, mark it to skip when a
tracer is active, or measure it outside the coverage job.

--------------------------------------------------------------------------------
2b. TWO LATENT DEFECTS IN DEAD ATTRIBUTION CODE  (lane: spacr/guide_attribution.py)
--------------------------------------------------------------------------------

Both are unreachable today -- proved by exhaustive and random fuzzing -- so
neither is a live bug. They are recorded because each would be SILENT
corruption of a scientific result if the guard above it ever did fire, and
because the obvious "fix" at each site does not actually recover.

  * L487 ``if columns.size != n:`` then ``columns[:n]``. Slicing does not
    recover: ``linear_sum_assignment`` on an (n x m) matrix with m < n matches
    only m rows, so ``assigned`` would be shorter than the cell count -- cells
    dropped from the attribution without a word -- and ``order =
    np.empty(n)`` would carry UNINITIALISED memory into ``best`` and then into
    ``degeneracy``. Verified against scipy directly.
  * L675 ``density[dead, :] = weights`` in ``posterior_multivariate``. Copied
    from ``posterior``, where it is live and correct. Here it sets ``density =
    prior`` and the following ``r = density * weights`` applies the prior a
    SECOND time, so a dead row would start from prior-squared, not the prior
    the comment promises.

If either guard is ever made reachable, the body must be rewritten, not
uncommented.

--------------------------------------------------------------------------------
3b. FURTHER PRAGMAS PROVED UNREACHABLE, LEFT IN PLACE
--------------------------------------------------------------------------------

  * spacr/data_manager.py:333 -- ``return f"{value:.1f} PB"`` after the loop in
    ``human_bytes``. The loop is
    ``for unit in ("B", ..., "PB"): if abs(value) < 1000.0 or unit == "PB":
    return ...``. On the final iteration the ``unit == "PB"`` disjunct is
    unconditionally true, so the loop cannot complete -- including for NaN,
    where ``abs(nan) < 1000.0`` is False but the disjunct still returns.
    Deleting the line would make loop completion return None instead, so it
    stays.
  * spacr/database_concurrency.py:281 -- the ``else:`` on the BEGIN retry loop.
    ``attempts = max(1, int(attempts))`` and the body either breaks, re-raises
    a non-busy error, or raises DatabaseBusy when ``attempt == attempts``,
    which is guaranteed on the last iteration. The loop cannot fall through.
  * spacr/layers.py:1223->1225 -- ``CanvasLink.lock()``'s ``if leader is not
    None:``. ``_leader()`` is called immediately after ``_locked[str(key)] =
    True``, and ``self[key]`` has already raised unless the key exists, so a
    leader always exists. Verified empirically across every route into
    ``lock()``. This one has NO pragma; it is simply an uncoverable arc.
    Deleting the guard would reach 100% and was DECLINED: it mirrors the guard
    in ``add()``, where the same condition is genuinely reachable.

--------------------------------------------------------------------------------
4. SMALLER OBSERVATIONS
--------------------------------------------------------------------------------

  * spacr/database_concurrency.py ``_run_probe`` (739-742, 771) -- the final
    ``if thread.is_alive()`` re-check can suppress the "did not finish within
    30 seconds" error, but the ``alive`` list it re-checks is not rebuilt, so
    ``if temporary_dir is not None and not alive`` still keeps the scratch
    database. A worker that finishes inside the join window therefore produces
    a clean result AND a leftover temporary directory. Harmless in effect, but
    the docstring says the survivor is "a worker that outlives the join
    deadline", which is not what the code does.

    FIXED 2026-08-29 in 430c0ade. The final liveness check now rebuilds the
    actual survivor list used by both error reporting and scratch retention.
    A worker that exits in the last join window leaves neither a false stall
    error nor a disposable database behind; a genuinely live worker is still
    retained for inspection. The deterministic delayed-``is_alive``
    regression now asserts that the scratch path is removed. Rechecked on
    current nightly on 2026-08-30: passed.
  * spacr/crops.py ``_region_for`` -- FIXED 2026-08-29 in 2b36b712. Its
    docstring now states the actual ndarray-only return contract, the stale
    ``region_mask_or_None`` description and dilation-to-stability story are
    gone, and the zero-radius guard remains documented. Rechecked with the
    known-bug contracts on 2026-08-30: 5 passed.
  * tests/test_the_name_is_spaCR.py::test_no_file_mis_cases_the_name --
    RESOLVED 2026-08-29 in cde2b73d. The original 1.5.0.x installers really
    created case-sensitive ``SpaCR`` directories, so changing those recovery
    paths would break existing installs. The casing test now removes only the
    exact legacy path/settings identifiers before checking the rest of each
    line; ordinary prose still has no exemption. Rechecked on current nightly
    on 2026-08-30: passed.
  * spacr/fit_resources.py ``readable`` and spacr/mixed_gpu.py ``_readable``
    still share the same byte-formatting loop. They are no longer literally
    byte-identical: the public ``fit_resources.readable`` also accepts
    ``None`` and returns ``"not measured"``. Deduplicating the common loop
    would still couple mixed_gpu to fit_resources, so it remains deliberately
    local to each module.
  * spacr/pipeline_graph.py:485 -- ``except (FileNotFoundError, OSError)``
    listed a subclass beside its base. FIXED 2026-08-29 in 591afe90: the race
    guard now catches ``OSError`` alone. The focused pipeline-graph suites
    were rechecked on 2026-08-30: 33 passed.
  * tests/test_api_i18n_extractor.py::test_public_docstrings_matches_reviewed_visible_coverage
    no longer fails on ``spacr.qt.widgets.fast_plots.FastPlot.closeEvent``.
    The symbol entered the generated catalogs in 2e3f0ca0, and the current
    reviewed public-surface ratchet is 8,719 canonical / 8,838 with aliases;
    its exact test passed on 2026-08-30. The documentation catalog hash test
    also no longer names FastPlot. At this recheck it instead named ten newer
    ``spacr.ml`` source/hash contracts, already owned by the active API-catalog
    lane; that is a separate current change, not this recorded defect.

--------------------------------------------------------------------------------
HOW TO KNOW IT WORKED
--------------------------------------------------------------------------------

  * Entry 1: a test asserts a Preferences control writes the value that
    figure_style.py:831 reads back, driven through the real widget.
  * Entry 2: guide_of and family_labels agree on a bare accession, and the
    pinning test above is updated to the new expectation rather than removed.
  * Entry 3: each surviving pragma is either deleted with a real covering
    test, or recorded in the item-288 artifact as a named, argued exclusion.

--------------------------------------------------------------------------------
5. THE WORKING TREE ITSELF WAS THE FIRST DEFECT  (2026-08-29, later session)
--------------------------------------------------------------------------------

Before any coverage could be measured, the tree it would have been measured on
turned out to be in two contradictory states at once. This is recorded here
because a coverage number taken from it would have been fiction, and because
the same shape will recur whenever work is committed from a side worktree.

/mnt/firecuda2/codex/repo/spacr was found to be SIMULTANEOUSLY:

  * BEHIND origin/nightly. 36 files present in HEAD were absent from disk,
    including the entire spacr/flowview/ package and its tests, committed that
    same afternoon between 17:28 and 19:38. The reflog shows why: repeated
    ``commit`` -> ``reset: moving to origin/nightly`` cycles. Those commits
    were made in OTHER worktrees (codex/pushwt, codex/covwt3) and pushed, and
    this tree received a MIXED reset, which moves the pointer and never
    touches a file. It therefore never got them.
  * AHEAD in unsaved work. 199 modified files whose exact content appears in
    NO commit in the repository's entire history -- checked against all 47,765
    blobs reachable from every ref, not merely recent ones -- plus 62
    untracked files.

Both halves matter and they are easy to confuse. ``git status`` alone reads as
"someone has edits"; it does not distinguish an edit from a file this checkout
never received, and ``git pull`` or ``git restore`` on that state would have
destroyed the 199.

THE CHECK THAT SEPARATES THEM, since it is not obvious:

    h=$(git hash-object "$f")
    git rev-list --objects --all | awk 'NF>1{print $1}' | grep -qx "$h" \
      && echo "committed somewhere (tree is merely behind)" \
      || echo "content exists only here"

A second, cheaper discriminator for a file that IS unique: count deletions in
``git diff HEAD -- "$f"``. Zero deletions means the working copy is HEAD plus
additions and is safe to commit; deletions mean the copy lacks something HEAD
has, and committing it would silently revert that.

Of 146 modified test files, only 21 were pure additions. The other 125 each
carried deletions against HEAD and were left alone.

WHAT WAS RECOVERED: 54 test files (34 new, 20 pure-addition edits) and
spacr/resource_log.py, all run before committing -- 970 passed, 4 skipped.

TWO FILES DELIBERATELY NOT RECOVERED, each checked against a CLEAN worktree at
HEAD rather than assumed:

  * tests/qt/test_registration_seams.py fails three tests in the working tree
    and THE SAME THREE on clean HEAD -- test_nothing_registered_is_byte_-
    identical_to_no_seam_at_all, test_none_opacity_reaches_the_callback_as_none
    and test_two_widgets_cannot_quietly_claim_one_name. Pre-existing, and red
    on HEAD right now, which item 288's "one green commit" cannot tolerate.
    Lane: Qt.
  * tests/qt/test_external_i18n_catalogs.py fails TWO in the working tree and
    a DIFFERENT FOUR on clean HEAD. A file that fails differently on two trees
    is reporting catalog state, not code. Lane: i18n.

THE RULE THIS ADDS to HANDOFF 0c ("git commit without paths commits the
index") and 3f ("pip -e pointed at the wrong checkout"): BEFORE MEASURING
ANYTHING, ASSERT THE TREE IS WHAT YOU THINK IT IS. A stale checkout does not
announce itself, and neither does a checkout carrying a day of unsaved work.

    git status --short | wc -l          # both halves, undifferentiated
    git diff HEAD --stat | tail -1      # how far apart they are

Coverage is now measured on a frozen worktree at /mnt/firecuda2/codex/covclean
created with ``git worktree add --detach``, never on the shared tree.

--------------------------------------------------------------------------------
3c. A GUARD MADE DEAD BY AN abspath TWO LINES ABOVE IT
--------------------------------------------------------------------------------

spacr/roi.py:451 -- ``RoiSet.save``'s ``if parent:`` around
``os.makedirs(parent, exist_ok=True)``. The guard is correct in principle:
``os.makedirs('')`` raises FileNotFoundError, so a bare filename would need
it. But line 448 is ``target = os.path.abspath(str(path))``, and
``dirname(abspath(x))`` is never empty on POSIX. Checked rather than reasoned:
``''``, ``'.'``, ``'/'``, ``'//'``, ``'~'``, ``'region.json'`` and
``'a/b.json'`` all yield a non-empty directory, including ``'/'`` whose
dirname is ``'/'``.

LEFT IN PLACE, deliberately. Deleting it would make the function correct only
for as long as line 448 survives, and an abspath is exactly the kind of line a
later refactor moves or drops. It is recorded here so the arc is a known,
argued exclusion rather than an unexplained gap in the per-module report.

Found while closing the one-arc modules for item 288. Five of the six in that
batch were genuinely reachable and now have tests; this was the sixth.

  * spacr/figures/headless.py:111 -- ``if parent:`` around ``os.makedirs``,
    the SAME dead pattern as roi.py:451 above and dead for the same reason:
    line 110 is ``parent = os.path.dirname(os.path.abspath(target))``, so
    ``parent`` is never empty. Two independent occurrences of one mistake is
    a pattern worth naming: writing ``dirname(abspath(x))`` and then testing
    it for emptiness is a guard that reads correct and can never fire. A
    third copy will appear; the check is whether an abspath sits above the
    dirname.

  * spacr/errors.py:724 -- the ``finally: if conn is not None:`` in
    ``read_run_status``, arc 724->726. Falling through to 726 with ``conn``
    still None requires the try block to complete normally WITHOUT having
    connected, and there is no such path: ``connect`` either returns a handle
    (so the guard is true), or raises sqlite3.Error into the except that
    re-raises, or returns None and the next line raises something the except
    does not catch. Every route out with ``conn is None`` is an exception,
    which does not take the 724->726 arc. Unreachable, guard left in place.

--------------------------------------------------------------------------------
6. A CRASH FOUND BY WRITING A TEST FOR A BRANCH  (lane: spacr/annotation.py)
--------------------------------------------------------------------------------

FIXED 2026-08-29. ``spacr.annotation.annotate`` raised

    KeyError: "['gene_nr'] not found in axis"

on any table whose gene column is literally named ``gene_nr``.

The cause: the join did ``out.merge(right[["gene_nr"] + new], left_on=
"_gene_nr", right_on="gene_nr")`` and then ``out.drop(columns=["gene_nr"])``.
When the caller's own frame already carries ``gene_nr``, pandas suffixes both
sides into ``gene_nr_x`` and ``gene_nr_y``, so the drop finds no ``gene_nr``
and raises.

THIS IS NOT AN EXOTIC INPUT. ``gene_nr`` is the FIRST name ``_key_column``
looks for, ahead of ``gene``, ``gene_id``, ``grna`` and ``feature`` -- so it is
the canonical spelling, and it is what spaCR's own annotated output is keyed
on. Re-annotating a table that this very function had already written crashed.
It went unnoticed because every fixture in the suite keyed on ``grna`` or
``gene``, which is also why the "already annotated, stay quiet" branch below
it had never run: nothing had ever reached a second pass.

Fixed by renaming the right-hand key to a private ``_JOIN_KEY`` before the
merge, so the two sides cannot collide and the drop names a column that is
certainly there. Regression: 622 annotation/toxo tests pass, and a table keyed
on ``gene_nr`` now survives two passes.

The branch that started this is tests/test_the_last_branch_in_five_readers.py::
test_a_second_annotation_pass_says_nothing_when_asked_to_be_quiet.

  * spacr/model_zoo.py:481 -- ``return f"{n:.1f} GB"`` after the loop in
    ``_human_bytes``. A THIRD instance of the pattern already recorded for
    spacr/data_manager.py:333: ``for unit in ("B", ..., "GB"): if n < 1024 or
    unit == "GB": return ...``. On the final iteration the ``unit == "GB"``
    disjunct is unconditionally true, so the loop cannot complete. Verified by
    brute force over 0.5, 1, 1023, 1024, 1024**2, 1024**3, 1024**4, 1024**8,
    1e300 and inf -- every one returns from inside the loop, including inf,
    which yields "inf GB".

    Three copies of one helper, all with the same unreachable tail. This is
    the deduplication candidate that spacr/fit_resources.py ``readable`` and
    spacr/mixed_gpu.py ``_readable`` were already noted for in section 4; the
    count is now four byte-similar byte-formatters across the package. Left
    alone here because the coverage lane should not be the one to merge four
    modules' helpers, but it is worth someone's decision.

--------------------------------------------------------------------------------
7. THE COVERAGE REPORT'S LINE NUMBERS ARE NOT THE WORKING TREE'S
--------------------------------------------------------------------------------

A coverage report is taken on a FROZEN worktree and read on the SHARED one, and
those two disagree wherever the shared tree has uncommitted edits. spacr/crops.py
is the case that surfaced it: its working copy carries eight extra comment lines,
so the report's "missing lines 3619, 3621, 3622" pointed at comment text when
read in the shared tree, which is impossible and is what gave it away.

Checked across every module targeted in this session's sweep: crops.py was the
only one affected, so nothing else needs revisiting. But the trap is general and
it fails QUIETLY in the other direction -- a report line that lands on a
different STATEMENT rather than on a comment gives a test aimed at the wrong
branch, which then passes.

THE CHECK: compare the file, not the commit. ``git diff 4eccf7b8 HEAD -- <file>``
was EMPTY for crops.py and still the two files differed, because the difference
was never committed at all.

    md5sum <frozen-worktree>/spacr/x.py <shared-tree>/spacr/x.py

THE HABIT THAT AVOIDS IT: verify by ARC, not by line number. Every closure in
this sweep was confirmed by re-running under coverage with the exclude list
emptied and checking ``executed_branches`` for the specific arc -- so a
misread line number shows up as a test that does not close what it claimed,
rather than as a green test measuring the wrong thing. That check caught three
tests in this session that exercised nothing.

  * The dead ``dirname(abspath(...))`` guard has TWELVE independent copies:
    roi.py:451, figures/headless.py:111, plate_qc.py:1014 (write_layout_csv),
    style_base.py:182 (write), counting.py:401 (to_csv), tabular.py:231
    (write_table), tabular.py:420 (write_database), illumination.py:386
    (IlluminationModel.save), convert.py:2041 (populate_db_from_map),
    curation.py:315 (CurationLog.write), curation.py:967 (save) and
    report.py's own walk helper. Same proof each time: '', '.', '/', 'out.csv' and 'a/b.csv'
    all yield a non-empty directory, so ``if parent:`` is unconditionally true
    and its false side can never be taken.

    TWELVE copies is not twelve slips, it is one idiom nobody has written down. The
    fix is a shared helper -- ``_ensure_parent(path)`` doing the abspath and
    the makedirs together and returning the absolute path -- which removes twelve
    uncoverable arcs and the twelve chances for a later edit to drop the abspath
    and make the guard load-bearing again. NOT REMOVED HERE, deliberately, and item 288's own wording is why: "Truly
    dead code may be removed only after proving it is unreachable AND
    unneeded." Unreachable is proved. Unneeded is not -- each guard becomes
    load-bearing the moment its abspath moves, which is exactly the edit a
    later refactor makes. So they are recorded as named, argued exclusions,
    which 288 explicitly permits, rather than deleted across twelve modules in
    eleven lanes.

    The shared ``_ensure_parent(path)`` remains the right fix and belongs to
    whoever owns the io layer; it would close twelve uncoverable arcs at once
    and leave no guard that can silently stop mattering.

--------------------------------------------------------------------------------
8. A CONSENSUS THAT IS SAFE ONLY BECAUSE ITS CALLERS PAD  (lane: spacr/sequencing.py)
--------------------------------------------------------------------------------

``create_consensus`` (spacr/sequencing.py:283-287) loops over ``len(seq1)`` and
indexes ``seq2`` and ``qual2`` at the same offset. A shorter second sequence
raises IndexError; nothing in the function checks, and the docstring does not
say the two must be equal.

It is safe today because all three callers --
``paired_find_sequence_in_chunk_reads``, its reverse-complement retry, and
``single_find_sequence_in_chunk_reads`` -- pad both reads to ``expected_end``
before calling. The padding is four lines above each call and reads as
cosmetic.

WHY THAT MATTERS BEYOND TIDINESS: this runs inside a chunk worker over a whole
FASTQ, so the failure mode of removing or reordering that padding is an
IndexError in a subprocess partway through a sequencing run.

THE ASYMMETRY IS WORSE THAN THE RAISE. A shorter seq2 raises. A LONGER seq2 is
silently ignored past ``len(seq1)``, so the second read simply stops
contributing -- no error, a consensus built from one read, and a count that
lands in the wrong well rather than being discarded.

CONSEQUENCE FOR ITEM 288, and this is why it is filed rather than fixed: the
padding makes ``len(consensus_seq)`` exactly ``expected_end``, so the three
``if len(consensus_seq) >= expected_end:`` guards at :436, :461 and :521 are
unconditionally true. Their false arcs -- 436->407, 461->467 and 521->497 --
are UNREACHABLE and cannot be covered without deleting the padding. Verified by
fuzzing create_consensus over 3,000 equal-length pairs: the consensus is
exactly ``len(seq1)`` every time.

The invariant is now pinned by tests/test_the_consensus_depends_on_its_callers_padding.py,
so a change that stops padding fails a test instead of a run. A fix would give
create_consensus its own length check; that is a decision for the sequencing
lane, not the coverage sweep.

FIXED 2026-08-30 in ``b9cf8222``. ``create_consensus`` now validates all four
sequence/quality lengths itself and raises a named ``ValueError`` containing
each observed length. Uneven reads can therefore neither raise an internal
``IndexError`` nor truncate silently, even for a future caller that does not
pad. The production invariant, asymmetric short/long failures, base-quality
selection and ``N`` preference are covered together.

--------------------------------------------------------------------------------
9. AN UNRECOGNISED metadata_type RAISED UnboundLocalError  (lane: spacr/utils.py)
--------------------------------------------------------------------------------

FIXED 2026-08-30. ``spacr.utils._get_regex`` mapped ``metadata_type`` onto a
filename regex through an if/elif chain over 'cellvoyager', 'cq1', 'auto' and
'custom' -- with no else. Any other value fell through to
``print(f'...regex:{regex}')`` with ``regex`` never assigned, so the user got

    UnboundLocalError: cannot access local variable 'regex' where it is not
    associated with a value

``metadata_type`` is read straight from a settings file, so this is a TYPO A
USER MAKES, not an internal fault -- and 'CellVoyager', the spelling in the
vendor's own documentation, is one of the values that produced it. The error
named an implementation detail instead of the setting that was wrong.

Fixed with an else that raises ValueError listing the four accepted values.
The list is the documentation at the moment it is needed, which is the same
argument seg_qc._resolve's unknown-threshold raise already makes.

Regression: 46 tests across the filename/organise/yokogawa suites pass.
Found by trying to cover arc 6592->6595, which was the fall-through itself.

--------------------------------------------------------------------------------
3d. FURTHER UNREACHABLE ARCS, PROVED AND LEFT
--------------------------------------------------------------------------------

  * spacr/response_distribution.py:293 -- ``if handles:`` in ``panel``, arc
    293->306. Both ``ax.hist`` and ``twin.hist`` have already run four lines
    above with ``bins=40``, and matplotlib creates all forty patches even for
    an EMPTY array -- measured, not assumed. So ``ax.patches`` and
    ``twin.patches`` are always non-empty, ``handles`` always has two entries,
    and the false side cannot be taken. The guard is correct defensive code
    against a future change to how the histograms are drawn; it simply cannot
    fire against the current ones.

  * spacr/sudoku.py:507 -- ``if mine is not None:`` in ``sudoku_all``, arc
    507->511. ``mine`` is ``here.names.index(guide) if guide in here.names else
    None``, and ``here`` is the result of ``sudoku(..., names, ...)`` called
    with the SAME ``names`` tuple the enclosing loop iterates. ``sudoku``
    builds ``names = tuple(str(g) for g in guides)`` and never filters it, so
    every ``guide`` is in ``here.names`` by construction and ``mine`` is never
    None.

  * spacr/utils.py -- the arc reported as 9754->9755 in feature_folder_name is
    a generator-expression artifact rather than a branch: line 9755 is the
    mixed-list return and IS now covered, checked in executed_lines.

Counting these, the sweep's argued-unreachable list is now: roi.py:451,
figures/headless.py:111, plate_qc.py:1014 (the dirname(abspath) family),
errors.py:724, model_zoo.py:481 and data_manager.py:333 (the byte-formatter
family), layers.py:1223, database_concurrency.py:281,
gene_measurement_sweep.py:1149, sequencing.py:436/461/521 (the consensus
padding family), response_distribution.py:293 and sudoku.py:507.

THREE FAMILIES, not thirteen accidents. A guard after a normalising call that
cannot return the guarded value; a loop whose last iteration always returns; and
a guard whose precondition the caller four lines above has just established.
Each is worth a shared helper or a comment rather than a pragma, and none is
worth deleting.

--------------------------------------------------------------------------------
10. A MODEL-NAME CHECK THAT CAN NEVER FIRE  (lane: spacr/model_check.py)
--------------------------------------------------------------------------------

FIXED 2026-08-30 in 3972514a. ``_known_builtin_models`` starts from the
lightweight curated settings inventory and augments it with
``torchvision.models.list_models`` when TorchVision is available. An absent or
broken optional TorchVision registry therefore cannot turn validation off,
and ``check_model`` now rejects ``maxvit_tt`` before training while accepting
``maxvit_t``.

The former current-behaviour pin was updated into the production contract in
``tests/test_the_model_check_cannot_currently_name_an_unknown_model.py``.
Rechecked with the focused model-check coverage tests on 2026-08-30: 22
passed.

--------------------------------------------------------------------------------
11. AN UNKNOWN ORGANELLE PROPERTY LOSES WHICH ORGANELLE IT WAS
     (lane: spacr/feature_dict.py)
--------------------------------------------------------------------------------

FIXED 2026-08-30 in 7eb8f2f8. ``_parse_organelle_summary`` now retains the
matched role and passes it to the unknown-feature fallback, so custom
properties under ``organelleb``, ``organellec`` and ``organelled`` remain
attributed to the object that produced them. Known properties still
canonicalise to the shared vocabulary and keep the role-specific
``object_type``.

The former current-behaviour pin now parametrizes all four organelle roles in
``tests/test_the_organelle_summary_names_resolve_to_one_vocabulary.py``.
Rechecked against current nightly on 2026-08-30: 8 passed. The shared
``spacr/feature_dict.py`` had unrelated uncommitted edits during this audit,
so no source file was touched here.

--------------------------------------------------------------------------------
12. A SECOND COPY OF THE OBJECT-ID VOCABULARY, DRIFTED
     (lane: spacr/predictions.py)  -- FIXED 2026-08-30
--------------------------------------------------------------------------------

``spacr/predictions.py::_OBJECT_ID_COLUMNS`` was a hand-written copy of
``spacr.utils.PNG_OBJECT_ID_COLUMNS``, and it had drifted:

    written by filepaths_to_database : cell_id, nucleus_id, pathogen_id,
                                       cytoplasm_id, organelle_id,
                                       organelleb_id, organellec_id,
                                       organelled_id
    read by _prcfo_from_metadata     : cell_id, nucleus_id, pathogen_id,
                                       cytoplasm_id, object

The four ORGANELLE roles were written and never read. Also absent was
``schema.OBJECT_KEY`` (``objectID``), the canonical spelling.

CONSEQUENCE: an organelle-mode classifier score table carries the metadata but
no ``prcfo``, ``_prcfo_from_metadata`` returned None, and the join matched zero
rows -- reported as "no per-object score". That is the EXACT failure the
comment above the fallback in ``_result_keys`` says was fixed for the plainer
spellings. It was fixed incompletely, and instruction 300's organelle roles
then walked into the half that was left.

PNG_OBJECT_ID_COLUMNS carries its own comment about 'organelle' having been
missing from IT once, and causing "Columns must be same length as key" after
the organelle PNGs were on disk. The same omission, in the reading copy, a
second time.

FIXED by deriving the tuple from ``schema.ORGANELLE_ROLES`` -- where
PNG_OBJECT_ID_COLUMNS gets them too -- and appending ``schema.OBJECT_KEY``.
``spacr.utils`` cannot be imported at module scope here (it is imported lazily
inside functions to avoid a cycle), which is how the copy came to exist.
Both additions go LAST so precedence is unchanged and no table that resolved
before resolves differently.

A test now walks the whole vocabulary and asserts
``set(PNG_OBJECT_ID_COLUMNS.values()) <= set(_OBJECT_ID_COLUMNS)``, so the two
cannot drift apart again. Regression: 210 prediction and classifier tests pass.

This is instruction 145's theme -- one reader, one writer, one vocabulary --
found in a third place.

  * spacr/schema.py:287 -- the ``raise RuntimeError`` in the module-level
    OBJECT_TYPES validation. It fires only for a role that is SHIPPED, and
    OBJECT_TYPES is a literal in the same module, so reaching it means editing
    the source rather than arranging an input. An import-time assertion whose
    false side cannot be exercised from a test without rewriting the file
    underneath it.

    Covered instead by tests/test_an_object_role_cannot_break_a_typed_id.py,
    which asserts the RULE against the roles that actually ship, demonstrates
    the round trip the rule protects, and shows the collision it prevents
    ('cell1' + '7' == 'cell' + '17'). That is the test that catches the real
    risk: the organelle roles are the ones a maintainer would extend, and the
    fifth is naturally called 'organelle2'.

--------------------------------------------------------------------------------
13. THE MEASUREMENT ITSELF IS NOT YET TRUSTWORTHY  (lane: item 288)
--------------------------------------------------------------------------------

Two complete full-suite runs now exist and their whole-library figures DISAGREE
in the wrong direction:

    2026-08-29, commit 4eccf7b8   95.00%   494 modules, 167 at 100%
                                  46,918 passed / 453 failed / 1:49:41
                                  2 'node down' events

    2026-08-30, commit 955faa4e   88.99%   508 modules, 132 at 100%
                                  47,673 passed / 421 failed / 2:05:00
                                  3 'node down' events

The second run has 755 MORE passing tests, 32 FEWER failures, and 99 new test
files -- and reports six points LESS coverage. That is not a regression, and
the tests prove it is not: 186 modules read materially lower and only 17 read
higher, which is a systematic loss rather than 186 separate ones.

THE CAUSE IS THE LOST WORKERS. Each 'node down' takes an xdist worker's
coverage data with it. The tests it had already run still count toward the
pass/fail summary -- the master reassigns them or the run continues -- but the
lines they executed are gone from the report.

PROOF, on a module whose gap this session closed: spacr/figure_font.py measures
51/51 units when its own test file is run alone, 38/51 in this full run, and
31/51 in the previous one. The module did not change between the last two
numbers; the worker that ran its tests did.

CONSEQUENCE FOR ITEM 288. Its contract wants "a clean frozen-worktree coverage
run [that] reports 100% statements and branches for every spaCR module". A run
that silently loses a worker's data cannot demonstrate that -- it will report
modules as short when they are complete, and no amount of test-writing will
close a gap that is an artifact of the harness.

WHAT WOULD FIX IT, in preference order:

  1. ``-p xdist --dist loadfile`` with ``--max-worker-restart`` set, so a
     worker that dies is replaced and its FILES are re-run rather than lost.
     This also restarts the interpreter periodically, which addresses the
     six-fold deceleration HANDOFF 3d records.
  2. Batched runs in fresh processes, combining ``.coverage.*`` at the end --
     the design already written at
     covscratch/run_coverage.sh. Slower, but every batch's data is on disk
     before the next begins, so one death costs one batch.
  3. A single-process run (no -n). Trustworthy and far too slow for 48,000
     tests, but it is the reference a batched run should be checked against
     once.

Until one of those is done, every whole-library percentage in this instruction
is a FLOOR, and the per-module rows are only trustworthy where they read 100%.
Individual closures in this sweep were each verified by re-running the module's
own tests under coverage and checking ``executed_branches`` for the specific
arc, which is why they are reliable while the aggregate is not.

  * spacr/hit_attribution.py:446 -- ``if folds < 2: raise
    InsufficientDesignError`` in ``crossfit_candidate_probabilities``.
    Unreachable, because a guard 30 lines above already requires at least four
    independent target wells AND four independent control wells. That forces
    at least eight distinct wells, so well-level grouping yields at least eight
    groups; the plate-level path is only taken when there are at least four
    plates. ``folds = min(max(2, n_splits), group_count)`` is therefore always
    at least 2.

    Left in place: it is the correct guard for the grouping, and the floor
    above it is a scientific rule that could be relaxed without anyone
    remembering this depends on it.

14. THE ANNOTATION BACKDROP WAS SQUARE BECAUSE setWidget() TURNS ON AUTO-FILL
    ------------------------------------------------------------------------
    spacr/qt/screens/annotate.py, the grid backdrop (GRID_OBJECT_NAME).

    The registered QSS block declares `border-radius: 8px` and the block was
    in the live application sheet, verified. The panel still painted a square
    corner in the running application.

    The cause is one line of Qt behaviour: QScrollArea.setWidget() turns
    autoFillBackground ON for the widget it is given. The auto-fill runs
    before the stylesheet's own painter and covers the entire rectangle with
    the palette's window brush -- and QSS had already propagated the block's
    `background` INTO that palette. So the panel came out the right colour,
    which is why nobody caught it by looking, and the radius was painted
    underneath an opaque square of the identical colour.

    Measured rather than reasoned about. On a fresh screen, the corner profile
    of the holder is:

        no change                  -> [0, 0, 0, 0]   square
        setAutoFillBackground(F)   -> [4, 2, 1, 1]   rounded
        unpolish + polish          -> [4, 2, 1, 1]   rounded (clears auto-fill)
        WA_StyledBackground(True)  -> [0, 0, 0, 0]   square, no effect

    The fourth row is worth keeping. WA_StyledBackground is the usual answer
    to "my QSS background is ignored", it is what theme.make_surface sets, and
    it does nothing at all here -- the background was never being ignored. A
    fix chosen by reasoning from the familiar symptom would have been wrong
    and would have looked right.

    Fixed: setAutoFillBackground(False) after setWidget, next to the identical
    call the viewport already had. tests/qt/test_the_backdrop_behind_the_
    crops_is_rounded.py measures the corner and now passes; it was one of the
    14 test files that genuinely failed rather than failing on isolation.

15. A FOURTH UNREACHABLE-ARC FAMILY: THE GUARD INSIDE A `finally`
    --------------------------------------------------------------
    Found on spacr/errors.py:724, and it generalises.

        conn = None
        try:
            conn = connect(...)          # raises -> conn stays None
            ...
        except sqlite3.Error as exc:
            raise RunStatusUnreadable(...) from exc
        finally:
            if conn is not None:         # 724
                conn.close()             # 725
        records = []                     # 726

    Arc 724->726 -- the guard being False and falling through -- can never be
    recorded. conn is None only when connect raised, and in that case the
    finally runs with an exception in flight and control leaves by re-raising,
    not by falling through to 726. When conn is NOT None the True arc is taken.
    There is no third case.

    PROVEN, not argued. A fifteen-line reproduction with no spaCR in it:

        def f(fail):
            conn = None
            try:
                if fail: raise ValueError("boom")
                conn = object()
            except ValueError:
                raise RuntimeError("converted")
            finally:
                if conn is not None:     # line 11
                    pass                 # line 12
            return "normal"              # line 13

        f(False); f(True)  -> executed [[5,6],[5,7],[11,12]]
                              missing  [[11,13]]

    Both paths through the function ran and 11->13 is still missing.

    WHY IT MATTERS BEYOND THIS ONE LINE. This is the shape of every "close it
    if we opened it" cleanup, so it will appear once per such function across
    the package, always as exactly one missing branch. A module sitting at
    "one branch short of 100%" is worth reading before it is worth testing:
    the arc may not be a gap at all.

    The guard is still worth a test, and errors.py now has one -- not for the
    arc, but for what removing the guard would do: the finally would raise
    AttributeError on None while the real exception was unwinding, and the
    caller would get a traceback about NoneType instead of the message telling
    them to wait for the writer or check the file.

    Families so far: (1) defensive re-checks after a guarded call, (2) loop
    else-arcs where the loop cannot complete, (3) model-check dead branches,
    (4) this one.

16. control_names.py:304 IS FAMILY 1, PROVEN BY SEARCH RATHER THAN BY ARGUMENT
    ---------------------------------------------------------------------------
    rows_for's retry, when the leading token turns out to be unused:

        if tail and unused:                          # 302
            shorter = resolve_control(tail, prefix=prefix)
            if shorter is not None:                  # 304
                ...
        if not found and strict:                     # 310

    Arc 304->310 cannot be taken. resolve_control returns None for exactly one
    reason -- str(x).strip() is empty -- and `tail` comes from spec.typed,
    which resolve_control itself already stripped. A truthy tail taken from a
    stripped string always has a character strip() will not remove, so line
    302 passing guarantees line 304 passing.

    Worth recording HOW this was settled, because the first attempt was wrong.
    The obvious input, "sgCtrl_   ", looks like it should reach the arc and
    does not: the strip happens before the partition, so the tail is "" and
    line 302 rejects it. A test was written, committed to the reasoning, and
    the arc check said MISSING. The candidates were then enumerated directly:

        '   ' '\t' '\n' '\xa0' ' '   truthy, strip to empty -> None
        '​' '﻿'                 truthy, DO NOT strip   -> a spec
        'a_​'  -> spec.typed 'a_​', tail '​', retry -> a spec

    The two zero-width characters are the interesting ones: they are truthy
    and `strip()` leaves them alone, so they are exactly the inputs that would
    reach the arc if anything could -- and they resolve to a spec instead.

    The tests written for it were kept, with their docstrings corrected to say
    what they actually check: a name that is nothing but a trailing separator
    matches zero rows without raising, and still raises under `strict`. Both
    are worth having; neither covers the arc, and the docstring no longer
    claims it does.

    THE HABIT THIS IS AN ARGUMENT FOR. Reasoning produced a plausible input,
    the test passed, and nothing was covered. Only the arc check caught it.
    That is now three times in this instruction (5, 7, this) that a test
    exercising nothing looked correct.

17. A DEAD `except` THAT LEFT NaN WHERE THE CODE MEANT None (sklearn 1.7+)
    ------------------------------------------------------------------------
    spacr/regression_annotation.py, `_score_holdout`.

        try:
            auc = float(roc_auc_score(y_test, values))
        except ValueError:
            auc = None

    A hold-out that comes out all-positive or all-negative is routine on a
    small screen. scikit-learn RAISED ValueError for it up to 1.6 and RETURNS
    NaN with an UndefinedMetricWarning from 1.7 (measured on 1.9.0). So the
    handler is dead on any current install, and `FitReport.roc_auc` carries
    NaN where its own type says `Optional[float]` and its docstring says
    "None when the hold-out has one class".

    THE COST IS NOT COSMETIC. Two readers check for None:

      FitReport.lift    falls back to balanced accuracy when roc_auc is None.
                        `nan is None` is False, so it takes NaN as the base
                        and returns NaN. That lift is the number the
                        named-method leak check compares between the fit with
                        the score's input columns and the fit without them --
                        the control that tells a real phenotype from a leak.
                        NaN compares False against everything, so the check
                        stops detecting either.

      FitReport.summary prints "ROC AUC nan" where it means "ROC AUC n/a".

    Fixed by normalising a non-finite AUC to None, keeping the except for
    older scikit-learn. The test asserts BOTH consequences -- the lift falling
    back and the summary saying n/a -- not merely that the field is None; and
    removing the fix makes it fail, which was checked rather than assumed.

    WORTH GENERALISING. This is a dependency changing an error CONTRACT
    without changing an API: same call, same arguments, same return type,
    different way of saying "undefined". A pinned upper bound would not have
    caught it and a type checker cannot see it. The only thing that does is a
    test that drives the degenerate case -- and the reason there wasn't one is
    that the branch looked defensive.

18. SIXTEEN COPIES OF THE SAME DEAD GUARD: `if parent:` AFTER `abspath()`
    -----------------------------------------------------------------------
    Every writer in spaCR that creates its own destination folder had this:

        target = os.path.abspath(...)
        parent = os.path.dirname(target)
        if parent:                       # can never be False
            os.makedirs(parent, exist_ok=True)

    `abspath` returns a rooted path, so its `dirname` is at least "/" -- and
    at least "C:\" on Windows. The False arc is unreachable. Enumerated over
    the inputs that look like they might produce nothing:

        dirname(abspath(''))       -> the cwd          truthy
        dirname(abspath('x.png'))  -> the cwd          truthy
        dirname(abspath('/'))      -> '/'              truthy
        dirname(abspath('/a'))     -> '/'              truthy
        dirname(abspath('//'))     -> '//'             truthy
        dirname(abspath('..'))     -> a real folder    truthy

    None of them. The guard was presumably written for the BARE FILENAME case
    -- "counts.csv", nothing else -- and for that input dirname answers the
    working directory, not "".

    Sixteen sites: align, convert, counting, curation (x2), figures/headless,
    figures/scene, illumination, ml (x2), plate_qc, roi, style_base,
    tabular (x2), volcano_style.

    Thirteen removed, in the eleven files no other lane had open. align.py and
    ml.py were modified in the shared tree at the time and keep theirs for
    whoever lands those; they are two arcs.

    tests/test_a_parent_directory_is_always_there_to_make.py asserts the
    property over eleven path shapes, and then asserts the BEHAVIOUR through
    two real writers -- a bare filename still lands in the working directory,
    and a nested destination still has its folders made. The property alone
    would not have caught deleting the makedirs along with the guard.

    WHY THIS ONE IS WORTH THE CHURN. Sixteen uncoverable branches is sixteen
    modules that cannot reach 100% however many tests are written, and 288
    allows no pragma to hide them. The alternative was sixteen entries in this
    document arguing the same thing sixteen times.

19. response_distribution.py:293 -- `if handles:` AFTER TWO `hist` CALLS
    ---------------------------------------------------------------------
    The twin-axis branch of `plot_response_distribution`:

        ax.hist(before, bins=40, ...)
        twin.hist(after, bins=40, ...)
        handles = [ax.patches[0]] if ax.patches else []
        handles += [twin.patches[0]] if twin.patches else []
        if handles:                      # 293
            ax.legend(...)

    Arc 293->306 cannot be taken. `Axes.hist` with a fixed `bins=40` adds
    forty patches whatever the data -- measured, an EMPTY array still produces
    forty zero-height bars -- and the only input that produces none is one
    that makes `hist` RAISE (an all-NaN array: "autodetected range of
    [nan, nan] is not finite"), which never reaches line 293 at all.

    So `ax.patches` is non-empty by the time the guard runs, and `handles`
    always holds at least one entry. Family 1 again: a defensive re-check
    after a call that already guarantees the condition.

    No test was written for it. The two `if ax.patches else []` guards on the
    lines above are the same shape and are equally dead, which is three arcs
    in four lines.

20. THE LINE-NUMBER TRAP AGAIN, THIS TIME SELF-INFLICTED (see also section 7)
    -------------------------------------------------------------------------
    Section 7 recorded that a coverage report's line numbers stop matching the
    working tree when the tree has edits the measured copy did not. Here is the
    same trap arriving by a route worth naming separately: MY OWN EDIT, EARLIER
    IN THE SAME SESSION.

    Removing the dead `if parent:` guard from counting.py deleted one line at
    401. Every line after it shifted up by one, so the batched run's

        spacr/counting.py arc 450->449

    became arc 449->448 in the tree. Checking the old numbers reported MISSING
    for an arc the new test had just covered, and the obvious next move -- write
    another test -- would have been chasing a number, not a gap.

    THE RULE THAT SURVIVES THIS. A coverage artifact is only valid against the
    exact tree it was measured on. After any source edit, either re-measure or
    re-derive the line from the code rather than from the report. The check
    script takes line numbers, so it inherits the staleness of whatever it is
    handed; reading the three lines around the arc costs seconds and settles it.

    Eleven files were edited in the guard removal, so eleven files' arcs in the
    2026-08-30 batched artifact are off by one or two after the edit point. The
    artifact is still correct for the commit it names.

21. TWO MORE FAMILY-1 GUARDS, FOUND WHILE CLOSING SINGLE-ARC MODULES
    ------------------------------------------------------------------
    portable_paths.py:55, `candidate_roots`:

        here = os.path.abspath(...)
        out = []
        for _ in range(_MAX_CLIMB + 1):
            if here and here not in out:      # 55 -- never False
                out.append(here)
            parent = os.path.dirname(here)
            if parent == here:
                break
            here = parent

    `here` is abspath'd, so it is never falsy. And each step replaces it with
    its own parent, which is strictly shorter, until the fixed point at "/"
    where the loop breaks -- so it can never repeat either. Both halves of the
    guard are dead.

    response_distribution.py:293 is recorded in section 19.

    RUNNING TALLY OF UNREACHABLE ARCS FOUND AND ACCOUNTED FOR:
      family 1 (defensive re-check after a guarded call)
        control_names.py:304, portable_paths.py:55,
        response_distribution.py:291, 292, 293,
        and the sixteen `if parent:` sites in section 18 (thirteen removed)
      family 4 (guard inside a finally)
        errors.py:724, qt/synthetic.py:1426, qt/widgets/field_fade.py:258

    None of them can be covered and none may carry a pragma, so each is either
    deleted (section 18) or written down here. That is the only way instruction
    288's per-module numbers mean anything: a module short of 100% is either a
    gap or a line in this document, and never a shrug.

22. THE COLOUR CHOOSER ADDED A DUPLICATE ROW EVERY TIME IT WAS USED
    ------------------------------------------------------------------
    spacr/qt/widgets/save_figure_dialog.py, `_resolve_choice`.

    The background colour box ships with:

        transparent ''    white '#FFFFFF'    black '#000000'    choose a colour…

    Picking a colour ran `box.findData(chosen.name())` to see whether the list
    already had it. `QColor.name()` answers in LOWER case; the shipped entries
    are written UPPER. So choosing white found nothing and inserted a second
    row -- displayed as "#ffffff", sitting under one displayed as "white",
    holding the same colour. Once per pick, without limit.

    Measured rather than reasoned about:

        before: transparent, white(#FFFFFF), black(#000000), choose…
        after : transparent, white(#FFFFFF), black(#000000), #ffffff, choose…

    Fixed by falling back to a case-insensitive scan before inserting. The
    exact `findData` stays first, because it is right whenever the case does
    match and it is the cheaper answer.

    Found by writing a test for the "already on the list" branch, which had
    never run -- the existing tests only ever picked a colour the list did not
    have. The test was written expecting it to pass; it failed, and the
    failure was the defect rather than the test. Removing the fix makes it
    fail again, which was checked.

23. A FOURTH TEST THAT ASSERTED NOTHING, AND WHAT THE FOUR HAVE IN COMMON
    ----------------------------------------------------------------------
    `_check_app_specific(settings, app)` returns an empty list for an app it
    has no rules for. A test written as

        problems = V._check_app_specific({...}, "explain_cv_model")
        assert "surrogate_model" not in _keys(problems)

    passes whatever the validator does, because the app key is "explain_cv"
    and the whole block was skipped. `not in []` is True.

    THE SHAPE, NOW SEEN FOUR TIMES (sections 5, 7, 16, this):

      * the runctx factory test        -- built the wrong object
      * a `to_crop_settings` stub      -- made `dict()` fail, so {} came back
      * the control_names retry        -- an input that could not reach the arc
      * this                           -- an identifier the function ignores

    Every one of them ASSERTS AN ABSENCE, and an absence is what a function
    returns when it did nothing. Every one of them passed. The arc check
    caught all four and nothing else would have.

    TWO HABITS FOLLOW.

    First: run the arc check on every test written for a named branch, before
    committing. That is already the practice here and it is the only thing
    that has ever caught this.

    Second, and cheaper: when a test asserts that something is ABSENT, assert
    in the same test that the machinery is live -- drive one input that
    produces the thing, so "no complaint" is the code agreeing rather than the
    code never having run. Both new tests here do that now, and it makes them
    fail on a mistyped app key without needing coverage at all.

24. THE ARC CHECKER COULD NOT READ AN EXIT ARC
    -------------------------------------------
    coverage.py writes a return or fall-off-the-end as a NEGATIVE target:

        [617, -610]   "line 617 exits the function that starts at 610"

    `check_arcs.py` split each target on "-" and int()'d both halves, so
    `617--610` produced an empty second field and the script died with

        ValueError: invalid literal for int() with base 10: ''

    -- in the middle of verifying a commit. Every early return in the package
    is an arc of that shape, so the one tool this work depends on could not
    check the single most common branch there is. It was worked around by
    hand twice before being fixed, which is twice too many.

    Fixed: split on the first separator after position 0, so a leading minus
    stays with the head and the tail keeps its own sign.

    Worth noting for what it says about the artifact rather than the script.
    A tool that silently could not answer a whole CLASS of question is the
    same failure as a coverage run that silently loses a worker: the number
    it gives is right, and it is not the number you asked for.

25. annotate_engine.py:870 -- `if not tokens` AFTER AN EMPTY-TEXT CHECK
    ---------------------------------------------------------------------
    `parse_image_type`:

        text = (expression or "").strip()
        if not text:
            return "", []
        tokens = _tokenise_image_type(text)
        if not tokens:            # 870 -- never True
            return "", []

    `_tokenise_image_type` returns [] for whitespace only, and whitespace has
    already been stripped away by the check above it. Enumerated: ",,,"
    tokenises to [',,,'], "()" to ['(', ')'], "\x00" to ['\x00'], "*" to
    ['*'] -- every non-whitespace input produces at least one token, and
    every whitespace input is gone before line 869.

    Family 1 again. Not removed: unlike section 18's sixteen copies this is a
    single site, and the tokeniser is the kind of function that grows a rule
    -- the day it learns to drop a token class is the day this guard starts
    mattering. Recorded instead, with a test that pins WHY it cannot fire: a
    stray comma is read as a literal to match on, which finds nothing and
    says so by showing nothing, rather than raising a syntax message about a
    character the user did not mean to type.

26. utils.py:9755 -- A TYPE CHECK NO CALLER CAN SATISFY
    ----------------------------------------------------
    `feature_folder_name`'s inner helper:

        def _one(member):
            if isinstance(member, int):
                return str(member)              # 9755 -- unreachable
            return re.sub(r'[^0-9A-Za-z]+', '_', str(member)).strip('_') or 'x'

    `_one` has exactly two call sites and neither can pass it an int:

        if isinstance(selection, int):   return f"channel_{selection}"
        if not isinstance(selection, list): return _one(selection)   # not an int
        if all(isinstance(m, int) ...):  return "channels_" + ...    # inline
        return "_".join(f"channel_{m}" if isinstance(m, int) else _one(m) ...)

    The last line makes the int case itself, so `_one` only ever sees the
    non-int members. And `feature_selection` canonicalises before any of this
    -- a numpy integer comes back a plain `int`, a float and a bool raise --
    so there is no third type that slips past `isinstance(selection, int)` and
    then satisfies `isinstance(member, int)`.

    THE MISTAKE WORTH RECORDING. The tests written for it pass, and the commit
    message first claimed they covered this line. They did not; the arc check
    said MISSING and the message was amended. That is the fifth time in this
    document -- see section 23 -- and it is the first where the WRONG CLAIM
    reached a commit message before being caught. The lesson stands and gains
    a clause: arc-check before writing the message, not after.

    The tests are kept. They pin that a channel number in a mixture keeps its
    digits, that no channel is dropped from a multi-channel name, and that a
    filter slugifying to nothing still names a folder -- all real properties
    of the folder a run writes its features into.

    A THIRD ROUTE TO THE SAME TRAP (see sections 7 and 20). Both earlier
    notes are about the tree having changed -- once because the shared tree
    carried edits the measured copy did not, once because I had edited it
    myself earlier in the session. The third is ANOTHER LANE's uncommitted
    work: settings_model.py had changes in the working tree that were not in
    the measured revision, so the artifact's "missing lines 9686-9688" pointed
    at an `except Exception` handler while the same numbers in the tree were a
    `return` and two comment lines.

    The give-away is the one worth remembering: coverage.py never reports a
    COMMENT as missing. A missing-line run that lands on comments is not a
    gap, it is proof the artifact and the tree disagree.

==============================================================================
APPENDIX A -- 58 DEFECTS REPORTED BY THE COVERAGE SWEEP (2026-08-30)
==============================================================================

Found by 79 agents writing tests one module at a time. REPORTED, NOT
VERIFIED: the sweep's adversarial stage checked that each agent's ARCS moved
and that its tests were not vacuous. It did not re-derive these defect
claims. Several carry their own measurements and read as solid; others may
be misreadings. Treat each as a lead with an address, not as a finding.

They are recorded because an agent that reads a module closely enough to
cover its branches is the best-placed reader it will get, and throwing that
away to keep the coverage number clean would be the wrong trade.

A1. spacr/qt/crash_recovery.py:79 (note_that_a_launch_began) together with :100 (note_a_clean_shutdown)
    WHAT: The unclean-exit test is `os.path.exists(marker)`, which is True for a *directory* at `<logdir>/running.marker`, while `os.remove` at line 100 can never delete one (it raises IsADirectoryError and is swallowed at 103-104). A stale directory at that path — a botched restore, an rsync of a tree, a sync tool that materialises a name as a folder — is therefore permanently interpreted as "the last run died" on every single launch, and no clean shutdown can ever clear it.
    COST: On such a machine the very first launch already reports 1 unclean exit instead of 0, so the backdrop is dropped one real crash earlier than the documented "two in a row", and the misdiagnosis is self-perpetuating because the marker cannot be removed. The user loses the animated backdrop with no crash having occurred and no setting they can point at. `os.path.isfile(marker)` (and, on the removal side, noticing that the path is not a file) would confine the mechanism to markers it actually wrote. Reported only, not fixed.
    VERDICT 2026-08-31: CONFIRMED and FIXED at 84e6b2bf1. exists() answered True
    for a directory and os.remove could never clear it. Narrowed to isfile; an
    obstruction now logs at WARNING because it does switch recovery off.

A2. spacr/resources/home/versions/_generators/common.py:436-441 (_with_late_registrations)
    WHAT: The fallback band is matched by exact title string. When no band in `cats` carries that title the loop runs to exhaustion, `missing` is silently discarded and the function returns the table unrepaired -- there is no diagnostic, unlike the loud AssertionError raised a few lines above for retired keys.
    COST: A typo or a renamed band title turns the documented safety net off without a word. The newly registered app is then absent from the categorisation and `check_coverage` raises `keys not categorised: [...]` at module import of the variant generators, taking all thirty Home renders down -- exactly the failure the function exists to prevent, but now blamed on the wrong place. All four shipped tables currently name a title that exists, so no live breakage today; the risk is the next rename.
    VERDICT 2026-08-31: CONFIRMED and FIXED at fe2271103. Raises now, naming the
    band asked for and the bands that exist. This overrode a deliberate pin;
    both sides are recorded in the dated section below.

A3. spacr/resources/home/versions/_generators/common.py:63-68 (_prefer_checkout_package)
    WHAT: `package_root` is built from `os.path.normcase(os.path.realpath(root))` but each origin is compared with `os.path.realpath(origin)` only, without `normcase`. The two sides are normalised differently.
    COST: On a case-insensitive platform (Windows) an already-imported checkout `spacr` whose recorded origin differs in case is judged foreign, so every `spacr*` entry is purged from `sys.modules` and re-imported. Harmless on Linux (normcase is the identity) and self-correcting since the checkout is `sys.path[0]`, but it makes the guard's own test of localness unreliable off-Linux.
    VERDICT 2026-08-31: STALE -- already fixed. Both sides of the comparison now
    apply normcase (common.py:54,56). No action.

A4. spacr/qt/screens/batch.py:841-855 (BatchScreen._load_log), constant at line 90
    WHAT: The log pane reads the HEAD of the log file, not the tail: `text = handle.read(_LOG_TAIL_BYTES)` opens the file and reads from byte 0 with no `seek(-_LOG_TAIL_BYTES, os.SEEK_END)`. The constant is named `_LOG_TAIL_BYTES` and the code immediately scrolls the pane to the bottom (`bar.setValue(bar.maximum())`), so both the name and the surrounding code state the intent is the tail.
    COST: For any job whose log exceeds 200 KB - routine for a cellpose mask run - the user is shown the first 200 KB and the pane scrolls to the bottom of that HEAD. The traceback or exit message that explains why the job failed is at the end of the file and is never displayed, and the pane also stops updating (the first 200 KB never change again) so a live job appears frozen. This is exactly the diagnosis path the Batch Runner exists to serve at 3 a.m.
    VERDICT 2026-08-31: STALE -- already fixed. `_load_log` now seeks to
    SEEK_END, measures the file and reads the last _LOG_TAIL_BYTES
    (batch.py:849-853). It is the tail, as the constant always claimed. The
    concurrent session was about to take this; it needs nothing.

A5. spacr/qt/screens/batch.py:352-369 (BatchScreen.add_job), docstring line 357 vs code line 365
    WHAT: The docstring says `:param settings: settings file path; the settings box when empty`, but the body passes `settings=settings` straight through with no fallback to `self._settings_edit.text()`. Only `module` has the documented fallback (`module or self._module_combo.currentData()`).
    COST: A caller that relies on the documented contract - `screen.add_job(module='mask')` after the user has typed a path into the settings box, e.g. from a future keyboard shortcut or a drag-and-drop handler - builds a Job with an empty settings path. `Queue.add` then refuses it with a validation error about a missing settings file rather than queueing the file the user typed, and the user is told their visible input does not exist. Today only `_on_add_clicked` calls it, and that one passes the box text explicitly, so the mismatch is latent.
    VERDICT 2026-08-31: NOT VERIFIED HERE -- screens/ belongs to the concurrent
    session and the claim is latent (only _on_add_clicked calls add_job, and it
    passes the box text explicitly). Left for that lane.

A6. spacr/qt/widgets/annotation_umap_tab.py:142-184 (AnnotationUmapTab.__init__) vs. 363-383 (guide_table) and 274/329/382 (_score)
    WHAT: __init__ sets self._frame and self._embedding but never self._controls or self._effects; those two attributes are created only by set_frame(). guide_table() is a public, documented method that reads self._effects, so calling it on a freshly constructed tab raises AttributeError: 'AnnotationUmapTab' object has no attribute '_effects' (verified live). _score() has the same hole on self._controls, reachable for any caller that assigns _frame directly rather than through set_frame().
    COST: A caller that rebuilds the per-guide table from a stored result -- e.g. restoring a previous run into a newly opened tab, or any code path that populates the frame without going through set_frame -- crashes the panel with an AttributeError instead of showing a table. The two guarded paths in run() (no frame / no cells) hide it today, so it will surface only when someone adds a second entry point.
    VERDICT 2026-08-31: STALE -- already fixed. `_controls` and `_effects` are
    both initialised in __init__ (annotation_umap_tab.py:154-155). No action.

A7. spacr/qt/theme.py:1732 `_scrim_bounds(palette, role, colour_role, under)`
    WHAT: The `role` parameter is never read in the body: `base = _channels(palette[colour_role])` and `inks = ... _scrim_rules(colour_role)` use `colour_role` only. The two published solvers this function is documented to reproduce exactly (`legible_scrim_floor`, `present_scrim_ceiling`) use `palette[colour_role or role]` and `_scrim_rules(colour_role or role)`, i.e. they fall back to `role` when `colour_role` is None.
    COST: No live miscomputation today: the only caller (line 1807) draws `colour_role` from `SCRIM_ROLES`, whose values are all non-None strings. But the equivalence the docstring insists on ('it has to stay exactly them') is not actually replicated: a future caller passing `colour_role=None` -- the documented, supported argument for the public solvers -- would get `KeyError: None` out of the drift solver at import instead of the role's own bounds, and the dead parameter hides that from a reader comparing the three implementations.
    VERDICT 2026-08-31: CONFIRMED and FIXED here. `role` was accepted and never
    read -- only `colour_role`, twice -- so the function was not the published
    solvers it says it has to stay exactly. Both now read `colour_role or
    role`. Latent, not live: every SCRIM_ROLES value is a non-None string, so
    no shipped caller reached the KeyError. Pinned in
    tests/qt/test_spaceout_looks_alive.py.

A8. spacr/cell_montage.py:1543-1557 (the attribution pre-flight inside select_montage)
    WHAT: `fractions_by_well` is guide-keyed (built by `_well_guide_fractions`), but `preflight` is called with `name`, which for a gene-level montage is a GENE. `guide_attribution.preflight` therefore never finds it and returns the verdict `"<GENE>: no well carries it."`, which is appended to `notes` and reaches the caption. Reproduced: a gene-level montage with picking='multivariate' over wells r1_c1 (GENE_1=0.4) and r1_c2 (GENE_2=0.5) draws 5 cells from those two wells while plan.notes == ('GENE: no well carries it.',). A guide-level montage on the same data gets a correct verdict, so only gene-level coefficients are affected.
    COST: Every gene-level montage that uses an attribution picker prints a caption line stating the coefficient is present in no well, directly above the cells it just selected from wells that do carry it. The pre-flight exists to explain an empty montage; here it makes a false statement about a non-empty one, and a reader who believes it will conclude the count data is broken.
    VERDICT 2026-08-31: STALE -- already fixed, and thoroughly. select_montage now
    pre-flights the guides a gene covers (`targets = covered if resolved_level
    == 'gene' else [name]`) with a comment naming this exact false verdict.

A9. spacr/cell_montage.py:1716-1719 vs 1690-1693 and 1713-1715
    WHAT: When `picking='attributed'` (or 'assigned') meets a well holding exactly one guide, `len(here_fractions) > 1` is False and control falls straight through to 1743 with `picked_by` still 'rank' and no note recorded. The two sibling pickers both disclose their fallback: multivariate sets picked_by to "attributed (this well has one guide, or none of the swept measurements are on the objects)" and sudoku to "sudoku could not run on this screen; fell back to rank".
    COST: A user who chose attribution gets a well captioned with pure rank arithmetic ("round(1 x 6) = 6, where 1 is this guide's 1 normalised by 1") and nothing anywhere says the picker they selected did not run on that well. In a montage mixing single-guide and multi-guide wells, two wells are chosen by different rules and only one of them says so.
    VERDICT 2026-08-31: CONFIRMED and FIXED here. The outer `if len(here_fractions)
    > 1` had no else, so a single-guide well kept picked_by='rank' silently.
    NOTE FOR THE NEXT READER: the obvious fix is wrong twice over. A non-'rank'
    prefix makes `_by_rank` False, which REPLACES the round(share x n) line that
    actually explains the chosen cells; a 'rank' prefix keeps it but that branch
    never printed picked_by at all, so the disclosure vanished. The fallback
    reason now rides WITH the arithmetic rather than instead of it.

A10. spacr/cell_montage.py:2572-2574 against spacr/crops.py:3564-3573
    WHAT: `resolve_montage_crop_source` unwraps a bare list/tuple `src` to its first element, but `resolve_crop_source` unwraps only inside its Mapping branch. A bare list therefore reaches `_looks_like_experiment_root(str(src))` as the Python repr of the list. Verified: `resolve_montage_crop_source([root])` for a root holding merged/*.npy returns available=False with reason "no crop source available for /mnt/firecuda2/codex/repo/spacr/['/tmp/.../plate1']", while the same root as a plain string or as {'src': [root]} resolves to the merged source.
    COST: A caller that passes src as a list -- the shape the wrapper's own code at 2573 is written for -- gets "no crop source available" for a run whose crops are present, and the message quotes a path built from a list repr joined onto the current working directory, which points a user at a folder that does not exist and cannot exist.
    VERDICT 2026-08-31: STALE -- already fixed. The bare list/tuple is unwrapped
    outside the Mapping branch (cell_montage.py:2584-2585). No action.

A11. spacr/core.py:250-253 (preprocess_generate_masks, the `consolidate` block) via spacr/utils.py:10710 (generate_image_path_map)
    WHAT: The consolidate block assumes `settings['src']` is a single string, but it runs BEFORE the str->list normalisation at 255-256 and after line 180, which preserves a list. With the documented plural form (`src` = list of plate folders) plus `consolidate=True`, `generate_image_path_map(settings['src'])` calls `os.walk(<list>)` and the run dies with `TypeError: expected str, bytes or os.PathLike object, not list`. Reproduced directly: src=[p1, p2], consolidate=True, masks=False. `os.path.join(settings['src'], 'consolidated')` at 253 would fail the same way for the same reason.
    COST: Two documented settings that are each individually supported (the docstring says src is 'str or list of str' and consolidate 'copy nested images into src/consolidated') cannot be combined. The user gets an os.walk TypeError that names neither `src` nor `consolidate`, before any plate is processed, with no indication that dropping to one folder at a time is the workaround.
    VERDICT 2026-08-31: STALE -- already fixed. The consolidate block now
    normalises first (`sources = settings['src'] if isinstance(..., list) else
    [settings['src']]`), loops, and writes back a list (core.py:250-258).

A12. spacr/parameter_sweep.py:369-375 (_named_control_rows)
    WHAT: The control's effect/q/p are read from `frame.loc[hit].iloc[0]` -- the FIRST row in table order that matches the alias -- while `{alias}_rank` is computed from `position.idxmax()` on the effect-sorted table, i.e. the BEST-ranked match. When an alias matches more than one guide (the normal case: controls={'positive': 'gra14'} matches gra14_1, gra14_2, ...), the two halves of the same row describe different guides. Verified: DataFrame(grna=['gra14_1','other_1','gra14_2'], coefficient=[0.02,0.5,0.95], q_value=[0.99,0.02,0.005]) returns {'positive_rank': 1, 'positive_effect': 0.02, 'positive_q': 0.99}.
    COST: A sweep row reports the positive control at rank 1 with q=0.99 and effect 0.02. rank_trials/summarise_sweep then rank that setting top on a rank belonging to a guide whose effect and q the row does not show, so a user picks a configuration on evidence that is not in the row they are reading.
    VERDICT 2026-08-31: STALE -- already fixed. `row` is reassigned from
    `ranked.loc[position].iloc[0]`, so effect/q/p come from the same row the
    rank names. Checked one step further, because `int(position.idxmax()) + 1`
    reads like a label/position confusion: `ranked` is built with
    `reset_index(drop=True)` (line 359), so idxmax IS the positional rank.
    Correct as written.

A13. spacr/parameter_sweep.py:1322-1324 (run_sweep, in-process failure path)
    WHAT: The `error.txt` write in run_sweep's `except Exception` handler is unguarded, while the identical write on the parallel path (_execute_trial, lines 964-969) is wrapped in `try/except OSError: pass`. An OSError from that open (read-only or full destination) is raised from inside the handler, aborting the whole sweep.
    COST: One trial failing on a destination that cannot be written turns a recorded failed row into a crash that ends the sweep and loses every row after it. Same family as the already-xfailed defect for run_sweep's every-trial CSV write (tests/test_cov_w2_5_parameter_sweep.py::test_a_csv_that_cannot_be_written_does_not_stop_the_sweep), but a separate unguarded write.
    VERDICT 2026-08-31: STALE -- already fixed. BOTH error.txt writes are wrapped
    in try/except OSError (parameter_sweep.py:1022-1027 and 1410-1415).

A14. spacr/volcano_style.py:683-692, _annotate
    WHAT: When a labelled point's y value falls inside the break of a split axis (outside every panel's ylim), the `for panel in panels` loop falls through and the label is annotated onto `panels[0]` at data coordinates outside that panel's y range. Annotations are created with clip_on=True, so the text is clipped away entirely. Verified by drawing: with split_y_lims=((0,2),(5,10)) and a point at y=3.5, the annotation's window extent comes back at y 0-1 while the panel occupies y 596-845 -- the text is present in `panels[0].texts` but paints nothing.
    COST: A user who explicitly types a guide into the annotation list on a broken-axis volcano gets no label and no message; the label is silently swallowed rather than clamped to the nearest panel edge or reported as unplottable. Low severity (the point itself is also hidden by the break), but the `target = axis` fall-through is effectively a no-op that reads like a deliberate fallback.
    VERDICT 2026-09-02: RESOLVED by `4d153c1b34`. A y value inside the split
    is clamped to the nearest visible panel edge and offset inward.
    `test_a_label_lands_on_the_panel_whose_range_can_show_its_point` proves
    both ordinary upper/lower placement and the rendered, visible clamped
    label; its clamped-anchor assertion fails against the parent source.

A15. spacr/qt/widgets/figure_settings.py:1534 (derive_replot_recipe)
    WHAT: The guard `if frame[DERIVED_GROUP].nunique() < 1: return None` cannot fire for any real figure. The group column is built only from `_named()`, which returns either a non-empty stripped tick label or `f"{float(x):g}"` -- always a str, never NaN -- and line 1531 has already guaranteed at least 2 rows, so nunique() is >= 1 by construction. It is a dead guard of the kind tests/test_a_guard_no_input_could_reach_is_gone.py exists to catch.
    COST: No user-visible misbehaviour, but the branch is unreachable from any figure: I could only close it by monkeypatching `_pairs_from_axes` to return NaN group names. A reader maintaining this function is told nameless groups are a real case that must be handled, and any future change to `_named` that CAN return NaN would be silently relying on a guard nobody has ever exercised with real data.
    VERDICT 2026-08-31: STILL LIVE, and cosmetic. The `nunique() < 1` guard is
    still at figure_settings.py:1534 and still cannot fire. Dead-guard family;
    deleting it needs the maintainer's standing no-deletion call, like A37.

A16. spacr/regression_qc.py:2683-2735 `_panel_p_value_histogram` together with `diagnose_p_value_histogram` (line 833: `np.histogram(p, bins=n_bins, range=(0.0, 1.0))`)
    WHAT: Finite p-values outside [0, 1] are silently discarded by the fixed histogram range, and only the non-finite ones are guarded against (`if finite.size == 0: raise PanelUnavailable`). Feeding `coef_df` a `p_value` column of `[3.0, 7.0, 12.0]` gives `n == 0`, `verdict == 'too-few'`, message 'only 0 p-value(s)', `frac_below_0.05 == nan`, an empty histogram and no uniform reference line -- with no complaint anywhere that three values were supplied and all three were thrown away.
    COST: A caller who wires the wrong column into `coef_df` (a test statistic, a -log10 p, a chi2) gets the panel's 'too few coefficients to read a shape' limitation stamped on the report for a screen with thousands of coefficients. The report then states a coefficient count that contradicts the table it was built from, and the reader concludes the model is small rather than that the input is not p-values. The panel's own reason for existing -- catching a mis-specified null -- is silently unavailable.
    VERDICT 2026-09-02: RESOLVED by `4876158a6c`. The diagnostic boundary now
    rejects finite p-values outside [0, 1] with `ValueError`, and the panel
    translates that into `PanelUnavailable`. Two focused regression-QC tests
    prove rejection and valid 0/1 boundaries, while
    `test_a_coefficient_table_outside_zero_to_one_draws_no_uniform_line`
    proves the panel boundary; the rejection tests fail against the parent
    source, which silently returned a zero-count `too-few` histogram.

A17. spacr/qt/mask_engine.py:733-753, magic_wand()
    WHAT: The max_pixels budget counts only pixels that CHANGE state (added is incremented at 743-746 only when a zero pixel is added or a nonzero pixel is erased), so a flood that changes nothing is completely unbounded. Erasing where the mask is already empty, or adding over ground the mask already owns, walks every in-tolerance pixel in the frame no matter how small max_pixels is.
    COST: A mis-click with the erase wand on background -- the most ordinary wrong click there is -- freezes the Make Masks screen for the whole BFS. Measured here: an 800x800 uniform field with max_pixels=100 took 4.8 s of pure-Python BFS over all 640,000 pixels (the same call on a mask where pixels do change returns in 0.001 s). Cost scales with frame area, so a 2048x2048 field is roughly half a minute of an unresponsive GUI with no way to cancel.
    VERDICT 2026-08-31: LIVE, but the entry is WRONG TWICE and the correction
    changes what should be done about it.

    (i) NOT "completely unbounded". `visited` is a full-image boolean array
    (mask_engine.py:746) and each pixel is dequeued once, so the flood is
    bounded by H x W. The real defect is narrower: max_pixels does not bound
    WORK, only changes.

    (ii) THE GUI DOES NOT CALL THIS FUNCTION. make_masks.py:929 -- the only
    caller in the tree -- calls `wand_rescue.magic_wand`, a separate
    vectorised implementation that merely MATCHES these semantics. Measured,
    no-op 'add' flood over an all-owned mask:

        side      mask_engine   wand_rescue
        512       0.712 s       0.327 s
        1024      2.873 s       0.385 s
        2048     11.723 s       1.207 s

    So the 11.7 s is real but is not on any user's GUI thread; the path that
    is runs in 1.2 s and scales sublinearly. Severity is far below the entry's
    implication.

    NOT FIXED, deliberately. `max_pixels` counting CHANGES rather than visits
    is pinned on purpose -- tests/qt/test_cov_wf_qt_mask_engine.py:171,
    "already-owned pixels cost nothing against max_pixels" -- so a work bound
    has to be a SECOND budget, and choosing its constant needs a realistic
    mask-size distribution rather than a guess. Left specified.

A18. spacr/qt/widgets/measure_preview.py:823-830 (_on_max_sets_changed) together with ImageSetSampler.describe in spacr/qt/widgets/preview_controls.py:855-869
    WHAT: The guard `if self.sample_note():` is meant to stop an empty sample sentence from blanking the status line, but `describe()` never returns an empty string -- with nothing enumerated it returns "showing all 0 image sets". Verified live: a fresh panel with no array loaded, status line set to "2 object(s) . 1 category", then `_on_max_sets_changed(7)` leaves status == "Showing all 0 image sets".
    COST: Nudging the FOV-cap spinbox before an array is loaded (or after a load failed) wipes whatever the status line was saying -- the crop count, or the sentence explaining why the last pass could not run -- and replaces it with a sentence about a folder that was never listed. The False side of the guard is dead code in production; my test had to substitute `apply_sample_to_combo` to exercise it.
    VERDICT 2026-08-31: STALE -- already fixed. The dead `if self.sample_note():`
    guard is gone; the real condition (`if not self._sampler.total: return`,
    measure_preview.py:830) replaced it and the status is set unconditionally.

A19. spacr/qt/widgets/measure_preview.py:1248-1259 (_on_thumb_clicked)
    WHAT: The selection set is toggled BEFORE the `0 <= index < len(self._crops)` range check, so an out-of-range index is still added to `self._selected`. Verified: with 2 crops loaded, `_on_thumb_clicked(99)` leaves `99 in panel._selected`.
    COST: A click delivered by a thumbnail that a newer, shorter crop pass has outlived inflates the "N selected" count on the status line and puts a phantom index into `current_params()["selected"]`, which then disagrees with `n_crops`. Low severity today (`_on_crops_ready` clears the set on every pass and nothing in spacr/ consumes `current_params()`), but the range check clearly intended to reject the click entirely.
    VERDICT 2026-08-31: STALE -- already fixed. The range check is now FIRST
    (measure_preview.py:1252-1253); the toggle cannot see an out-of-range index.

A20. spacr/figures/scene.py:1186-1205, _add_legend
    WHAT: The entry texts are FILTERED (`if body:` skips a legend text whose string is empty) but they are then paired with the unfiltered handle list: `for entry, handle in zip(entries, legend.legend_handles)`. Every surviving label after a blank one is therefore zipped against the handle of the previous entry, so the swatch beside it is the wrong series' colour. Measured: `axes.legend(handles=[red_line, blue_line], labels=["", "kept"])` translates to a single row labelled 'kept' drawn with pen #ff0000 -- the RED line's colour -- although 'kept' is the blue series. The same shift applies to every entry after the blank one when there are more.
    COST: A pyqtgraph-rendered panel prints a key that names a series in another series' colour, which is the one failure a legend exists to prevent, and it is silent: the translation reports itself complete and the matplotlib page (which is correct) is not written. Reachable whenever a panel passes explicit `handles=`/`labels=` with a blank spacer label, which is how three of the QC panels build their legends.
    VERDICT 2026-08-31: STALE -- already fixed. texts and handles are zipped
    BEFORE the `if body:` filter (scene.py:1190-1193), so each kept entry
    carries its own handle. The mispairing is impossible as written.

A21. spacr/qt/widgets/picture_settings_dialog.py:337-339 (_say_what_the_cap_costs, early return on an empty cost)
    WHAT: When montage_cap_cost() answers "" -- which it does for any cap that is not a positive count, and the cap QSpinBox's range starts at 0 -- the method returns without touching the label, so the PREVIOUS cap's cost sentence stays on the tooltip. Verified: a dialog opened at cap=2000 and then set to 0 still shows 'about 287 MB of crops held while the tab is open' beside a box reading 0.
    COST: The help beside the cap prices a limit the control no longer holds. A user who clears the box (or passes through 0 while retyping) reads a page count, a memory figure and a cut time that belong to a number they have just replaced, and nothing on screen says the figures are stale.
    VERDICT 2026-08-31: STALE -- already fixed. An empty cost no longer returns
    early; it REMOVES the previous count's sentence, with a comment saying why
    ('ZERO HAS NO PRICE', picture_settings_dialog.py:363-366).

A22. spacr/qt/widgets/picture_settings_dialog.py:238-241 (the cap live-pricing connection) with a non-int stored cap
    WHAT: A settings blob that stores cap as a decimal (e.g. 2000.0 round-tripped through a float column) builds a QDoubleSpinBox, which is not a QSpinBox, so valueChanged is never connected. Verified: with values={'cap': 12.5} the label prices '12 objects' at build time and keeps saying 12 after the control is dragged to 30.0; values()['cap'] then returns 30.0, a float, where every other reader of this setting expects a count.
    COST: For that user the cost line silently freezes at whatever the file held -- it never again describes the cap they are choosing -- and the dialog hands the montage a float cap. The guard correctly keeps the window openable; the missing part is any signal that the live pricing is off.
    VERDICT 2026-08-31: STALE -- already fixed. A float cap is normalised to int
    at the boundary so the control stays a QSpinBox and stays live-wired
    (picture_settings_dialog.py:213-219), with a comment naming this exact case.

A23. spacr/qt/ai/providers.py:147-151 (terminate_all_streams) and 222-226 (_stream_process finally)
    WHAT: After the escalation calls proc.kill() there is no follow-up proc.wait(), unlike ChatProvider.cancel_stream() (lines 93-97) which does wait after kill.
    COST: A vendor CLI child that ignored SIGTERM is SIGKILLed but never reaped, so it stays a zombie for the life of the GUI process and Popen.__del__ emits a ResourceWarning about a still-running child. On a long session with several stuck streams the zombies accumulate.
    VERDICT 2026-08-31: STALE -- already fixed. terminate_all_streams delegates to
    `_terminate_and_reap`, which calls proc.wait(timeout=...) and falls back to
    `_kill_and_reap` (providers.py:162-181).

A24. spacr/qt/ai/providers.py:152-158 (terminate_all_streams)
    WHAT: When proc.terminate() raises (e.g. OSError for a pid the OS refuses), the exception is swallowed by the outer `except Exception: pass` but the finally still removes the entry from _LIVE_STREAMS.
    COST: A child that is still running but momentarily unsignallable is dropped from the registry, so a later terminate_all_streams() (or the autouse shutdown at app exit) can never reach it again -- exactly the blocked-reader-thread-outlives-its-owner situation this registry exists to prevent. Confirmed by test_a_child_that_cannot_be_signalled_does_not_strand_the_others: the unreachable child is left running yet the registry ends up empty.
    VERDICT 2026-08-31: STALE -- already fixed by the same change. The entry is
    only discarded when the reap CONFIRMS exit (`if finished:`,
    providers.py:202-203); a terminate that raises returns finished=False and
    the process stays tracked.

A25. spacr/report.py:812-816, _collect_run_status (the `if isinstance(failure, dict)` guard)
    WHAT: A run-status stamp whose `failures` list holds bare strings instead of {item, stage, error} records (a hand-written sidecar, or one from an older writer) has those entries silently dropped: the count line says "2 of 3 item(s) failed" but only the dict-shaped failure gets a bullet, and the string's text appears nowhere in the HTML or the text/PDF rendering.
    COST: A reader of the shared report sees a failure count with fewer -- possibly zero -- explanations than there were failures, and has no way to tell that the reasons were withheld. The module's stated rule is that failure goes at the top; here part of the failure evidence is read off disk and then thrown away without a note.
    VERDICT 2026-08-31: STALE -- already fixed. A non-dict failure now takes an
    `else` branch through `_failure_record_text` (report.py:847-849), so a bare
    string is rendered rather than dropped.

A26. spacr/report.py:1657-1699, _collect_appendix (the `elif families:` fall-through at 1663)
    WHAT: When a measurements database exists but describes nothing -- e.g. a run killed before Measure wrote a table, so describe_database returns an empty frame -- _feature_dictionary returns no families and no error, and the appendix emits nothing at all about that database: no "Measured features" heading, no note, no mention of the file by name (verified: section.notes == [] and 'Measured features' not in body).
    COST: The appendix looks identical to one built for a folder that has no database, contradicting the module's own rule that "a missing section is stated, never omitted". A reader cannot distinguish "the database holds no measurements" from "there is no database", which is exactly the partial-run-forwarded-as-complete failure the report exists to prevent.
    VERDICT 2026-08-31: STALE -- already fixed. The `elif families:` now has an
    `else` that states the empty database and sets have_something, so 'no
    measurements' is distinguishable from 'no database'.

A27. spacr/qt/widgets/fast_plots.py:2344-2370, FastPlot._gap_for_split
    WHAT: The height of the axis break is summed over EVERY drawn item instead of being measured once over the range that is kept: `span += ...` accumulates each entry's below-band and above-band spread, so the gap grows with the number of items on the plot rather than with the data. Measured: one scatter spanning y=1..3 with a split at (10, 100) gives _split_gap == 0.12; three identical scatters over the same y=1..3 give 0.36.
    COST: A volcano draws several scatter layers (hits, non-hits, controls) over the same y range, so the break is painted three or more times taller than the documented 6% of what is kept -- it eats the vertical space the split exists to give back, and the same data redrawn as one layer instead of three produces a visibly different figure.
    VERDICT 2026-08-31: STALE -- already fixed. `_gap_for_split` tracks running
    below_min/below_max/above_min/above_max across entries
    (fast_plots.py:2354-2378); the per-entry `span +=` accumulation is gone.

A28. spacr/sequencing.py:1054-1064, generate_barecode_mapping sample loop
    WHAT: With mode='single', only the exact strings 'R1' and 'R2' bind R1/R2 (lines 1057-1062). Any other spelling -- e.g. the lower-case 'r2' a user types into the settings -- falls straight through to the call at 1064 with R1 and R2 unbound, raising `NameError: name 'R1' is not defined`. There is no validation of single_direction anywhere: set_default_generate_barecode_mapping only does settings.setdefault('single_direction', 'R1').
    COST: A one-character settings typo kills the sample with an exception that names neither the setting nor the sample, after os.makedirs at line 1045 has already created <src>/<sample>_single_<direction>/ -- so the user is left with an empty sample folder and a NameError about a local variable. Under an on_error policy that continues past a failed sample, every sample fails identically and the run finishes with an empty output folder and nothing pointing at single_direction.
    VERDICT 2026-08-31: STALE -- already fixed, and better than the entry asked.
    `single_direction` is now VALIDATED against {'R1','R2'} and raises with a
    message naming the accepted spellings (sequencing.py:1038-1041), so a
    lower-case 'r2' is refused loudly instead of falling through silently.

A29. spacr/surrogate.py:784-788 (_shap_importance)
    WHAT: Dead guard. `if importance.shape[0] != x_test.shape[1]` can never be true: every branch that reaches it derived `importance` by averaging over an array whose shape was already matched against `sample.shape`, and `sample` only ever differs from `x_test` in ROWS (x_test.sample(max_samples)). The warning it appends ('SHAP returned N values for M features; column omitted') can never be emitted.
    COST: No user-visible misbehaviour, but it reads as a live safeguard against a SHAP/feature-count mismatch that is in fact structurally impossible, and this repo's own convention (tests/test_a_guard_no_input_could_reach_is_gone.py) treats such a guard as something to delete rather than to keep. Reported, not fixed -- spacr/ was not edited.
    VERDICT 2026-08-31: STALE -- the dead guard is gone; `importance.shape` no
    longer appears in surrogate.py at all.

A30. spacr/spacrops.py:3005-3012, stitch_cycle_wells, the "organize into per-well folders" loop
    WHAT: When the plate is re-organized in place (dst_root == src, tiles already at <dst_root>/<well>/<name>) with collision="overwrite", `_resolve_collision` returns the destination unchanged, so sp == dp == rp. The code then runs `os.remove(dp)` - which deletes the SOURCE file, because it is the same file - and afterwards skips `shutil.move` since `if sp != rp` is False, while still doing `moved += 1` and appending the (now missing) path to the well's tile list.
    COST: A second run of the organizer over an already organized plate with collision="overwrite" permanently deletes the raw microscope frames. The returned summary reports them as moved and lists paths that no longer exist, so the loss is silent until a downstream step opens one. Pinned (as the current behaviour, with a DEFECT comment) by tests/test_cov_wf_spacrops.py::test_reorganising_a_plate_onto_itself_with_overwrite_destroys_the_tiles; the collision="rename" half of that test shows the same layout is handled without data loss.
    VERDICT 2026-08-31: STALE -- already fixed. An `already_in_place` test
    compares normcase(realpath(sp)) with the resolved destination and skips the
    copy (spacrops.py:3008-3012).

A31. spacr/seg_qc.py:1706-1707 (_flag_findings)
    WHAT: The `elif fields:` guard is dead code that does not do what it was written for. `fields` is a tuple with one entry per member field, so it is truthy even when every member's name is the empty string; `_name_list` then drops those empty names and returns "". The headline is built as `where += f", fields {_name_list(...)}"` regardless.
    COST: A card whose field column is blank produces a finding headlined `1 cell field(s) on this project, fields : no object at all in the field` -- a dangling "fields :" with nothing after it, which is exactly the case the guard was meant to suppress. Reproduced with _flag_findings([FieldQC(field="", object_type="cell", n_objects=0, flags=[FLAG_EMPTY], severity="fail")], 3). Low severity in practice: field names come from .npy stems and are never empty on the normal path, which is also why the false arc is uncoverable.
    VERDICT 2026-08-31: STALE -- already fixed. The guard is now `elif
    named_fields:` -- the RENDERED string, not the tuple (seg_qc.py:1707-1708) --
    so an all-empty field set is falsy and the dangling 'fields :' cannot occur.

A32. pytest.ini:24 -- `error::pandas.errors.SettingWithCopyWarning` in filterwarnings
    WHAT: The pandas installed in this environment changed to 3.0.5 partway through this session (numpy to 2.5.2 with it), and pandas 3 removed `pandas.errors.SettingWithCopyWarning`. pytest resolves ini warning categories during startup, so it now raises `AttributeError: module 'pandas.errors' has no attribute 'SettingWithCopyWarning'` before a single test is collected.
    COST: No test in the repository can be collected or run at all until that filter is dropped or conditioned. I worked around it per-run with `-o filterwarnings=` (no file was edited). Whoever is running the coverage campaign on this machine will see every agent's suite die at startup otherwise.
    VERDICT 2026-08-31: STALE, twice over. The line is gone from pytest.ini --
    only the pandas FutureWarning/DeprecationWarning filters remain (lines
    20-21) -- and the premise no longer holds either: this env has pandas 2.3.3
    and `pandas.errors.SettingWithCopyWarning` still exists.

A33. spacr/qt/widgets/setup_slides.py:1686 (_fade_the_greeting_away) vs 1721 (_place_the_greeting)
    WHAT: `_place_the_greeting` guards its use of the greeting label with `if self._greeting:` while `_fade_the_greeting_away`, which `_show_slide` calls for every slide after the first, dereferences `self._greeting.isVisibleTo(...)` with no guard.
    COST: None today -- `_greeting` is set in `__init__` and never cleared, so the guarded and unguarded paths agree in production. Reported only because the two methods disagree about whether the label may be absent; if the guard at 1721 is ever load-bearing, `_show_slide` will raise from a slide change. I did not change either.
    VERDICT 2026-08-31: STILL LIVE, and NOT VERIFIED FURTHER -- setup_slides.py
    belongs to the concurrent session. The asymmetry the entry reports is intact:
    `_place_the_greeting` guards with `if self._greeting:` while
    `_fade_the_greeting_away` dereferences it directly
    (`self._greeting.isVisibleTo(self.card)`, setup_slides.py:1703). Left for
    that lane.

A34. spacr/macro.py:558-561, Macro.write
    WHAT: The temporary file is opened before the script is rendered: `with open(temporary, "w") as handle: handle.write(self.source())`. If `self.source()` raises (any settings value whose `__str__`/`__repr__` raises, or any rendering failure), the exception propagates out of `write` after the temp file has already been created, and nothing removes it. Reproduced: a one-step Macro whose settings hold an object with a raising `__str__` left `macro.py.2058192.tmp` in the target folder. There is no try/finally and no cleanup anywhere else — `_write_everywhere` only logs the exception.
    COST: The stable macro folder (~/.spacr/macros, or SPACR_MACRO_DIR) silently accumulates one orphaned `<macro_id>.py.<pid>.tmp` per failed write, forever; nothing ever reaps them, and a user inspecting the folder for the script that reproduces a run sees half-written debris next to the real scripts. The same debris lands inside a run journal folder, next to manifest.json, where `spacr-repro` and the notebook exporter look.
    VERDICT 2026-08-31: STALE -- already fixed. `rendered = self.source()` is
    evaluated BEFORE the temporary file is opened (macro.py:563-565), so a
    raising __str__ cannot leave a half-written temp behind.

A35. spacr/sequencing_qc.py:702-706 (collision_summary), reached in production from barcode_qc at line 1652 which writes collision_summary.csv
    WHAT: reads_at_risk is computed for EVERY reference by matching that reference's barcode names against the count table's `grna` column. The docstring says only the gRNA reference reports it ("the gRNA reference also reports reads_at_risk"), and for the row and column references the names never appear in the grna column, so the division produces a hard 0.0 rather than being left unmeasured. Verified: references {'grna': {'sg0','sg1'}, 'rowID': {'r1','r2'}} with a count table where every read carries row barcode r1 yields rows grna reads_at_risk=0.667 and rowID reads_at_risk=0.0, even though r1/r2 are one substitution apart and 100% of the reads carry r1.
    COST: collision_summary.csv, which barcode_qc writes for every run, tells the user that 0% of their reads are at risk from row- and column-barcode collisions. That is the reassuring reading of a number that was never computed for those fields, so a genuinely ambiguous row or column barcode set is dismissed on the strength of a 0 the code invented. The gRNA row's number, the only one the docstring claims, is correct.
    VERDICT 2026-08-31: STALE -- already fixed. reads_at_risk is now guarded by
    `label == "grna"` (sequencing_qc.py:702), exactly as the docstring says.

A36. spacr/plate_measurements.py:147 (TableMerge.merged_column, via _UNPREFIXED at line 87) surfacing in PlateMerge.dropped_columns (line 209) and describe() (line 266)
    WHAT: _UNPREFIXED contains OBJECT_COLUMN ('object_label') unconditionally, so merged_column() returns the bare name for a CHILD table's object_label. But roll_up only leaves the JOIN KEYS unprefixed, and object_label is not a key for a child table (keys are IDENTITY + screenID + source_database + cell_id) -- the frame really carries it as 'pathogen_object_label'. Verified: with object_label present in the pathogen table the merged frame has 'pathogen_object_label' while entry.merged_column('object_label') returns 'object_label'.
    COST: When a child table's object_label is dropped by columns='common' (present in one database, absent from another), the Measurements tab reports '1 measurement(s) present in only some databases were dropped: object_label' -- naming a column that IS in the merged frame (the anchor's own object_label) and never saying which table lost it. If two child tables both lose it, the two distinct losses collapse into a single reported name, so the user cannot tell that both the pathogen and the nucleus measurements are gone.
    VERDICT 2026-08-31: STALE -- already fixed. The OBJECT_COLUMN exception is
    explicit: `if name in _UNPREFIXED and name != OBJECT_COLUMN`
    (plate_measurements.py:169).

A37. spacr/qt/screens/train_compare.py:451-471 (_apply_runs)
    WHAT: _apply_runs replaces self._runs (452) and nulls self._comparison (453) BEFORE it clears the canvas (_clear_plot, 457). Any failure in between -- _fill_runs_list, _fill_problems, _clear_diff, or _clear_plot itself, which calls self._canvas.draw_idle() and can raise RuntimeError when Qt has already deleted the canvas's C++ half -- leaves the screen holding the new run list, the new metric list and the OLD curves plus the OLD figure.spacr_series_by_label mapping. Nothing marks the plot as stale; the caller only sees the error text in the status label.
    COST: After a half-applied scan the user reads a plot belonging to a tree that is no longer listed. The guards at 811 (`if run is not None`) and 806/808 (`last`/`best` is None) stop it from crashing or naming the wrong folder, so a click degrades to 'label - epochs 1-25' with no metric reading and no path, but the curves themselves stay on screen under a run list that contradicts them. Clearing the plot (or snapshotting and restoring the run list) before mutating self._runs would close the window. Not fixed -- reported only.
    VERDICT 2026-08-31: STALE -- already fixed. The order is inverted from what
    the entry describes: `self._clear_plot()` runs at train_compare.py:457,
    BEFORE `self._runs = next_runs` (458) and `self._comparison = None` (459),
    so a failure between them can no longer leave the canvas describing runs
    the panel has forgotten.

A38. spacr/qt/screens/gate_editor.py:1343 (save_graph, `except Exception as exc:  # pragma: no cover`)
    WHAT: The only handler that reports a renderer that RAISED (as opposed to returning False) is hidden behind a `# pragma: no cover`, which instruction 288 forbids. Nothing in tests/ asserted its message either -- `grep -rn 'Could not save the graph:' tests/` returned nothing -- so the pragma was hiding a genuinely untested handler rather than an unreachable one. Under the workflow's nopragma rc, lines 1344-1346 were measured and MISSING. Not edited (no changes under spacr/); the new test now exercises the handler.
    COST: A regression in that handler -- a raise escaping into the Qt slot behind the Save graph menu item and the toolbar button -- would have been invisible to both coverage and the suite, and would reach the user as a silent no-file save or a traceback out of the event loop.
    VERDICT 2026-08-31: STALE -- already fixed. gate_editor.py carries no
    `# pragma: no cover` at all, so the raised-renderer handler is no longer
    excluded from coverage.

A39. spacr/qt/layer_viewer.py:768-771 (LayerViewer._sync_controls)
    WHAT: When the selected ImageLayer's colormap is not one of the built-in COLORMAPS (any hex/named-colour ramp, which ImageLayer accepts via `colormaps='#ff00aa'`), `self.colormap_combo.findText(...)` returns -1 and the combo is left showing whatever the PREVIOUS selection put there. Confirmed live: a stack with layer 'green' (colormap 'green') and layer 'hex' (colormap '#ff00aa') leaves the combo reading 'green' after selecting 'hex'.
    COST: The property panel silently mislabels the selected layer's colour: the user reads 'green' for a channel that is actually magenta-pink. The combo is also the control that writes the colormap back, so the next unrelated touch of that widget would push the wrong, stale colour onto the layer. Leaving the combo unchanged is the deliberate safe half of this; what is missing is any indication that the shown name does not belong to the selected layer (e.g. clearing/disabling the combo, or inserting the custom name as a transient entry).
    VERDICT 2026-08-31: STALE -- already fixed. A colormap the combo does not
    carry is ADDED and tracked as `_custom_colormap_name`
    (layer_viewer.py:798-802); findText's -1 is handled rather than used.

A40. spacr/qt/widgets/figure_grid_view.py:688-703 (FigureGridView.apply_workspace_state)
    WHAT: The restored `collapsed` set is assigned (line 690) and `applied` is set True, but nothing relayouts. The only relayout in the method is a side effect of the `cell_width` branch calling `set_target_cell_width` (line 699), which is skipped whenever the saved width is missing or falsy.
    COST: A workspace document that carries folds but no `cell_width` (an older document, a hand-edited one, or one written with 0) restores fold state that never reaches the screen: `apply_workspace_state` returns True and `is_section_collapsed(label, start)` answers True while every figure of that run is still visible under a heading the next relayout will draw as folded. Verified live: on a grid holding two figures of `("run one", 0, 2)`, `apply_workspace_state({"collapsed": [["run one", 0]]})` left `[cell.isHidden() for cell in grid._cells] == [False, False]`; adding `"cell_width": 260` to the same document made it `[True, True]`. The user reopens a saved run and the sections they folded away are back until something unrelated (a window resize, the next figure landing) triggers a layout pass.
    VERDICT 2026-08-31: STALE -- already fixed. `needs_relayout` is tracked
    explicitly through apply_workspace_state (figure_grid_view.py:694), so a
    restored `collapsed` set no longer depends on the cell_width branch to
    relayout.

A41. spacr/qt/comparison_grid.py:429-438 (_refresh_status), reached from add_panel line 276-280
    WHAT: A panel whose stack is empty never enters self._canvas_link (add_panel's `if canvas is not None:` guard at line 276 skips the link insert). _refresh_status computes `free` as `[key for key in self._panels if key in self._canvas_link and not is_locked(key)]`, so a key that is not in the link at all is filtered out of `free` and the status line reports it as linked: a grid of one loaded and one unloaded panel says "2 panel(s) - all linked".
    COST: The status line, which is the only feedback the grid gives about which panels share the world window, positively asserts that a panel is on the shared window when it is not in the link at all. A user whose fourth channel failed to load is told all four panels are linked, then finds that panning and Fit never move that cell, with nothing in the UI explaining why. The honest wording would distinguish "not linked" from "free".
    VERDICT 2026-08-31: STALE -- already fixed. Panels absent from _canvas_link
    are counted as `unlinked` in their own list (comparison_grid.py:436-437)
    rather than silently vanishing from the status line.

A42. coverage harness — /mnt/firecuda2/codex/repo/spacr/.coverage (shared by every parallel wf subagent)
    WHAT: pytest-cov defaults its data file to <rootdir>/.coverage. Several of these coverage subagents run pytest in the same repo directory at the same time, so they clobber one another's SQLite data file mid-run. My first run produced a JSON claiming spacr/train_compare.py was 14% covered with `from __future__ import annotations` (line 105) unexecuted, and check_arcs.py printed MISSING for all six targets; the report step also emitted dozens of `Couldn't use data file '.../.coverage': no such table: arc` warnings. Re-running the identical command with COVERAGE_FILE pointed at a private scratch path gave 64% and REACHED for all six.
    COST: Any subagent that trusts its first arc check will report arcs as MISSING that are in fact REACHED, then write redundant tests to 'close' them — or, in the opposite direction, a clobbered file could mark an arc REACHED that is not. Every wf coverage run in this batch should set COVERAGE_FILE to a per-agent path.
    VERDICT 2026-08-31: STALE -- already fixed. `.coveragerc` sets
    `parallel = True`, which suffixes each process's data file, so concurrent
    runs in one rootdir cannot clobber one another.

A43. /mnt/firecuda2/codex/repo/spacr/spacr/zstack.py:3046-3059 (plan_4d_from_settings, the block commented "Fail here rather than after the model has been loaded and the first timepoint read")
    WHAT: The plan-time validation never checks that a micrometre gate has a voxel size. `plan_4d_from_settings({'t_stack': True, 't_axis_order': 'TYX', 't_track_backend': 'centroid', 't_max_displacement_um': 5})` returns a spec with max_displacement_um=5 and voxel_size_um=None without complaint; the refusal only fires later inside _displacement_scale (line 2380) when track_4d runs. The neighbouring checks in the same block (missing gate -> UnknownDisplacementError, volumetric mode without anisotropy -> UnknownAnisotropyError) are exactly the checks this one is missing.
    COST: A run configured in micrometres but missing voxel_size_z_um / voxel_size_xy_um passes settings validation, loads the model and segments every timepoint, then dies with TStackError at the linking step -- the precise late failure the plan-time block exists to prevent. On a long movie that is hours of GPU time thrown away for a settings error that was knowable before the first file was read. Reported, not fixed (spacr/ is off-limits for this task); no test was added for it because pinning either the current acceptance or a future refusal would enshrine one side of the defect.
    VERDICT 2026-08-31: STALE -- already fixed, exactly as asked. The plan-time
    block now carries `if spec.max_displacement_um is not None and
    spec.voxel_size_um is None: raise TStackError(...)` naming both settings
    and the pixel alternative (zstack.py:3057-3063).

A44. spacr/qt/shortcuts.py:429-430, _toggle_ai
    WHAT: The handler ends in a bare `except Exception: pass` with no logging, unlike every neighbouring optional-path handler in the same module (_open_palette, _open_preferences, _consoles all do LOG.debug(...) / exc_info=True).
    COST: If `from .screens.app_screen import AppScreen` fails, or the screen's `_ai_switch.setChecked` raises, Ctrl+/ silently does nothing and leaves no trace at any log level, so a broken AI toggle is undiagnosable from a user's log even with debug logging on.
    VERDICT 2026-08-31: STALE -- already fixed. `_toggle_ai` ends in
    LOG.debug(..., exc_info=True), like its neighbours. No action.

A45. spacr/qt/widgets/umap_figure_settings.py:245-250 (redraw_umap_figure)
    WHAT: The label guard is incomplete. `labels = payload.get("plot_labels")`, falling back to `payload.get("labels")`, is passed straight to `np.asarray(labels)`; when neither key is present that yields a 0-d object array and the very next line, `if len(labels) != len(embedding)`, raises `TypeError: len() of unsized object`. Every other malformed-payload case in the same function (fig is None, payload not a dict, embedding not 2-d or empty, label count mismatch) returns False instead. Confirmed by direct call: `redraw_umap_figure(Figure(), {"embedding": emb}, {})` raises TypeError.
    COST: A redraw-tier edit (image count, figure size, draw-images, ...) on a figure whose stashed `_spacr_umap_payload` carries an embedding but no labels crashes instead of quietly declining. Both callers -- `figure_settings.FigureSettings._on_umap_changed` (spacr/qt/widgets/figure_settings.py:268-273) and `figure_queue._on_umap_changed` (spacr/qt/widgets/figure_queue.py:2502-2505) -- call `apply_to_figure` with no try/except, and `apply_to_figure` line 320 does not guard either, so the TypeError escapes a Qt slot: the user moving a spin box in the Image UMAP settings window gets an unhandled exception rather than a figure that simply does not redraw. Not fixed, per instructions.
    VERDICT 2026-08-31: STALE -- already fixed. `if labels is None: return False`
    precedes the np.asarray (umap_figure_settings.py:246-250).

A46. spacr/qt/widgets/column_picker.py:412 find_existing (with validate_column_name at :377 and the allow_new=False branch at :930-937)
    WHAT: find_existing strips/lowercases the TYPED name but compares it against the stored column names unstripped (`str(col).lower() == target`). A column whose stored name carries a leading or trailing space -- exactly what pandas.to_sql writes from a CSV header that read ' area' -- is listed in the column tree but never matches. With allow_new=False the dialog then prints "'area' is not a column of imported" directly under a tree that lists ' area'; with allow_new=True validate_column_name refuses it for the leading space instead.
    COST: A column the picker itself is displaying can never be picked, in either mode, and the sentence the dialog shows contradicts the list right above it. The user's only route is to type the name with the space by hand, which the verdict also refuses.
    VERDICT 2026-08-31: STALE -- already fixed. Two passes: exact
    `str(col).lower() == raw.lower()`, then `str(col).strip().lower() ==
    target` (column_picker.py:416-421), so a stored name carrying whitespace
    is matched rather than duplicated.

A47. spacr/qt/widgets/column_picker.py:1495-1498 attach_column_picker (host layout = spacr.qt.widgets.flow.FlowLayout)
    WHAT: attach_column_picker swaps the field for a wrapper with `host.replaceWidget(field, wrapper)`. FlowLayout is a Python QLayout subclass with no replaceAt, so QLayout::replaceWidget finds the widget, fails at QLayoutPrivate::replaceAt and returns None having replaced nothing -- while the following `row.addWidget(field, 1)` still re-parents the field into the wrapper, and QLayout::addChildWidget removes it from the flow. Measured: flow.count() goes 1 -> 0, flow.indexOf(field) and flow.indexOf(wrapper) are both -1.
    COST: Attaching an SQL picker to a column field that sits in one of spaCR's wrapping rows empties that row: the field and its new button become unmanaged children of the host with no geometry from any layout, so the control the user was typing into is mispositioned or invisible. No exception is raised, so nothing reports it. (Today's call sites all pass box/form layouts, so this is latent rather than live.)
    VERDICT 2026-08-31: STALE -- already fixed. `_replace_layout_widget` detects
    the failed replaceWidget and rebuilds the suffix around the occupied slot,
    with a comment naming the protected-replaceAt cause (column_picker.py:1442+).

A48. spacr/qt/widgets/external_mask_inputs.py:186-187 (_rebuild) together with spacr/external_masks.py InputGroup.from_value
    WHAT: A saved answer restored through set_value() may carry an object_type that is not in OBJECT_TYPES (InputGroup.from_value accepts any truthy string). _rebuild then does object_box.findData(group.object_type) -> -1 and setCurrentIndex(max(-1, 0)) -> 0, so the editable box displays 'Choose...' with data None, while the read-only 'Detected as' cell displays 'mask - mitochondrion' and get_value() still returns object_type='mitochondrion'. The widget's own guard set_group_object_type() refuses exactly that value, so the table accepts through restore what it refuses through the UI.
    COST: A user reopening a saved External Masks project sees an object-type dropdown that reads 'Choose...' (i.e. nothing assigned) and a row that claims a plane it cannot show. Changing nothing and pressing run hands the invalid object_type downstream, where it either raises later or writes masks/<bogus>_mask_stack that Measure will not read. Verified live: set_value([{...,'object_type':'mitochondrion'}]) gives currentText()='Choose...', currentData()=None, item(0,1).text()='mask - mitochondrion', get_value()[0]['object_type']='mitochondrion'.
    VERDICT 2026-08-31: STALE, and it took a mutation check to see it. The
    described state is unreachable: `InputGroup.from_value` normalises an
    unrecognised object_type to None BEFORE the widget sees it, so the group
    and the "Choose…" box agree by construction and get_value() returns None.

    Recorded at length because the near-miss is the useful part. A guard was
    added in `_rebuild` for exactly the reported case, and two tests written
    for it PASSED IDENTICALLY WITH THE GUARD REVERTED -- they asserted a
    property that was already true, which is the vacuous-test shape 288's
    arc-check rule exists to catch. Both the guard (dead code by this
    package's own convention) and the tests were withdrawn; the tests were
    rewritten to pin `from_value`, which is where the normalisation lives.

    THE METHOD POINT: verifying a lead by writing a test that passes is not
    verification. The test has to be shown to fail without the change.

A49. spacr/qt/verbose_logger.py:73-75 (_ATTACHED_LOGGERS) used by _ensure_file_handler (212-215) and _ensure_handler (330-333)
    WHAT: The same handler object is attached to a logger AND to its descendants ('spacr', 'spacr.qt', 'spacr.qt.hf_download', 'spacr.qt.plate_queue', ...). logging.callHandlers walks the whole ancestor chain and does not de-duplicate handlers, so one record is emitted through the same handler once per attached ancestor. Measured on this checkout: a single logging.getLogger('spacr.qt.hf_download').warning('DOWNLOAD FAILED') produced 3 identical lines in ~/.spacr/logs/spacr-YYYYMMDD.log and 3 identical deliveries to the registered ConsolePanel; a record on 'spacr.qt' produced 2 of each. Only records logged on 'spacr' itself appear once.
    COST: Every diagnostic from the Qt layer, the plate queue, the HF downloader and the function trace is duplicated 2-3x in both the in-app console and the rotating log file users attach to bug reports. The console scrolls 2-3x faster than it should when verbose logging is on, the log file rotates at 5 MB 2-3x sooner (discarding older history), and any count read off the log ('this failed twice') is wrong. Not fixed here per instructions; the fix is to attach the handler only to the root 'spacr' logger (descendants already propagate to it) or to skip a name whose ancestor is already in the list.
    VERDICT 2026-08-31: STALE -- already fixed. The handler is now REMOVED from
    every logger except the sink (verbose_logger.py:216-219, 340-343), so a
    record is emitted once rather than 2-3x. No action.

A50. spacr/layers.py, LabelsLayer.data setter (~lines 1970-1983), consumed by spacr/qt/ortho_view.py:507-518
    WHAT: The constructor refuses a non-integer array ("a labels layer needs integer labels"), but the data setter validates only shape, so `mask.data = float_array` is accepted. `LabelsLayer.labels()` then returns the raw float values (e.g. 17.5) while every consumer compares with `int(label)`: `OrthoView.on_linked_selection_changed` matches `field.object_key(int(17.5)) == 'plate1_A_1_1_17'`, sets `selected_label = 17`, and then `np.argwhere(data == 17)` finds nothing. `LabelsLayer.label_at_world` truncates the same way.
    COST: A labels layer that passes through the setter is silently self-inconsistent: it reports labels that its own array does not contain. Here the `if len(where):` guard absorbs it (the crosshair just does not move, so a linked selection from the UMAP silently does nothing); without that guard `where.mean(axis=0)` would be nan and `_snap`'s `int(round(nan))` would raise ValueError out of the selection-bus signal handler. The setter should apply the constructor's dtype check.
    VERDICT 2026-08-31: STALE -- already fixed. The integer-dtype refusal appears
    twice in layers.py (2148 and 2173), i.e. in the setter as well as the
    constructor.

A51. spacr/guide_attribution.py:283-284, in attributable()
    WHAT: `rest = [(float(e), float(w)) for e, w in (others or ()) if float(w) > 0.0]` converts each weight TWICE -- once in the `if` and again in the stored tuple. A weight object whose conversion is not pure (a lazily fetched count, a mutable proxy, a value re-read from a stream) can therefore pass the positivity filter and be stored as a non-positive number. This is the ONLY way the guard on line 286 (`if total <= 0: return False, 0.0`) can fire, which is why that arc was uncovered.
    COST: Minor but real: the weight the filter approved is not the weight used, so a well can be reported as 'this guide can never be called' (False, 0.0) on data whose weights all looked positive when checked. Reading each weight once into a local would make the guard genuinely dead code instead. Not fixed -- reported only.

    MERGED WITH A52 on 2026-08-31 -- both entries name the SAME two lines, and
    only one of them is a defect. Kept together so they are read together.

    A52's claim (behavioural, NOT a defect): a competitor whose weight is NaN
    is dropped by `float(w) > 0.0`, and if every competitor is dropped the code
    falls back to the single flat rival. That is the documented behaviour of an
    omitted `others` -- "the most permissive comparison" -- so a NaN weight
    lands on a defined path rather than an undefined one. Worth a diagnostic,
    not a fix.

    A51's claim (THE DEFECT, still live as of 2026-08-31): each weight is
    converted TWICE -- once in the `if float(w) > 0.0` and again in the stored
    tuple -- so a weight whose conversion is not pure can pass the positivity
    filter and be stored non-positive. Reading each weight once into a local
    fixes it and makes the downstream guard genuinely dead.

    So: fix A51's double conversion; do not "fix" A52.


A53. spacr/qt/widgets/refit_dialog.py:192-198 (_refresh), with spacr/refit.py:243-262 (destination)
    WHAT: When destination() cannot derive an output folder it returns None, and _refresh then drops the "Writes to ..." line silently. If there are also no notes, the notice falls through to "Nothing to change: re-fitting these settings would repeat the run you are looking at." while the Re-fit button stays ENABLED. Reachable with settings whose count_data is a list with a falsy first entry (e.g. [""]), which refit_settings accepts because the list itself is truthy (refit.py:143) but destination rejects at refit.py:251-254.
    COST: The dialog makes a false statement about the run it is about to start: it says the re-fit would merely repeat the run on screen, when in fact it will start a real fit whose destination the dialog could not work out. The user gets no warning that the output location is underivable, and nothing tells them where to look for the results afterwards.

A54. spacr/qt/widgets/feature_explorer.py:251-253 (rank_now, the `if self._frame is None:` early return)
    WHAT: Unloading the table leaves every other part of the panel stale. set_frame(None) empties the Split-by picker and rank_now() sets the summary to "no table loaded" and returns None, but it never clears self._result, the table rows or the figure. Verified live: after set_frame(frame) then set_frame(None), panel.summary() == 'no table loaded' while panel.table.rowCount() is still 2, panel._figure.get_axes() still holds 2 strips, and panel.result still returns the previous ExplorerResult (so selected_feature() still names a column of the table that was unloaded).
    COST: A user who closes or swaps out the source table sees a ranking table and histograms describing data that is no longer loaded, under a summary saying nothing is loaded. Any caller reading panel.result (the screen's export_ranking, for example) writes out the previous table's ranking as if it were current.

A55. spacr/qt/widgets/feature_explorer.py:294-295 (_fill_table, `if len(result.scores): self.table.selectRow(0)`)
    WHAT: selectRow(0) only emits feature_selected when the current cell actually changes, so a re-rank that keeps the selection on row 0 announces nothing even when a different feature is now on that row. Verified live with columns weak_shift (a=1..4, b=2..5) and shape (KS 0.5): ranking by AUC puts weak_shift at row 0; switching the picker to KS re-ranks so shape is at row 0 and selected_feature() returns 'shape', but feature_selected was emitted zero times during the re-rank.
    COST: Everything wired to feature_selected -- the linked views on FeatureExplorerScreen and anything downstream of the panel signal -- keeps showing the feature from the previous statistic while the table highlights a different one, so the highlighted row and the linked detail disagree until the user clicks another row.

A56. spacr/qt/widgets/trellis_view.py:463-481, TrellisPanelWidget._sync (the two `>= 0` guards at lines 471 and 477)
    WHAT: When the incoming spec names a plot kind or a scale mode the picker has no item for, _sync leaves the combo box showing its PREVIOUS value rather than clearing it or falling back to "Automatic". The combo's data then disagrees with the spec, and _on_controls_changed reads the combo, not the spec.
    COST: The controls lie about the spec and then rewrite it. Verified by hand: with the Plot picker on Histogram, pushing TrellisSpec(graph=GraphSpec(x="area", kind=EMPTY, bins=7)) leaves the picker reading "Histogram" while panel.spec.graph.kind == "empty"; touching the Bins spin box next makes panel.spec.graph.kind == "histogram". So a spec restored from a saved layout has its plot kind silently replaced by whatever the shelf happened to be showing, without the user going near the Plot row. The scale pickers have the same shape (a mode the box cannot show is overwritten by the box's stale mode on the next control change). Docstring of _sync says "Make the controls show what the spec actually says", which is exactly what does not happen here.

A57. spacr/qt/widgets/feature_rank.py:658-670 (_null_threshold)
    WHAT: A permutation in which no feature's separation could be computed is recorded in `best` as 0.0 rather than being dropped. `top` is initialised to 0.0 before the feature loop and only ever raised by a finite score, so a shuffle where every `_separation` returned NaN (each candidate class empty after the finite mask) contributes a literal "chance reached zero" to the null distribution instead of "this shuffle produced no measurement".
    COST: On a table whose columns are sparsely measured, a sizeable fraction of the null distribution can be spurious zeros, which deflates the 95th percentile and makes `ExplorerResult.above_null()` list features that did not actually beat chance. Reported, not fixed, per the task constraints. Severity is low in practice: with several features, at least one is usually computable in every shuffle, so `top` is a real value.

A58. spacr/settings_advisor.py:844-848 (_thresholds), reached from read_the_counts lines 244-256
    WHAT: When every well total is zero (a plate that sequenced to nothing), fractions_from_counts returns a 'fraction' column that is present and numeric but entirely NaN. read_the_counts filters it to empty at line 246, so fraction_median stays None, and _thresholds then explains the missing recommendation with 'the count tables did not yield a fraction column'. The column WAS yielded; every value in it is NaN because the denominator was zero. read_the_counts also adds nothing to 'trouble' for this case, so the reading reports plates/wells/guides with no note at all.
    COST: A user whose sequencing failed (or whose count column was read as zeros) is told to go looking for a missing 'fraction' column that is sitting right there in their table, and the reading beside the advice looks healthy -- it names the plates, wells and guides it counted and flags no trouble. The one number that would have told them the run is worthless (no reads anywhere) is the one that is silently absent.
    VERDICT 2026-09-02: RESOLVED by `6b2916aff`. Zero-read tables now carry
    `_NO_USABLE_FRACTIONS`, so `_thresholds` reports that no fraction can be
    computed rather than claiming the column is missing; a genuinely absent
    fraction column remains a separate diagnosis. The focused zero-read and
    missing-column counterparts pass together (2 passed in 5.55 s).


==============================================================================
APPENDIX B -- 45 ARCS PROVED UNREACHABLE, WITH THEIR PROOFS
==============================================================================

Each was argued by the agent that tried to cover it. They map onto the four
families already in this document; a few are new instances worth reading.

  spacr/qt/theme.py  1616->1610
      In `_hue_ink` the first ramp is `candidate = (round(base[0]*level), round(base[1]*level), round(base[2]*level))` with `base = _hue_rgb(hue)` at the default saturation 1.0, which always returns the pure hue tuple containing exactly one 1.0 channel and one 0.0 channel; so `spread = max - min = level` exactly (verified empirically: 0 mismatches over 360 hues x 256 levels), levels are visited in increasing order, and `chroma` holds the last accepted level (or -1), so `spread > chroma` at line 1616 is true every time it is evaluated and the False arc back to the loop header can never be recorded.

  spacr/qt/widgets/regression_results.py  1108->1109 (and line 1109)
      `_keyed_plots()` (line 1737) returns the fixed tuple (volcano, effect_rank, qq, controls, agreement); it can never yield self.p_values or self.effect_distribution, so the `if plot in (self.p_values, self.effect_distribution): continue` guard inside `for plot in self._keyed_plots():` never fires -- family 4, a type/membership check whose only caller has already excluded the case.

  spacr/cell_montage.py  1305->1306 (and lines 1306, 1307)
      In _sudoku_calls, `fractions`'s keys are built at 1248 from `sorted(set(wells))`, `mine` (1284) is a non-empty subset of those keys (guarded by the `if not mine: return None` at 1286), and `keep = set(mine) | anchoring` at 1297, so at least one label in `keep` is in `wells` and `rows = [i for i, w in enumerate(wells) if w in keep]` (1304) can never be empty -- family 1, a defensive re-check after code that already guarantees the condition.

  spacr/cell_montage.py  2575->2587
      `resolve_crop_source` (spacr/crops.py:3564-3573) performs the identical Mapping/list unwrap and raises CropError on a falsy src, and that CropError returns from resolve_montage_crop_source at 2561-2565, so `root` at 2575 is always truthy by the time the line runs -- family 1. The only structural hole (crops does NOT unwrap a bare list, cell_montage does) is unusable because a bare list is stringified to its repr and `os.path` can only find such a root if a directory literally named `['']` exists in the cwd; that is defect 3 below, not a reachable user input.

  spacr/qt/screens/annotate.py  878->879
      paintEvent already returned at 868 when _pixmap is null, and picture_rect() then always yields w,h > 0 (box_w/box_h are max(1.0, ...) and scale=min(box_w/pm.width(), box_h/pm.height()) > 0), so box.isEmpty() is never True -- family 1, a re-check after a call that guarantees the condition.

  spacr/qt/screens/annotate.py  879
      Body of the 878->879 guard above; unreachable for the same reason (picture_rect cannot return an empty QRectF once the pixmap is known non-null).

  spacr/qt/screens/annotate.py  3282->3274
      _apply_to_slots skips every slot failing _slot_is_valid (0 <= slot < min(len(_page_paths), len(_thumbs))), and _set_annotation returns False only for slot outside 0..len(_page_paths); the surviving slots always satisfy it, so the False arc never occurs -- family 1.

  spacr/qt/screens/annotate.py  3695->-3689
      _toggle_annotation returns at 3692 unless _slot_is_valid(slot), which already guarantees the only condition under which _set_annotation returns False, so `if self._set_annotation(...)` is never False -- family 1.

  spacr/qt/screens/annotate.py  3910->3913
      _kbd_move is called from exactly one place (line 3823) under `if token in ('left','right','up','down')`; left/right/up each leave the elif chain before 3910, so the `elif token == 'down'` test is only ever reached with token=='down' -- family 4.

  spacr/qt/screens/annotate.py  4063->4065
      A MouseButtonRelease only enters that block when _band_origin is not None, which is set only by the press branch at 4044-4052 that also creates self._band (and nothing ever sets _band back to None after __init__), so `if self._band is not None` is never False -- family 1.

  spacr/core.py  258->625
      Family 1 (defensive re-check). `if isinstance(settings['src'], list):` at 258 can never be False: lines 174-178 raise unless src is str or list, line 180 runs it through utils.normalize_src_path (utils.py:10669-10694), which returns only a list or a str and raises ValueError otherwise, and lines 255-256 wrap any remaining str in a list. Only a monkeypatched normalize_src_path returning e.g. a tuple could take the False arc, which tests the mock rather than the module.

  spacr/parameter_sweep.py  374->376
      Family 1 (defensive re-check). Control flow only reaches line 374 after `hit.any()` was True on `labels`; `ranked_labels` is the SAME column of the SAME rows (frame.assign(_abs=...).sort_values(...).reset_index(drop=True) reorders but never drops a row), so `position = ranked_labels.str.contains(needle, regex=False)` has the identical truth values as `hit` up to permutation and `position.any()` is therefore always True.

  spacr/parameter_sweep.py  1115->1114
      Family 2 (loop that always breaks). The body of `for future in as_completed(list(futures))` ends with an unconditional `break` at line 1136 and contains no continue, and the enclosing `while futures:` guarantees `list(futures)` is non-empty so as_completed always yields at least one item; the for-loop therefore never exhausts and never falls back to the while header.

  spacr/regression_qc.py  2958->2960
      Family 1 (defensive re-check after a call that already guarantees the condition). In `_positional_effect_panel`, `groups` comes from `pd.unique(keys)` and `values = [ctx.resid[keys == g] for g in groups]` in `_grouped_residuals`, so every group is non-empty by construction (and `_align_metadata` guarantees len(keys) == len(ctx.resid) == n). Under the enclosing `len(groups) >= 4`, `edge = concat(values[0], values[-1])` has >= 2 entries and `interior = concat(values[1:-1])` has >= 2, so `if edge.size and interior.size:` is never False.

  spacr/regression_qc.py  2988->2987
      Same invariant, same family. The loop `for i, v in enumerate(values): if v.size:` iterates the same per-group residual arrays, each of which contains at least the one observation whose key created the group, so `v.size` is never 0 and the loop never falls through to the next iteration without drawing.

  spacr/qt/mask_engine.py  735->736 (and line 736)
      Family 1 -- defensive re-check after the caller already guaranteed the condition. magic_wand seeds the BFS queue only after line 725 verifies 0<=seed_y<h and 0<=seed_x<w, and the sole other q.append (line 753) is guarded at line 752 by 0<=nx<image.shape[1] and 0<=ny<image.shape[0]; so every (cx, cy) popped at line 734 is in bounds and the `not (...)` test at 735 is always False, making the `continue` at 736 dead.

  spacr/qt/widgets/fast_plots.py  1156->1157 (line 1157)
      Family 1, a defensive re-check the preceding guard already guarantees: line 1150 returns unless min(v) and max(v) are both finite with max > min, and np.min/np.max propagate NaN, so v holds only finite values inside [low, high]; np.histogram(v, bins, range=(low, high)) therefore counts every element and counts.max() >= 1 > 0, so `if peak <= 0` can never be true.

  spacr/qt/app.py  3306->-3236
      Family 1 (defensive re-check after a call that already guarantees the condition): line 3301 calls super().closeEvent(event); MainWindow inherits QMainWindow's, which is QWidget::closeEvent, whose entire body is event->accept(). Every path that reaches 3306 has therefore just accepted the event, so `if event.isAccepted():` is always True and the skip arc is never recorded. The two early-out paths that DO ignore the event (3254 and 3275/3280) return before 3301.

  spacr/sequencing.py  436->407
      Family 1: r1_seq/r2_seq are padded to exactly expected_end at lines 427-433 (the slice can never be longer), and create_consensus returns one base per position of seq1, so len(consensus_seq) >= expected_end at 436 is always True.

  spacr/sequencing.py  461->467
      Family 1: the consensus_seq kept for the orientation hint came from that same padded path, so its length is always exactly expected_end and the re-check at 461 cannot be False (the 460->467 arc for consensus_seq=None is already covered).

  spacr/sequencing.py  521->497
      Family 1: in the single-end reader consensus_seq IS r1_seq, which lines 513-515 pad to exactly expected_end two statements earlier, so the length guard at 521 is never False.

  spacr/sequencing.py  580->582
      Family 4: df is built at 563-571 from a literal dict that always contains 'columnID', and df2 = df.copy(), so `'columnID' in df2.columns` cannot be False.

  spacr/sequencing.py  582->584
      Family 4: same literal dict always supplies 'rowID', so `'rowID' in df2.columns` cannot be False.

  spacr/sequencing.py  584->587
      Family 4: same literal dict always supplies 'grna_name', so `'grna_name' in df2.columns` cannot be False.

  spacr/sequencing.py  1054->1064
      Family 1: the guard at 1037 only lets the body run when settings['mode'] is 'paired' or 'single', so when 1049 ('paired') is False the elif at 1054 ('single') is necessarily True.

  spacr/sequencing.py  1290->1301
      Family 1/4: find_and_visualize_fraction_threshold is a closure with a single call site (line 1352) that always passes dst=os.path.dirname(settings['count_data'][0]) -- a str (possibly ''), never None -- as its own docstring at 1219-1221 states.

  spacr/surrogate.py  784->785
      In _shap_importance every surviving normalisation path fixes importance.shape[0] to sample.shape[1], and sample is x_test (or x_test.sample(rows), which changes rows only) -- 2-D requires array.shape == sample.shape then means over axis 0; both 3-D forms require a match against sample.shape then mean over axes (0,2). So importance.shape[0] != x_test.shape[1] can never hold: a defensive re-check after code that already guarantees the condition (family 1).

  spacr/surrogate.py  785
      Body of the 784 guard; only reachable through arc 784->785, which no input can take (see above).

  spacr/surrogate.py  788
      The `return None` of the same guard; only reachable through arc 784->785.

  spacr/spacrops.py  924->928
      spacrStitcher.stitch_pair sets `s = self.downsample if self.downsample > 0 else 1.0` (line 881), so s is either a positive float or 1.0; `if s != 0` at 924 can never be False (defensive re-check after a computation that already guarantees the condition).

  spacr/spacrops.py  1968->1969
      _compute_mosaic_transforms already returned at line 1799 (`if not nodes: return {}, []`) and never reassigns `nodes`, so `root = max(nodes, ...) if nodes else None` at 1967 is never None; the `if root is None` re-check at 1968 cannot be True.

  spacr/spacrops.py  line 1969
      Body of that same unreachable `if root is None:` guard (`return {}, used_edges`) - unreachable for the same reason.

  spacr/spacrops.py  2814->2818
      StitchedMultiAligner.align computes `s = float(self.downsample)` (2747) and downsamples the reference to `max(1, round(H*s))` px. s == 0 collapses the reference to 1x1, ORB finds no keypoints, so `ptsA.shape[0] >= 4` fails, M_ds is None and the loop `continue`s at 2796 - line 2814 is never reached with s == 0.

  spacr/spacrops.py  3185->3197
      In stitch_cycle_wells' post-stitch loop, every `sp` in well_files is `<dst_root>/<well>/<name>` (organize) or `<dst_root>/_links/<well>/<name>` (symlink), while `rp` is always inside `orig_outdir = <dst_root>/<well>/<well>`; a file directly under <dst_root>/<well> can never have the same abspath as one under <dst_root>/<well>/<well>, so `os.path.abspath(sp) != os.path.abspath(rp)` is always True.

  spacr/spacrops.py  3539->3544
      FOVAlignAndCropper.run takes `s = float(self._aligner.downsample)` (3466) and resizes both mosaic and FOV to `max(1, round(n*s))` px. s == 0 makes both 1x1, so `_match` returns <4 points and the per-FOV loop `continue`s at 3512 long before line 3539.

  spacr/seg_qc.py  1706-1715
      `elif fields:` in _flag_findings can never be False: `members` comes from `grouped.setdefault(key, []).append(qc)`, so it always holds >=1 FieldQC, and `fields = tuple(sorted(q.field for q in members))` therefore always has >=1 element (a tuple of empty strings is still truthy). Verified empirically: _flag_findings([FieldQC(field="", ...)], 3) yields fields == ('',) and takes the True arc.

  spacr/qt/widgets/setup_slides.py  1152->1153
      `read = self.terms_were_read()` and terms_were_read (line 1079-1096) has no branch at all: its body is `self._terms_read = True; return True`. `if not read:` is therefore never true, so the false arc into 1153 cannot be recorded by any caller. (Family 4: a check whose only source has already decided it.)

  spacr/qt/widgets/setup_slides.py  1153
      The one statement guarded by 1152->1153; dead for the same reason -- terms_were_read is a literal True, so the SCROLL_HINT prefix can never be prepended to WHY_NOT_YET.

  spacr/qt/widgets/setup_slides.py  1024->1027
      `bar = scroll.verticalScrollBar()` on a QScrollArea; Qt creates the scroll bar on demand and the accessor never returns null, so the false arc of `if bar is not None:` is unreachable. (Family 1: a defensive re-check after a call that already guarantees the condition.)

  spacr/qt/screens/model_compare.py  1002->1001
      The False (continue) side of `if candidate.strip():` inside `_on_worker_error_text` needs the scan-back loop over `reversed(str(tb).strip().splitlines())` to reject its first candidate. It cannot: every character `str.splitlines()` treats as a line boundary (LF, CR, VT, FF, \x1c-\x1e, NEL, U+2028, U+2029) is also `str.isspace()`, so `.strip()` always removes it from the end. A stripped string is therefore either empty (list empty, body never runs -- that is arc 1001->1005, which IS closed) or ends in a non-blank character, which makes the LAST splitlines element non-blank and hence the FIRST item under `reversed`, so the loop breaks on iteration one. Verified by walking all 0x110000 code points (no boundary char survives a trailing strip) and by 400k random strings over the boundary alphabet (zero multi-iteration cases). Family 2: a loop whose first iteration always breaks.

  spacr/gene_measurement_compare.py  1232->1240
      In render_comparison, line 1200 `if not any(len(s) for s in series): return None, None` already returns unless some series has a non-zero length; line 1231 builds `alive = [(i, s) for i, s in enumerate(series) if len(s)]` from the same unmodified `series` with the identical predicate, so `alive` is never empty at line 1232 and the False arc to 1240 cannot be taken (family 1: defensive re-check after a guard that already guarantees the condition).

  spacr/convert.py  2049->2053
      `if 'target' in frame.columns:` in populate_db_from_map is a defensive re-check after read_map, which raises ConfigurationError when 'target' is absent ('target' is the first entry of _REQUIRED_MAP_COLUMNS); verified empirically: read_map on a CSV lacking 'target' raises "is not a spaCR conversion map - missing column(s): target", so the frame reaching line 2049 always has the column. Family 1 (defensive re-check after a call that already guarantees the condition).

  spacr/guide_attribution.py  674->675
      `dead = density.sum(axis=1) <= 0` can never be True: every entry of log_density is finite by construction (each increment is np.log(np.maximum(density, 1e-300)) on a nan_to_num'd, finite, non-negative density, and `factor` is finite in (0,1] -- n_eff<=0/nan leaves factor=1.0, min(...,1.0) caps it), so line 669's `log_density -= log_density.max(axis=1, keepdims=True)` leaves exactly one entry per row equal to 0.0, np.exp gives 1.0 there, and the row sum is >= 1; a NaN row would compare False too. Family (1): a defensive re-check after a normalisation that already guarantees the condition. Empirically probed with 1e300 values, an all-NaN column, beta likelihood at the [0,1] boundary with 1e-4 scales, and 400 columns at 50-sigma effects -- every row summed to 1.

  spacr/qt/widgets/motility_preview.py  1025->-1020
      `_on_max_sets_changed` calls `self._populate_group_box()` on line 1024, and line 999 of that method unconditionally assigns `self._sample_note = self._sampler.describe(len(shown))`. `ImageSetSampler.describe` (preview_controls.py:855-869) has exactly two returns, `f"showing all {self.total} image sets"` and the `f"showing a random sample of ..."` string, neither of which can be empty. So `self.sample_note()` on line 1025 is always truthy by the time it is evaluated -- family 1, a defensive re-check after a call that already guarantees the condition. Line 999 is the only assignment to `_sample_note` in this module (grep confirms; the other previews have their own).

  spacr/qt/widgets/feature_explorer.py  215->219
      False arc of `if position >= 0:` in set_spec, where position = self._statistic.findData(spec.statistic): ExplorerSpec.__post_init__ raises ExplorerError unless statistic is in feature_rank.STATISTICS, and __init__ populates the picker with an item for every entry of that same STATISTICS tuple, so findData never returns -1 for any spec that can be constructed (family 1: a defensive re-check after a value the constructor already guarantees). My test test_the_statistic_picker_offers_exactly_the_known_statistics pins both halves of that invariant.


==============================================================================
27. TWO MEASUREMENT DEFECTS THAT SILENTLY INVALIDATE A COVERAGE RUN
==============================================================================

Both cost real time, both produce a number that looks fine, and neither
announces itself. Recorded because the failure mode is indistinguishable
from success until something else contradicts it.

27a. MEASURING A DIFFERENT TREE THAN THE AGENTS TEST

  I measured coverage in a clean git worktree checked out at HEAD, for
  reproducibility. The agents closing arcs ran pytest in the ordinary
  working tree. The two differed by 247 modified files, because a second
  lane had uncommitted work in flight.

  Nothing errors. Both runs pass. But an arc is a pair of line numbers,
  so every target in a dirty file named a line in a file the agent never
  saw. 1,662 arcs -- 38% of everything left -- looked "blocked by
  uncommitted work" when they were only blocked by my measuring the
  wrong tree.

  The fix is not to commit the other lane's work to make HEAD match. It
  is to measure what the agents actually import: rsync the working tree
  into the measurement worktree and measure the snapshot. That also buys
  immunity to a second race (27b), because a snapshot cannot be rewritten
  underneath a running measurement.

  Rule: the measured tree and the tested tree must be the same tree.
  Verify it, do not assume it -- `diff -rq` between them should print
  nothing before a run is trusted.

27b. A FULL-SUITE RUN CANNOT SHARE A CHECKOUT WITH AGENTS WRITING TESTS

  A whole-suite xdist run in the live repo aborted at collection:

    Different tests were collected between gw5 and gw0

  A coverage agent had rewritten tests/qt/test_cov_r3_qt_app_1.py while
  the workers were collecting, so two workers enumerated two different
  versions of the same file and xdist refused to schedule. The run died
  after 50 seconds having measured nothing.

  This is not xdist being fragile. It is a genuine race, and xdist
  detecting it is the good outcome -- the bad outcome is 27a, where the
  same race produces a plausible number instead of an error.

  Rule: any measurement that takes longer than an agent takes to write a
  file must run against a snapshot, never the live checkout.

27c. THE COROLLARY FOR `--cov-append`

  Both defects share a root: coverage data is only meaningful relative to
  the exact source that produced it. Appending across runs, unioning two
  JSON artifacts, or reusing yesterday's target list all assume a tree
  that has not moved. When the tree has moved, every one of those
  operations produces a number that is wrong in the optimistic direction.
  An earlier union of two runs across commits read 98.68%; restricted to
  the files that were byte-identical between them it read 98.06%.

==============================================================================
28. THE anndata DEPENDENCY BLOCK WAS AN ARTIFACT OF THE OLD PIN, NOT anndata
==============================================================================

721 arcs live in modules that import anndata or catboost, and were
written off as unreachable because installing anndata into the pinned
environment broke the stack: pip pulled numpy 2.5.2 and pandas 3.0.5 on
top of a tree pinned to numpy 1.26 / pandas 2.2, and sklearn then failed
to import with

    module 'numpy' has no attribute 'long'

raised from scipy/sparse/_sputils.py. The obvious reading is "anndata is
incompatible with this package". That reading is wrong.

The error is scipy 1.13 being too old for numpy 2, not anndata being
wrong. Against a coherent modern stack the same two packages resolve
with no downgrade of anything:

    numpy    2.5.2      anndata   0.13.3.post0
    pandas   3.0.5      catboost  1.2.10
    scipy    1.18.1     sklearn   1.9.0

`pip install --dry-run anndata catboost` reports only additions -- zarr,
h5py, plotly and friends -- and touches no pinned package. All six
import together.

So the 721 arcs are not dependency-blocked. They are blocked on the
modernization, which is a decision rather than an obstacle, and they
become reachable the moment it lands. The only reason they looked
permanent is that the first install attempt was made against the stack
the modernization is meant to replace.

==============================================================================
29. THE GPU BACKDROP STORMED BECAUSE ITS FALLBACK GUARDED THE WRONG MOMENT
==============================================================================

Reported from a real session, not found by coverage: running `spaceout`
on Wayland printed

    ERROR: Invoking ... _Canvas.on_draw ... for DrawEvent
    ERROR: Invoking ... repeat 2, 4, 8, 16, 32, 64, 128, 256 ...

forever, over a window that never drew a frame.

WHY THE EXISTING FALLBACK DID NOT FIRE

`create_fractal_widget` already wraps `_make_gpu_widget` in `except
Exception` and falls back to the CPU renderer, with a warning that was
itself added after an earlier bug where GPU failures were swallowed
silently. It did not fire here, and the reason is worth generalising:

    the guard was around CONSTRUCTION, and the failure was at FIRST USE.

vispy links a shader program lazily, on its first draw. Construction
therefore succeeds on a context that cannot run the shaders at all. The
first draw happens inside a DrawEvent, and vispy's dispatcher catches
what a handler raises, logs it, and RETRIES with a doubling counter --
so the one condition the fallback existed for arrived in the one place
the fallback could not see, and the retry turned a single failure into
an unbounded log.

The fix is to make the failure happen where the guard already is: link
the program in `_Canvas.__init__`, inside the try the caller already
has. `on_draw` is also guarded now, and checks `_dead` on entry, because
a context can be lost after a successful link and one lost context must
not become a storm either.

THE UNDERLYING INCOMPATIBILITY

On Wayland, Qt gives vispy a GLES context. vispy prepends `#version 120`
-- desktop GLSL -- to shaders that carry no version directive of their
own, and the driver answers

    on line 2: error C0201: unsupported version 120
    on line 3: error C7532: global type vec2 requires "#version 100" or later

The "requires #version 100 or later" phrasing is GLSL ES, which is what
identifies the context as ES rather than desktop. The identical session
under QT_QPA_PLATFORM=xcb gets a GLX context and runs on the GPU with no
errors at all, so this is a property of the Qt platform plugin, not of
the machine, the driver, or the card.

Not fixed: making the shaders compile on a GLES context. That needs them
written to a version GLES accepts, and the fragment shaders are the
substantial ones. Until then Wayland gets the CPU renderer.

THE GENERAL SHAPE, WHICH IS THE PART WORTH KEEPING

A `try` around construction only covers what construction actually does.
Any resource acquired lazily -- a shader program, a database handle, a
model weight file, a compiled kernel -- moves its failure out of that try
and into the first call that touches it. Where the caller's recovery is
attached to construction, laziness silently disables the recovery. Force
the acquisition inside the guard, or move the guard to where the
acquisition happens.

THE TESTS, AND WHY THEY DO NOT USE A REAL CONTEXT

Both contracts are driven through an injected program that refuses to
link, not through a GL context a test would have to hope is broken. The
offscreen platform the suite runs on compiles these shaders fine, so a
test that waited for a real failure would skip everywhere it mattered.
The first version of the file did exactly that and the suite's own
hygiene guard rejected it, correctly: `except Exception: pytest.skip(...)`
would have reported a genuine break in the factory as a skip. It now
skips only on `GpuBackendError`, which is precisely "no usable GL
context here".

==============================================================================
30. TWO EDITABLE INSTALLS OF spaCR ARE ACTIVE IN THE SAME INTERPRETER
==============================================================================

Found while tracking down 29, and unrelated to it, but it will waste
somebody's afternoon eventually. In the base anaconda environment:

  spacr-1.5.0.4          -> /mnt/firecuda2/Claude/repo/spacr/spacr
  spacr_nightly-1.1.5    -> /home/olafsson/repo/spacr-claude/spacr   (ABSENT)

The nightly's finder points at a directory that no longer exists, and
site-packages/spacr survives as a bare `resources` directory with no
`__init__.py`, which makes `spacr` resolve as a NAMESPACE package there
-- so `import spacr` succeeds and then individual submodules are missing,
which reads like a corrupt install rather than a path problem.

The base environment's `spaceout` console script also still points at
`spacr.gui:gui_app`, while setup.py declares `spacr.qt.spaceout:main`.

None of this affected 29: the `(spacr)` conda environment resolves
correctly to /mnt/firecuda2/codex/repo/spacr, which is where that bug
was found and fixed. But `spaceout` run from base anaconda executes
different code than the tree being worked on, which is the kind of thing
that makes a fix look like it did not take.

------------------------------------------------------------------------------
27c, CONTINUED: THE INSTANCE THAT HAPPENED AN HOUR AFTER 27c WAS WRITTEN
------------------------------------------------------------------------------

Recorded because the abstract rule did not prevent the concrete mistake,
which is the most useful thing to know about a rule.

Round 3's first three files were measured against a baseline of the 34
pre-existing test files for spacr.io and spacr.regression_annotation, and
the marginal figure -- coverage those files add that the baseline does
not already have -- came out as 939 lines+arcs. That number went into the
commit message of 9295a875.

Re-running the identical comparison later gave 1533, itemised as io.py
715 and regression_annotation.py 818. Nothing about the test files had
changed. What changed was `base.json`: it had been overwritten between
the two computations, so the second run subtracted a different baseline
from the same measurement.

The commit message therefore states a number that cannot be reproduced.
It does not change what was decided -- the files close real coverage
either way, and the later keep-or-revert decision between the committed
and the agent-revised versions (1533 vs 1549) was computed inside a
single script against one baseline, so that comparison is sound.

Two things follow, and the second is the point:

  - A baseline artifact must be frozen before it is used. Copy it to a
    timestamped path and `chmod 444`. A baseline that can be rewritten is
    not a baseline; it is a variable that happens to be in a file.

  - Comparisons whose operands were produced at different times are worth
    less than comparisons computed together, even when both look equally
    like arithmetic. Prefer to load both operands and print the answer in
    one script, and prefer to re-derive a number over quoting an earlier
    one -- a figure copied forward carries none of the context that would
    reveal it had gone stale.

The general defect in 27 is that coverage data is meaningless once its
source has moved. This instance shows the same thing one level up: it is
just as meaningless once its BASELINE has moved, and the baseline is the
half nobody thinks to pin.

==============================================================================
31. `--cov=<DOTTED PATH TO A Qt MODULE>` DUMPS CORE BEFORE ANY TEST RUNS
==============================================================================

Four agents on round 4 hit this independently and each spent real time on
it, so it is worth stating exactly. Measuring a Qt module by its dotted
module path kills the process:

    pytest <any test file> --cov=spacr.qt.widgets.annotation_strategy_panel
    -> "timeout: the monitored command dumped core"

No traceback, no pytest output, no coverage data. It reproduces with a
trivial one-line test file, so it is nothing to do with the tests being
measured. One agent traced the death to tests/conftest.py's
`from PySide6.QtCore import QSettings` inside `pytest_configure`.

WHAT DOES AND DOES NOT CRASH -- measured, not assumed:

    --cov=spacr                                        works
    --cov=spacr.qt.widgets            (package)        works
    --cov=spacr.utils                 (non-Qt module)  works
    --cov=spacr.qt.widgets.<any Qt widget module>      DUMPS CORE

It is not the import on its own. Importing that module in a bare
interpreter with no QApplication succeeds, and constructing
`coverage.Coverage(source=['spacr.qt.widgets.annotation_strategy_panel'])`
outside pytest succeeds too. It needs both: coverage resolving a `source=`
entry by importing it WHILE conftest is bringing PySide6 up in
`pytest_configure`. An ordering interaction, not a plain import failure.

THE PRACTICAL RULE

Measure with a package spec and filter afterwards:

    pytest <files> --cov=spacr --cov-branch --cov-report=json:out.json
    # then select the module out of out.json["files"]

`tools/coverage/batched_coverage.sh` already uses `--cov=spacr`, so the
whole-library measurement is not affected by this. What IS affected is
every ad-hoc per-module check, which is exactly what a coverage agent
reaches for first.

WHY IT MATTERS BEYOND THE INCONVENIENCE

A crashed process writes no coverage data, but the tests it was running
still ran. Anyone who takes the surviving data as the module's coverage
reads a module that has been measured as far less covered than it is --
which is one of the ways a target list comes to name arcs that are
already closed. See 32.

==============================================================================
32. A STALE TARGET LIST SENDS AGENTS TO ALREADY-COVERED CODE
==============================================================================

Round 4 was scoped from a coverage artifact measured at 06:11, filtered
for modules whose source had not changed since. That filter was not
enough, and five agents reported the same thing from different modules:

  setup_slides      226 of the chunk's 229 arcs already closed
  annotation_umap_tab   every listed line and arc already closed; the
                        module was already at 100%/100%
  figure_settings   all 13 targets already closed
  model_zoo (screen)  7 of 12 arcs already closed
  memory_budget     18 of 21 already closed, by a commit landed that day
  mask              chunk said 59.8%; the pre-existing tests give 77%

Three separate causes, and they need separating because the fixes differ:

  1. DATA LOST AT MEASUREMENT TIME. A batch whose worker died contributes
     nothing while its tests still count as passed, so real coverage is
     recorded as missing. 31 is one way that happens.

  2. THE ARTIFACT PREDATES RECENT WORK. Round 3 landed between the
     artifact and round 4, so round 4's biggest targets were precisely
     what round 3 had just finished. Excluding round 3's six modules by
     hand caught most but not all of it -- memory_budget was closed by a
     commit that same morning.

  3. LINE NUMBERS DRIFT FROM COMMITS, NOT JUST FROM UNCOMMITTED EDITS.
     One agent proved its chunk was measured at commit 80a1a4b6 by
     re-parsing every revision of the file and finding exactly one with
     zero invalid statements and zero invalid arcs; HEAD's copy had been
     rewritten by 84b3bff2, so 51 of 58 arcs named positions that are not
     statements any more. Another found model_zoo's numbers uniformly off
     by +2. Both worked around it by building a difflib line map from the
     measured revision to HEAD, which is the right move and should be the
     documented one.

THE RULE THAT FOLLOWS

A target list is only valid against the exact tree it was measured on.
Before dispatching a round, either re-measure at HEAD, or record the
commit the artifact was measured at and remap through difflib -- and in
both cases drop any module that has acquired a test file since. An
mtime filter against the artifact catches uncommitted edits only; it
does not catch a commit, and it does not catch coverage that was closed
by someone else's test file.

The cheapest available check is the strongest: a module that already has
a `test_cov_*` file naming it is a module whose chunk should be
re-derived before anyone is sent to it.

==============================================================================
33. live_preview's MIP TOOLTIP READS THE FIELD ID AS A TIME AXIS
==============================================================================

Reported by a coverage agent, verified here, NOT fixed -- the fix is a
design decision rather than a correction.

spacr/qt/widgets/live_preview.py:2019, in `_refresh_mip_toggle`:

    timed = any(len(s.key) > 2 and s.key[2] for s in sets) and planes > 1

`s` is an `ImageSet`, and its key is declared at preview_controls.py:400
as

    key: Tuple[str, str, str]     # (plate, well, field)

which `ImageSet.label()` unpacks eight lines later as
`_plate, well, fieldid = self.key`. So `s.key[2]` is the FIELD ID, not a
timepoint. It is non-empty for every ordinary acquisition, so `timed` is
true for essentially every z-stack, and the tooltip gains

    "Projects within a timepoint only; the time axis is left alone."

on data that has no time axis at all.

WHY THIS IS NOT A ONE-LINE FIX

There is no time signal available to correct it with. `ImageSet` carries
exactly four fields -- key, directory, channels, planes -- and none of
them is a timepoint or a time count. The word "timelapse" does not appear
anywhere in live_preview.py except inside the docstring of this very
method, which says the 4-D axis order "is never inferred here; that is
`t_axis_order`'s job in the pipeline, and it deliberately has no default
because (T,Z,Y,X) and (Z,T,Y,X) cannot be told apart."

So the method's own documentation says this widget cannot know, and the
line beneath it guesses anyway from a field that means something else.

The three ways out are a product decision, not a correction:

  1. Drop the sentence. The tooltip then describes the z-projection only,
     which is all this widget can honestly claim.
  2. Plumb the timelapse flag in from the settings the run was configured
     with, and say it only when that flag is set.
  3. Give `ImageSet` a real time field at discovery time, which is the
     largest change and the only one that would let the preview show
     anything else time-aware later.

Left alone deliberately. Whoever owns the preview should pick; pinning
the current behaviour in a test would only make the wrong sentence
harder to remove.

FIXED 2026-08-30. The tooltip now reports only the z-plane projection that
this surface can prove. It no longer treats ``ImageSet.key[2]`` (the field
identifier) as time metadata, and the production-path test proves that both
empty and populated field IDs leave time-axis claims out of the tooltip.

CONTRAST WITH THE custom_model DEFECT FIXED IN 387b47b5

That one was also found by reading rather than by a failing test, and it
was fixed the same afternoon -- because the correct behaviour was not in
question. The description, the validator and every consumer agreed
`custom_model` is a path; only the type table disagreed, and the repo
already had a precedent for exactly that fix. Where the intended
behaviour is unambiguous, fix it. Where the fix requires choosing what
the software should do, report it and stop. The difference is not how
sure you are that the code is wrong; it is whether "right" has one
answer.

==============================================================================
34. figure_queue's TEARDOWN SWEEP HAS NEVER DISCONNECTED ANYTHING
==============================================================================

Found by a round-5 agent, verified here, NOT fixed -- switching it on is
a behaviour change, not a correction.

`_teardown_canvas` (spacr/qt/widgets/figure_queue.py:1662-1671) walks the
canvas's callback registry looking for callbacks owned by the two widgets
about to die:

    owner = getattr(getattr(proxy, "func", None), "__self__", None)
    if owner is not None and id(owner) in doomed:
        canvas.mpl_disconnect(cid)

No proxy in the registry has a `.func`. Measured against the pinned
matplotlib 3.8.4, every entry is one of

    matplotlib.cbook._StrongRef      (holds its target in `._obj`)
    weakref.WeakMethod               (yields its target when CALLED)

so `getattr(proxy, "func", None)` is `None` for all of them, `owner` is
always `None`, and the loop disconnects nothing. It has presumably never
disconnected anything on this matplotlib.

WHAT IT COSTS

Less than it looks. The explicit loop above it disconnects the toolbar's
own `_id_press`/`_id_release`/`_id_drag`/`_id_zoom`/`_id_pan`, which is
the real work, and matplotlib stores bound methods as `WeakMethod`, so a
callback owned by a dead widget mostly evaporates on its own. The sweep
is a belt to that braces. But the comment above it says "a stale callback
of any kind is a crash here", and for anything the explicit list does not
name, that belt is not fastened.

WHY IT IS NOT FIXED HERE

Reading the owner correctly is easy --

    target = proxy() if isinstance(proxy, weakref.WeakMethod) else \
             getattr(proxy, "_obj", None)
    owner = getattr(target, "__self__", None)

-- but the effect is to switch on a loop that has been inert for its
whole life, and the block's own comment records that matplotlib keeps its
own entries in that registry, "the pylab figure manager's _cidgcf among
them". Making the sweep work means it starts disconnecting things, and
what it would disconnect has never been observed. That is a change to
behaviour on a teardown path, and it wants a deliberate decision and a
run behind it rather than a drive-by.

The two honest options are to make it work as above, or to delete it and
rely on the explicit ids plus WeakMethod, which is what has actually been
happening. Either is better than a loop that reads as protection and
provides none.

THE SHAPE, WHICH IS THE REUSABLE PART

`getattr(x, "name", None)` inside a `try: ... except Exception: pass` can
never fail out loud. A wrong attribute name and a legitimately absent one
are the same silent `None`, so the code degrades to a no-op that looks
exactly like a clean pass. Every ingredient here is individually
reasonable defensive style; together they hide a dead loop.

Where a defensive accessor is guarding something that matters, assert the
shape once -- in a test, against the real library -- so the guard fails
loudly when the library changes underneath it. The agent that found this
did exactly that: its test installs a proxy of the shape the sweep is
written against, so the contract is now pinned even though the sweep does
not yet meet it.

FIXED 2026-08-30. The sweep now resolves ``weakref.WeakMethod`` and
Matplotlib ``_StrongRef`` wrappers without executing unknown callable objects,
then disconnects only callbacks bound to the dying canvas or toolbar. Its test
now registers callbacks through the real Matplotlib registry and proves that
canvas and toolbar callbacks leave while an unrelated callback survives; the
old synthetic ``.func`` proxy that made the dead implementation appear to work
is gone.


35. THREE TESTS THAT ONLY FAILED BATCHED, AND WHAT THE THREE HAVE IN COMMON
---------------------------------------------------------------------------

All three passed alone and failed in the batched coverage run. None was
flaky in the sense of "sometimes"; each was deterministic given what ran
before it, which is worse, because it looks like noise and is not.

35a. A PATCH THAT LANDED ON A SECOND MODULE OBJECT.
`test_the_class_editor_is_handed_the_live_preview_frame` patched
`ClassEditorWidget` on a freshly imported `spacr.qt.screens.settings_model`
and restored it in a `finally`. That is the same object as the test
module's `SM` right up until something drops the module from `sys.modules`
and lets it be imported again. After that there are two module objects:
the patch lands on the new one, `_widget_for` keeps reading the globals it
was DEFINED against, and the `finally` restores the wrong copy too. The
real widget then received the test's `object()` sentinel and raised
`'object' object has no attribute 'columns'`.

Reproduced directly -- delete from `sys.modules`, re-import, run both
forms -- rather than inferred. `importlib.reload` does NOT cause it: it
re-executes in place and the object stays identical, which is why
grepping the batch for a reload found nothing.

FIXED by targeting `type(widgets)._widget_for.__globals__`, the dict the
function body genuinely resolves against, with `monkeypatch.setitem` so
it is restored even when the assertion fails.

THE RULE: to patch a name a function reads as a module global, patch the
function's `__globals__`. `import x as m; m.NAME = ...` is the same thing
only while nothing has re-imported `x`.

35b. A MEASUREMENT TAKEN WHILE THE THING BEING MEASURED WAS STILL MOVING.
`test_it_opens_at_the_size_it_always_did` compares a settings window's
size with an event filter off and on. Qt warms font metrics and style
sheets per widget class, so successive builds of the same dialog shrink
slightly until the caches are full. An earlier fix here opened one
throwaway first -- right argument, unfinished -- and the size was still
moving on the THIRD build, which is the one the assertion reads:
`annotate._SettingsDialog` 1,183 px against 1,191, `ExecutionProfileDialog`
615 against 616. The residue was being charged to the filter.

FIXED by building each side until two consecutive sizes agree, capped at
six, with the filter reinstalled from a `finally`.

THE RULE, and it is 27's rule in another costume: a warmup that moves a
number without settling it converts a reproducible difference into a
plausible one. Settle, then measure.

35c. AN ASSERTION THAT CANNOT HOLD FROM THE TREE THE SUITE IS MEASURED IN.
`test_this_checkout_is_recognised_as_editable` asserts the running tree
IS the editable install. The coverage runs measure a frozen SNAPSHOT of
the tree (see 27), so the running tree is a copy and the editable install
still points at the original. False for a reason that says nothing about
the guard.

FIXED by splitting it: what the guard depends on -- a location is found,
it is a directory, it contains a `spacr` package -- is asserted
everywhere and more tightly than before, so a stale path now fails here
instead of passing. The identity is skipped with both paths named, rather
than passed over by a silent `if`; a conditional that quietly does
nothing is exactly the failure mode this file exists to catch.

WHAT THE THREE SHARE: each was a test that could not see the state it
depended on. Two were made honest by making the dependency explicit (the
globals dict, the settled size) and one by admitting the dependency
cannot be satisfied from a copy. None was fixed by loosening a tolerance,
and none should be.

36. ENTRY 30 NOW BLOCKS ITEM 306's CAPTION REVIEW
-------------------------------------------------

Cross-reference, not a new defect. 30 records that two editable installs
of spaCR are active in the same interpreter. That has so far shown up in
tests; it now blocks TOOLING.

Item 306's external caption inventory is 102 records ahead of its
ratchet, and the identities are pinned as a digest rather than listed --
so reviewing the additions means recomputing the previous set by running
`tools/build_i18n_catalogs.canonical_sources()` against the tree at
`d0c1a633c`. Doing that from a worktree at that commit dies in the
import: the interpreter mixes the worktree's modules with the editable
install's, and `spacr.qt.screens.app_screen` asks the old `theme` for a
symbol only the new one has.

The workaround, if 30 stays open: run the old builder in a SUBPROCESS
whose `sys.path` cannot see the editable install at all, rather than
trying to shadow it from inside a live interpreter.


37. THE COMPOSITE THEME TOKEN STILL HANDLES A THEME THAT WAS REMOVED
--------------------------------------------------------------------

Found by trying to drive five uncovered items in `spacr/qt/preferences.py`
and discovering there is no input that reaches them.

`get_theme_choice` and `set_theme_choice` collapse the theme and its
image variant into one token for the Theme combo box. Both carry a
`space:` arm:

    preferences.py:1493   if theme == "space":
    preferences.py:1494       return f"space:{get_space_variant()}"

    preferences.py:1506   if choice.startswith("space:"):
    preferences.py:1507       set_space_variant(choice.split(":", 1)[1])
    preferences.py:1508       set_theme("space")

BOTH ARE DEAD, and by two independent routes:

  * `theme_choices()` says so itself -- "Space is gone; its variants are
    no longer offered" -- so no `space:` token exists. `set_theme_choice`
    refuses anything not in that set three lines above, so its arm cannot
    be entered. If it somehow were, `set_theme("space")` on line 1508
    would raise, because VALID_THEMES is ('dark','light','cell','glass',
    'system').
  * `get_theme()` returns `raw if raw in VALID_THEMES else DEFAULT_THEME`,
    so it cannot answer "space" -- not even from a settings file written
    before the removal, which is the one case an arm like this exists for.
    That case is DRIVEN in the test: writing "space" straight into the
    store, past `set_theme`'s validation, reads back as the default.

NOT the same as the space WALLPAPER machinery, which is live:
`get_space_variant`, `space_variants` and `SPACE_PHOTO_VARIANTS` are
still used by the background-path helper at preferences.py:1587, which
`spaceout` needs. Only the two THEME-TOKEN arms are orphaned. Anyone
acting on this should not follow the symbol names outward.

NOT FIXED. Deleting five lines is the obvious repair and this package's
own convention (`tests/test_a_guard_no_input_could_reach_is_gone.py`),
but the standing rule is that nothing is deleted until the maintainer
says so. Pinned instead, with the pins written so they FAIL if either
route reopens -- if "space" returns to VALID_THEMES, or if `get_theme`
stops filtering what it reads, or if `theme_choices` offers a space
token again. See tests/qt/test_cov_r9_preferences_space_theme.py, which
also covers the live `cell:` and plain arms and adds a round-trip over
every offered token, since the two functions are far enough apart in the
file that one can grow a case the other does not.

WORTH NOTING for the sweep generally: five items that looked drivable
were not, and the way to find that out was to try to drive them and read
why the assertion failed. A "reachable-looking" branch is not evidence
that it is reachable.


==============================================================================
30 AND 36, CORRECTED 2026-08-31: THE HAZARD IS REAL, THE DIAGNOSIS WAS NOT
==============================================================================

Entry 30's hazard is confirmed on this machine, with different paths than it
recorded, and it is worse than "two editable installs" -- the surviving one
points somewhere nobody would guess:

  env `spacr` (py3.13)   __editable___spacr_1_5_0_4_finder.MAPPING
                         spacr -> /media/carruthers/mnt3/claude/repo/spacr/spacr
                         which is the OTHER lineage, not this checkout.
  env `base`  (py3.9)    site-packages/spacr/ holds only `resources/` and has
                         no __init__.py, so `spacr` resolves as a NAMESPACE
                         package and every submodule appears missing.

The finder sits on `sys.meta_path` AFTER `PathFinder`, so it answers only when
sys.path misses -- which means it wins silently whenever the intended tree is
not on the path, and stays invisible whenever it is. That is the whole trap:
the same command reads two different lineages depending on the cwd.

BUT ENTRY 36'S DIAGNOSIS IS WRONG, and it sent the review down the wrong road.
It says the caption builder fails at d0c1a633c because "the interpreter mixes
the worktree's modules with the editable install's". It does not. Both sides
of that ImportError are inside the worktree:

    app_screen.py:48  from ..theme import (..., preserve_widget_qss_overlay)
    theme.py          does not define it -- nor _LOCAL_WIDGET_QSS_ATTRIBUTE

d0c1a633c DOES NOT IMPORT ON ITS OWN. No foreign module takes part, and no
amount of sys.path isolation would have helped. `a3c18f779` added both the
definition and its use; the lane that produced d0c1a633c kept the use and lost
the definition. app_screen calls it at 1714 and 1735, so this is not a stray
import -- that revision's app screen would fail at runtime too.

THE GENERAL POINT, which is the reusable half: "cannot import name X from Y"
looks identical whether Y is the wrong copy or the right copy missing X. The
question that separates them is where Y actually lives, and it is one line to
ask. Entry 36 assumed the first without asking, because entry 30 had made the
first salient.

WHAT WAS BUILT: tools/i18n_reference_sources.py reads canonical_sources() from
any revision in a subprocess whose sys.meta_path and sys.path cannot reach an
editable install -- and then PROVES it by checking every loaded spacr* module
resolves inside the target tree. It reports the two faults separately, which is
what this entry got wrong. `doctor` reports the hazards above for whichever
interpreter runs it; both are still present and are environment repairs, not
code changes, so neither is done here.

Guard: tests/test_i18n_reference_sources.py, 7 tests. The three that carry the
argument -- the isolation proof, the child's own sys.path scrub, and the
internally-inconsistent verdict -- were mutation-checked; the first version of
the scrub test passed with the scrub disabled and was replaced.


ENTRY 30, RESOLVED FOR THE `spacr` ENVIRONMENT 2026-08-31
------------------------------------------------------------------------------

Half of this entry is now closed by repair rather than by argument.

  * The `spacr` env's editable install was reinstalled against
    /home/carruthers/Documents/repo/spacr, and the duplicate checkout it had
    been pointing at (/media/carruthers/mnt3/claude/repo/spacr) was deleted on
    the maintainer's instruction after being shown to carry zero commits that
    were not already on origin/nightly. Its one uncommitted edit was preserved
    -- see instruction 314's dated entry. `doctor` now reports 0 hazards, and
    `import spacr` from a neutral cwd resolves to the one tree.

  * STILL OPEN, and it is an environment repair rather than a code change: the
    base anaconda env (py3.9) still has site-packages/spacr holding only
    `resources/` with no __init__.py, so `spacr` resolves there as a NAMESPACE
    package and every submodule appears missing. Nothing in this repository can
    fix that; it needs the directory removed from that environment. Until then,
    do not run spaCR tooling from base.

The lesson the deletion confirms rather than the fix: the duplicate had zero
unique commits, so the lineage that had been silently answering imports for an
unknown length of time was contributing nothing but the ambiguity.

ENTRY 30, FULLY RESOLVED ON THE CURRENT HOST 2026-09-02
------------------------------------------------------------------------------

The same base-environment hazard persisted on this host under Python 3.12:
`site-packages/spacr` was still a resource-only namespace, and the second
editable distribution `spacr_nightly-1.1.5` still pointed at the absent
`/home/olafsson/repo/spacr-claude/spacr`. Before repair, a neutral
`import spacr` produced `__file__ = None` and no version.

The namespace directory and the stale nightly distribution's `.pth`, finder,
bytecode and dist-info were moved—not deleted—to the recoverable backup
`/home/olafsson/.cache/spacr/environment-backups/`
`base-spacr-namespace-20260902-eahb67`. The valid editable `spacr-1.5.0.4`
distribution was left intact. From `/tmp`, `spacr`, `spacr.cli`, and
`spacr.qt.spaceout` now import from `/mnt/firecuda2/Claude/repo/spacr/spacr`.

The authoritative neutral-cwd verification is `spacr-doctor --no-gpu-probe
--json`: 14 PASS, 0 FAIL, 0 ERROR; it reports one importable spaCR package,
one spaCR distribution, all 15 commands resolved, core imports available, and
the expected editable checkout running. The sole warning is the unrelated
optional `lightgbm` extra. Entry 30's environment repair is therefore complete
and can be restored by moving the five backup artifacts to their original
paths if needed.


==============================================================================
38. A NEGATIVE RESULT: THE OBVIOUS EARLY-OUT IN button_roles IS 5% SLOWER
==============================================================================

Recorded here as a named entry, not only in 315's prose, because the next
person to read that filter will have the same idea and it is wrong.

`_SemanticButtonFilter.eventFilter` is installed on the QApplication. Measured
during ONE warm Regression screen build on a real display:

    events dispatched to it              109,410
    events sent to a QPushButton at all     4,576   (4.2%)
    events of the four types it handles       155   (0.14%)

It asks `isinstance(watched, QPushButton)` first. So it runs a type check
109,410 times to do work 155 times, and the obvious repair is to test
`event.type()` against the four handled types first and reject 95.8% of
dispatches before the isinstance. That was implemented, and A/B measured over
seven consecutive warm builds each:

    original ordering       1,282 ms median
    "optimised" ordering    1,352 ms median      5% SLOWER

REVERTED. The reason is that the two tests are not the same price.
`event.type()` is a PySide call that crosses into C++ and constructs a Python
enum object; `isinstance` against a shiboken-registered type is a C-level
check. The original ordering pays for `type()` only on the 4,576 events that
already passed the cheap test, and the "optimisation" made all 109,410 pay it.

THE GENERAL RULE, which is the reusable part: in PySide the test that LOOKS
cheaper -- an int compare against a small set -- can cost more than a type
check, because the int has to be marshalled out of C++ before it can be
compared at all. Selectivity is not the only term in the product; the price of
each test is the other, and it is not visible in the source. Any change to a
filter on this path is an A/B measurement, not an argument.

The ordering that is there now is deliberate. Leave it.


==============================================================================
APPENDIX A, RE-TRIAGED 2026-08-31: A THIRD OF THE FIRST BATCH IS ALREADY FIXED
==============================================================================

Appendix A is a 2026-08-30 snapshot and roughly 900 commits have landed since.
Its own preamble says to treat each entry as a lead with an address rather than
a finding; the addresses have ALSO drifted, so a line number in it locates
nothing and several entries describe code that no longer exists.

VERDICTS from the first pass. "FIXED ALREADY" means the current source carries
the repair the entry asks for, checked by reading the code, not the line:

  A1   CONFIRMED, FIXED HERE. See the dated entry above.
  A2   CONFIRMED, FIXED HERE. Detail below -- it overrides a deliberate pin.
  A3   FIXED ALREADY. Both sides of the comparison now normcase.
  A6   FIXED ALREADY. `_controls` and `_effects` are initialised in __init__.
  A8   FIXED ALREADY, and thoroughly: cell_montage now pre-flights the guides a
       gene covers, with a comment naming the exact false verdict A8 reports.
  A44  FIXED ALREADY. `_toggle_ai` logs at debug with exc_info.
  A49  FIXED ALREADY. The handler is now REMOVED from every non-sink logger, so
       one record is emitted once rather than 2-3x.
  A51  Still open, and A52 IS THE SAME ADDRESS -- two entries, one site. Whoever
       takes it should read them together; they are different claims about the
       same two lines and only one of them is a defect.

So of nine leads examined, five needed nothing. ANYONE CONTINUING THIS APPENDIX
SHOULD RE-VERIFY BEFORE PLANNING, and should budget for the verification rather
than for the fix: reading the current code took far less time than the fix
would have, and five of nine fixes would have been written against code that
had already moved.

A2 IN DETAIL, BECAUSE IT OVERRIDES A DELIBERATE DECISION
--------------------------------------------------------

`_with_late_registrations` matches its fallback band by exact title. When no
band answers, the loop ran to exhaustion and returned the table unrepaired,
silently. `tests/test_cov_wf_resources_home_versions__generators_common.py`
PINNED that, and reasoned about it: the failure still surfaces, as
`check_coverage` refusing the table, "which is the message a maintainer needs
to see".

Both positions agree it fails loudly. They disagree about where. The late
message is `keys not categorised: ['brand_new']`, which points at the app and
the registry -- both of which are correct -- while the mistake is a band title
three arguments away, and it arrives at module import of the variant
generators and takes all thirty Home renders with it. The earlier raise names
the title asked for, the titles that exist, and the keys that could not be
placed, at the only line that can see all three.

`check_coverage` keeps its own half: it is still the net for a key that is
genuinely uncategorised rather than misdirected, and the test asserts both.
All four shipped tables name a title that exists, so nothing live changes.

Recorded at this length because reversing it should be cheap for whoever
disagrees: the position being overridden was written down, which is the only
reason it could be weighed at all.


==============================================================================
39. THE MASK PANEL RENDERS NO PATHOGEN MODEL ROW  (lane: spacr/qt/screens)
==============================================================================

Found 2026-08-31 while attaching the model-zoo button, and it is why the
maintainer's "pathogen model setting isn't wired yet" was right in a way
neither session expected.

`pathogen_model` and `pathogen_model_name` are BOTH offered by
`set_default_settings_preprocess_generate_masks`, and BOTH categorised under
"Pathogen" in `spacr.settings.categories`. Neither is in `_APP_HIDDEN_KEYS`.
And the Mask panel builds no widget for either: measured across every built-in
module, exactly ONE renders a key that names a Cellpose checkpoint --
`analyze_plaques` renders `plaque_model`.

    AppScreen(app_key="mask")._settings_model._widgets
        94 widgets, and the only "model" key among them is `cell_model_name`

`nucleus_model_name` and `organelle_model_name` are absent for the same
reason. The Pathogen, Nucleus and Organelle sections are conditional on the run
having that object, and setting `pathogen_channel` through the widget did not
bring the rows back -- `_lay_out_the_rows_that_are_back` takes an argument this
session did not trace.

WHY IT MATTERS BEYOND THE BUTTON. The BACKEND honours `pathogen_model`
end-to-end: `object.py:769-771` reads it for `object_type == 'pathogen'` and
hands it to `_resolve_cellpose_pretrained`, which returns an existing file
as-is and raises FileNotFoundError rather than silently falling back to cpsam;
`core.py:432` reaches that on the default Mask path. Instruction 23eacd71c
corrected the comments that said otherwise. So the setting works from a
settings CSV or the API, and there is no way to reach it from the screen --
which is the state most likely to be read as "the setting does nothing".

NOT FIXED. `spacr/qt/screens/` belongs to the concurrent session, and the fix
is a row that renders rather than a button that attaches. The button is
already written and lands on `plaque_model` today
(`AppScreen._with_a_model_zoo_button`, `_MODEL_ZOO_KEYS`), so whenever the
pathogen row appears it acquires the button with no further work.

--------------------------------------------------------------------------------
2026-09-01 — AUDIT: A COUNT, SO THE ITEM HAS A PERCENTAGE
--------------------------------------------------------------------------------

This file had no headline figure, which made it impossible to report against
the reporting rule in 325 section 4. Counted mechanically over both entry
shapes (the numbered narrative sections and the A-entries), calling an entry
resolved when its body carries FIXED / WITHDRAWN / NOT A DEFECT / CLOSED:

    97 entries, 26 resolved, 71 open   -->  ~27%

TREAT THAT AS APPROXIMATE AND NOT AS A LEDGER. It is a text scan, so an entry
fixed in code without its verdict written back here counts as open, and an
entry that merely quotes the word "fixed" while discussing something else
counts as resolved. Two of this repository's most expensive mistakes were
believing a header over the code, so the number above is a floor on progress,
not a measurement of it.

WHAT WOULD MAKE IT A MEASUREMENT: give every entry a one-word verdict line in
a fixed position, the way the newer A-entries nearly do. Then the count is a
grep and the next session does not re-derive it.


==============================================================================
40. AN ENABLED TIMELAPSE COULD OMIT A MALFORMED SOURCE FIELD
==============================================================================

Found during the post-commit review of instruction 339. The V1 timelapse path
trusted `_generate_time_lists`: an NPY whose name could not be parsed as
`plate_well_field_time` was absent from the returned groups, so illumination
correction could finish with a provenance manifest that never mentioned that
source field. Cellpose would process the grouped subset without reporting the
omission.

VERDICT 2026-09-02: RESOLVED by `cd09994e1`. When illumination is enabled,
`concatenate_and_normalize` now compares the complete source-NPY inventory to
the grouped inventory and refuses the run with the exact omitted filenames.
The focused malformed-time-name test proves the refusal, while the ordinary
timelapse counterpart proves valid names still group and publish.


==============================================================================
41. A SHORTER V1 RERUN COULD LEAVE A STALE NPZ FOR CELLPOSE
==============================================================================

Also found during instruction 339's post-commit review. V1 wrote each new
normalised archive over the old directory independently; if a rerun produced
fewer batches, an old top-level NPZ survived and remained visible to the
Cellpose glob even though it was not in the new illumination manifest.

VERDICT 2026-09-02: RESOLVED by `cd09994e1`. V1 now builds and validates the
complete archive set in a private staging directory, atomically replaces the
published set with rollback on failure, and removes staging on every exit.
Focused tests prove a shorter rerun removes stale archives, successful
publication is exact, and a mid-publication failure restores the prior set.

REVISED COUNT after recording these two already-resolved findings and the A14,
A16 and A58 verdict corrections above: 99 entries, 31 recorded as resolved. This
is still a prose-derived progress aid rather than an acceptance ledger.


==============================================================================
42. A REMOVAL-ONLY INSTALL PLAN WAS REPORTED AS CHANGING NOTHING
==============================================================================

Found 2026-09-03 while tracing the constructor fields in ``spacr/updater.py``.
The uv dry-run parser correctly retained a standalone ``- name==version`` as a
``PackageChange(name, version, None)``, but ``PackageChange`` classified only
additions and version moves. Consequently ``DryRun.summary`` described a
removal-only plan as "change nothing -- it is already satisfied", and
``install_decision`` did not require a second confirmation even when the
removed distribution was protected NumPy, PyTorch, pandas, or scikit-learn.

VERDICT 2026-09-03: RESOLVED by ``2a7b927d7``. Removals are now a distinct
change kind, appear with their installed versions in the pre-install report,
and protected removals enter the same explicit second-confirmation gate as
protected version moves. Focused tests cover both the protected and
unprotected cases; changing the gate back to version moves alone makes the
protected-removal test fail.

REVISED COUNT: 100 entries, 32 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
43. AN EMPTY SWEEP AXIS WAS REPORTED AS ONE COMBINATION
==============================================================================

Found 2026-09-03 while completing function documentation in
``spacr/parameter_sweep.py``. ``SweepSpace.size`` multiplied each axis by at
least one, so an explicitly empty value list was reported as one raw
combination. ``build_trials`` correctly used the Cartesian product and built
zero trials, leaving the displayed size inconsistent with the work that could
actually run.

VERDICT 2026-09-03: RESOLVED. ``SweepSpace.size`` now multiplies by the real
axis length, retaining the conventional one combination for a space with no
axes while returning zero when any configured axis is empty. The focused
regression first failed with ``1 == 0`` against the old implementation and now
pins ``space.size() == len(build_trials(space)) == 0``.

REVISED COUNT: 101 entries, 33 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
44. SEARCH-TABLE SLICES RETURNED A LIST DESPITE A SINGLE-ROW TYPE CONTRACT
==============================================================================

Found 2026-09-03 while completing function documentation in
``spacr/umap_search.py``. ``SearchTable.__getitem__`` delegated directly to a
list, so a slice returned ``list[SearchRow]``, but its annotation accepted only
an integer and promised one ``SearchRow``. Runtime callers worked while static
API users were told the valid slice operation had the wrong input and result.

VERDICT 2026-09-03: RESOLVED. The annotation and prose now describe both list
lookup forms, and a focused test proves integer lookup returns the retained row
while slicing returns a detachable outer list whose mutation cannot reorder
the table. The same pass corrected the stronger false claim that a recipe can
rebuild identical coordinates across nondeterministic backends; the stored
``SearchRow.embedding`` is the exact scored map.

REVISED COUNT: 102 entries, 34 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
45. A DIRECT NONPOSITIVE MAD THRESHOLD MARKED ORDINARY VALUES AS OUTLIERS
==============================================================================

Found 2026-09-03 while completing function documentation in
``spacr/outlier_filter.py``. The public ``apply`` path treated a nonpositive
MAD threshold as disabled, but the lower-level ``outliers`` helper multiplied
by it directly. At ``0`` every non-median value was flagged; below zero every
finite value was flagged, so direct API callers received the opposite of the
documented disabled-filter behavior.

VERDICT 2026-09-03: RESOLVED in ``spacr/outlier_filter.py:88-90``. The helper
now returns an all-false mask for nonpositive or non-finite thresholds, matching
``apply``. A parameterized test covers negative, zero, NaN, and both infinities
and retains a positive-threshold counterpart; restoring the old multiplication
made the negative, zero, and negative-infinity cases fail.

REVISED COUNT: 103 entries, 35 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
46. THE PACKAGE NULL HANDLER PREVENTED FILE LOGGING FROM BEING CONFIGURED
==============================================================================

Found 2026-09-03 while checking the documentation contract in
``spacr/logger.py``. Importing the package installs a ``NullHandler`` on the
``spacr`` logger, while ``configure_logger`` returned early whenever *any*
handler existed. Its default call therefore installed no rotating file
handler. The same early return prevented a later ``stream=True`` request and
updated the logger level without updating the handlers it claimed to control.

VERDICT 2026-09-03: RESOLVED in ``spacr/logger.py:39-74``. Handlers installed
by this helper are tagged as file or stream handlers, foreign/bootstrap
handlers are preserved without counting as configuration, late stream requests
add exactly one handler, and every reuse updates the owned handler levels.
Three focused regressions all failed when the old early return was restored
and pass with the repaired lifecycle.

REVISED COUNT: 104 entries, 36 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
47. PNG-LIST FALLBACKS COULD CUT OR LABEL A DIFFERENT OBJECT
==============================================================================

Found 2026-09-03 while auditing the lightweight crop resolver in
``spacr/png_list.py``. Three permissive conversions shared the same failure
mode: a fractional float was truncated into another label (and a boolean into
object 1), an unknown ``object_type`` silently selected ``cell_id``, and a
frame carrying another mode's ID column borrowed that label while continuing
to stamp and resolve paths under the requested mode. With multiple alternate
ID columns, dictionary order silently chose the mask.

VERDICT 2026-09-03: RESOLVED in ``spacr/png_list.py:40-168``. Only finite,
integral, non-boolean numeric IDs are accepted; unsupported modes are named and
refused; one alternate ID column carries its effective object type through the
field lookup and returned rows; multiple alternates are refused as ambiguous.
Mutation proof against the permissive implementation produced eight focused
failures, including both wrong results and infinity exceptions, and the
restored resolver passes the downstream crop-source check.

REVISED COUNT: 105 entries, 37 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
48. A FAILED FRAME STAGE LEAKED ITS OFFER AND COULD DAMAGE THE OLD ARTEFACT
==============================================================================

Found 2026-09-03 while completing function documentation in
``spacr/frame_handoff.py``. ``stage`` deliberately offered a frame before its
durable write so a concurrent reader could not parse a partial file, but a
writer exception left that unsuccessful frame live in the offer registry. The
writer also targeted the final path directly, so a failed replacement could
truncate a previously valid artefact even though ``stage`` raised.

VERDICT 2026-09-03: RESOLVED in ``spacr/frame_handoff.py``. Staging now writes
to a same-directory temporary path and atomically replaces the destination
only after success; every failure removes the temporary and exactly restores
the prior offer and shape before re-raising the original exception. Focused
tests observe the new frame during the attempted write, then prove a new-path
failure leaves no offer or partial file and a failed restage preserves both
the prior live frame and durable bytes. Removing atomic staging made both
tests fail, while replacing exact registry rollback with unconditional
release made the prior-offer test fail.

REVISED COUNT: 106 entries, 38 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
49. PERMUTATION QC ACCEPTED MISALIGNED OBSERVATION AND LABEL VECTORS
==============================================================================

Found 2026-09-03 while completing function documentation in
``spacr/permutation_qc.py``. ``position_effect`` and
``block_residual_report`` assumed that residuals, block labels, and every
position column had identical lengths. Unequal inputs escaped the public API
contract and failed later as NumPy boolean-index ``IndexError`` exceptions,
rather than explicitly refusing a statistically invalid pairing before any
partial report was assembled.

VERDICT 2026-09-03: RESOLVED in ``spacr/permutation_qc.py``. Both public
calculations now materialise and validate their paired inputs first, naming
the mismatched vector and both lengths in ``ValueError``. Focused tests cover
shorter and longer position vectors, a short block vector, and a short named
position column; removing the guards made all four cases fail with the former
raw ``IndexError``, while the existing valid noise, gradient, and per-block
counterparts remain green.

REVISED COUNT: 107 entries, 39 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
50. NON-FINITE FLOWVIEW FEEDER LIMITS PASSED POSITIVITY CHECKS
==============================================================================

Found 2026-09-03 while completing function documentation in
``spacr/flowview/feeder.py``. The constructor and transport validator checked
limits only with ``<= 0``. Consequently NaN passed every guard, an infinite
poll interval could fail or strand the feeder thread, a NaN byte ceiling
silently rejected every otherwise valid event, and ``stop(timeout=nan)``
reached platform thread behavior with an invalid join timeout. Fractional and
boolean byte limits were also accepted despite the integer byte contract.

VERDICT 2026-09-03: RESOLVED in ``spacr/flowview/feeder.py``. Poll and stop
durations must now be finite numeric seconds with the appropriate zero policy,
and byte ceilings must be positive non-boolean integer-index values. Fourteen
parameterized invalid cases failed when the former comparison-only guards were
restored, while the focused delivery, restart, shutdown, and producer-helper
counterparts remain green.

REVISED COUNT: 108 entries, 40 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
51. FLOWVIEW USER DOCUMENTATION OVERSTATES QUEUE OWNERSHIP ON SHUTDOWN
==============================================================================

Found 2026-09-03 while auditing ``spacr/flowview/feeder.py``. The source and
its existing focused shutdown test establish that a value returned by an
in-flight queue read after shutdown is consumed and discarded. The class/API
contract now says so, but ``docs/flowview.md`` still promises that stopping
the feeder never drains the source and therefore remains false.

VERDICT 2026-09-03: OPEN, documentation-owner handoff. The docs lane is owned
by the other active session, so this pass did not edit that protected file.
The repair is wording-only: stopping does not close or intentionally empty the
caller-owned queue, but one value returned by an in-flight read can be
consumed and discarded; the existing
``test_stop_does_not_emit_a_value_released_after_shutdown_request`` pins the
behavior the prose must match.

REVISED COUNT: 109 entries, 40 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
52. AN UNMEASURABLE RANK CORRELATION WAS REPORTED AS DISAGREEMENT
==============================================================================

Found 2026-09-03 while completing constructor documentation in
``spacr/nonparametric_fits.py``. ``agreement`` deliberately returns NaN when
fewer than three guide names are shared because a Spearman correlation is not
informative there, but ``Agreement.summary`` sent that NaN through a numeric
threshold and described the methods as ``DISAGREE``. The report therefore
turned absence of evidence into a scientific conclusion.

VERDICT 2026-09-03: RESOLVED in ``spacr/nonparametric_fits.py``. Non-finite
correlations are now reported as unavailable with fewer than three shared
guides, while finite correlations retain the established agree/disagree
threshold. The existing insufficient-guide case now pins both the NaN result
and neutral summary; restoring the threshold-only formatter makes that test
fail, while the ordinary comparison tests retain their positive counterpart.

REVISED COUNT: 110 entries, 41 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
53. NON-RUNTIME LIBRARY NAMES WERE MISCLASSIFIED AS OPENMP RUNTIMES
==============================================================================

Found 2026-09-03 while completing function documentation in
``spacr/openmp_guard.py``. Runtime detection searched for a marker anywhere in
the mapped image's basename, so a library such as ``notlibomp.so`` was counted
as OpenMP. Two unrelated lookalikes could therefore trigger an unnecessary
single-thread clamp and warning on guarded platforms.

VERDICT 2026-09-03: RESOLVED in ``spacr/openmp_guard.py``. Supported runtime
markers must now begin the basename, retaining ordinary and content-hashed
``libomp``/``libgomp`` spellings while rejecting embedded lookalikes. Restoring
substring matching makes the focused negative case fail; the two real-runtime
names are its positive counterparts.

REVISED COUNT: 111 entries, 42 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
54. A BROKEN WARNING STREAM PREVENTED THE OPENMP SAFETY CLAMP
==============================================================================

Found 2026-09-03 while auditing the duplicate-runtime guard. Its one-time
warning ran before any runtime was clamped. If output itself raised—for
example during interpreter or redirected-stream teardown—the context's broad
fallback suppressed the warning error but entered the protected model body
with both hazardous runtimes still at their original thread counts.

VERDICT 2026-09-03: RESOLVED in ``spacr/openmp_guard.py``. Warning output is
now best-effort, goes to stderr, describes the clamp as an attempt, and cannot
interrupt runtime handling. A focused broken-stream test observes both fake
runtimes clamped inside the body and restored afterwards; re-raising the
warning failure makes it fail, while the ordinary once-only warning remains
the positive counterpart.

REVISED COUNT: 112 entries, 43 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
55. RE-ENTERING ONE OPENMP CONTEXT LOST THE OUTER RESTORATION STATE
==============================================================================

Found 2026-09-03 while documenting ``single_threaded_openmp``. Reusing one
context instance in nested ``with`` blocks replaced its single restoration
list. The inner exit then restored the original maximum and cleared the list,
so the outer protected region unexpectedly regained parallel OpenMP and its
exit had no state left to restore.

VERDICT 2026-09-03: RESOLVED in ``spacr/openmp_guard.py``. Each direct entry
now pushes an independent restoration list and each exit pops only its own,
while decorated calls continue to receive independent contexts through
``_recreate_cm``. The focused nested-use test proves the inner exit remains
clamped and the outer exit returns to the original maximum; collapsing the
stack back to one restoration list makes that test fail.

REVISED COUNT: 113 entries, 44 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.

---

2026-09-03, from the other session. REGRESSION IN 5172fe0036, found by
bisect: choosing an object type on the "cells behind the dot" montage no
longer changes which mask plane the crop is cut by.

`tests/qt/test_cells_behind_the_dot_tab.py::test_the_object_type_chooses_
the_mask_plane_the_crop_is_cut_by` passed at 6a6c7b746, the commit that
introduced it, and fails today. `git bisect` over the 2,785 commits between
names 5172fe0036 ("Resolve png list object identities") as the first bad one.

THE LINE IS `df['object_type'] = effective_object_type`. When the png_list
carries only `cell_id` and the caller asks for `nucleus`, the fallback now
resolves the MODE along with the column -- so `effective_object_type` becomes
`cell`, and the frame comes back saying the crops are cells. The montage
reads that column to pick the mask plane, so both requests cut by the cell
plane and the two montages are pixel-identical. The user's choice of object
does nothing.

WHY THE INTENT WAS RIGHT AND THE EFFECT IS NOT. The commit's own comment is
sound -- "carrying nucleus labels forward as cells silently cuts a different
object" -- but the correction silently ignores the request instead, which is
the same class of failure pointing the other way. The two honest answers are
to cut by the requested plane with the labels that exist (what it did
before), or to refuse and say the png_list carries no nucleus labels.

NOT CHANGED HERE. `spacr/png_list.py` is the other session's file and the
commit is 40 minutes old, so it may still be in flight. The semantics are a
judgement call that belongs with whoever made it.

ALSO NOTE `_merged_field_paths` was switched to `effective_object_type` in
the same commit and that part looks harmless: its own docstring says every
object table names the same field and the others are tried in turn, so which
one is preferred does not decide the plane. The `object_type` COLUMN is what
does.

==============================================================================
56. A FAILED CHECKPOINT WRITE POISONED THE NEXT SUCCESSFUL UPDATE
==============================================================================

Found 2026-09-03 while completing function documentation in
``spacr/checkpoint.py``. ``mark`` and ``update`` changed the live document
before attempting the atomic write. A disk-full failure therefore raised but
left the rejected unit, metadata, and status in memory, and the next successful
write made that failed state durable.

VERDICT 2026-09-03: RESOLVED in ``spacr/checkpoint.py:293-375``. Updates now
build a copy-on-write candidate, persist it, and publish it only after the
atomic replacement succeeds; cancellation is still checked after the completed
unit is durable. Two focused failure/recovery tests turned red when publication
was deliberately moved before the write and pass with transactional ordering.

REVISED COUNT: 114 entries, 45 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
57. CHECKPOINT ACCESSORS EXPOSED NESTED LIVE STATE
==============================================================================

Found 2026-09-03 in ``spacr/checkpoint.py``. The ``completed`` and ``meta``
properties copied only their outer dictionaries, so callers could append to a
nested list or mutate a nested mapping and silently alter a later checkpoint
without calling ``mark`` or ``update``.

VERDICT 2026-09-03: RESOLVED in ``spacr/checkpoint.py:215-225``. Both public
accessors now return recursively detached copies. The focused test mutates
nested values through both accessors and checks memory and disk; replacing the
deep copies with the former shallow copies makes it fail.

REVISED COUNT: 115 entries, 46 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
58. RESUME IGNORED A DIFFERENT SAFE-UNIT BOUNDARY
==============================================================================

Found 2026-09-03 in ``spacr/checkpoint.py``. A checkpoint records whether its
completed keys represent fields, trials, or another safe unit, but resume
validated version, workflow, and signature without comparing that boundary.
Compatible settings could therefore combine progress recorded at incompatible
granularity.

VERDICT 2026-09-03: RESOLVED in ``spacr/checkpoint.py:280-285``. Resume now
names and refuses a boundary mismatch before exposing completed state. Removing
the new comparison makes the field-versus-trial regression fail, while the
same-boundary resume lifecycle remains its positive counterpart.

REVISED COUNT: 116 entries, 47 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
59. INVALID UTF-8 ESCAPED THE CHECKPOINT ERROR CONTRACT
==============================================================================

Found 2026-09-03 in ``spacr/checkpoint.py``. A corrupt JSON file was wrapped
with actionable checkpoint guidance, but invalid UTF-8 escaped as a raw
``UnicodeDecodeError`` even though both inputs are unreadable checkpoint
documents and must be preserved for diagnosis.

VERDICT 2026-09-03: RESOLVED in ``spacr/checkpoint.py:239-246``. Decode errors
now become ``CheckpointError`` with the existing recovery guidance and never
overwrite the bad bytes. Removing ``UnicodeError`` from the handler makes the
focused corrupt-byte test fail with the former raw exception.

REVISED COUNT: 117 entries, 48 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
60. STRING-NORMALISED MAPPING KEYS COULD SILENTLY COLLIDE
==============================================================================

Found 2026-09-03 in ``spacr/checkpoint.py``. ``json_safe`` converts mapping
keys to strings, so distinct keys such as integer ``1`` and string ``"1"``
overwrote one another. A workflow signature could consequently omit a material
setting and treat different configurations as resumable.

VERDICT 2026-09-03: RESOLVED in ``spacr/checkpoint.py:74-85``. Normalisation
now detects and names colliding original keys instead of discarding either.
Both insertion orders are covered; removing the collision guard makes both
focused cases fail while ordinary mixed keys retain deterministic ordering.

REVISED COUNT: 118 entries, 49 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
61. ARTIFACT SUFFIXES COULD ESCAPE THE CHECKPOINT DIRECTORY
==============================================================================

Found 2026-09-03 in ``spacr/checkpoint.py``. ``artifact_path`` appended its
caller-supplied suffix after a digest without validation. A suffix containing
path separators or traversal components could return a path outside the
artifact directory, and invalid empty, dot-only, or NUL forms were also
accepted after creating that directory.

VERDICT 2026-09-03: RESOLVED in ``spacr/checkpoint.py:386-410``. The helper
validates the suffix before creating storage, refusing separators, NUL,
emptiness, and dot-only values while retaining unusual path-safe and compound
suffixes. Removing validation makes all seven focused unsafe cases fail; the
existing ``.npy`` and ``npz`` calls remain positive counterparts.

REVISED COUNT: 119 entries, 50 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
62. SUDOKU REACH DOCUMENTATION DESCRIBED THE WRONG QUANTITY
==============================================================================

Found 2026-09-03 while completing constructor documentation in
``spacr/sudoku.py``. ``SudokuResult.reach`` was described as total propagated
anchor mass, but ``sudoku`` divides that mass by the median positive reach and
``sudoku_all`` retains each cell's maximum relative value across rounds. A
caller interpreting the field as absolute mass would compare incompatible
quantities.

VERDICT 2026-09-03: RESOLVED in ``spacr/sudoku.py:53-77``. Constructor prose
now names the normalized quantity and sequential aggregation, while focused
empty and single-measurement runs pin the runtime shapes. Replacing the
normalized contract with the former mass wording makes its focused API
assertion fail.

REVISED COUNT: 120 entries, 51 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
63. REPORT SECTION DOCUMENTATION EXCLUDED SUPPORTED PLUGIN KEYS
==============================================================================

Found 2026-09-03 while completing constructor documentation in
``spacr/report.py``. ``Section.key`` claimed every stable identifier belonged
to the built-in ``SECTION_KEYS`` collection, although registered report
plugins intentionally contribute their own identifiers and the builder accepts
them.

VERDICT 2026-09-03: RESOLVED in ``spacr/report.py:489-519``. The contract now
distinguishes built-in from registered plugin keys. The plugin insertion test
pins the supported runtime path, and replacing the contract with the former
built-in-only claim makes the focused API assertion fail.

REVISED COUNT: 121 entries, 52 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
64. FLOWVIEW COLLECTOR SHARED NESTED NODE STATE ACROSS PUBLIC BOUNDARIES
==============================================================================

Found 2026-09-03 while completing function documentation in
``spacr/flowview/collector.py``. Construction, immediate folds, queued node
events, and returned snapshots copied only outer dictionaries or retained the
same ``Node``. A caller could mutate nested parameters after any of those
operations and silently change the collector's supposedly detached state.

VERDICT 2026-09-03: RESOLVED in ``spacr/flowview/collector.py:47-241``. Node
payloads are recursively copied at every public ownership boundary, while the
private fold path uses already-owned events to avoid redundant copies. Focused
tests mutate constructor input, direct-fold input, queued input before drain,
and a returned snapshot; restoring shallow node copies makes both ownership
tests fail, while queuing the original event makes the queue case fail alone.

REVISED COUNT: 122 entries, 53 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
65. CONCURRENT FLOWVIEW DRAINS COULD APPLY EVENTS OUT OF FIFO ORDER
==============================================================================

Found 2026-09-03 in ``spacr/flowview/collector.py``. One consumer removed an
older event before waiting for the state lock, allowing another consumer to
remove and fold a newer event first. A delayed start could therefore overwrite
a later completion and leave the displayed stage running.

VERDICT 2026-09-03: RESOLVED in ``spacr/flowview/collector.py:132-160``. A
drain now owns the state lock before extracting its batch, serializing event
application without holding the queue lock during folding or blocking
producers. A deterministic overtaking test ends ``DONE`` with the fixed order;
restoring extraction-before-state ends ``RUNNING`` and makes the test fail.

REVISED COUNT: 123 entries, 54 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
66. NON-INTEGER QUEUE SIZES COULD DEFEAT FLOWVIEW'S MEMORY BOUND
==============================================================================

Found 2026-09-03 in ``spacr/flowview/collector.py``. Validation used only
``<= 0``. NaN and infinity therefore made ``Queue.full`` permanently false,
turning the documented bounded event stream into an unbounded one; fractional
capacities and booleans also acquired surprising effective sizes.

VERDICT 2026-09-03: RESOLVED in ``spacr/flowview/collector.py:59-78``. The
capacity must now satisfy Python's integer-index protocol, cannot be boolean,
and must be positive. Restoring comparison-only validation makes five of the
seven invalid cases fail, while the ordinary two-event bounded-queue test
retains the positive behavior.

REVISED COUNT: 124 entries, 55 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
67. FLOWVIEW DRAIN LIMITS ACCEPTED BOOLEANS AND NON-FINITE NUMBERS
==============================================================================

Found 2026-09-03 in ``spacr/flowview/collector.py``. The annotated integer
limit was sent through ``min`` and ``max`` without validation. Boolean limits
were silently treated as zero or one, NaN and infinity behaved as accidental
special cases, and other invalid values failed inconsistently only when queue
contents happened to reach ``range``.

VERDICT 2026-09-03: RESOLVED in ``spacr/flowview/collector.py:132-160``.
Non-``None`` limits now require a non-boolean integer index before state or
queue mutation; negative integers deliberately retain their documented zero
behavior. Removing validation makes all five invalid-input cases fail, while
the bounded-queue lifecycle retains ``None``, negative, and positive examples.

REVISED COUNT: 125 entries, 56 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
68. THUMBNAIL CACHE MAINTENANCE CLAIMED EVERY PNG IN A SHARED DIRECTORY
==============================================================================

Found 2026-09-03 while completing function documentation in
``spacr/flowview/thumbs.py``. Size accounting, eviction, and ``clear`` treated
every ``*.png`` in the cache directory as a generated thumbnail. A user image
such as ``figure.png`` could therefore consume the cache budget and be deleted
by an otherwise routine thumbnail write or cleanup.

VERDICT 2026-09-03: RESOLVED in ``spacr/flowview/thumbs.py``. Cache ownership
is now restricted to regular PNG files whose stems match the 64-character
lowercase SHA-256 namespace emitted by ``path_for``. A focused test places an
older foreign PNG beside a generated entry and proves it is neither counted,
evicted, nor cleared; restoring the broad glob makes that test fail at its
first byte-count assertion.

REVISED COUNT: 126 entries, 57 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
69. THUMBNAIL WRITES PUBLISHED PARTIAL PNG FILES
==============================================================================

Found 2026-09-03 in ``spacr/flowview/thumbs.py``. ``store`` wrote directly to
the final cache path, while ``get`` read without the instance lock. Concurrent
readers could therefore observe a truncated PNG during replacement, and a
failed write could destroy the prior complete thumbnail for the same key.

VERDICT 2026-09-03: RESOLVED in ``spacr/flowview/thumbs.py``. Encoding is
published with a same-directory temporary file and ``os.replace`` while the
instance lock orders same-cache reads. A coordinated two-instance test pauses
immediately before replacement: the independent reader decodes the complete
old PNG, the same-instance reader waits, and both see the complete new PNG
after publication. Removing atomic replacement or the read lock makes the
focused test fail.

REVISED COUNT: 127 entries, 58 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
70. REUSING A TIMER EXPOSED AND RECOMPUTED A STALE INTERVAL
==============================================================================

Found 2026-09-03 while completing function documentation in
``spacr/logging_util.py``. Entering a previously used ``Timer`` retained its
old ``elapsed_ms`` while the next block was still running, and ``__exit__``
left the start marker armed. A second exit therefore recomputed and logged the
same interval again even though no new interval had begun.

VERDICT 2026-09-03: RESOLVED in ``spacr/logging_util.py``. Entry now clears
the prior duration, while exit consumes and clears the start marker before it
calculates or logs. One focused test observes ``None`` inside the second use;
another exits the same interval twice and requires one stable duration and one
record. Restoring either stale-state assignment makes its exact test fail.

REVISED COUNT: 128 entries, 59 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
71. SOURCE-IMAGE DOCUMENTATION INVENTED AN ABSOLUTE PATH AND EMPTY PLATE
==============================================================================

Found 2026-09-03 while repairing constructor documentation in
``spacr/convert.py``. ``SourceImage.path`` was described as always absolute,
although a relative scan root produces relative paths, and ``plate`` claimed
to be empty without a plate folder even though well and flat layouts use the
source-directory basename.

VERDICT 2026-09-03: RESOLVED in ``spacr/convert.py``. The constructor contract
now describes both relative-path preservation and the layout-specific plate
key. Existing layout and explicit-layout tests exercise the stated cases, and
changing the first field back to ``:ivar:`` makes the exact repaired-record
contract fail.

REVISED COUNT: 129 entries, 60 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
72. CONVERSION MAPPING DOCUMENTATION ALSO INVENTED AN ABSOLUTE SOURCE PATH
==============================================================================

Found 2026-09-03 in ``spacr/convert.py``. ``Mapping.source`` repeated the
absolute-path claim even though it retains ``SourceImage.path`` unchanged; its
z provenance prose also blurred filename tokens, plane ordinals, and projected
ranges into one supposed index.

VERDICT 2026-09-03: RESOLVED in ``spacr/convert.py``. The constructor contract
now names the retained scan path and separately describes each supported z
provenance form. Naming and reversibility tests remain the positive runtime
counterparts, while reverting the first parameter field makes the exact
contract test fail.

REVISED COUNT: 130 entries, 61 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
73. CONVERSION RESULT DOCUMENTATION OMITTED ATTEMPTED FAILURES FROM skipped
==============================================================================

Found 2026-09-03 in ``spacr/convert.py``. ``ConversionResult.skipped`` claimed
to contain only sources skipped without an attempt. Runtime conversion errors
also append the failing source or source-series item there, in addition to the
corresponding planned mappings in ``failed``.

VERDICT 2026-09-03: RESOLVED in ``spacr/convert.py``. The contract now names
both unreadable inputs and attempted groups that failed, and distinguishes the
mapping-level ``failed`` list. Focused failure-summary tests exercise this
shape; reverting the required ``plan`` field makes the exact record contract
fail rather than letting the repaired record disappear from the audit.

REVISED COUNT: 131 entries, 62 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
74. COUNTING MARKERS ACCEPTED NONPOSITIVE AND NON-FINITE DIAMETERS
==============================================================================

Found 2026-09-03 while completing function documentation in
``spacr/counting.py``. Zero, negative, NaN, and infinite marker diameters were
accepted and passed into point-layer rendering, while a nonnumeric value
escaped as a raw ``ValueError`` rather than the module's ``LayerError`` setup
contract.

VERDICT 2026-09-03: RESOLVED in ``spacr/counting.py``. Session construction
now normalizes the value once and requires a positive finite world-unit
diameter before creating any layer. Five focused invalid cases fail on the old
source and now produce the same actionable ``LayerError``; ordinary finite
marker sizes retain their existing placement and radius tests.

REVISED COUNT: 132 entries, 63 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
75. NEGATIVE COUNTING SHORTCUT POSITIONS GENERATED INVALID KEYS
==============================================================================

Found 2026-09-03 in ``spacr/counting.py``. The documented zero-based
``shortcut_index`` accepted negative positions, turning ``-1`` into shortcut
``"0"`` and lower values into negative key strings that the numeric shortcut
row can never select.

VERDICT 2026-09-03: RESOLVED in ``spacr/counting.py``. An auto-assigned
shortcut position must now be non-negative, while an explicit shortcut keeps
the documented precedence and remains independent of the ignored index. The
negative-position test is red without the guard and the existing ``1`` through
``9`` assignment test remains its positive counterpart.

REVISED COUNT: 133 entries, 64 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
76. DUPLICATE COUNTING SHORTCUTS MADE A LATER CLASS UNREACHABLE
==============================================================================

Found 2026-09-03 in ``spacr/counting.py``. Class names were unique but their
nonblank keyboard shortcuts were not. ``class_for_shortcut`` returns the first
match, so a later class sharing that key could be displayed and counted but
never selected with the shortcut it advertised.

VERDICT 2026-09-03: RESOLVED in ``spacr/counting.py``. Class insertion now
refuses a nonblank shortcut already assigned in the session before it creates
a layer. The focused test first accepts two distinct explicit shortcuts, then
requires the duplicate to fail; removing the uniqueness check makes only that
refusal turn red.

REVISED COUNT: 134 entries, 65 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
77. ALIGNMENT TILE DOCUMENTATION CONFUSED SOURCE AND ASSEMBLED CHANNELS
==============================================================================

Found 2026-09-03 while repairing constructor documentation in
``spacr/align.py``. ``Tile.channel`` was described as the acquisition channel
that drives registration. For split-channel input it is instead the zero-based
index in the assembled sibling sequence; selecting source channel 4 from
``[2, 4]`` stores index 1.

VERDICT 2026-09-03: RESOLVED in ``spacr/align.py``. The constructor contract
now names the assembled index and separately describes the ordered absolute
sibling paths. Reference-channel scan and shared-solution tests exercise that
boundary; reverting the required ``path`` field makes the exact record test
fail.

REVISED COUNT: 135 entries, 66 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
78. REFUSED ALIGNMENT PAIRS WERE DOCUMENTED AS MEASURED WITH ZERO CONFIDENCE
==============================================================================

Found 2026-09-03 in ``spacr/align.py``. ``PairResult.dy`` and ``dx`` were
called measured values unconditionally, while refused pairs normally retain
nominal fallback displacement. The same prose claimed refusal forces zero
confidence even though a scored candidate below threshold keeps its positive
score.

VERDICT 2026-09-03: RESOLVED in ``spacr/align.py``. The contract now
distinguishes accepted measurements, refused nominal fallback, sub-threshold
scores, and cases where no candidate was scorable. Noise-refusal, no-overlap,
drift, and recovered-offset tests provide positive runtime examples; changing
``i`` back to ``:ivar:`` makes the exact contract fail.

REVISED COUNT: 136 entries, 67 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
79. ALIGNMENT RESULT DOCUMENTATION CALLED PREVIEW STATE A LEDGER WRITE
==============================================================================

Found 2026-09-03 in ``spacr/align.py``. ``AlignResult.plan`` and ``canvas``
were described as already written even for preview, dry, and empty results,
and ``status`` was labelled a ``RunLedger`` value although ``write_stack``
assigns the local outcome directly.

VERDICT 2026-09-03: RESOLVED in ``spacr/align.py``. Constructor prose now
describes planned geometry, every no-path case, and the exact empty, complete,
and partial write outcomes. Dry-run, preview, write-failure, and bounded-buffer
tests exercise those cases; reverting ``plan`` to ``:ivar:`` makes the exact
record contract fail.

REVISED COUNT: 137 entries, 68 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
80. SOURCE-SUMMARY ROWS WERE DOCUMENTED AS THE PREVIEW ROWS
==============================================================================

Found 2026-09-03 while repairing constructor documentation in
``spacr/multi_database.py``. ``SourceSummary.rows`` was described as the rows
the source contributes, which becomes false when a caller asks
``read_merged`` for a per-source preview limit: the summary records the full
table count before that limit.

VERDICT 2026-09-03: RESOLVED in ``spacr/multi_database.py``. The constructor
contract now distinguishes the inspected table count from a later preview,
and names the stored and canonical identities carried by every field.
Reverting ``path`` to ``:ivar:`` makes the exact record contract fail.

REVISED COUNT: 138 entries, 69 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
81. MERGE-DECISION DOCUMENTATION EXCLUDED A LIVE OUTCOME
==============================================================================

Found 2026-09-03 in ``spacr/multi_database.py``. ``MergeDecision.outcome``
and :func:`decision_for` claimed only ``merged`` and ``refused`` were valid,
although the established audit-log path records ``resolved`` and the function
accepts other caller-supplied labels. The timestamp was also described as the
append time even though it is created before :func:`record_decision` runs.

VERDICT 2026-09-03: RESOLVED in ``spacr/multi_database.py``. Class, field,
and function prose now agree with the actual open outcome vocabulary and name
decision construction as the timestamp boundary. Reverting ``table`` to
``:ivar:`` makes the exact record contract fail.

REVISED COUNT: 139 entries, 70 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
82. STORED SCREEN LABELS BYPASSED PLATE-ID CANONICALISATION
==============================================================================

Found 2026-09-03 while verifying ``SourceSummary.screen_plates``. The branch
that trusted a stored ``screenID`` returned the stored plate spelling, while
the explicit-screen and no-screen branches canonicalised it. Two databases
from screen ``kd`` containing ``pplate1`` and ``plate1`` therefore appeared
safe in the plan, then both became ``plate1`` when the frames were read.

VERDICT 2026-09-03: RESOLVED in ``spacr/multi_database.py``. Every
``screen_plates`` branch now uses :func:`canonical_plate_id`, so the preview
refuses exactly the identity the merged data would otherwise pool. A focused
regression drives both spellings through stored screen labels; restoring the
raw string makes that test fail, while distinct stored screens still pass.

REVISED COUNT: 140 entries, 71 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.


==============================================================================
83. REPORT RESULT DOCUMENTATION MISSTATED THREE COLLECTION BOUNDARIES
==============================================================================

Found 2026-09-03 while repairing constructor documentation in
``spacr/report.py``. ``Report.sections`` excluded registered plugin chapters,
``spacr_version`` was called the version that originally wrote the run rather
than the version collecting the report, and ``n_figures_embedded`` did not
distinguish retained raster payloads from discovered or listed figures.

VERDICT 2026-09-03: RESOLVED in ``spacr/report.py``. All nine constructor
fields now describe the collection and rendering boundaries actually used by
the report engine, including unresolved input paths and bounded artifact
scans. Plugin insertion, partial/empty reports, figure caps, and unknown
version behavior provide positive examples; reverting ``src`` to ``:ivar:``
makes the exact constructor contract fail.

REVISED COUNT: 141 entries, 72 recorded as resolved. This remains the same
prose-derived progress aid rather than an acceptance ledger described above.
