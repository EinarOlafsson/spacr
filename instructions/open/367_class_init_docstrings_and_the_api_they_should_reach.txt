================================================================================
CLASSES DOCUMENT THEMSELVES BUT NOT THEIR __init__, WHERE THE PARAMETERS ARE
================================================================================

Status:    measured 2026-09-02, implementation started.
Requested: 2026-09-02 -- "i noticed for classes in spacr many have a short
           docstring and nothing for the __init__. the __init__ is where the
           settings imports lie. so this section should have a docstring that
           explains the settings. these explanations should be available in
           the API. now users have to go to the source code to even see the
           settings and there are often no descriptions there. so for classes
           the API needs a complete docstring and API overhaul. implement
           this."
Owner:     every module defining a class; docs/source/conf.py is already
           correct and needs no change.

--------------------------------------------------------------------------------
MEASURED, 2026-09-02 -- AND THE FIRST MEASUREMENT WAS WRONG
--------------------------------------------------------------------------------

    classes with a parameterised __init__            530
      every parameter documented somewhere           145   (27%)
        ... of those, via the CLASS docstring only   101
      __init__ carrying its own docstring             59
      PARAMETERS UNDOCUMENTED                        385   (72%)

  THE FIRST COUNT SAID 495 OF 554 (10% documented) AND WAS WRONG. It asked
  "does __init__ have a docstring", which is not the question. A class may
  document its constructor's parameters on the CLASS docstring with `:param:`
  fields, and `autoapi_python_class_content = 'both'` merges the two, so the
  reader sees them either way. 101 classes do exactly that -- it is the house
  pattern, `spacr.submodules.CellposeLazyDataset` is the worked example, and
  counting them as undocumented would have sent this work to rewrite pages
  that are already correct.

  The right question is whether EVERY PARAMETER IS NAMED in a `:param:` field
  anywhere the API will show. Asked that way the answer is 385 classes, and
  those are the ones to fix. Where the class docstring is the natural home,
  put it there; do not move existing prose onto `__init__` for uniformity.

  THE WORST, all public or near-public:

      16 of 16 params   _GlumResults                   spacr/ml.py
      14 of 14          StitchedMultiAligner           spacr/spacrops.py
      13 of 13          _AbsorbedLeastSquaresResults   spacr/ml.py
       9 of  9          AmbientEngine                  spacr/qt/widgets/ambient.py
       8 of  8          BenchmarkController            spacr/qt/startup_benchmark.py
       7 of  7          _ResourceSampler               spacr/fit_resources.py
       7 of  7          _ListEditor                    spacr/qt/screens/settings_model.py

  Of the 11 constructors taking an argument literally named `settings`, 3
  were already documented and the rest are listed below.

--------------------------------------------------------------------------------
THE GOOD NEWS: THE API MACHINERY ALREADY DOES THIS
--------------------------------------------------------------------------------

`docs/source/conf.py` already sets

    autoapi_python_class_content = 'both'   # class docstring + __init__ docstring

so an `__init__` docstring is CONCATENATED onto its class's API page today.
Nothing needs configuring, nothing needs a template change, and there is no
"API overhaul" to do in the build -- the pipeline works and is waiting for
content. Writing the docstring is the whole of the work, and it appears in
the API the moment it exists.

  VERIFY THIS ON A REAL BUILD BEFORE WRITING 495 DOCSTRINGS. Document one
  constructor, build the docs, and confirm the parameter list appears on the
  class page. If `autoapi_options` lacking `special-members` turns out to
  suppress it after all, that is a one-line config change -- and much better
  discovered now than after the writing.

--------------------------------------------------------------------------------
WHAT A CONSTRUCTOR DOCSTRING HAS TO SAY
--------------------------------------------------------------------------------

The repo already uses reST field lists and has a test that parses them
(`tests/test_docstring_field_lists_are_parsed.py`), so the form is settled:

    :param settings: ...
    :param parent: ...
    :raises ValueError: ...

EVERY PARAMETER, and for a `settings` mapping that means the KEYS it reads,
not the sentence "a settings dictionary". The complaint is that users "have
to go to the source code to even see the settings", and a docstring saying
"settings: the settings" leaves them exactly where they were.

  WHERE THE KEY DESCRIPTIONS ALREADY EXIST, LINK RATHER THAN COPY.
  `spacr/settings.py` carries tooltips for settings keys. A constructor
  docstring that restates one is a second copy that goes stale -- the same
  fault instruction 362 removed from the README grid and the model zoo table.
  Name the keys and point at the settings reference; copy prose only where
  none exists.

  DO NOT DESCRIBE `self`, and do not write `:param parent: the parent` for a
  Qt widget's parent. A docstring that documents the obvious to reach 100%
  makes the page longer without making it more useful, and buries the
  parameters that matter.

--------------------------------------------------------------------------------
ORDER OF WORK
--------------------------------------------------------------------------------

  1. The 8 undocumented `settings` constructors listed above.
  2. Public classes in the core pipeline modules -- core, measure, io,
     submodules, ml, classify, sequencing -- since these are what the module
     tiles in the README link to, and instruction 366 is removing README
     prose on the promise the API says it better.
  3. Public classes elsewhere.
  4. Private (`_`-prefixed) classes LAST, and only where a maintainer would
     benefit; they are not in the public API and 366's promise does not cover
     them.

--------------------------------------------------------------------------------
TRAPS
--------------------------------------------------------------------------------

* A NEW DOCSTRING IS NEW SOURCE TEXT FOR THE TRANSLATION CATALOGS. Every
  docstring added here stales `docs/i18n/reviewed/api/**` records pinned to
  the old text and adds rows to the translation backlog. One two-word default
  change staled two reviewed records this week; 495 docstrings will not be
  free. Coordinate with 306 and 316, and expect the API catalog job to go red
  until the catalogs are rebuilt.
* `tests/test_docstring_correctness.py` and
  `tests/test_user_facing_docstrings.py` already fail at HEAD (6 failures).
  Read them before writing, so 495 docstrings are not written in a shape
  those tests reject.
* Do not let the count become the goal. 10% -> 100% is not the objective;
  a caller being able to use the class without opening the source is.

--------------------------------------------------------------------------------
HOW IT WILL BE CHECKED
--------------------------------------------------------------------------------

* The 8 settings constructors document every key they read.
* A built API page for a documented class shows its parameters.
* A test fails when a PUBLIC class's `__init__` takes parameters and
  documents none of them -- ratcheted, so the number cannot go back up.
* No constructor docstring restates a settings tooltip verbatim.

--------------------------------------------------------------------------------
2026-09-03: CHECK 3 ALREADY EXISTS. DO NOT BUILD IT AGAIN.
--------------------------------------------------------------------------------

This file's third acceptance check -- "a test fails when a PUBLIC class's
`__init__` takes parameters and documents none of them -- ratcheted, so the
number cannot go back up" -- reads as work to do. IT IS ALREADY DONE, and
better than the check asks for:

    tests/test_docstring_correctness.py
        ::test_no_new_undocumented_required_public_parameters

It ratchets EVERY undocumented required parameter across every public
callable, not just the all-or-nothing case, with an exact count, exact
per-category counts and a digest "so deleting prose, weakening a boundary, or
swapping one omission for another cannot turn this test green". Its own
inventory says `__init__` (or `__new__`) owns its class's signature, so
constructors are in scope.

  A SECOND RATCHET WAS WRITTEN AND DELETED before it was committed. It asked
  the narrower question -- how many public classes document NONE of their
  parameters -- and answered 169. That is a real number and is recorded below
  as a landmark, but a second test over the same ground is two things to keep
  in step, and they disagree first where nobody is looking.

MEASURED WHILE CHECKING, with the same rule this file defines (is every
parameter NAMED in a `:param:` field on either docstring, since autoapi
merges them):

    classes with a parameterised __init__          540
      every parameter documented                   160   (30%)
      parameters undocumented                      380   (70%)
    public classes documenting NONE of theirs      169

  So 385 -> 380 since this file was written. The 30% is up from 27%.

TWO THINGS FIXED WHILE MEASURING:

  * `FormulaPanel` (qt/widgets/formula_editor.py) documented a `frame`
    parameter its constructor does not take -- the frame arrives through
    `set_frame`. A `:param:` for an argument that cannot be passed is worse
    than none: a reader following the API would call `FormulaPanel(frame=df)`
    and get a TypeError. Replaced with the real parameter and a sentence
    about `set_frame`.
  * `EmptyState` and `GracefulQuitWatcher` now document all eleven of their
    parameters between them.

A TRAP FOR WHOEVER WRITES THE OTHER 380, and it is not in this file's list:
`tests/test_user_facing_docstrings.py` REJECTS INTERNAL PROVENANCE in public
docstrings -- instruction numbers, the word "maintainer", "asked for on
20xx-", and more. The house comment style used everywhere else in this
repository is exactly what it forbids. Docstrings here must be plain API
prose: what the parameter is, what it does, its default and its units.

  AND THE EXISTING RATCHET IS CURRENTLY RED ON DRIFT, not on this work: it
  pins exact counts (8,328 against a recorded 7,984; 9,273 against 8,861) and
  the other session has been adding docstrings all night. Those numbers want
  re-recording by whoever owns that campaign, and until they are, a genuine
  regression in that file would be invisible behind the drift.
