================================================================================
REVIEW: MODERN ATTRIBUTION METHODS AND MODEL COVERAGE
================================================================================

Status:    not started - a written answer, not code
Requested: 2026-08-06 - "look at the activation map module and let me know if there are any more modern ways to look at activation in addition to the strategies i have implemented, and if the models covered by my current implementation could be expanded"

--------------------------------------------------------------------------------
WHAT THE STATE IS
--------------------------------------------------------------------------------

Not started. The module is spacr/attribution.py plus the Activation Maps
screen; settings_model.py around line 1621 mentions Grad-CAM and SmoothGrad.

--------------------------------------------------------------------------------
WHY IT MATTERS
--------------------------------------------------------------------------------

The user asked a question and expects an answer, not a patch. Answering it
badly - a list of method names - is worse than not answering.

--------------------------------------------------------------------------------
WHAT TO DO
--------------------------------------------------------------------------------

Read spacr/attribution.py and the screen's model list, then report:
  * which methods are implemented now, and on which architectures;
  * what is worth adding, assessed against what is already there. Candidates:
    Score-CAM / Ablation-CAM (gradient-free, survive saturated gradients),
    HiResCAM and LayerCAM (better localisation than Grad-CAM on deep layers),
    Integrated Gradients and DeepLIFT/DeepSHAP (axiomatic, and they connect
    directly to the SHAP work in instruction 17), attention rollout and
    generic transformer attribution for ViT/MaxViT;
  * sanity checks, which matter more than another map: the Adebayo
    model-randomisation and data-randomisation tests catch a saliency method
    that is really an edge detector;
  * which architectures the current hooks cover and which they silently do
    not - CNN and transformer target layers are not interchangeable, and
    spaCR ships maxvit_t as a default in at least one module.
Recommend an order, do not just enumerate.

--------------------------------------------------------------------------------
HOW TO KNOW IT WORKED
--------------------------------------------------------------------------------

Not applicable - the deliverable is the written answer. Done when the user
has it and has said which parts they want built.
