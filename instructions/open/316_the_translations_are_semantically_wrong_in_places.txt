================================================================================
THE TRANSLATIONS ARE PRESENT, CURRENT, AND IN PLACES WRONG
================================================================================

Status:    OPEN, filed 2026-08-31. RELEASE-BLOCKING for any claim that
           spaCR is translated, though NOT for the release itself -- an
           English fallback is readable and a wrong translation is not.
Owner:     the nine runtime catalogs.
Requested: not asked for; found on 2026-08-31 while enumerating 306's
           caption review.
Splits from: 306, which is about the catalogs being COMPLETE and
           CURRENT. They now are. This is about them being RIGHT, which
           no gate in 306 can measure.

--------------------------------------------------------------------------------
WHAT THE STATE IS
--------------------------------------------------------------------------------

Every runtime catalog is at 4983/4983 with current source hashes. That
is the mechanical fixed point 306 was driving at, and it has been
reached.

It is not evidence of correctness. Five defects found by READING, all of
which pass every gate this repository has -- present, source-hash-
current, normal length:

  Icelandic   "1.5 er óþekkt"      = "1.5 is unknown"    (English: default)
  Icelandic   "Allir notendur"     = "all users"         (English: all apps)
  Spanish     "se ciernen para revelar" = "they loom in order to reveal"
              -- the bird-of-prey sense of "hover", conjugated to agree
              with "aplicaciones"
  Hindi       drops the hover instruction entirely
  Chinese     leaves "hover to reveal" in English

Icelandic is the worst of the nine and is the one the maintainer can
check fastest without help.

--------------------------------------------------------------------------------
THREE MECHANICAL PROXIES THAT DO NOT WORK, SO NOBODY REBUILDS THEM
--------------------------------------------------------------------------------

Tried across all 134 x 9 candidates. Every one is dominated by false
positives:

  LENGTH RATIO          flagged 64. Chinese legitimately runs at 0.36 of
                        the English length -- measured from the catalogs
                        themselves, not assumed.
  UNTRANSLATED FRAGMENT flagged 41. Nearly all are legitimate identifiers
                        that must stay English: `mean-field ADVI`,
                        `well_abundance_factor_mu`, and the settings keys.
  TRANSLATION == SOURCE flagged "FlowView", which is a product name.

The five real defects were found by reading, not by any of these. That
is the finding: a mechanical fixed point is not a correctness claim, and
the remaining criterion needs a reader per locale.

--------------------------------------------------------------------------------
WHAT TO DO
--------------------------------------------------------------------------------

  1. Fix the five above. They are known, named, and each is one string.
  2. Decide what "translated" may be claimed in the README, the paper and
     the Zenodo record. "Nine languages, machine-drafted and
     technically reviewed" is defensible today; "translated into nine
     languages" is not, until a reader per locale has passed over them.
  3. If a per-locale reader is not available, say so in the record rather
     than leaving the claim unqualified.

HOW TO KNOW IT WORKED

  * a native or fluent reader has passed over each locale's runtime
    strings and signed the evidence file for it;
  * the five named defects are gone;
  * the README's claim matches what was actually done.


================================================================================
ADDENDUM 2026-08-31 — CAPTIONS AWAITING THE MAINTAINER'S TRANSLATION PASS
================================================================================

Einar will do ONE translation pass, last, covering both the five defects
above and the strings listed here. Nothing below is machine-drafted, on
purpose: this file's own finding is that a machine-drafted translation
passes every gate this repository has while being wrong, and adding more
of them to clear a ratchet would be adding to the problem the file exists
to record.

CONSEQUENCE, STATED PLAINLY: `tests/qt/test_i18n_caption_ratchet.py` is
RED until that pass lands, and it is red ON PURPOSE. It is not rot and it
is not a broken gate. Do not "fix" it by pinning the new counts or by
filling the rows with the English text -- either would make the suite
green while leaving the UI untranslated, which is worse than the failure,
because the failure is the only thing still asking for the work.

ALREADY DONE, so the pass does not need to revisit them:

    "Import"   the module (Format Converter and External Masks fold in)
    "Tools"    the Home band

Both are single words with a settled equivalent in all nine languages.
They are in `spacr/qt/i18n.py::_ROWS`.

NEEDS NINE TRANSLATIONS EACH -- exact English source, verbatim:

1. Home's grouping caption, `spacr/qt/widgets/home.py`:

   "Primary modules are grouped here into Core, Data, Tools and Assays;
   related workflows are reached from their host module. Click any name to
   open it. Ctrl+1 through Ctrl+9 opens the first nine apps in sidebar
   order."

   WATCH: "Core", "Data", "Tools" and "Assays" are the four band names and
   MUST match whatever `_ROWS` gives those four words, or the caption will
   name bands the screen does not show. "Ctrl+1" and "Ctrl+9" are key
   names and stay as they are.

2. The performance-level caption, `spacr/qt/widgets/setup_slides.py`:

   "How much of this machine spaCR keeps between runs: processes, caches
   and GPU memory. Laptop keeps the least — for 8 GB or on battery;
   Workstation the most. The science is identical at every level, and the
   reproducibility hash records what each run used."

   WATCH: "Laptop" and "Workstation" are LEVEL NAMES shown in the control
   beside this text, not descriptions -- they must match their `_ROWS`
   entries, and both are declared deliberately untranslated in German.
   "The science is identical at every level" is the load-bearing clause:
   it is what tells a user the setting cannot change their results, and a
   translation that softens it into "similar" or "comparable" is a
   correctness bug, not a style one.

   This one was 667 CHARACTERS until 2026-08-31 and named all five levels
   with a clause each. It was cut to 253 before being offered for
   translation, because nobody reads a 667-character caption in any
   language and the length would have been paid nine times over in text
   no reviewer could check at a glance. If a caption here ever grows past
   roughly 250 characters again, shorten it before translating it.

ALSO NEEDED, in `spacr/qt/i18n_catalogs/en.py::UI_SOURCES` (English
manifest only -- these are not `_ROWS` rows):

    "Delete every journalled run on this machine. The runs' own output
    folders inside your projects are not touched."

    "The journal folders are removed permanently. Outputs written into
    your projects are not touched."

Both are from Run History's delete affordance. Both make a promise about
what is NOT deleted, so a translation that loses the distinction between
the run journal and the user's own project outputs is the dangerous kind
of wrong -- it would read as "this deletes your results".

RATCHET COUNTS after the pass: SETTING_LABELS 1018 -> 1024, UI 2695 ->
2698. Re-pin against a capture taken AFTER the translations land and
verified green before pinning, not before -- pinning a digest nobody has
seen pass is how a ratchet becomes decoration.


--------------------------------------------------------------------------------
ADDED 2026-08-31 (later) — THE NINE READMEs STILL SAY CUDA-ONLY
--------------------------------------------------------------------------------

Instruction 319 landed and the ENGLISH README now names every backend.
The nine under `docs/i18n/readme/` do not: each still describes CUDA as
the only way to get a GPU.

This is not a small wrong. A reader of the Swedish or Chinese README owns
a machine spaCR now runs 139x faster on -- the Radeon in an Intel iMac,
through Metal -- and their page tells them they need an NVIDIA card.
Measured on that machine: Cellpose on one 256x256 image, 444.5 s on the
CPU against 3.2 s on the Radeon.

DO NOT HAND-EDIT THEM. They are generated by
`tools/build_documentation_i18n.py`; an edit is overwritten by the next
run and puts unreviewed text in the repo in the meantime.

ENGLISH SOURCE to translate, from README.rst lines 76-78 and 295-298:

  "NVIDIA (CUDA), AMD (ROCm on Linux, Metal on macOS), Apple Silicon
  (Metal) and Intel Arc/Xe (XPU). spaCR picks the device for you"

  "Linux is recommended for the heaviest CUDA and ROCm work, macOS
  through Metal, which covers Apple Silicon and the AMD cards in Intel
  Macs, and Windows through CUDA or DirectML."

WATCH, and this is the whole difficulty:

  CUDA, ROCm, Metal, XPU and DirectML are PRODUCT NAMES and stay as they
  are in all nine. So do NVIDIA, AMD, Intel, Apple and Arc/Xe.

  "Metal on macOS" is the load-bearing clause and the one a translator
  will be tempted to smooth. It is not a synonym for Apple Silicon: on
  an Intel Mac, Metal is what drives the AMD card. 319's own backend
  table got this wrong -- it filed Metal under "Apple Silicon" and AMD
  under ROCm, and since ROCm has no macOS build at all, the combination
  that actually works was named nowhere. A translation that collapses
  the two tells an Intel-Mac owner their card is unsupported.

  "spaCR picks the device for you" must not become "spaCR uses the GPU".
  The resolver reports DETECTED and USABLE separately on purpose, and a
  translation that promises use where only detection happened is the
  failure 319 spends a section warning about.


--------------------------------------------------------------------------------
WHICH TESTS ARE RED WHILE THIS IS OPEN, AND WHY THAT IS CORRECT
--------------------------------------------------------------------------------

Two suites fail until the translation pass lands. Both are red ON
PURPOSE. Do not green them by filling rows with the English text or by
re-pinning a count -- that makes the suite pass while leaving the UI
untranslated, and removes the only thing still asking for the work.

  tests/qt/test_i18n_caption_ratchet.py
      three failures: the compact caption surface, the generated-catalog
      owners, and the external caption layer. New user-facing strings
      landed today -- the Home band names, nine Help labels, the Import
      rename, the hardware table headers -- and the ratchet counts them.

  tests/qt/test_the_setup_screen_speaks_the_chosen_language.py
      one failure: `test_every_slide_title_and_blurb_is_in_the_catalog`,
      for the spaCR-mode caption. It was green before 2026-08-31 with the
      667-character version; cutting that caption to 253 replaced the
      string, so the catalog entry no longer matches. The shorter text is
      the one to translate -- translating the long one would be nine
      translations of text that is not on screen.

A NOTE ON WHY THESE WERE NOT SIMPLY LEFT ALONE. The caption was cut
because 667 characters is not readable in any language and would have
been a cost paid nine times in text no reviewer could check at a glance.
Shortening it before translating is cheaper than translating it and
shortening it afterwards, which would waste the pass this file is
waiting for.

--------------------------------------------------------------------------------
2026-09-01 — THREE RENAMED LABELS NOW RENDER AS WORD SALAD
--------------------------------------------------------------------------------

The example-data buttons were renamed on the maintainer's instruction --
"change that button text to Load test data same for measure and classify
and annotate and map barcodes". `tr()` keys by SOURCE STRING, so the nine
translations of the old labels were orphaned by the rename and the
runtime fallback now produces half-translated captions:

    Load test data…      de  "Load Test Daten…"
                         es  "Load Prueba Datos…"
                         sv  "Load Test Data…"
    Fetching test data…  de  "Fetching Test Daten…"
                         es  "Fetching Prueba Datos…"
    Load test data       de  "Load Test Daten"
                         es  "Load Prueba Datos"

This is THIS FILE'S OWN FINDING happening live: a mechanical pipeline
producing text that passes every gate while being wrong. It is worse than
the five defects above, because those are at least fluent -- these are
visibly broken in the first word.

NOT FIXED HERE, AND DELIBERATELY. Three routes were considered:

  * hand-edit `spacr/qt/i18n_catalogs/<lang>.py`. Refused: 319 already
    recorded that those files are generated and a hand edit is
    overwritten by the next build.
  * machine-draft nine translations through the catalog builder. Refused:
    that is the practice this instruction exists to stop, and a
    machine-drafted "Load test data" is how the current wrong text was
    produced in the first place.
  * write nine translations by hand into the reviewed set. This is the
    only correct route and it is the maintainer's single pass, which is
    what this file is already waiting for.

WHAT TO TRANSLATE, and it is three short strings rather than prose:
"Load test data", "Load test data…" and "Fetching test data…". The old
rows exist under the previous English and can be replaced rather than
added, the way 319 replaced the setup slide's sentence, so the count this
lane ratchets on does not move.

Until then the buttons read as broken in every locale but English. That
is a visible regression against the rename, and it is recorded here
rather than quietly left for someone to find.

--------------------------------------------------------------------------------
2026-09-02 — THE FIRST HUMAN-REVIEWED TRANSLATIONS THIS ITEM HAS EVER HAD
--------------------------------------------------------------------------------

Instruction 357 put the questions to the maintainer in VS Code and he answered
them string by string. WHAT WAS REVIEWED, BY WHOM, WHEN:

    Reviewer   Einar Olafsson (the maintainer)
    Locales    ICELANDIC, SWEDISH, GERMAN -- and only these three. He was
               asked which he can read and named exactly those.
    Date       2026-09-02
    Method     shown the English and one locale at a time, approving or
               correcting each draft.

    "none detected"                     approved is, sv, de
    "detected, not used by spaCR"       approved is, sv, de
    Laptop / Extra Performance /
      Performance / Balanced /
      Workstation                       approved is, sv, de
    the performance/resource caption    approved is, sv, de
    the Home grouping caption           approved is, sv, de

ONE CORRECTION HE MADE, and it is the kind only a reader catches: German
"Workstation" was left as the English loanword in the existing catalog row.
He translated it -- ARBEITSSTATION -- and asked for the other level names to
be translated too rather than half the dropdown reading in English.

TWO CHANGES TO THE ENGLISH ITSELF came out of the review:

  * "The science is identical at every level" is now "The same spaCR code runs
    at every level". His reason: refer to the code that executes being the
    same, not to "the science". The old phrase claimed something broader than
    the software can promise.
  * A KEYBOARD KEY IS NAMED IN BOTH: "Ctrl (Strg)" in German. His instruction
    was general -- "for these types of examples use Ctrl (Strg)" -- so it is a
    CONVENTION, not one string: name the key as spaCR prints it, with the
    local keyboard's name in parentheses. Only German is affected today; a
    locale added later with its own key name follows the same rule.

THE SIX LOCALES HE DID NOT CLAIM -- es, zh_CN, pt, hi, ko, fr -- have machine
drafts with English fallback and ARE NOT RECORDED AS REVIEWED. 357 guideline 4
forbids claiming a review for a locale the reviewer did not say he could
evaluate, and this is the case it was written for.

CPU AND GPU ARE NOW EXEMPT, per his answer to 316-A. `GPU` was already in
`_RUNTIME_IDENTITY_CAPTIONS` and simultaneously required to have a translated
row, because the compact surface never subtracted that set. It does now, and
`CPU` joined it.

    tests/qt/test_i18n_caption_ratchet.py + the setup-screen file:
        6 failed at the start of the day -> 1 failed, 15 passed

THE ONE REMAINING FAILURE IS DELIBERATE.
`test_external_caption_layer_is_complete_exclusive_and_pinned` counts the
GENERATED layer, and `audit_i18n_coverage` reads 4946/5093 per locale -- 147
rows behind. Moving that pin would green a test over catalogs that are stale,
which is the mistake this item's history is made of. It stays red until the
rows exist.

--------------------------------------------------------------------------------
2026-09-03: ORPHANED REVIEW EVIDENCE IN THREE PLACES, AND A MECHANISM FOR IT
--------------------------------------------------------------------------------

The same problem turned up three times tonight, in three different files, and
it is one problem: A HUMAN REVIEW BOUND TO SOURCE TEXT THAT HAS SINCE BEEN
REWRITTEN. The review happened. The sentence it covered did not survive.

  1. `docs/i18n/reviewed/readme/2026-08-26-nightly-merge-gaps.json`
     11 of 16 records, all marked reviewed in nine locales, bound to README
     text removed in `f9f43a5ba`. RESOLVED: the maintainer chose "mark them
     retired, in place" over archiving to a second file and over deleting
     them, and each now carries `retired` and a `retired_reason`.

  2. `docs/i18n/reviewed/api/is/2026-08-14-tail-075-099.json`
     One record, `spacr.io.concatenate_and_normalize#4`, whose source block
     appears nowhere in the current docstring. This one BLOCKED THE WHOLE
     DOCS AUDIT -- `reviewed_api_block_translations` raises on the first
     stale record, so nothing downstream ran. RESOLVED the same way: the
     reader now skips a record carrying `retired`, which is the mechanism
     the maintainer approved for (1) applied to the same question.

     A second API record was merely MIS-KEYED rather than orphaned:
     `usage_bar.UsageBar#1` became `#2` when today's 350 fix added a
     paragraph above it. Re-keyed by matching the recorded hash against the
     current blocks, which is repair rather than retirement -- the review is
     still valid for text that still exists, it had just moved.

  3. `REVIEWED_README_BLOCKS` in `tools/build_documentation_i18n.py`
     MEASURED: 37 of its 47 keys are no longer in README.rst.
     `test_github_summary_has_reviewed_domain_translations` asserts every
     key is present, and has been red for some time.

     NOT RESOLVED TONIGHT, and the reason is the number. The maintainer
     approved retirement for eleven records they were shown; 37 of 47 is a
     different question, and at that scale some are likely to be NEAR
     MISSES -- the README was reworded, not rewritten, so the reviewed
     sentence may exist in a slightly changed form and want RE-BINDING
     rather than retiring. Retiring a review that could have been re-bound
     loses it for nothing.

     WHAT TO DO: sort the 37 into "the text is gone" and "the text moved or
     was lightly edited" before touching any of them. The second group is
     repair, like the UsageBar case. Only the first is a retirement, and
     only that group needs the maintainer's decision again -- at which
     point the mechanism already exists in two of the three files.

THE PATTERN WORTH NAMING: reviewed evidence is bound to source text by hash
or by position, and every ordinary edit to prose is a potential orphaning.
Nothing warns at the time -- the audit notices later, and in case (2) it
notices by refusing to run at all.
