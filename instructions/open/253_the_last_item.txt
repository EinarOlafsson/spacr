================================================================================
THE LAST ITEM: GO THROUGH THE WHOLE LIST AGAIN, THEN SHIP
================================================================================

Status:    OPEN. Requested 2026-08-25. THIS ITEM IS LAST BY CONSTRUCTION --
           it cannot be closed until every other item is.
Owner:     the whole tree.
Depends:   every other open item. At this reconciliation that is 01, 02, 03,
           05, 07, 08, 288, 304, 305, 306 and 307; the live index remains
           authoritative if more work is discovered.
Requested: 2026-08-28 — "then finish all items on the linstructions list
           including these."

--------------------------------------------------------------------------------
WHAT WAS ASKED
--------------------------------------------------------------------------------

"last item on the instruction list, go throu the finishe instructions list
when all items are marked as done and double check that all features are
implemented and working. then finish all items on the instructions list and
finish all test on all moduals so they are 100%. then show me what you have
done. do not stop untill these goals are met."

--------------------------------------------------------------------------------
1. THE SWEEP, AFTER EVERYTHING ELSE IS MARKED DONE
--------------------------------------------------------------------------------

Every item in `instructions/done/` gets checked against the code ONE MORE
TIME, once nothing is left open. Not the Status header -- the code.

WHY IT IS WORTH DOING TWICE. This list has been wrong in both directions.
Items marked open were finished; items marked done had closures resting on
files that a later deletion took away, and nothing noticed because the thing
that would have noticed was deleted in the same commit. A sweep run while
items are still landing measures a moving target, which is why this one runs
at the end.

227 of the items carry their own "HOW TO KNOW IT WORKED" section. That is
the checklist -- each criterion gets a verdict with evidence, and a
criterion that cannot be checked is reported as unchecked rather than
assumed. A false pass is worse than an admitted gap, because a false pass is
the exact thing this sweep exists to catch.

KEEP THE SLICES SMALL. A first attempt handed thirty items to one agent and
seven of eight stalled without returning anything. Ten is the working size.

--------------------------------------------------------------------------------
2. 100% TEST COVERAGE, EVERY MODULE
--------------------------------------------------------------------------------

Every module, not a headline number. The standing constraints hold:

  * no `# pragma: no cover`, ever. A defensive branch is covered by making
    the failure happen, not by excusing the line;
  * every test asserts something real. A test that imports a module and
    checks nothing raises the number and finds nothing;
  * measure on a FROZEN tree. Every measurement taken while agents were
    editing the working tree was corrupt -- one read 16.2% against a real
    92% because workers died on half-written files.

--------------------------------------------------------------------------------
3. THEN, AND ONLY THEN, SHIP
--------------------------------------------------------------------------------

"after codex and you are done with everything bump the version to 1.5.0.5
and copy nightly to main"

In this order, none of it before the two sections above are true:

  a. the version becomes 1.5.0.5;
  b. `nightly` goes to `main`;
  c. the history rewrite of item 250 -- the contributor identities and the
     working folders -- runs LAST of all, because it invalidates every
     outstanding branch and working tree, including any a concurrent
     session still holds.

CONFIRM BEFORE (b) AND (c). Both rewrite what the public sees, and (c)
cannot be undone by another commit.

--------------------------------------------------------------------------------
HOW TO KNOW IT WORKED
--------------------------------------------------------------------------------

  * `instructions/open/` holds nothing but this file, and then nothing;
  * every item in `done/` has been re-checked against the code AFTER the
    last one closed, with a verdict per criterion;
  * coverage is 100% per module on a frozen tree, with no pragma anywhere;
  * the version reads 1.5.0.5 and `main` is `nightly`;
  * the contributor list and the published tree are as item 250 requires.
