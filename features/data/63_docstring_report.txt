All paths below are relative to /mnt/firecuda2/codex/repo/spacr/ (e.g. spacr/utils.py:8831 = /mnt/firecuda2/codex/repo/spacr/spacr/utils.py:8831).

COUNT AND CAUSE

104 verified defects. By category: stale 59, params 18, returns 15, summary 12. By severity: 30 high, 62 medium, 12 low.

The dominant cause is not missing documentation, it is documentation that was correct when written and was never touched when the code under it moved. 59 of 104 make a factual claim the code contradicts. 21 of those 59 trace to six identifiable refactors that shipped without a doc pass: figure saving centralised through plot.save_figure (4), crop PNG format 2 to 3 (5), the multiclass probability head in deep_spacr (2), the dry_run preflight branch (2, counted under returns), run_journal hashing/caching/manifest (8), and the artifacts shared registry (2). The remaining 38 stale defects are individual drifts. The other 45 findings are absence rather than falsehood, and they concentrate in exactly the places where absence costs most: settings-dict entry points, functions with 15-39 parameters, and returned objects other code indexes by key.

Nothing here is Sphinx-detectable. Every docstring exists, every markup block is valid, and the build is clean. That is the point: the API page renders 59 confident false statements without a warning.

STALE (59) — each is a separate edit

Pipeline core:
spacr/utils.py:8831 generate_cytoplasm_mask — Returns promises "1 for cytoplasm, 0 for nucleus and pathogens"; body is np.where(nucleus_mask != 0, 0, cell_mask) and there is no pathogen parameter. Fix: "cell mask labels with nucleus pixels zeroed"; drop the pathogen clause and the binary claim.
spacr/io.py:5599 generate_loaders — ":param n_jobs: Default: derived from CPU count"; body gives 0 when unset, and the comment above it says the CPU-derived minimum was removed deliberately. Fix: "None means 0 workers; dataset reads stay in-process."
spacr/utils.py:4302 choose_model — 'custom' is listed as a legal model_type but raises NotImplementedError; chan_dict and channels are never read. Fix: remove 'custom' from the list (or state it raises) and delete the two dead parameters rather than documenting them.
spacr/utils.py:8957 correct_metadata_column_names — ":returns: The same DataFrame with columns renamed in place"; every branch is df = df.rename(...). Fix: ":returns: a new DataFrame; the input is unchanged." Leave canonicalize_measurement_columns (utils.py:9006) alone — it really does mutate.
spacr/io.py:3235 read_plot_model_stats — "write PDFs"; the inner helper goes through plot.save_figure, which picks the format from the user preference and rewrites the extension. Fix: "writes figures in the configured figure format."

Segmentation:
spacr/deep_spacr.py:168 apply_model_to_tar — claims non-(N,2) output is sigmoided; _probability_columns softmaxes any C>2 head. Also undocumented: predicted_label, prob_class_<i>, and the overwritten cv_predictions. Fix: state the three-way branch (1 logit sigmoid, 2 softmax[:,1], >2 softmax max + per-class columns) and list the added columns.
spacr/deep_spacr.py:75 apply_model — same drift, plus the frame is documented as path/pred only. Same fix.
spacr/deep_spacr.py:652 test_model_core — docstring implies (metrics, df); returns (metrics, probs, labels, results_df). A caller following it gets ValueError. Fix: document the 4-tuple.
spacr/deep_spacr.py:1277 train_test_model — ":returns: path to saved best model" is false under cross-validation, where the train branch returns cv_result_loc (per-fold CSV, or None). Fix: document the CV return and add cross_validation_enabled / cross_validation_folds / cv_group_by to the settings list.
spacr/diameter.py:59 module docstring — says fusion needs both signals and that "the threshold estimate stands" otherwise; _aggregate has fused = split.size > 0 and (collapsed or outnumbered), and only outnumbered checks density. In the collapsed case there is no threshold estimate to stand. Fix the prose and the duplicated inline comment at diameter.py:741-761.

Measurement:
spacr/measure.py:2675 measure_crop — ":raises ValueError" for normalize/mask/crop checks; only the src check raises, the rest warn and return None, and strict mode raises ConfigurationError (not a ValueError subclass). Fix: document warn-and-return-None, name ConfigurationError under SPACR_STRICT_ERRORS, keep ValueError for src only.
spacr/measure.py:1905 save_and_add_image_to_grid — the channel-order note describes format 2; the caller now builds planes with build_png_channels(resolve_png_channel_mapping(...)), whose default puts png_dims[0] in blue. The closing "read_crop_png converts legacy content on load" is also inverted.
spacr/crops.py:1151 to_cv2_bgr — the whole rationale paragraph is the format-2 fix, which is now the format the module calls wrong; input is already file-order RGB. Also the "2 channels padded" bullet contradicts measure.py:2595. Fix: describe it as the BGR adapter for a file-order array.
spacr/crops.py:50 module docstring — every specific claim in the "Crop PNG format" summary is out of date (current format is 3; format 2 is the broken one; legacy format 1 is read untouched). Only the sidecar sentence survives. Fix: rewrite against the table at crops.py:972-1054.
spacr/custom_features.py:1 module docstring — describes auto-discovery at measure_crop boot and one DB column per key; nothing in the package calls discover_features or call_feature, only tests. Fix requires a decision (below), not a doc edit.
spacr/measure.py:3023 process_meassure_crop_results — documents a save_figures parameter that does not exist; saving is unconditional. Fix: delete the phantom param and the word "optionally" in the summary.
spacr/measure.py:3046 generate_cellpose_train_set — ":returns: None"; ends with return ledger. Fix: ":returns: the RunLedger."
spacr/crops.py:1782 read_crop_png — "undoes the legacy channel reversal"; legacy (format 1) is exactly what is not reversed, format 2 is. Fix: name format 2.
spacr/crops.py:1464 write_crop_folder_marker — ":param fmt:" lists formats 1 and 2 and omits 3, which is the parameter's own default and the only format written. Fix: mirror stamp_crop_folder (crops.py:1524).
spacr/measure.py:3316 generate_object_dataset — 'cytoplasm' is advertised but raises with the default mask_dims, which has no cytoplasm entry. Fix: state that cytoplasm requires an explicit mask_dims index (its slice position varies with the run).

ML and stats:
spacr/ml.py:3950 generate_ml_scores — ":returns:" names a 10-tuple of paths; returns [output, plate_heatmap]. The ten names are locals. Fix: document the 2-element list and what output contains.
spacr/ml.py:4782 interpret_vision_model — ":returns: None"; ends with return merged_df. The See Also makes it worse by pointing at the other entry point as the one "returning result DataFrames".
spacr/ml.py:350 prepare_formula — "using random effects for plate, row, and column"; the default branch puts rowID/columnID in as fixed effects and plate appears in neither branch (it is fit_mixed_model's groups/re_formula).
spacr/ml.py:3649 clean_controls — ":param values: Value or list"; the body is wrapped in isinstance(values, list), so a scalar is a silent no-op. Note the stale comment at ml.py:2962 repeats the false claim at the only production call site. Decision needed (below).
spacr/ml.py:2177 save_summary_to_file — "CSV or text file"; only ever writes as_text(), and the default file_path='summary.csv' ships plain text under a .csv name. Fix: either say "plain-text summary" or switch to Summary.as_csv().
spacr/ml.py:3673 process_scores — "by count for Poisson"; the branch is grouped.sum() and also covers 'horseshoe'. The function's own :param regression_type: two paragraphs down is correct.

Sequencing:
spacr/sequencing.py:130 save_qc_df_to_csv — "summing element-wise"; index=False on write plus qc_df.add on a mismatched index appends a row per chunk instead of accumulating. Reproduced on pandas 2.3.3. This is a code bug, not a doc bug (below).
spacr/sequencing.py:212 process_chunk — attribution inverted: columnID/rowID are the shipped, mandated, help-documented names; column/row appear in no shipped default. A reader following this writes a regex settings.py calls wrong.
spacr/counting.py:329 CountingSession.describe — the single example is wrong; every class gets a percentage and a total is appended ("infected 12 (40%) · uninfected 18 (60%) · 30 total"). The empty-session 'nothing counted yet' branch is also undocumented.
spacr/sequencing.py:919 barecodes_reverse_complement — "<csv_file>_RC.csv"; the extension is stripped first, so rows.csv gives rows_RC.csv.
spacr/counting.py:75 CountClass — ":param shortcut: '1'-'9' by position when the caller does not choose"; CountClass never assigns one (a direct construction gets ''). The numbering is CountingSession.add_class:162-167. Fix: move the sentence.

Plotting:
spacr/plot.py:4851 plot_region — "writes three PDFs"; all three go through save_figure_as_pdf, which is a renamed wrapper over save_figure.
spacr/report.py:1719 collect_report — ":returns: a Report with one section per SECTION_KEYS"; plugin sections are inserted and their keys are required to be new. Same stale phrasing on the Report dataclass at report.py:495.
spacr/plot.py:3887 spacrGraph.create_plot — "Create and display"; it never calls show(). Every caller does create_plot() then get_figure() then plt.show(). The ax parameter is undocumented too.
spacr/plot.py:3487 spacrGraph — the plot-type list omits 'line', 'line_std' and 'jitter_bar', all of which have handlers and dedicated guards throughout create_plot.
spacr/plot.py:2520 plot_plates — ":param min_max:" documents 'auto'; generate_plate_heatmap dispatches on 'all'. 'auto' falls through to the untested else branch. Everything else in the package (settings_spec.py:143, settings.py:2799) says 'all'.
spacr/toxo.py:36 custom_volcano_plot — ":param save_path: save the figure as a PDF"; save_figure rewrites the extension to the preference. Same wording at toxo.py:437 and toxo.py:528.

Timelapse and ops:
spacr/timelapse.py:1850 analyze_calcium_oscillations — ":returns: per-peak and per-well summary"; returns (result_df, peak_details_df, fig). The per-well summaries are only written to CSV. Three early paths return bare None and are undocumented.
spacr/timelapse.py:7661 automated_motility_assay — ":returns: None"; last statement is return all_df.
spacr/timelapse.py:1842 analyze_calcium_oscillations — ":param fluctuation_threshold: minimum normalised fluctuation"; it is a maximum allowed coefficient of variation of the size column (size_diff <= threshold), not a minimum on the intensity trace. Both direction and subject are wrong.
spacr/timelapse.py:1631 save_figure — "figure_<n>.pdf"; the name is a proposal handed to plot.save_figure, which rewrites the extension.
spacr/timelapse.py:1844 analyze_calcium_oscillations — ":param peak_height: minimum peak prominence"; passed as find_peaks(height=...). Different scale entirely.
spacr/spacrops.py:3495 align_image_to_stitch — ":param qc_outlines: emit QC outline overlays"; the body never reads it. ops_preprocess defaults it to True and forwards it into nothing. Code regression (below).
spacr/spacrops.py:3635 ops_preprocess — the returned npy_out_root is an empty directory; the .npy stacks go to each well's crops_20x. Fix the return or the doc, not both.
spacr/align.py:1869 write_stack — "peak_buffer_bytes is the measured allocation ceiling, not an estimate"; it is rows * _band_bytes_per_row computed before any allocation, and is returned unchanged on the dry_run path where nothing is allocated. Fix: call it a projection.

Infrastructure:
spacr/run_journal.py:1178 journal_totals — still promises "cheap enough to call on Home-screen construction"; the comment nine lines below retracts that sentence by name (671 ms at 3521 runs). The function is now incremental and writes .journal_totals.json. The advice "callers that need more should cache" describes caching the function now does.
spacr/run_journal.py:1 module docstring — four false claims: capture_stdout does not exist anywhere in the package, stdout.txt is never written, no FAILED marker file is written (failure is status:"failed" in manifest.json), and open_run returns nothing from __exit__. The illustrated folder name also omits the __<app> suffix _new_run_dir appends. Delete the vestigial Run.stdout_path field (run_journal.py:500) with it.
spacr/run_journal.py:531 Run.record_input — documents hashing unconditionally; the first line is a no-op unless settings['hash_inputs'] is set, which is off by default. Neither this nor the Run class docstring mentions the switch.
spacr/run_journal.py:475 Run — ":ivar model_hashes: {human-name → sha256-16}"; record_model stores "filename:digest". journal_totals' own comment calls this out after a bug that came from assuming the documented shape.
spacr/run_journal.py:546 Run.record_output — same hash_inputs no-op, plus the setting_key keyword is undocumented. record_input directly above it documents both correctly.
spacr/run_journal.py:511 Run.record_model — "silently no-ops"; it appends to provenance_warnings (which lands in manifest.json) and LOG.warnings, and hash_file logs too.
spacr/run_journal.py:893 recent_runs — "corrupt / partial run folders are silently skipped"; the code logs, and its comment insists on logging. Also undocumented: only the newest max(limit*4, limit+64) folders by name are examined.
spacr/run_journal.py:1169 journal_totals — models_recorded is documented as "across all mask runs"; model_hashes are collected from every run folder regardless of app_key. The repo's own test counts a classify run's model.
spacr/database_schema.py:91 MigrationReport.changed — "whether at least one schema transition was applied"; the property is a disjunction that is also True for rename-only repairs, which is exactly what ensure_database_schema builds from repair_legacy_columns with applied empty.

CLI and plugins:
spacr/cli.py:111 Module (defaults_entry) — "two pipelines keep their own"; six built-ins set defaults_entry (foreign, external_masks, convert, illumination, barcode_qc, anndata_export) plus every plugin app, and four of the helper names differ from the pattern the docstring implies. module_defaults (cli.py:738) already lists all six correctly.
spacr/cli.py:107 Module (call_style) — "'folder' for the one entry point that takes a bare path"; no built-in sets it any more (the convert repoint removed the last one). Fix: say the style now arrives only via plugin AppContributions.
spacr/artifacts.py:1033 latest — ":param project: the project root"; project selects the registry file and is never forwarded as a filter, so under SPACR_ARTIFACTS_DB this returns another project's artifact. Code bug (below).
spacr/artifacts.py:1002 by_kind — identical defect, identical fix.
spacr/artifacts.py:357 content_fingerprint — "every file's relative path, size and mtime"; the walk uses followlinks=False and skips symlinked files, so a link-staged folder (which spacrops.py:3536 creates routinely) fingerprints as empty and never registers as changed. data_manager.py:472 already has the sentence this one is missing.

THE REMAINING 45, BY CAUSE

Cause 1 — the dry_run preflight branch was added to both pipeline entry points and neither :returns: nor the settings-key list was updated (2). Fix once: add dry_run to the enumerated keys and state that the branch returns List[Problem] from validate.run_preflight and writes nothing.
spacr/core.py:65 preprocess_generate_masks; spacr/measure.py:2643 measure_crop.

Cause 2 — functions with no :param: entries at all, where the parameters are mode switches and enumerated strings the caller cannot guess (8). Fix once: document the behaviour-changing knobs, not all of them — the string-enum parameters whose legal values exist only in an error message or an if/elif chain (optimizer_type, schedule, loss_type, mode, detector, outline_source, point_color, blend), the boolean pairs that are easily confused (all_on_all vs all_outlines, plot_by_cluster vs plot_outlines), and any parameter whose type is not what its name implies (resnet takes a constructor, not a model; interactive_payload is a Qt bridge payload; filter_dict is a nested {name: (area_tuple, intensity_tuple)}). Group the rest in one sentence.
spacr/utils.py:7012 plot_embedding (21 params, 16 required, 0 documented); spacr/plot.py:208 plot_image_mask_overlay (15, plus an undocumented Figure return and an assumed .npy plane layout); spacr/deep_spacr.py:2160 train_model (39, of which 4 are documented as a "New parameters:" changelog, plus an undocumented (model, path) return with a (None, None) failure sentinel); spacr/spacrops.py:108 spacrStitcher (33 constructor params, 10 mentioned in prose, 4 with ValueError-enforced vocabularies); spacr/spacrops.py:1392 build_multichannel_mosaic_from_manifest (7, plus the returned out_tif path); spacr/plot.py:562 plot_image_mask_overlay_magenta_outlines (14, and a byte-identical summary to the function above it on the same page — see Cause 6); spacr/plot.py:3734 spacrGraph.perform_statistical_tests (is_normal selects the entire test family; the Comparison label names only the first two of N groups); spacr/utils.py:7946 extract_features.

Cause 3 — a single settings dict is the whole parameter contract and none of its keys are documented (5). Fix once: enumerate the keys the body reads, mark the ones indexed unconditionally as required, name the canonicalizer, and add :raises: for the ConfigurationError paths.
spacr/io.py:5736 generate_training_dataset (16 keys, 3 mandatory, 3 selection modes; the docstring's space goes to an internal "Required helpers:" note instead — delete that); spacr/external_masks.py:522 run_external_masks (the full Measure contract plus 10 distinct ConfigurationError raises and an undocumented ExternalMaskResult return); spacr/ml.py:520 minimum_cell_simulation (6 params, 0 documented; see also Cause 5); spacr/sequencing.py:786 generate_barecode_mapping (the list is otherwise complete but omits barcode_qc and target_grnas_per_well, which gate an entire extra stage and appear in no settings default or help text either — this docstring is the only place they could be learned); spacr/batch_correction.py:823 correction_kwargs (3 params and 6 emitted keys undocumented, and the deliberate omission of batch_covariate_column / batch_combat_mean_only is recorded only in call-site comments — hyperparam.py:3178 is the caller that got it wrong).

Cause 4 — the parameter list stops where the docstring was last edited; knobs added since are undocumented (3). Fix once: append the missing entries; where the documented parameter is dead, delete it instead of keeping it.
spacr/utils.py:7108 plot_clusters (point_color, point_alpha, outline_width all drive rendering and are missing, while the one documented parameter, verbose, is correctly flagged unused — delete it); spacr/counting.py:147 CountingSession.add_class (shortcut_index is public, keyword-only, 0-based while the key is 1-based, and silently ignored past index 9); spacr/doctor.py:1747 run_checks (checks replaces the global registry, and the summary "Run every check" asserts the opposite).

Cause 5 — the returned object is the function's whole point and is undocumented or under-specified (11). Fix once: add a :returns: naming the container, its keys or columns, and any invariant another function depends on.
spacr/plot.py:3313 create_grouped_plot is the worst of these — it documents ":returns: None" and returns (Figure, results_df), so the statistics frame is invisible to anyone reading the page. Then: spacr/utils.py:7995 perform_statistical_tests (2-tuple with different columns per frame, and the one-feature-lands-in-exactly-one-frame invariant that combine_results' validate='one_to_one' merge depends on); spacr/external_masks.py:687 prepare_external_masks (returns ExternalMaskPlan or ExternalMaskResult depending on preview_only, annotated Any, and prints both summaries to stdout); spacr/feature_dict.py:3199 describe_database (documents 12 columns, returns 18); spacr/ml.py:395 check_and_clean_data (drops everything outside a whitelist, manufactures the gene_fraction column the regression formula regresses on, and raises ValueError on duplicate fractions); spacr/ml.py:520 minimum_cell_simulation (returns the elbow sample size that becomes settings['min_cell_count'], and writes results/cell_min_threshold.pdf next to settings['count_data'][0]); spacr/ml.py:895 check_distribution (returns one of 'logit'/'quasi_binomial'/'beta'/'ols'/'glm', which regression() feeds straight into regression_type, and epsilon is the boundary that picks quasi_binomial vs beta); spacr/classifier_evaluation.py:805 evaluate_predictions (the six-key bundle write_evaluation_bundle indexes by name — note spacr/confusion.py:41 currently documents the predictions frame on its behalf); spacr/counting.py:367 CountingSession.summary (the enumeration is meant to be exhaustive and omits the total column, which reaches disk via to_csv); spacr/plot.py:3573 spacrGraph.preprocess_data (returns a new aggregated frame with a different shape and an ordered Categorical grouping column; the summary recites the steps instead); spacr/run_journal.py:315 extract_seeds (the dict lands in manifest.json, and on failure numpy_random_state_sha256 / torch_initial_seed are replaced by *_error keys, so a consumer gets a KeyError).

Cause 6 — the summary restates the function name and says nothing (5). Fix once: one sentence stating what happens, in verb form, replacing the noun phrase.
spacr/utils.py:7969 random_forest_feature_importance ("Random Forest feature importance." — features are z-scaled and a 100-tree classifier is fitted against cluster_col); spacr/ml.py:334 select_glm_family (a reader on the same API page cannot tell it from the fully documented pick_glm_family_and_link at ml.py:1005, which does the same job with different rules); spacr/plot.py:2724 normalize_and_visualize ("Utility function for visualization" — it shows original and normalised side by side and averages multi-channel input); spacr/external_masks.py:689 prepare_external_masks (names its callers instead of its effect: it plans, prints, and unless preview_only writes the whole project and runs Measure); spacr/deep_spacr.py:725 test_model_performance ("Wrapper kept for API compatibility with your caller" — a note to the author, addressed to nobody, that never says it evaluates a model on one loader).

Cause 7 — the summary states something specific that the code does differently (4). Fix once: correct the sentence; these are one-line edits with disproportionate value because autosummary shows only the first line.
spacr/deep_spacr.py:3739 annotate_filter_vision (order is backwards and the step is conditional: it annotates, filters, writes, then strips training images only when remove_train is set; filter_column, upper_threshold, lower_threshold and remove_train are all undocumented and indexed directly); spacr/deep_spacr.py:2979 visualize_classes ("one image per class" over a hardcoded range(2) — a 3-class model silently loses classes, a 1-name class_names raises IndexError, and the limitation is disclosed only in a parenthesis on another parameter); spacr/run_journal.py:805 current_run ("open in this process" over threading.local — the docstring's own last paragraph contradicts the summary, and a worker thread silently gets None); spacr/run_journal.py:206 hash_file ("a SHA-256 digest" — the default returns the first 16 hex characters, disclosed only under :param full:).

Cause 8 — the documented input-frame contract is not the frame the function can consume (2, both timelapse). Fix once: name the columns actually indexed.
spacr/timelapse.py:1797 summarize_per_well_inf_non_inf (needs a column literally named infected; the docstring says "pathogen counts" and the adjacent comment says pathogen_count, so following either raises KeyError — and ":returns: two rows per well" is overstated, since groupby emits one row for a single-status well); spacr/timelapse.py:1593 infected_vs_noninfected (documented as taking plate/row/column/field identifiers; the body indexes only the pre-composed plate_row_column_field_object, time, and delta_<measurement>, so the documented frame KeyErrors on line one).

Cause 9 — the three public plugin SDK contribution records are undocumented, and two of them describe the callable they point at rather than themselves (3). Fix once: document the fields, and say "record referencing" not "a callable".
spacr/plugins.py:61 AppContribution (20 fields, 0 documented, 4 with ValueError-enforced vocabularies — section core/data/models/results/toxo, stage alpha/beta/stable, kind assay/importer/analysis/utility, call_style settings/folder — none of which appear anywhere under docs/; the internal spacr.cli.Module documents its equivalents); spacr/plugins.py:87 ModelProviderContribution (a frozen dataclass holding a "module:callable" reference string, described as "a callable"); spacr/plugins.py:95 ReportSectionContribution (same mis-subject, and after is the one field an author must decide, whose legal values are the eight core section keys built in report.py:1744 and whose unknown values silently append instead of raising).

Two singletons, not patterns:
spacr/plugins.py:387 discover_plugins — "every valid discovered plugin" returns an empty tuple when SPACR_DISABLE_PLUGINS is 1/true/yes/on. The kill switch appears in no docstring or doc page, and diagnostics() is empty too, so the one scenario a user consults this in — plugins vanished — is the one the docstring misleads on.
spacr/cli.py:1701 main — the exit-code enumeration is presented as exact (the module docstring says so) but cmd_run passes a module's own SystemExit code through verbatim, so any integer reaches the shell. A scheduler branching on status is exactly who reads this.

NOT WORTH FIXING AS DOCUMENTATION

Six of these are code defects that a docstring edit would cement. Editing the prose to match the bug makes the bug permanent and removes the only remaining evidence of intent. Move them to the FIXES list instead:
spacr/sequencing.py:130 save_qc_df_to_csv — the docstring describes what the function is for; the accumulation is simply broken (index=False write plus .add on a mismatched index appends a row per chunk). Fix the code.
spacr/artifacts.py:1033 latest and spacr/artifacts.py:1002 by_kind — one-line code fix (kwargs.setdefault("project", project), as the sibling register already does). Under SPACR_ARTIFACTS_DB these silently return another project's artifacts; anndata_export/__init__.py:1547 would record a foreign input edge.
spacr/spacrops.py:3495 align_image_to_stitch — qc_outlines was dropped during the run() signature refactor (the shipped docs/_modules HTML still shows it being passed). ops_preprocess defaults it to True, so the default configuration silently produces no overlays. Restore the argument.
spacr/ml.py:520 minimum_cell_simulation — tolerance=0.02 in the signature is never read; the sole caller passes tolerance=settings['tolerance'] and it is discarded. Delete the parameter or wire it.
spacr/ml.py:3649 clean_controls — a scalar is silently ignored, and tests/test_cov_ml_reads_scores.py:227 pins that as intended. Decide once: normalize values the way the sibling column parameter is normalized, or narrow the doc and drop the "Value or" wording. Do not do both halves in different sessions.

Do not document dead parameters — delete them. utils.py:4302 chan_dict and channels, utils.py:7108 verbose, ml.py:520 tolerance, spacrops.py:3495 qc_outlines. A documented knob that does nothing is worse than an undocumented one, because it invites callers to set it.

Do not hand-write 39 parameter entries for deep_spacr.py:2160 train_model or 33 for spacrops.py:108 spacrStitcher. The value is concentrated in maybe eight parameters each (the string enums whose legal set exists only inside a raise, and the pairs that are easy to confuse); the remaining twenty-odd are self-describing floats and booleans and can be covered in a sentence. Full coverage there costs hours and buys nothing on the API page.

spacr/custom_features.py:1 is not a documentation task at all. The module documents an extension mechanism that is not connected to anything — measure.py never imports it, and ~/.spacr/features appears nowhere else in the package. Either wire discover_features into measure_crop or mark the module experimental and say so in one line. Writing accurate prose about an unreachable feature is the worst of the three options.

Finally, do not add :param:/:returns: to private helpers or expand internal notes. The two internal notes found here (io.py:5736's "Required helpers:" block and diameter.py:741-761's duplicated fusion comment) should be deleted, not corrected — one occupies the space the parameter contract needs, the other duplicates a claim that is already wrong once.