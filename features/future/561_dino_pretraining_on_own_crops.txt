SELF-SUPERVISED PRETRAINING (DINO) ON THE USER'S OWN UNLABELLED CROPS

Status: FILED 2026-09-26. Not started.
Owner: unassigned. Proposed from the 2026-09-26 deep-learning/GPU list.

REQUEST (the maintainer, 2026-09-26, verbatim)
    "Add to future features list: B1,2,3,4,(in its own env like cellpose 3),
    B5 (if this isn't the same as magnifier),B7(if this is connecting
    objects over frames), B8,B9,B10,B11B12,B13,B14,B15,B16,B17,B19,B20"

EVIDENCE
    https://www.biorxiv.org/content/10.1101/2023.04.28.538691

WHAT TO BUILD
    Pretrain a ViT with DINO on a screen's own single-cell crops (no labels)
    to get a screen-specific feature extractor, then use it for embeddings,
    UMAP and fine-tuned classifiers. GPU training through gpu_turn.sh,
    resumable.

DONE WHEN
    On a labelled subset, the self-supervised features beat ImageNet
    features for kNN classification of phenotypes.
