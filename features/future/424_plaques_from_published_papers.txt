================================================================================
PLAQUE ASSAYS FROM PUBLISHED PAPERS: DOI OR PDF IN, MEASURED PLAQUES OUT
================================================================================

Filed as 415 on 2026-09-18; renumbered 2026-09-19 because 415 also names features/new/415_the_startup_window_text_is_bright_in_dark_mode.txt.

Status:    NOT STARTED AS A MODULE, BUT ITS FIRST OPEN QUESTION IS NOW
           ANSWERED, AND THE ANSWER IS YES.
           2026-09-19: the transfer spike was built and run -- 28 open-access
           CC-BY papers, 182 figures, 369 plaque-image regions labelled by eye
           -- and then found in review to have detected every figure with RED
           AND BLUE SWAPPED, so its verdict of PARTIAL was withdrawn.
           2026-09-20: THE RERUN THAT NOTE ASKED FOR IS DONE. The corpus was
           refetched and all 182 figures are byte-identical to the committed
           record; both detectors hash-identical; detect rerun through the
           fixed harness, and the box totals reproduce exactly the BGR totals
           the review predicted (371 for v1, 148 for v2). Every figure on
           which the CURRENT zoo detector drew a box was then looked at -- all
           17 region-bearing ones, the 7 it did not box, and, after review
           caught that they had only been counted, the 4 no-region figures it
           boxed anyway.
           THE CURRENT ZOO DETECTOR (v2, yolo_welldetect_v4.pt) TRANSFERS.
           Cropped plaque panels, which the withdrawn verdict called invisible
           at 6 of 110 and 0 of 61 in Toxoplasma, are found 63 of 110 and
           57 of 61. Wells 61 of 259, or 61 of 67 once the one 96-well plate
           photograph printed at ~30 px a well is set aside. Precision 0.838:
           ZERO false boxes on the 24 region-bearing figures, which carry
           gels, blots, IF panels, FACS scatters and structures; 15 on the
           other 158, and those 15 are NOT chart furniture -- 10 are Listeria
           motility agar plates, 2 whole electron micrographs, 2 brightfield
           monolayers and 1 a box inside a schematic flowchart.
           THE OLD DETECTOR (v1) IS THE ONE THAT DOES NOT TRANSFER to crops
           (at most 6 of 110, a bound from its box counts and not a judged
           recall), so the withdrawn note's "prefer v3 for a cross-organism
           pipeline" is reversed as well; 437 still must not be changed on it.
           WHAT IT MISSES AT 640 IS ONE NAMEABLE FORM: phage dilution-spot
           lawn strips, 43 of the 47 crops it missed.
           2026-09-20, STEP 2: IMGSZ 640 vs 1280 over the same corpus. The
           larger input MOVES the failure rather than removing it -- the three
           lawn-strip figures go from 0 boxes to 29, 14 and 11, on the spots,
           while three Toxoplasma crop figures lose faint whole panels (4/4 ->
           2/4 twice) and false boxes go 15 -> 12 with a new one on a cartoon
           virion. One inference size is not right for both forms.
           Harness: tools/measure_plaque_detector_transfer.py, which now keeps
           ultralytics out of the user's config and records which copy of
           spacr it measured.
           Evidence: features/data/424_detector_transfer_rerun_2026-09-20.json
           and features/data/424_imgsz_sweep_2026-09-20.json (the 2026-09-19
           file is kept; its box data stays void).
           NEXT: the module itself; nothing below it has been built. The
           queued training round is STILL NOT JUSTIFIED -- it was written to
           cure a blindness that turns out not to exist, and imgsz reaches
           much of it for free. 445, which was the same swap in the shipped
           plate-splitting path, LANDED on 2026-09-20 -- the conversion now
           lives inside spacr.plaque.detect_wells, which takes RGB -- so the
           sections below saying it is unfixed are history. This note's
           numbers were reproduced on that code, unchanged, after rebasing.

Shape:     The plaque module takes a DOI, a PMID or a PDF. It finds the figures,
           finds the plaque images inside them with the trained YOLO detector,
           segments and measures the plaques, works out WHAT CONDITION each
           plaque image shows, and writes the lot to the database.

--------------------------------------------------------------------------------
THE PIPELINE
--------------------------------------------------------------------------------

  1. INPUT: a DOI, a PMID, or a PDF the user already has.

  2. FETCH. Given a DOI or PMID, search the European database -- the same one
     that was scraped to build the plaque model's training set, so the
     retrieval path already exists in this project and should be reused rather
     than written again.

  3. FIND THE PLAQUE IMAGES. Run the trained YOLO detector over the downloaded
     files. It was trained to locate wells in plate images; here it is being
     asked to find plaque images inside FIGURES, which is a related but not
     identical problem -- see the open questions.

  4. MEASURE. Segment and measure the plaques in each detected image, and save
     to the database, the same measurements the plaque module already produces
     for images from a microscope.

  5. ATTRIBUTE THE CONDITION. Below. This is the part that makes the output
     worth anything: a plaque area with no condition attached is a number
     nobody can use.

--------------------------------------------------------------------------------
ATTRIBUTING THE CONDITION -- TWO STRATEGIES, ALWAYS BOTH
--------------------------------------------------------------------------------

Both are ALWAYS applied, not one as a fallback for the other failing. Two
independent readings that agree is evidence; one reading is a guess.

  STRATEGY 1 -- TEXT AROUND THE IMAGE.
      Detect the text immediately around each plaque image -- the label above,
      beside or beneath it -- and annotate the image with it. This is the
      direct route: most figures label the panel with the condition.

  STRATEGY 2 -- PANEL LAYOUT PLUS FIGURE LEGEND.
      Detect which panel each plaque image sits in, and where each image sits
      RELATIVE TO THE OTHERS. Then read the figure legend and attribute each
      image from the panel letter, its position, and what the legend says about
      that panel. This is how a human reads a figure whose panels are labelled
      only A, B, C.

  WHEN BOTH FAIL.
      Annotate each plaque by the DOCUMENT it came from and its position
      relative to the other plaques in that document. That is weak, and it
      must be recorded AS weak: a row whose condition came from this route must
      be distinguishable in the database from one that was read off a label,
      or the whole dataset inherits the uncertainty of its worst rows.

--------------------------------------------------------------------------------
NOTE -- THE MODELS THIS DEPENDS ON ARE ABOUT TO CHANGE
--------------------------------------------------------------------------------

Many more images have been curated for a new training round of BOTH the YOLO
detector and the plaque segmentation model, and the plaque model is to move to
a cpsam_v2 backbone. That is expected in the near future.

So this module should not hard-code a checkpoint. It should ask the model zoo
for the current plaque detector and the current plaque segmenter, so the
retrain lands here without a code change -- and so a result can record WHICH
model version produced it, which matters more here than usual, because these
measurements come from other people's published figures.

--------------------------------------------------------------------------------
OPEN QUESTIONS -- ANSWER BEFORE BUILDING
--------------------------------------------------------------------------------

  - The YOLO detector finds WELLS in plate photographs. Finding plaque images
    inside a published figure is a different distribution -- cropped, rescaled,
    JPEG-compressed, often greyscale. Does it transfer, or does it need its own
    training round? Measure this before building on it.
  - Scale. A plaque area in pixels is meaningless across papers with different
    magnifications and no scale bar. Is a scale bar detected, is a well
    diameter used as the ruler the way the plate pipeline does, or are only
    RATIOS within one figure reported?
  - Licensing and redistribution: which of these papers may have their figures
    downloaded, stored and republished as crops in a database?
  - What happens when the two strategies DISAGREE? Recording both with a
    conflict flag is more useful than silently preferring one.
  - PDF text extraction is unreliable on scanned and vector-mixed figures. Is
    OCR in scope, and which engine?
  - Deduplication: the same figure appears in a preprint and the published
    version, and the same plaque should not be counted twice.

--------------------------------------------------------------------------------
WHY IT IS FILED RATHER THAN BUILT
--------------------------------------------------------------------------------

Requested 2026-09-18 with the instruction to add it to the list and not
implement it.

--------------------------------------------------------------------------------
Decided: 2026-09-19, by the maintainer, through the question prompt
--------------------------------------------------------------------------------

  "Go as recommended"

The recommendation accepted: FIRST measure whether the plate-photo YOLO
detector transfers to figure crops at all, and build on it only if it does;
scale reported as ratios within a figure unless a scale bar or a well is
detected, saying which; derived measurements plus DOI and figure reference
stored for every paper, image crops only for open-access CC-BY papers; when
the two condition strategies disagree, record both with a conflict flag; PDF
text layer only in v1, no OCR; deduplicate by DOI plus image hash.

This note answers the first clause and nothing else. Scale, conditions, the
database and the OCR question are untouched.

--------------------------------------------------------------------------------
2026-09-19 — DOES IT TRANSFER? THE ANSWER IS WITHDRAWN: THE INSTRUMENT WAS WRONG
--------------------------------------------------------------------------------

READ THIS BEFORE THE TABLE

  This note was written to report PARTIAL -- round wells transfer, cropped
  panels do not. Review found the measurement itself broken, and the verdict
  is withdrawn. What follows is kept, with the numbers exactly as they were
  taken, because it is the record of a run that happened and because the
  corpus and the region counts in it are still good. It is not an answer to
  424's first question.

  WHAT WAS WRONG. Ultralytics decodes a file path with OpenCV, so it trains
  and infers in BGR; handed a numpy array it assumes the caller has already
  done the same. `_load_image` builds an RGB array because the overlays are
  drawn with Pillow, and that array went straight to
  `spacr.plaque.detect_wells`. Every one of the 182 figures was therefore
  shown to the detector with red and blue exchanged.

  HOW IT WAS ESTABLISHED, not inferred: over all 182 figures, passing the
  FILE PATH (ultralytics' own canonical input, and how every training image
  reached these models) and passing a BGR array give identical box counts --
  182 of 182 agree -- while RGB differs. Re-running the same
  `detect_wells` call over the same 182 files both ways reproduces the
  committed record exactly on the RGB side, which is what identifies the
  committed record as the RGB one:

    model, 640 px, conf 0.25     boxes on        boxes on       boxes on the
                                 the 14 figures  the 10 figures 158 figures
                                 holding wells   holding crops  holding neither

    v1  RGB  (what was measured)      136              17              23
    v1  BGR  (what it should be)      333              16              22
    v2  RGB  (what was measured)       13              15              13
    v2  BGR  (what it should be)       61              72              15

  v1's RGB row sums to 176 boxes and v2's to 41, which are exactly the box
  totals in the table below; that is the check that ties this to the record.

  WHY THIS VOIDS THE VERDICT AND NOT JUST THE PRECISION. The verdict rests on
  one claim -- that the detectors cannot see a cropped rectangular plaque
  panel (6 of 110, and 0 of 61 in Toxoplasma). With the channels the right way
  round the CURRENT zoo detector puts 72 boxes on the cropped-panel figures
  instead of 15, and puts at least one box on 7 of the 10 such figures instead
  of 1. Nobody has looked at whether those boxes land ON the panels, so this
  is not evidence that it does transfer either. It is evidence that the
  question was never asked. The false-box count barely moves (23 -> 22 and
  13 -> 15), so this is not the detector simply firing everywhere.

  WHAT SURVIVES: the 28 papers with their DOIs and licences, the 182 figures
  with their hashes and pixel sizes, the 369 REGION counts (those were counted
  from the figures, not from the boxes), the 670-article exclusion, the
  harness, and the scoring arithmetic with its guards. WHAT DOES NOT: every
  box, every box verdict, every `found` count, and therefore every precision,
  recall and verdict below.

  WHAT IT COSTS TO FIX: one `detect` stage rerun (about 30 s of CPU) and a
  fresh eye pass over the overlays. The region counts do not need redoing.
  The harness now converts to BGR and records `channel_order` in
  `detections.json`, so a result file says which question it answered.

  THE SAME SWAP IS IN THE SHIPPED PRODUCT, not only in this harness.
  `spacr/submodules.py` reads plate photographs with `cellpose.io.imread`,
  which returns RGB, and passes them to the same `detect_wells`, whose
  signature documents no channel order at all. That is a user-facing
  under-detection on the plate-splitting path and it is NOT fixed here,
  because it needs measuring on real plate photographs rather than on
  figures. It is written up as item 445.

WHAT WAS MEASURED, AND WHAT A REGION MEANS

  Two samples, drawn from Europe PMC's open-access subset, CC-BY only, with a
  fixed seed, and with EVERY PMC ARTICLE IN THE v4 DETECTOR'S PUBLISHED SPLIT
  (670 of them) refused at sampling time -- v4 trained on 939 reviewed PMC
  figures, so a paper it trained on measures nothing.

    arm A   20 papers, any organism, "plaque assay" and its variants.
            129 figures. Mostly virology and phage; that is what the
            open-access plaque literature is.
    arm B    8 papers matched by a Toxoplasma query, none of them in v4's
            split. 53 figures. The in-domain arm -- but only 6 of the 8 are
            actually Toxoplasma papers. The query ORs "plaque size"/"plaque
            number" against FULL TEXT and only needs Toxoplasma to appear
            somewhere in the article, so it also pulled in a von Willebrand
            factor scoping review (PMC13294430) and an ocular-microbiome /
            Alzheimer's review (PMC13564517). Those two contribute 5 of the
            53 figures and 0 of the 69 regions, so every rate below stands
            exactly as printed; what was wrong was the description of the
            arm, in the arm the verdict leans on. Read it as 6 Toxoplasma
            papers, 48 figures, 69 regions, plus 5 figures of ballast.

  Every figure of every sampled paper was kept -- blots, graphs, schematics,
  micrographs -- because precision measured only on plaque figures is not
  precision. 158 of the 182 figures hold no plaque image at all.

  A REGION is one plaque-assay image a measurement would have to separate from
  its neighbours, counted from the figure by eye, in two kinds:

    well    a whole round culture surface photographed whole: a well, a dish,
            a plate. This is what the detector was trained to find.
    crop    a plaque image that is not round: a stained monolayer field, a
            lawn strip, a dilution row.

  Whether plaques happen to be countable in a region does not change that it
  is one; a graph OF plaque counts is not a region. Every box was judged from
  a per-model overlay: tp = first box on a region, dup = a later box on a
  region already claimed, fp = anything else. Duplicates count against
  precision because a duplicate becomes a duplicated measurement row.

THE TABLE

  v1 = toxoplasma_well_detector_v1, yolo_welldetect_v3.pt, PLATE PHOTOGRAPHS
       ONLY. This is the one 424's question is about.
  v2 = toxoplasma_well_detector_v2, yolo_welldetect_v4.pt, the CURRENT zoo
       detector, trained with 939 PMC figures in it.
  Both at conf 0.25, imgsz 640, on the CPU with the GPU hidden; 182 figures
  x 2 models in 31 s, which is why this measurement never queued for a GPU.

  arm A -- 20 papers, any organism; 129 figures, 251 wells, 49 crops,
           114 figures with no plaque image

    model  boxes   TP  dup   FP  precision  wells found   crops found  macro
    v1       168  127   21   20      0.756  121/251 .482    6/49 .122   .631
    v2        36   15    9   12      0.417    9/251 .036    6/49 .122   .233

  arm B -- 8 papers matched by the Toxoplasma query, 6 of them Toxoplasma
           papers; none in v4's split; 53 figures, 8 wells, 61 crops,
           44 figures with no plaque image. All 69 regions are in the 6.

    model  boxes   TP  dup   FP  precision  wells found   crops found  macro
    v1         8    4    0    4      0.500      4/8 .500    0/61 .000   .111
    v2         5    4    0    1      0.800      4/8 .500    0/61 .000   .111

  POOLED: wells 125/259 (48 %) for v1 and 13/259 (5 %) for v2; crops 6/110
  (5 %) for both; false boxes on the 158 figures with no plaque image, 23 for
  v1 and 13 for v2.

  TWO RECALLS ARE PRINTED AND NEITHER IS "THE" ONE. Micro pools every region,
  so ONE 96-well plate photograph (PMC13278376 Fig 8, 192 wells, of which v1
  boxed 88 and v2 none) carries 64 % of arm A's regions. Macro averages the
  per-figure recall, so every figure counts once. They disagree -- .423 and
  .631 for v1 in arm A -- and printing one alone would be choosing.

THE VERDICT AS IT WAS WRITTEN — WITHDRAWN, KEPT FOR THE RECORD

  Everything in this section was measured through the channel swap. The
  second bullet is the one the swap most likely destroys: it is the claim
  that the detectors cannot see a cropped panel, and it is the claim the
  corrected box counts contradict. Do not cite any of it.

  * It TRANSFERS to round wells and dishes printed in figures. v1 finds about
    half of them, across organisms it never trained on -- phage plates,
    thogotovirus and adenovirus crystal-violet wells, Listeria plaque
    monolayers. Cropped, rescaled and JPEG-compressed did not stop it.
  * It DOES NOT transfer to cropped rectangular plaque panels: 6 of 110, and
    all six of those are the round dilution spots of one phage strip figure.
    In Toxoplasma papers the number is 0 of 61.
  * That is the form Toxoplasma papers print. 61 of arm B's 69 regions were
    crops: 38 in one mSphere figure (TgPDE1-18, vehicle and auxin), 14 in a
    PLOS Pathogens glycolysis paper, 5 stained fields in a PLOS NTD paper,
    4 in an mBio figure. All of them measurable; none of them found.
  * PRECISION IS NOT FREE EITHER. v1's 23 false boxes landed on a pie chart,
    a cartoon virion, two clusters of scatter dots, a brightfield organoid,
    ten bacterial MOTILITY plates -- round dishes that are not plaque assays
    at all -- and a domain diagram. A pipeline that measures those and writes
    them to the database with a DOI attached is worse than one that finds
    less.

THE SURPRISE, AND IT MATTERS FOR THE ZOO — ALSO WITHDRAWN

  This was the finding this note was proudest of, and it is the one the swap
  hits hardest. Read the corrected box counts above: RGB makes v4 look inert
  (13 boxes on the well figures, 15 on the crop figures) and v3 look dominant
  (136 and 17). BGR closes most of that gap on wells (61 against 333) and
  REVERSES it on crops (72 against 16). A recommendation to prefer the
  superseded v3 row for a cross-organism pipeline cannot be made on the RGB
  numbers, and it is not being made. 437 has not been changed on the strength
  of this and must not be.

  THE OLDER DETECTOR BEAT THE CURRENT ONE ON EVERY FIGURE THAT WAS NOT
  TOXOPLASMA. v4 fired 36 times against v1's 168 in arm A and found 9 of 251
  wells against 121. It is not broken: on the Toxoplasma arm the two are level
  (4 of 8 wells each) and v4 is the cleaner of the two (1 false box against 4).
  v4 was trained with 614 confirmed no-well figures to stop it boxing
  everything, and what it appears to have learned with them is Toxoplasma
  plaque wells specifically. 437 records that v4 beats v3 on the shared test
  set; that test set is Toxoplasma. Both statements are true, and 424 needs
  the second one: THE ZOO'S CURRENT DETECTOR IS THE WRONG FRONT HALF FOR A
  CROSS-ORGANISM LITERATURE PIPELINE, and the deprecated-looking v3 row is
  the better one for it. Neither row says this; 437's "deliberately not
  removing the v3 row" turns out to have been righter than its reason.

WHAT THIS SAYS ABOUT BUILDING 424

  Nothing yet. That is the honest position after the swap: 424's first open
  question is unanswered, and the maintainer's "build on it only if it does"
  has no measurement under it. Do not start the module.

  IT DOES NOT SAY THE TRAINING ROUND IS NEEDED. The queued GPU job
  (scratchpad/gpu_queue/424a-detector-transfer-1.md) exists to teach the
  detector a class it was said to be blind to, and the evidence for that
  blindness is the part of this run the swap destroys. It is marked NOT
  JUSTIFIED and must not be run. Three things come first, cheapest last:

    1. RERUN `detect` WITH THE FIXED HARNESS and relabel the overlays. About
       30 s of CPU plus an eye pass. This alone may answer 424's question.
    2. TRY A LARGER INFERENCE SIZE while relabelling -- see the resolution
       section below. Free, and it is confounded with everything else here.
    3. ONLY THEN decide whether 300-600 figures need annotating and a GPU
       round needs running.

  A round trained to fix a blindness the detector may not have would be paid
  for in human annotation hours before it was paid for in GPU minutes.

A MEASUREMENT DEFECT FOUND AND FIXED MID-RUN, RECORDED BECAUSE IT WOULD HAVE
GONE UNNOTICED

  The figure fetcher kept one file per figure ranked BY FORMAT. Journals ship
  `g001.webp` at 1600 px beside `g001.gif` at 200 px, and webp was not in the
  extension list at all, so the gif won every time both existed and those
  figures were handed to the detector as 200-pixel thumbnails. Nothing failed;
  the detector simply found nothing, and "found nothing" is the result being
  measured. The rule is now SIZE FIRST with format only breaking ties, the
  figures were refetched and relabelled, and every figure's pixel dimensions
  are now recorded beside its hash.

  THE SCOPE, COUNTED RATHER THAN REMEMBERED. This note first said "six PLOS
  figures" and "one extra false box". Both were written from arm A alone and
  both are wrong: arm B was fetched with the same broken rule twenty-five
  minutes before the fix. Comparing each run's pre-fix manifest against its
  post-fix one, the rule thumbnailed 17 FIGURES IN 4 PAPERS ACROSS 3
  PUBLISHERS --

    PMC13524343  PLOS Pathogens            6 figures   (arm A)
    PMC13515172  MDPI Microorganisms       6 figures   (arm B, and one of the
                                                        6 genuine Toxoplasma
                                                        papers in it)
    PMC13294430  Springer, ICM Experimental 3 figures  (arm B)
    PMC13564517  MDPI Cells                2 figures   (arm B)

  -- and at full size THREE false boxes now sit on those figures, all of them
  v1's: PMC13524343 g004, PMC13564517 g001, PMC13564517 g002. The 17 refetched
  .webp files are in the committed evidence and EVERY ONE OF THEM HOLDS ZERO
  plaque regions, so no recall, no rate and no verdict in this note moves; the
  only numbers the fix touched are false boxes, and the corrected ones are the
  ones printed above.

  A section whose stated purpose is recording a defect that would otherwise
  have gone unnoticed has no business being approximate about it. It was, and
  this is the correction.

  THE SAME DEFECT HAD A SECOND WAY TO SURVIVE, now closed. The fetcher
  returned any images already sitting in the destination without re-applying
  the rule, so rerunning the `figures` stage into a populated directory --
  which is exactly what the queued training job asks the next person to do --
  would have served the thumbnails straight back and looked like a fresh run.
  The cache is now keyed on the RULE: a `.selection` marker records which rule
  filled the directory, and anything else is refetched.

RESOLUTION: THE CONFOUND THIS RUN ARGUED ABOUT AND THEN DID NOT CONTROL

  The harness records every figure's pixel size, and its own docstring says a
  detector that finds nothing in a 200 px preview has not been asked the
  question this harness is asking. That principle was applied to thumbnails
  and not one level up, where it also applies.

  EVERY FIGURE IN THIS CORPUS THAT HOLDS A PLAQUE REGION IS SMALL. All 24 of
  them are at most 984 px on the long side; the median over all 182 is
  767 px. Of the 37 figures at 1000 px or more, and the 22 at 1200 px or
  more, NOT ONE holds a plaque region. So the entire measurement of recall
  was taken on the small end of the corpus.

  IT IS WORSE THAN THAT FOR THE HEADLINE NUMBER. 192 of arm A's 251 wells are
  in a single figure, PMC13278376 g008, which is 735 x 278 px -- about 30 px
  per well before the detector letterboxes it to 640. The 38-crop mSphere
  figure that carries most of arm B's crops is 753 x 984 px. "Finds half the
  wells" and "finds no crops" are both statements about ~30-90 px objects.

  THIS IS THE ROUTE'S CEILING, NOT A SELECTION BUG. The chooser now takes the
  biggest file in the bundle, and for the two figures that carry most of the
  regions the Europe PMC supplementaryFiles bundle holds nothing bigger --
  checked by listing them: PMC13278376 has g008.jpg at 130 KB and g008.gif at
  21 KB and no other copy; PMC8809380 has f004.jpg at 369 KB, f004.gif, and a
  .tif that belongs to a different figure (sf004, a supplement). Whether the
  publisher's own original is larger, through the NCBI OA package or the
  article page, is UNTESTED and worth ten minutes before anyone spends 35 GPU
  minutes.

  AND INFERENCE SIZE IS FREE TO VARY. A quick unjudged probe -- box counts
  only, nobody looked at where they landed -- over the same 182 files at
  640, 1280 and 1600 px showed it is not a neutral knob:

    v3: 13 of the 24 region-bearing figures get a box at 640, 16 at 1280 --
        but false boxes on the other 158 go 22 -> 103.
    v4: 17 at 640, 19 at 1280, and false boxes stay flat, 15 -> 12.

  Those are box counts, not recall, and they were taken with the corrected
  channels; they settle nothing. What they show is that imgsz, resolution and
  the channel swap are all tangled together in the one number this item was
  about to spend a training round on. Vary them before annotating anything.

HOW TO REPRODUCE IT

  arm A -- 20 papers, any organism:

    python tools/measure_plaque_detector_transfer.py --out <dir> \
        --stages sample,figures,detect --papers 20 --seed 424 --pool 400 \
        --main-figures-only --overlay-all \
        --exclude-pmcids <v4 repo training/split.csv>

  arm B -- the Toxoplasma query, which is where the other 8 papers came from
  and which was missing from this block:

    python tools/measure_plaque_detector_transfer.py --out <dir2> \
        --stages sample,figures,detect --papers 8 --seed 424 --pool 300 \
        --main-figures-only --overlay-all \
        --exclude-pmcids <v4 repo training/split.csv> \
        --query '(("plaque assay" OR "plaque formation" OR "plaquing" OR
                   "plaque size" OR "plaque number") AND (Toxoplasma OR
                   "T. gondii")) AND (HAS_FT:Y AND OPEN_ACCESS:Y AND
                   IN_EPMC:Y)'

  then, for either:

    (look at overlays/<model>/*.png, write labels.json)
    python tools/measure_plaque_detector_transfer.py --out <dir> --stages score

  Both queries are also recorded verbatim in the evidence file, along with
  the seed, the pool, the licence filter and now the full list of the 670
  excluded PMC ids and the revision of the split file they came from.

  The labels, the boxes, the papers with their DOIs and licences, and both
  summaries are committed at
  features/data/424_detector_transfer_2026-09-19.json. THE REGION COUNTS in
  it are reusable; the BOX VERDICTS are the RGB run's and are not.

WHAT IS LEFT, HONESTLY

  * THE MEASUREMENT ITSELF, AGAIN. The detect stage must be rerun with the
    fixed harness and the overlays relabelled. Until then 424's first open
    question has no answer and this item has produced a corpus, a harness and
    a bug report, which is less than it looked like this morning.
  * RESOLUTION AND INFERENCE SIZE ARE UNCONTROLLED, and they are confounded
    with the thing being measured. See the section above. Every region-bearing
    figure is <= 984 px; 192 of 251 arm A wells are ~30 px objects in one
    735 x 278 figure.
  * ONE READER LABELLED 182 FIGURES, from the overlays, in one pass. Nobody
    has checked the labels against a second reader, and the boxes were visible
    while the regions were being counted, so a missed region next to a drawn
    box is the anchoring failure this design can have. The 96-well figure was
    counted from a zoomed crop and its duplicate boxes were found by IoU
    rather than by eye.
  * n IS SMALL WHERE IT MATTERS MOST. Arm B holds 8 round wells across 6
    genuine Toxoplasma papers, and the crops that carried the withdrawn
    verdict sit in only 10 figures in the whole corpus. Even once the rerun is
    done, 10 figures is a thin base for a claim about what Toxoplasma papers
    print; the next sample should be drawn for crop-bearing figures on
    purpose.
  * ONLY THE FIRST OPEN QUESTION IS ANSWERED. Scale, the two condition
    strategies and their conflict flag, deduplication by DOI plus image hash
    (the harness hashes and flags duplicates but nothing consumes it), the
    database, and the PDF text layer are all untouched.
  * The harness calls spacr.plaque.detect_wells with min_axis_ratio=0, so
    nothing is dropped before a person sees it; each box records whether the
    shipped 0.7 filter would have kept it. It would have kept 173 of v1's 176
    boxes and 40 of v2's 41, so the filter is not what is losing the crops.
  * A GPU JOB IS QUEUED AND IS NOT JUSTIFIED:
    scratchpad/gpu_queue/424a-detector-transfer-1.md. It trains a two-class
    detector (well, plaque_panel) on v4's data plus 300-600 newly annotated
    figures, 30-45 min, ~6 GB VRAM. It was written to cure a blindness the
    RGB run reported and the corrected box counts put in doubt, and its
    success check ("6 of 110 crops today") is an RGB number. It is marked NOT
    JUSTIFIED at the top of the file. Rerun the measurement first.
  * THE SHIPPED PLATE-SPLITTING PATH HAS THE SAME SWAP and is NOT fixed here:
    `spacr/submodules.py` hands `cellpose.io.imread`'s RGB array to
    `detect_wells`, and `detect_wells` documents no channel order. Item 445.
    It needs measuring on real plate photographs, which this unit does not
    have, before anyone changes the behaviour users already have results from.

--------------------------------------------------------------------------------
2026-09-20 — THE RERUN: IT TRANSFERS, AND THE WITHDRAWN VERDICT WAS BACKWARDS
--------------------------------------------------------------------------------

Decided: "Go as recommended" -- the maintainer, 2026-09-19, accepting a
recommendation whose first clause is to measure whether the detector transfers
to figure crops and to build on it only if it does. This note answers that
clause. It does not start the module.

THE INSTRUMENT WAS CHECKED BEFORE IT WAS BELIEVED

  The 2026-09-19 run was void because it showed the detector the wrong channel
  order, so the first thing the rerun owes is evidence it is measuring the same
  thing with that one fault removed.

  * THE CORPUS IS THE SAME CORPUS. papers.json was rebuilt from the committed
    evidence rather than resampled, so Europe PMC's index moving cannot change
    which papers are in it. The figures stage refetched all 182, and all 182
    are BYTE-IDENTICAL to the committed sha256 -- 129 of 129 in arm A, 53 of
    53 in arm B. The .webp size-first rule and its .selection marker held.
  * THE DETECTORS ARE THE SAME DETECTORS. sha256 b826058754fb… for v1 and
    f2a1e1110f09… for v2, identical to the spike's record in both arms.
  * THE BOX TOTALS LAND WHERE THE REVIEW SAID THEY WOULD. The review predicted
    the BGR totals over the 182 figures from its own re-run of detect_wells:
    371 for v1 and 148 for v2. This run, through the harness, gives 362 + 9 =
    371 and 80 + 68 = 148. That is the check that ties the rerun to the record
    and it is exact.

WHAT WAS LOOKED AT, AND WHAT WAS NOT

  Every figure on which v2 drew a box was opened and judged from its overlay:
  all 17 region-bearing figures it boxed, the 7 region-bearing figures it did
  not box, and the 4 figures with no region that it boxed anyway. (Those last
  4 were counted and not opened when this note was first written; the sentence
  said 11 for the first group, which was simply wrong -- the evidence file has
  17 -- and the correction is dated below.) The REGION COUNTS ARE NOT NEW --
  they are the 2026-09-19 reader's 369, reused unchanged, which is exactly
  what that note said survived. So this is a rerun of the boxes, not a second
  measurement.

  V1'S BOXES WERE NOT JUDGED ONE BY ONE and no precision is claimed for it
  here. Its 271 boxes on the one 96-well plate figure are past what an eye pass
  can honestly resolve; the original run used IoU there and this one did not
  repeat it. Only v1's counts appear below. SO "v1 FINDS 6 OF 110 CROPS" BELOW
  IS AN UPPER BOUND AND NOT A JUDGED RECALL: v1 drew no box at all on 9 of the
  10 crop-bearing figures and 16 boxes on the tenth, which carries 6 crop
  regions, so 6 is the most it can have found. The conclusion it supports --
  that v1 does not transfer to crops -- does not depend on where in 0..6 the
  true number is.

THE CURRENT ZOO DETECTOR, v2 = yolo_welldetect_v4.pt, conf 0.25, imgsz 640

    regions          found   of      note
    wells              61    259     23.6 % micro
    wells, less one    61     67     91 %; the excluded figure is PMC13278376
      96-well figure                 g008, 192 wells at about 30 px each
    crops              63    110     57 %, against 6/110 in the withdrawn run
    crops, arm B       57     61     93 %, against 0/61 in the withdrawn run
    wells, arm B        8      8
    macro, 24 figures  0.670

    tp 124, duplicates 9, FALSE BOXES ON REGION-BEARING FIGURES 0,
    false boxes on the 158 figures with no plaque image 15, precision 0.838.

  THE ZERO IS THE NUMBER TO READ TWICE, AND IT MEANS LESS THAN THE SENTENCE
  THAT FIRST STOOD HERE CLAIMED. On the 24 region-bearing figures, v2 put no
  box on a gel, a western blot, an immunofluorescence panel, a FACS scatter, a
  bar chart, a survival curve, a protein structure or a sequence alignment --
  on figures carrying all of those, often twenty panels deep. That is the zero
  and it is worth having.

  WHAT IT DOES NOT COVER IS THE 15 BOXES ON THE OTHER 158 FIGURES, and this
  note first generalised over them without opening them. They were opened
  afterwards -- 4 figures -- and they are not chart furniture; they are things
  that genuinely look like a plate or a monolayer, plus one that does not:

    * PMC13028617 microorganisms-14-00645 g004, 10 boxes: every one on a
      LISTERIA MOTILITY AGAR PLATE in panel A, none on the bar chart in
      panel B. Round dish, diffuse central zone -- the object v1 was faulted
      for in the withdrawn run.
    * PMC13309054 pntd.0014454 g002, 2 boxes: each framing a whole ELECTRON
      MICROGRAPH of a single bacterium.
    * PMC13464350 mbio.01284-26 f005, 2 boxes: two brightfield monolayer
      panels in panel D, with nothing on the gels in panel E or the five bar
      charts around them.
    * PMC13294430 40635_2026_935 Fig3, 1 box: inside a SCHEMATIC FLOWCHART,
      around one of its rounded boxes. The only one with no image in it.

  SO THE WITHDRAWN RUN'S PRECISION WORRY -- v1's 23 false boxes on pie charts,
  cartoon virions and motility plates -- IS HALF ALIVE FOR v2, NOT DEAD. The
  motility plates are still boxed. The charts and the virions are not, at 640;
  see the imgsz note below, where one cartoon virion is.

  None of this moves the arithmetic: those 15 were already counted as false
  and precision is still 124/148 = 0.838. It moves what the number licenses
  you to say about WHICH objects a literature pipeline will have to reject.

  THE FIGURE THE WITHDRAWN VERDICT RESTED ON. PMC8809380 msphere.00793-21
  f004 carries 38 cropped Toxoplasma plaque panels and was the single largest
  block of "crops the detector cannot see": 0 found under RGB. Under BGR v2
  finds 38 OF 38, each box on its panel, none on the paired scatter plots
  beside them. PMC9744290 (6/6, 4/4, 4/4), PMC13170237 (4/4) and PMC11414933
  (4/4) are the same story.

WHAT IT STILL MISSES, NAMED RATHER THAN AVERAGED

  * PHAGE DILUTION-SPOT LAWN STRIPS: 43 of the 47 crops missed. Three figures
    of one Nature Communications phage paper (30, 8 and 5 regions) score 0.
    The region there is a horizontal strip of decreasing spots on a grey lawn.
    A fourth figure in the SAME paper, whose spots are large and round, scores
    6 of 6 -- with 9 duplicates, because v2 boxed individual spots while the
    reader counted the row as one region. So the form is what it misses, not
    the paper, the organism or the compression.
  * VERY SMALL WELLS: the 96-well plate figure at ~30 px a well, 0 of 192.
    v1 boxes it heavily and v2 not at all, which is the one place the old
    detector is still worth something.
  * DENSE MONOLAYER FIELDS: PMC13309054 g003, five near-identical stained
    fields, 1 found. The one box is correct.

WHAT THIS CHANGES

  * 424's first open question is answered YES for the detector the module was
    told to ask the zoo for, and the maintainer's "build on it only if it
    does" is now satisfied. The module can be built.
  * THE "SURPRISE" IN THE WITHDRAWN NOTE IS REVERSED, as that note warned it
    might be. v1 finds at most 6 of 110 crops and v2 finds 63; for a literature
    pipeline the CURRENT detector is the right front half after all. 437 was
    not changed on the RGB numbers and must not be changed on these either
    without its own test set being rerun.
  * THE QUEUED GPU JOB IS STILL NOT JUSTIFIED and is now less justified, not
    more: scratchpad/gpu_queue/424a-detector-transfer-1.md exists to teach the
    detector a class it can already find. Anyone who wants to spend the GPU on
    this should spend it on the lawn-strip form instead -- and imgsz, which
    was the free thing to try first, has now been tried: see the note below.
    It finds a large part of the lawn-strip form for nothing, which is a
    further argument against buying it with a training run.

WHAT IS STILL OPEN

  * ONE READER, ONE PASS, AGAIN, and the same anchoring risk: the boxes were
    visible while the calls were made. The counts being reused means the
    recall denominator at least was fixed before these boxes were drawn.
  * PRECISION FOR v1 IS NOT MEASURED HERE. (The 15 false boxes for v2 on
    no-region figures were counted and not inspected when this was written;
    they have since been opened and are described above.)
  * IMGSZ IS NO LONGER UNCONTROLLED AT ONE STEP -- see the imgsz note below,
    which compares 640 with 1280 over the same corpus. Everything in THIS
    section is 640 px, and no size between or above the two was tried.
  * n IS STILL SMALL WHERE IT MATTERS: 10 crop-bearing figures, and 38 of the
    110 crops are one figure. A sample drawn for crop-bearing figures on
    purpose is still the right next measurement.
  * Scale, the two condition strategies and their conflict flag, dedup by DOI
    plus image hash, the database and the PDF text layer are all untouched.
  * 445 WAS UNTOUCHED BY THIS UNIT AND HAS SINCE LANDED (d60894e1a, the same
    day). The conversion moved INTO `spacr.plaque.detect_wells`, which now
    takes RGB and converts, so the harness must hand it RGB and not convert.
    That is a different code path from the one these numbers were taken on,
    so they were taken again on it after the rebase: arm B at 640 gives 9 and
    68 and both arms at 1280 give 372/134 and 11/60, identical box for box.
    The measurement is therefore a check on 445's refactor as well.

TWO THINGS THIS RUN GOT WRONG ABOUT ITS OWN TOOLING, RECORDED SO THEY COST
NOBODY ELSE ANYTHING

  * THE HARNESS IMPORTS SPACR FROM WHEREVER THE EDITABLE INSTALL POINTS, not
    from the tree it is run in. Running `python tools/measure_...py` puts
    tools/ on sys.path[0], not the working directory, so the first attempt
    scored the SHARED checkout's spacr.plaque while sitting in a worktree.
    Everything above was run with PYTHONPATH set to the worktree. A harness
    whose whole job is measuring a function should say which copy it measured;
    it does not, and that is worth fixing.
  * IMPORTING ULTRALYTICS REWRITES ~/.config/Ultralytics/settings.json. It did
    so here, on a file dating from July, announcing "settings reset to default
    values"; there was no backup and whatever non-default entries it held are
    gone. Every later run in this unit set YOLO_CONFIG_DIR, HOME, XDG_CONFIG_HOME
    and XDG_DATA_HOME into the scratchpad. ultralytics is not in any spacr env;
    it was installed into a throwaway venv with --system-site-packages, and
    pinning an old version there silently shadowed numpy 2.5.2 with 1.26.4,
    which is its own way to measure the wrong thing.
    IT HAPPENED A SECOND TIME on 2026-09-20 while this file was being
    corrected: a one-line `import ultralytics` typed to check the venv's
    version, without the sandbox, rewrote the same file again. Nothing more
    was lost -- the file was already at defaults -- but a rule that has to be
    remembered at every command line is a rule that will be broken, and it was
    broken twice in one day by two people who both knew it.
    SO THE HARNESS NOW DOES IT INSTEAD OF ASKING. `stage_detect` sets
    YOLO_CONFIG_DIR to <out>/yolo-config before anything imports ultralytics,
    prints where it pointed, and records it in detections.json;
    --yolo-config-dir overrides it, so using the real one is possible and has
    to be typed. Verified by rerunning arm B with no YOLO_CONFIG_DIR in the
    environment: settings.json was created under the run's own directory, the
    user's file was not touched (mtime unchanged), and the box counts came
    back 9 and 68, identical to this note's.
    The same commit makes detections.json record `spacr_from`, so a result
    file says which copy of spacr produced it -- the first fault above.

--------------------------------------------------------------------------------
2026-09-20 — STEP 2, IMGSZ: A LARGER INPUT MOVES THE FAILURE, IT DOES NOT
REMOVE IT
--------------------------------------------------------------------------------

Decided: "Go as recommended" -- the maintainer, 2026-09-19. The plan that
answers to names three steps in order, and step 2 is "TRY A LARGER INFERENCE
SIZE WHILE RELABELLING -- free, and it is confounded with everything else
here". Step 1 was done above and step 2 was skipped, which left the transfer
answer confounded with a resolution nobody had chosen. This is step 2 at one
larger size.

SAME EVERYTHING EXCEPT THE NUMBER. Same 182 figures, same two checkpoints by
sha256, same conf 0.25, same BGR conversion, same harness; imgsz 640 -> 1280.
Detect was run twice at 1280 and gave identical counts. Arm B was also rerun
at 640 through the changed harness and gave 9 and 68, identical to the section
above, so the harness change did not move the instrument.
Evidence: features/data/424_imgsz_sweep_2026-09-20.json.

    v2 boxes over 182 figures      148 -> 194
    v1 boxes over 182 figures      371 -> 383
    v2 false boxes on the 158       15 -> 12
      figures with no region

  Twelve figures changed. Every one of them was opened at 1280.

THE ONE NAMEABLE FAILURE LARGELY GIVES WAY, FOR FREE

  The three phage figures of lawn strips that scored ZERO boxes at 640 -- 30,
  8 and 5 regions -- draw 29, 14 and 11 boxes at 1280, and every one of those
  boxes is on a dilution spot inside a strip. Counting how many strips carry
  at least one box, by clustering the box centres into bands and checking that
  against the overlay: about 9 of the strips on Fig1, 4 of the 8 on Fig2, 4 of
  the 5 on Fig3, against 0, 0 and 0 at 640.

  A PER-REGION RECALL AT 1280 IS NOT REPORTED, on purpose. The 2026-09-19
  reader recorded region COUNTS, not geometry, and at 1280 v2 boxes individual
  spots where that reader counted a strip of spots as one region. Dividing one
  by the other would be a number with no denominator behind it. Saying "9 of
  Fig1's strips" is as far as the record supports.

  Precision on those same figures holds: nothing on the ribbon diagrams, the
  gene-map cartoons, the sequence logos or the protein gel printed beside the
  strips.

AND IT COSTS SOMETHING, ON EXACTLY THE FIGURES THE ANSWER ABOVE RESTS ON

  * PMC9744290 g006  4 of 4 -> 2 of 4. Only panel D's -Rapamycin row survives;
    both +Rapamycin crops, which are sparse and low-contrast, are lost.
  * PMC9744290 g007  4 of 4 -> 2 of 4, the same way: the +ATc row is lost.
  * PMC9744290 g004  6 of 6 -> 5 of 6.
  * PMC13309054 g003 1 -> 0. Its one correct box on the dense stained
    monolayer fields is gone.
  * PMC8809380 f004 HOLDS AT 38 OF 38, and the well figures are unchanged, and
    the 192-well plate at ~30 px a well is still 0.

  The shape of it: at 1280 the detector wins small repeated objects and loses
  whole faint panels. That is a real finding for the module and not a tuning
  detail -- ONE INFERENCE SIZE IS NOT RIGHT FOR BOTH FORMS a literature corpus
  contains. Running both sizes and merging is the obvious next thing to
  measure, and it has not been measured.

FALSE BOXES: THREE FEWER, AND A DIFFERENT THREE

  15 -> 12 on the 158 figures with no region. The schematic-flowchart box and
  the two brightfield-monolayer boxes go away; one of the two whole-electron-
  micrograph boxes goes away; the ten Listeria motility-agar-plate boxes stay,
  every one of them; and a NEW one appears, on a cartoon virion in a graphical
  abstract (PMC12930041 fx1). The withdrawn run's "cartoon virions" worry is
  therefore alive for v2 at 1280, which the 640 section could not have known
  because it had not opened those figures.

WHAT THIS DOES AND DOES NOT CHANGE

  * It does not change the transfer answer. v2 transfers at both sizes; v1
    transfers to crops at neither (its crop-bearing box counts barely move).
  * It does change the advice to the module: imgsz is a parameter the module
    must expose and record per run, not a constant, and the detector should be
    asked at more than one size before a figure is called empty.
  * It removes the last excuse for the queued training round. The form that
    round was written to buy is largely reachable by typing a bigger number.

STILL NOT MEASURED HERE: any size other than 640 and 1280; merging the two;
tiling a figure instead of scaling it; v1's boxes one by one; and a corpus
sampled for crop-bearing figures on purpose.
