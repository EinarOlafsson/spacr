493 — PARALLEL MULTI-GPU MASK BATCHES
====================================
Status: IMPLEMENTED AS ALPHA, requested2026-09-23. Pipeline, Mask Generation
and Cluster Distribution controls are wired; output matches the single-GPU run
byte for byte on plate1. Validation on a machine with two physical GPUs remains
open (this machine has one).
Scope: Cluster Distribution and Mask Generation. Filed for later work;
this request does not add a prerequisite to the1.5.1.0release plan.

Let the user select several compatible GPUs and process image batches on
them in parallel. Prepare the temporary NPZ batches first, then dispatch
each NPZ to one GPU, or partition the NPZ paths into per-GPU lists that
independent workers process concurrently. Each batch belongs to exactly
one worker; preserve field identities and the existing mask/output layout.

1. Prepare inputs once using the current preprocessing/normalization path.
   Generate a manifest of the temporary NPZ files and their source fields
   before dispatch. Avoid repeating preparation separately on every GPU.
2. Discover eligible devices and let the user choose participating GPUs.
   Expose the same option in Mask Generation when multiple compatible
   GPUs are detected. Otherwise keep it greyed out with an explanation.
   In Cluster Distribution, use the devices actually allocated to the job.
3. Run separate workers bound to their assigned GPU, with one loaded model
   per worker. Support a batch queue or disjoint per-GPU NPZ lists so that
   multiple devices segment concurrently without duplicate output writes.
4. Show per-GPU progress and overall completed/failed batch counts. Make
   cancellation, failures and resume preserve completed outputs and report
   unfinished work. Delete temporary inputs only after their outputs have
   been committed successfully, respecting existing retention settings.
5. Validate on a real multi-GPU system: simultaneous execution on selected
   devices, every input processed exactly once, results consistent with the
   equivalent single-GPU path, restart after interruption, mixed device
   memory limits, and no accidental use of unselected devices. Verify the
   disabled control on zero/single-compatible-GPU systems as well.

Implementation owns the scheduler, device binding and runtime controls.
The separate documentation lane owns resulting API descriptions, settings
help, tutorials and translations once the runtime contract is implemented.

2026-09-24 — Prepared-input manifest and exclusive field ownership
spacr.io._mask_batch_manifest now inventories prepared archives without
materializing image pixels. Records contain absolute paths, archive SHA256,
byte sizes, exact output filenames and channel counts. Duplicate fields
within/across batches, paths escaping the mask folder, hidden outputs,
unreadable/legacy pickled identities and mid-inspection file changes are
refused before scheduling. Source archives are unchanged. The existing
header inspector accepts an explicit field axis for time-stack manifests.
66 manifest, archive-integrity and preprocessing checks pass. No GPU work
was run and no parallel UI option is exposed yet. Remaining implementation:
allocated-device discovery/selection, persistent per-device model workers,
cooperative stop/resume and progress, serialization of shared QC/reporting,
Mask/Cluster Distribution wiring and the real multi-GPU acceptance protocol.

2026-09-24 — Allocated-device discovery and worker environment preparation
Private accelerator helpers now enumerate the process-visible CUDA/ROCm
ordinals, names and memory without tensor allocation. Explicit single-device
or CPU overrides disable parallel discovery. A worker environment selects
one allocated ordinal, preserves CUDA UUID/MIG identifiers and ROCR limits,
and does not mutate the parent environment. Conflicting HIP/CUDA aliases,
invalid ordinals and changed/disabled visibility are refused. These helpers
are not yet wired to controls or model workers; no GPU execution is claimed.
79 discovery/allocation/existing accelerator checks pass with mocked devices.
Source contracts consulted: NVIDIA CUDA Environment Variables
https://docs.nvidia.com/cuda/cuda-programming-guide/05-appendices/environment-variables.html
and AMD GPU isolation
https://rocm.docs.amd.com/en/develop/reference/system-optimization/gpu-isolation.html
The actual multi-GPU acceptance requirement remains unchanged and open.

2026-09-24 — Normal mask generator accepts exclusive worker assignments
The Cellpose-SAM pipeline now accepts optional batch_paths, on_batch_done and
run_qc keyword arguments. An assigned worker keeps the original input/output
root, loads one model for its whole path list, and reports each archive only
after its selected fields complete. Empty assignments load no model; duplicate,
missing, hidden and outside-root paths are refused before model loading.
The coordinator can defer shared QC until all workers finish. Ordinary calls
keep their existing three arguments and whole-directory/QC behavior.
111 pipeline/assignment/label-capacity/raw-intensity checks pass. CPU model
stand-ins verify persisted masks, no unassigned outputs, one model load,
no completion event after a failed save, and cooperative cancellation after
one completed archive with all inputs retained. Parallel process dispatch,
resume ledger, controls and actual multi-GPU execution remain open.

2026-09-24 — Persistent process workers and cooperative stop
Private spacr._mask_workers now balances disjoint archive lists by stored
bytes and spawns one persistent model process per nonempty device assignment.
Each child binds its environment before loading the generator, checks that
exactly one GPU is visible, and forwards logs and completed-archive events.
The parent reports independent per-device progress snapshots, rejects duplicate
completion, detects exceptions/native crashes/missing final events, and stops
peers cooperatively. Completed outputs and all prepared inputs are retained.
72 worker/assignment/manifest/device/inventory checks pass; the new module has
94.12% combined statement/branch coverage. Real spawned CPU workers verify
concurrency and process isolation; this is not a real multi-GPU inference
acceptance. Persistent resume provenance, shared finalization, controls and
Mask/Cluster Distribution integration remain open.

2026-09-24 — Durable archive receipts and interrupted-worker resume
The process coordinator now has an atomic CheckpointStore adapter. It binds
prepared-input records and explicit field exclusions to a caller-supplied
material-settings/resolved-model SHA256 signature. Existing masks without a
ledger are refused before a new ledger can claim them, including on a second
attempt. Every completion receipt validates and hashes all selected saved
arrays. Resume skips verified archives, requeues missing/truncated outputs,
and refuses complete masks whose bytes changed. It preserves prepared inputs
and records failed/cancelled/segmented status; segmented is not final QC.
29 real-process/provenance/partial-write checks pass in36.12s; combined module
coverage95.49%. A cancelled CPU process run resumes only its two pending
archives and preserves the first mask's bytes and modification time.
Still required: resolve model-content signatures at the actual pipeline entry,
handle post-segmentation adjustments/shared QC and timelapse aggregation,
connect Mask/Cluster controls and perform the real multi-GPU acceptance.

2026-09-24 — Serialize motility analysis after complete frame merging
The SAM and legacy generators previously called automated_motility_assay
inside each inference batch, before saving masks and before merged frames
existed. Core now runs the requested assay once per plate after every object
role and the merge finish. It passes the experiment root (not masks/) and
rebuilds assay measurements from current merged masks. Per-object workers no
longer launch competing whole-plate analyses. Direct generator calls perform
segmentation/tracking only; the full pipeline owns the optional assay.
Two order regressions fail before the fix;114SAM/core/assignment checks and
93legacy/core checks pass. Failure/cancellation checks confirm no analysis on
failed merges and no input cleanup after a failed or cancelled finalization.
Pipeline/control integration and actual multi-GPU acceptance remain open.

2026-09-24 — Resolved model bytes participate in resume identity
Private _prepare_mask_model now returns canonical detached worker settings,
a material-settings/model signature and the resolved checkpoint identity.
Custom weights and pathogen overrides stay explicit; stock weights resolve
once via the installed Cellpose cache API, including the zero-argument
cache_CPSAM_model_path of the supported4.0.7minimum. Empty/missing or changing
weights are refused. Replacing weights at the same filename changes the
signature; GPU selection and cosmetic settings do not. No model is built.
The helper currently covers the Cellpose backend; other backends still need
resolved-artifact contracts before parallel dispatch. No new UI option is
exposed by this batch.41worker/model/resume checks pass; module coverage96.13%.
Official minimum-version source checked:
https://github.com/MouseLand/cellpose/blob/v4.0.7/cellpose/models.py
Remaining integration includes invoking this helper at pipeline dispatch,
figure delivery, finalized/adjusted-mask provenance, shared QC, controls and
real multi-GPU acceptance. Source-module inventory remains643.

2026-09-24 — Worker plots survive process isolation
Workers capture Matplotlib output with Agg and send private transport-file
paths through the bounded event queue. Figures retain their image arrays,
colormaps, axes, labels and legends; decoding does not recreate child pyplot
windows. The parent delivers figures to the existing figure sink or a supplied
on_figure callback. Headless runs save PNGs in <experiment>/mask_worker_plots,
outside the temporary masks folder, so ordinary input cleanup retains them.
Repeated show/publish calls do not duplicate static figures. Transport storage
is cleaned on success/failure; failed delivery preserves the prepared inputs.
47worker/model/resume/plot checks pass,96.64% module coverage.18Qt bridge checks
pass, including a real analysis QThread spawning a process and delivering two
readable PNGs through figure_ready to a GUI-thread QObject. Final image-array,
colormap and durable-path checks pass7tests. No GPU inference was performed.
Pipeline/control integration, adjusted-mask finalization and real multi-GPU
acceptance remain open. The source module inventory is unchanged at643.

2026-09-24 — Shared adjustment validates field ownership before writing
adjust_cell_masks now rejects equal-sized parasite/cell/nuclei folders when
the filenames differ. Count equality alone could adjust an early field before
a later missing partner failed. Filename preflight finishes before workers
start and names the mismatched fields. Six role/worker-count cases verify no
worker call and byte-identical inputs on refusal; complete adjustment/mask
cohort56passes. This also protects ordinary single-GPU finalization. Multi-GPU
controls/pipeline wiring and adjusted-mask resume provenance remain open.

2026-09-24 — Adjustment preflights all array headers before mutation
A later field with mismatched dimensions or damaged bytes could previously
fail after earlier cell masks had already been overwritten. adjust_cell_masks
now reads every selected mask header before starting work, checking complete
numeric2Darrays and matching field dimensions, including available organelle
masks. Header reads avoid loading full arrays and honor cancellation between
fields. Baseline reproduced early processing;24role/damage/worker cases now
refuse before any worker call and preserve every input byte.141related tests
pass. This protects ordinary adjustment too; parallel pipeline integration
and finalized-mask provenance remain open.

2026-09-24 — Adjustment uses actual cell IDs throughout
Overlap queries previously used compact connected-component IDs while writes
compared those numbers with original cell IDs. Nonconsecutive labels could
therefore miss merges or affect a different object. The shared adjustment
routine now compares/writes current IDs and compacts only its returned mask.
Six baseline failures cover parasite, nucleus and shared-perimeter merges
with IDs7/300in both orders; all now match the dense-label result and preserve
the full foreground.147related tests pass. No thresholds/settings changed.

2026-09-24 — Separate adjusted masks preserve raw resume receipts
adjust_cell_masks and process_mask_file_adjust_cell accept keyword-only
output_folder=None. Defaults retain in-place behavior; a separate destination
rebuilds selected fields from raw masks, atomically, preserving all raw bytes.
Aliased source folders and unrelated destination fields are refused. An
interrupted adjustment can be rerun without invalidating segmentation receipts.
_load_and_concatenate_arrays accepts mask_folders role overrides, and shared
_run_seg_qc accepts mask_folder, so merged planes and QC consume the adjusted
cells while the source arrays and raw receipts remain valid.125checks pass,
including actual adjustment, merged arrays, QC count/CSV, interruption/restart,
source symlinks and invalid overrides. Parallel pipeline/control dispatch
still needs wiring; these additions keep the ordinary default path unchanged.

2026-09-26 — Pipeline dispatch, controls and single-card acceptance
preprocess_generate_masks dispatches each object role to the process workers
when mask_parallel is on and two or more GPUs are visible (or named in
mask_gpu_indices). Preparation runs once. Shared QC, cell adjustment (written
to masks/adjusted_cell_mask_stack, raw cell masks kept) and the merge run once,
after every worker finishes. A per-role ledger (.mask-workers-<role>.json)
resumes only unfinished archives and refuses masks from different settings.
Parallel is refused for timelapse and t_stack runs. With fewer than two GPUs
the ordinary single-device path runs unchanged. mask_parallel defaults off.
Controls: mask_parallel and mask_gpu_indices are in Mask's Runtime & Reliability group;
both are greyed, with the reason on hover, when fewer than two compatible
CUDA/ROCm GPUs are visible, and the GPU list is greyed while mask_parallel is
off. Timelapse keeps both hidden. Cluster Distribution has a Mask-only checkbox
that submits mask_parallel on with a blank GPU list, so the job uses the GPUs
its scheduler allocates. A progress label under the run shows overall and
per-GPU archive counts.
Acceptance on real plate1 (13 fields, cell/nucleus/pathogen, Cellpose-SAM,
one RTX 3090, each run a separate gpu_turn.sh turn): nightly, this branch
serial and this branch parallel. The parallel run had two worker processes
sharing the one card via a patched device table. merged/, cell, nucleus and
pathogen mask stacks were byte-identical across all three (parallel adjusted
cells equal serial in-place cells). measurements.db rows were identical apart
from run IDs and timestamps (object_counts rows arrive in worker-completion
order). All three ledgers finalized 7/7 archives. Wall time: nightly 482 s,
serial 446 s, parallel 728 s. Two workers on one card are slower, so this is
not a speed measurement.
Tests: 28 pipeline tests (real spawned CPU workers: serial equality, exactly-
once writes, interrupted-worker resume, changed-settings refusal, core
dispatch) and 7 Qt control tests pass with CUDA_VISIBLE_DEVICES=''. The ten
new captions wait in _AWAITING_CATALOG_REBUILD for the catalog lane.
Still open: step 5 on two or more physical GPUs (simultaneous execution on
distinct devices, mixed memory limits, no use of unselected devices), and
resolved-model contracts for backends other than Cellpose.
ALPHA: settings mask_parallel, mask_gpu_indices; widgets: the mask_parallel
and mask_gpu_indices controls in Mask Generation, the Cluster Distribution
checkbox DistributedAllocatedGpus, the progress label MaskGpuProgress.
