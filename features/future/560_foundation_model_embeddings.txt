FOUNDATION-MODEL EMBEDDINGS: SubCell, OpenPhenom, ChAda-ViT, Cell-DINO

Status: FILED 2026-09-26. Not started.
Owner: unassigned. Proposed from the 2026-09-26 deep-learning/GPU list.

REQUEST (the maintainer, 2026-09-26, verbatim)
    "Add to future features list: B1,2,3,4,(in its own env like cellpose 3),
    B5 (if this isn't the same as magnifier),B7(if this is connecting
    objects over frames), B8,B9,B10,B11B12,B13,B14,B15,B16,B17,B19,B20"

EVIDENCE
    https://www.ncbi.nlm.nih.gov/pmc/articles/PMC12826486/

WHAT TO BUILD
    Add channel-adaptive / single-cell foundation models as backbones for
    spaCR's embeddings, image UMAP and classifiers, each in its own
    environment where dependencies clash.

DONE WHEN
    Embeddings from each backbone separate known phenotypes on a labelled
    set (kNN accuracy / mAP) compared with current backbones.
