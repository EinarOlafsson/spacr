490 — MEASURED AND CALCULATED POINT SPREAD FUNCTIONS
=================================================
Status: IN PROGRESS. Engine, Make Masks, Mask, Measure and Mask Live preview
validated2026-09-23; real measured3DPSF application validated2026-09-24.
Documentation remains; instrument-specific optical accuracy is not claimed.

Let users modify images using a measured PSF or a calculated approximate PSF,
first in Make Masks enhancement, then in Mask generation and Measure after
validating behavior. Clearly distinguish convolution from deconvolution and
explain the assumptions of an approximate kernel; do not silently infer optical
parameters or present a Gaussian approximation as a measured microscope PSF.

Acceptance: load calibrated measured kernels and validate dimensions, sampling,
finite nonnegative values and positive normalization; calculate a documented
kernel from explicit parameters; preserve source data, image dimensions and
channel identity; define boundary behavior and dtype/intensity handling.
Operations opt-in/off by default, cancellable off the GUI thread, integrated
with Compare/Apply, and persisted in provenance including kernel identity and
all restoration settings. Measure must record whether quantitative features
use source or processed intensities. Validate synthetic known-convolution
behavior and representative microscopy data before offering downstream use.
API descriptions/tooltips/animations and translated tutorials belong in the
shared documentation handoff. No claim of biological recovery from appearance.

2026-09-23 — NUMERICAL ENGINE IMPLEMENTED; INTEGRATIONS REMAIN
spacr.point_spread supports calibrated2D/3D measured TIFF/NPY kernels and
explicit sampled Gaussian approximations from FWHM/spacing. Immutable kernel
bytes retain normalized shape/sampling/hash and source-file SHA256. Odd spatial
axes, finite nonnegative positive signal and calibrated sampling are checked;
NPY/TIFF shapes are bounded before decoding. No implicit channel interpretation,
kernel resampling, optical inference, integer clipping or source mutation.
Convolution and Richardson–Lucy use half-sample symmetric boundaries. The
adjoint folds boundary contributions exactly, with sensitivity normalization;
an independent dense direct-convolution matrix verifies asymmetric-kernel RL.
Each channel is independent; cancellation checks separate expensive operations.
Outputs are float32 in original intensity units; all settings enter provenance.

64 focused checks pass; branch-inclusive module coverage92.90%. Real public
THP1/RNF213 field1998x1998x4 processed in16.23s CPU under4GiB cap. Added known
Gaussian blur then deconvolved20iterations; MSE ratios relative to blurred
image were0.250,0.183,0.202,0.585. Source unchanged, all mask planes excluded.
Calibration1um/px and2um FWHM are declared verification geometry, not measured
microscope calibration. This proves numerical/software behavior, not biological
recovery. Receipt: features/data/490_psf_microscopy_acceptance_2026-09-23.json.

Remaining: Make Masks controls and Compare/Apply/provenance integration;
Mask/Measure settings and processing with explicit intensity source; actual
measured optical PSF/application acceptance; docs/API/tooltips/translations.
Do not mark490complete from numerical checks alone.
Primary references: SciPy fftconvolve documentation and scikit-image
Richardson–Lucy documentation, linked in spacr.point_spread's module docstring.

2026-09-23 — MAKE MASKS CONTROLS AND COMPARE/APPLY IMPLEMENTED
Image enhancement now offers off-by-default PSF convolution or Richardson–Lucy,
a Gaussian approximation with explicit Y/X FWHM, or a measured2D TIFF/NPY.
Image and measured-kernel pixel spacing are explicit; zero/unset or mismatched
calibration refuses processing. Kernel loading/generation runs off-thread,
with generation guards. Captured immutable bytes remain unchanged if a file
changes on disk; Reload explicitly reads the new version. Controls wrap in a
narrow settings column and link to point_spread.apply_psf API help.

The fixed chain order is stretch/background/PSF/denoise/contrast/sharpen before
detection. PSF failures propagate instead of silently skipping the operation.
Compare and Apply run in background; cancellation reaches deconvolution's
iteration checks. Whole-image detection reuses the scientific float result
from the background Apply worker, rather than recomputing PSF on the GUI thread.
Wand uses those scientific intensities too; display scaling is separate.
Mask history records operation, image spacing, kernel/file identity, iterations,
boundary/algorithm and the fact that measurements still use original pixels.

182 focused numerical/Qt/help/normalization/cancellation checks passed.
Full1998x1998public THP1 Hoechst field passed Compare, Apply, detection, Save
and sidecar readback:364labels, source pixels/file unchanged, both PSF calls
off the GUI thread,867Qt ticks over11.94s; maximum event gap0.715s includes
subsequent synchronous classical detection/save. Verification geometry1um/px
and Gaussian FWHM2um are not measured optical calibration. This is software/UI
acceptance, not biological recovery or segmentation accuracy. Receipt:
features/data/490_psf_make_masks_acceptance_2026-09-23.json.

Remaining: Mask/Measure pipeline settings and processing with explicit measured
intensity-source choice; measured optical PSF validation; documentation,
translations and an appropriate PSF animation.490remains IN PROGRESS.

2026-09-23 — MASK PIPELINE INTEGRATION VERIFIED
Mask and Timelapse now offer Point Spread Function settings: psf_operation
(none/convolve/deconvolve), psf_source (gaussian/measured), psf_path,
psf_image_sampling_um, psf_kernel_sampling_um, psf_fwhm_um and psf_iterations.
Off by default; explicit Y/X calibration is required. A single captured kernel
applies independently to all selected segmentation channels, so its calibration
must suit every selected channel. These pipelines process projected2D fields;
this does not reconstruct a3D optical volume. Preflight validates the kernel.

V1 and V2 process private intensities after illumination and before background,
normalization and model inference. V1 handles each field before batch padding;
time-series frames are processed independently. Float intensities remain float,
raw stacks remain unchanged, and PSFs never operate on saved mask-label planes.
V1 publishes a complete staged archive set and invalidates old derived masks.
The application record captures channel order, exact kernel/file identity,
calibration, algorithm, RL parameters and completed fields. SHA256 checks of
all completed V1 NPZ files guard reuse; changed settings, tampered archives,
incomplete runs and switching PSF off require rebuilding with preprocessing on.
Cancellation leaves no partial published archive set or false completion record.
V2 preserves stored raw intensities and marks fields after durable mask writes.

216 focused PSF/GUI/preflight/illumination checks pass. Another104 settings,
Cellpose4training and host–pathogen checks pass (two GUI cases overlap the216).
During these checks, repaired missing shared categories for training inputs,
sampling and checkpoints, and moved Host–Pathogen settings into the canonical
category registry so cached category lists and help headings agree.

Full1998x1998public microscopy field processed with RL20on channels0and2 in
25.02s CPU; source/raw-stack bytes unchanged and archive reuse verified.
Actual Cellpose SAM CPU inference on a256x256crop completed in295.17s with
three labels. No GPU used. Explicit1um/px and2um Gaussian FWHM are verification
geometry, not measured microscope optics; no accuracy/recovery claim.
Receipt: features/data/490_psf_mask_pipeline_acceptance_2026-09-23.json.
Remaining: Measure intensity-source selection and durable quantitative provenance;
measured optical PSF/application acceptance; docs/translations/tutorial animation.

2026-09-23 — QUANTITATIVE MEASURE PSF INTEGRATION VERIFIED
Measure now has the same calibrated PSF parameters plus psf_measurement_source:
original (default) retains normal Measure intensities, including its standard
rescaling and registered preprocessing hooks; processed applies the captured
PSF after those stages before quantitative features. Original ignores stored
PSF parameters. Source files, mask labels and exported crops keep their existing
pixels. Fractional processed values remain float, including before GLCM texture
scaling; they are not truncated to integers.2D YX and3D ZYX kernels are supported;
a volume's PSF sampling must agree with Measure voxel calibration/anisotropy.

The immutable captured plan reaches spawned workers, rather than reopening a
possibly changed kernel file for every field. Stop is relayed through a process-
safe event while the parent awaits the field; cancellation escapes as cancelled,
not as an ordinary failed field. The existing intensity_rescale table gains
psf_measurement_source, psf_signature and psf_provenance through safe migration.
Every new field records channel order, actual input/output dtype, hooks/stage,
kernel/file identity, calibration, algorithm/RL settings and crop intensity source.
Existing base rescaling provenance remains separate and intact.

Changed kernels (including changed bytes at the same measured path), processed
versus original choices and algorithm settings cannot mix with existing
quantitative rows, even if resume is off. A project holding only crops can
start its first quantitative run. Resume compares captured active identity;
dormant new defaults do not block legacy original-intensity projects.

260pipeline/hook/resume/3D checks and275settings/GUI/preflight/Mask/numerical
checks passed. Focused follow-up checks cover process-safe cancellation, measured
file replacement, preprocessing order, legacy table migration and crop-only
projects. Actual spawned Measure run on the public1998x1998field completed in
22.05s CPU:104cells,120nuclei,37vacuoles and104cytoplasm rows. All104cell means
across channels0and2 matched independent processed-pixel means (maximum absolute
error0.0003894 intensity units). Original source and project copy unchanged;
resume0.37s added no rows, changed kernel refused. No GPU used.1um/px and2um
Gaussian FWHM remain declared verification geometry, not microscope calibration.
Receipt: features/data/490_psf_measure_acceptance_2026-09-23.json.

Remaining: ensure Mask Live preview honors PSF settings with its own cancellation
and provenance; measured optical PSF/application acceptance; documentation,
translations and the matching PSF tutorial/animation. Measure crop preview is
intentionally still a preview of unchanged exported source crops.
Final Measure follow-ups:33provenance/migration checks pass;5targeted cancellation/
full-entry checks pass, including an actual spawned worker stopped during RL.
An unresponsive worker gets a five-second cooperative grace period before the
parent exits through normal pipeline cancellation and cleans up the pool.

2026-09-23 — MASK LIVE PREVIEW PSF INTEGRATION
The background worker captures one calibrated kernel per request and applies
it to each selected intensity channel before background thresholding and
model normalization. Channels shared by several objects are processed once;
each segmenter receives its own copy. Invalid calibration fails before model
construction. Classical organelle previews need no Cellpose model at all.
Requests snapshot nested settings. Original loaded pixels and intensity filters
remain unchanged; labels never pass through PSF processing.

Cancel reaches PSF iteration/convolution boundaries. Native inference is allowed
to finish, with its obsolete result discarded. Closing the preview detaches and
parks an unfinished thread safely rather than destroying a running QThread.
Every new run has its own generation. Only accepted masks acquire the captured
processing/model details; failed or cancelled reruns cannot relabel old masks.
History stores complete PSF/kernel/settings identity, channel mapping and stage;
status and history tooltips expose the record. Classical history names its
algorithm rather than implying that Cellpose ran.

Preview normalization remains field-local, and the loaded field receives no
full-pipeline illumination correction. Both limits are stated explicitly in
the provenance and tooltip; this integration does not claim exact plate-run
mask equality. Real CPU Cellpose-SAM preview of a128x128public microscopy crop
completed in21.84s and returned two labels. Its exact model input matched the
PSF engine, source bytes stayed unchanged, and1956Qt ticks ran (maximum gap
0.557s). No GPU used.1um/px and Gaussian FWHM2um are verification geometry,
not microscope calibration. Receipt:
features/data/490_psf_live_preview_acceptance_2026-09-23.json.
Remaining: measured optical PSF/application acceptance, documentation,
translations and the matching PSF tutorial/animation.490remains in progress.
Validation:162preview/model/channel checks passed; additional coverage run had
197passes and one old shared-request identity regression, fixed by retaining
one captured pre/post mapping. Final15focused PSF/request checks all pass.
Earlier stale-token test failure was fixed; no failing check is claimed green.

2026-09-24 — REAL MEASURED OPTICAL PSF APPLICATION, CPU
Ran tools/verify_measured_psf.py against the public Zeiss LLS7 fluorescent-bead
volume and measured psf-200nm from doi:10.5281/zenodo.14203207 (Chiaruttini
and Guiet, CC-BY-4.0). Both downloads match the record's MD5; original SHA256s
remain unchanged. The raw volume is128x256x256uint16, with Z/Y/X spacing
0.2/0.14499219434521743/0.14499219434521743um. The PSF records
0.2/0.1450000152250016/0.1450000152250016um. The original mismatch is correctly
REFUSED. No sampling tolerance or application behavior was relaxed.

The acceptance script explicitly resamples a scratch image to the PSF grid
with a recorded center-aligned affine transform and linear interpolation.
It pads the original128x68x45measured PSF to129x71x45with zeros to place the
measured peak at the odd center, retaining every original value and total
signal before normalization. These are explicit preparation steps, not
new automatic runtime behavior. Both datasets retain raw non-deskewed axes.

Actual10iteration Richardson-Lucy completed in28.07seconds on CPU under6GiB,
with all10progress events. Output is float32, finite, nonnegative and the
same shape; processing input and source files remain unchanged. Kernel/file
identities, exact calibration and processing provenance are recorded in
features/data/490_measured_psf_application_2026-09-24.json, together with the
executed script's hash. A preliminary equivalent run took35.09seconds.

This closes the missing real measured-kernel application check. It does not
claim optical resolution, recovery accuracy or biological improvement:
98input voxels saturate, the beads have finite200nmextent, and the record's
best-settings PSF is not independently verified as an exact optical-setting
match to this bead acquisition. GUI/integration evidence remains the prior
checks; this is an additional3DCPU engine acceptance. API/tutorial/animation
and translation follow-up still belongs to the documentation lane.
