487 — GITHUB ISSUE AUDIT AND RESOLUTION
======================================
Status: IN PROGRESS. User explicitly requested all issues and discussions be
read, resolved and then commented on and closed. Snapshot2026-09-23:103 issues,
9 open. Pull requests excluded. Historical closure is not proof of current
correctness; check relevant code, tests and any unresolved trailing comments.

Runtime lane owns issue updates. Coordinate #130 with the documentation lane
in325; it owns API/tutorial/translation delivery. Never close an issue merely
because a related feature exists. Distinguish nightly fixes from released builds.

Priority update2026-09-23: the maintainer explicitly requires this item finished
before updating to1.5.1.0. It now precedes the version bump/main promotion.
Four issues currently open:127,129,130,131.73 was reclosed after581 focused
checks verified the classification decoder correction. The historical audit
remains incomplete. External prerequisites do not justify closing an issue.

2026-09-23: maintainer confirms131institutional ticket remains pending. Issue
updated with that status; no signed/notarized-installer acceptance claimed.
Historical first regression group361passed; second129passed. Covers ML control
selection/dispatcher/OpenMP guard, active learning, database path/read-only
behavior, figure fonts, remasking, AppleDouble handling, flow defaults, montage
path cost, report prompts, plaque preview recovery and interrupted-mask/archive
recovery.124follow-up comment supersedes its old two outstanding caveats:
adjusted masks are atomic and mask reuse validates with resume both on/off.
This is partial audit evidence, not a complete103-issue audit claim.

129CLOSED2026-09-23 after independent live audit against GitHub's current
published asset catalogue: packaging/release.py audit-published --site
https://einarolafsson.github.io/spacr/installers.html passes. Documentation
lane deployed separate main/nightly channels. Resolution comment5800101268.
Three issues remain open:127,130,131. Historical review remains in progress.

2026-09-23 — HISTORICAL116 FOLLOW-UP /118–122 VERIFICATION
Read116's remaining long run log and all discussion,118's full report and
119/120/121/122's reports and discussions.127focused checks pass for reruns
from orig/stack, test-mode isolation, preview channel columns, dynamically
shown segmentation settings, hand-drawn-mask measurements and annotation
contrast. Measurement-input tests run the actual Measure path on synthetic
fields and compare its database outputs. No native macOS acceptance claimed.
121follow-up comment5800358469 removes the obsolete advice to delete macOS
sidecars before Measure; existing5e0975316 and current tests cover that gap.

116's promised loader progress/cancellation was still missing. Added a Cancel
button and stage/database/crop progress, relayed safely to the GUI thread.
Cancellation stops queued coefficients and later worker stages; an ongoing
disk operation may finish. Source/setting changes and clear/shutdown also
retire progress and results. Late worker failures cannot overwrite a newer
load.184focused montage/UI/path-resolution checks pass, including a real
two-database load stopped before its second read, successful crop loading,
GUI responsiveness and late success/error/progress rejection.
This closes a historical follow-up, not the entire103-issue audit.

2026-09-23 — ISSUE7 THRESHOLD FILTER CORRECTED
The operator filter worked for ordinary pages/counts/skip but the measurement-
threshold route still interpreted it as pandas regex. It now uses the same
bound SQLite predicate as browsing, including NOT/AND/OR, parentheses and
case/wildcard semantics. Invalid expressions fail before expensive joins.
151focused annotation/filter/threshold and measurement-writer checks pass;
the latter revalidates15's concurrent inserts, schema widening and rollback.

133 — CLOSED after revalidating pointer-anchored zoom from d8fa55897.
128 — CLOSED after five real settings-dialog visibility/scrolling checks passed;
      existing fix7f04a5790 keeps OK/Cancel outside the scrolling form.
125 — CLOSED after fix44c59b714 and regression/visual checks; exclude highlight from shade instead of
      clearing the window backing store; highlight menu-bar action geometry;
      responsive card bounded to visible screen, scroll long explanations.
126 — CLOSED after fix44c59b714; actions wrap at full size, four layout cases pass.
127 — Seven separate requests: terminology; automatic reopen after update;
      functional in-app update; pixel diameter measurement; preview prominence;
      adaptable laptop layout; plaque cropping freeze. Audit each separately.
129 — Repository installer links synchronized to published1.5.0.9; public
      website still marks1.5.0.7 current. New live audit detects the remaining
      publication lag; keep open until Pages is refreshed and the audit passes.
130 — Home onboarding/sample project/module links: substantial implementation
      exists; documentation/media delivery and original tile failure need review.
131 — macOS signing: institutional request submitted by maintainer. Check
      automation; actual credentials/notarized build remain an external dependency.
132 — CLOSED after9a4afbcce and247 checks: rescore adjusted cells, exclude
      stale results, qualify heuristic explanations. Original cluster dataset
      biological quality was not validated; no raw images/settings supplied.

All initial open issue bodies and discussions have been read, including #132's
full console report. Historical issue audit is ongoing; no all-issues-complete
claim. Raw downloaded discussions remain outside the repo in task scratch.

GUI verification:160 related checks pass. Nine localized pathway checks expose
missing translations of a previously added import action; handed to docs lane,
not weakened or falsely counted as passes. Offscreen visual review completed.

2026-09-23: four initial open issues now closed with commit links and validation
evidence (#125/#126/#128/#133). Five remain open (#127/#129/#130/#131/#132).

2026-09-23 — ISSUE132 REPORTING FIX VERIFIED
Cell-mask adjustment now refreshes its QC card before merged arrays are made.
Stale/unreadable cards no longer contribute current field counts or findings;
incomplete readable coverage cannot produce a clean pass. Thresholds/severity
policy are unchanged. Explanations distinguish review heuristics from confirmed
causes, allow treatment/infection/layout differences, qualify illumination
correction, and restrict diameter advice to models that actually use it.
247 focused scoring, orchestration and offscreen GUI tests passed. The real
cluster images/settings were not supplied, so biological mask quality is not
claimed to be validated. These changes resolve the misleading/stale reporting
shown in132; they are nightly changes, not a newly published installer.

2026-09-23 — ISSUE129 SOURCE SYNCHRONIZATION / PUBLICATION STILL OPEN
GitHub published releases are the source of truth. release.py now has
sync-published (all README installer blocks and archive) and audit-published
(read-only, including optional --site URL). Pagination retains the full release
history; drafts/prereleases are excluded. A complete published installer set
sets the advertised version independently of a newer development package.
Actual published filenames/case are preserved. Partial platform releases remain
in the historical table without fabricated missing links.
Nightly README and nine translated download blocks now advertise1.5.0.9;
archive includes1.5.0.7/8/9. Nine missing small installers restored with SHA-256
verified against GitHub's asset metadata. All existing versions retained.
115 focused offline release/installer checks pass. The new network CI test
correctly FAILS on the currently deployed site: six missing assets for0.8/0.9
and an obsolete0.7 current marker. Earlier0.8 site estimate was its docs-build
label, not the archive's current installer. Docs owner notified; no deployment
claimed and129 remains open. Five of nine initial issues closed, four remain:
127/129/130/131. Historical reading through digest line345; audit incomplete.

2026-09-23 — ISSUE127 TERMINOLOGY ITEM COMPLETE
The settings-strip button now says Templates; its Help menu action, shortcut
reference, dialog title, explanations, file filters and error messages say
Settings templates/template consistently. Existing Recipe API identifiers,
spacr_recipe JSON version, storage paths and saved user files stay compatible.
New user-visible strings use tr(); documentation session owns translation/API
refresh.80 existing recipe persistence/dialog checks pass with the renamed
captions. The other six issue127 items remain under audit; issue stays open.

2026-09-23 — ISSUE127 PACKAGE UPDATE / RESTART FIXES VERIFIED
The GUI no longer calls an editable checkout's refused upgrade a success:
it explains the source location and git-pull route before cleanup/install.
Active analyses defer updating, and repeated Check clicks cannot start a
second updater while one is running. An accepted package update pins the
version offered and verifies that distribution in a fresh isolated interpreter.
Only verified success proceeds to a normal restart: save module/settings,
cooperatively close, then launch this interpreter's Qt entry after the old
event loop returns. Failed state saving or deferred shutdown leaves the window
open and does not launch a duplicate; failed installs do not restart.

99 focused updater/GUI checks pass (72backend/sequence/restart;27existing
window/update paths). A real isolated pip upgrade of checksum-verified published
wheels1.5.0.8→1.5.0.9 passed the new version probe. Dependencies were deliberately
not installed in that scratch environment, so this is package-update validation,
not a scientific-runtime acceptance test. A real offscreen Qt launch/update/
close cycle reached the replacement-launch boundary only after loopLevel()==0;
replacement process creation was intercepted to avoid opening another GUI.
Native macOS/Windows installer replacement/relaunch remains to be validated.
Issue127 remains open for those paths, layout/measurement audit and plaque freeze.

2026-09-23 — HISTORICAL73 REPRODUCED AND CORRECTED
Read all44 synthetic boom issues (28–70 and75): identical Error:boom traceback;
current process/session write guards validated. Read72/73/76/77/78 discussions;
37 existing guard/channel/backbone/best-checkpoint/F1 checks passed. The72
native resize stress evidence is historical, not a new native test here.

73 was reopened after reproducing direct-PIL uint16 clipping and format2
channel mismatch in both labelled/unlabelled loaders. New classification
datasets share crops.decode_crop_image: high-byte uint8 narrowing, declared
order and EXIF orientation. Checkpoints record crop_loading_policy; untagged
models explicitly retain historical stored-PIL inputs. Folder/tar inference,
held-out evaluation, activation maps and attribution search read that policy.
Resume/fine-tuning inherit it before loaders are built; direct training rejects
known loader/checkpoint conflicts. Fusion/distillation preserve the record and
reject conflicting source policies. No automatic reinterpretation of old weights.

Generated training folders and uniform PNG archives now carry format markers.
Mixed formats become declared uint8 destination copies, leaving sources intact.
Per-file migration state is respected. Root-only historical training markers
remain readable. No claim to infer the origin of already-unmarked format2 data.
Real image/checkpoint tests cover uint16,1/2/3formats,preloading,EXIF,tar folder
markers,migration state,actual saved-model predictions and one-epoch training
plus resumed second epoch. Focused regression evidence recorded with the commit.
No biological classification accuracy or GPU/native-platform validation claimed.

131: read maintainer's ticket-submitted comment; repository secret list is empty
and environments are github-pages/pypi. Build still signs app ad-hoc and only
optionally signs pkg, with no notarization. Institutional ticket status beyond
that comment is unknown. Remains open and outside1.5.1.0 until credentials exist.

2026-09-23 — ISSUE7 COMMENT / HISTORICAL READING CONTINUED
Posted the threshold-filter fix and151-test evidence on7:
https://github.com/EinarOlafsson/spacr/issues/7#issuecomment-5800931230
Read108's full settings/body and all three discussions; its path/font fixes
were already in the first historical regression cohort. Read110–114 bodies
and every distinct discussion (exact repeated reports verified equivalent),
including the alternate Python/Qt/Torch environment in114. Current ML code
retains both unmatched/empty-control and empty-grouped-split guards. Read117's
entire long console report and all discussions: AppleDouble discovery,
incorrect historical flow-default warning and expired AI authentication are
separate defects already covered by the earlier verification. The report
contains no demonstrated MPS kernel failure despite its Apple M4 title.
No new claim that all103issues have been audited;80–90 exact body-difference
reading and92/93 remaining detailed reports are still being completed.

2026-09-23 — ISSUE127 PLAQUE PREVIEW RESPONSIVENESS DEFECT FIXED
Reproduced legend CSV reads, annotation CSV reads and scale-bar inference on
the Qt event thread after detection returned. Initial review and subsequent
text reannotation now prepare on workers. Snapshot/generation guards discard
late success and late failure after image navigation; edits made during a
review retain their condition, calibration and approval. Annotation/legend
writes run on a serialized worker using captured rows; navigation does not
redirect the saved file or let an old completion replace current status.

116 focused Qt checks pass, including deliberately blocked sidecar I/O while
Qt timers continue, old-result/error rejection, delayed reannotation with a
new manual edit, and annotation save after navigation. A separate earlier
fix499 removed the pixmap/layout autozoom feedback. This is a verified GUI
blocking defect; the original reporter's exact cropping sequence is not
available, so it is not claimed as a reproduction of that entire incident.
Issue127 remains open for the rest of its acceptance and platform validation.

Historical80–90 exact body differences and discussions reviewed. Duplicated
tracebacks were compared after normalizing timestamps/run IDs; all distinct
settings/version/trace differences retained. Alternate controls include
None, empty quoted string and c2; location_column None; channels1/3; object
filters differ. They all reach the same empty-split failure addressed by the
existing ML control-selection and pre-split guards verified in earlier cohorts.
92/93 detailed reports remain to review; no full audit completion claim.

2026-09-23 — HISTORICAL READING FINISHED; RESOLUTION STILL OPEN
Read92 and93 bodies and every discussion, comparing repeated log blocks after
normalizing only timestamps/run identifiers.92 includes a separate maxvit_t
ML-dispatch traceback; current classify.resolve_ml_model_type uses the ML key,
and the earlier classifier cohort covers this.93's stale annotation column
is explicitly rejected with available columns and migration advice; current
ML analysis derives the training column locally. Earlier ML controls/merged
classifier regression cohorts cover the zero/missing-control and settings
preservation paths. Reconciled the complete103-issue snapshot against the
issue-number/title list: all bodies and discussions have now been read.

This completes reading, not resolution of every issue.127 still has remaining
layout/platform acceptance;130 needs finished/public onboarding delivery and
the reported dead Home-tile path verified;131 remains pending institutional
Apple signing access. Posted the verified plaque responsiveness work on127:
https://github.com/EinarOlafsson/spacr/issues/127#issuecomment-5802098915
No1.5.1.0 bump or main promotion yet. Do not close these issues as a proxy for
finishing the audit, and do not claim exact native-platform reproduction.

2026-09-23 — HOME NAVIGATION / LAPTOP PREVIEW ACCEPTANCE
Real Home tile clicks open Mask, Measure, Annotate, Classify and Toxoplasma.
Mask and Plaque Live preview collapse supporting panels, occupy the available
space and allow reopening/resizing Settings at1024x768 and1280x800. Found and
fixed Mask's excessive minimum width: actions now wrap and the image path
can shrink. Missing/unreadable workflow-map fallback also lost its input,
output and API descriptions; these now come from runtime module declarations
and GUI editing contracts. Fallback training includes Cellpose Workbench;
barcode mapping runs independently and converges on regression.
63 focused Linux/offscreen Qt checks passed. Public onboarding delivery and
native-platform acceptance remain distinct outstanding evidence; do not close
127/130 based solely on this cohort.131 institutional signing access pending.

2026-09-23 — NEW USER REPORT: PLAQUE RERUN AFTER SETTINGS CHANGE CRASHES
Requested: "running the plaquew model after changing the settings has atendency
to crahs spacr. cant model inference be run on a different thread?"
Inference already runs through JobRunner, but cancel clears its pending/busy
state before native inference exits, permitting another run against the shared
cached model. Investigating rerun serialization, settings snapshots and thread
lifetime. Current logs contain no matching recent fatal traceback, so do not
claim the exact crash reproduced. User asked whether Live preview or main Run.

2026-09-23 — PLAQUE INFERENCE RERUN DEFECT REPRODUCED AND FIXED
Two regression checks failed before the fix: Cancel plus changed settings
started a second inference while the first still ran, and two callers entered
the cached-model path concurrently (maximum concurrent calls=2). Both now pass.
Plaque rerun/well controls stay disabled until the previous QThread retires;
model construction/evaluation is serialized across plaque previews. Wells in
one batch use settings captured at batch start. Well errors now pass through
the same generation gate as successes, so an abandoned failure cannot replace
a new selection's status. Existing worker inference remains off the GUI thread.
110 focused checks passed; final control/message cohort13passed.

Real local cpsam_plaque_r5_v2 checkpoint on a published WT plaque crop also
passed: CPU,128x128, flow threshold0.4 cancelled then0.2 rerun, two worker-thread
inferences, no stale result, final15labels.823Qt timer ticks over17.28s, maximum
timer gap0.339s. Receipt: features/data/487_plaque_rerun_cpu_2026-09-23.json.
This validates rerun/thread lifecycle, not segmentation accuracy or the exact
reported native/GPU crash. No GPU used; issue127 remains open.

2026-09-23 — CPU-ONLY INSTALLATION DIAGNOSTIC FIX
Documentation's clean public1.5.0.9installation passed pip check/imports but
Doctor falsely diagnosed intentionally hidden CUDA as a driver mismatch.
Doctor now recognizes empty/-1visibility restrictions and explicit CPU choice,
leaves non-CUDA backends eligible, and recordsSKIPwithout probing hidden CUDA.
--no-gpu-probe skips explicit CUDA init, CUDA/XPU device-name queries and
indirect dtype allocations through accelerator.capabilities. Capability rows
reuse the diagnosis snapshot; normal runtime resolve() behavior is unchanged.
Also corrected a measured contradiction: CPU renderer was labelled GPU in
that table. Display acceleration now uses the same result as its explanation.
261 focused doctor/accelerator/Qt checks passed; final renderer cohort68passed.
Fresh real torch2.6.0+cu124process stayed CUDA-uninitialized for both probe
settings withCUDA_VISIBLE_DEVICES=''. Receipt:
features/data/487_doctor_hidden_gpu_2026-09-23.json.
This verifies nightly source changes; the published wheel does not yet include
them. Other installation and release gates remain separate.
