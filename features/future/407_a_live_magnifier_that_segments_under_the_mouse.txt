================================================================================
A LIVE MAGNIFIER IN MAKE MASKS THAT SEGMENTS WHAT IS UNDER THE MOUSE
================================================================================

Status:    DONE, 2026-09-21. Every point this item asked for, every
           DONE WHEN criterion under HOW TO KNOW IT WORKED, and the four
           things the 2026-09-20 notes left open are built and measured --
           see the 2026-09-21 note at the foot: the M key, the Overlap
           preview under Whole image, whole-image Cellpose on a CPU (float32
           weights, tile-by-tile progress, a Cancel that stops the model),
           and the box's picture at the largest sizes. On nightly and inside
           v1.5.0.8 before that: 4811b649b (the magnifier, 2026-09-14) and
           1078ca51f (the border option and the whole-image mode,
           2026-09-15). `_MAGNIFIER_SEGMENTERS`
           (spacr/qt/screens/make_masks.py) offers otsu, cellpose, four
           Cellpose 3 models, dinocell and samcell. The maintainer's second
           round is new/417_the_magnifier_round_two.txt, DONE.
           (Was: BUILT, ~97%, corrected 2026-09-20; BUILT, ~90%, corrected
           2026-09-19; before that FUTURE, filed 2026-09-15, "Not
           started".)
Requested: 2026-09-15, by the maintainer, in these words: "if the user flips
           this on, then a box appears with the region around the mouse
           slightly magnified, and in this region the square enhanced region
           is passed through some lightweight model (there should be options)
           and a mask for the objects in that region are shown live. if the
           user clicks on the image the objects in the enhanced region are
           saved on the image, so when the user toggles this feature off, or
           clicks save, etc., the objects are in the mask for that image."
Owner:     unassigned. `spacr/qt/screens/make_masks.py` (the screen),
           `spacr/qt/mask_engine.py` (pure-Python mask editing and
           persistence), and a new inference seam for the model.

--------------------------------------------------------------------------------
WHAT IS WANTED
--------------------------------------------------------------------------------

A toggle on the Make Masks editor. While it is on:

  1. A square box follows the mouse over the image and shows the region
     around the cursor, slightly MAGNIFIED.
  2. That region -- the enhanced crop, not the whole image -- is passed
     through a LIGHTWEIGHT segmentation model, and the objects it finds are
     drawn over the box LIVE, as the mouse moves.
  3. A CLICK commits the objects currently shown in the box into the
     image's mask. They stay there when the toggle goes off, when the user
     saves, when the user moves to the next field -- they are ordinary mask
     objects from that moment on, editable and erasable like any other.

Settings the maintainer asked for, by name:

    mode          which lightweight model runs on the region ("there should
                  be options")
    size          the diameter, in image pixels, of the region of interest
    zoom          the magnification -- and the MOUSE WHEEL CHANGES IT LIVE
                  while the feature is on
    sensitivity   the model's acceptance threshold, "like cellpose cell
                  probability score"

--------------------------------------------------------------------------------
WHY
--------------------------------------------------------------------------------

Curation is where mask quality is actually decided, and today a missed
object means drawing it by hand. This turns "the model missed three cells in
the corner" into three clicks, with the model working at a magnification
where small or dim objects are visible, and with the user choosing exactly
which objects to accept. It is also the natural front end for the
curation queue in 396: fields come from the queue, and missed objects are
added here rather than by a full re-run.

--------------------------------------------------------------------------------
DESIGN NOTES -- NOT DECISIONS
--------------------------------------------------------------------------------

THE MODEL NEVER RUNS ON THE GUI THREAD. HANDOFF and 284 are unambiguous that
a module screen must stay responsive; a model call on every mouse move is
the fastest possible way to break that. Inference belongs on a worker, with
only the NEWEST request kept -- a mouse that has moved on makes every queued
region stale, so a request superseded before it starts is dropped, not run.

THE REGION IS SMALL, WHICH IS WHAT MAKES "LIVE" POSSIBLE. A 128-256 px crop
is a few milliseconds on a GPU for the models below and tolerable on a CPU.
Throttle to the worker's own pace rather than to a fixed rate, and show the
last completed result with a subtle "updating" mark rather than blanking.

CANDIDATES FOR `mode`, all already adjacent to spaCR:

    Cellpose (cyto / nuclei, the small models)  -- spaCR's existing path;
        `sensitivity` maps directly onto cellprob_threshold
    DINOCell            -- future item 404; predicts Cellpose-style flows, so
                           the same post-processing applies
    SAMCell / MobileSAM -- future item 405; prompt-free on a crop, and a
                           natural fit for a click-to-accept interaction
    classical           -- threshold + watershed, for machines with no model
                           installed; also the fallback when a model import
                           fails, so the feature never simply does nothing

MAGNIFICATION IS FOR THE EYE AND POSSIBLY FOR THE MODEL, AND THOSE ARE TWO
DIFFERENT SETTINGS. Upsampling the crop before inference helps small objects
under models trained at a fixed object diameter; showing it magnified helps
the user see them. `zoom` should control the view; whether the model sees the
zoomed or the native-resolution crop needs measuring, not assuming.

COMMITTING IS A MASK EDIT, AND IT SHOULD GO THROUGH `mask_engine`. New label
ids must not collide with existing ones; an object overlapping an existing
object needs a rule (skip, replace, or clip to background) -- and the rule
should be a visible choice, not a hidden default. One click should be one
undo step.

COORDINATES. The box, the crop, the magnified view and the mask all live in
different coordinate systems; every commit should be tested for an off-by-one
at the crop edge and at the image border, where the region is clipped.

--------------------------------------------------------------------------------
OPEN QUESTIONS FOR THE MAINTAINER
--------------------------------------------------------------------------------

  * Which models must `mode` offer on day one, and which can wait?
  * On a click, commit ALL objects in the box, or only the object under the
    cursor? (The request says all; the second is a cheap addition.)
  * Overlap with an existing object: skip, replace, or clip?

--------------------------------------------------------------------------------
HOW TO KNOW IT WORKED
--------------------------------------------------------------------------------

  * With the toggle on, moving the mouse never drops the Make Masks screen
    below its responsiveness budget (the harness from 380 measures it).
  * Clicking commits exactly the objects drawn in the box, at the right
    pixels, including a box clipped by the image border -- a test with a
    synthetic image and a stub model that returns known labels.
  * The committed objects survive toggling off, saving, and reopening the
    field.
  * Removing every model leaves the classical mode working, rather than a
    feature that silently does nothing.

--------------------------------------------------------------------------------
2026-09-14 -- A FIRST VERSION EXISTS, ON BRANCH wip/407-live-magnifier
--------------------------------------------------------------------------------

Commit b9adbacb3, not pushed, not merged. Everything below is what that
branch does and what was measured on it; none of it is on nightly.

WHAT IT DOES

  * A checkable "Magnifier" button in the tool row (beside Cellpose-SAM
    detect) and a "Live magnifier" card on the settings panel: Mode, Size
    (px), Zoom, Sensitivity, Overlap.
  * While on, a box follows the cursor: the region contrast-stretched on its
    own and drawn zoom x the canvas's display scale, with the pixel under
    the cursor kept under the cursor. The last completed result stays drawn
    where ITS region lies; the frame goes dashed with "Updating..." until
    the result for the current region arrives.
  * The wheel changes the magnifier's zoom (at the canvas's Zoom per notch)
    and not the view. Right-button sweep and Shift/Alt pan still work.
  * A click pastes the objects the box outlines into the mask through
    `mask_engine._paste_region_objects`: ids start one past the top id, one
    click is one undo step and one "magnifier" ledger entry. A click on a
    region not yet segmented PINS that request on the worker, so moving on
    cannot supersede it, and its objects go in when it arrives.
  * Committed objects are ordinary mask edits: they survive the toggle going
    off and a save. Moving to the next field without saving still discards
    them, exactly as it discards a brush stroke -- navigation was not changed.

DECISIONS TAKEN FOR THE FIRST VERSION -- THE OPEN QUESTIONS ABOVE STILL STAND

  * Modes on day one: classical and Cellpose. Classical is Otsu when the
    region is two clear populations and a noise floor (median + 4 robust
    deviations, after sigma-1 smoothing) otherwise, then a watershed; it is
    also what runs, with a status-line note, whenever a model cannot load.
    Cellpose uses the Cellpose-SAM card's model and diameter. Adding
    DINOCell or SAMCell is one function in `_MAGNIFIER_SEGMENTERS`.
  * A click commits ALL objects the box outlines. Objects touching an edge of
    the box that lies inside the image are not offered (the recrop rule: an
    object cut by the box is not that object); objects at the image border
    are.
  * Overlap is a visible choice, default Clip (keep only unlabelled pixels,
    largest piece). Skip and Replace are the other two.
  * Sensitivity reads the way the word does: higher accepts more. For
    Cellpose it is cellprob_threshold with the sign reversed.
  * Defaults: size 128 (32-512), zoom 2.0 (1-8), sensitivity 0 (-6..6).
    Nothing persists between sessions, like every other setting on the panel.
  * Zoom is for the eye only; the model sees the native-resolution crop.
    Whether upsampling helps a model was NOT measured.

EVIDENCE

  * tests/qt/test_the_live_magnifier_segments_under_the_mouse.py, 21 passed:
    exact pixels from a stub model that reads its position from coded pixel
    values, both image corners, id collision and uint8->uint16 widening, clip
    and skip on the screen and all three rules at the engine, one undo per
    click, toggle-off/save/next/prev, newest-only and pinned requests, a
    click that waits for its result, the model never on the GUI thread (26
    moves against a 1 s model, slowest move well under 0.5 s, 2-3 model calls
    instead of 26), classical with Cellpose removed from sys.modules, the
    fallback note, wheel, lens geometry and the drawn frame.
  * Existing Make Masks suites: 533 passed, 1 failed -- the catalog-row test
    below. Docstring, API-symbol and module-coverage ratchets unchanged: no
    new public symbol, no new module.
  * Classical, synthetic 128 px regions, 5 seeds each: noise and a noise +
    gradient background 0 objects at sensitivity -2/0/+2; three disks at
    amplitude 3000/600/300 over noise 150 found 3/3; 36 confluent disks
    36/36; two touching disks 2/2; dark objects 3/3. At amplitude 150 (SNR 1)
    sensitivity 0 finds 0-1 and +2 finds 4-6 fragments. ~3 ms per 128 px
    region, 41 ms for 512 px.
  * Cellpose (cpsam 4.2.1.1) on CPU only, machine shared with other runs:
    load 5.4 s, one 128 px region 51-62 s, one 256 px region ~195 s, 3/3
    objects. On CPU the Cellpose mode is NOT live; it needs a GPU.

WHAT IS OWED BEFORE THIS CAN MERGE

  * Nine translations for the new captions. Until then
    test_i18n_caption_ratchet (3 tests, UI 3291 -> 3308) and
    test_every_help_string_on_the_panel_has_an_exact_catalog_row fail.
  * The 380 responsiveness harness was not run with the toggle on.

SECOND PASS

  * Preview the overlap rule inside the box (outline what a click would
    actually add, not what the model found).
  * While a CPU Cellpose call holds the shared model lock, Cellpose-SAM detect
    waits for it on the GUI thread -- up to one region's inference. Harmless
    on a GPU, a minute on this CPU.
  * Commit only the object under the cursor (a modifier-click), a keyboard
    shortcut for the toggle, and measuring whether models do better on the
    upsampled crop.

--------------------------------------------------------------------------------
2026-09-15 -- THE MAINTAINER'S ANSWER ON CLICK BEHAVIOUR, AND A NEW MODE
--------------------------------------------------------------------------------

Asked with the question tool whether a click should add every object the box
outlines, only the object under the cursor, or both with a modifier. Answer,
verbatim:

  "All the objects in the magnified box get added with the option to exclude
  objects touching the border that may not be completely in the area and with
  an additional mode that generates object on the entire image once then the
  user goes over with the mag box and clicks objects that are to be added and
  rich clicks to remove"

WHAT THAT ASKS FOR:
  1. Keep: a click adds ALL the objects outlined in the magnified box.
  2. An OPTION (a visible setting on the Live magnifier card) to exclude
     objects that touch the box's border, because they may not be completely
     inside the area. Today this exclusion is always on for the box's inner
     edge; it becomes the user's choice.
  3. An ADDITIONAL MODE: segment the ENTIRE image once; then the magnifier box
     shows those precomputed objects as the user moves over the image; a
     left-click ADDS the object(s) clicked, a right-click REMOVES the clicked
     object from the mask.

--------------------------------------------------------------------------------
2026-09-15 -- THE ANSWER BUILT: A BORDER OPTION AND A WHOLE-IMAGE MODE
--------------------------------------------------------------------------------

Branch wip/407-whole-image, first cut from nightly a08ea44a9 and rebased onto
nightly df1216b3f: code and tests first, reviewed records, ratchets and this
note next, and the runtime catalogs regenerated on the new base in their own
final commit. Not pushed.

WHAT EACH PART OF THE ANSWER DOES NOW

  1. KEPT. Under "Region under the mouse" (still the default) a left click
     adds every object the box outlines, one undo step, one "magnifier"
     ledger entry.
  2. "Exclude objects touching the box border", a checkbox on the Live
     magnifier card, ticked by default (the old behaviour). Unticked, an
     object the box's INNER edge cuts is offered and added as far as the box
     saw it. Objects at the IMAGE border are offered either way. Toggling it
     does not call the model again: the region worker keeps the model's last
     answer for the region and only re-filters it.
  3. "Segment: Whole image", a new row beside Mode. The chosen model runs ONCE
     on the entire field, on a second newest-only worker, with a busy bar and
     a Cancel button on the card. The box then draws its slice of those
     objects (the object under the cursor filled more strongly) with no model
     call per move.
       LEFT CLICK adds the one object whose label covers the clicked pixel,
       through the Overlap rule, as one undo step and a "magnifier" ledger
       entry with scope="image". A click on no object adds nothing and the
       status line says so.
       RIGHT CLICK removes the mask object under the click, whatever put it
       there, as its own undo step and a "delete" ledger entry with
       tool="magnifier". A right click on background changes nothing,
       records nothing, and says so.
       Changing a setting the model reads (Mode, Sensitivity, the Cellpose-SAM
       model and diameter, Bright, Min area) discards the whole-image objects
       at once, says so, and starts a new run. Size, Zoom and Overlap do not.

DECISIONS THAT HAD TO BE MADE

  * A whole-image click adds the WHOLE object, including any part outside
    the box: the whole image was segmented, so nothing cut it. The border
    checkbox is greyed out under Whole image for that reason.
  * A right click under Whole image removes one object at the press and does
    not sweep, so it removes exactly what was clicked.
  * A click before the run finishes adds nothing and is NOT pinned, unlike
    the region mode. A run can take minutes on a CPU, and an object going in
    long after the click would be a surprise.
  * Progress is an indeterminate busy bar. Neither classical nor Cellpose
    reports steps, and tiling the field to get real progress would cut
    objects at tile seams.
  * Cancel cannot interrupt a model call already running. It drops a waiting
    run and discards a running run's answer when it arrives. Moving the mouse
    does not restart a cancelled or failed run; a click or a setting change
    does.
  * A setting change restarts the run automatically. The worker keeps only
    the newest request, so a burst of changes costs at most one extra run.
  * Turning the magnifier off leaves a run going, so its objects are ready
    when it comes back on. Leaving Whole image for the region mode drops the
    run; objects already found are kept and offered again if nothing the
    model reads has changed.
  * A field change drops the whole-image objects and segments the new field
    when it loads. Coming back to a field segments it again.
  * Three existing captions became untrue and were reworded: the card
    subtitle, the Size tooltip and the Magnifier button tooltip. Their nine
    old reviewed records were retired (deleted, as the runtime lane requires).
  * "Magnifier added {n} object(s)" moved from an f-string to a tr() template
    so it is catalogued. It is shared by both modes.

EVIDENCE

  * tests/qt/test_the_live_magnifier_segments_under_the_mouse.py: 12 new
    tests, all red before the change (12 failed, 21 passed), 33 passed
    after. The border option on and off with an object the box edge cuts;
    image-border objects either way and no model call on toggle; Whole image
    segments once and never per move; a left click adds exactly the clicked
    object and a click on background adds nothing; a right click removes
    exactly one object and undo restores it; sensitivity and Min area discard
    the map while size, zoom and overlap do not; a 1 s stub model never
    blocks switching on or 26 moves (slowest well under 0.5 s), off the GUI
    thread; Cancel discards and a click restarts; save and field change; and
    the worker's drop_waiting.
  * Make Masks suites (42 files): 762 passed, 1 failed before the catalogs
    (the panel's exact-catalog-row test, expected until the rows exist).
  * 21 new captions x 9 languages, hand-written and accepted by the builder's
    gates through tools/write_reviewed_api_record.py
    (2026-09-15-magnifier-whole-image.json); evidence checker clean.
  * With the catalogs in, the full Make Masks suite (the 22 files named for
    make_masks, magnifier or mask_engine, serial, offscreen): 561 passed,
    1 skipped (the opt-in real-Cellpose detect test), 0 failed.
  * REAL CELLPOSE, MEASURED on a training field (plate1_A02_12, 1994x1994,
    outside every round-3 hold-out; cross-validation fold 3), on the RTX 3090
    under the GPU lock, driven through the screen itself (Cellpose mode,
    Whole image, magnifier on). 39 s for the whole script, lock wait
    included:
        stock cpsam        48 objects  10.3 s with model load
        cpsam_v2_toxo_r2   37 objects   7.7 s with model load
    A Sensitivity change discarded the objects at once and re-ran in 3.9 s
    and 3.7 s with the model cached (48 and 38 objects). A left click on
    the largest object added exactly it (5,050 px), a right click removed
    it, and undo restored it; the ledger held one "magnifier" and one
    "delete" entry.
  * THE GUI STALL, CHECKED. Turning the magnifier on returned in 2 ms. The
    longest gap between event-loop ticks during a run was 0.47 s, once,
    while the first model loaded (the worker's first import of torch and
    Cellpose); every other run stayed at 15-23 ms.

WHAT IS LEFT

  * The region mode's other f-string status lines (could not segment this
    region, the classical fallback note) are still not catalogued.
  * Not run: the 380 responsiveness harness with Whole image on, and
    whole-image Cellpose on a CPU, where a field this size would take far
    longer than on the GPU.
  * The busy bar has no ETA. The whole-image objects are not cached per
    field, so revisiting a field re-segments it.
  * Still open from the second pass above: previewing the Overlap rule inside
    the box, and a keyboard shortcut for the toggle.

--------------------------------------------------------------------------------
2026-09-19 — STATUS CORRECTED: BUILT AND SHIPPED, NOT "NOT STARTED"
--------------------------------------------------------------------------------

The Status line said "Not started" over two dated build notes. Checked
today:

    git merge-base --is-ancestor 4811b649b origin/nightly    yes
    git merge-base --is-ancestor 1078ca51f origin/nightly    yes
    git merge-base --is-ancestor <each> v1.5.0.8             yes
    grep _MAGNIFIER_SEGMENTERS spacr/qt/screens/make_masks.py
        :1605  classical, cellpose, dinocell, samcell

~90% is the share of what was asked that exists and runs; the rest is the
WHAT IS LEFT list above, none of which new/417 (round two) claims to have
done -- 417 itself records "Not measured ... under the 380 responsiveness
harness". This item stays in features/future/ until that list is either done
or declined. No test was run for this note.

--------------------------------------------------------------------------------
2026-09-20 — SIX OF THE SEVEN THINGS LEFT, AND A SIX-SECOND FREEZE NOBODY
              HAD MEASURED
--------------------------------------------------------------------------------

Decided: the maintainer asked on 2026-09-19 for both feature lists to be
finished, which for this item means the WHAT IS LEFT list above. Branch
home/407-magnifier-polish, cut from nightly 5800dd4bf and rebased once, mid-
run, onto 419 points 4-6, which renamed the magnifier's threshold mode from
Classical to Otsu. Not pushed.

WHAT WAS DONE

  1. THE REMAINING f-STRING STATUS LINES ARE CATALOGUED. Three sentences were
     built with f-strings and one was a bare literal, so nothing could
     translate them: the pinned-click notice, "could not segment this
     region: {error}", the fallback note, and "could not add objects:
     {error}" on the screen. Each is a tr() template with named values now.
     Three more -- the toggle's two sentences and the "nothing to add"
     refusal -- were already collected as sources, because the generator
     reads a setText literal, and were shown without tr(): the rows existed
     and nothing used them.
     The fallback note also named the mode by its internal key, so a user who
     had chosen "Cellpose 3 · cyto3" was told cellpose3:cyto3 could not run
     and had no row to look for. `_magnifier_mode_label` names it the way the
     Mode box does, translated, through `canonical_magnifier_mode` so a saved
     session saying `classical` is still named.

  2. THE 380 HARNESS HAS A MAGNIFIER ROW AND IT HAS BEEN RUN WITH WHOLE IMAGE
     ON. It could not be run before: the harness measured the backdrop, a
     theme change and three settings-panel interactions, none of which is the
     magnifier. `tools/perf_paint.py --only magnifier` opens a synthetic
     field on a real Make Masks screen, turns the magnifier on in each scope
     and times forty mouse moves, each one event delivery plus a repaint, in
     the Otsu mode because it needs nothing installed.

       2048 px field, three runs, load average 18-21, Whole image still
       segmenting throughout:
         region       median 5.1-6.2 ms  slowest 10.2-13.1 ms  0 frames dropped
         whole image  median 2.1-2.8 ms  slowest  4.1-6.9 ms   0 frames dropped
       1024 px field, quietest run (load 5):
         region       median 4.8 ms      slowest 11.9 ms       0 frames dropped
         whole image  median 2.3 ms      slowest  3.9 ms       0 frames dropped

     NO BUDGET BREACH ON A MOVE AT THE DEFAULT BOX. One hitch remains and is
     reported on its own: the first move after the magnifier is switched on
     costs 17-34 ms, one or two frames, once -- the worker's first import of
     the segmentation stack.
     THAT SENTENCE WAS TOO BROAD, and the second note of 2026-09-20 corrects
     it: the row measured one box size on a field with no mask file, which is
     the one case in which neither the Overlap rule nor most of the lens
     costs anything. At the largest box item 417 allows the same move cost a
     median 183.9 ms. See that note for what it is now and what is left.

     THE LOAD AVERAGE NEARLY TURNED THIS INTO A FALSE REGRESSION. The first
     reading had the region scope at a median 17.5 ms against the base's
     13.0. Three alternating runs of base and branch said 12.6/16.5,
     15.2/10.5 and 4.8/5.4: the machine's load, which ran between 5 and 25
     while other sessions built, and not the branch. The load is now written
     onto the row and not only into the record's environment.

  3. A SIX-SECOND FREEZE ON A CLICK, FOUND BY TRYING TO DRAW THE OVERLAP
     RULE. The rule ran a whole-region comparison per object and, under Clip,
     a whole connected-component pass per object as well. Item 417 let the
     Size box go as high as the image is wide, so the cost is the region's
     pixels times its objects, on the GUI thread, with the click waiting:

       region  objects  clip                skip               replace
       128 px       10    0.4 ms (was 0.3)    0.3 (0.3)          0.3
       256 px       30    1.3 ms (was 7.2)    1.1 (1.2)          0.9
       512 px       60    5.2 ms (was 44.8)   4.4 (6.1)          3.6
      1024 px      200   29.3 ms (was 595)   22.2 (60.5)        17.4
      2048 px      500  117.9 ms (was 6085)  87.4 (821)         72.1

     It is counted once over the pixels now, in
     `mask_engine._surviving_region_objects`, which the paste calls and the
     box draws. The ids added are identical at every size above, and
     `test_the_overlap_rule_agrees_with_the_rule_it_replaced` runs the old
     rule written out object by object beside the new one over random
     regions, for all three rules and three Min areas. That guard earned its
     keep at once: the first version used `scipy.ndimage.label`, which labels
     connected runs of TRUE, so two touching objects became one piece; it
     failed on two pixels of one seed. `skimage.measure.label` labels
     connected runs of one VALUE, which is what a piece of an object is.

  4. THE OVERLAP PREVIEW, under Region under the mouse. The pixels the rule
     would take away keep a quarter of their alpha: solid is what a click
     adds, ghosted is what it does not. Nothing is computed under Replace,
     which takes nothing away, or over an empty mask. Changing the rule asks
     no model and does not discard the whole-image objects.
     IT IS BUILT ON THE WORKER, beside the outlines, since 2026-09-20 -- the
     first version ghosted inside paintEvent. See the second note of that
     date.

  5. AN ETA ON THE BUSY BAR, from a measurement and only from one: the
     seconds per megapixel the last run under this mode and model took. The
     FIRST run of a session promises nothing and the bar stays indeterminate,
     which is honest and also right -- on a cold model most of a first run is
     the load, measured here on 2026-09-15 at 10.3 s against 3.9 s for the
     same field with the model cached. An estimate that runs out goes back to
     indeterminate rather than counting past zero.

  6. A PER-FIELD CACHE OF THE WHOLE-IMAGE OBJECTS. Leaving a field and coming
     back re-ran the model: 3.7-10.3 s on the RTX 3090 by this item's own
     measurement, minutes on a CPU, and a curator walking a plate goes back
     and forth. A finished answer is kept under the FIELD and every setting a
     model reads, so a settings change still discards it. The field could not
     be named before -- a run key opens with a generation counter that counts
     LOADS -- so `set_field` names it by the image file's path. What is kept
     is bounded by `memory_budget.what_to_drop`, the policy every other cache
     in the application is trimmed by: the user's idle timeout releases a
     field nobody went back to, and the ceiling is the lower of the user's
     cache ceiling and 256 MB.

WHAT IS STILL LEFT, AND WHY THIS IS NOT IN features/new

  * A KEYBOARD SHORTCUT FOR THE TOGGLE. Not done, and the reason is worth
     recording because it is a two-minute change behind a gate. Binding any
     key obliges a `ShortcutSpec` in `spacr/qt/shortcuts.py`
     (`test_every_bound_key_is_on_the_map`), and a spec's label enters the
     COMPACT caption layer, which is hand-written: it needs a nine-language
     `_row` in `spacr/qt/i18n.py` and a move of `COMPACT_CAPTION_COUNT` and
     `COMPACT_CAPTION_SHA256` in tests/qt/test_i18n_caption_ratchet.py. The
     label cannot be "Magnifier" or "Live magnifier": both are external
     sources already, and `test_compact_and_generated_caption_owners_are_
     disjoint` refuses a caption owned by both layers. This run was told not
     to write catalog rows and that a consolidated pass runs at the end, and
     a ratchet is to be subtracted from rather than bumped, so it is left
     whole for that pass. The recipe: one ShortcutSpec ("M", a new label,
     "Make Masks", "the Make Masks screen"), one QShortcut beside the others
     in `_install_shortcuts`, one row in `SHORTCUT_HINTS`, nine `_row`
     values, and the two pins.
  * THE OVERLAP PREVIEW UNDER WHOLE IMAGE. A click there adds one whole
     object, most of which can lie outside the box, and the rule's answer for
     it cannot be read off the box's slice: Skip asks whether the object
     touches the mask anywhere, and Clip keeps its largest surviving piece,
     which may be outside. It needs the object's whole extent --
     `scipy.ndimage.find_objects` once per run would give every bounding box
     for the cost of one pass -- and was left undone rather than approximated
     into a box that would sometimes lie.
  * WHOLE-IMAGE CELLPOSE ON A CPU, still not measured. No GPU job was needed
     or queued for this unit; everything above is CPU and offscreen.

EVIDENCE

  * tests/qt/test_the_live_magnifier_segments_under_the_mouse.py: 30 new
    tests. The four magnifier suites together, 128 passed.
  * tests/qt/test_the_paint_harness_can_see_a_second_layer.py: 3 new tests
    for the new row, including the calibration that a frame shrunk to a
    tenth of a millisecond makes every move read as dropped. 8 passed.
  * The 32 files named for make_masks, magnifier, mask_engine or the paint
    harness: see the commit bodies for the count of the run each was taken
    from.
  * RED, EXPECTED, AND MEASURED AGAINST THE MERGE BASE (82d9b5f6f):
    `test_every_help_string_on_the_panel_has_an_exact_catalog_row` goes from
    90 missing rows to 99 -- eleven sources in nine locales, against ten.
    The nine added are ONE tooltip -- Overlap, which gained a sentence about
    what the box now shows -- in NINE locales: the old wording ("Replace lets
    the new object take every pixel it covers.") is in all nine catalogs
    under docs/i18n/reviewed/runtime/, so the rewording orphans nine rows and
    not eight. The other 90 are 419's and 423's rename of Classical to Otsu,
    red on this branch's base before this branch existed.
    (CORRECTED 2026-09-20. This block first said 80 -> 88, "the ninth locale
    was missing that row already", and named the wrong base. Every figure was
    wrong and the debt was understated by one locale, which would have left
    one language with the Overlap tooltip untranslated after the
    consolidated pass. Counted again, per locale, from the test's own
    output.)
    AND AFTER THE REBASE ONTO origin/nightly 5c8369fed, which is the number
    anyone running this test on nightly will see: 144, sixteen sources in
    nine locales. Item 435 landed five more captions of its own on 2026-09-20
    and its note records the list going from ten to fifteen; this branch adds
    exactly one source to it, the reworded Overlap tooltip, which is the same
    nine rows. Measured on the rebased branch, per locale, not assumed.
  * i18n owed, listed exactly: five new UI sources --
      "Magnifier: segmenting this region — its objects are added as soon as
       the box is up to date."
      "Magnifier could not segment this region: {error}"
      "Magnifier: {mode} could not run ({reason}); the Otsu mode is
       segmenting instead."
      "Magnifier could not add objects: {error}"
      "about {seconds} s left"
    and one reworded source, the Overlap tooltip.

--------------------------------------------------------------------------------
2026-09-20 — THE REVIEW'S FIVE, AND THE BOX MEASURED AT THE SIZE THE USER
              IS ALLOWED TO SET IT TO
--------------------------------------------------------------------------------

Decided: the maintainer asked on 2026-09-19 for both feature lists to be
finished; this pass is the review of the note above, applied. Nothing here
changes what was asked for -- it fixes what the last pass got wrong, and
measures the one thing it had measured in the only case where the answer is
free. Same branch, home/407-magnifier-polish, rebased onto origin/nightly.

1. THE HARNESS WAS WRITING TO THE MAINTAINER'S OWN SETTINGS. `measure_
   magnifier` drives a real Make Masks screen, and opening a folder is
   remembered: `_open_folder` ends in `prefs.push_recent_source`, which
   writes $HOME/.config/spacr/qt.conf. The row runs by default and the tool
   documents itself as `python tools/perf_paint.py`, so a run by hand put the
   harness's own TemporaryDirectory at the head of the recent-folder list and
   left `last` pointing at a path that had already been deleted. Every
   measurement now runs inside `_settings_elsewhere`, which points QSettings
   at a throwaway folder and puts each format's location back afterwards --
   reversible rather than an env var, so a harness called from a test session
   leaves that session's sandbox where it found it.
   `test_the_magnifier_row_leaves_the_users_own_settings_alone` pins it, and
   was calibrated the way this harness's other tests are: with the isolation
   neutered it fails.

2. THE BOX'S PROMISE IS BUILT ON THE WORKER NOW. The ghost -- the rule's
   answer, drawn as faded pixels -- was computed inside paintEvent, over the
   box's pixels, per delivered result. The rule is the same one whose cost is
   in the table above: 125.5 ms at 2,048 px under Clip, on the GUI thread,
   while the mouse moves. It is `_ghosted_overlay` on the region worker now,
   beside the outlines it fades; the mask crop it needs travels in the
   request, as the image crop always has, and one QImage is still built per
   result because the ghost IS the picture rather than a layer over it.
   A rule change or an edit to the mask under the box makes that promise
   stale: the request carries the rule and a mask generation number, the box
   notices when it is drawn, the frame goes dashed -- the vocabulary it
   already had for "not up to date" -- and the region is asked for again. The
   model is not: the worker keeps its last raw answer for the same box and
   settings. A drag's frames ask for no ghost at all, since nobody draws
   them.

3. THE HARNESS COULD NOT SEE ANY OF THAT, which is why the last note could
   say "no budget breach" and be wrong. It wrote one field and no mask file,
   so `load_image_and_mask` made a mask of zeros, so the rule had nothing to
   take away and the preview never ran in any harness run; and it measured
   one box size, 128 px, when item 417's whole point was that the Size box
   goes as high as the field is wide. The field is opened with half of every
   object already painted in now, and the largest box is a row of its own.
   Measured, 2048 px field, 40 moves, load 11, Otsu, Clip, ghost built:

     box      before this note              after
     128 px   median  6.7 ms  worst  12.1   median  3.9 ms  worst   8.8
     2048 px  median 183.9 ms  worst 391.2   median 53.4 ms  worst 110.9
                          23 frames dropped               6 frames dropped

4. WHAT THE 2,048 px ROW ACTUALLY WAS, and it was not the ghost. The box is
   not bounded by the window: its side is the Size box's value in IMAGE
   pixels and the lens draws it `zoom` times larger again, so on that field
   most of the lens is off the widget -- and every pixel of the box was being
   percentile-stretched, blended with the mask and handed to Qt on every
   move. `_visible_part` cuts that to the part of the box that is on the
   canvas, and `_stretch_for_box` splits "read the levels" from "rescale
   these pixels": the levels still come from the WHOLE region the box
   magnifies, from at most 262,144 of its pixels on a grid, so the box does
   not change contrast as it slides off the canvas edge. At a box the window
   holds, every pixel is sampled and every pixel is returned and the answer
   is `normalize_uint16`'s to the byte --
   `test_a_box_that_fits_is_stretched_exactly_as_it_always_was`.

5. STILL OVER BUDGET AT THE LARGEST BOX, and this is the honest end of it.
   53.4 ms is three frames, not one. Profiled at 2,048 px, per move, the
   remainder is `mask_engine.overlay_mask` over the 1.4 Mpx of box that IS on
   the canvas: 41.5 ms of float passes over 4.2 M elements, against 6.8 ms
   for the stretch, 0.4 ms for the model's crop copy and 0.2 ms for the mask
   crop. A magnifier set to the width of the field is the whole window, and
   rendering a megapixel per mouse move is what that means. THE FIX IS NAMED
   AND NOT DONE: at that size the box is CONSTANT -- `_magnifier_box` clips
   to the field for every cursor -- so the picture could be built once per
   (box, mask, levels) and every later move would be a clipped blit of a
   sub-rectangle instead of a re-render. It is left out of this pass because
   the miss that builds it is a 150 ms frame of its own and choosing when to
   pay it is a design question, not a patch.

6. THE ETA NO LONGER LEARNS ITS PACE FROM THE RUN THAT LOADED THE MODEL.
   `_note_pace` kept whichever run finished first under a (mode, model), and
   on a model that loads, that is the cold one: 10.3 s against 3.9 s for the
   same field, this item's own measurement. The second run of a session --
   the first one that shows a countdown -- therefore counted down from an
   estimate about 2.6x too long and finished while the bar still promised
   seconds. A mode that loads a model now spends its first measurement
   learning that the model is in memory; the bar stays indeterminate through
   the second run and counts down from the third. The Otsu mode loads
   nothing, so its first run is still its pace.

7. `_tick_magnifier_eta` reached into `_magnifier._image_estimate` for the
   other half of an answer it had just been given publicly.
   `estimated_seconds()` is beside `remaining_seconds()` now.

The i18n debt in EVIDENCE was recounted and was wrong in every figure; the
correction is in that block, with what it would have cost the consolidated
pass. Tests: the four magnifier suites 131 passed, the paint harness 10
passed (3 new), the comment guard 41 passed. STILL OPEN, unchanged: the
toggle's keyboard shortcut, the Overlap preview under Whole image, and
whole-image Cellpose on a CPU -- plus, new and measured, the box's picture at
the largest sizes (5 above). ~97%, still features/future.

--------------------------------------------------------------------------------
2026-09-21 — THE LAST FOUR: A KEY, A PROMISE, A RUN THAT STOPS, A PICTURE
              THAT KEEPS UP
--------------------------------------------------------------------------------

Decided: the four STILL OPEN at the end of the second 2026-09-20 note, each
done and measured. Branch magnifier-407-0921, cut from nightly bfb2c7d99.
Every number below was taken on this machine (AMD Ryzen 9 5950X, 32
threads, the RTX 3090 not used: CPU only, offscreen) while other sessions
held it at a load average of 48-103. The load is written beside each
number because it moves every one of them.

1. THE TOGGLE'S KEY IS M. Bare, on the Make Masks screen only, like B E W D
   V Z R: it is the first free letter of "magnifier", it clashes with
   nothing in SHORTCUTS or SCREEN_SHORTCUTS, and it carries no Ctrl, so it
   carries no Command on macOS, where Cmd+M minimises the window.
   `MakeMasksScreen._toggle_magnifier_key` presses the tool-row button
   itself (`toggle()`), so the status line, the box and the button's look go
   through the button's own handler; a disabled button is not pressed by its
   key. It is on the shortcut map (SCREEN_SHORTCUTS, scope "the Make Masks
   screen") and on the screen's own shortcut panel.
   THE CATALOG GATE, AND WHY IT COST NO TRANSLATION. The 2026-09-20 note
   recorded a recipe of nine hand-written `_row` values and two moved pins,
   because every shortcut label entered the compact caption layer and the
   compact layer refuses a caption the generated layer also owns. The label
   is "Live magnifier" -- the card's own title, already a generated source
   with a reviewed row in all nine languages. `_shortcut_caption_fields`
   (tests/qt/test_i18n_caption_ratchet.py) now leaves copy the generated
   catalog already owns to that catalog, and the new
   `test_every_shortcut_caption_has_exactly_one_translated_owner` holds
   such copy to a real row in every language, so leaving a label to the
   generated layer cannot become a way of leaving it untranslated. Measured,
   before and after: the compact surface is 236 captions with digest
   3e23d765..., the generated UI inventory 3,955 with digest 69d5a08f...,
   both identical to the base; the Make Masks panel's exact-catalog-row test
   reports the same 49 lines of missing rows on the base and on the branch.
   NO NEW i18n DEBT from this note -- no caption was added or reworded.

2. THE OVERLAP PREVIEW UNDER WHOLE IMAGE. The object under the mouse -- the
   one a click adds -- is drawn solid where the rule keeps it and ghosted to
   a quarter of its alpha where the rule takes it away, the region mode's
   vocabulary. `_LiveMagnifier._image_promise` computes the rule over the
   OBJECT'S OWN BOUNDING BOX, not the box's slice, with
   `mask_engine._surviving_region_objects` -- the function, over the same
   window, that the click's paste runs -- so Skip sees a touch 50 px off
   the box and Clip knows its largest piece lies outside it. The bounding
   boxes are one `scipy.ndimage.find_objects` pass on the worker
   (`_MagnifierResult.extents`, which `_single_object` now reads too,
   instead of a whole-field comparison per click). The promise is kept per
   (object, rule, mask) and redrawn when any of them changes; no model is
   asked. The whole field's outlines are built once on the worker
   (`_MagnifierResult.overlay`) and sliced per move rather than outlined
   again, so the memory budget now counts a kept result's labels AND its
   picture (`_MagnifierResult.nbytes`).
   The Overlap tooltip still says the box shows the rule "under Region
   under the mouse". It is incomplete rather than false, and rewording it
   would orphan nine reviewed rows; left for the consolidated pass.

3. WHOLE-IMAGE CELLPOSE ON A CPU, MEASURED, THEN MADE USABLE.
   Measured first, cpsam 4.2.1.1 through this screen's own
   `load_cellpose_model` and `cellpose_detect`, field plate1_A02_12
   (1994 x 1994, channel 0), torch at 16 threads:
     one 256 px tile, bfloat16 weights (the default)  162-197 s   load 64-100
     one 256 px tile, float32 weights                  31-46 s    load 87-100
       (alternating, same process, same load)
     the whole field, float32, before this change could stop it:
       100 tiles, median 26.8 s a tile, 3,106 s in all (51.8 min), of which
       365 s after the last tile started -- that tile and the flow dynamics
       over 4 Mpx; 91 objects; load 86
     the same field in bfloat16, extrapolated from the tile: about 4.5 h.
   The Ryzen has no native bfloat16 arithmetic, and Cellpose-SAM loads its
   weights in bfloat16 by default. What changed:
   a. FLOAT32 ON A CPU. `load_cellpose_model` -- this screen's loader, used
      by the magnifier and the Object detection button -- passes
      use_bfloat16=False when `cellpose_kwargs()` says the device is not a
      GPU. On a GPU nothing changes. The pipeline's own CPU runs still load
      bfloat16: that is `spacr.accelerator.cellpose_kwargs`, shared by eleven
      call sites, and changing what every CPU mask run computes is the
      maintainer's decision, not this item's. It is the same measurement.
   b. PROGRESS FROM THE FIRST TILE. A whole-image request carries a
      `_RunTicket`; `_counting_tiles` puts a forward pre-hook on the model's
      network for the length of the call, counting tiles against
      `_cellpose_tile_count`, which is `cellpose.core.run_net`'s own
      arithmetic and is tested against run_net itself (six shapes and
      diameters, one of them this field: 100). The busy bar fills by tiles
      and says "about N s left" from THIS run's pace, so the first run of a
      session -- the one the per-megapixel ETA had to stay silent through --
      has an estimate from its second tile. After the last tile it says
      only a percentage: the dynamics are not tiles and are not guessed.
   c. A CANCEL THAT STOPS THE MODEL. The same hook raises `_RunCancelled`
      before the next tile once the ticket is cancelled; `_segment_region`
      lets it through instead of falling back to Otsu, and the delivery is
      dropped without a word. Cancel, leaving Whole image, a field change,
      a setting change that starts a new run, and closing the screen all
      cancel the run on its way. Before, the model went on holding the
      processor and the shared model lock to the end of the run.
      End to end on this CPU, real cpsam, a 600 x 600 crop of the same
      field: 9 tiles predicted and counted, float32, "about 304 s left" on
      the bar at tile 2 (72.5 s in), Cancel stopped the run 37.1 s later --
      the tile already in flight -- and the hook was off the network.
   d. NOT DONE, ON PURPOSE: spaCR-side tiling, which cuts objects at the
      seams (the 2026-09-15 decision stands, and Cellpose already tiles --
      it is those tiles the bar counts), and an automatic downscale, which
      changes the answer. The Object detection diameter is the downscale
      that exists: 60 rescales by one half and the same field is 25 tiles
      instead of 100 (`_cellpose_tile_count`, tested against run_net).
   A CPU whole-image Cellpose run is still most of an hour on this machine
   under this load; it is now an hour that says how long it has left and
   stops when asked, instead of four and a half that did neither.

4. THE BOX'S PICTURE AT THE LARGEST SIZES, THE DESIGN DECISION. The
   2026-09-20 note named a cache: build the picture once per (box, mask,
   levels) and blit sub-rectangles. Not taken: the box is NOT constant at
   the largest size -- a side of 2,048 on a 2,048 px field still starts at
   cursor - 1,024, so it moves with the mouse, and the levels are read from
   it -- and the mask changes with every edit, so that cache would miss on
   exactly the moves a user is working through, each miss a whole-field
   render. Instead every move is made cheap and nothing is cached between
   moves: `_box_picture` turns the stretch and the 8-bit conversion into one
   look-up in a 65,536-entry table built per levels (`_box_grey_table`),
   straight into Qt's native RGB32 pixel, and does the alpha-0.5 blend as a
   carry-free integer byte average only where the mask has an object.
   BYTE-IDENTICAL to the two float passes it replaces
   (`test_the_fast_box_picture_is_the_slow_one_to_the_byte`, uint16 and
   uint8, three seeds, three pairs of levels, 700 labels; and end to end
   through paint). It stays exactly as sharp: nearest-neighbour, one picture
   pixel per image pixel at every zoom that magnifies; only below one device
   pixel per image pixel is the picture thinned, to what Qt would have drawn
   anyway (`_picture_step`). Micro-benchmark, 1,400 x 1,050 part of a
   2,048 px field with 600 objects, load 88: 225-270 ms for the two passes,
   21-41 ms for `_box_picture`.
   THE HARNESS, `tools/perf_paint.py --only magnifier --field-px 2048`,
   which now also measures the largest box under Whole image. Three runs of
   the base (bfb2c7d99) and three of the branch, alternating, 40 moves each:

     row                 base median / worst       branch median / worst
     region  128 px      35.2-78.6 / 77.6-160.8    23.0-33.2 / 47.6-99.9
     image   128 px      15.7-29.5 / 46.5-69.0     14.8-23.1 / 45.4-61.1
     region 2048 px     379.2-500.2 / 716.6-1509   62.1-97.7 / 168.8-389.2
     image  2048 px     362.1-412.1 / 723.1-877.2  53.2-78.4 / 142.9-171.7
       load 59-69 (base), 48-60 (branch); every row ghosted=True

   Five to eight times faster at the largest box, in both scopes. On the
   branch the largest box costs 2.0-3.3x the default box in the same run;
   the default box measured 3.9 ms median on a quiet machine on 2026-09-20,
   which puts the largest at about 8-13 ms -- inside one frame. THAT IS AN
   INFERENCE, NOT A READING: this machine was never quiet today, and the
   absolute numbers above are the load's as much as the code's. The
   remaining per-move cost at 2,048 px, profiled: `_box_picture` about half
   of the repaint, then Qt's own drawing (3-7 ms per image, measured
   separately), the canvas's own paint, and the region worker's 2,048 px
   Otsu competing for the processor.

WHAT IS LEFT, NONE OF IT BLOCKING THIS ITEM
  * A quiet-machine run of the harness row above, to replace the inference.
  * The Overlap tooltip's wording (point 2), for the consolidated i18n pass.
  * Whether the pipeline should load cpsam in float32 on a CPU too (3a).
  * The Object detection button still runs Cellpose on the GUI thread, and
    on a CPU it therefore freezes the screen for the length of a run. That
    is the button's, not the magnifier's, and it is older than this item.

DONE WHEN, AGAINST HOW TO KNOW IT WORKED:
  * responsiveness with the toggle on -- measured by the 380 harness in both
    scopes and at both box sizes; the largest box is five to eight times
    faster than it was, and inside a frame by inference (see 4);
  * a click commits exactly the objects drawn, border-clipped boxes
    included -- the 2026-09-14 tests, and now the whole-image promise,
    pixel for pixel, for every rule;
  * committed objects survive toggling off, saving and reopening -- tested
    since 2026-09-14;
  * with every model removed the Otsu mode still works -- tested since
    2026-09-14.
  DONE.

EVIDENCE
  * tests/qt/test_the_magnifier_finishes_what_407_left_open.py: 35 tests --
    the key (5), the whole-image promise (7, including one per rule that the
    promise is what the click adds), tile counting against run_net (6),
    cancel and progress with a counted stand-in network (6), and the
    picture's byte-identity, thinning and end-to-end paint (11).
  * The 41 files named for make_masks, magnifier, mask_engine, the paint
    harness, hotkeys or shortcuts, serial, offscreen: 991 passed, 2 skipped,
    1 failed -- `test_every_help_string_on_the_panel_has_an_exact_catalog_row`,
    red on the base with the identical 49-line list.
  * tests/qt/test_i18n_caption_ratchet.py: three tests red on the base are
    red on the branch for the same captions (other items' debt); the new
    owner test passes.
  * Screenshots, offscreen, a 1024 px crop of plate1_A02_12 with half of
    every other object already painted, Clip: the region box at 224 px and
    at the largest size (1,024), and the whole-image box at 160 px and at
    the largest size, the hovered object solid where a click adds it and
    ghosted over the half the mask already owns.
