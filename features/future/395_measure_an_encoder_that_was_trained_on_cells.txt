================================================================================
MEASURE AN ENCODER THAT WAS TRAINED ON CELLS, BECAUSE THE ONE THAT LOST WAS
TRAINED ON PHOTOGRAPHS
================================================================================

Status:    OPEN AND UNBLOCKED, 2026-09-14. Ordinary work, no longer waiting
           on anybody. Filed 2026-09-12; corrected 2026-09-13 when the
           retrieval harness that produced the baseline turned out not to be
           in this repository; unblocked 2026-09-14 by the maintainer's answer
           below.
Owner:     unassigned. `spacr/embeddings.py` holds `EmbeddingSpec` and
           `_timm_encoder`; the harness has to be rebuilt under `tools/`.

           THE BLOCKING QUESTION WAS ASKED AND ANSWERED. Asked: which plate
           produced the 2026-09-12 baseline? Answered 2026-09-14, verbatim --
           "Don't remember -- rerun from scratch".

           SO THE BASELINE IS ABANDONED, DELIBERATELY AND ON THE RECORD.
           0.695 and 0.894 are not reproducible: the harness is gone (searched
           2026-09-14 across /media/carruthers/mnt3/claude and
           /media/carruthers/mnt1 -- no file named for retrieval or
           gene_retrieval anywhere, and every hit for the literal strings
           "0.894" and "0.695" is a coincidental value in an unrelated CSV)
           and the plate is not remembered. A rebuilt harness is a new split
           on possibly different data.

           WHAT THAT CHANGES ABOUT THE SUCCESS CRITERION, and it is the whole
           point of this note: this item used to require a number "reported
           beside 0.695 and 0.894, ON THE SAME PLATE, the same objects and the
           same folds". THAT CRITERION IS RETIRED. It cannot be met. Anyone
           who prints a new number next to 0.695 is comparing two measurements
           that share nothing but a name.

           THE REPLACEMENT CRITERION IS A FRESH PAIR. Rerun BOTH arms --
           embeddings AND the measured panel -- in the same rebuilt harness,
           on one named plate, in one session. The claim this item exists to
           test is "an encoder trained on cells beats an encoder trained on
           photographs", and that is a comparison INTERNAL to the new run. It
           does not need the old numbers and must not borrow them.

           STEP 1 IS THEREFORE THE HARNESS, and it lands under `tools/` WITH
           THE PLATE NAMED IN IT, so that the next person to ask "which plate"
           reads the answer instead of filing this note again.

--------------------------------------------------------------------------------
WHY THIS EXISTS
--------------------------------------------------------------------------------

386's retrieval measurement ran on 2026-09-12 and the default encoder LOST to
the hand-designed panel by about twenty points of balanced accuracy:

    gene retrieval, embeddings          0.695
    gene retrieval, measured panel      0.894
    position control, embeddings        0.494
    position control, panel             0.514

The measurement is sound -- the position control reads chance across all
twenty adjacent library-column pairs, the shuffled nulls read chance, the
split is grouped by well, and the obvious excuse was falsified (restricting
the panel to the encoder's three channels WIDENS the gap rather than closing
it). So the number is real and it is not an artefact.

WHAT IT IS A NUMBER ABOUT IS THE POINT. It was measured with
`EmbeddingSpec(backbone='resnet18')` -- ImageNet-supervised weights, a
network trained to tell a dog from a bicycle, applied to fluorescence crops.
That is the weakest thing the module can be pointed at and the least
interesting encoder to have measured. 386 could not say this because it had
not run anything; now it has, and the honest reading is "this encoder loses",
not "embeddings lose".

--------------------------------------------------------------------------------
WHAT TO DO
--------------------------------------------------------------------------------

REBUILD THE HARNESS FIRST, then point `EmbeddingSpec` at a backbone trained
on microscopy. Candidates, in the order worth trying:

    Cell-DINO / DINOv2 fine-tuned on cells
    OpenPhenom
    SubCell

REVISED 2026-09-14. This section used to say "re-run 386's measurement
UNCHANGED", which was the right instruction while the harness existed. It does
not, and the maintainer's answer is "rerun from scratch" -- so the instruction
is now:

  BUILD ONE HARNESS AND RUN EVERY ARM THROUGH IT. The design is still 386's
  and none of it should change: grouped-by-well split, the twenty adjacent
  library-column pairs as a position control, shuffled nulls. What changes is
  that the MEASURED PANEL is re-run too rather than read off this file. Both
  arms, one harness, one plate, one session -- that is what makes the
  comparison mean anything now that the old pair cannot be reproduced.

  The four figures above are HISTORY, not a baseline to beat. They record what
  was measured on 2026-09-12 on a plate nobody remembers, and the new numbers
  sit beside them as a separate run rather than in the same table.

  The position control must be run for every arm: a different encoder is a
  different chance of finding position instead of biology.

--------------------------------------------------------------------------------
HOW TO KNOW IT WORKED
--------------------------------------------------------------------------------

REWRITTEN 2026-09-14. The first bullet used to read "reported beside 0.695
and 0.894, on the same plate, the same objects and the same folds". That is
unsatisfiable -- see the Status block -- and an unsatisfiable criterion is how
an item stays open forever or gets closed dishonestly.

  * BOTH ARMS COME OUT OF ONE RUN. The cell-trained encoder AND the measured
    panel, same plate, same objects, same folds, same harness, reported
    together. The comparison is internal to the new run.
  * THE PLATE IS NAMED, in the harness and in the result. This item exists in
    its present form because the last one was not.
  * The position control is run for every arm and still reads chance. Without
    that the new number means nothing, for the same reason the old one would
    not have.
  * resnet18 IS ONE OF THE ARMS. It is the cheap way to find out whether the
    new harness reproduces the old finding at all: if resnet18 scores far from
    0.695 on the new plate, that is information about the harness and the
    data, not about encoders, and it must be reported rather than quietly
    absorbed.
  * Whatever the result, it is written into 386 as a NEW TABLE beside the
    2026-09-12 figures rather than replacing them or merging with them. Two
    encoders measured the same way is the useful artefact; two numbers from
    two harnesses in one column is the thing this file spent two days
    preventing.

A RESULT THAT IS STILL BELOW THE PANEL IS A SUCCESS FOR THIS ITEM. The
question is what a cell-trained encoder scores, not whether it wins, and
"the panel is better on this task too" is a finding worth having written
down once rather than assumed either way.

--------------------------------------------------------------------------------
WHAT THIS DOES NOT COVER
--------------------------------------------------------------------------------

Not "the interesting result" -- whether an embedding separates something the
panel does NOT. 386 says plainly that retrieval of a KNOWN phenotype is the
wrong instrument for that, because the positive control is the screen's top
hit precisely because the panel already detects it. That needs a different
design and a human looking at crops, and it is not this item.

--------------------------------------------------------------------------------
2026-09-13 -- THE HARNESS IS NOT IN THE REPOSITORY, SO THIS IS NOT ONE LINE
--------------------------------------------------------------------------------

This file says "the engine, the screen, the column family and the measurement
harness all exist and all have been run end to end. What is missing is one line
of configuration and a second run."

THREE OF THOSE FOUR ARE TRUE. `spacr/embeddings.py` holds `EmbeddingSpec`,
`EmbeddingResult`, the channel policies and the `emb_` column prefix;
`spacr/qt/screens/embeddings.py` is the screen; the column family reaches the
pickers. Pointing the spec at another backbone really is one line.

THE FOURTH IS NOT. Searched 2026-09-13:

    tools/           no embedding/retrieval harness of any name
    spacr/           `balanced_accuracy` appears in scorecard, surrogate, ml,
                     regression_annotation and classifier_evaluation -- general
                     utilities, none of them the 386 measurement
    tests/           nothing that reproduces the split or the control
    features/data/   no 386 or 395 artefact, no run summary, no folds

So the thing that produced

    gene retrieval, embeddings      0.695     position control  0.494
    gene retrieval, measured panel  0.894     position control  0.514

on 2026-09-12 -- the grouped-by-well split, the twenty adjacent library-column
pairs, the shuffled nulls, and the falsified three-channel excuse -- exists
only as those numbers. The plate is not named in either file.

  THIS MATTERS MORE THAN THE MISSING SCRIPT. This file's own success criterion
  is "reported beside 0.695 and 0.894, ON THE SAME PLATE, the same objects and
  the same folds". That cannot be satisfied by a harness written fresh, because
  a new harness is a new split -- and the item says so itself: "changing the
  encoder and the measurement in the same step produces a number nobody can
  attribute."

WHAT IS ACTUALLY OWED, in order:

  1. RECOVER OR REBUILD THE HARNESS and commit it under `tools/`, with the
     plate named. Until it is in the repository the baseline is unreproducible
     and every future comparison inherits that.
  2. Only then swap the backbone. `BiomedCLIP-PubMedBERT_256-vit_base_patch16_224`
     is already in the local HuggingFace cache and is a domain-shifted encoder
     that needs no download -- a cheap first probe, though not one of the three
     this file names (Cell-DINO, OpenPhenom, SubCell).

  SAME SHAPE AS ITEM 400, FOUND THE SAME DAY: work that was really done, whose
  artefact is not reachable from the checkout, and whose file says it is ready.
  An item is only as actionable as the code a reader can find.
