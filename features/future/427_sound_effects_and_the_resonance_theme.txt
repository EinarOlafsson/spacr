================================================================================
SOUND EFFECTS, BACKGROUND MUSIC, AND THE RESONANCE THEME
================================================================================

Filed as 418 on 2026-09-18; renumbered 2026-09-19 because 418 also names features/new/418_mask_filters_objects_by_min_and_max_intensity.txt.

Status:    IN PROGRESS -- PARTS A AND B OF THREE BUILT 2026-09-19, part C
           not started. Built: the sound engine (spacr/qt/sound.py), the
           synthesiser and its cache (spacr/qt/sound_synth.py), the Sound tab
           in Preferences (spacr/qt/sound_preferences.py), ONE reference
           sound set "Orbit", the 32-bar composed music bed with its kick and
           shaker, a music file of the user's own, and the Resonance
           backdrop (spacr/qt/resonance.py, ResonanceEngine in
           spacr/qt/widgets/ambient.py). Not built: the ten themes (part C).
           Everything is off by default and the backdrop is one animation
           among seven. Part A was reviewed the same day and the five nits it
           found are fixed. THE BED GAINED A MELODY AND A SIDE-CHAIN THAT
           CAN BE HEARD on 2026-09-19 -- the sentence this item exists to
           answer is "melodic space house", and until then the bed had the
           space house and not the melodic. See the four 2026-09-19 notes
           at the end; the last two list what is still open, including
           whether section 2's sound for a run STARTING is wanted at all.
           Renders for the maintainer's ear have now been sent; until he
           says, NOBODY HAS STILL REALLY HEARD WHETHER IT IS ANY GOOD.

Shape:     A Sound tab in Preferences, LAST tab, everything OFF BY DEFAULT.
           Interface feedback sounds, start/stop sounds for a run, optional
           background music, and a theme whose background is driven by whatever
           is playing.

--------------------------------------------------------------------------------
WHAT IT IS
--------------------------------------------------------------------------------

1. INTERFACE FEEDBACK.
   A small sound on click and on hover, in the spirit of the iPhone click:
   short, quiet, unobtrusive, felt more than heard. The point is confirmation
   that a control registered, not decoration.

2. RUN SOUNDS.
   A distinct sound when a run STARTS and another when it STOPS. These earn
   their place: a spaCR run is long, the user is usually in another window, and
   an audible "it finished" is the one sound in this list with a real job. The
   stop sound should differ for finished-successfully and stopped-with-an-error
   -- a user who hears "done" and finds a traceback will not trust the sound
   again.

3. BACKGROUND MUSIC.
   A separate option from the feedback sounds, with its own switch: someone who
   wants click feedback while working in silence must not get music too.
   Decided 2026-09-19: no streaming service and no bundled recordings. The
   music is a looping bed SYNTHESIZED by spaCR in the theme's key (so nothing
   carries a licence question), or a local audio file the user picks.

4. THE RESONANCE THEME.
   A new theme whose background is a visualised FLOOR with particles on it. The
   particles flow, gather into shapes and bounce in response to the sound's
   resonance -- amplitude and pitch -- like a digital Chladni plate. Chladni
   figures are the right reference: real resonance patterns on a vibrating
   plate are exactly particles settling at the nodes, so the physical analogy
   gives a principled rule for where particles should go rather than an
   arbitrary one.

--------------------------------------------------------------------------------
THE PART THAT DECIDES WHETHER THIS IS ANY GOOD
--------------------------------------------------------------------------------

EVERYTHING OFF BY DEFAULT, AND IT MUST STAY THAT WAY. A scientific tool that
makes noise the first time it is opened, in a shared office or during a talk,
is a tool people learn to distrust. Off is the default, and the tab is last
because it is the least important thing in Preferences.

HOVER SOUNDS ARE THE RISKY ONE. A sound on every hover, on a dense settings
panel, becomes a stream of clicks as the pointer crosses the window. If it is
built: debounce it hard, only on interactive controls, never on a control that
is disabled, and give it its own switch separate from click feedback so it can
be turned off alone.

AUDIO MUST NOT TOUCH THE RUN. Sound belongs on the GUI thread's own timeline
and must never block, delay or fail a pipeline. A missing audio device, a
busy PulseAudio, or no sound stack at all -- headless, a cluster, SSH -- must
degrade to silence without a warning dialog and without an exception.

THE VISUALISER NEEDS A FRAME BUDGET. A particle field reacting to audio is a
per-frame loop competing with a GUI that is already doing image work on the
same machine, sometimes while the GPU is training. It needs a hard cap, a
setting to reduce or disable it, and it must yield: if frames are being
dropped, the visualiser is what gets cheaper, not the image canvas.

--------------------------------------------------------------------------------
OPEN QUESTIONS -- ANSWER BEFORE BUILDING
--------------------------------------------------------------------------------

  ALL ANSWERED 2026-09-19 -- see "Decided:" at the end. In short:

  - What plays the sound? Qt Multimedia's QSoundEffect, on its own thread,
    falling back to silence when Qt Multimedia or a device is missing.
  - Where do the sounds come from? Synthesized in code with numpy, so there
    is no licence question at all.
  - What feeds the visualiser? spaCR's own sounds, or a local audio file the
    user picks (part B).
  - Does the Resonance theme have to work with no audio at all? Yes: it
    idles beautifully in silence (part B).

--------------------------------------------------------------------------------
WHY IT IS FILED RATHER THAN BUILT
--------------------------------------------------------------------------------

Requested 2026-09-18 as a list item, explicitly not to be implemented.

--------------------------------------------------------------------------------
Decided: 2026-09-19, by the maintainer, through the question prompt
--------------------------------------------------------------------------------

  "Ok build as recomended. But skip the Spotify link and integration
  entirely, instead I want you to make 10 themes for spacr that should all
  try and capture a space house theme vibe. Artists I like include worakls,
  NTO, rivière monk, BIRRD."

  and, separately: "The theme should sound like melodic space house".

The recommendation accepted: off by default; click, hover and run-finished
sounds SYNTHESIZED IN CODE (no licence question), played with Qt's
QSoundEffect, falling back to silence; the visualiser reacts to spaCR's own
sounds or a local audio file the user picks, and idles beautifully in
silence. No streaming service anywhere. Split into three parts: A the sound
engine, the Sound tab and one reference sound set; B the music beds and the
Resonance visualiser; C the ten themes.

--------------------------------------------------------------------------------
2026-09-19 — PART A BUILT: THE ENGINE, THE SOUND TAB, AND "ORBIT"
--------------------------------------------------------------------------------

WHAT EXISTS NOW

  spacr/qt/sound_synth.py       Qt-free. A SoundTheme (key, scale, tempo,
                                progression, timbre numbers) renders every
                                sound with numpy/scipy into 48 kHz 16-bit
                                stereo WAVs under ~/.spacr/sounds/<key>-<hash>
                                (SPACR_SOUND_CACHE moves it). The hash covers
                                SYNTH_VERSION and every theme field, so a
                                stale file can never answer; older folders of
                                the same set are removed.
  spacr/qt/sound.py             The engine. Nothing is imported, built or
                                installed while sound is off. When on: a
                                QThread "spacr-sound" owns every QSoundEffect
                                and does every render; the GUI thread only
                                emits queued requests. An application-wide
                                input filter exists only while click or hover
                                is wanted. Run sounds come through
                                notify.announce_pipeline_finished, i.e.
                                AppScreen._on_finished.
  spacr/qt/sound_preferences.py The Sound tab, LAST in Preferences (after the
                                launcher-only Fractal tab too): Play sounds
                                (off), Volume (50 %), Sound set, and a switch
                                plus a Preview button for Click (on), Hover
                                (off), Run finished (on), Run failed (on) and
                                Music bed (off). Everything but the master is
                                greyed while the master is off.
  spacr/qt/preferences.py       get/set_sound_enabled, _volume, _theme,
                                _event_enabled, sound_bed_rests; the page is
                                built, saved and reset with the dialog;
                                apply_preferences_to_app hands off to the
                                engine only once sound has been switched on.

THE REFERENCE SET, "ORBIT" -- melodic space house in A minor, 122 BPM.
Warm pads of five detuned PolyBLEP saws (+-16 cents) through a 4th-order
low-pass at 1.1 kHz that breathes to 2.2 kHz; plucks built from harmonics
that die faster the higher they are; a dotted-eighth ping-pong delay whose
repeats darken; a sine sub with a touch of second harmonic; a synthetic
3.2 s darkening reverb. Measured on the samples
(tests/qt/test_the_sounds_are_melodic_space_house.py):

  click x4   A4 C5 E5 B4 plucks, -12 dBFS peak, echo 0.369 s later (a
             dotted eighth at 122 BPM), 1.3 s with its tail
  hover x4   E5 G5 A5 C6, muted and shorter, -20 dBFS peak, no echo
  finished   16ths climbing the VII triad (G B D) two octaves, a whole tone
             up onto A5 with A4 under it, the pad swelling from G into
             Am(add9) (+5.4 dB), a sub entering on the resolution; 4.9 s
  failed     slower eighths falling E5 D5 C5 A4 over a closed Dm7 that never
             resolves, spectral centroid 256 Hz against the finished
             sound's 409; 4.8 s
  bed        8 bars Am7-Fmaj7-Cmaj7-G7, pumping pads, an eighth-note arp
             with dotted-eighth delay, root sub; 15.74 s, loops with no
             seam (jump 0.013 at the join, p99 sample step 0.025), -13 dBFS
             peak, and played at 0.6 of the other sounds' level

MEASURED, WITH THE SANDBOX RULES (offscreen, private HOME, volume 0)

  * Sound off: launch never imports spacr.qt.sound (26 ms cold) or Qt
    Multimedia; apply_sound_preferences reads one setting (36 us).
  * Save with sound switched on: 406-416 ms, against 400-410 ms with it left
    off -- the engine adds under 10 ms.
  * A GUI STALL FOUND AND FIXED. The first QSoundEffect built on the audio
    thread still stopped the GUI thread for 180-320 ms. Listing the devices
    first (QMediaDevices.defaultAudioOutput) on the audio thread did the
    same 185-201 ms connection with the GUI thread's worst gap at 5.2-8.3
    ms. The first probe of this had reported 10.3 ms because it excluded the
    tick before the work started; the notes record how.
  * Switching sound on: first ever (scipy import + render on the audio
    thread) worst GUI gap 34-52 ms, ready after 1.3-1.8 s. With the files
    already rendered: about 30 ms once per process, ready after 0.22-0.32 s;
    a second switch-on inside the same process is 5.4 ms, the tick itself,
    ready in 3 ms. THE "5.3 ms, 0.26 s" ROW FIRST WRITTEN HERE WAS THE
    SECOND CASE LABELLED AS THE FIRST, corrected after review; the 30 ms is
    the audio thread's one-off connection to the sound server and lands
    +192 to +282 ms in. See docs/notes/spacr/qt/sound.md, which also records
    why the Preferences dialog is the wrong place to measure this.
  * With no sound server at all (PULSE_SERVER=unix:/nonexistent): every
    effect loads with Status.Error, no exception, no dialog, silence. Qt
    prints its own "playback of this format is not supported" line.
  * The input filter costs 0.97-1.00 us per delivered event while installed.
  * A SEGFAULT FOUND AND FIXED while writing the tests: deleteLater on the
    filter, then Python collecting the engine before the event loop ran,
    crashed in ~QObject at the next sendPostedEvents. Children are now never
    deleteLater'd; see docs/notes/spacr/qt/sound.md.

TESTS: tests/qt/test_the_sounds_are_melodic_space_house.py (38),
tests/qt/test_sound_is_silent_until_asked.py (41),
tests/qt/test_the_sound_tab_is_last_and_off.py (12), and the Sound tab added
to tests/qt/test_preferences_tabs.py. The audio sink is faked throughout.
Every absence the tests assert was mutation-checked: removing the rule turns
its test red, except two redundant early-outs that are documented as such.
The last dialog test switches sound on with the mouse, presses Save, and
hears the click from a button in another window.

WHAT IS LEFT

  * Part B: the music beds proper (more than the one reference loop, a
    local audio file the user picks) and the Resonance visualiser.
  * Part C: the ten melodic-space-house themes, which slot into
    sound_synth.SOUND_THEMES; the Sound set combo already lists whatever is
    there.
  * Translations are owed for every new caption and tooltip on the Sound
    tab and every new public docstring; the consolidated i18n pass will
    carry them. SoundTheme labels and descriptions reach tr() as attributes,
    so the extractor needs a rule for SOUND_THEMES before part C's ten
    descriptions can be translated.
  * Not heard by a person yet. The numbers say what was asked for; whether
    it sounds like Worakls or like a test tone is the maintainer's ear.
  * The frozen bundle will now pick up Qt Multimedia and its FFmpeg
    libraries through the import in _make_effect; its size was not measured.
  * A SOUND WHEN A RUN STARTS, WHICH SECTION 2 ASKS FOR AND PART A DID NOT
    BUILD, and nobody had written down which of those two was meant. The
    recommendation the maintainer accepted names "click, hover and
    run-finished"; part A's own list is click, hover, run finished, run
    failed and the music bed. So a start sound was never in part A, and it
    has not been dropped either -- it is an open question for the
    maintainer, not a decision anybody has made. The argument against it:
    the run sounds earn their place because the user is in another window
    and the run is long, and the user who just pressed Run is looking at
    the screen. The argument for: it confirms the press did something. If
    it is wanted, it is one more entry in FEEDBACK_EVENTS, one figure in
    sound_synth and one row on the tab.
  * Windows and macOS audio backends are untested; only Linux/PipeWire
    through Qt's FFmpeg backend has been run.

--------------------------------------------------------------------------------
2026-09-19 — PART A AFTER REVIEW: FIVE NITS, ALL FIXED
--------------------------------------------------------------------------------

The review found no correctness or safety problem and five nits. All five
are fixed; four were real and the fifth turned out to be a measurement this
file had got wrong.

  1. "Nothing is imported while sound is off" was true at launch and not
     afterwards. notify.announce_pipeline_finished imported spacr.qt.sound
     unconditionally, so the first run that ENDED in any session paid 20.4
     ms on the GUI thread for a user who had never asked for sound, and
     SoundPage did the same on every Preferences close. Both now ask
     sys.modules first, the way apply_preferences_to_app already did.
     Nothing is lost: without an engine both calls return at once, and the
     engine is only ever built by apply_sound_preferences.

  2. THE "every later start: 5.3 ms, ready after 0.26 s" ROW WAS THE WRONG
     CASE. Re-measured four times in fresh interpreters against a warm
     cache: the first switch-on in a process costs about 30 ms (30.0, 30.2,
     31.6 and 9.5 ms) and is ready after 0.22-0.32 s. 5.3 ms is what a
     SECOND switch-on inside one process costs. The reviewer was right and
     the original probe measured through the Preferences dialog, where
     Save's own 390-460 ms of unrelated work runs with no event-loop tick
     inside it and hides the audio thread's connection entirely. The row
     and the notes now carry both cases and say which is which.

  3. Pressing a Preview button played the click sound as well as the
     previewed one -- on the Click row, the same pluck twice. The Preview
     buttons now carry the Qt property sound.SILENT_PRESS_PROPERTY
     ("spacrSilentPress"), which the input filter reads on a left press on
     a pressable widget and nowhere else.

  4. Two more things the music bed's Preview got wrong. Switching the
     master off in the dialog only greyed the rows and left nine seconds of
     bed playing underneath a switch that said sound was off. And a
     Preview taken while the STORED bed was playing silenced it after the
     fade until the dialog closed, because the fade's last step and the
     bed's stop were the same stop. The last fade step now emits
     bed_faded and the engine answers it by putting the stored settings
     back on the bed.

  5. tests/conftest.py sandboxes every other resolver that writes under the
     real ~/.spacr -- the run journal, the plate queue -- but not
     sound_synth.sound_cache_root, whose fallback is Path.home(). The three
     sound test files each point the cache somewhere of their own, so
     nothing had been written there; the first test that built a
     SoundEngine without saying where would have. SPACR_SOUND_CACHE is now
     set in pytest_configure, before collection, which also reaches the
     subprocess tests.

Tests: 5 new ones, all mutation-checked (each guard removed on its own
turned its own test red). tests/qt/test_sound_is_silent_until_asked.py 44,
tests/qt/test_the_sound_tab_is_last_and_off.py 14,
tests/qt/test_the_sounds_are_melodic_space_house.py 38.

Still open, and recorded above rather than decided here: nobody has
listened to "Orbit"; the run-START sound of section 2; the frozen bundle's
size with Qt Multimedia in it; Windows and macOS.

--------------------------------------------------------------------------------
2026-09-19 — PART B BUILT: THE COMPOSED BED, AND THE RESONANCE BACKDROP
--------------------------------------------------------------------------------

Decided: 2026-09-19, by the maintainer, through the question prompt —
"The theme should sound like melodic space house" — with the artists named
as the reference (Worakls, N'to, Rivière Monk, BIRRD), and part B's own
brief: a procedural composer rendering a seamless 32-bar loop at 120-124
BPM from parameters, with a soft four-on-the-floor kick and shaker that can
be turned down to near-silent, checked by ANALYSING the render rather than
by listening to it; and a Chladni-figure visualiser driven by the bed's
precomputed envelope and spectrum in sync with playback position, or a
local WAV the user picks, with no system-audio capture.

WHAT EXISTS NOW

  spacr/qt/sound_synth.py       SYNTH_VERSION 2. The bed is a 32-bar
                                arrangement: BED_SECTIONS gives it four
                                eight-bar sections (drift, pulse, lift,
                                return) and bed_plan ramps every part to
                                its section's level over the section's
                                first half, the FIRST section ramping from
                                the LAST one -- so the plan is a circle and
                                bar one continues bar thirty-two. A soft
                                kick (_kick) and an eighth-note shaker
                                (_shaker) come and go with the sections and
                                leave the render entirely below PART_FLOOR.
                                loudness_lufs is the gated integrated
                                measurement of ITU-R BS.1770-4 and
                                _to_loudness sets the bed's level with it.
  spacr/qt/resonance.py         NEW, Qt-free, numpy-only, no scipy. The
                                audio analysis (envelope, four band
                                energies, spectral centroid, onset with a
                                release) written beside the WAV as a
                                ~90 kB .npz; the Chladni plate (lattice,
                                settle, MODES); and the NowPlaying record
                                the audio thread writes and the backdrop
                                reads.
  spacr/qt/widgets/ambient.py   ResonanceEngine, a seventh animation. It is
                                in AMBIENT_THEMES, so the Animation
                                preference offers it, None still means no
                                widget at all, and every existing ambient
                                test sweeps it.
  spacr/qt/sound.py             The worker measures and analyses the bed
                                BEFORE it plays it, and records the instant
                                the file is LOADED rather than the instant
                                play() returned. A music file of the user's
                                own is what the bed plays and what the
                                backdrop follows; a chosen file that has
                                gone falls back to spaCR's own.
  spacr/qt/preferences.py       sound/music_file, empty by default.
  spacr/qt/sound_preferences.py A Music file row: an editable field and a
                                Browse button.
  tools/perf_paint.py           --animation, so 380's harness can measure a
                                named ambient animation instead of whatever
                                the default is.

MEASURED (offscreen, private HOME, sandboxed cache)

  THE BED, 32 bars at 122 BPM, A minor, i-VI-III-VII:
    62.95 s, 12.09 MB as 48 kHz 16-bit stereo, 3.3 s to render once
    -18.00 LUFS against the -18 asked for; peak -9.13 dBFS; no sample at
      or past full scale
    loop seam 0.0024 / 0.0034 against a p99.5 sample step of 0.0397
    low (35-90 Hz) / mid (150-1500 Hz) = 0.44
    by section: drift -18.8, pulse -18.0, lift -17.3, return -18.0 LUFS
    kick 98.4 % below 160 Hz; the shaker adds 6.2 dB to the 4-12 kHz band
    with kick_level and shaker_level at 0: the same piece without drums,
      no kick or shaker notes in the schedule at all

  THE BACKDROP:
    shade 1.59 ms at 1080p, 1.72 ms at 4K (buffer 320x180)
    tools/perf_paint.py --only backdrop --animation resonance: 25.0 fps at
      1080p AND at 4K, on all four colour themes -- the 24 fps cap held,
      the same as blobs and cells measured the same way in the same run
    reading the drive on the GUI thread: 6.8 us, against 41 ms a frame
    analysis of the 63 s bed: 63 ms, 89.7 kB, 3 777 frames
    mean frame lightness 0.092 idle and 0.099 with the bed playing,
      against blobs 0.165, bokeh 0.160, cells 0.098, drift 0.079

  THREE THINGS THAT WERE WRONG FIRST AND ARE NOW TESTED AGAINST:
    * every figure carried the same diagonal, because the minus sign makes
      w(x, x) vanish identically. MODES now alternates the two signs.
    * particles piled on the plate's rim and in its corners: a Newton step
      is unbounded where the gradient vanishes and clipping the overshoot
      parks them there for good. Swept over every mode pair, 16 800
      grains: shipped 3 corners and 0.85 % on the rim; without the step
      clamp 17 and 1.05 %; clipping instead of reflecting 7 and 1.97 %;
      neither 162 and 4.15 %.
    * the shaker was inaudible -- 0.8 dB over a drumless render, a setting
      that does nothing.

TESTS: tests/qt/test_the_music_bed_is_a_piece.py (20),
tests/qt/test_the_resonance_plate_answers_the_music.py (30), plus
"resonance" entries in tests/qt/test_ambient.py's MIN_PAINTED / MIN_CHANGED,
the pinned tuple in tests/qt/test_ambient_none.py and a plate branch in
tests/qt/test_ambient_motion.py. The existing ambient suites sweep the new
animation unchanged: test_ambient.py, test_ambient_motion.py,
test_ambient_none.py and test_the_backdrop_survives_a_run.py are green,
324 tests between them.

WHAT IS LEFT

  * Part C: the ten melodic-space-house sound sets, which slot into
    sound_synth.SOUND_THEMES. Every parameter part B needed is a theme
    field already, drums included, so part C is a table of numbers and ten
    descriptions rather than more code.
  * NOBODY HAS LISTENED TO ANY OF IT. Part A said so and it is still true:
    the numbers say what was asked for, and whether it sounds like Worakls
    or like a test tone is the maintainer's ear. The one thing that can be
    said is that the render is now checkable by analysis in the way the
    request asked for.
  * Translations are owed for the new captions -- the Music file row and
    its Browse button, placeholder and tooltip, the file dialog's title and
    filter, and the Resonance animation's label and note -- and for every
    new public docstring, spacr/qt/resonance.py being a whole new public
    module. The consolidated pass carries them; the caption ratchet is red
    for exactly those sources and for nothing else.
  * The frozen bundle was not measured, on top of part A's Qt Multimedia
    question: resonance.py adds no dependency (numpy only, no scipy) but
    the 12 MB bed WAV is now the largest thing the cache holds.
  * Windows and macOS are still untested, and the load latency of a 12 MB
    WAV on a real sound server is unmeasured -- the code records the
    loaded instant rather than depending on it, which is the part that can
    be checked without one.
  * No reaper for the analysis of a music file the user has stopped using;
    they are ~90 kB each and stay in the cache.

--------------------------------------------------------------------------------
2026-09-19 -- part B reviewed before landing; six findings, all six fixed

Decided: "The theme should sound like melodic space house" -- the maintainer,
2026-09-19, answering what the sound sets and the bed should be.

The review found no defect in the code that makes sound or pictures. Five of
the six were documentation or user-facing copy that part B had left behind
its own change, and the sixth was a preference that did not reach half of the
new engine.

  * THE MODULE SAID SIX THEMES. spacr/qt/widgets/ambient.py's docstring --
    including the doc-comment on the line the change edited -- still said six
    animations, a seventh engine called fractal and five buffered themes,
    while docs/notes said seven. Corrected, and the docstring's theme list
    now has a resonance entry like the other six.
  * THE COPY DID NOT MENTION THE DRUMS. ORBIT.description and the Music bed
    row's tooltip both still described the version-1 bed -- pads, arpeggio,
    sub -- under a bed that now has a kick at 0.5 and a shaker at 0.34. Both
    say so now. There is still no per-user drum level: kick_level and
    shaker_level are theme fields, which is what makes part C a table of
    numbers, and a user's controls are the bed's volume and the bed's
    switch.
  * THREE DOCSTRINGS SAID SIXTEENTHS. The shaker plays eighths -- range(8)
    over a four-beat bar, 208 hits over 26 sounding bars -- and part C is
    meant to be written from those :param: docs.
  * THE COST TABLE HAD NO RESONANCE ROW. It has one now for the idle column,
    which reproduces the existing rows exactly (blobs 0.246 against the
    table's 0.240, cells 0.521 against 0.538); resonance is 1.005 to 1.072 ms
    at 1080p. The contended column is a stated range rather than a number:
    four nine-round repeats gave medians of 10, 174, 286 and 407 ms, because
    this is the one theme whose shading is dozens of small numpy calls each
    giving the interpreter lock back and queueing for it again. The cost is
    paid on the producer thread, so it is a repeated frame and not a slow
    interface, and a running module holds gil_priority.BUSY_INTERVAL where
    the same measurement is 6 to 24 ms. Nobody has watched it under a real
    Python worker.
  * RESONANCE WAS IN ONE ANIMATION MENU AND NOT THE OTHER. preferences.
    POPUP_BACKDROPS, the backdrop behind a settings window, was never
    extended. Decided that this setting is curated by being its own setting
    and not by offering fewer animations: it is now 'off' plus every name in
    AMBIENT_THEMES alphabetically, with a test asserting that equality.
  * THE SPEED PREFERENCE DID NOT REACH THE MUSIC. advance() scales the clock
    by speed, which is the whole of the preference for every theme whose
    motion is a function of the clock. Half of this one is a function of the
    music: at speed 0.1 the breath, the mode walk and the wander crawled at a
    tenth while a 122 BPM kick still threw the sand 6 % of the plate twice a
    second, for somebody who had moved the slider to its minimum to stop
    exactly that. ResonanceEngine.answering() now scales the moment by
    min(1, speed) before it is stored; above 1.0 it is not scaled up, because
    every field of a Moment is already 0 to 1 against the loop's own loudest.

TESTS ADDED: test_the_speed_setting_reaches_the_music_and_not_only_the_clock
(tests/qt/test_the_resonance_plate_answers_the_music.py, 31 now) and
test_every_animation_is_offered_here_too
(tests/qt/test_the_rim_has_modes_and_the_windows_are_bare.py). Run green after
the fixes: the resonance suite (31), the four ambient suites and
test_preferences.py (432), the five sound and Preferences suites (177), the
four popup and rim suites (149), and seven adjacent suites (183). The four
docstring suites are 8 failed / 133 passed on the branch and at the merge
base alike, on sources this work never touched.

STILL OPEN: everything under WHAT IS LEFT above, unchanged -- part C, that
nobody has listened to any of it, the frozen bundle, Windows and macOS, the
load latency of a 12 MB WAV, and the analysis files nothing reaps -- plus a
per-user drum level if the maintainer wants one when he hears it.

--------------------------------------------------------------------------------
2026-09-19 — THE BED GAINED A MELODY, AND A SIDE-CHAIN THAT CAN BE HEARD
--------------------------------------------------------------------------------

Decided: 2026-09-19, by the maintainer, through the question prompt

  "The theme should sound like melodic space house"

That sentence is quoted three times in this item already and parts A and B
were both built against it. Rendering the bed they built and measuring it
against the genre rather than against the parts list found two things
missing, and both of them are the words in the sentence:

  * NO MELODY. Every pitched note in the sixty-three seconds came from
    the arpeggio -- sixteen a bar, no rests, no shape. That is a texture.
    What Worakls, N'to, Rivière and BIRRD are remembered for is a phrase.
  * NO AUDIBLE SIDE-CHAIN. The pads were ducked on the beat and the
    reverb they fed was not, so a three-second tail filled the dip back
    in: 1.2 dB on the mix where the pads themselves were down 3.1. A
    house record's pump is not a subtlety, it is the pulse.

WHAT WAS BUILT

  sound_synth.LEAD_MOTIF     Twelve entries over four bars, in scale
                             degrees and beats: E5 D5 C5 / D5 C5 A4 /
                             E5 G5 A5 / G5 E5, and a rest. Over
                             i-VI-III-VII every note is a chord tone or
                             the ninth, so it sings across the
                             progression instead of following it.
  SoundTheme.lead_*          pattern, level, octave, attack and
                             brightness -- five fields, because part C's
                             ten themes each need their own tune, not
                             this one transposed.
  sound_synth._lead_voice    A sustaining voice: three saws seven cents
                             apart, a 45 ms attack, a filter that opens
                             on the note and closes across it. A pluck
                             cannot carry a tune over eight plucks a bar.
  BED_SECTIONS               A seventh column, the melody's level per
                             section: away in drift, 0.60 in pulse, 1.00
                             in lift, 0.28 in return. The tune ARRIVES.
  the pump                   Applied to the whole music bus before the
                             reverb rather than to the pads after it, and
                             given a 40 ms fall (PUMP_ATTACK) so the gain
                             curve is continuous where it wraps.
  LEAD_AIR_HZ                An eighth-order wall at 3 kHz on the melody.

MEASURED, with the theme key held fixed and one field changed at a time

  * 72 lead notes in the loop against the arpeggio's 256; the melody adds
    2.6 dB to the lift's 0.5-2.5 kHz band and is silent for bars 4-11.
  * The mix dips 4.1 dB on the beat, against 1.2 dB before. With the
    drums taken out it is 3.8 dB, and with pad_pump at 0 it is 1.7 dB --
    that 1.7 is the arpeggio's own accent on the downbeat, which is music
    and not side-chain. Anything that measures the pump through the kick
    is measuring the kick.
  * Loudness, peak and the seam all still land: -18.6 LUFS, -11.2 dBFS,
    a join of 0.0032 against a p99.5 step of 0.027.
  * The lead is +0.2 dB from its first quarter-second to its third; the
    pluck is -8.3 dB. That difference is the whole reason a tune is
    audible over an arpeggio.

THREE MEASUREMENTS THAT WERE WRONG FIRST, all recorded in
docs/notes/spacr/qt/sound_synth.md

  1. The pump was folded on a whole number of 10 ms hops. The beat is
     49.18 hops, so thirty-two bars drift half a beat and smear the dip
     to nothing. It reported 1.2 dB for a bus that was down 3.1.
  2. The lead's sustain was read over 50 ms windows. Three saws seven
     cents apart beat with a period of a third of a second, so the
     windows sampled the beating and the lead looked as short as a pluck.
  3. THE THEME KEY SEEDS THE SHAKER'S OWN NOISE, so a variant rendered
     under a different key is a different shaker. Four contradictory
     attributions came out of that before it was noticed; every number
     here holds the key fixed.

ONE TEST OF PART B'S WENT RED AND IT WAS RIGHT TO
test_the_drums_are_audible_when_they_are_asked_for measures the shaker as
energy above 4 kHz. The first melody was bright enough to take that band:
the shaker adds 5.36 dB with no melody and 3.23 dB with it. The melody was
warmed to lead_brightness 0.18 (5.44 dB back) and LEAD_AIR_HZ added so no
theme can ask for the brightness that takes it again. Part B's test is
unchanged and passes.

TESTS  tests/qt/test_the_bed_has_a_melody.py (12). Every one was
mutation-checked: the melody off, the bus undicked, the scheduler back on
seconds, the rest played as a note, the lead voice made a pluck, the
melody left on through every section, and the air wall removed -- each on
its own turns its own test red. The 90 tests in part A's and B's four
sound files and the resonance file all still pass, and the shaker test
above is among them.

A FLOAT BUG THE COUNT CAUGHT. The scheduler first added each note's
length in SECONDS. A note landing on a bar line arrived a float's breadth
early, was priced at the PREVIOUS bar's level and was dropped where that
bar was silent. It cost exactly one note of seventy-two and nothing but a
pinned count would have found it. Positions are counted in beats now,
which are exact halves.

WHAT IS LEFT

  * PART C, THE TEN THEMES, is still not started, and it is the rest of
    the maintainer's answer ("make 10 themes ... worakls, NTO, rivière
    monk, BIRRD"). The five lead_* fields and BED_SECTIONS' seventh
    column exist so each theme can have its own tune rather than this one
    transposed.
  * ORBIT's `description` changed and is a user-facing string: the
    consolidated i18n pass owes nine languages for it. Nothing else
    visible changed.
  * NOBODY HAS LISTENED TO IT STILL, which is the one thing no test can
    answer. Three renders were sent for the maintainer's ear on
    2026-09-19: the bed twice round so the join is heard, the lift before
    and after the melody, and the interface sounds.
  * The bed now lands 0.56 LU under its own target and 2.3 dB under its
    peak ceiling, because `_to_loudness` gives up after two rounds and
    the melody costs it a round. It is QUIETER than asked, not louder,
    and part B's tolerance is 1.0 LU, so nothing is red. Left alone
    deliberately: `_to_loudness` is part B's and was being written in
    another tree while this was measured.
  * The kick does not separate from the sub in the 40-110 Hz band (0.25
    dB of on-beat over off-beat) because the kick's fundamental is at
    33 Hz and the sub sits at 44-65 Hz on top of it. Part B's drum tests
    measure the kick in isolation and pass; this is about the mix, and it
    is the next thing worth listening for.
