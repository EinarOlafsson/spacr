================================================================================
SAMCELL, TRAINED ON LIVECELL, FOR TIMELAPSE
================================================================================

Status:    INTEGRATED; real isolated CPU install and pretrained synthetic
           inference verified2026-09-23. Install through the Model Zoo into
           SAMCell's own environment, not spaCR's environment. The isolated
           installer now includes the plotting dependency imported by
           SAMCell1.2.0 and keeps both model caches inside that environment.
           The held-out modality-matched comparison against Cellpose remains
           open; integration checks are not accuracy scorecards. Originally
           built2026-09-15 in14b459a18, approved2026-09-14 by the maintainer:
           "implement both, both are available on pypi".
Requested: 2026-09-14, verbatim -- "we shuld also support SAMCell-LIVECell
           especislly for timelapse", and then the source, verbatim --
           "this is the samcell i mean https://github.com/saahilsanganeriya/SAMCell"
Owner:     Claude home session (built in the 63/394 integration). Shares a seam with 404 (DINOCell): both answer "what
           segments a cell spaCR was not trained on", and they must extend ONE
           backend mechanism rather than growing two a month apart.

--------------------------------------------------------------------------------
WHAT IT IS, READ FROM THE REPOSITORY 2026-09-14
--------------------------------------------------------------------------------

github.com/saahilsanganeriya/SAMCell. Label-free biological cell
segmentation on Meta's Segment Anything backbone -- SAM-Base (ViT-B), 89M
parameters, encoder pretrained on 11M images. MIT licence.

    pip install samcell
    samcell segment image.png --model samcell-generalist.pt
    samcell.FinetunedSAM() ; samcell.SAMCellPipeline()

Dependencies: Python >= 3.8, torch >= 1.9, transformers >= 4.26, OpenCV >= 4.5,
scikit-image >= 0.19, pandas >= 1.3.

TRAINED ON LIVECELL, which is the whole reason this item exists: 5,000+
PHASE-CONTRAST images across 8 cell types, plus Cellpose Cytoplasm (~600
images from diverse sources). Two checkpoints ship, SAMCell-Generalist and
SAMCell-Cyto.

AN EARLIER DRAFT OF THIS FILE POINTED AT THE WRONG REPOSITORY --
github.com/NathanMalta/SAMCell, which has no README -- and said so rather than
guessing at its contents. The maintainer supplied the right one. Recorded
because "the repository has no README" was a true statement about the wrong
project, and a true statement about the wrong thing is the most convincing
kind of wrong.

--------------------------------------------------------------------------------
WHY TIMELAPSE SPECIFICALLY
--------------------------------------------------------------------------------

Every Cellpose model in spaCR's zoo is fluorescence-oriented and all three
published entries are Toxoplasma-specific. Timelapse is routinely
phase-contrast or brightfield, so a live-cell run today asks a fluorescence
model to do something it was not trained for.

AND A SEGMENTATION FAILURE IN TIMELAPSE COMPOUNDS RATHER THAN STAYING LOCAL.
A single bad frame is one bad measurement. A model that is systematically
weaker on label-free images produces a track that breaks, and a broken track
is a lineage error that propagates through every downstream number. That is
why the maintainer's "especially for timelapse" is the right emphasis, and why
the scorecard for this one has to count SPLITS AND MERGES rather than only
Dice -- those are the failures that break a track.

--------------------------------------------------------------------------------
WHAT TO BUILD
--------------------------------------------------------------------------------

  * ONE BACKEND SEAM SHARED WITH 404. `ModelEntry.kind` is already the
    discriminator -- 'cellpose' and 'detector' both exist. DINOCell emits
    Cellpose-style flows and may need no new mask path at all; SAMCell emits
    masks through `SAMCellPipeline`, so IT is the one that decides whether a
    second path is needed. Establish that before writing either.
  * AN OPTIONAL DEPENDENCY, not a core one. `samcell` pulls torch,
    transformers and a ViT checkpoint. A user who never opens it must not pay
    for it: item 282's lazy-import discipline is the standard, and
    `tests/test_perf_guard.py` is what enforces it by asserting the module is
    absent from `sys.modules` after the Qt launch path.
  * BOTH CHECKPOINTS IN THE ZOO, named for what they were trained on.
    Generalist and Cyto are different answers and a user choosing between them
    needs to know which is which -- `ModelEntry.trained_on` is where that
    goes, as it already does for the three Toxoplasma entries.

--------------------------------------------------------------------------------
HOW IT WILL BE CHECKED
--------------------------------------------------------------------------------

A phase-contrast or brightfield timelapse runs end to end and produces tracks,
with no import of `samcell` in a process that does not use it. And a scorecard
-- `spacr/scorecard.py`, which already scores Dice, IoU, boundary F1, AUPRC
and splits/merges -- against a labelled held-out set OF THE SAME MODALITY,
published beside whatever Cellpose scores on the same frames.

The comparison is the deliverable. "Better on label-free" is the claim, and a
number measured on fluorescence data does not test it.

The held-out set is being labelled for the purpose (see 370); the integration
can land before it, the RECOMMENDATION cannot.

2026-09-23 — REAL ISOLATED INSTALL, CACHE REPAIR AND PHASE-CONTRAST SMOKE
The ordinary Model Zoo installer was exercised in a private backend root.
Its first self-test failed because SAMCell1.2.0 imports matplotlib.pyplot
from utils.py unconditionally, while its wheel declares matplotlib only in
training/evaluation extras. The failed environment was removed normally.
The backend requirements now include matplotlib>=3.3.0. Reinstall succeeds:
Python3.12.4, SAMCell1.2.0, Torch2.14.0+cpu, NumPy2.5.3, Transformers5.17.0,
Hugging Face Hub1.32.0, Matplotlib3.11.2. pip check is clean. No package was
installed into spaCR's own environment.

Both model downloads now belong to that environment: TORCH_HOME points at
<env>/torch, HF_HOME at<env>/huggingface. HF hub/assets/xet overrides and
SAMCell's inherited legacy Transformers/modules cache overrides are removed.
A fresh-process before/after probe using real Torch2.6.0, Transformers4.48.3
and Hub0.36.2 resolves six cache paths: every old path escapes into the
inherited shared locations, every new path stays within the environment.
Unrelated backend variables and the parent's environment are unchanged.

Actual pretrained inference then ran through spaCR's isolated worker with
model downloads disabled, using copies of the existing local generalist
and SAM backbone weights. The original shared caches were not changed.
The synthetic256x256nine-object field yields nine labels in42.3seconds;
host Python does not import samcell. This is a smoke, not accuracy evidence.

The ordinary generate_cellpose_masks_sam path also ran three consecutive
fixed256x256center crops of CTC PhC-U373 sequence01, frames000–002. It used
segmentation_backend=samcell and timelapse_mode=iou, saved all three uint16
masks, an input movie and the track CSV. Objects/frame:5,4,4;13track rows,
six track IDs, three spanning all three frames. Total123.8seconds on CPU.
Input file hashes are unchanged. This verifies a short real phase-contrast
segmentation-to-tracking path; it does not evaluate a whole movie, reference
tracking accuracy, split/merge rates or superiority to Cellpose.

The install had a6GiB cap and inference8GiB, both with swap disabled and
two compute threads. Initial backend tests:220passed, two optional
in-process checks skipped; final isolated-backend tests after the dependency
addition:118passed. Fatal Ruff checks pass. Exact versions, source hashes,
cache paths, mask outputs, track evidence and limitations are recorded in
features/data/405_isolated_install_cache_and_phase_smoke_2026-09-23.json.
The modality-matched held-out scorecard and recommendation remain OPEN.
