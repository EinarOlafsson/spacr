================================================================================
THE SETUP SCREEN INSTALLS THE AI PROVIDER IT OFFERS, INSTEAD OF PRINTING A LINK
================================================================================

Status:    DONE ON LINUX AND macOS 2026-09-21: the AI sign-ins run inside spaCR
           on a pseudo-terminal, no terminal window (Windows still opens one).
           Earlier: BUILT ON LINUX AND LANDED ON NIGHTLY 2026-09-19, after review.
           NOT DONE: the AI CLIs' sign-in still runs in a terminal window
           spaCR opens, so the first DONE WHEN ("no terminal used") is met
           for the GitHub CLI only. Windows and macOS commands are data from
           the vendors' docs, UNVERIFIED on those hosts. No real install was
           run anywhere. See the two 2026-09-19 notes at the end.
Requested: 2026-09-16, by the maintainer, in these words:

           "in the startup spacr when the use clicks an AI provider there
           should be an aditional button, install which automatically
           downloads and installs the chosen ai provider and asks the user for
           the needed information and sets up the AI provider . so instead of
           giving the user a curl link it uses the curl link to downloade e.g.
           claude and then installs it."

Owner:     unassigned. spacr/qt/widgets/setup_slides.py (the startup slide),
           spacr/qt/ai/providers.py (what each provider knows about itself),
           spacr/qt/widgets/ai_chat_panel.py (the same hint, shown again).

WHAT IS THERE TODAY (read on nightly c6c49f6ae)
------------------------------------------------

* Each provider already knows how it would be installed and whether it is:
  ChatProvider (providers.py ~35) carries install_hint (~50), is_installed()
  (~62), is_logged_in() (~69) and status() (~79). ClaudeCliProvider (~297)
  holds the curl one-liner that item 414 made work on every system --
  cmd /c "curl -fsSL https://claude.ai/install.cmd ..." on Windows, else
  curl -fsSL https://claude.ai/install.sh | bash (~303-307). CodexCliProvider
  (~335) holds "npm install -g @openai/codex   # or brew install codex"
  (~341); GeminiCliProvider is ~367.
* The startup slide already branches on that state: setup_slides.py ~1241
  (not installed -> a mark), ~1362 (installed and logged in), ~1382-1384,
  where it reads install_hint and shows it. ai_chat_panel.py ~392-406 shows
  the same hint in a QLineEdit to copy.
* So today every path ENDS at a command the user must run somewhere else.
  This item turns that last step into a button.
* No API keys are involved: spacr/qt/ai/keys.py records that the CLI-based
  providers take none. "The needed information" is therefore the provider's
  own login (and whatever it prompts for), not a key to store.

WHAT TO BUILD
-------------

1. Clicking a provider on the startup screen offers an "Install" button
   whenever that provider is not installed.
2. The button runs the provider's own installer -- the same curl or package
   command install_hint already holds, chosen per operating system -- rather
   than showing it. One source of truth: the button and the hint must not
   drift apart.
3. It then takes the user through the provider's login, asking for whatever
   the provider needs, and leaves the provider SET UP: is_installed() and
   is_logged_in() both true, and the slide's mark turns to READY without a
   restart.
4. It says what it is doing while it runs, and a failure leaves a message the
   user can act on, not a dead button: no network, a refused download, a
   missing package manager, a cancelled login.

WHAT TO BE CAREFUL WITH
-----------------------

* This downloads and executes an installer from the network on the user's
  machine, on their click. Show what will run before running it, never run it
  unasked, and keep the command visible for the user who would rather run it
  themselves.
* It must not block the GUI thread: the install and the login are long,
  interactive processes.
* The three providers install in three different ways (curl script, curl +
  cmd on Windows, npm or brew). A provider whose install cannot be automated
  on the running system should say so and fall back to today's copyable hint.
* New captions (the button, its progress and its failures) are translated
  strings in nine languages: list them for the catalog lane rather than
  regenerating spacr/qt/i18n_catalogs.
* Item 414 (BUILT, b3f7bd6c9) is what made the Claude command work on every
  system, Windows included; this item consumes it. Item 416's updater does
  the find/delete/install dance for spaCR itself and is the nearest existing
  example of running an install from inside the app.

DONE WHEN
---------

* On a machine without the CLI, clicking the provider and pressing Install
  leaves it installed, logged in and marked READY, with no terminal used.
* The command that runs is the provider's own install_hint for that operating
  system, and there is one definition of it.
* Every failure path shows a message and leaves the screen usable.

--------------------------------------------------------------------------------
2026-09-16 — ADDED BY THE MAINTAINER: THE GITHUB CLI TOO, AND A WAY TO GET AN ACCOUNT
--------------------------------------------------------------------------------

In his words:

    "and same for the github cli, add that to and a button that links to
    generating a github account"

So the Install button above covers the GitHub CLI as well, and beside it there
is a button that takes a user who has no GitHub account to where they can make
one.

WHAT IS THERE TODAY (read on nightly 7fa5105a1)

  * The GitHub CLI is NOT a ChatProvider, so it has none of the machinery the
    AI providers have -- no install_hint, no is_installed(), no is_logged_in().
    spaCR only consumes it: spacr/qt/ai/github_auth.py reads a token with
    `gh auth token` (~84) and its docstring tells interactive users to run
    `gh auth login` themselves (~62); spacr/qt/ai/issue_report.py reports the
    CLI's version or the bare marker "not installed" (~278-286).
  * So today a user without `gh` is told it is missing and left to install and
    log in somewhere else -- the same dead end this item removes for the AI
    providers.

WHAT TO BUILD

  * Give the GitHub CLI the same three states the AI providers have -- not
    installed, installed but not logged in, ready -- and the same Install
    button, running the CLI's own installer for the running operating system.
    Either extend the provider seam to cover it or give it the same small
    interface; do not grow a second, different way of asking "is it there?".
  * After installing, take the user through `gh auth login` and leave the CLI
    logged in, so `gh auth token` answers and the issue reporter works without
    a restart.
  * Beside it, a button for a user with no GitHub account at all: it opens
    GitHub's sign-up page in the browser. Creating the account happens on
    GitHub, in the browser -- the button is the link, nothing more -- and when
    the user comes back, Install and login finish the job.
  * The cautions above apply unchanged: show the command before running it,
    never run it unasked, keep the GUI responsive, and leave an actionable
    message on every failure. The new captions are translated strings for the
    catalog lane.

DONE WHEN

  * On a machine without `gh`, Install leaves it installed and logged in, and
    spacr/qt/ai/github_auth.py gets a token without the user opening a
    terminal.
  * The account button opens GitHub's sign-up page.
  * A user who declines any step is left with a working screen and the command
    still visible.

--------------------------------------------------------------------------------
2026-09-19 — BUILT ON LINUX; WINDOWS AND MACOS UNVERIFIED
--------------------------------------------------------------------------------

WHAT WAS DONE

  * One definition. spacr/qt/ai/providers.py now holds INSTALL_METHODS: per
    tool (claude, codex, gemini, gh) and per OS (linux, darwin, win32), the
    ordered ways of installing it, each with the programs it needs. Each
    provider's install_hint is BUILT from those rows (the Linux/macOS hints
    are unchanged character for character; the Windows hint shows only the
    first row, because cmd has no `#` comment). The Install button runs the
    first row whose programs are on PATH -- so the button and the hint cannot
    drift apart. UNVERIFIED_PLATFORMS = ("darwin", "win32") says so in code.
  * The GitHub CLI got the same interface, not a second one: CommandLineTool
    is the shared base (is_installed, install_methods, install_hint,
    login_command, status_command, check_signed_in); ChatProvider and the new
    GitHubCli both derive from it; providers.github_cli() returns it.
  * spacr/qt/ai/cli_install.py (Qt-free) runs a row: no stdin, a scratch
    working folder, its own process group; `curl | bash` under
    `bash -o pipefail` so a failed download fails; output streamed line by
    line (split on \r too); Cancel and a 20-minute limit via a watcher
    thread; the verdict is a kind -- installed, cancelled, timed out, no
    network, refused, permission, not found, not started, not automatic,
    failed. Afterwards it looks for the CLI in the folders installers use
    (~/.local/bin, the conda env, Homebrew, npm's global bin) and the GUI
    thread adds that folder to PATH, so the mark turns READY with no restart.
  * spacr/qt/widgets/cli_setup_panel.py shows it: the command (always visible,
    with Copy), a busy bar, the installer's latest line, Cancel, then Try
    again / Open the page / the next step. The install runs on a JobRunner
    worker thread, never on the GUI thread. After a successful install it
    starts the sign-in and asks the tool's status command every 3 s (off the
    GUI thread) until it says yes, giving up after 10 minutes with a message.
  * The setup screen: the prompt a not-ready provider already opened now has
    Install (default) beside Open the page / Copy / Later, and shows "Install
    runs: <command>" before anything runs. With no usable row it names the
    missing program ("npm (Node.js) or Homebrew") and keeps the page and the
    command. The GitHub mark with no `gh` opens the same kind of prompt;
    after install it goes straight into spaCR's existing in-app
    `gh auth login` flow, and a `gh` that exits non-zero ends the wait with
    a message. "Create a GitHub account" beside the mark opens
    https://github.com/signup and hides once a token is found. Closing the
    screen while an installer runs asks first (see the review note below).

WHAT WAS MEASURED (this machine, 2026-09-19)

  * `claude auth status` exits 1 in an empty HOME, 0 signed in. `gh auth
    status` exits 1 with an empty GH_CONFIG_DIR, 0 signed in. `gh auth
    token` still answered 0 with a fake HOME and GH_CONFIG_DIR -- it reads
    the system keyring -- so it is not a signed-out probe under a fake
    config. `codex login status` was NOT run (no codex here); it is from
    Codex's docs.
  * The real screen, offscreen, with stand-in installers on PATH (a shell
    `npm` that writes a `codex` into ~/.local/bin of a temporary HOME, and a
    shell `conda` that prints conda's HTTP 000 CONNECTION FAILED): GPT went
    not installed -> prompt showing `npm install -g @openai/codex` ->
    installing (latest line shown while it ran) -> signing in (`codex login`
    handed to the terminal launcher) -> READY, and the mark turned READY
    without a restart. The GitHub row went prompt -> installing -> "could
    not reach its download server ... press Try again", command still shown.
  * Tests press the buttons with the process faked, and two use real
    processes (shell scripts in tmp_path): one full GPT install to READY on
    the real worker thread, and one that checks Cancel kills the child of a
    `bash` pipeline too. cli_install.py and cli_setup_panel.py are at 100%
    statements and branches from the two new test files.

WHAT IS LEFT

  * Windows and macOS: every row for those hosts is unverified. In
    particular the Windows `cmd /c "..."` line handed to Popen as a string,
    CREATE_NO_WINDOW, `taskkill /T` for Cancel, and the winget and npm.cmd
    rows need one run on a Windows machine; `brew install` rows need macOS.
  * No real install was run on any machine, by instruction. The Claude
    installer, npm, conda and Homebrew were never actually invoked.
  * On a stock Linux, Node from the distribution puts npm's global folder
    under /usr, so `npm install -g` fails with EACCES; the panel says to run
    `npm config set prefix ~/.local` and try again, but does not do it.
  * Linux has no root-free official `gh` installer: without Homebrew or
    conda the prompt sends the user to cli.github.com.
  * ChatProvider.is_logged_in is still "installed", so a mark reads READY
    as soon as the CLI exists even before sign-in; the panel's status
    polling is what follows the sign-in. Making the marks use the status
    commands would put a process per mark on the GUI thread.
  * THE FIRST DONE WHEN IS NOT MET FOR THE AI PROVIDERS. It asks for
    "installed, logged in and marked READY, with no terminal used". The
    install needs no terminal, but after it the Claude, GPT and Gemini
    sign-ins (`claude auth login`, `codex login`, `gemini`) are handed to
    a terminal window spaCR opens (_sign_in_to_provider ->
    _run_in_a_terminal), and the measurement above says so for GPT. Only
    the GitHub CLI signs in inside spaCR. Driving the AI sign-ins as a
    QProcess, the way `gh auth login` is driven, was not tried: whether
    each CLI signs in without a terminal cannot be checked here without
    signing in to a real account.
  * Translations: every new caption is English until the consolidated
    catalog pass (listed in the unit's i18n_owed).

--------------------------------------------------------------------------------
2026-09-19 — REVIEW FIXES, LANDED ON NIGHTLY; ONE DONE WHEN STILL UNMET
--------------------------------------------------------------------------------

Decided: no new question about this item was put to the maintainer. The
build follows his two requests of 2026-09-16, quoted above, and keeps
`claude auth login` as Claude's sign-in (item 432, checked against the
installed CLI).

The review found the core mechanism sound and two things to fix before
landing, plus six smaller points. What was done about each:

  * Closing the screen mid-install threw the install away without a word
    (should-fix). Measured by review with a sleeping stand-in `npm`: Start
    spaCR closed the dialog, SIGTERMed the installer and recorded the
    screen as done. Now accept and reject ask first: "Keep installing"
    (the default, and Escape) leaves the screen open; "Stop it and close"
    stops it, the panel says it was cancelled, and the screen closes.
    Tests press both, from accept and from reject.
  * This ledger did not admit the unmet "no terminal used" (should-fix).
    It does now: Status and WHAT IS LEFT. It was recorded, not met; see
    WHAT IS LEFT for why.
  * Cancel could not reach a child left holding the installer's output.
    The group is now signalled even after the installer has exited, and
    killed if the output is still open after the grace period. A real
    `bash` that starts `sleep 30 &` and exits is cancelled in well under a
    second; before, the wait lasted the whole sleep. A Cancel that races an
    install that finished is reported as installed when the CLI is there.
  * The conda row installed into whatever environment was active, which is
    `base` when spaCR starts from an icon, and then could not find `gh`.
    It now names spaCR's own environment with `--prefix` and adds `gh`
    with `--freeze-installed`. Both options were checked with
    `conda install --help`; the command was not run.
  * "it needs {needs}, and none was found" showed when a row could run but
    the panel was gone, and any failure to start was blamed on "another
    install". Both are keyed on the real reason now.
  * The mark tooltip for a provider that is not installed now says
    choosing it offers the install.
  * The GitHub row asks whether `gh` is there through
    GitHubCli.is_installed(), the question the panel asks, and the
    duplicate re-read after an install is gone. The token read itself
    (github_auth.auth_source, `gh auth token` on the GUI thread) is as it
    was before item 420: it also knows GITHUB_TOKEN and a stored token,
    which the GitHub CLI's status check does not, so it was not replaced.
  * tests/conftest.py now refuses cli_install._spawn in every test, so a
    test that presses the prompt's default button cannot start a real
    installer; tests/test_no_test_runs_a_real_installer.py checks it.

Left: everything in WHAT IS LEFT above. Windows, macOS, a real install and
the AI sign-ins without a terminal are the open parts.

--------------------------------------------------------------------------------
2026-09-21 -- NO TERMINAL: THE AI SIGN-INS RUN INSIDE spaCR
--------------------------------------------------------------------------------

The open DONE WHEN was "no terminal used": after the install, the Claude, GPT
and Gemini sign-ins were handed to a terminal window. They now run on a
pseudo-terminal inside a small spaCR window (spacr/qt/ai/pty_sign_in.py): the
tool believes it is on a terminal, the sign-in page it prints is opened in the
browser, and a code it asks for is typed into a box and sent. The panel's
status polling marks the provider READY as before.

Measured with the real Claude CLI on this machine, in a scratch HOME so no
real login was touched: `claude auth login` on the pseudo-terminal printed its
OAuth link and "Paste code here if prompted >", which is exactly the shape the
window handles; it was stopped before signing in. A stand-in tool that exits
unless it IS on a terminal, prints a coloured link and checks a pasted code
passes end to end in tests/qt/test_ai_sign_in_happens_inside_spacr.py.

Windows keeps the terminal: it has no pseudo-terminal in the standard
library. `codex login` and `gemini` were not run here (not installed).

2026-09-22 — EMBEDDED SIGN-IN LIFETIME AND TERMINAL CLEANUP
The sign-in window now owns the entire process group it starts. Cancelling
stops the launcher and its descendants, including a child that ignores TERM;
it reaps the launcher and joins the terminal reader. Cleanup is idempotent,
so closing/destroying a finished window cannot close a reused descriptor.
Failed process creation closes both terminal descriptors. Completion drains
remaining output and releases the terminal while the result window stays open.
Destruction of the parent also stops sign-in; closing setup or choosing a new
sign-in closes the old embedded window. The setup note now says "Signing in
to {label}…" rather than incorrectly promising an external terminal.

Three regressions reproduced on the previous code: parent destruction left
sign-in running, failed creation leaked descriptors, and cancelling a launcher
left its child alive. All now pass with real local Python stand-ins; no vendor
authentication is needed. Older setup tests now explicitly cover embedded and
terminal-fallback paths. The Qt test harness refuses an unmocked embedded
process launch; tests of real terminal behavior opt into local stand-ins.
Windows still uses the external terminal fallback. Real-host acceptance and
the wider item420 requirements remain OPEN.
Validation: 155 distinct focused setup/sign-in checks pass across the recorded
runs (125 initial, 28 additional setup checks, and two added lifecycle cases).
The final nine terminal lifecycle checks pass in5.93s; fatal-error Ruff passes.
