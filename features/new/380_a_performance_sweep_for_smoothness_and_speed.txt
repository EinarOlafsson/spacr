================================================================================
A PERFORMANCE SWEEP: STARTUP, THEMES, MODULES, WIDGETS AND SETTINGS
================================================================================

Status:    THE HARNESS EXISTS AND IS CALIBRATED, 2026-09-09; THE THEME
           DEFECT IT NAMED IS FIXED, 2026-09-11. Two of the five areas
           were already covered by tooling nobody had connected to this
           item; the two the request judges "smoothness" by were not, and
           are now. The largest single defect the harness found -- a
           preference save repolishing every widget in every hidden
           screen -- went 10.2 s -> 3.1 s -> 1.7 s in two steps, both
           recorded below. Filed 2026-09-07.
           AREA 3 IS HALF DONE AND MEASURED, 2026-09-19: the events a module
           open delivers are counted and named, most of them were repolishes
           the application asked for twice, and paying a screen's stylesheet
           once plus five cheaper event filters took the total over-ceiling
           stall across 45 modules from 30,089 ms to 15,182 ms and the events
           per Regression open from 139,963 to 92,829. Areas 1, 2 and 5 stand
           where 2026-09-09 to 2026-09-11 left them; "on all platforms" is
           still unanswered -- every number in this file is Linux, offscreen.
           2026-09-25, AREA 3 AGAIN: a module open breathes between its
           steps, Make Masks is styled once instead of three times and
           Preferences once instead of twice; over-ceiling stall across the
           sweep 38.7-48.4 s -> 14.0-16.8 s at load 37-53 (284's note has
           the table). Opens paint frames while they build, so several take
           longer at that load; still OPEN on the ceiling. xvfb numbers now
           exist; macOS/Windows and a real compositor still do not.
           2026-09-26, AREAS 3 AND 5: the thirteen application-wide Python
           event filters share one hub that calls a filter only for its own
           event types; a screen's first stylesheet fell ~2.5x (Make Masks
           650-790 -> 250-280 ms thread CPU at load 90-100) and at load
           11-13 no module crosses 500 ms (6 of 6 runs; nightly 6 of 6 do).
           Preferences is still over at load 15-21. See 284's note.
Requested: 2026-09-07, verbatim -- "do an in depth performance sweep to check
           and fix any performance defects in starting spacr, running the
           themes, or starting the moduals and handelling the widgets and
           settings in the moduals and on the home screen. this should all be
           optimized for smoothness and speed (on all platforms) while not
           comproon functionallity."
Owner:     spacr/qt/** -- startup, theme, screens, widgets, settings panels.

THE LAST CLAUSE IS THE CONSTRAINT, NOT A PLEASANTRY. "while not compromising
functionality" rules out the cheap wins: dropping the backdrop, lowering the
frame rate, lazy-loading a panel so it appears blank, eliding text that then
cannot be read. A change that makes spaCR faster by making it do less is not
an answer to this item unless the maintainer agrees the thing removed was not
wanted.

--------------------------------------------------------------------------------
WHY THIS IS FILED SEPARATELY, AND WHAT IT IS NOT
--------------------------------------------------------------------------------

Performance work has been done here in pieces, always reactively, and each
piece was found by the maintainer noticing something rather than by anyone
looking. That is the pattern this item exists to break.

  315  THE FORCE-QUIT DIALOG. "Force quit window still appears at each
       modular start." Root-caused 2026-09-05: two comprehensions in
       `settings.py` rebuilt constants inside inner loops (1932 ms -> 484 ms),
       plus a 30-second `urlopen` on the GUI thread. The desktop offering to
       kill spaCR is the most severe symptom this class of defect has, and it
       was shipped for weeks.
  378  HOLD Z AND SCROLL. Measured `app.setStyleSheet` + re-polish at 587.5 ms
       on 847 widgets, against 107 ms for the visible screen alone. That
       number was measured because one feature needed it, and it describes
       every theme change spaCR makes, not just that one.
  305  LOAD TIME WITHIN RESOURCES. Filed, deferred to after the version bump.
  327  THE BACKDROP CAMERA. "the fullscreen mode fractal is super choppy while
       the normal background one is super smoothe."
  The one-backdrop fix, 2026-09-05: TWO full-size animated fields were being
       shaded and blitted per frame on a 4K screen -- 950 paints/s, 393 Mpx/s,
       idle GUI-thread CPU at ~16% of a core, for a picture nobody could see.

SO THIS ITEM IS THE SWEEP, not another instance. Its output is a MEASUREMENT
HARNESS plus the defects that harness finds, in that order. An audit that only
records "this felt slow" produces a list nobody can act on, and the four
entries above show the causes are unrelated to each other.

--------------------------------------------------------------------------------
WHAT TO MEASURE, AND THE FIVE AREAS THE REQUEST NAMES
--------------------------------------------------------------------------------

1. STARTING SPACR. Cold import to first painted frame, broken down: which
   imports, which registry builds, which file reads. `spacr-doctor` and the
   preloader already exist and should be read before anything new is written.
   THE TARGET IS THE SLOWEST STEP NAMED, not a total -- a 3-second startup
   with one 2.4-second step is a different problem from thirty 100 ms steps.

2. RUNNING THE THEMES. Frames per second and paint cost per theme, at 1080p
   AND at 4K, because the cost is counted in pixels and 327 was reported from
   a 3840x2160 machine. Include the fullscreen screensaver path, which 327
   says is choppy where the windowed one is smooth -- that gap is a defect
   with a cause, not a fact of life.

3. STARTING A MODULE. Registry lookup to first painted settings panel, per
   module, since 315 showed one module's settings build can dominate. Every
   declared app, not a sample: the four that were sampled in 350's sweep all
   passed and the sixteen added later found nothing, but that was luck rather
   than method.

4. HANDLING THE WIDGETS AND SETTINGS. The interactions, which is where
   "smoothness" is actually judged: expanding a section, typing in a field,
   scrolling a long settings column, dragging a splitter, hovering the grid.
   Measure INPUT LATENCY and DROPPED FRAMES, not wall-clock -- a 200 ms
   hitch on a keystroke is felt, and a 200 ms total spread over ten frames
   is not.

5. THE HOME SCREEN. Named separately by the maintainer, and it is the one
   surface every user sees before anything else.

--------------------------------------------------------------------------------
"ON ALL PLATFORMS", WHICH IS THE HARD PART OF THIS ITEM
--------------------------------------------------------------------------------

The maintainer said all platforms and it cannot be waved through, because
this project has already shipped two defects that were invisible on the
machine they were written on:

  * the packaging failure (331/PKG) reproduced only where setuptools was >= 77,
    and the home workstation has 69.5.1 -- so a green build there was never
    evidence about anything;
  * the label-contrast failures in issue #6 were reported as a macOS problem
    and were really a light-theme one, because macOS defaults to the light
    appearance far more often than Linux does.

A PROFILE FROM ONE MACHINE IS A FACT ABOUT THAT MACHINE. So the harness has
to run in CI on all three platforms and record numbers, even if the fixing is
done locally. Scale matters too: font scale, screen resolution and HiDPI all
multiply paint cost, and 04 already records that the tests measured a font
scale no user has.

--------------------------------------------------------------------------------
HOW TO DO IT
--------------------------------------------------------------------------------

1. WRITE THE HARNESS FIRST, and prove it catches a defect that is already
   known before trusting it on anything else -- 350 did exactly this against
   the Annotate instance and it is why that sweep is believable. The
   one-backdrop regression is the ideal calibration case: a harness that
   cannot see 950 paints/s for an invisible layer is not measuring paint.

2. RECORD A BASELINE and commit it, so later work can be shown to have moved
   the number rather than asserted to have.

3. FIX BY CAUSE, NOT BY SYMPTOM, and record which cause each fix addressed.
   The four known instances had four unrelated causes -- work in an inner
   loop, a blocking call on the GUI thread, a whole-application re-polish,
   and a duplicated layer -- and a list organised by symptom would have
   grouped none of them correctly.

4. RATCHET WHAT IS FIXED. A performance defect with no test comes back; 315
   came back three times. Prefer a ceiling assertion ("first paint under
   N ms", "no more than one animated layer") over a benchmark that fails on
   a loaded machine.

--------------------------------------------------------------------------------
WATCH
--------------------------------------------------------------------------------

* MEASURE ON THE MACHINE THAT CANNOT COPE, not the one that can. The
  maintainer's report that started 315 was "It's like my computer has an
  unusually low tolerance for applications loading." That is the environment
  to optimise for.
* DO NOT MEASURE WITH `QWidget.grab()`. It does not capture the GL-backed
  AmbientWidget, so a grab-based check reports a black window and cannot tell
  a working backdrop from a broken one. Found the hard way on 2026-09-07,
  after an A/B across two commits came out byte-identical for that reason.
* RAM IS PART OF PERFORMANCE HERE. This machine has been taken down twice by
  spaCR's own test suite; a "faster" change that raises peak RSS is not
  obviously a win.
* THE FIRST RUN AFTER AN INSTALL IS THE ONE USERS JUDGE. Warm-cache numbers
  flatter every result in this file.


--------------------------------------------------------------------------------
2026-09-09 -- WHAT ALREADY EXISTED, AND THE TWO HALVES THAT DID NOT
--------------------------------------------------------------------------------

"READ THEM BEFORE ANYTHING NEW IS WRITTEN" was the right instruction and it
saved most of this item. `tools/spacr_startup_benchmark.py` is 988 lines and
already covers three of the five areas properly:

    1. STARTING SPACR      cold and warm, fresh interpreter per run,
                           real entry point, isolated home
    3. STARTING A MODULE   every live sidebar app pressed, time to ready
    5. THE HOME SCREEN     Home ready as its own budgeted number

with a 5 s Home budget, a 10 s module budget, a 500 ms event-loop stall
detector and a JSON schema. It also states, correctly, that a hosted CI
runner is not release performance evidence: no real display, no compositor,
no hardware profile, shared-host scheduling.

  SO AREA 1, 3 AND 5 NEED RUNNING AND WATCHING, NOT BUILDING. What this
  item adds there is the discipline of step 2 -- record a baseline and
  commit it -- rather than another harness.

AND A THIRD TOOL WAS MISSED ON THE FIRST PASS, which is worth recording
because this item's whole argument is that reading first is cheaper than
writing. `tools/measure_the_backdrop.py` (instruction 327, 255 lines) is
AUTHORITATIVE for area 2's cost half: the CPU kernel per surface at the
adaptive scale's top and at its 0.58 floor, the whole pipeline through the
real widget, and each GPU shader into an offscreen framebuffer with a real
GL context. It was found an hour after `perf_paint.py` was written, and the
overlap is now stated in that file's own docstring rather than left for
somebody to trip over: measure_the_backdrop answers what a frame COSTS,
perf_paint answers what the RATE is per theme, and neither replaces the
other. Anyone extending this should grep `tools/` before adding a fourth.

WHAT WAS MISSING IS EXACTLY THE TWO THE REQUEST JUDGES SMOOTHNESS BY, and
`tools/perf_paint.py` is those two:

    2. RUNNING THE THEMES  backdrop paints/s per theme at 1080p AND 4K,
                           and the cost of a whole-application theme change
                           with the widget count beside it
    4. WIDGETS AND SETTINGS  input latency for expanding a section, typing
                           a character and scrolling the settings column,
                           reported as FIRST and REPEAT with dropped frames

IT COUNTS PAINTS RATHER THAN GRABBING PIXELS, per this file's own WATCH
list: `QWidget.grab()` does not capture the GL-backed `AmbientWidget`.
`ambient.total_frames_painted()` already existed and counts the thing that
costs.

CALIBRATED FIRST, WHICH IS THIS FILE'S OWN RULE.
`tests/qt/test_the_paint_harness_can_see_a_second_layer.py` reproduces the
one-backdrop regression deliberately -- two layers must read as at least
1.5x the paints of one -- and asserts the ratio rather than a frame count,
because the count moves with the machine's load and the ratio does not. It
also pins two mistakes the harness made while being written, so neither can
come back silently:

  * `app.findChildren(QWidget)` reported 1 widget where `app.allWidgets()`
    reports 1,050. A top-level window is not a CHILD of the application
    object, and a per-widget cost over a denominator of one reads as a
    flattering result rather than as a broken measurement.
  * a bare `AmbientWidget()` measures the DEFAULT theme however the row is
    labelled: the look is read at construction. Four identical numbers
    under four theme names were the only clue.

--------------------------------------------------------------------------------
THE FIRST BASELINE, 2026-09-09. TAKEN UNDER LOAD, AND SAYING SO.
--------------------------------------------------------------------------------

`docs/perf_baseline.json`, offscreen, while the full test suite was running
in another process -- the record carries the load average and the Qt
platform for exactly that reason. Every number here is a CEILING on the
speed rather than the speed.

    BACKDROP     24.7 fps at 1080p and 24.7 at 4K, every theme
                 51 Mpx/s at 1080p, 205 Mpx/s at 4K

    THEME CHANGE 299-340 ms over 1,050 widgets, all four palettes

    INTERACTION  expand a section   47.6 ms first, 4.2 ms repeat, 2 frames
                 collapse           12.3 ms first, 3.8 ms repeat
                 type a character    0.06 ms
                 scroll              0.01 ms

THREE THINGS THE FIRST RUN ALREADY SAYS:

  1. THE FRAME RATE IS THE SAME AT FOUR TIMES THE PIXELS, which means the
     cap is holding and the per-frame COST is what varies -- 4x the
     megapixels for the same 24.7 fps. So a 4K machine is doing four times
     the shading work per second for the same picture, which is consistent
     with 327 being reported from a 3840x2160 screen and is the thread to
     pull on next.
  2. THE THEME CHANGE IS STILL A THIRD OF A SECOND. 378 measured 587.5 ms
     on 847 widgets; this is 299-340 ms on 1,050, on a loaded machine and
     offscreen. The shape is unchanged: every theme change repolishes the
     whole application.
  3. EXPANDING A SECTION DROPS TWO FRAMES THE FIRST TIME AND NONE
     AFTERWARDS. That is work done once per section, and the first time is
     the time a user meets it. Typing and scrolling are free, which is
     worth recording precisely so that nobody optimises them.

WHAT THIS BASELINE IS NOT. It is one machine, offscreen, under load. "On
all platforms" is unanswered and needs the harness in CI on all three, per
this file's own argument about setuptools 77 and the light theme. And the
fullscreen screensaver path -- 327's actual complaint -- is not measured
yet: it needs a real display, and this session has none.

--------------------------------------------------------------------------------
2026-09-09 -- FIRST FIX LANDED: THE LANGUAGE IS RESOLVED ONCE PER BUILD, NOT
ONCE PER TRANSLATED STRING
--------------------------------------------------------------------------------

Found while measuring something else -- the backdrop freeze of 385 -- which is
the pattern this item exists to break, so it is recorded here rather than
there.

THE DEFECT. `i18n.current_language()` reaches into `QSettings` on every call,
and `tr()` calls it once per string. Profiling one `PreferencesDialog(...)`
build:

    346 tr() calls -> 347 get_language() -> 415 QSettings reads   18 ms of 55

Every one of those 415 reads returns the same answer. Nothing runs between
them: it is one synchronous build.

WHY IT SURVIVED. A scope for exactly this already existed in TWO places and
neither covered `tr`. `settings_model.language_resolved_once` memoises what
*that* module resolves and is what took the Mask screen's build down from
3,516 lookups for 1,538 settings. And `i18n._RESOLVED_LANGUAGE` -- a
ContextVar read by `current_language` on both arms -- was declared, read, and
**set nowhere in the package**. `tests/qt/test_cov_r8_i18n_current_language.py`
had pinned that dead state deliberately, with a test asserting no setter
exists and a note that a future caller wiring it up "has a description to
check against".

THE FIX, three small pieces:

  * `i18n.ui_language_resolved_once()` -- a context manager that opens the
    ContextVar scope. Nested scopes share the outermost dict and only the
    outermost resets it, so a screen wrapping its build and a helper
    wrapping itself do not drop each other's cache.
  * `settings_model.language_resolved_once` opens it too, so ONE scope
    covers both mechanisms and callers do not have to know there are two.
  * `PreferencesDialog.__new__` wraps its build in it. The body moved to
    `_build_the_dialog` unchanged rather than being re-indented, so the
    diff is the wrapper and nothing else.

MEASURED, same machine, same dialog, imports already warm:

    before   55 ms   415 QSettings reads   current_language 4th by cumtime
    after    30 ms    70 QSettings reads   current_language absent from the
                                           top 28 entirely

45% off a build that happens every time the user opens Preferences.

FUNCTIONALITY IS NOT COMPROMISED, which is this item's constraint. The scope
is per-build, not per-process: a language change between two builds is seen
by the second, because the dict is discarded when the outermost scope exits.
The `SPACR_LANGUAGE` environment override still wins -- `current_language`
consults it before filling the scope -- and there is a test for that.

TESTS. `test_cov_r8_i18n_current_language.py` grew from 8 to 15: the
dead-cache class became `TestTheContextVarCacheThatIsNowOpened`, and the
"nothing sets it" assertion became "exactly `i18n.py` sets it" -- which is
the assertion worth keeping, because a SECOND setter would have both scopes
resetting the same ContextVar and each dropping the other's cache, with no
failure anywhere, only a silent return to reading the store. Plus the read
count as a test, nesting, an exception inside the body, the environment
override, and settings_model opening it. 160 tests across
`test_preferences.py`, `test_cov_s1_preferences_edges.py`,
`test_ambient_wiring.py` and `test_ambient_none.py` pass unchanged.

STILL OPEN, AND BIGGER: the FIRST `PreferencesDialog(...)` in a session costs
**902 ms**, against 55 ms (now 30) for the second. The difference is import
work -- `figure_settings`, the settings model, the catalogs -- paid on the
GUI thread at the moment the user asks for Preferences, with the backdrop
frozen for all of it. 385 has the paint counts. The backdrop's own heavy
import is already warmed off the GUI thread (`_HeavyImportInProgress`); this
one is not, and the same treatment applies.

--------------------------------------------------------------------------------
2026-09-09 -- SECOND FIX: THE 902 ms FIRST OPEN IS NOW 139 ms
--------------------------------------------------------------------------------

The "STILL OPEN, AND BIGGER" note above is closed. Broken down by import,
first `PreferencesDialog(...)` in a cold process:

    474.3 ms  spacr.qt.screens.settings_model
     84.9 ms  spacr.qt.theme
     83.3 ms  spacr.qt.imagery
     48.5 ms  matplotlib.colors
     34.3 ms  spacr.qt.widgets.toggle
                                        812 ms of import in an 812 ms build

`launch` ALREADY warms heavy imports on a daemon thread while the user looks
at the home screen (`app.py:6094`), and the list held one name. Two were
added -- `settings_model` and `imagery` -- which is 557 ms of the 812 moved
off the GUI thread. `theme` is already imported during startup by
`apply_preferences_to_app`, so it is warm by the time anyone can click.

    first open, cold          902 ms
    first open, prewarmed     139 ms
    second open                30 ms   (was 55 before the language scope)

VERIFIED THAT THE OFF-THREAD IMPORT IS ACTUALLY SAFE rather than assumed:
both modules were imported on a daemon thread with a live QApplication and a
shown widget, and the GUI thread pumped events throughout. No exception, no
Qt object constructed at import time. This is the same claim the existing
comment makes for `spacr.settings`; it is now measured for the two new names
as well.

`PREWARMED_MODULES` in `tests/qt/test_cov_qt_app.py` is updated in the same
change, which its own note demands: the list is read back out of `launch`'s
source and compared to the table, so a silent edit to either fails.

WHAT THIS DOES NOT FIX. 139 ms is still eight times the warm build. The
remainder is `matplotlib.colors` and `widgets.toggle`, neither of which is
worth a third name in the prewarm list until something else wants them.

--------------------------------------------------------------------------------
2026-09-10 -- DEFECT 2 MEASURED PROPERLY, AND THE OBVIOUS FIX IS RULED OUT
--------------------------------------------------------------------------------

The baseline's second finding -- "THE THEME CHANGE IS STILL A THIRD OF A
SECOND" -- is now decomposed. This is a NEGATIVE result and it is worth
more than a guess: it closes the optimisation everybody reaches for first.

WHERE THE TIME IS. One theme change on a Mask screen, 1,049 live widgets,
offscreen:

    stylesheet() composition          0.7 ms
    app.setStyleSheet(sheet)        262 ms
    the whole apply                 307 ms

Composing the sheet is FREE. All of it is the one Qt call. And there is
exactly ONE such call per preference change -- traced, not assumed; an
earlier count of two was this measurement's own clearing call being
counted with it. So there is no redundant apply to remove.

WHAT THE COST IS NOT: THE SHEET. The sheet is 71,472 characters, 389
blocks, ~655 selectors. Measured on the same 1,049 widgets:

    empty sheet                       0.8 ms
    ONE rule (`QWidget { color }`)   89.2 ms
    half the sheet, by characters   182.6 ms
    base only, 196 blocks, 382 sel  199.2 ms
    the whole sheet                 204.6 ms

Dropping the 42 registered widget-QSS blocks -- 25,195 characters and 273
selectors, 42 % of the sheet by size -- saves FIVE MILLISECONDS of 205.
Going from 655 selectors to ONE saves 56 %. So the curve is a step, not a
slope: there is a large fixed cost for having any sheet at all, and after
that the sheet's size barely matters. TRIMMING OR SPLITTING THE SHEET IS
NOT THE LEVER, and an afternoon spent shrinking it would buy single-digit
milliseconds.

WHAT THE COST IS: THE WIDGET COUNT. Same sheet, a window filled with plain
labels:

        1 widget       1.0 ms
      250 widgets      9.6 ms      38 us/widget
      500 widgets     19.9 ms      40 us/widget
    1,000 widgets     46.8 ms      46 us/widget
    2,000 widgets    132.0 ms      66 us/widget

Linear at first and worse than linear by 2,000. A real module screen costs
more per widget than a QLabel does -- 1,049 of them come to 205 ms, not the
50 the label curve predicts -- because they are nested, named and styled.

SO THE ONLY LEVER IS NOT REPOLISHING EVERYTHING, which is what 378 already
measured from the other side: 107 ms for the visible screen against 587 for
the application. The change that would do it is to stop setting the sheet on
the QApplication and set it per top-level window, marking hidden module
screens dirty so their `showEvent` applies it.

NOT ATTEMPTED HERE, and the reason is this item's own constraint rather than
timidity. `app.setStyleSheet` covers every widget that exists AND every one
created later -- dialogs, popups, menus, a screen built after the change.
Per-window application has to reproduce that, and the failure mode is a
dialog opening in the previous theme, which is exactly "compromising
functionality". It needs a display to verify and a session that can watch
it happen.

WHAT IS WORTH KNOWING BEFORE THAT IS TRIED: `apply_preferences_to_app`
already has a signature guard that skips the whole rebuild when nothing
visual changed, and it already clears and reinstalls screen-local QSS
overlays around the global call. Whoever does the per-window version
inherits both, and the overlay mechanism is the half that already knows how
to give one screen its own sheet.

--------------------------------------------------------------------------------
2026-09-10 -- FINDING 3 WAS THE HARNESS, NOT THE APPLICATION. WITHDRAWN.
--------------------------------------------------------------------------------

"EXPANDING A SECTION DROPS TWO FRAMES THE FIRST TIME AND NONE AFTERWARDS"
is not true, and this item's own rule -- write the harness first and prove
it catches a defect that is already there -- is what caught it, one step
late.

WHAT THE 47.6 ms WAS. `AppScreen` translates arriving subtrees from an
event filter that defers the work A TURN LATER; `_on_arrivals_in` says so
in its own docstring. `tools/perf_paint.py` built the screen, called
`app.processEvents()` ONCE, and started timing. That single call does not
drain a queue whose entries are posted for the next turn, so the build's
own deferred passes ran inside the first interaction measured and were
billed to it.

MEASURED BOTH WAYS, on the same panel, same machine, minutes apart:

    one processEvents() after the build   146.3 ms   "2 frames dropped"
    drained until quiet                     0.5 ms first, 0.1 ms after

    and through the corrected harness, which alternates collapse/expand:

    action                        first_ms  repeat_ms  frames
    expand a section                  4.23       3.80       0
    collapse a section                3.62       2.75       0
    type one character                0.07       0.01       0
    scroll the settings column        0.01       0.00       0

THE 146 ms IS REAL WORK AND IT IS NOT THE INTERACTION'S. Profiled, it is
four `retranslate_widget_tree` passes over 18,307 widgets making 88,388
`QObject.property()` calls -- the cost of BUILDING the panel, collected by
whatever ran next. Reporting it as the cost of expanding a section sends
someone to optimise a toggle that already takes a fifth of a millisecond,
which is exactly the "fix by symptom" this file forbids.

THE HARNESS IS FIXED, not worked around: `_drain_until_quiet` runs before
the first timed interaction, and its docstring carries both numbers so the
next reader can see why it exists. The `timed()` docstring's own example --
"typing the first character measured 39 ms, two dropped frames" -- was the
same artefact and is corrected in place; the same keystroke measures 0.07 ms
cold once the queue is drained.

  THE PRINCIPLE SURVIVES THE EXAMPLE. A cold number and a warm one are
  different questions and work done once per widget is real. But a "first"
  measured before the build has settled is not that work, it is the build.

WHAT THIS CHANGES ABOUT THE ITEM. Interaction latency is NOT a defect on
this screen -- nothing a user does to a settings panel costs a frame. The
two findings that survive the first run are the ones already recorded: the
theme change repolishing the whole application (a third of a second), and
the backdrop shading four times the pixels at 4K for the same frame rate.
BUILDING the panel is now a third candidate, with a number attached: 146 ms
of deferred translation, four full-tree walks, on a panel whose widgets were
already translated once as they were constructed.

--------------------------------------------------------------------------------
2026-09-10 -- THE BUILD'S 146 ms, LOCATED: THE TREE IS TRANSLATED THREE TIMES
--------------------------------------------------------------------------------

Following the number the correction above freed up. Instrumented
`_LateCaptionTranslator._translate` and counted every pass and every
descendant it walked, building one Measure screen:

    pass  batch  root                 descendants
      0      1   AmbientWidget                  1
      1      1   ModuleHeader                   6
      2      1   Divider                        0
      3      1   QSplitter                  8,034
      4      6   QWidget                    7,389
      5      6   QScrollArea                  641
      6      6   QSplitterHandle                0
      7      6   QSplitterHandle                0
      8      7   QSplitter                  7,327
      9      7   Card                          31
     10      7   QWidget                       20
     11      7   QLabel                         5
     12      7   QLabel                         0

    13 passes, 23,454 widget visits, of which THREE PASSES ARE 22,750.

THE THREE BIG ONES ARE NESTED AND IN DIFFERENT BATCHES -- 1, 6 and 7. Each
is a near-root walk triggered when another large container is parented in,
so the tree is translated roughly three times over as it is assembled, and
every widget in it was already translated once as it was constructed.

WHY THE OBVIOUS FIX DOES NOT WORK. Deduplicating roots WITHIN one
`_on_arrivals_in` batch would save nothing: the three expensive passes are
in three different batches, an event turn apart. The saving needs a marker
that survives between turns -- "this subtree was translated at generation
N" -- so a later overlapping pass skips what is already done and redoes it
only when the language actually changes.

NOT ATTEMPTED TONIGHT, and the reason is the risk rather than the work. The
failure mode of getting it wrong is a widget left in English in one of nine
locales, which is invisible on this machine and to this test suite. It IS
verifiable headlessly -- capture every translated widget's text after a
build, apply the change, compare the sets -- and that verification is the
first half of the job, not an afterthought.

    WHOEVER TAKES IT: the cost is build-time only. Interaction latency is
    now measured as clean (4 ms to expand, 0.07 ms to type, no dropped
    frames), so this is about how long a module screen takes to appear, not
    about smoothness once it is there.

--------------------------------------------------------------------------------
2026-09-10 -- THE GUARD FOR THE PER-WINDOW STYLESHEET, WRITTEN FIRST
--------------------------------------------------------------------------------

This item's own method is "WRITE THE HARNESS FIRST, and prove it catches a
defect that is already there". The remaining lever -- setting the sheet per
top-level window instead of on the QApplication -- has not been written
because of a named risk: "the failure mode is a dialog opening in the
previous theme, which is exactly compromising functionality", and "it needs
a display to verify and a session that can watch it happen".

THE SECOND HALF OF THAT IS TRUE OF BEAUTY AND NOT OF THIS. Whether a theme
LOOKS right needs a display. Whether a widget created after a theme change
is painting the PREVIOUS theme does not: a stylesheet colour resolves into
the widget's palette, and an offscreen widget reports it.

    QLabel { color: rgb(200,100,50) }  ->  palette WindowText #c86432

measured on this box, offscreen, after `ensurePolished()` and one
`processEvents()`.

SO THE GUARD IS IN, BEFORE THE CHANGE:
`tests/qt/test_a_dialog_never_opens_in_the_previous_theme.py`. Five cases,
holding exactly one property and nothing about aesthetics -- a widget
created AFTER a theme change resolves to the SAME theme as one created
before it:

  * a plain widget made after the change;
  * one that existed across it;
  * A DIALOG opened after it, which is 380's own sentence for the risk and
    is its own top-level window -- precisely what a per-window sheet has to
    remember;
  * a whole QMainWindow built after it, which is the module screen the fix
    would mark dirty and re-sheet on `showEvent`.

AND A NEGATIVE CASE THAT MAKES THE OTHER FOUR MEAN SOMETHING. Under
`app.setStyleSheet` all four pass for free, because a global sheet covers
widgets that do not exist yet -- so on their own they would pass for ever
and prove nothing about the change they exist to protect. The fifth does
the wrong thing ON PURPOSE: sets the sheet on ONE window's tree, the way a
per-window implementation looks when it forgets a window, and asserts a
second top-level does NOT get it.

WHAT IS STILL NOT DONE, and now for a smaller reason. The change itself:
`apply_preferences_to_app` already has a signature guard that skips the
rebuild when nothing visual changed, and already clears and reinstalls
screen-local QSS overlays around the global call, so the overlay mechanism
is the half that already knows how to give one screen its own sheet.
Whoever writes it now has a test that fails the moment a window is
forgotten, which is the part that made writing it unsafe.

  MENUS AND TOOLTIPS ARE STILL UNCOVERED BY THIS GUARD and are the
  remaining display question: they are top-level windows Qt creates
  itself, and a test cannot open a native menu. That is a narrower thing
  to ask someone to look at than "does the theme still work".

--------------------------------------------------------------------------------
2026-09-11 -- THE TRIPLE TRANSLATION IS FIXED. 67-74% OF THE BUILD'S
TRANSLATION WORK REMOVED, AND THE STRINGS ARE BYTE-IDENTICAL
--------------------------------------------------------------------------------

The 2026-09-10 note above located the defect and stopped, for a stated
reason: "the failure mode of getting it wrong is a widget left in English in
one of nine locales, which is invisible on this machine and to this test
suite. It IS verifiable headlessly -- capture every translated widget's text
after a build, apply the change, compare the sets -- and that verification is
the first half of the job, not an afterthought."

THE VERIFICATION WAS DONE FIRST AND IT IS WHAT MAKES THIS LANDABLE.
`tools/translate_census.py` builds a module screen exactly as `MainWindow`
does, lets the deferred passes run, and records every visible string on every
widget keyed by its structural path -- text, tooltip, window title,
placeholder, group title, accessible name and description. Four modules, two
locales, before and after:

    module           lang   strings   only-before  only-after  changed
    measure          sv       3,777         0           0         0
    measure          ko       3,777         0           0         0
    mask             sv         532         0           0         0
    mask             ko         532         0           0         0
    classify         sv         272         0           0         0
    classify         ko         271         0           0         0
    analyze_plaques  sv         110         0           0         0
    analyze_plaques  ko         110         0           0         0

BYTE-IDENTICAL, with this much of the work gone:

    module           per-widget translation calls    reduction
    measure              72,139  ->  18,457            74.4 %
    mask                 13,211  ->   3,840            70.9 %
    classify              8,256  ->   2,557            69.0 %
    analyze_plaques       3,376  ->   1,116            66.9 %

THE CHANGE, which is the marker the 2026-09-10 note said was needed --
"a marker that survives between turns, so a later overlapping pass skips what
is already done and redoes it only when the language actually changes":

  * `retranslate_widget_tree` stamps every widget it visits with
    `<language>@<catalog generation>`, as a Qt dynamic property so it
    survives the Python wrapper being collected and recreated.
  * A new keyword-only `only_new=` skips widgets whose stamp still matches.
  * `_LateCaptionTranslator._translate` -- the three near-root passes, an
    event turn apart, that were the whole defect -- is the ONE caller that
    asks for it.
  * `add_translation` moves the generation, because apps hand their display
    name to the catalogs as they register and that happens while screens
    already exist. A stamp that did not move with the catalog would leave
    that name in English for the life of the process.

OFF BY DEFAULT, and that is the safety argument rather than caution.
`_translate_qt_text` detects a caption replaced by application code by
comparing what it last rendered, and a skipped widget is not compared -- so
every caller that has just set a caption itself keeps the full walk. Only the
build-time passes, where nothing has run between them, opt in.

WHY THE STAMP CARRIES THE LANGUAGE rather than being a boolean. A boolean
"already translated" flag would pin the first language a process ever
rendered, and there is a test for exactly that.

AND ONE MEASUREMENT MISTAKE WORTH RECORDING, because it cost an hour and
would have been reported as "the change does nothing". The census script
lives outside the worktree, and a Python SCRIPT puts its own directory on
`sys.path` -- not the working directory. So it imported the INSTALLED spacr
and measured 72,085 calls before and 72,088 after, from the same unchanged
copy. The tell was in the output all along: the line numbers it attributed
the calls to did not exist in the file being edited.

    A HARNESS THAT REPORTS NO CHANGE MAY NOT BE LOOKING AT THE CHANGE.
    Check which file it loaded before concluding anything about the edit.

AND THE FIRST VERSION OF IT WAS WRONG, caught by the suite rather than by
the census, which is why both exist. A `QTabWidget` renders its TABS, and a
tab can arrive without the strip itself changing at all -- so a stamped
strip meant the second fold opened on the Classify screen got an English tab
over a Swedish page (`test_activation_folds_into_classify::
test_the_second_fold_opened_is_translated_as_well`). Combo items and table
and tree headers are the same shape: a caption CACHE keyed on a length that
can grow.

  A WIDGET WHOSE CAPTIONS ARE A COLLECTION IS NEVER "ALREADY DONE". Those
  four classes are exempt from the skip, which costs one isinstance per
  widget and is why the saving is 74.4% rather than 74.5%.

  THE CENSUS COULD NOT HAVE FOUND IT. It builds a screen ONCE; the defect
  needs a second subtree arriving on a strip that a previous pass already
  saw. A whole-output comparison is necessary and is not sufficient.

THE GUARD: `tests/qt/test_the_tree_is_translated_once_not_three_times.py`.
Six tests, five of which hold the OUTPUT and one the cost:

  * the build renders what a full pass renders -- built with the skip, then
    walked again with it off, and nothing changes;
  * a language change after the build reaches every widget;
  * a row catalogued after a pass is not missed;
  * a strip that gains a tab is not already done;
  * `only_new` is off by default;
  * and the ceiling: at most two widget visits per widget in the tree.

CALIBRATED AGAINST THE DEFECT, per this file's own rule. With the change
taken back out the ceiling test reports "852 widget visits to translate 252
widgets (3.4 passes over the tree)" and fails; the other four pass in both
states, which is what a safety property should do.

WHAT THIS DOES NOT FIX. The cost is build-time only. Interaction latency was
already measured clean, so this is about how long a module screen takes to
appear, not about smoothness once it is there.

--------------------------------------------------------------------------------
2026-09-11 -- THE THEME CHANGE IS NOT A THIRD OF A SECOND. IT IS TEN SECONDS,
AND FOUR FIFTHS OF THE WORK IS ON SCREENS NOBODY CAN SEE
--------------------------------------------------------------------------------

The 2026-09-09 baseline recorded "THE THEME CHANGE IS STILL A THIRD OF A
SECOND ... 299-340 ms over 1,050 widgets". THAT WAS MEASURED ON A FRESH
SESSION WITH ONE SCREEN BUILT, and it is the best case rather than the
typical one. `MainWindow` builds a module screen on FIRST NAVIGATION
(`app.py:4950`, `if key not in self._screens`) and keeps it in `_screens`
and in the stack afterwards, so the cost grows with every module the user
opens and never comes down.

MEASURED, offscreen, one module opened at a time, each number the best of
several alternating dark/light applications:

    modules   live      on      hidden   hidden   app.setStyleSheet
    opened    widgets   screen              %
      1        1,693    1,278        0     0.0 %       721 ms
      2        5,833    4,138    1,278    21.9 %     5,350 ms
      3        6,529      695    5,416    83.0 %     5,914 ms
      4        9,041    1,488    6,111    67.6 %     8,000 ms
      5        9,354      312    7,599    81.2 %     7,635 ms
      6       10,009      654    7,911    79.0 %     8,560 ms

FOUR FIFTHS OF EVERY THEME CHANGE IS SPENT REPOLISHING SCREENS THAT ARE NOT
ON SCREEN. A user who has opened six modules and then changes a preference
waits eight and a half seconds, offscreen and unloaded, for a picture in
which 79 % of the work is invisible by construction.

--------------------------------------------------------------------------------
AND THE PER-WINDOW FIX WORKS -- 10.2 s -> 3.1 s THROUGH THE REAL PATH
--------------------------------------------------------------------------------

The 2026-09-10 note left this as "NOT ATTEMPTED HERE ... it needs a display
to verify and a session that can watch it happen", and the 2026-09-10 guard
note narrowed that to menus and tooltips. Both are now answered.

FOUR LEVERS, EACH IN ITS OWN INTERPRETER. The first version of this
measurement ran them in one process and reported the do-nothing baseline at
2,836 ms and two levers at 7,400 -- which says more about the order than
about the levers, because a lever measured after three others have already
re-sheeted the application is not the same measurement as the first.
Four modules open, 9,045 widgets, 6,111 of them hidden:

    lever                          first      steady
    app.setStyleSheet            2,836 ms   ~7,500 ms
    every top-level window       1,684 ms   ~1,900 ms
    app + updates disabled       2,854 ms   ~8,000 ms   NO HELP
    the visible screen alone       222 ms     ~230 ms   the floor

  `setUpdatesEnabled(False)` AROUND THE CALL DOES NOTHING, which is worth
  recording because it is the first thing anybody tries. So does parking
  hidden screens out of the stack for the duration -- and that one also
  crashed the process, so it is ruled out twice.

AND THROUGH `apply_preferences_to_app`, WHICH IS WHAT THE PREFERENCES
DIALOG ACTUALLY RUNS -- palette, overlays, field fade, button roles and
ambient preferences included, six alternating saves:

    before   3,641 ms first,  10,165 ms median
    after    2,352 ms first,   3,057 ms median

A PREFERENCE SAVE WENT FROM TEN SECONDS TO THREE. Not a benchmark of one
Qt call: the whole thing the user waits for.

THE CHANGE. `theme.apply_stylesheet_per_window(app, sheet)` puts the sheet
on every top-level window instead of on the QApplication, records it on the
application so a window born later can find it, and installs ONE event
filter that sheets any window that appears afterwards. The serial the filter
stamps makes `Polish` and `Show` cost one application rather than two, and
`preserve_widget_qss_overlay` keeps a screen's own late block appended, so
the overlay mechanism this file already pointed at keeps working unchanged.
`apply_preferences_to_app`'s signature guard now reads the sheet back with
`window_stylesheet(app)` -- `app.styleSheet()` is empty now and comparing it
would report a change on every save, which is the rebuild that guard exists
to skip.

MENUS AND TOOLTIPS ARE COVERED, and that was recorded here as "the
remaining display question ... a test cannot open a native menu". True, and
beside the point: a QMenu and a tooltip are ORDINARY TOP-LEVEL QWIDGETS, so
they get the same `Polish` event as anything else and the filter reaches
them without knowing they exist. Verified offscreen alongside a dialog and
a whole QMainWindow -- all four resolve to the theme in force when they were
created, not the previous one.

THE GUARD WAS WRITTEN FIRST AND IS WHY THIS WAS SAFE TO DO.
`tests/qt/test_a_dialog_never_opens_in_the_previous_theme.py` grew from 5
tests to 13: the original five hold the PROPERTY under any implementation,
including the negative case that makes them mean something, and eight new
ones exercise this implementation -- a window that lived across the change,
four kinds of window born after it, the application sheet staying empty
(which is where the saving comes from, stated as a property so it cannot
quietly come back), and the serial not re-sheeting a window on every Show.

WHAT IT COSTS, BECAUSE A FIX WITH NO COST RECORDED IS A FIX NOBODY CAN
JUDGE. `QCoreApplication::installEventFilter` is the special one -- it
receives EVERY event sent to EVERY object in the thread, not only events
for the application object -- so a Python callback goes on the path of
every keystroke, every mouse move and every paint. Measured by delivering
4,000 key events to a field on a 1,043-widget Mask screen, twice each way
in fresh interpreters:

    filter absent      8.6-8.9 us per delivered event
    filter installed  11.3-11.5 us per delivered event

+2.7 us. Against a keystroke this file already measures at 60 us end to
end, that is 4.5 %, and against a 16 ms frame it is nothing. Interaction
latency through `tools/perf_paint.py` is unchanged: 3.6 ms to expand a
section, 0.06 ms to type, no dropped frames.

AND THE APPLICATION SHEET IS TAKEN DOWN when the windows take it over,
once, guarded -- because an application sheet left standing goes on
applying to every widget and an OLD one shows through as a colour from the
wrong theme. In production that clear is paid at startup with one small
window on screen, never on the preference save this change exists to make
cheap. Between TESTS it is a different matter, and
`tests/qt/conftest.py::_the_window_stylesheet_is_left_as_it_was_found`
undoes the whole mechanism after each one: a window sheet outranks the
application sheet for its tree, so a test that applies a theme the direct
way was building its widgets inside a window still wearing an earlier
test's theme. Measured as `HomePage inlines #000000 (dark bg)` under the
light, cell and glass themes, in a run of 1,045 and in none of the three
files alone.

AND THE CHANGE HAD A REAL BUG IN IT, FOUND BY THE SWEEP RATHER THAN BY THE
GUARD. A PARENTLESS WIDGET IS A WINDOW -- Qt says so -- and several of them
set their own rules. Under `QApplication.setStyleSheet` those rules were
MERGED with the global ones by Qt; replacing the window's sheet outright
threw them away.

    NOT A TEST ARTEFACT. It cost `test_field_fade::
    test_the_text_stays_fully_opaque_all_the_way_across` an alpha of 254
    where it demands 255 -- DETERMINISTICALLY, which is what makes it
    evidence. Six runs each, same command: 6/6 red with the fix disabled,
    6/6 green with it, 6/6 green before the per-window change existed.

    AND A CLAIM MADE HERE FIRST TIME ROUND WAS WRONG, corrected rather
    than quietly dropped. This note originally also blamed the change for
    `test_provider_marks_uncovered_paths`, on "10/10 green before, 8/10
    after, 8/8 after the fix". Those were three small samples of a test
    that is INHERENTLY FLAKY, and fifteen runs each say so:

        before the change   11/15
        after the fix       13/15

    The pre-change tree is the flakier of the two. The change did not
    cause it and the fix did not cure it; three runs of ten looked like a
    signal because the sample was too small to be one.

      A TEST THAT PASSES TEN TIMES IS NOT A DETERMINISTIC TEST, and the
      sweep read this one as ORDER-DEPENDENCE for the same reason. It is
      an open flake in its own right -- two identically built
      `ProviderMark` widgets render differently about a fifth of the time
      -- and it belongs to whoever next touches that widget.

The window's own sheet is now remembered the first time and appended AFTER
the global one, where it still wins. Remembered rather than re-read,
because by the second pass the widget is wearing ours and reading it back
would fold the global rules into "its own" and grow the sheet at every
theme change. Both properties are pinned in the guard file, which is now
15 tests.

  WHAT IT SAYS ABOUT THE SWEEP: a change whose blast radius is public Qt
  state does not show up in the tests written for it. Seven tests moved,
  and only one of the seven was about theming.

--------------------------------------------------------------------------------
2026-09-11 -- THE LAST LEVER IS PRICED AND PROVEN REACHABLE
--------------------------------------------------------------------------------

"Not sheeting the hidden screens either" was recorded as a bigger change
with a number attached. It now has a shape as well, measured rather than
sketched, because the question it turned on was whether the set to sheet
can be enumerated at all.

WHY THE WINDOW CANNOT BE THE TARGET. Qt cascades a window's sheet to every
descendant, and with four modules open the window holds 8,002 widgets of
which 7,595 are inside screens -- 95 %, of which one screen is visible.
Sheeting the window necessarily repolishes every hidden screen, which is
the whole of the remaining cost.

THE CHROME IS 407 WIDGETS AND IS NOT WHERE ONE WOULD GUESS. It is not
reachable from the central widget's children: of those 407, exactly TWO are.
The rest hang off the window directly -- the dock, the sidebar, the status
strip -- so an implementation that walked the central widget would leave
405 widgets unstyled and look almost right.

    ENUMERATED PROPERLY -- the window's direct children except the central
    widget, the central widget's direct children except the stack, and the
    stack's CURRENT page -- it is 27 targets reaching 1,525 of the 8,002.

MEASURED, four modules open, alternating dark and light:

    sheeting the whole window          945 ms first, ~1,479 ms steady
    chrome + the visible screen        300 ms first,  ~310 ms steady

4.8x, on top of the 4x the per-window change already banked.

THE HESITATION IS THE FAILURE MODE, NOT THE WORK. Every widget that is
not reached is unstyled, and the thing that makes this cheap -- touching
1,525 widgets instead of 8,002 -- is exactly what makes a missed root
invisible until somebody opens the screen it was on. The per-window change
was a quarter of this size and moved seven tests, one of which was a real
regression that only a pixel assertion caught.

    SO IT NEEDED TWO THINGS THAT DID NOT EXIST, AND THE FIRST IS THIS:
    `tests/qt/test_every_widget_wears_the_current_theme.py`, seven tests,
    written before the change per this file's own method.

      * every widget in a synthetic window wears the sheet, at depth;
      * a widget parented in AFTER the change wears it too, which is the
        half a dirty-mark has to cover;
      * a real `MainWindow` with two modules open, sampled at ~200 of its
        widgets;
      * and TWO negative cases, because without them the rest would pass
        under any implementation that sheets the window and say nothing
        about one that does not. The second does the narrow
        implementation's mistake ON THE REAL WINDOW -- sheets the central
        widget's tree only -- and asserts the probe notices the chrome it
        left behind.

    TWO INSTRUMENT ERRORS WORTH KEEPING, because both produced confident
    wrong numbers on the way:

      * reading the colour of the labels ALREADY in the window said 146 of
        480 were unstyled. They were not: they carry their own colour --
        `ElidingLabel`, `HomeSectionNote` -- so "not wearing the sentinel"
        means "overridden" as often as "never reached". A FRESH label
        parented under a widget resolves purely from the cascade there,
        which is the actual question.
      * even then 4 of 204 hosts reported stale, and those four SET THEIR
        OWN COLOUR: their rule is appended after the global sheet and wins
        for their subtree, so a probe under them cannot answer either way.
        They are excluded by asking whether the host's own stylesheet
        mentions a colour.

    THE SECOND THING is the dirty-mark on hidden screens, so a screen
    shown later is sheeted on its `showEvent`. The event filter that
    already catches new windows is the natural home for it, and the guard
    above is what makes writing it safe. Both now exist; the section below
    is what happened when they were used.


--------------------------------------------------------------------------------
2026-09-11 -- THE LAST LEVER, TAKEN
--------------------------------------------------------------------------------

`MainWindow.stylesheet_roots()` now names the widgets that carry the
application sheet instead of the window, and `theme._roots_for` asks every
window for that list. A window that does not answer -- every dialog, menu,
tooltip and test double -- is sheeted whole, exactly as before, which is
why this is a change to ONE window rather than to the theme engine.

MEASURED END TO END, same process, same window, four modules open, the two
arms differing in one decision and nothing else: `stylesheet_roots`
answering its real list, against the same function answering `[self]`,
which is the fallback any non-nominating window already takes.

    8,008 widgets, 6,432 of them inside screens that are not on show

    nominated roots      1,323 ms first, ~1,700 ms steady
    the whole window     ~3,930 ms steady

`apply_preferences_to_app` end to end, not the `setStyleSheet` call alone,
so it includes the palette, the overlays and the language pass. 2.3x, on
top of the 4x the per-window change banked: a preference save on this
session went 10.2 s -> 3.1 s -> 1.7 s.

THE ONE THING THE GUARD FOUND, AND IT WOULD NOT HAVE BEEN FOUND BY READING.
The obvious implementation sheets the stack's PAGES -- `stack.widget(i)`
for the current index. The sidebar's `EdgeDrawer` is parented to the stack
and is NOT one of its pages, so that loop misses it and every dock row
inside it. The guard sampled 207 widgets of a real two-module window and
reported exactly one genuinely unsheeted host, with its ancestry; the fix
is `findChildren(..., FindDirectChildrenOnly)` over the stack rather than a
loop over indices, and the visibility test is `is current or isVisible()`
for the same reason -- the drawer is on screen BESIDE the page, and "the
one the stack would raise" is a different question from "the one the user
can see".

    Reach after the fix, same sampler: 207 sampled, 161 sheeted, 46 inside
    a hidden page, 0 genuinely missed.

HIDDEN PAGES ARE MARKED, NOT SHEETED. `mark_as_a_sheet_target` sets a
property; the event filter that already sheets every new window now also
sheets a marked widget on its `Polish` or `Show`, which is before its first
paint. That is what makes leaving 6,432 widgets alone safe rather than
merely cheap.

WHAT MOVED IN THE TESTS, and neither was a regression:

  * `test_photo_themes.py::test_windows_are_repainted_for_the_new_theme`
    asserted the WINDOW carries a sheet. It deliberately does not any more,
    so it asks its roots instead.
  * `test_home_layout.py` ratcheted 29 -> 30 apps. NOT this change: it
    fails identically without it, and the 30th is `embeddings` from 386.

WHAT IS LEFT. Nothing on this lever. The remaining ~1.7 s is the palette,
the overlays and the 1,525 widgets that genuinely do need repolishing, and
cutting into that is a different item from this one.


--------------------------------------------------------------------------------
2026-09-11, THE SAME EVENING -- THE LEVER COST THREE DEFECTS, ALL OF ONE KIND,
AND THE REACH MEASUREMENT COULD NOT HAVE FOUND ANY OF THEM
--------------------------------------------------------------------------------

The section above records the change as taken and its reach as "207
sampled, 161 sheeted, 46 inside a hidden page, 0 genuinely missed". THAT
MEASUREMENT WAS TAKEN THE WRONG WAY ROUND. It opened its modules and THEN
applied the sheet. spaCR applies the sheet at launch and the user opens
modules afterwards, so the measurement sampled the one ordering in which
the change works.

THREE DEFECTS, and they are one defect wearing three coats: CODE THAT
ASSUMED THE APPLICATION CARRIED THE THEME.

  1. A MODULE OPENED AFTER LAUNCH WORE NO SHEET AT ALL. `stylesheet_roots`
     marks the pages that exist when it runs, and it runs while a sheet is
     being applied. A page created later was never marked and the window is
     deliberately bare, so it inherited nothing. A probe under a newly
     opened module resolved to `#000000` on the dark theme -- black text on
     a dark screen, a whole module wide. That is the ordinary path, not an
     edge.

     Fixed by marking at each of the three places a page joins the stack,
     so the event filter sheets it on `Polish` or `Show`.

  2. MARKED, SHEETED, AND THEN WIPED. Marking alone did not fix it, and the
     trace said why in two lines: the filter sheets the screen while it is
     still PARENTLESS during `__init__` -- a parentless widget is a window --
     and `AppScreen._sync_page_palette` then calls `setStyleSheet("")` on
     its own root. Correct when the application carried the theme and a
     screen's own sheet was a local suffix; fatal now the screen carries
     the whole 73 KB.

     TWO FIXES, because the two halves fail at different times.
     `_sheet_one_window` now compares a DIGEST of the text it last applied
     against the widget's current stylesheet, so a sheet replaced by anyone
     is repaired at that widget's next polish. And `_sync_page_palette`
     writes through `set_a_sheeted_widgets_own_rule`, which replaces the
     screen's OWN rule and re-applies the window sheet under it -- because
     a page that is already on show never gets another polish, and the
     digest alone left it black until the next theme change.

       A CACHE KEY THAT RECORDS WHAT WE DID RATHER THAN WHAT IS TRUE WILL
       EVENTUALLY BE WRONG. Nothing in Qt promises that a stylesheet we set
       is the stylesheet that is there.

  3. THE LATE-BLOCK SEAM STOPPED WORKING ENTIRELY, and this one was
     invisible to every test. `ensure_widget_qss_applied` -- the thing that
     puts a block registered at a screen's import onto that screen before
     it paints -- began:

         app_sheet = app.styleSheet()
         if not app_sheet:
             return False

     on the reasoning that a caller who never opted into spaCR styling is
     not opted in by constructing one of its widgets. Per-window sheeting
     takes the application sheet DOWN on purpose. So in every production
     run that guard read "nobody opted in" and the function did nothing.

       MEASURED: opening one module registers four blocks -- `SettingsBox`,
       `ClassEditor`, `SettingAlphabetChip`, `TableChip` -- and NOT ONE of
       the four reached the screen that had just imported them. That is
       precisely the defect `tests/qt/test_widget_qss_is_complete.py`
       exists for, reintroduced from underneath by a change to who owns
       the sheet.

     `test_a_late_registered_block_reaches_its_screen_before_first_paint`
     could not see it: its fixture applies the theme with
     `app.setStyleSheet`, so the old question still had the old answer
     there. The seam now asks `app.styleSheet() or window_stylesheet(app)`,
     and a new guard applies the theme the way the application does.

WHAT THIS SAYS ABOUT THE SWEEP, and it is the same lesson as the per-window
step one size up. A change whose blast radius is "who owns a piece of
global Qt state" is not tested by the tests written for the change. The
per-window step moved seven tests, one a real regression. This one shipped
three defects, all found within the hour, all by guards written for it --
and the first was live on nightly for about an hour before it was found.

  THE GUARD FOUND TWO AND THE THIRD NEEDED A DIFFERENT QUESTION. Asking
  "does every widget wear the sheet" catches 1 and 2. Nothing about the
  cascade catches 3, because the block that never arrived was not
  overridden -- it was never written, and a widget with no rule looks
  exactly like a widget whose rule says nothing.

STILL WORTH IT, on the numbers, and the numbers are unchanged by the three
fixes: nominated roots ~1,671 ms against ~3,739 ms for the whole window,
same process, same window, four modules open. But the honest price of the
4.8x is an evening of defects in code that had no reason to expect the
change, and any future step in this direction should budget for that rather
than for the diff.


--------------------------------------------------------------------------------
2026-09-11 -- AREA 3 IS MEASURING 31 OF 45 MODULES, AND THE FOURTEEN IT MISSES
ARE THE ONES THE USER REACHES THROUGH A DOOR THE DRIVER DOES NOT KNOW ABOUT
--------------------------------------------------------------------------------

The 2026-09-09 audit above concluded that areas 1, 3 and 5 "need running and
watching, not building". Run and watched. `tools/spacr_startup_benchmark.py
--offscreen --runs 2 --record-only`, cold and warm, both processes agreeing
exactly:

    47 readiness records, 32 within budget, 15 not, 14 ERRORED
    every error identical: "expected one live sidebar button, found 0"

THE FOURTEEN ARE NOT RANDOM. They are `feature_explorer`, `convert`,
`external_masks`, `lineage`, `layer_viewer`, `tabulate`, `outliers`,
`control_chart`, `train_compare`, `plate_view`, `profiler`,
`investigate_hit`, `feature_dict` and `trellis` -- every one of them in
`TILELESS_APPS`, and every one reached from a HOST MODULE'S BUTTON, from
Help, or from Ctrl+K rather than from a sidebar row. Verified directly: a
real `MainWindow`, drawer opened, `findChildren(QAbstractButton)` over the
whole window finds NO button carrying any of those fourteen `navKey`s.

  THIS HAS HAPPENED BEFORE AND THE DRIVER SAYS SO IN ITS OWN COMMENT.
  "NINE KEYS HAVE TWO ROWS... BUT THIS DRIVER DEMANDED EXACTLY ONE AND
  ERRORED ON ALL NINE, which is how instruction 284's ratchet stopped
  measuring nine modules without anyone noticing -- 314's own suspicion,
  that 'the ratchet is not running on the path the user actually takes',
  made concrete."

  The fix then was to prefer the registry row over the fold child. THAT FIX
  HAS SILENTLY STOPPED WORKING: `isFoldChild` is now READ in
  `spacr/qt/startup_benchmark.py:316` and SET NOWHERE IN THE TREE -- one
  grep, no matches outside that line. The rows it discriminated between no
  longer exist, so the branch is dead code guarding nothing, the count went
  from nine unmeasured modules to fourteen, and the artifact reports
  `passed: false` with 463 violations, which reads as a catastrophe rather
  than as "the driver cannot find fourteen doors".

    A RATCHET THAT ERRORS IS NOT A RATCHET THAT FAILED. Fourteen errors and
    463 violations look like a broken application; they are a broken
    measurement, and the difference is invisible from the summary line.
    This is the same shape as the two stale premises 381 recorded tonight
    -- a check whose world moved underneath it -- in a third form: a probe
    that navigates by a structure the application stopped having.

WHAT THE THIRTY-ONE IT DID MEASURE SAY. Under load, so every number is a
CEILING rather than a speed -- CI shard 11 was running in another process
and the 1-minute load average was 19.8 on 32 cores. Recorded anyway,
because the SHAPE is not a load artefact:

    Home                 2.85 s     within the 5 s budget
    measure             14.42 s     OVER the 10 s module budget
    regression           9.09 s     inside it, and not by much
    mask                 6.57 s
    classify_merged      6.04 s
    umap                 3.91 s
    everything else      under 2.5 s, most under 0.5 s

  ONE MODULE IS FOUR TIMES THE NEXT-SLOWEST AND TWICE ANY OTHER. `measure`
  is the defect area 3 exists to name, and the shape -- one dominant step
  rather than thirty small ones -- is exactly what this item's own opening
  asks to be distinguished: "a 3-second startup with one 2.4-second step is
  a different problem from thirty 100 ms steps."

WHAT IS NEXT HERE, in order, and the first is not the fix:

  1. A CLEAN RUN. These numbers were taken at load 19.8 and are ceilings.
     `measure` at 14.42 s against a 10 s budget is a large enough margin
     that load alone is unlikely to explain it, but "unlikely" is not the
     standard this file sets for anything else.
  2. THE DRIVER'S FOURTEENTH DOOR. Pressing a host module's button, a Help
     entry or the command palette is how a user reaches these, so that is
     what the driver has to do. `TILELESS_APPS` already records which host
     each one belongs to, in a comment beside every key.
  3. THEN `measure`, with the breakdown area 3 asks for: registry lookup,
     screen construction, settings build, first paint.


--------------------------------------------------------------------------------
2026-09-11 -- `measure` IS THE AREA 3 DEFECT: 13 SECONDS, ONE STALL, AND SIX
EXPLANATIONS MEASURED FALSE
--------------------------------------------------------------------------------

CONFIRMED ON A SETTLED BOX, load 4.0, with the driver's second door open so
nothing errors and all 45 modules are timed:

    run   __home__            measure
    1     2.84 s, stall 503ms    13.36 s, stall 13,017 ms   OVER 10 s
    2     1.66 s, stall 507ms    13.27 s, stall 12,939 ms   OVER 10 s

and 13.01 / 12.87 on the previous pair. Four measurements, two sessions,
12.9-13.4 s every time.

IT IS ONE STALL, NOT MANY STEPS. `worst_event_loop_stall_ms` is ~13,000
against a total of ~13,300: the GUI thread does not answer for essentially
the whole open. `mask` (5,722 ms) and `regression` (8,194 ms) have the same
shape, so EVERY module open freezes the interface for its duration. That is
the 315 class -- "the desktop offering to kill spaCR" -- one report away.

WHERE IT IS, FROM THE SUITE'S OWN TIMING LOG:

    build screen  [measure]        7,067.9 ms
      build widgets [67 settings]    307.3 ms

so 6.8 s of the construction is NOT the settings, and the remaining ~6.3 s
of the 13.4 is after the build, between construction and a painted usable
state. THE COST IS NOT PROPORTIONAL TO SETTINGS, which is the first thing
anyone would assume:

    screen             build      widgets   settings
    measure            7,068 ms     307 ms      67
    regression         4,475 ms     638 ms      88
    mask               3,436 ms     460 ms     193
    classify_merged    3,042 ms     615 ms     108

`mask` builds three times the widgets in half the time. `measure` has the
FEWEST settings of the heavy screens and the longest build.

SIX EXPLANATIONS TESTED AND EACH ELIMINATED, because this file's method is
that a cause is measured rather than argued:

  * NOT IMPORTS. Re-run with `SPACR_TIMING_IMPORTS=1` -- which the driver
    hard-codes to 0, so a lazy import inside a build is billed to
    `build screen` and named nothing. 208 timed imports, 7.13 s total, and
    filtering them by timestamp: ZERO fall inside the measure build window,
    0.000 s.
  * NOT THE BACKDROP. 1.21 s with ambient on against 1.71 s off at
    1400x900, and 1.57 s against 2.10 s at 3840x2160. Off is SLOWER, which
    also disposes of the obvious follow-up.
  * NOT THE ISOLATED HOME. The driver's worker runs with `HOME`,
    `XDG_CACHE_HOME`, `XDG_CONFIG_HOME`, `SPACR_NO_SETUP`, `SPACR_TIMING`,
    `MPLBACKEND` and `PYTHONDONTWRITEBYTECODE` all set; applying every one
    of them to an in-process probe gives 1.28 s cold and 1.15 s warm.
  * NOT LOAD. The 1-minute average was 19.8 for the first pair and 4.0 for
    the second; the numbers moved by 3 %.
  * NOT THE DEGRADED-SHUTDOWN STATE. Killing experiments tripped spaCR's
    own "did not shut down cleanly 2 times running, so this start has no
    animated background". Cleared by one clean run; the figure is
    unchanged.
  * NOT THE INOTIFY EXHAUSTION this machine reports. `max_user_instances`
    is 128 with 87 in use, filled by concurrent Qt processes of my own --
    and `QFileSystemWatcher` appears NOWHERE in `spacr/`, so the watches
    are Qt's own and not something spaCR waits on.

    THE INSTRUMENT WAS CHECKED BEFORE THE FINDING WAS KEPT, and two of the
    six confounds were of my own making. A number measured on a machine
    the measurer has disturbed is a number about the disturbance.

WHAT IS ACTUALLY LEFT, STATED AS THE OPEN QUESTION IT IS. Building
`MainWindow` directly in-process and pressing `mask` then `measure` gives
`_build_screen` at 0.85 s for the same screen -- AN 8x GAP THAT APPEARS
ONLY THROUGH `spacr.qt.run()`'s full launch. So the cost is state the
launch sets up, not the screen, and the next step is to name the blocking
call rather than to optimise anything.

  WHAT WAS TRIED AND DID NOT WORK, so nobody repeats it. The suite has the
  right instrument -- `tools/watch_the_gui_thread.py` dumps the GUI
  thread's stack whenever it stops answering, and the last frame IS the
  blocking call -- but it could not be pointed at the benchmark. Driving
  `qt.run()` in-process with the watchdog attached makes Home itself time
  out at 30 s, so the launch is sensitive to being hosted. Injecting the
  watchdog into the driver's `python -c` worker through a `sitecustomize`
  on its `PYTHONPATH` runs (the marker file proves it) but the patched
  `QApplication` is never constructed, so nothing attaches.

  THE NEXT PERSON SHOULD PUT THE WATCHDOG INSIDE `spacr.qt.launch` behind
  an environment flag rather than trying to reach in from outside. The
  benchmark already has six such flags and this is the seventh.


--------------------------------------------------------------------------------
2026-09-11 -- THE CAUSE, NAMED AND PRICED: 135,800 EVENTS PER MODULE OPEN,
MULTIPLIED BY EVERY APPLICATION-WIDE EVENT FILTER
--------------------------------------------------------------------------------

The section above leaves the 8x gap as an open question and says the next
attempt should put the watchdog INSIDE `launch` rather than reach in from
outside. Done -- `spacr/qt/stall_watch.py`, behind `SPACR_WATCH_GUI_STALLS`,
one `os.environ.get` when off -- and it answered.

FIRST IT EXPOSED A FLAW IN ITSELF, which is worth recording because it is
the same shape as the evening's other instrument errors. One snapshot per
stall gave FOUR STALLS WITH FOUR DIFFERENT LAST FRAMES: an event filter, a
screen constructor, a settings-search install, another event filter.

  ONE SNAPSHOT NAMES WHERE THE THREAD WAS, NOT WHERE THE TIME WENT, and
  those are different questions whenever the stall is a LOOP rather than a
  single blocking call. A list of four suspects reads like an answer.

So the watch now samples for as long as the stall lasts and counts the
innermost frames. Six stalls, 63 samples, aggregated:

    IN AN APPLICATION-WIDE EVENT FILTER   32 of 63 = 50.8 %

       14   22.2 %  live_zoom.py:192 eventFilter
        5    7.9 %  dialogs.py:800 eventFilter
        4    6.3 %  field_fade.py:231 eventFilter
        3    4.8 %  feature_dictionary.py:858 eventFilter
        2    3.2 %  i18n.py:4491 eventFilter
        2    3.2 %  module_hints.py:142 eventFilter
        1    1.6 %  button_roles.py:247 eventFilter
        1    1.6 %  theme.py:3540 eventFilter

EIGHT DISTINCT FILTERS, and an application-level filter sees EVERY event
for EVERY object in the thread. That also explains the 8x: an in-process
`MainWindow` installs TWO of them, because the other six are installed by
`launch`.

THEN THE CONTROLLED VERSION, because the above is a correlation. One
process, one screen, the NUMBER of filters as the only variable, each
extra filter doing nothing but counting and returning False:

    + 0 filters   measure opens in   2.53 s
    + 6 filters                      2.60 s
    +24 filters                      4.86 s
    +96 filters                     14.65 s

    135,800 EVENTS ARE DELIVERED DURING ONE MODULE OPEN -- counted, not
    estimated: each passenger filter saw 135,776-135,867 of them.

That is ~126 ms per application-wide filter per module open, or 0.93 us
per event per filter, for a filter that does NOTHING. The real eight do
work.

  THE DEFECT IS NOT ANY ONE FILTER. It is that opening a screen delivers a
  hundred and thirty-five thousand events, and every one of them is
  multiplied by the length of a chain that only ever grows. Eight filters
  is ~1 s of pure dispatch before any of them does anything useful, and
  nothing in the code says the chain has a cost, so the ninth will be added
  the same way the eighth was.

AND IT IS AREA 4 AS WELL AS AREA 3. The request judges "smoothness" by
typing, scrolling and hovering, and every one of those events pays the same
multiplier. A filter chain is a tax on input latency that does not show up
in any per-screen measurement.

WHAT TO DO ABOUT IT, in the order this file's method requires -- measure,
then fix by cause:

  1. THE EVENT COUNT IS THE BIGGER HALF. 135,800 events to build one
     screen is the number to attack first; halving it halves every
     filter's bill at once. Nobody has yet asked WHICH events those are,
     and the same sampling hook can answer it.
  2. THEN THE CHAIN. Several of the eight are narrow enough to be
     installed on a widget rather than on the application -- `field_fade`
     and `feature_dictionary` both filter for one widget's benefit --
     and a filter installed on the widget it serves costs the application
     nothing.
  3. `live_zoom` IS 22.2 % ON ITS OWN and is the one to read first.



--------------------------------------------------------------------------------
2026-09-19 -- AREA 3, THE EVENT COUNT ATTACKED AND THE CHAIN MADE CHEAPER:
135,800 EVENTS PER OPEN BECOMES 93,000, AND THE STALL TOTAL HALVES
--------------------------------------------------------------------------------

Decided: NO DECISION IS ON RECORD FOR THIS PASS, and no maintainer answer
specific to 380 reached it. It works to this item's own request of
2026-09-07, quoted verbatim in the header above, and to its last clause in
particular -- nothing here makes spaCR faster by making it do less. The two
levers that would need an answer (sheeting popups later, and a smaller or
already-parented per-screen sheet) are described at the end of this note
and left untaken.

The 2026-09-11 note ends with three things to do in order. This is the
first two of them, and the first one turned out to be about STYLE rather
than about events: most of the 135,800 events a module open delivers are
the application asking Qt to repolish the same widgets again.

1. WHICH EVENTS THEY ARE, WHICH NOBODY HAD ASKED. Counted with a filter
installed for the length of one open, keyed by type and by receiver class:

    Regression, origin/nightly at 3f925f684, 139,963 events
        PaletteChange           34,374
        FontChange              27,340
        StyleChange             17,771
        DynamicPropertyChange   17,168     = 97,000 of the 140,000
        everything else         43,310

    Mask, 72,996 events, the same four making 42,000 of them.

A widget that is repolished sends PaletteChange, FontChange and
StyleChange; the sheet's own bookkeeping sets four dynamic properties per
sheeted root. So the event count IS the repolish count, and the way to
halve one is to halve the other.

2. WHERE THE REPOLISHES CAME FROM. The same counting filter, recording the
spaCR call site of each event, then `QWidget.setStyleSheet` wrapped to time
each assignment and count the subtree under it. One Regression open styled
its 1,385-widget screen FOUR times before its first paint -- inside
`AppScreen.__init__`, again for the late widget blocks, again because Qt
repolishes a sheeted subtree when it is reparented into the stack, and a
fourth time with byte-identical text at the page's Show. Item 284 carries
the table and the fix; both are on branch `home/284-380-perf`.

3. THEN THE CHAIN, which is this file's own step 2. Five of the ten
application-wide filters were doing work before they knew the event was
theirs -- the dialog detacher imported QEvent and QDialog from PySide6 on
every event in the process, the feature-help filter ran two liveness checks
before reading the type, glass and button_roles rebuilt a tuple of enum
members per event, and the i18n dialog filter looked its constant up
through two attributes. All five now test the type first, from a set built
once. cProfile of one Regression open before that: 956,294 `QEvent.type()`
calls and 958,602 enum constructions under them, the detacher alone 1.148 s
cumulative.

    `live_zoom`, which this file names as 22.2% on its own, IS NOT TOUCHED
    HERE: its filter is already four comparisons and an early return, and
    its icon handling belongs to another unit.

MEASURED, paired `tools/spacr_startup_benchmark.py --offscreen --runs 1
--record-only` runs alternating between trees, two per arm, load ~3:

    worst event-loop stall (ms)  3f925f684  + sheeting   + filters
    regression                    6632       4517         3680
    mask                          3664       2311         1920
    classify_merged               3463       2112         1693
    umap                          2528       1537         1208
    measure                       2086       1147          961
    __home__                       229        230          231

    modules over the 500 ms ceiling    16      17           13
    total of those stalls (ms)     30,089  20,880       15,182

    events per open: Regression 139,963 -> 92,829, Mask 72,996 -> 43,344

AREA 4 GETS THE SAME MONEY WITHOUT BEING MEASURED AGAIN. The filter chain
is a tax on every keystroke, scroll and hover, and the five fast paths cut
what each of those pays; the 2026-09-09 baseline measures typing at 0.06 ms
and scrolling at 0.01 ms, so there was nothing there to see and this does
not claim any.

WHAT IS LEFT IN AREA 3, in size order, with the decision each one needs:

  1. ~825 ms of a Regression open still sheets ~500 POPUPS at their
     `Polish` -- 351 parentless QMenus, pyqtgraph's ViewBoxMenu and its
     submenus, and about 50 QComboBox popup containers -- each parsing the
     whole ~73 KB sheet for a menu that may never be opened. NEEDS A
     DESIGN DECISION: sheeting a QMenu from `aboutToShow` (which Qt emits
     before it measures the menu, so nothing moves) leaves the QFrame
     popup containers with no equivalent signal, and letting a parented
     popup inherit its page's sheet instead changes QSS precedence inside
     menus. It also contradicts a contract 2026-09-14 wrote down on
     purpose and pinned in
     `test_a_menu_is_still_sheeted_before_it_can_be_shown`.
  2. ~640 ms is the one legitimate sheeting of the page. Cheaper only by
     making the per-screen sheet smaller or by building screens already
     parented -- both design decisions.
  3. ~250 ms is `settings_search.install` reparenting the whole settings
     form into its new container at first show, after the page has been
     sheeted. Installing the strip BEFORE the page is shown would make the
     move free, but the splitter sizes it restores are read pre-layout
     then, which this file's own WATCH list warns about.

AND THE INSTRUMENTS, so the next person does not rebuild them. Everything
above was taken by driving `spacr.qt.run(["--no-setup"])` in a child
process with `SPACR_BENCHMARK_JSON` set -- the benchmark's own controller,
so the readiness and stall numbers are the ones this item closes against --
with three probes wrapped around it: a counting event filter (types,
receiver classes, and the spaCR frame that caused each event), a wrapper on
`QWidget.setStyleSheet` that records caller, subtree size and duration, and
a per-widget fingerprint of all 45 screens for the equivalence check in
284. `SPACR_WATCH_GUI_STALLS=1` composes with all of it and names what the
thread was doing.

--------------------------------------------------------------------------------
2026-09-20 -- TWO OF THE THREE COSTS ABOVE ARE GONE
--------------------------------------------------------------------------------

(1) THE ~500 SHEETED POPUPS. Closed by item 284 earlier today, on the
maintainer's decision: a menu is sheeted at its `aboutToShow` rather than at
every `Polish`. The contract this list warned it would contradict moved with
it, on purpose, and its test now drives the property the test's name
promises. 284's note of this date has the arms and the numbers.

(2) THE ~250 ms SETTINGS-FORM REPARENT. Closed here. `settings_search.
install` was called when a screen became current -- at first show, after the
page had been sheeted -- so reparenting the whole settings form into its new
container repolished a sheeted subtree. `spacr/qt/app.py` now installs from
`_a_page_joined_the_stack`, where the page is marked for the sheet and has
NOT yet received it, so the same move is free. The stack watcher still runs
and `install` returns the strip it already made, so the two cannot fight.

THE TRAP THIS LIST NAMED WAS REAL AND IS HANDLED. A splitter that has never
been laid out answers `sizes()` with pre-layout defaults, and the restore
after the insert would have written those defaults over the layout the first
show was about to compute -- the WATCH entry about reading geometry before
the layout settles. `install` now captures and restores the sizes only when
the form is visible, and two tests hold both halves: an unshown screen has
`setSizes` called zero times, and a shown one keeps the sizes it had.

MEASURED, `tools/spacr_startup_benchmark.py --offscreen --runs 1
--record-only`, arms alternated, baseline origin/nightly c9023024c (which
already carries 284's change). Worst event-loop stall per module, in ms:

    module            base a  base b   mine a  mine b   conservative
    regression          2684    2795     2358    2560          -124
    mask                1721    1682     1443    1554          -129
    classify_merged     1572    1678     1319    1414          -158
    umap                1141    1251      905     978          -162
    measure              894     938      729     773          -120
    external_masks       716     821      496     549          -167
    __preferences__      930     939      901    1018           +89
    __home__             232     233      224     245           +13

The "conservative" column is the changed arm's WORST against the baseline's
BEST; mean against mean is 200-270 ms on every module with a settings form,
which is the figure this list predicted and it applies to all of them rather
than to Regression alone.

PREFERENCES AND HOME DO NOT IMPROVE, and the +89 ms on Preferences is said
rather than buried: it is inside that arm's own spread (901 and 1018 on the
two runs) and Preferences is a dialog, not a stack page with a settings
form, so there is nothing here for it to gain. Home has no settings form
either.

WHAT IS LEFT OF THE THREE: (3) the ~640 ms legitimate sheeting of the page
itself, which needs a design decision -- a smaller per-screen sheet, or
building screens already parented. Plus the macOS and Windows numbers,
which need those machines.

--------------------------------------------------------------------------------
2026-09-20 -- THE THIRD COST: THE CHEAP VERSION WAS TRIED AND DOES NOT PAY
--------------------------------------------------------------------------------

The maintainer chose "build screens already parented" over making the
per-screen sheet smaller. Written down before anyone spends a day on it:
THE CHEAP VERSION OF THAT IDEA WAS BUILT AND MEASURED TODAY, AND IT DOES
NOT WORK.

WHAT WAS TRIED. The ~640 ms is the page's own stylesheet being applied over
its ~1,500 widgets, and the event filter does it at the page's `Show` --
inside the module open the user is waiting through. The cheap variant
applies it right after the page joins the stack, while it is still hidden,
so the repolish happens before the open rather than during it.

MEASURED, paired arms on one machine against nightly 652417220, worst
event-loop stall per module in ms:

    module            base   pre-sheet   delta
    regression        2398        2352     -46
    mask              1464        1376     -88
    classify_merged   1344        1253     -91
    umap               914        1005     +91
    measure            747         705     -42
    external_masks     497         465     -32
    __preferences__    927         913     -14
    __home__           227         204     -23

MIXED SIGNS AND ALL OF IT INSIDE THE NOISE. Earlier today the same harness
gave a run-to-run spread of 50 to 120 ms on these modules with no change at
all, and umap moved the wrong way by 91. Nothing here is a result.

WHY IT CANNOT WORK, which the numbers only confirm: the benchmark measures
the whole module open, and the build is inside it. Moving the sheet from
the page's Show to a moment two lines earlier moves the work within the
window being measured. It was worth one experiment because a stall the user
watches during an explicit "Preparing" card is not the same as one they
watch while a module appears -- but it is not a saving, and shipping a
change that adds a call and a method for no measured gain would be worse
than not having tried.

THE CHANGE WAS REVERTED. What the maintainer chose still stands and still
needs the real version: the screens have to be built with their sheet
already on an ancestor, so each widget is polished once as it is created
instead of all 1,500 being repolished afterwards. That is a change to all
forty-five screen constructors -- none of them takes a parent today -- and
it is a day's careful work, not an afternoon's.

--------------------------------------------------------------------------------
2026-09-20 -- THE THIRD COST: THE SECOND MECHANISM ALSO FAILS, AND WHY BOTH DO
--------------------------------------------------------------------------------

The note above recorded the CHEAP version of "build screens already
parented" as measured and worthless. This is the REAL version, and it is
worse than worthless -- it is a large regression. Both results are here so
nobody spends a day rediscovering either.

WHAT WAS TRIED, and it is a better idea than the first one. `AppScreen.
__init__` calls `super().__init__(parent)` before it builds anything, so
the sheet can go on the page while the page is still an empty shell. There
is then nothing under it to repolish, and the settings column, the console
and the figures card inherit it as Qt creates them. One place, not the
forty-five constructors the earlier note feared.

MEASURED, paired arms on one machine:

    module            base   shell-sheeted
    regression        3526            7756
    mask              2037            3272
    classify_merged   2914            2630
    umap              1974            2854
    measure           1274            2713
    external_masks     780            3292
    __preferences__   1647            2013
    __home__          1088             748

REGRESSION IS 2.2x WORSE AND MASK 1.6x. Read as a rejection and not as a
measurement: these runs were taken while a 485-field crop job held eight
cores, which is why the BASELINE is also higher than the 2,398 the same
harness gave for Regression earlier in the day. An effect that size is far
outside the 50-120 ms spread that load produces, so it decides the
question; the individual numbers do not mean much.

WHY BOTH MECHANISMS FAIL, WHICH IS THE PART WORTH KEEPING. The assumption
under both is that the ~640 ms is one big repolish that could be avoided by
arranging for it to happen at a better moment. IT IS NOT ONE BIG REPOLISH.
Qt polishes PER WIDGET, so a stylesheet present on an ancestor during
construction is paid on every child as it is created -- about 1,500 times,
each one resolving against the sheet. Applying it once at the end is the
CHEAP arrangement, not the expensive one. Moving it earlier (the first
attempt) buys nothing because the work is inside the same window; putting
it there before the children exist (this one) multiplies the work by the
number of children.

SO THE THIRD COST IS NOT REMOVABLE BY EITHER ROUTE THE ITEM PROPOSED, and
the remaining honest options are smaller ones: cut what the per-screen
sheet covers so each application is cheaper, or build fewer widgets per
page. Both are real work on the pages themselves rather than on when the
sheet lands.

WHAT 380 HAS ACTUALLY GAINED TODAY, unchanged by this: the ~500 sheeted
popups (284) and the ~250 ms settings-form reparent are both gone, and
every module with a settings form opens 120-167 ms faster read at its most
conservative.

--------------------------------------------------------------------------------
2026-09-21 -- A SMALLER SHEET PER SCREEN WAS MEASURED: NO SAFE VERSION PAYS
--------------------------------------------------------------------------------

The last design option the 2026-09-20 note left -- "cut what the per-screen
sheet covers so each application is cheaper" -- measured directly, offscreen,
median of three per arm, the dark sheet (402 rules, 74,815 characters):

    screen            widgets   full sheet   window rules dropped
    regression          1,376       351 ms          322 ms
    mask                  887       177 ms          178 ms
    classify_merged       830       190 ms          194 ms

Only 7 of the 402 rules name nothing but top-level window types (menus,
tooltips, dialogs, which carry their own sheet), so dropping them from a
screen's sheet saves nothing measurable. Dropping every rule that names no
widget type or object name present on the screen does pay (Regression 403 ->
288 ms, Mask 183 -> 139 ms) but is NOT SAFE: settings rows are built when a
category is first opened, so a type absent at the first sheet can appear
later and would be drawn unstyled. That variant is rejected, not shipped.

So the ~640 ms (measured under a different harness) is the per-widget polish
of ~900-1,400 widgets against the full sheet, and the lever that remains is
the one the previous note named second: build fewer widgets when a module
opens. Recorded here as the next piece of work on 284/380.

--------------------------------------------------------------------------------
2026-09-21 -- FEWER WIDGETS AT OPEN: HIDDEN PANELS ARE BUILT WHEN FIRST SHOWN
--------------------------------------------------------------------------------

The lever the previous note left: build fewer widgets when a module opens.
Taken, on the five heaviest screens, by deferring the panels that sit in a
card that starts hidden. Nothing is removed; each panel is built, whole, the
first time its card is shown or anything asks for it.

WHAT WAS HIDDEN AT OPEN, counted with the page on screen in a real
MainWindow (every widget under the page, and which subtrees were not
visible):

    screen           widgets  hidden   the big hidden subtrees
    regression         1,455   1,192   Figures card 747 (results tabs 661:
                                       results panel 332, Measurements 214,
                                       Cells 79, Runs 29; figure pages 79)
    mask               1,124     956   Live preview 281, Timelapse fold's
                                       Track preview 130, settings sections
    classify_merged      857     769   settings sections 572, Hyperparameter
                                       search 133
    umap                 669     513   settings sections 210, Hyperparameter
                                       search 136, Figures 65
    measure              591     446   settings sections 246, Crop preview 120

WHAT IS DEFERRED NOW:
  * Regression: the results panel and everything built with it -- the Runs,
    Results, Measurements and Cells tabs, the figure grid, detail and
    volcano pages, the scan and sweep panels -- built when the Figures card
    is first shown or any of those attributes is used. The fold strip's
    extras (Hits tab, publication figure, one-family cut) wait for it
    instead of building it.
  * UMAP and Classify: the hyperparameter search panel, built when its
    switch first shows the card.
  * Mask: the Cellpose live preview, built when Live is first switched on;
    a src typed before then is loaded when it is built. The folded
    Timelapse Track preview (attached through preview_registry), built when
    its toggle first opens it.
  * Measure: the crop preview, built when Live is first switched on.
  Settings sections are NOT touched: rows are already built per category,
  and the hidden sections that remain are the next lever (below).

HOW, in one mechanism with one helper:
  * `widgets/card.py:_CardBuiltWhenShown` -- a Card that runs a builder
    once, from setVisible BEFORE the card becomes visible (no empty frame),
    with showEvent as the belt for a card revealed by its parent.
  * `AppScreen` owes a "part"; `_BuiltOnFirstUse` descriptors on the
    part's attributes (_results_panel, _results_tabs, _figure_grid, ...,
    _hyperparam, _live_preview, _measure_preview) build it on first read or
    assignment, so every one of the ~40 call sites and ~100 tests that read
    those names works unchanged. SHIBOKEN TRAP, recorded in the class
    docstring: a PySide wrapper's getattr reads the instance dict BEFORE a
    class data descriptor, so the values are stored under another key or
    the descriptor is never consulted again.
  * A late part gets what opening gave everything else, in the same order:
    the transparent-container sweep, then queued decorations (Hits tab),
    then ensurePolished, then the language pass and help-onto-caption move.
    Polish BEFORE translate matters: button_roles classifies by visible
    text at polish, and a Swedish "Kör förhandsgranskning" is not "Run".
  * Imports stay at the open (the panel modules, hit_list), so the widget
    QSS blocks they register reach every page's sheet exactly as before;
    only construction waits.
  * Teardown and pure questions never build: closeEvent, the rebuild-for-
    scale carry-over, and regression.project_path peek.

WIDGETS AT OPEN, same probe, same tree, before -> after:

    regression   1,455 ->   730
    mask         1,124 ->   723
    classify       857 ->   728
    umap           669 ->   537
    measure        591 ->   476

MEASURED, `tools/spacr_startup_benchmark.py --offscreen --runs 1
--record-only`, six runs per arm alternating base/changed on one machine,
base origin/nightly 92b0dcfc4. THE MACHINE WAS AT LOAD 23-36 ON 32 CORES
(other sessions' test batches), against ~3 for the earlier notes in this
file, so every absolute below is inflated and noisy; read the medians and
the conservative column only. "cons" is the changed arm's 75th percentile
minus the baseline's 25th: negative means better even read pessimistically.
Worst event-loop stall per module, ms:

    module            base med  lazy med   delta    cons   open s base->lazy
    regression            4770      2290   -2480   -1312    6.12 -> 4.29
    mask                  3577      2904    -673    -359    5.75 -> 4.69
    umap                  2621      1680    -942    -170    4.28 -> 2.54
    measure               1766      1431    -335     +20    2.40 -> 2.12
    classify_merged       2938      2802    -136   +1587    3.90 -> 4.03
    external_masks  (ctl) 1016      1138    +122    +470
    __preferences__ (ctl) 2076      2384    +308   +1209
    __home__        (ctl)  632       772    +140    +330

    intervals over 500 ms (median of six)   29.5 -> 29.0
    total of those stalls, ms (median)    42,357 -> 34,388

READ CONSERVATIVELY: the three controls, which this change cannot reach,
moved by +120 to +310 ms on the medians, so that is the noise floor here.
Regression's halving is far outside it and holds at the pessimistic
quantiles; Mask and UMAP hold too. Measure's -335 is inside the noise read
pessimistically, and Classify shows nothing -- expected, since only 130 of
its 857 widgets were deferred and its hidden mass is settings sections. The
over-ceiling COUNT does not move at this load, because at load ~30 almost
every module breaches 500 ms; the count needs a quiet machine to mean
anything.

BEHAVIOUR IS THE SAME, CHECKED RATHER THAN ARGUED. Fingerprint of every
widget (class, name, geometry, visibility, eight palette roles, font, size
hints, autoFill, WA_StyledBackground, text, tooltip, placeholder, tab texts,
dynamic properties, own stylesheet) on both trees, English and Swedish:
  * at open, every VISIBLE widget on all five screens is identical except
    the live RAM/CPU/GPU usage bars, and the page stylesheets are
    byte-identical;
  * after revealing the deferred parts, UMAP's search panel is identical;
    Classify differs only in three never-laid-out hidden widgets; Regression
    differs only in hidden widgets' geometry/size hints (unshown scroll
    bars, tab scroll arrows, a hidden table header) and an i18n bookkeeping
    property on one hidden combo whose displayed tooltip is identical;
  * Mask's live preview and Measure's crop preview match in visible layout
    and text. Measure's status line after priming with the placeholder src
    races a worker against a 250 ms timer ("Not a file or folder: path" vs
    "Load a merged array first."); the base shows the same race.
TWO DIFFERENCES ARE FIXES, and are said rather than hidden: Mask's folded
Track preview is now TRANSLATED -- it was inserted into the runtime splitter
after the language pass and no watcher reached it, so on a Swedish UI it
opened in English -- which also widens the runtime column by ~18 px in
Swedish; and Regression's Hits tab and results containers are swept like
the rest of the page.

WHAT BUILDS A DEFERRED PART, so nobody is surprised by a stall later: the
card being shown (results arriving, the Live / search / track switches),
any read of its attributes (a dropped results folder, a refit, the UMAP GPU
switch), and the run journal's workspace collection at the end of ANY run
while a Regression screen that never showed results is open -- kept on
purpose, because the journal records the panel's (default) state and
skipping it would change what the workspace file says.

TESTS: `tests/qt/test_a_hidden_panel_is_built_when_it_is_first_shown.py`
(19): not built at open, built once and whole on first show or first use,
fold extras wait, close does not build, translated in Swedish, Run keeps
its role under translation, attached previews build on first toggle. Eight
existing tests that counted the regression plots, tab bars, "View:" label
or UMAP search controls at construction now ask for the part first; the
public-callable parameter ratchet moves 18,311 -> 18,315 (four optional
keywords), and the signature digest move is proved by subtraction.

WHERE AREA 3 STANDS: the three costs the 2026-09-19 note priced are
closed (popups at aboutToShow, the settings-form reparent) or proven
immovable (the page sheet); this note takes the lever that was left, and
Regression -- the worst stall since 380 began -- is halved again. 380 is
NOT done: area 3's stalls are still over 500 ms on the heavy screens, and
"on all platforms" still needs the macOS and Windows machines. The next
lever is the hidden settings sections named in 284's note of today.

--------------------------------------------------------------------------------
2026-09-22 -- HIDDEN SETTINGS CATEGORIES WAIT OFF THE PAGE UNTIL OPENED
--------------------------------------------------------------------------------

The lever the 2026-09-21 note named next, asked for by the coordinator:
the settings sections a screen hides at open.

WHAT IS HIDDEN AT OPEN, and why it cannot simply be "not built". Counted
on a real MainWindow: Classify had 584 of its 596 settings widgets hidden
(every category but one, under the Essentials view), Mask 490 of 576 and
Measure 305 of 334 (collapsed bodies, plus beta categories hidden by the
maturity preference). About 60% of those widgets are the controls
themselves -- 352 of Classify's 584 -- and THE CONTROLS ARE THE SETTINGS
MODEL: `SettingsWidgets.collect()` reads them, `set_value_for_key` writes
them, the dependency and object-visibility rules are wired to their
signals, and recipes, CSV import and the drop handlers all go through
`_widgets`. Not building them means a second value store and every one of
those paths learning it. Measured before choosing, offscreen, one Classify
screen: the constructor is 379 ms, of which the model's control build is
209 ms and the category captions 51 ms; the page's stylesheet costs 178 ms
with everything on the page and 38 ms with the hidden bodies off it, and
removing only the captions moved it to 174 ms. So captions alone buy
nothing, controls cannot go, and the cost worth taking is the styling and
the per-widget passes over widgets nobody can see.

TRIED FIRST AND REJECTED, WITH NUMBERS: leave hidden widgets in place but
mark them unpolished (clear WA_WState_Polished) so the sheet skips them,
and polish them when their category opens. Fingerprints were identical,
but six alternating benchmark runs per arm made it WORSE -- Classify's
worst stall 1,678 -> 1,885 ms median, the over-ceiling total 17.6 s ->
21.3 s -- and an event count showed why: Qt still delivers Style, Palette
and Font changes down the whole subtree whether it polishes or not
(30,392 vs 29,662 events on a Classify open). Reverted.

WHAT SHIPPED: the body of every top-level category that is collapsed or
hidden is taken OUT of the page -- made parentless, not rebuilt -- by
`AppScreen._detach_what_the_form_hides`, which the window calls just
before the page is first shown (after maturity, the dimension switches
and the search strip's Essentials view have decided what is on the form).
`Section._attach_body` puts it back, at its old layout index, the moment
the category is expanded or an open category is shown (a per-section
Show filter, installed only while the body is away), and the screen then
runs the language pass and the help-onto-caption move over it, so a
category that was away while the language changed opens in the new one.
The same trial with bodies detached cut Classify's events 29,662 ->
18,869.

WHAT STILL REACHES A CATEGORY THAT IS AWAY, and how:
  * the run: the controls are untouched, so collect() returns every key,
    including values set while the body was away (tested);
  * recipes, CSV import, drops: all go through the model's `_widgets`;
  * the search strip: indexes rows from each section's own form, finds
    matches by key, and opening a match's section brings its body back
    (tested); its sub-heading roll-up walks `_logical_parent`, which
    answers a detached body's category;
  * the palette's reveal: `Section._holds` instead of isAncestorOf;
  * the empty-heading checks and the object rule's slot-heading scan:
    `_sections_below` sees through detached bodies;
  * the theme: a detached body is not a window, so per-window sheeting
    skips it and it wears the page's current sheet when it comes back;
  * teardown: a body away when its category dies goes with it.
Everything new is private, so the public API ratchets do not move (the
same seven fail on nightly c4d57c90a and here, with identical numbers).

WIDGETS UNDER THE PAGE AT OPEN, same probe, nightly -> this change:

    classify_merged    728 ->  172
    mask               723 ->  413
    measure            476 ->  243
    umap               537 ->  345
    regression         730 ->  550
    external_masks     498 ->  233
    analyze_plaques    519 ->  329

MEASURED, `tools/spacr_startup_benchmark.py --offscreen --runs 1
--record-only`, six runs per arm alternating nightly 64addaf2d and this
change, AT LOAD 34-36 ON 32 CORES (other sessions), so read pessimistically:
"cons" is the changed arm's 75th percentile minus nightly's 25th.

    module            nightly med  this med   delta    cons
    classify_merged          3585      1924   -1660    -224
    umap                     2512      2059    -453    +267
    mask                     3405      3062    -343    +422
    analyze_plaques          1981      1629    -352    +181
    external_masks           1473      1186    -287    +266
    measure                  1763      1487    -275    +171
    regression               2633      2764    +131    +596
    __preferences__ (ctl)    2564      2858    +294   +1208
    __home__        (ctl)     691       858    +167    +202

CLASSIFY IS THE ONE THAT CLEARS THE NOISE: its stall nearly halves and
stays better at the pessimistic quantiles (six of six paired runs better).
The others move the right way on the medians by about as much as the
controls moved the wrong way (+170 to +290), so they are reported, not
claimed. Regression is flat, as expected: its settings mostly stay on the
page. Over-ceiling count 35 -> 35.5 and total 50.0 s -> 48.1 s, both inside
the noise at this load.

BEHAVIOUR IS THE SAME, CHECKED: every visible settings widget
fingerprinted (class, name, geometry, visibility, eight palette roles,
font, size hints, text, tooltip, dynamic properties, own stylesheet) on
seven screens, both trees, English and Swedish, at open and again with
"All settings" and every category expanded -- identical, 0 differing rows
of 2,144. One Swedish run once showed a 21 px larger size hint on one
Classify chip strip (never its geometry); four repeats did not reproduce it.

TESTS: `tests/qt/test_a_hidden_category_stays_off_the_page_until_opened.py`
(13): a collapsed body leaves and an open one stays, expanding brings it
back styled, showing an open hidden category brings it back, a collapsed
one shown keeps its body away, it returns to its index, nested headings
and `_holds` see through it, the came-back hook fires, and on a real
Classify window: >200 widgets away, collect() has every key, a value set
while away is collected, search reveals an away setting, and a category
that comes back after a switch to Swedish is in Swedish. Two survey tests
that walk the page with findChildren now open each category first, or use
`_sections_below` -- the path a user takes, since a closed category's rows
are not reachable any other way.

380 STILL NOT DONE, for the same reasons 284 gives in its note of today;
this closes the last lever the earlier notes named on the settings side.

--------------------------------------------------------------------------------
2026-09-22 -- A CLOSED CATEGORY IS BUILT WHEN IT IS OPENED; ITS VALUES ARE READ UNBUILT
--------------------------------------------------------------------------------

The last settings-side lever the notes named -- the model's control build --
taken; the full account (design, call sites, equivalence checks, tests) is
in 284's note of the same title. In one paragraph: `SettingsWidgets._widgets`
is now a mapping whose plain controls may wait (membership and keys answer
without building, a read builds one control, collect() and the rules read a
waiting control from its plan, held equal to the built control for all
1,135 plain settings of 59 modules), and a category closed at open is built
as a heading only, its rows laid out the first time it is opened, revealed
or asked for. No second value store: 48 of 886 settings read back
differently from their declared default because the control normalises
them, so a store would either change what the run gets or duplicate each
control's normalisation.

WIDGETS CREATED BY OPENING THE MODULE, nightly ad01caeca -> 48e431db4:
Classify 738 -> 335, Mask 742 -> 459, Measure 486 -> 302, UMAP 547 -> 384,
Regression 740 -> 601, External masks 508 -> 319, Plaques 529 -> 370.

WORST STALL, twelve alternating benchmark runs per arm, paired median of the
per-run difference (controls Home / Preferences / Annotate / Map barcodes
+1..+21 ms, which is the noise floor): Classify -159 ms (better in 12 of 12
pairs), Mask -134 (10/12), UMAP -130 (11/12), Regression -109 (11/12),
Measure -75 (11/12), External masks -55 (10/12), Plaques -7 (7/12). Over-500
total 13.7 s -> 12.4 s, count 15.5 -> 14.5. Module open 10-30% shorter.

A MEASUREMENT TRAP, FOR WHOEVER RUNS THE NEXT A/B: the benchmark sets
PYTHONDONTWRITEBYTECODE=1, so an arm whose edited modules have no .pyc
recompiles them in every run. It cost the changed arm ~0.8 s of startup and
~200 ms on Home until found; `python -m compileall -q spacr` in both trees
first. The earlier A/Bs in this file compared a freshly edited tree with a
cached one and carry that bias against the change.

380 NOT DONE: every heavy module still breaches 500 ms at load ~10 (Mask
1.3 s, Regression 1.1 s, UMAP 0.8 s, Classify 0.7 s). What remains is the
visible page's construction and polish, and the quiet-machine, real-display
and macOS/Windows close-out.

--------------------------------------------------------------------------------
2026-09-22 -- THE CLOSED CATEGORIES ARE BUILT IN IDLE TIME, A FEW MS AT A TIME
--------------------------------------------------------------------------------

The cost the previous note moved, not removed: a category closed at open is
built on its first open, 36-200 ms on that click. Asked for by the
coordinator: build them beforehand while nobody is using the screen, in
slices well under a frame, stopping for input, through exactly the passes a
click-open runs.

WHAT SHIPPED, in one paragraph; the full account -- the steps, the two
whole-panel costs the slices exposed, the greyed-name fix, the equivalence
check and the tests -- is in 284's note of the same title. The settings
form's builders are generators now, run to the end by a click and a step at
a time by ``_IdlePrebuild``, which takes steps for about 4 ms at a time once
the screen has been shown for 400 ms with no input and stops on any pointer,
wheel, touch or key event; a click in the middle finishes the build in
progress. The object rule now sets each row once instead of showing every
row for the search strip to hide again -- 68 ms of a 90 ms pass on
Regression under Essentials, and the pass ran twice per category built --
and ``essential_keys`` (16 ms) and ``explainer_width`` (20 ms) are cached
rather than recomputed per pass. All three are paid by clicks and channel
commits too, not only by the prebuild.

FIRST OPEN OF A CLOSED CATEGORY, ms, All settings (under Essentials the
closed categories are hidden until All is chosen), median / max over every
closed category of the module, offscreen, load ~3:

    module            nightly ad01caeca    lazy e2f082bf8    idle-prebuilt
                      (built at open)      (built on click)
    mask                 33 /  47             55 /  83           24 / 26
    measure              36 /  58             64 / 106           25 / 28
    classify_merged      45 /  74             71 / 139           28 / 31
    regression           47 /  87             92 / 175           27 / 37
    umap                 32 /  40             49 /  88           23 / 28
    external_masks       34 /  65             49 /  94           26 / 32
    analyze_plaques      28 /  63             35 / 119           27 / 28

A prebuilt category opens faster than one built at open used to: its body
is on the page (hidden), where nightly's detached bodies paid a reparent and
restyle on the first open.

THE IDLE BUILD ITSELF, per module, offscreen, a 16 ms timer measuring how
late it fires; "moving" = pointer moved every 20 ms from 0.6 s after the
open (the build had started at 0.4 s), then 6 s idle:

                    slices   worst slice  idle: worst lateness     slices while   input delay
                             ms           (nightly | prebuild) ms  moving         max ms
    Essentials view
    mask               64      16.5          28 | 27                  0              0.5
    measure            59      25.2           9 | 55*                 0              1.9
    classify_merged   107      13.1           1 | 12                  0              1.4
    regression         45      11.3           3 | 12                  0              0.5
    umap               61      12.3          32 | 19                  0              2.3
    external_masks     57      23.8          27 | 11                  0              1.7
    analyze_plaques    37       9.1          51 | 37                  0              0.2
    All settings
    mask               89      10.6          44 | 18                  0              1.4
    measure            59      24.5          42 | 39                  0              1.9
    classify_merged    99      14.1          48 | 16                  0              0.3
    regression         97      20.3          28 | 23                  0              0.8
    umap               68       9.2          15 | 19                  0              1.5
    external_masks     70      23.5          30 | 21                  0              0.7
    analyze_plaques    38       9.4           3 | 26                  0              0.2

  (* the machine's load rose from ~3 to ~8 during that row; nightly, which
  runs no prebuild at all, shows 28-51 ms idle lateness on its own -- the
  gc tick, usage timers and the ambient backdrop -- so the idle column's
  worst values are that noise floor, not the build.)

READ PLAINLY: p95 slices are 7-9 ms. The worst slice is 20-25 ms on Measure,
External masks and Regression -- over the 16 ms target by up to 9 ms -- and
it is ONE indivisible Qt call: laying out one combo row (`addRow` plus the
maturity repolish, 12-20 ms for "Crop mode" / "Number of organelles") or
Regression's section explainer. No slice ran while the pointer moved, in any
module, and input waited at most 2.3 ms. So the worst a user moving the
mouse can meet is the one slice already running when they started, at most
about one frame; a 100 ms freeze from the prebuild did not occur in any run.

BENCHMARK, `tools/spacr_startup_benchmark.py --offscreen --runs 1
--record-only`, six alternating runs per arm, BOTH TREES BYTE-COMPILED,
nightly 954587cac vs this change, load 3.8-4.9. The benchmark leaves each
module within ~100 ms, before the 400 ms wait ends, so this measures what
the prebuild costs a module open (nothing), not what it saves:

    module            nightly med  this med  delta  cons
    regression             795       764     -31    +12
    mask                   831       811     -20    +30
    classify_merged        515       508      -8    +17
    umap                   588       570     -18    +14
    measure                423       408     -14    +19
    external_masks         308       302      -6     +1
    analyze_plaques        466       484     +17    +36
    __preferences__ (ctl)  937       915     -22    +16
    __home__        (ctl)  229       224      -5    +22
    over-500 count 6.5 -> 7.0, total 4,973 -> 5,258 ms (inside the noise)

THE .pyc BIAS, AGAIN, BECAUSE IT DECIDES A/B RESULTS: the benchmark runs
with PYTHONDONTWRITEBYTECODE=1, so a tree whose edited modules have no .pyc
recompiles them in every run (~0.8 s startup, ~+200 ms on Home). Every A/B
here byte-compiled both trees first (`python -m compileall -q spacr`); any
A/B in 284 or 380 older than today's lazy-category note compared a freshly
edited tree against a cached one and is biased against the change.

BEHAVIOUR IS THE SAME, CHECKED; the differences from nightly (158 greyed
labels that now say why they are greyed, and the row flags of the z/t rows
inside the category the 3D switch hides) are listed in 284's note, with the
tests.

380 STILL NOT DONE: this does not touch the module open itself, and every
heavy module is still over the 500 ms ceiling on a loaded machine. What it
closes is the click that used to pay for a closed category, and two
whole-panel passes that were costing every channel commit as well.

2026-09-22 — REGRESSION DATA IMPORTS WAIT FOR RESULTS
See the dated note in 284 and features/data/284_regression_imports_2026-09-22.json.
The hidden results dependency preflight and Hit List style-import path no longer
load pandas on first Regression open. The direct full-window median changed
1630 -> 1461 ms in three fresh processes per version; this is diagnostic only.
100 distinct focused tests pass, including cold import, QSS registration,
first-use construction, missing-dependency fallback and hit-list capabilities.
The full 45-module run still has nine intervals above 500 ms. 380 REMAINS OPEN.

2026-09-22 — UMAP EXPLORER CONSTRUCTION WAITS FOR RESULTS
See284's dated note and features/data/284_umap_explorer_2026-09-22.json.
Home -> UMAP median1626.90 ->1263.79ms in six alternating fresh processes;
46 fewer widgets at open, with the explorer module still unloaded. Existing
first-use machinery preserves language, settings, static/interactive modes,
ordinary figures and teardown. This closes only hidden-explorer construction
at open; first use still pays for it. Item380 and its500ms budget remain OPEN.
Validation after integration: 115 focused tests and fatal-error Ruff pass.

2026-09-22 — EARLIER FOLD CONTROL INSTALLATION
Classify's fold strip and lazy FlowView section now exist before the first
page theme pass, avoiding late whole-screen restyling from currentChanged.
Six alternating real navigation probes: median986.48->912.30ms, root style
changes5->2, same186 widgets. Existing stack watcher remains idempotent.
Full measurement method/limitations are in item284's trailing note and
features/data/284_fold_controls_before_sheet_2026-09-22.json. This closes a
measured repeated-styling cost, not the500ms ceiling or cross-platform gate.

2026-09-23 — UNUSED ICON FONT INITIALIZATION REMOVED
See 284's dated note and data/284_requested_icon_fonts_2026-09-23.json.
Semantic glyphs initialize one QtAwesome family instead of twelve, retain
the same renderer/checksum/fallback behavior and compare pixel-identically
across themes, sizes and states. 151 focused checks pass. Isolated first-icon
creation/rendering median falls 89.36 -> 55.13 ms across eight runs per arm;
QtAwesome import is separately measured and remains. Full Home->Classify
results are noisy: final six-pair median 1606.60 -> 1578.44 ms, with only
three paired improvements. No reliable whole-navigation gain is claimed.
The 500 ms ceiling and other-platform validation remain OPEN.

2026-09-25 — MODULE OPENS BREATHE; MAKE MASKS AND PREFERENCES STYLED ONCE
See 284's dated note and features/data/284_open_breaths_2026-09-25.json for
the method (a GUI-thread stack sampler bounded by the worst heartbeat gap)
and the per-module table. Area 3 (starting the modules): the open's steps
are separated by event-loop breaths taken only after 150 ms of work; icons
are re-inked on a worker after launch; the first-visit walkthrough waits for
the switch; the language is read once per open. Area 5 (widgets/settings):
Make Masks' root sheet waits for the show (was: sheeted empty, polished per
card, repolished on reparent, sheeted again), and the glass card rule joins
a dialog's window sheet (was: a second full Preferences repolish).
Offscreen, four alternating pairs at load 37.5-52.8: intervals over 500 ms
35-42 -> 19-23 of 51; over-500 total 38.7-48.4 s -> 14.0-16.8 s; Make Masks
4,254 -> 916 ms, Regression 2,454 -> 735, Preferences 2,869 -> 1,972. Under
xvfb-run (real xcb windows) the same shape. THE TRADE, for the "speed" half
of this item: a breath is a painted frame, so several opens are 0.1-0.7 s
longer at that load; that is the frame stream 284 requires, kept small by
the 150 ms threshold. The remaining floor is each screen's first
setStyleSheet (one Qt call, 360-590 ms CPU on the heavy screens at load
70-80): a widget-count or sheet-size question for this item's theme area.
380 REMAINS OPEN; no other-platform or quiet-machine evidence was possible.

2026-09-26 — ONE EVENT HUB INSTEAD OF THIRTEEN APPLICATION FILTERS
See 284's dated note and features/data/284_event_hub_2026-09-26.json. Area 3
(starting the modules) and area 5 (handling the widgets): a screen's first
setStyleSheet delivers ~7 events per widget (6,288 on Make Masks), and each
went through every Python filter on the QApplication. Those filters now sit
behind one hub in spacr.qt.gil_priority that reads the event type once and
calls only the filters that named it; Qt's ordering, consumption and removal
rules are kept, and a test pins them. Sheet thread CPU at load 90-100: Make
Masks 648-791 -> 251-278 ms, Regression 616-813 -> 276-312, Mask 566-640 ->
215-251, Preferences 721-1140 -> 200-269. The rules themselves were the
smaller half (one trivial rule cost half of the full 59 KB sheet), so the
sheet's text was left as it is. Annotate no longer imports pandas, and pandas
is imported on a worker after the first module screen. Final sweep, nightly
-> branch, load 10.9-12.9, offscreen 4 pairs and xvfb 2 pairs: runs with any
interval over 500 ms 6 of 6 -> 0 of 6; Regression open 1.6 -> 0.7 s, Make
Masks 1.3 -> 0.6 s. 380 REMAINS OPEN: Preferences' show is over the ceiling
at load 15-21, and nothing here is macOS, Windows or a real compositor.
