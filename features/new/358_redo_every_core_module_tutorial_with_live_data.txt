================================================================================
REDO EVERY CORE-MODULE TUTORIAL WITH LIVE DATA AND THE FOLDED-IN ACTIONS
================================================================================

Status:    OPEN, filed 2026-09-02. The version-update hold it was filed behind
           was lifted on 2026-09-15 and the candidate's media is uploaded, so
           what is left is work rather than a wait. As of 2026-09-22: current
           Home/installers, Annotate and Classify CV/ML captures have private
           English review masters. Annotate and CV pass full decode and
           browser checks; all 150 narration tracks for 01/04/05 pass technical
           verification after CPU rendering. Classify overview has 11 scenes
           composed from verified current navigation and CV/ML recordings;
           its English review media pass decode/audio/player checks. Map's
           50 voices and Regression's current 4K master/player are verified.
           Make Masks has a current combined magnifier/FEATURES/editor capture;
           its updated lesson, translations and media remain private work. OPEN ON:
           Mask's GPU recapture, remaining
           captures, full language/voice/caption and audible acceptance,
           complete-set publication, and the maintainer's Pages deployment.
           Detailed dated receipts below preserve each acceptance boundary.
Requested: 2026-09-02 — "the tutorials for all of the core moduals need to be
           redone. there is now live data for all core moduals that should be
           used in the tutorials, there are also several buttons that have been
           folded into the core modules that to be mentioned in the core
           modual tutorials. but for these we can retain the old tutorials and
           just reference other tutorials in the core module tutorials."
Owner:     the Codex tutorial session. The editable tutorial source and the
           derived documentation/media tree must not be edited concurrently by
           another session; instruction 48's ownership rule still applies.

WHY THIS IS NEW WORK
--------------------

Instruction 48 verified the previous tutorial release, but the application has
since changed its module structure. Core modules now expose downloadable live
test data and absorb actions that used to appear as separate module tiles. A
tutorial can therefore be technically polished and still teach a navigation
path, input source or module boundary the current application no longer has.

The Core tutorial is the current top-level workflow. A still-accurate older
specialist tutorial remains useful evidence and should be linked at the point
the folded action is introduced; it should not be deleted or narrated again
merely to make the Core tutorial self-contained.

WHAT TO DO
----------

1. Derive the Core-module inventory from the runtime module registry and Home
   grouping used by the running application. Do not copy a historical list
   from instruction 48 or hard-code a second list that can drift.

2. For every current Core module, rebuild its top-level tutorial against the
   running UI and current module name, category, settings layout, action row,
   output surfaces and completion state.

3. Use that module's real downloadable/live test dataset through the same
   control a user sees. Show the input arriving, run a bounded representative
   operation, and inspect a real output; a mocked path, static screenshot or
   source-only validation is not a live-data tutorial.

4. Name every action/button folded into the Core module and show where it now
   lives. When an existing specialist tutorial still explains the action
   accurately, retain it and link to it from the Core tutorial at that point
   instead of duplicating its full walkthrough.

5. Update references to renamed, merged or removed modules and controls. In
   particular, explain current test-data controls, live previews, GPU/device
   acceleration where the module uses it, and other capabilities added since
   the last tutorial release; do not present NVIDIA/CUDA as the only
   acceleration route.

6. Keep useful old tutorials. Remove one only when its workflow no longer
   exists or its surviving content is fully and more accurately replaced, and
   record that decision in this file before removing any published lesson.

7. Re-run instruction 48's production checks: current screenshots/video,
   highlight geometry and timing, concise narration, intelligible technical
   pronunciation, caption/narration synchronisation, mobile playback, media
   hashes/budgets and the live deployment verifier. The new lessons inherit
   the old quality bar rather than replacing it.

8. Publish only after the version-update hold is released and the intended
   application version is the one shown. Commit and push each completed Core
   tutorial or small coherent tutorial group so a crash cannot strand the
   whole rebuild.

HOW TO KNOW IT WORKED
---------------------

* A generated inventory test maps every current runtime Core module to exactly
  one current top-level tutorial and fails when a new Core module appears
  without one.
* Every Core tutorial downloads/loads its registered live dataset through the
  UI, completes a bounded real operation, and shows an inspectable real output.
* A generated folded-action inventory proves each action is mentioned in its
  owning Core tutorial and that every referenced specialist tutorial exists.
* Links are valid and no retained lesson points to a removed module, stale
  button name, obsolete settings category or old module hierarchy.
* Tutorials that use acceleration describe the current resolver and supported
  GPU backends accurately, and remain usable on CPU-only hardware.
* The repository tutorial/media tests, external tutorial suite, multilingual
  narration/caption checks, mobile player checks, hashes and media budget all
  pass against the exact published artifacts.
* A manual catalog-order review confirms the tutorial set teaches the current
  spaCR module structure and features rather than merely satisfying the
  inventory tests.

BOUNDARY
--------

This item records and later executes the tutorial rebuild. It does not weaken
the explicit hold on 1.5.0.5, move tutorials onto the pre-update list, or
authorise edits in the tutorial media tree by a second concurrent session.

--------------------------------------------------------------------------------
2026-09-08 -- WHY test_tutorial_release_audit CANNOT BE CLOSED AS A CODE FIX
--------------------------------------------------------------------------------

`test_every_module_and_fold_has_exactly_one_current_tutorial_route` is red and
will stay red until this instruction runs. Recorded here because from the
failure alone it reads like a missing dictionary entry, and it is not.

THREE ROUTES HAVE NO LESSON: `ops`, `regression_diagnostics`, `import_images`.
The assertion is an equality -- every registered module and every folded host
has exactly one lesson -- so a subset will not do and the three cannot simply
be excused.

WHAT ONE LESSON ACTUALLY COSTS, measured from the catalog:

  * `docs/source/_extra/tutorials/catalog/` holds fourteen files:
    `lessons_en` plus lessons in es, fr, hi, it, ja, pt-BR, zh-CN, and
    captions in da, de, is, ko, nb, sv.
  * `test_every_translated_tutorial_catalog_has_the_english_structure`
    requires every one of them to carry the same lesson ids, numbers,
    routes and SCENE COUNT as English. A lesson added to English alone
    fails that test instead of the one above it -- the count of red tests
    does not fall, it moves.
  * each scene carries `narration`, a `visual` key, `hold_after`, and
    `speech_text` with IPA for the reader -- see
    `test_every_spoken_pypi_is_the_single_word_pypie`, which pins a
    pronunciation across every narration language.
  * `voice_catalog.js` sits beside them; the README describes 73 lessons
    with 50 voices across eight languages.

So three lessons is roughly forty translated scene sets and their audio, not
three JSON objects. It needs the same checkpoints the API catalogs need and it
needs them for narration rather than tooltips.

FOR THE RELEASE CONVERSATION: this is not a gate anybody can close in an
afternoon, and it should be counted with 358 rather than with 288's failure
list. Writing the three lessons in English only would make the suite LOOK one
test better while breaking a different one and shipping a tutorial library
that is complete in one language and short in thirteen.

--------------------------------------------------------------------------------
2026-09-09 -- CODEX OWNS THE TUTORIALS. NEITHER CLAUDE SESSION TOUCHES THEM.
--------------------------------------------------------------------------------

The maintainer started a Codex session on the tutorials this morning and told
this session plainly not to touch them. That settles an ownership question
that had drifted: the other Claude session had taken the three missing
lessons an hour earlier, on the strength of owning the only machine with the
translation models. It has been told to drop it.

OUT OF BOUNDS for both Claude sessions until he says otherwise:

    docs/source/_extra/tutorials/**     lesson and caption catalogs
    spacr/qt/tutorial/**                the engine
    docs/tutorial/, docs/resources/tutorial/
    this instruction, and the tutorial half of 384

WHAT THIS DOES TO 288, and it is the reason this note is here rather than
only in a message: 288's LAST failing test is
`test_every_module_and_fold_has_exactly_one_current_tutorial_route`, and it
fails because ops, regression_diagnostics and import_images have no lesson.
That test now belongs to CODEX. 288 sits at one failure that neither Claude
session may fix, so nobody should read it as work either of them is about to
pick up.

WHAT CODEX INHERITS, so it is not re-derived: a lesson is FOURTEEN catalog
files, not one -- lessons in es, fr, hi, it, ja, pt-BR, zh-CN plus English,
and captions in da, de, is, ko, nb, sv -- and
`test_every_translated_tutorial_catalog_has_the_english_structure` requires
every one to carry the same lesson ids, numbers, routes and SCENE COUNT as
English. Writing the three in English alone makes the route test pass and
breaks the structure test. Each scene also carries `narration`, a `visual`
key, `hold_after`, and `speech_text` with IPA, pinned across languages by
`test_every_spoken_pypi_is_the_single_word_pypie`.

The translation models are at
/mnt/firecuda2/Claude/toxoplasma_projects/tutorials/project/translation_models
on the home machine -- nine opus pairs, m2m100_418M, madlad400-3b and 7b.

--------------------------------------------------------------------------------
2026-09-09 -- FOR CODEX: 109 COMMITS, AND THE BLOCKING TEST HAS NOT MOVED
--------------------------------------------------------------------------------

Written in the file rather than relayed, because two sessions have now tried
to pass this on and neither can confirm it arrived.

`test_every_module_and_fold_has_exactly_one_current_tutorial_route` reports
exactly what it reported this morning:

    Extra items in the right set: 'import_images', 'regression_diagnostics',
                                  'ops'

The work being done is careful -- Gate Editor in 13 locales with all 50
voices checked, a known-geometry stitch verified to exact pixel identities,
Converter frozen across 13 translations. But it is DEEPENING lessons that
already exist, and only one thing closes this test: three new entries in
`docs/source/_extra/tutorials/lesson_catalog.js`, each with an `app_key`
naming a live Home key -- or the exact `host_app_key` when the workflow is
reached through a fold.

This is 288's LAST failing test. Everything else in that instruction is
green.

>>> DO NOT WRITE THE `ops` LESSON YET.

The maintainer moved the OPS button from Align & Stitch to Mask Generation
today -- see 372, which is mid-implementation in another session. A lesson
written now would route to a host OPS is leaving, and the `host_app_key` it
records would be wrong the moment that lands.

`import_images` and `regression_diagnostics` have no such constraint and can
be written immediately.

TWO THINGS THAT WILL COST A RE-RECORDING IF THEY ARE NOT KNOWN FIRST:

  * SETTINGS RENAMES ARE COMING. Seven are approved and queued -- see 364's
    2026-09-09 entry for the table and the modules they touch. Every one
    changes a visible label, so a frame showing that row, or narration
    reading it aloud, is wrong from the moment they land. Regression and Map
    Barcodes are the affected tutorials.
  * RETIREMENTS ARE WORSE THAN RENAMES for a recording: the row disappears
    rather than changing its name.

Neither has landed. The renames wait on the session that owns the modules
that READ those keys.

--------------------------------------------------------------------------------
2026-09-09, LATER -- CODEX IS NOT CLOSED, AND OWNERSHIP RETURNS TO IT
--------------------------------------------------------------------------------

For about half an hour this instruction was handed to the other Claude
session on the grounds that Codex had been shut. It had not: what the
maintainer closed by accident was a session on the WORK machine. Codex runs
on the home machine and never stopped -- its newest tutorial commit is
17:17, after the handover was announced.

The other session has stood down and touched nothing. 358 IS CODEX'S.

WHY IT WAS NOT CAUGHT BY EITHER SESSION, recorded because it will recur:
neither Claude session can see Codex's process. Its COMMITS are the only
signal, and they are ambiguous by construction -- the median gap across its
last nineteen is four minutes, with two eleven-minute gaps already in that
history, so "stopped" and "a normal pause" look identical. A claim about
what a session IS is not a measurement of what it is DOING, and only the
maintainer could tell them apart.

>>> THE `ops` CONSTRAINT, which is the one thing Codex still needs from us:

DO NOT WRITE THE `ops` LESSON AGAINST ALIGN & STITCH. The maintainer moved
the OPS button to Mask Generation today (see 372), and the session
implementing that move is the one that will know which host and which fold
the lesson should name. A lesson written now records a `host_app_key` that
is wrong the moment 372 lands.

`import_images` and `regression_diagnostics` carry no such constraint and
are the two that close 288's last failing test.

--------------------------------------------------------------------------------
2026-09-11 -- THREE REDS THE FULL SWEEP LEFT IN CODEX'S HALF, UNTOUCHED
--------------------------------------------------------------------------------

A full-suite run of 2,754 files at `ae8bdb417` -- 61,904 passed, 28 distinct
failures -- left exactly three in tutorial-owned files. Recorded here rather
than fixed, because this file says 358 IS CODEX'S and because two of the
three are judgements about what a lesson claims, not typos.

  1. `tests/test_the_name_is_spaCR.py::test_no_file_mis_cases_the_name`

         tools/tutorials/authoring/tools/capture_install_web.py:331  SpaCR
         tools/tutorials/authoring/tools/translate_pre_app.py:264    SPACR
         tools/tutorials/authoring/tools/translate_pre_app.py:265    SPACR

     UPDATED 2026-09-11: judgement WAS required, and one of the three
     would have been a defect to "fix". `translate_pre_app.py:265` is

         text = re.sub(r"SPACR[A-Z]*TOKEN", "spaCR", text)

     a REGEX matching an uppercased placeholder the sentence model mangles,
     with the correct spelling already in the replacement. Lowercasing the
     pattern stops it matching, the placeholder is never restored, and every
     affected translation ships with a raw token in it. The naming rule now
     exempts an all-caps placeholder ending in TOKEN, so that line is
     settled and needs nothing from anybody.

     THE OTHER TWO ARE STILL CODEX'S AND STILL NEED A DECISION:

       * `translate_pre_app.py:264` -- prose, "The stable SPACR prefix and
         TOKEN suffix still identify it uniquely". It is naming the token's
         literal text, so the capitals are right and the sentence is what is
         wrong: a code quote around `SPACR` says so and satisfies the rule.

       * `capture_install_web.py:331` -- asserts that GitHub's release page
         renders "SpaCR <version>". If the page really says that, the
         assertion is correct and the PAGE is the mis-cased thing; if it
         says spaCR, the assertion has never matched and the capture has
         been reporting a miss quietly. Either way the fix is not in that
         line alone, and only somebody who can look at the page can say
         which.

  2. `tests/test_docs_media_budget.py::test_narration_is_the_stable_mobile
     _clock` expects `app_v2.js?v=20260910-example-downloads` and the
     player page carries `app_v2.js?v=20260910-plaque-example-zip`. One of
     the two is stale and only the session that changed the cache-buster
     knows which.

  3. `tests/test_tutorial_release_audit.py::test_every_module_and_fold_has
     _exactly_one_current_tutorial_route`.

THE CLAUDE SESSION'S HALF OF THAT RUN IS CLOSED. Of the 28, the ones that
were not tutorial-shaped are fixed: the caption ratchet, four docstring
ratchets, two segfaults, an abort, a teardown error, two stale message
expectations, the module denominator, an undeclared dependency, a phantom
`__all__` export, an assertion-free test, the French reviewed count, a dead
packaging exclusion, three archived installers, and the environment and
app-registry leaks. What is left is these three plus the API catalogs,
which are rebuilding.

--------------------------------------------------------------------------------
2026-09-15 -- OWNERSHIP MOVES FROM CODEX. THE TRUE STATE, AND WHAT IS OWED
--------------------------------------------------------------------------------

OWNERSHIP. The maintainer, 2026-09-15: "codex is not working on this
anymore". 358 now belongs to the Claude session working in
/mnt/firecuda2/Claude/repo/spacr, and the 2026-09-09 "neither Claude session
touches them" rule no longer applies to this item. Nothing below reads or
writes /mnt/firecuda2/codex.

THE 2026-09-11 NOTE IS STALE. All three of its reds pass at a8fdda325:
test_the_name_is_spaCR.py 5 passed; test_tutorial_release_audit.py plus
test_docs_media_budget.py 36 passed. The route test is green because ops,
import_images and regression_diagnostics now have lessons (76, 74, 75).

WHAT EXISTS

  * Private candidate refresh_2026-09-09/release-candidate-zveusu2j: 77
    routes, 76 ready lessons plus Investigate Hit as Coming soon, 3,800
    tracks, unpublished, release hold in force. verify_release_candidate.py:
    exit 0, 90 PASS (14 Coming-soon languages, 76 playback cases). A second
    run while a Qt capture held the CPU at 100% failed a single clock check
    (01_pypi_github video 0.55 s behind audio, limit 0.5). A re-run with no
    capture of mine running failed the same check (0.73 s). Other sessions
    held the load average at 25-31 on 32 cores (neardup.py workers and
    build_documentation_i18n.py --device cuda). Both runs passed the
    candidate's media-hash validation before playback, and nothing under
    release_candidate/ changed. Re-run on a quiet machine before trusting
    either result.
  * Running app, 1.5.0.7: 45 registered modules; Home bands Core, Data,
    Tools, Assays; Core is Mask, Measure, Annotate, Classify, Map Barcodes,
    Regression. 36 physical folds plus 3 hosted modes make 39 folded routes.
    The candidate's host_app_key map equals that map exactly, and its 71
    routed lessons equal registry plus folds.

AUTHORING SUITE (tools/tutorials/authoring/tests; CI does not collect it):
7 failed / 101 passed -> 108 passed. Every failure was the TEST or the
SNAPSHOT behind the app and candidate, not a lesson defect.

  * Route and Home tests pinned 23 folds, 67 routes, 44 modules and six Home
    categories. FOLDED_LESSON_HOSTS is now the live 39. The live-app
    ratchets read release_candidate/web/catalog, because authoring/catalog
    is the held pre-refresh set the reviewed-translation tests still pin.
    Home core names come from the Home Core band: Feature Explorer is
    registered Core but folded into Classify, so it has no tile. The
    refreshed nine-scene Home narrates no module count, no
    Alpha/Beta/Stable and no Performance selector, so those assertions
    were DROPPED, not moved. Whether Home should teach the Performance
    selector again is the maintainer's call.
  * Player test: "8 languages · 50 voices" left index.html on 09-11
    (5da4a6a95). Counts are now ratcheted from voice_catalog.js (8
    languages, 50 voices); the app_v2 key is 20260911-narration-captions.
  * web/logo_spacr.png (the same git blob as spacr/resources/icons) and
    production/01_pypi_github/scenes.json were copied from the workspace.
  * SPACR_REPO defaulted to the Codex tree in test_catalog_routes.py,
    publish_tutorials.py and capture_make_masks_experiment.py. It now
    defaults to the checkout containing the file. Another 24
    authoring/tools/*.py still hard-code /mnt/firecuda2/codex; no suite runs
    them.
  * HAZARD: snapshot_sources.py copies the workspace over authoring/. The
    workspace still has the old tests, so a routine snapshot reverts all
    of this.

tools/tutorials/tests is ALSO NOT A READABLE GATE, AND IT IS NOT IN CI. It is
order-dependent: 749 passed / 2857 errors before these edits, 1828 passed /
10 failed / 679 errors after. The errors are teardowns in tests/conftest.py
calling torch.cuda after a tutorial test stubbed torch with a
SimpleNamespace. The 10 failures are test_image_scatter_evidence.py
("painted X" discrepancy). That file fails the same way when run alone and
reads nothing these edits touched.

PER-LESSON STATE (scene numbers are 1-based in the candidate catalog)

  07_mask -- STALE: re-capture AND re-narration.
    * Scene 1 never named OPS, which is now folded into Mask's action row
      (3D, Time, Live, OPS, AI; confirmed on 1.5.0.7). SOURCE EDITED in
      lessons/07_mask.json: scene 1 narration, related_lessons += 76_ops,
      objective 1, and the prerequisite's "OPS remains unproven and is not
      taught here". The existing frame shows the current row but a 1.5.0.6
      footer.
    * Scenes 4-8 (preview_04/05/09/10/12) were recorded 09-09 on 1.5.0.4.
      Their Live settings dialog shows Min object area, Min distance, Area
      multiplier, Min intensity pct, Max intensity pct, Intensity percentile
      and an Intensity threshold method. Today it shows Minimum area to
      split, Minimum watershed distance and an absolute Intensity threshold,
      and the other four rows are gone (391: 4fcb3266b and 9fc41242d, 09-12).
      The form behind the dialog still shows the retired Normalize plots.
      The lesson's own reuse rule (live_preview.py byte-identical) is
      broken, and compose_mask_capture.py refuses at HEAD for that reason.
      The counts narrated in scenes 6-8 (51 -> 25 -> 51 at 5642 px; 42 after
      diameter 30 -> 60) must be re-measured on the new capture before any
      narration is rendered.
    * Scenes 2, 3, 9, 10 and 16 (09-11, 1.5.0.6) show Essentials rows only,
      and none of those rows is renamed. Scene 10's overlay-loop failure has
      not been re-checked at HEAD. Scenes 11-15 are terminal and external
      viewer frames and are unaffected.
    * CAPTURE HYGIENE: a fresh, isolated 1.5.0.7 capture shows Source =
      /tmp/pytest-of-olafsson/pytest-1133/test_the_chain_runs_mask_then_0/toxo
      before Load test data. A test writes state that the capture's
      XDG_CONFIG_HOME isolation does not cover; the store was not found in
      ~/.config, ~/.local/share or ~/.spacr. Until that is fixed, no
      pre-download Mask frame is usable.
  13_regression -- FRAMES STALE; narration current.
    Recorded 09-09 (2a2917985, 1.5.0.4), before 364's renames (3758412b0 on
    09-09, 313d36f54 on 09-10). Every frame showing the full form has
    CONTROLS & FILTERS rows Positive control, Negative control, Min cell
    count and Min n. Today they read Positive control ID, Negative control
    ID, Min cells per well and Min observations per hit; Control gRNA/Gene
    kept its label under nontargeting_control_grnas. Viewed: scenes 1, 2,
    12, 14, 19/21 and 20. Scenes 13, 15, 16, 17 and 18 use the same layout
    and were not viewed individually. Scenes 3-11 are search-filtered and
    unaffected. No narration reads a renamed label. Re-capture with
    capture_refresh.py --module regression --download --run --ai-controls
    --settings-tour.
  08_measure -- CURRENT on settings. Its 09-10 capture (88abc232c) has the
    same 67 keys as today's factory, and no label has changed since.
  12_map_barcodes -- CURRENT on settings. Recorded 09-13 (6e72b1611). The
    only later label change is the grna retirement (2acee777a), and grna was
    never a row on the Map form (grna_csv is). If the held `barcodes`
    retirement lands, recheck gui_04, gui_11 and gui_20.
  09_annotate and the Classify lessons -- not re-audited tonight.
  05_home (not a Core lesson; found in passing) -- scene 3 lists Data as
    Import, Run Compare, Experiment Design, Power and Design, Dose Response
    and quality control. The live Data band also has Embeddings (7f0f74ac7,
    09-11).

CPU CAPTURE RUN TONIGHT (Qt offscreen, CUDA_VISIBLE_DEVICES="", new stage
refresh_2026-09-15_cpu so refresh_2026-09-09 is not overwritten):

    capture_refresh.py --module mask --stage $W/refresh_2026-09-15_cpu
      --download --preview --preview-variants --capture-name <name>
      --timeout 3000 --platform offscreen

  * mask_preview_cpu: the 1.65 GB example download broke at 323 MB
    (IncompleteRead); no images arrived. Frames 00-03 only.
  * mask_preview_cpu2: the download completed (208 TIFFs), then Live loaded
    no field within 90 s. After the 09-12 rewrite, the panel fills its set
    table only from an image the user chooses. capture_refresh.py now falls
    back to the real "Choose image…" dialog (plate1 E01 F001) and records
    preview_choice.json.
  * mask_preview_cpu3: FAILED at 22:46 (exit 134) with "Preview not finished:
    Running preview…" after the full 3000 s --timeout. A single CPSAM preview
    on one 4-channel field did not finish in 50 minutes on CPU, on a machine
    other sessions held at load 25-31. Frames 00-06 exist. Its
    05_live_settings shows today's dialog (Min area, Max area, Minimum area
    to split, Minimum watershed distance, Perimeter fraction, absolute
    Intensity threshold, merge/split/border/Adjust cells). The retired rows
    are gone, and Normalize plots is gone from the form behind it. Frames
    07-12, preview_outputs.json and live_variants.json were NOT produced, so
    the scene 6-8 counts are still unmeasured.
  * On this run the cached-load route loaded a field by itself within the
    3 s settle, so the new "Choose image…" fallback was NOT exercised. It is
    untested; it exists because run 2 (fresh download) loaded nothing in
    90 s.
  * NEXT CAPTURE: this preview is GPU-scale. Re-run the same command
    without CUDA_VISIBLE_DEVICES="" once the GPU is free, or on CPU with
    --timeout 7200 on a quiet machine. Use a new --capture-name. The 1.6 GB
    stage (example data included) is outside the repository.

OWED, IN ORDER. Nothing here has been run. [GPU] marks the GPU steps.

  CPU first: point compose_mask_capture.py at the new preview capture
  instead of commit 04b23431f and captures/mask; independently re-verify the
  preview counts (mask_preview_evidence.py); update the scene 6-8 numbers;
  run stage_lesson.py lessons/07_mask.json --capture-module mask. Re-capture
  Regression and recompose 13_regression's visuals.

  Common:
    cd /mnt/firecuda2/Claude/repo/spacr
    V=/mnt/firecuda2/Claude/toxoplasma_projects/tutorials/refresh_2026-09-09/.venv/bin/python
    W=/mnt/firecuda2/Claude/toxoplasma_projects/tutorials
    ST=$W/refresh_2026-09-09

  07_mask (narration changed: translation, 50 voices and master):
    [GPU] python tools/run_memory_guarded.py --limit-gib 100 -- $V tools/tutorials/translate_refresh.py --lessons 07_mask --device cuda
          # default languages are all 13: es fr hi it ja pt-BR zh-CN da de is ko nb sv.
          # Drafts land in $ST/catalog-drafts. Review them, then:
          $V tools/tutorials/apply_translation_review.py <review.json>
    [GPU] SPACR_TUTORIAL_WORKSPACE=$ST python tools/run_memory_guarded.py --limit-gib 100 -- $V $W/tools/render_all_voices.py --lessons 7 --device cuda
          # the 50-voice matrix over the render_kokoro_narration.py stack; one track only:
          # $V $W/tools/render_kokoro_narration.py <scenes.json> --output-dir <dir> --voice af_heart --device cuda
          $V tools/tutorials/verify_staged_audio.py --lesson 07_mask --languages en es fr hi it ja pt-BR zh-CN
    [encode] $V $W/tools/render_visual_master.py $ST/production/07_mask/visual.json --timings $ST/production/07_mask/audio/en/af_heart.json --output $ST/production/07_mask/video/07_mask_silent.mp4
          $V tools/tutorials/check_completed_matrix.py --lesson 07_mask
          # then refresh the library checkpoint with verify_library_checkpoint.py
    [encode] $V tools/tutorials/stage_web_renditions.py --lesson 07_mask
          # then build_release_candidate.py and verify_release_candidate.py <new candidate>

  13_regression (narration unchanged: no translation, no voices):
    [encode] $V $W/tools/render_visual_master.py $ST/production/13_regression/visual.json --timings $ST/production/13_regression/audio/en/af_heart.json --output $ST/production/13_regression/video/13_regression_silent.mp4
    [encode] $V tools/tutorials/stage_web_renditions.py --lesson 13_regression

  05_home, if Embeddings is added to scene 3: as 07_mask, with
  --lessons 05_home for translation and --lessons 5 for voices.

  08_measure, 12_map_barcodes: nothing owed for renames.

NEEDS THE MAINTAINER: lifting the publication hold (the candidate still uses
local media_host roots), uploading a new versioned media revision to
Hugging Face, deploying Pages from main, and the Home/Performance question
above.

--------------------------------------------------------------------------------
2026-09-15, CLAUDE -- CAPTURE HYGIENE FIXED; 13_REGRESSION RE-RECORDED
--------------------------------------------------------------------------------

Worked in the worktree /mnt/firecuda2/Claude/repo/spacr-358, branch
wip/358-lessons (from nightly 63f1d502b). Stage: refresh_2026-09-09 with NEW
capture names only; every overwritten staged file has a copy under
production/<lesson>/pre-2026-09-15/.

CAPTURE HYGIENE -- ROOT CAUSE. Mask's remembered `src` is a CHAINING PIN,
kept by spacr.chaining.PinStore in XDG *state* storage:
~/.local/state/spacr/chaining/pins.json. It is not under XDG_CONFIG_HOME,
~/.local/share or ~/.spacr, which is why the earlier search missed it. The
real file pinned Mask to /tmp/pytest-of-olafsson/pytest-2295/
test_head_loaddata0/plate1 and Timelapse to another pytest folder. Nothing
in tests/conftest.py sandboxed that store, and capture_refresh.py isolated
XDG_CONFIG_HOME only, so a "fresh" capture opened on whatever a test last
pinned. The Qt chaining strip writes a pin whenever a bound path differs
from the one it offered (spacr/qt/chaining.py _capture_edits).

FIX, at both ends:
  * tests/conftest.py: SPACR_CHAINING_PINS points at a sandbox from
    pytest_configure (before collection), and an autouse fixture gives each
    test its own pin file.
  * tools/tutorials/capture_refresh.py: XDG_STATE_HOME =
    <stage>/state/<capture-name>; an inherited SPACR_CHAINING_PINS is
    removed; a capture name that already has pins is refused.
  * tests/test_the_chaining_pin_store_is_sandboxed.py: 4 regression tests.

MEASURED, against a stand-in XDG_STATE_HOME in place of the real home:
  BEFORE (HEAD conftest): 3 of 3 pin tests fail; the stand-in store
    received {"mask": {"src": "/tmp/pytest-of-olafsson/pytest-2325/
    test_a_pin_written_in_a_test_l0/plate1"}}, the same shape as the leak.
  AFTER: 4 passed; the stand-in store has 0 files. test_chaining.py,
    test_cov_w2_4_remote_execution.py and the new file: 133 passed.
  CAPTURE: regression_2026-09-15 opened with no src, and its one pin
    landed in <stage>/state/regression_2026-09-15, not in ~/.local/state.
  The reused 09-11 native Mask captures (mask_fresh_v1, the overlay command
  and viewer captures, captures/mask) name no pytest path.
  NOT CHANGED: the maintainer's real ~/.local/state/spacr/chaining/pins.json
    still holds the two pytest pins. Deleting those two entries is safe;
    the file is his, so it was left alone. Test runs from trees without this
    fix will keep writing it until the branch lands.

13_REGRESSION -- visuals refreshed, narration retained.
  capture_refresh.py --module regression --download --run --ai-controls
    --settings-tour --capture-name regression_2026-09-15: exit 0,
    1.5.0.7 at 63f1d502b, batch accepted (worker ok, 5 figures).
  Frames viewed (01_module, 21_batch_running): CONTROLS & FILTERS read
    Positive control ID, Negative control ID, Min cells per well and Min
    observations per hit; the footer reads 1.5.0.7.
  stage_lesson.py --capture-module regression_2026-09-15: 21 scenes, all 21
    images changed, 0 focus rectangles changed, English catalog
    byte-identical.
  render_visual_master.py: 30,206,284 bytes, 389.33 s, 11,680 frames (the
    old master had the same duration and frame count).
  check_completed_matrix.py: passed, 50 tracks, 14 browser checks.
  poster.jpg regenerated from scene 1 at 1280x720 (93 KB), matching the
    convention of the 07/08/13 posters.
  The 14 browser playback reports predate the new master. The matrix pins
    audio hash and clocks, not video bytes; verify_release_candidate's
    playback pass is what exercises the new bytes.

  LIBRARY CHECKPOINT: THE HELD LIST MUST SUBTRACT THE PROMOTIONS. A re-run
    with all four coming_soon.HELD ids (the list the 09-12 file records)
    fails in 1 s with "Unselected lesson was modified:
    lessons_en.json/12_map_barcodes". The list is NOT stale.
    build_release_candidate.build() verifies with HELD minus the promoted
    lessons (12_map_barcodes via barcode_promotion; 21_model_compare and
    22_model_zoo via model_promotion.MODELS). That leaves 71_investigate_hit
    only. No repo script passes those promotions, so the candidate build is
    the inline call below. The failed run wrote nothing; the 09-12 checkpoint
    is intact, with a copy at
    library-checkpoint-2026-09-11.pre-2026-09-15.json. A re-run with
    --held 71_investigate_hit PASSED: 76/77 lessons, 3,800 tracks, 1,064
    browser cases, including the new 13_regression master. Output kept
    outside the stage, at <scratchpad>/358_reg/library-checkpoint-2026-09-15.scratch.json.
  13_regression's rendition therefore took the route complete_map_media.py
    and complete_model_media.py already use: check() -> stage_one(...,
    'reconciliation': proof) -> verify_staged_lesson.py --web-rendition.
    stage_one re-checks the master hash against the proof, so a stale proof
    cannot pass.
  RENDITION RESULT. The 09-11 1440p copy and its receipt were moved to
    production/13_regression/pre-2026-09-15/web-renditions/ first, because
    stage_one refuses to replace an existing rendition that differs.
    stage_one: accepted, 9,251,825 bytes (was 32.5 MB 4K master ->
    1440p), 11,680 frames, every presentation time matching, full decode
    passed, 140 s. verify_staged_lesson.py --web-rendition: passed, 21
    scenes, 9 chapter/transcript links, clocks audio 84.128 / video 84.057 /
    expected 84.126 s (0.069 s apart, limit 0.5). Its desktop screenshot
    shows the new 1.5.0.7 frame in the player.
  13_REGRESSION STATE: captured, composed (staged), master encoded, matrix
    passed, 1440p staged and browser-checked. NOT in a release candidate
    yet (see below). Narration, translations and the 50 tracks are unchanged.

07_MASK SCENE 10 RE-CHECKED AT HEAD (code reading, not a new run). The batch
  overlay loop in spacr/core.py (preprocess_generate_masks, plot=True, not
  timelapse) still does `files = os.listdir(merged_src)` with no `.npy`
  filter, shuffles, and plots the first examples_to_plot entries. Only the
  test-mode COUNT filters `.npy`. So .spacr_plane_layout.json can still be
  handed to plot_image_mask_overlay as an image, which is the failure scene 10
  narrates. The only commit on spacr/core.py or spacr/plot.py since the 09-11
  run is d0334c103 (09-13, comment removal). Scene 10 and its "STILL fails"
  prerequisite stay accurate. The application fix belongs to that file's
  owner, not to the tutorial lane.
  mask_preview_evidence.inspect on the 09-09 captures/mask reproduces 51 ->
  25 (minimum area 5642) -> 51 and 42 after the diameter change. The tool is
  sound for the re-recording.

RELEASE CANDIDATE WITH THE NEW 13_REGRESSION (built before 07_mask, so a
  verified candidate exists whatever happens to the GPU queue tonight).
  release-candidate-tytdds1k: 76 ready, 77 routes, 3,800 tracks, 7,875
  files, 365,626,408 web bytes (ceiling 734,003,200), Coming soon =
  71_investigate_hit. Compared with zveusu2j file by file, it differs in
  exactly four paths: 13_regression's web copy (4c82cc82...), 4K master
  (27d062fd...), poster, and web/module_navigation.js, whose only changed
  value is source_commit (455c4d29 -> e668e60a). No other lesson's bytes
  moved. It stays private and held; nothing was uploaded.
  Build call (no repo script passes the promotions):
    cd tools/tutorials && PYTHONPATH=<repo>:<repo>/tools/tutorials $V -c "
    from pathlib import Path; from build_release_candidate import build
    from model_promotion import MODELS; s = Path('$ST')
    build(s, baseline=s / 'baseline-d2d4c189b-hmx3Us/docs/source/_extra/tutorials/catalog',
          model_promotions=set(MODELS), barcode_promotion=True)"
  VERIFIED: verify_release_candidate.py release-candidate-tytdds1k exit 0
    (04:10:52 -> 04:15:13 UTC, load ~11 on 32 cores). 90 PASS: 14 Coming-soon
    language screens and 76 candidate playback cases, 13_regression
    included. 01_pypi_github PASSED its clock check this time; it failed
    twice on 09-11 under load 25-31, which fits the earlier reading that the
    failure was machine load, not a media defect.

07_MASK TRANSLATIONS ARE STALE EVEN BEFORE THE NEW COUNTS. Read
  lessons/reviews/07_mask.es.json against the current English: scene 1 still
  says "Live, Time and three-dimensional settings" and names only Time's
  linked tutorial (no OPS), objective 1 lists "Live, Time and 3D", and the
  prerequisite ends "OPS remains unproven and is not taught here". Its
  review pins english_sha256 7f8ea6e1...; the current English canonical hash
  is ec839288.... So all 13 reviews need at least scene 1, objective 1 and
  the prerequisite updated. Scenes 6-8 and the prerequisite's "Live
  implementation unchanged / 51->25->51 / 42" wording must follow the new
  measurement.

07_MASK REVIEW SCOPE, MEASURED. The 13 lessons/reviews/07_mask.<lang>.json
  pin english_sha256 7f8ea6e1..., which is exactly the canonical hash of
  lessons/07_mask.json at 8dfce47d7^ (verified). Against that English the
  current source changes exactly three translated rows:
    objectives[0]   "... Live, Time and 3D controls." -> "... Live, Time, 3D
                    and OPS controls."
    scene 1         Live/Time/3D "settings are reached inside it; Time has
                    its own linked tutorial" -> "its action row holds the
                    Live, Time, three-dimensional and OPS switches. Time and
                    OPS each have their own linked tutorial ..."
    prerequisite    ONE sentence of 16: "OPS remains unproven and is not
                    taught here." -> "OPS is a separate page opened from
                    Mask's action row and has its own lesson; this lesson
                    does not run it."
  (scene 1 related_lessons gained 76_ops; links are not translated.)
  The new preview recording will add scenes 6, 7 and 8 and the
  prerequisite's live-preview sentence (reuse rule, 51->25->51, 5642, 42)
  once the counts are measured.
  REVIEW METHOD: translate_refresh.py re-drafts the whole lesson, so a
  review must NOT take the draft wholesale. Keep each existing reviewed row
  whose English did not change. For each changed row, read the draft
  against the new English and write the reviewed text. Then diff every row
  of each new review against its old file: only the changed rows may differ.
  EVIDENCE FOR THE REVIEW RULE, before any new draft exists:
    $ST/catalog-drafts already holds a 07_mask machine draft from an
    earlier run. It translates the OLD English, and it renames the UI.
    Spanish objective 1 "Encuentra Mask en el hogar y sus controles en vivo,
    tiempo y 3D" and scene 1 "La máscara abierta desde casa ... El tiempo
    tiene su propio tutorial enlazado bajo Máscara" make Home "hogar"/"casa",
    Live "en vivo", Time "tiempo" and Mask "máscara". None of those names
    exists in the interface. The reviewed rows say "Home", "Live", "Time"
    and "Mask". A stale draft can therefore look plausible, and even a
    current draft must not be applied without reading each row.
    Snapshot of the 13 reviews before editing:
    <scratchpad>/358_mask/reviews_before/ (sha256 prefixes recorded there).
  OPS ROWS WRITTEN AND VALIDATED FOR ALL 13 LANGUAGES (not yet applied). The
    reviewed text for objective 1, scene 1 and the prerequisite's final OPS
    sentence is in <scratchpad>/358_mask/ops_rows.json. Each is a minimal edit
    of that language's existing reviewed row, with Home, Mask, Live, Time and
    OPS kept literal. Where a possessive would inflect a screen name (Danish
    "Masks"), the sentence says "in Mask" instead. Korean keeps the review's
    attached particles ("Mask의"); the spacing workaround applies only to the
    i18n builders' protected-literal regex, which tutorial reviews do not
    pass through.
    Dry validation against the 13 real review files: every old prerequisite
    sentence occurs exactly once and is the final one; every new row carries
    all five UI literals; scene 1's closing sentence is unchanged in the
    alphabetic languages; es fr it pt-BR hi ja zh-CN pass spoken_form +
    assert_pronunciation_safe. Problems: none.
    Applying waits for the new counts: build refuses unless the staged
    English equals the source lesson, and scenes 6-8 plus the preview
    sentence of the prerequisite still have to be added to the same reviews.
    Helper: <scratchpad>/358_mask/review_tool.py (show / build / diff); it
    refuses a draft unless the staged English is the current source and the
    draft manifest ties the draft to it.
    PRONUNCIATION CHECKED AGAINST THE PUBLISHED OPS LESSON: staged 76_ops
    speech_text keeps "OPS" literal in ja, zh-CN and hi and respells only
    spaCR (スペーカー / 斯佩克尔 / स्पेकर). The 07_mask bundle's speech
    likewise passes "OPS" through unchanged, so both lessons voice it the
    same way.

GPU QUEUE, 04:27 UTC. The 07_mask preview capture has waited on the shared
  GPU lock since 03:54. The holder is another session's
  build_documentation_i18n.py --repair-api-blocks (9 languages). Its last
  catalog writes were fr.json/en.json at 04:18, but it is NOT hung: from
  04:24:31 to 04:26:53 it used +141.7 CPU-s (one full core), read +14.5 MB
  and wrote 0 B, with the GPU at 0 %. That fits a CPU-only audit pass. It was
  left alone. Samples: <scratchpad>/358_mask/holder_liveness.txt.
  VOICE RENDER RE-RUN RULE CHECKED (render_all_voices.py, read, not run):
    with SPACR_TUTORIAL_WORKSPACE=$ST it reads $ST/catalog and writes
    $ST/production. A complete existing track is SKIPPED only when its
    render_fingerprint matches (the fingerprint covers the per-scene speech
    plan), or when only display narration changed with an identical speech
    plan and a matching media hash (audio_equivalent_render_inputs strips
    `narration`, never speech). New scene 1 / scene 6-8 wording therefore
    re-renders the affected tracks without --force. Do not add --force: it
    also re-synthesizes tracks whose speech did not change.

07_MASK -- EXACT RESUME SEQUENCE. STATUS (updated 2026-09-15 08:07 UTC): steps
1-10 are DONE and verified -- see the dated notes further down (capture fresh2,
compose 2026-09-15c, English d174cefda, staged d988500d, 13 reviews 889cbb02f,
50 voices incl. the bm_fable peak repair ae4f80b00, decode/browser/master/
matrix/rendition/web playback). Step 11 (release candidate) is running; if it
did not finish, re-run step 11 only. Reviewed OPS rows are committed at
tools/tutorials/lessons/reviews/pending_07_mask_ops_rows.json. The pending_
prefix keeps the file out of every `<lesson>.*.json` glob, so it cannot be
read as a fourteenth language.

  R=/mnt/firecuda2/Claude/repo/spacr-358       # or wherever wip/358-lessons is checked out
  W=/mnt/firecuda2/Claude/toxoplasma_projects/tutorials; ST=$W/refresh_2026-09-09
  V=$ST/.venv/bin/python; G=<shared gpu.lock>; M="$V $R/tools/run_memory_guarded.py --limit-gib 60 --"
  export PYTHONPATH=$R:$R/tools/tutorials; cd $R/tools/tutorials
  CAP=mask_preview_gpu_2026-09-15b; COMP=mask_preview_and_explicit_overlay_2026-09-15b
  # NOT mask_preview_gpu_2026-09-15: that capture read a stale run-saved
  # gen_mask_settings.csv (src=<src>, diameter 60) and is superseded; see below.

  1 [GPU] capture. FIRST confirm example_data/plate1/settings holds only the
    shipped pack (gen_masks_settings.csv, crop_measure_settings.csv); a
    run-saved gen_mask_settings.csv is read first and poisons the counts.
    A failed run needs a NEW --capture-name; the private
    state guard refuses a name that already has pins.
      # Use a FRESH example copy (refresh_2026-09-15_cpu: TIFFs + shipped pack only);
      # never `xvfb-run -a` or a 2 h timeout under the lock (see the 06:52 failure):
      flock $G run_capture_guarded.sh <fresh stage> <new capture name> <log> 358
      # (fixed display, own process group, X11-break watchdog, --timeout 900, whole run 1500)
    View 04_live_image, 05_live_settings and 07-12 before using the counts.
  2 Recount independently:
      $V -c "from mask_preview_evidence import inspect; print(inspect('$ST/captures/$CAP'))"
  3 Compose (checks live_preview.py/preview_contract.py against $CAP's own commit):
      $V compose_mask_capture.py --stage $ST --preview-capture $CAP --destination $COMP
  4 English: in lessons/07_mask.json set scenes 6-8 and the prerequisite's
    preview sentence from step 2's before/after/restored/minimum_area and
    parameter_variant_objects, with live_variants.json's diameters. Numbers
    come only from the measurement.
  5 Stage: $V stage_lesson.py lessons/07_mask.json --capture-module $COMP --stage $ST
  6 [GPU] Drafts: flock $G $M $V translate_refresh.py --stage $ST --lessons 07_mask --device cuda
  7 Review, per language (13): start from lessons/reviews/07_mask.<lang>.json.
    Apply the pending bundle's objectives[0] and scenes[0], and its exact
    prerequisite sentence replacement (the old sentence must occur exactly
    once). Write scenes[5..7] and the preview sentence by reading each
    changed row's draft against the English; never take a draft wholesale
    (the leftover draft renamed Home/Live/Time/Mask). Set english_sha256 to
    the canonical hash of the STAGED 07_mask, then:
      $V apply_translation_review.py lessons/reviews/07_mask.<lang>.json --stage $ST
    Then `git diff` every review: only objectives[0], scenes[0], scenes[5-7]
    and the prerequisite may differ. Delete the pending bundle once applied.
  8 [GPU] Voices (no --force; changed speech re-renders by fingerprint):
      flock $G env SPACR_TUTORIAL_WORKSPACE=$ST $M $V $W/tools/render_all_voices.py --lessons 7 --device cuda
      $V verify_staged_audio.py --stage $ST --lesson 07_mask --languages en es fr hi it ja pt-BR zh-CN
      $V -c "import subprocess,sys; from pathlib import Path; from check_completed_matrix import voice_matrix
      m=voice_matrix(Path('$W/tools/render_all_voices.py')); run=lambda *a: subprocess.run([sys.executable,'verify_staged_lesson.py','--stage','$ST','--lesson','07_mask',*a],check=True)
      [run('--language',l,'--voice',v[0]) for l,v in m.items()]; [run('--caption-language',c) for c in ('da','de','is','ko','nb','sv')]"
  9 Back up production/07_mask/{video,poster.jpg,final-artifact-checks.json,
    refresh-status.json} and web-renditions/07_mask to
    production/07_mask/pre-2026-09-15/. Then:
      $M $V $W/tools/render_visual_master.py $ST/production/07_mask/visual.json \
        --timings $ST/production/07_mask/audio/en/af_heart.json --output $ST/production/07_mask/video/07_mask_silent.mp4
      $V check_completed_matrix.py --stage $ST --lesson 07_mask
 10 Rendition (the library checkpoint route is not needed; same as 13_regression):
      check() proof -> stage_web_renditions.stage_one(Path('$ST'), {'lesson':'07_mask',
        'scope':'staged final bytes','reconciliation':proof}, publisher with
        ENCODE_ARGS prefixed '-threads 2 -filter_threads 2')
      $V verify_staged_lesson.py --stage $ST --lesson 07_mask --web-rendition
 11 Candidate: the build() call recorded above (model_promotions=set(MODELS),
    barcode_promotion=True), then $V verify_release_candidate.py <new candidate>.

BRANCH BASE MOVED UNDER THIS WORK (measured 04:3x UTC). wip/358-lessons was
  created from nightly at 63f1d502b. At 04:17 UTC nightly was rebased onto
  fafa785b4 ("the work session's batch"), and origin/nightly is now
  bac9151d0; local nightly has since moved on to dfafc0c7d. 63f1d502b is no
  longer an ancestor of either, so `git diff nightly..wip/358-lessons` shows
  other sessions' rewritten commits (build_i18n_catalogs.py,
  00_INDEX.txt, ...) that are NOT this branch's work.
  * This branch's own 10 commits (63f1d502b..wip/358-lessons) change 6
    files: item 358, tests/conftest.py,
    tests/test_the_chaining_pin_store_is_sandboxed.py,
    tools/tutorials/capture_refresh.py, tools/tutorials/compose_mask_capture.py,
    tools/tutorials/lessons/reviews/pending_07_mask_ops_rows.json. None is on
    tonight's do-not-touch list. No co-author trailers, not pushed.
  * REPLAY TESTED: all 10 cherry-pick cleanly onto nightly dfafc0c7d (scratch
    detached worktree, removed afterwards), with the same 6 files.
  * DRIFT on what tonight depends on (63f1d502b -> nightly): only
    spacr/chaining.py (_canonical now folds hyphens via
    validate.canonical_app_key; PinStore, state_path, pin_store and
    SPACR_CHAINING_PINS unchanged, so the sandbox fix and its tests still
    hold) and settings_model.py (289: help stays on the label, no row
    added/removed/renamed, so the 13_regression frames stay current).
    live_preview.py, preview_contract.py, core.py, qt/chaining.py, both
    lesson sources, the reviews and the tutorial tools are unchanged.
    __version__ is 1.5.0.7 on both sides.
  * NOT rebased here, deliberately: the queued Mask capture imports from this
    worktree, and compose_mask_capture.py `git show`s the capture's recorded
    commit. For the maintainer, AFTER the Mask capture is composed:
      git tag 358-capture-base-63f1d502b 63f1d502b   # keeps the 13_regression / Mask provenance commits reachable
      git rebase --onto nightly 63f1d502b wip/358-lessons

07_MASK PREVIEW CAPTURE #1 IS SUPERSEDED: A STALE SETTINGS FILE SHADOWED THE PACK.
  mask_preview_gpu_2026-09-15 ran on the GPU (lock 04:34:43, exit 0 at
  04:35:53, 1.5.0.7 at afc60c409, completed_capture true). The "Choose
  image…" fallback worked on its first real run (plate1 E01 F001), and
  05_live_settings shows today's dialog (Min/Max area px², Minimum area to
  split, Minimum watershed distance, Perimeter fraction, absolute Intensity
  threshold; no retired rows; no Normalize plots). Its arrays recount
  exactly: 35 -> 17 at min area 6714 -> 35, and 35 after diameter 60 -> 120.
  Frames 04 and 07, however, show Source = the literal "<src>".
  CAUSE, measured (not inferred):
    * refresh_2026-09-09/example_data/plate1/settings holds two files the
      shipped pack does not: gen_mask_settings.csv/.json (09-01 14:30,
      205 keys, src=<src>, cell/nucleus/pathogen diameter 60/30/20,
      cell_FT 1.0, cell_CP_prob 0). This is a RUN-SAVED file:
      core.py:344 save_settings(settings, name='gen_mask_settings').
    * The shipped pack (gen_masks_settings.csv + crop_measure_settings.csv)
      is byte-identical in this stage and in the fresh 09-14 download. It
      has 59 keys, src=/home/carruthers/datasets/plate1 (replaced on load),
      no cell_diameter, cell_FT 100, cell_CP_prob -1.
    * settings_pack.PACK_FILES["mask"] = ("gen_mask_settings.csv",
      "gen_masks_settings.csv"): the run-saved name is tried FIRST, so a
      prior run's own settings outrank the pack on the cached route.
    * The fresh-download CPU captures (mask_preview_cpu2/3) recorded Source
      = the real example path. Their Mask pin was then a pytest path, so
      that value came from the app. The earlier 09-09/09-11 real-path
      Source readings came from a stage or pin store that differed; either
      way, tonight's "<src>" is stage contamination on the cached route.
  So capture #1's counts reflect somebody's 09-01 run settings (flow
  threshold 1.0, diameter 60), not what Load test data gives a new user. Do
  not compose it. Evidence kept. The diameter-rerun comparison was measured
  anyway (foreground IoU 0.977, 32/35 objects best-match IoU >= 0.9, 2 <
  0.5, 1 unmatched; smallest object 100 -> 20 px): at the same count the
  objects can differ.
  ACTION: the two stale files were moved to
  production/07_mask/pre-2026-09-15/stale_example_settings/, which makes
  the stage's settings folder identical to the fresh download (checked with
  diff -r). A clean recapture, mask_preview_gpu_2026-09-15b, is queued on
  the GPU lock.
  APP GAP, for the owner of settings_pack.py / AppScreen._EXAMPLE_SETTINGS_FILES
  (app_screen.py is another agent's tonight; not patched here): a user who
  runs Mask on the example folder and later presses Load test data on the
  cached example gets their OWN saved run settings back instead of the pack,
  including a literal "<src>" if that run was saved with the placeholder.
  Loading could prefer the shipped name for the example pack, or ignore a
  src that is still a <placeholder>.

07_MASK CLEAN RECAPTURE mask_preview_gpu_2026-09-15b -- THE COUNTS OF RECORD.
  GPU lock 04:43:23, exit 0 at 04:44:35. 1.5.0.7 at d73621b58, completed.
  Stale settings removed beforehand (example settings folder = fresh
  download, diff -r). Checked mid-run: Source =
  /home/olafsson/.cache/spacr/example_data/plate1 (not <src>). The shipped
  pack's legacy keys were migrated: cell/nucleus/pathogen_flow_threshold
  100.0; cell and pathogen_cellprob_threshold -1.0, nucleus 0.0. The pack
  sets no diameter, so Live used its 30 px default. The field came through
  the Choose image… dialog (plate1 E01 F001).
  Counts (live_variants.json / preview_outputs.json):
    baseline 105 cell labels; minimum area 510 px (median area + 1) -> 52;
    restored -> 105; diameter 30 -> 60 rerun -> 36 labels.
  INDEPENDENT RECOUNT (mask_preview_evidence.inspect): 105 -> 52 at 510 ->
  105, variant 36. Matches exactly.
  OBJECT COMPARISON (baseline vs diameter-60 arrays): foreground IoU 0.832;
  median object area 509 px at 30 vs 6,862 px at 60 (smallest 15 vs 151);
  only 33 of the 105 baseline objects have a best-match IoU >= 0.9, and 66
  have no overlapping partner. The 30 px default splits this field into
  many small objects, and 60 px gives cell-sized ones. Scene 8's "fewer
  labels do not establish a better model; compare boundaries with the
  image" is exactly the right claim. No narration may rank either setting.
  Old narration numbers (51 -> 25 -> 51 at 5,642; 42 after 30 -> 60) are
  retired. The diameter step itself (30 -> 60) is unchanged.
  FRAME CHECK OF 15b: frames 07 / 09 / 12 show Source = the real example path
    and "Found cell=105" / "cell=52" (Min area 510) / "cell=36" (Diameter
    60.0 px). Frame 05 shows today's dialog with Diameter 30.00 px, Flow
    threshold 100.00 and Cell probability -1.00. Composed as
    mask_preview_and_explicit_overlay_2026-09-15b (accepted, 20 frames,
    recount embedded).
  BUT 15b IS NOT STAGED. Frames 05, 07, 09 and 12 carry the orange warning
    "The settings-csv this run produced is out of date: the settings on
    this screen differ from the ones that produced it." refresh_2026-09-09's
    cached plate1 still holds artifacts.db, measurements/, merged/, qc/ and
    test/ from the 09-01/09-09 runs, and the screen's pack settings differ
    from that old run's. A new user's fresh download has no prior run, so
    the warning would teach a state they never see. It would also
    contradict scene 2 ("the recording uses a fresh private copy, so a
    previous run cannot supply stale results"). Capture #1 showed no warning
    only because it had loaded that same old run's settings.
  FIX: refresh_2026-09-15_cpu/example_data/plate1 is still a clean fresh
    download (208 TIFFs, identical names; settings/ holds only
    gen_masks_settings.csv + crop_measure_settings.csv; no run outputs;
    1.6 GB vs 2.5 GB). A guarded GPU recapture there is queued:
    mask_preview_gpu_fresh_2026-09-15. The guard refuses to run if any run
    output or extra settings file is present. Expect the counts to reproduce
    105/52/105/36. The capture directory will be copied into
    refresh_2026-09-09/captures with hashes intact for composition.

FRESH-COPY RECAPTURE FAILED, AND IT HELD THE SHARED GPU LOCK FOR TWO IDLE HOURS.
  mask_preview_gpu_fresh_2026-09-15 (refresh_2026-09-15_cpu, guard passed:
  208 TIFFs, shipped settings only) took the lock at 04:52:27 and recorded
  00-03 plus 04a_choose_image. The Source check mid-run was correct (real
  path; flow 100, cellprob -1). The log then reads "The X11 connection broke
  (error 1). Did the X11 server die?" and nothing more. The Qt process did not
  exit; `timeout 7200` killed it at 06:52:27 (exit 124). The GPU was idle, but
  the lock was held the whole time: two other sessions' GPU jobs waited
  2h01m and 1h34m behind it. No processes of this run were left over, and the
  example folder was unchanged (0 extras).
  CAUSE (probable, not proven): an `xvfb-run -a` display-number race. Another
  session's Xvfb has held :99 for four days, and other sessions keep starting
  `xvfb-run -a` pytest runs (one on :100 at 06:53). The stage was ruled out:
  the two stages' Mask qt.conf differ only in [recent] mask\last and
  figure_color_scale.
  RULE FOR EVERY FUTURE GPU CAPTURE UNDER THE LOCK: never `xvfb-run -a`, and
  never a long timeout around a GUI run. Use a fixed display checked free
  first, run the capture in its own process group, and kill the group on "X11
  connection broke". Keep the timeouts short (recorder --timeout 900, whole
  run 1500; a GPU preview capture takes ~70 s). A copy of the wrapper is at
  <scratchpad>/358_mask/run_capture_guarded.sh.
  RETRY QUEUED: mask_preview_gpu_fresh2_2026-09-15 on display :358 with that
  watchdog. Same guard, same fresh stage.
  RETRY #3 SUCCEEDED: mask_preview_gpu_fresh2_2026-09-15 (fresh copy,
    refresh_2026-09-15_cpu; guard passed). Watchdog wrapper on display
    :358. Lock 07:14:16, exit 0 at 07:15:16 (60 s on the lock). No X11
    break, no leftover processes, example folder unchanged. The recorder
    had waited in line behind two other sessions' GPU jobs (07:14 start).
    * 1.5.0.7 at dec126440, completed. Source = the real example path;
      shipped pack thresholds (flow 100, cellprob -1), Live default 30 px.
    * Counts 105 -> 52 at 510 px -> 105; 36 after 30 -> 60. The independent
      recount matches. preview_cell.npy and the diameter variant are
      BYTE-IDENTICAL to 15b: the stale run folder never changed the masks,
      only the on-screen warning.
    * FRAMES VIEWED 05, 07, 09, 10, 12: no stale-run warning in any of
      them. Frame 07 shows Source and "Found cell=105", 09 "Min area 510 /
      cell=52", 10 "cell=105" and 12 "Diameter 60.0 px / cell=36".
    * Copied into refresh_2026-09-09/captures/ (23 files, every sha256
      identical) for composition. The 15b and #1 captures stay as evidence
      and are NOT staged.
  COMPOSED AND STAGED. compose_mask_capture.py --preview-capture
    mask_preview_gpu_fresh2_2026-09-15 --destination
    mask_preview_and_explicit_overlay_2026-09-15c: accepted, 20 frames, the
    recount embedded, every preview frame from the fresh capture, and
    live_preview.py/preview_contract.py pinned to dec126440. The English
    change (d174cefda) touches exactly five lines, checked by structured
    comparison and a word diff. stage_lesson.py --capture-module
    mask_preview_and_explicit_overlay_2026-09-15c: 16 real capture scenes;
    staged English == source (canonical d988500d...). Scenes 4-8 carry no
    authored focus rectangles, so the new frames cannot leave a highlight on
    the wrong control. Backups: production/07_mask/pre-2026-09-15/ (visual,
    lesson.en, refresh-status, final-artifact-checks, poster, staged English
    catalog). Translation drafts are queued on the GPU lock.
  COUNT ROWS WRITTEN AND VALIDATED FOR ALL 13 LANGUAGES (not yet applied):
    tools/tutorials/lessons/reviews/pending_07_mask_count_rows.json. The
    edits are exact old -> new substrings of each language's existing
    reviewed rows: scene 4 (the preview is no longer "the preserved
    recording"), scenes 6-8 (105 / 510 / 52 / 36), and the prerequisite's
    live-preview sentence(s). Spoken languages spell the numbers out, as
    before (es ciento cinco, fr cent cinq, it centocinque, pt-BR cento e
    cinco, hi एक सौ पाँच, ja 百五, zh-CN 一百零五); caption languages use
    digits, as before. Icelandic 51 took the singular "frumumerkingu"; 105
    takes the plural "frumumerkingar".
    DRY BUILD of OPS + count bundles against the 13 real reviews, nothing
    written: every substring matches exactly once; exactly 7 rows change
    per language (prerequisite, objectives[0], scenes[0,3,5,6,7]); Home /
    Mask / Live / Time / OPS survive; es fr it pt-BR hi ja zh-CN pass
    spoken_form + assert_pronunciation_safe on every changed scene. Two
    stale-word hits (is "varðveitt", pt-BR "preservad") were FALSE
    POSITIVES. Each sits in an UNCHANGED sentence that legitimately says
    keep: "retaining known channel identities" and "failed runs and probes
    are preserved but not presented as complete". Neither refers to the
    retired "preserved recording".
    Still to do before applying: compare with the GPU drafts once they land.

07_MASK TRANSLATION REVIEW: 13 LANGUAGES, EVERY CHANGED ROW READ.
  translate_refresh.py --lessons 07_mask --device cuda (GPU lock 07:39:02,
  exit 0 at 07:41:04; 7-14 s per language). The manifest is tied to the
  staged English d988500d. Spoken drafts carry every field; caption drafts
  (da de is ko nb sv) carry scene narration only, so for those, objective 1
  and the prerequisite rest on the reviewed text alone. For each language,
  every changed row (objectives[0], scenes 1/4/6/7/8, and the prerequisite's
  OPS and preview sentences) was read against the English, the English the
  review was pinned to, the existing reviewed row, and the fresh draft.
  VERDICT: the prepared reviewed rows stand in all 13; no draft row was
  adopted. The drafts are not usable as translations:
    * THE HEADLINE COUNT: 105 was wrong in at least one scene in 11 of 13
      languages -- fr 150/150, it 105/100, pt-BR 125/125, ja 1500/1500,
      zh-CN 15/15, de 125/125, da 125/150, sv 115/105, is 125/105, ko
      150/150, nb 115/500. Only hi was right both times; es said "cien y
      cinco" (ungrammatical). 52, 510 and 36 were mostly right.
    * FACTUAL: is "30 to 70" pixels; ja "半径" (radius) for diameter; zh-CN
      dropped the whole "52 visible labels" sentence; es dropped the
      prerequisite's OPS sentence.
    * UI NAMES TRANSLATED in every language: Mask -> máscara / masque /
      Maske / マスク / 口罩 (a face mask) / 마스크; Home -> casa / chez nous /
      zu Hause / 家庭 / घर / hjemmet; Live -> en vivo / ライブ; Time ->
      tiempo / Zeit / Tid.
    * SCRIPT LEAK: ja contains the Hindi word "अंतर्गत".
    * RUN -> race: carrera, course, corsa, corrida, दौरा, 경주, løb, løp.
  This is the HANDOFF rule measured: a model that confidently narrates
  "1500 cell labels" passes every automated gate here. Only reading each
  row against the measurement catches it.
  APPLIED. For each of 13 languages: review_tool build (OPS bundle + count
    bundle, every substring matched exactly once, english_sha256 re-pinned
    to the staged d988500d) -> apply_translation_review.py (staged catalog
    + production/07_mask/review.<lang>.json; speech_text regenerated and
    gated for the 7 spoken languages) -> row diff against the pre-edit
    snapshot. Every language: exactly prerequisite, objectives[0] and scenes
    1/4/6/7/8 changed, no other key. git: 13 files x 8 lines (7 rows + pin).
    Cross-check: 13/13 staged catalogs equal their review (narration,
    prerequisite, objectives, speech_text present, production copy
    identical). The two pending bundles are deleted now that they are
    applied. Next: 50 voices on the GPU lock (no --force).
  VOICES RUNNING. The existing 50 m4a + 50 caption files (106 MB) were
    copied to production/07_mask/pre-2026-09-15/ first. render_all_voices.py
    --lessons 7 --device cuda took the GPU lock at 07:43:35; the first tracks
    re-rendered at ~16 s each (en/af_heart 336.7 s of narration). Voice
    matrix: en 24, es 3, fr 1, hi 4, it 2, pt-BR 3, ja 5, zh-CN 8 = 50.
    Scene 1's image (the poster source) is unchanged, capture sha
    46330261..., so poster.jpg stays valid. verify_staged_audio writes
    audio-checks.<'+'.join(languages)>.json and check_completed_matrix
    reconciles EVERY such file, so re-run it with the 09-11 order (en es fr
    hi it pt-BR ja zh-CN): a different order leaves a stale report behind
    that fails reconcile_audio.

05_HOME -- OWED, NOT STARTED (the brief made it conditional on everything
else being done; 07_mask was still rendering). Measured facts, so it can be
picked up cold:
  * LIVE HOME BANDS (spacr.qt.app.home_bands(APPS) at this branch):
      Core   6: Mask, Measure, Annotate, Classify, Map Barcodes, Regression
      Data   7: Import [foreign], Embeddings [embeddings], Run Compare,
                Experiment Design, Power / Design, Dose-Response, QC
      Tools  5: Make Masks, Align & Stitch, Image UMAP, Gate Editor, Graph Builder
      Assays 4: Plaque Assay, Recruitment, Invasion Assay, Replication Assay
    Only Data changed: Embeddings joined it in 7f0f74ac7 ("386's tile was in
    the registry and in none of the tables that draw it"). Core, Tools and
    Assays still match scenes 2, 4 and 5 key for key.
  * SCENE 3 today: visual "02_data  (6)", from captures/home at 04b23431f
    on 1.5.0.4, before the tile existed; focus_modules = [foreign,
    run_compare, experiment_design, power, dose_response, qc_dashboard];
    related = [36_import, 35_converter, 31_external_masks]; narration "Data
    brings together Import, Run Compare, Experiment Design, Power and
    Design, Dose Response, and quality control. ..."
  * CHANGES OWED in lessons/05_home.json scene 3:
      narration  "Data brings together Import, Embeddings, Run Compare, ..."
      focus_modules  insert "embeddings" second (band order)
      related_lessons  add "77_embeddings"
      visual  take the key from the NEW capture's frames.json; it encodes
              the tile count and will very likely become "02_data  (7)"
  * SEQUENCE (as 07_mask; Home needs no GPU to capture):
      capture_refresh.py --module home --stage $ST --capture-name home_2026-09-xx --platform xcb
        (fixed xvfb display, never -a; view 02_data for 7 tiles and the 1.5.0.7 footer)
      edit scene 3 as above -> stage_lesson.py lessons/05_home.json --capture-module home_2026-09-xx
      [GPU] translate_refresh.py --lessons 05_home --device cuda
      review: only scene 3 narration changed (plus the pin). Insert the
        localized "Embeddings" into each reviewed row by hand; the 07_mask
        drafts translated every UI name, so do not take a draft row
      apply_translation_review.py x13 -> row diff: exactly scenes[2]
      [GPU] SPACR_TUTORIAL_WORKSPACE=$ST render_all_voices.py --lessons 5 --device cuda
      verify_staged_audio (09-xx report language order), 14 browser checks,
      render_visual_master, check_completed_matrix, stage_one rendition,
      verify_staged_lesson --web-rendition, build() + verify_release_candidate.
  VOICE RENDER STOPPED AT TRACK 22/50 (07:50:27, exit 1): "Decoded true peak
    -0.8 dBFS exceeds -1.0 dBFS: .../07_mask/audio/en/bm_fable.m4a". This is
    the renderer's release gate. It masters with a three-step cascade
    (loudnorm TP -3; then TP -5 + alimiter 0.7; then TP -5 + alimiter 0.5)
    and keeps the first result that decodes at <= -1.0 dBFS. On the NEW
    English narration, bm_fable still decoded at -0.8 after the strongest
    step. (Its 09-11 render of the old narration passed.)
    STATE LEFT BEHIND, measured: the gate runs before os.replace, so
    bm_fable still holds its intact 09-11 m4a/json (fingerprint acf89009,
    old narration, media hash unchanged). 21 tracks carry the new narration
    (en af_* x8, am_* x9, bf_* x4, bm_daniel); 29 are still old. That mix
    must NOT reach verify_staged_audio, the browser checks or the matrix.
    A plain re-run retries bm_fable (fingerprint mismatch) and skips the 21.
  PEAK REPAIR APPLIED -- the renderer's own narrow mechanism, used for this
    same voice once before. render_all_voices.mastering_config() already
    pins "measured, narrowly scoped" repairs by (lesson, language, voice):
    18_motility/en/bf_isabella (-0.5 dBFS) and 12_map_barcodes/en/bm_fable
    (-0.6 dBFS, 455c4d298) each append the strongest filter plus
    volume=-2dB. Added ("07_mask", "en", "bm_fable") with the same chain and
    the measured -0.8 dBFS in its comment. The -1 dBFS gate still decides.
    Applied identically to BOTH copies (workspace tools/render_all_voices.py
    and tools/tutorials/authoring/tools/render_all_voices.py; they were
    identical before at 40a3ae41 and are identical after at 0696cd40, because
    snapshot_sources.py copies the workspace over authoring/).
    SCOPE MEASURED by loading mastering_config from the before and after
    sources: only 07_mask/en/bm_fable changes (3 -> 4 filters); 07_mask
    bm_daniel, bm_george, es/ef_dora, Map's and Motility's repaired tracks,
    13_regression/en/bm_fable and 05_home/en/af_heart are identical. The
    chain lives in that one track's fingerprint, and
    assert_frozen_sources_current hashes pronunciation and cadence sources,
    not the renderer, so no other track in any lesson goes stale.
    Pre-edit copy: <scratchpad>/358_mask/render_all_voices.pre-bm_fable.py.
    RENDER RELAUNCHED on the GPU lock (no --force): the 21 finished tracks
    should skip, and bm_fable plus the 28 others render.
  REPAIR RESULT. The retry skipped the 21 finished tracks and rendered
    en/bm_fable first (363.6 s of narration), which passed the gate. Measured
    independently with ffmpeg ebur128 (peak=true):
      en/bm_fable   true peak -1.2 dBFS, integrated -17.8 LUFS
                    (4 mastering filters, last ...,volume=-2dB; new narration)
      en/bm_daniel  true peak -2.0 dBFS, integrated -16.9 LUFS (no repair)
    So the repair clears the -1 dBFS gate by 0.2 dB, and that one voice is
    ~0.9 LU quieter than its neighbour. The trade is the same one Map's
    bm_fable made. For a listening review: that is why 07_mask/en/bm_fable
    sounds slightly softer.
  ALL 50 VOICES RENDERED: "complete rendered=29 skipped=21", exit 0 at
    08:00:06 (lock 07:53:03, ~7 min on the GPU). With the stopped first run
    (21 tracks) that makes 50 tracks on the reviewed narration. Running the
    post-voice chain now: narration check across all 50, full decode, 14
    browser cases, master, matrix, 1440p rendition, web-rendition playback.
  POST-VOICE CHAIN, STEPS 0-4 PASSED (<scratchpad>/358_mask/post_voice_chain.sh):
    0 narration: 50/50 tracks' render_inputs carry the reviewed staged
      narration; none stale from the stopped run.
    1 verify_staged_audio.py (en es fr hi it pt-BR ja zh-CN): passed, 50
      tracks fully decoded, 0 errors. It rewrote the single 09-11-named
      report; no second report was left behind.
    2 verify_staged_lesson.py: 14/14 browser cases (first voice of the 8
      spoken languages + da de is ko nb sv captions).
    3 render_visual_master.py: master c9135f6f..., 336.700 s, 10,101
      frames, 20,010,129 bytes (old 3c34e9f7..., 335.000 s, 10,050 frames).
      The +1.7 s is the longer spoken counts.
    4 check_completed_matrix.py: "07_mask: 50 final tracks, 14 browser
      checks, matching sources".
    Steps 5-6 (1440p rendition, web-rendition playback) running; then the
    release candidate.
  POST-VOICE CHAIN STEPS 5-6 PASSED (chain exit 0 at 08:06:33):
    5 stage_one: 1440p rendition accepted, 6,139,064 bytes, 10,101 frames,
      every presentation time matching, full decode passed (92.6 s);
      master c9135f6f..., rendition bd486083.... The 09-11 copy
      is in production/07_mask/pre-2026-09-15/web-renditions/.
    6 verify_staged_lesson.py --web-rendition: passed, 16 scenes; clocks audio
      105.465 / video 105.306 / expected 105.464 s (0.157 s apart, limit 0.5).
  07_MASK STATE: captured (fresh copy), composed, English staged, 13
    reviews applied, 50 voices, audio decoded, 14 browser checks, master,
    matrix, 1440p rendition and web playback all PASSED. Release candidate
    build + verify_release_candidate running.

RELEASE CANDIDATE WITH BOTH REFRESHED LESSONS: release-candidate-8738b_pd.
  Built 08:07:57 with the recorded build() call (model_promotions=set(MODELS),
  barcode_promotion=True). Its internal library check passed first. 76 ready,
  77 routes, 3,800 tracks, 7,875 files, 365,792,357 web bytes (ceiling
  734,003,200), Coming soon = 71_investigate_hit, release_hold true,
  published false. It supersedes tytdds1k, which had only 13_regression.
  DIFF AGAINST zveusu2j (the 09-13 candidate), measured path by path and then
  by parsing every catalog: 121 paths differ, all accounted for.
    07_mask        102: 50 m4a + 50 timing json + 1440p web copy + 4K master.
                   The poster is unchanged; scene 1's image did not change.
    13_regression    3: web copy, 4K master, poster (as in tytdds1k)
    14 catalogs + lesson_catalog.js: ONLY the 07_mask entry differs in each;
                   lesson order and every other top-level key unchanged
    module_navigation.js: only source_commit
  No other lesson's bytes or catalog text moved. verify_release_candidate.py
  is running on it.
  VERIFIED: verify_release_candidate.py release-candidate-8738b_pd exit 0
    (08:07:57 -> 08:12:14). 90 PASS: 14 Coming-soon language screens + 76
    candidate playback cases, including 07_mask, 13_regression and
    01_pypi_github (whose clock check failed twice on 09-11 under load).

--------------------------------------------------------------------------------
2026-09-15 08:13 UTC -- CLAUDE SESSION SUMMARY: WHAT WAS DONE, WHAT IS OWED
--------------------------------------------------------------------------------

BRANCH wip/358-lessons in /mnt/firecuda2/Claude/repo/spacr-358, NOT pushed.
From 63f1d502b (nightly at 03:42). nightly was rebased under it at 04:17. The
first 10 commits were replay-tested onto the rebased nightly and applied
cleanly; later commits touch only tools/tutorials and this file. For the
maintainer:
    git tag 358-capture-base-63f1d502b 63f1d502b
    git rebase --onto nightly 63f1d502b wip/358-lessons
The commits touch only: this file, tests/conftest.py,
tests/test_the_chaining_pin_store_is_sandboxed.py,
tools/tutorials/{capture_refresh.py, compose_mask_capture.py,
lessons/07_mask.json, lessons/reviews/07_mask.*.json (13),
authoring/tools/render_all_voices.py}. None is on tonight's do-not-touch list.

DONE
  1 Capture hygiene. Root cause: chaining pins in XDG STATE storage
    (~/.local/state/spacr/chaining/pins.json), covered by neither the tests'
    nor the capture's XDG_CONFIG_HOME. Fixed in tests/conftest.py and
    capture_refresh.py, plus 4 regression tests. Before: the stand-in store
    received a /tmp/pytest mask pin; after: 0 files; 245 tests passed.
  2 13_regression. Re-recorded on 1.5.0.7 with today's labels; 21 new
    frames, master, matrix, 1440p copy, web playback.
  3 07_mask. Fresh-copy GPU preview capture: 105 -> 52 at 510 px -> 105; 36
    after 30 -> 60. The recount matches and the arrays are byte-identical
    across two recordings. Composed; English changed on exactly 5 lines; 13
    reviews applied (7 rows each, drafts read row by row); 50 voices (one
    pinned -2 dB peak repair); decode, 14 browser checks, master, matrix,
    rendition, web playback.
  4 Candidate 8738b_pd holds both refreshed lessons; 90 PASS; the change is
    confined to those two lessons.

FOUND ON THE WAY (for owners of app code; not patched here)
  * settings_pack.PACK_FILES["mask"] tries the run-saved gen_mask_settings.csv
    before the shipped gen_masks_settings.csv. Load test data on a cached
    example therefore returns the user's old run settings, including a
    literal "<src>". (app_screen.py was another agent's tonight.)
  * spacr/core.py's batch overlay loop still lists merged/ unfiltered, so
    the plane-layout JSON is still plotted as an image. Scene 10 stays true.
  * The maintainer's real ~/.local/state/spacr/chaining/pins.json still pins
    Mask and Timelapse to deleted pytest folders. Safe to delete those two
    entries; the file is his.
  * The machine-translation drafts are not usable unread: 11 of 13 languages
    got 105 wrong in at least one scene (150, 125, 1500, 15, ...); all
    translated UI names.

OWED
  * 05_home scene 3 (Embeddings in the Data band). Not started; the brief
    made it conditional. The exact edits and the sequence are in the
    "05_HOME -- OWED" note above.
  * Publication: the release hold, the Hugging Face media revision, Pages
    from main, and the Home/Performance question. All the maintainer's.
  * Native-speaker and listening review of the 07_mask rows (editorial
    only). A listener will hear 07_mask/en/bm_fable about 0.9 LU softer.
  * 09_annotate and the Classify lessons were not re-audited.

RE-RUN COMMANDS FOR WHAT FAILED TONIGHT (all later succeeded)
  * Mask preview capture on a cached example: remove any run-saved
    settings/gen_mask_settings.* first, or use a fresh copy
    (refresh_2026-09-15_cpu), and run under run_capture_guarded.sh (fixed
    xvfb display, X11-break watchdog), never `xvfb-run -a` with a 2 h
    timeout.
  * Voice render stopped on a true peak: pin the track in
    render_all_voices.mastering_config() (as ae4f80b00), in BOTH copies,
    then re-run without --force.
  * Library checkpoint: --held 71_investigate_hit, not all four HELD ids.

--------------------------------------------------------------------------------
2026-09-15 (08:20 UTC) -- 07_MASK SCENE 10 NOW NARRATES A BUG THAT IS FIXED
--------------------------------------------------------------------------------

The batch overlay loop in spacr/core.py (preprocess_generate_masks, plot=True)
no longer plots merged/'s sidecars: `_overlay_candidates` returns only the
`.npy` stacks, the same set the test-mode example count already counted, so
.spacr_plane_layout.json is never handed to plot_image_mask_overlay as an
image. Test: tests/test_the_overlay_loop_plots_only_image_stacks.py (red
against the old listing, green after).

OWED FOR THE LESSON: scene 10 and its "STILL fails" prerequisite describe the
old failure, so they are now inaccurate. Re-capture scene 10's overlay step on
a build with this fix, change the narration to what the plots show, and run
the 07_mask translation -> voices -> master -> rendition -> candidate chain
again for that scene (the resume sequence above covers the commands).

--------------------------------------------------------------------------------
2026-09-15 -- THE MAINTAINER'S REAL PIN FILE IS CLEANED, AND THE HOLD IS LIFTED
--------------------------------------------------------------------------------

Asked with the question tool. (1) "Remove the two entries": the maintainer's
real ~/.local/state/spacr/chaining/pins.json had Mask's and Timelapse's `src`
pinned to deleted pytest folders
(/tmp/pytest-of-olafsson/pytest-2295/test_head_loaddata0/plate1 and
.../pytest-3431/test_load_demo_generates_data_4/scratch/timelapse), left there
by tests before the conftest sandbox. Only those two `src` keys were removed;
the other eight modules' pins are untouched, and a byte copy of the old file is
kept beside it as pins.json.bak-2026-09-15. (2) "Publish now": the release hold
on candidate release-candidate-8738b_pd is lifted by the maintainer; the
publication steps are recorded in the next section when they land.
2026-09-15 -- PUBLICATION OF release-candidate-8738b_pd: THE PLAN (WRITTEN BEFORE ACTING)
--------------------------------------------------------------------------------

AUTHORITY. The maintainer answered "Publish now" through the question tool on
2026-09-15. That approves three things: lifting the release hold, uploading a
new versioned media revision to Hugging Face, and deploying the tutorial pages.
Worktree /mnt/firecuda2/Claude/repo/spacr-358pub, branch wip/358-publish, from
nightly a5361a4f2. Nothing is pushed.

WHAT "PUBLISH" MEANS IN THIS REPO, READ FROM THE CODE
  * TWO HOMES FOR MEDIA (authoring/tools/publish_tutorials.py docstring).
    GitHub Pages serves docs/source/_extra/tutorials: the player, catalogs,
    posters and 1440p video. The Hugging Face DATASET
    einarolafsson/spacr-tutorials (public) serves the 50 narration voices,
    their timing JSON and the 4K masters, under <lesson>/audio/<lang>/<voice>.
    {m4a,json} and <lesson>/video/<lesson>_silent.mp4. The candidate's
    media_host/ has exactly that layout.
  * THE HOLD IS A FLAG PLUS A SEPARATION, NOT A FILE ON ITS OWN.
    build_release_candidate.py writes release_hold true, uploaded false and
    published false into every candidate's release-manifest.json.
    refresh_candidate_player() and checkpoint_release_candidate.py refuse any
    candidate that is not held. The repo checkpoint
    (tools/tutorials/release_candidate/checkpoint.json and its manifest) says
    the same, and test_release_candidate_routes.py pins
    `published is False and release_hold is True`. The separation: the
    candidate's index.html points data-audio-root and data-video4k-root at
    ../media_host (an offline preview; the builder's comment says "Deployment
    must explicitly replace them with an immutable uploaded media revision
    after the hold"). Meanwhile the public tree keeps the 09-11 catalogs (71
    ready, 6 Coming soon, 498 scenes) and resolve/main. test_public_coming_soon.py
    and verify_tutorial_live.py pin that public state.
  * NO SCRIPT DEPLOYS A CANDIDATE. publish_tutorials.py is the legacy
    publisher. It reads authoring/web and authoring/production, and it uploads
    with `hf upload` to main with no revision. README.md says not to run it
    against the old authoring catalogs. So its upload command is used here
    with an explicit new --revision against the candidate's media_host/, and
    nothing is uploaded to main.
  * WHY NOT MAIN. The live site reads resolve/main. Measured against main at
    003f4c8e88: 3,571 of the candidate's LFS media differ in bytes (every
    lesson's 50 voices and master except 33/34), 404 files are new (74, 75,
    76, 77), and the 3,600 timing JSON are plain git files. Uploading to main
    would put new narration under the OLD live catalogs and captions before
    Pages changes. A new revision cannot disturb the live site.

THE STEPS, IN ORDER
  1 verify_release_candidate.py on 8738b_pd again, on a quiet machine.
  2 Preflight: the token is fine-grained with repo.write on einarolafsson
    (whoami, read only). The dataset has one branch (main) and no tags.
  3 Create branch `candidate-8738b_pd` from main 003f4c8e88
    (exist_ok=False, so an existing name is refused rather than reused). Run
    `hf upload einarolafsson/spacr-tutorials <candidate>/media_host .
    --repo-type dataset --revision candidate-8738b_pd`.
  4 Read back EVERY one of the 7,676 media files at the resulting commit:
    first the tree metadata (size, LFS sha256 or git blob id), then a
    streamed download of each file's bytes from resolve/<commit>, hashed
    against release-manifest.json.
  5 Tag the commit `tutorials-2026-09-15-8738b_pd` (exist_ok=False). The site
    is pinned to the 40-character COMMIT, not to the tag or branch, because
    only a commit id cannot be moved.
  6 Pages tree: copy the candidate's web/ into docs/source/_extra/tutorials,
    byte-checked against the manifest. index.html changes only in the two
    media roots (resolve/<commit>), the restored styles.css key, new
    cache keys for the two changed scripts (lesson_catalog.js and
    module_navigation.js), and the static counts (76 available / 77 total).
    The 540 tracked legacy audio files under the docs tree are left alone:
    their removal waits on the maintainer (325, 2026-09-09).
  7 Playback against the PUBLISHED media: the same browser checks, with only
    localhost and the Hugging Face hosts allowed, and the audio hashes
    compared with the manifest.
  8 Lift the hold in the repository: move the pins that describe the held
    public state (NARRATION_HOST, test_public_coming_soon.py,
    verify_tutorial_live.py constants, the checkpoint's hold flags), then
    commit on wip/358-publish with explicit paths.

PAGES DEPLOY ONLY FROM MAIN. Read from .github/workflows/docs.yml: a push to
main runs configure-pages, upload-pages-artifact and deploy-pages. A push to
nightly builds and stops, so the Pages-ready tree on this branch reaches
https://einarolafsson.github.io/spacr/tutorials/ only when it is merged to main.
(The only other route is a deliberately DISPATCHED nightly run; nothing here
dispatches or pushes.) The 1.5.0.8 merge to main is gated on CI and is not
done here.

--------------------------------------------------------------------------------
2026-09-15 -- PUBLISHED: MEDIA REVISION UPLOADED AND READ BACK, PAGES TREE READY,
HOLD LIFTED IN THE REPOSITORY (branch wip/358-publish, not pushed)
--------------------------------------------------------------------------------

THE MAINTAINER'S DECISIONS, VERBATIM (all through the question tool, 2026-09-15)
  0 Publication of release-candidate-8738b_pd: "Publish now".
  1 Asked "Should I finish those local steps?" (record the hold lift, run the
    remaining tests, write the dated note in 358, commit on the branch;
    nothing pushed; the site changes only with the main merge he approves
    separately). Answer: "Finish the local steps (Recommended)".
  2 PyPI's pronunciation: narration "pype" (the 08-28 rule) or the 08-26 test
    that requires "pypie". Answer: "\"pype\" (Recommended)". Keep the narration
    and update the old test to the 08-28 rule. Nothing is re-rendered.

WHAT WAS PUBLISHED, WHERE
  Media host  https://huggingface.co/datasets/einarolafsson/spacr-tutorials (public)
    branch  candidate-8738b_pd, created from main 003f4c8e88 (exist_ok=False)
    commit  e35b2c12f00260629d9d5353b86dcd4577b9b617, 12:22:58 UTC,
            "Tutorial media for release candidate release-candidate-8738b_pd"
    tag     tutorials-2026-09-15-8738b_pd. It resolves to e35b2c12
            (dataset_info(revision=tag).sha, and x-repo-commit on resolve/).
    main    unchanged at 003f4c8e88, so the live site's resolve/main is untouched
            and remains the rollback revision.
    Pinned media root (narration and 4K):
      https://huggingface.co/datasets/einarolafsson/spacr-tutorials/resolve/e35b2c12f00260629d9d5353b86dcd4577b9b617
  Pages tree  docs/source/_extra/tutorials on this branch. It reaches
    https://einarolafsson.github.io/spacr/tutorials/ ONLY WITH THE MERGE TO
    MAIN. docs.yml runs configure-pages and deploy-pages on a push to main.
    A push to nightly builds and stops. The one other route is a deliberately
    dispatched nightly run, which nothing here uses.

VERIFICATION, IN ORDER
  * verify_release_candidate.py 8738b_pd, quiet machine (load ~1.4): exit 0,
    90 PASS (14 Coming-soon languages, 76 playback cases), largest video-clock
    gap 0.199 s, 11:44 -> 11:48 UTC. check_placeholder_mutations.py: both
    guards OBSERVED RED, baseline passed before and after.
    checkpoint_release_candidate.py: 203 files, now bound to 8738b_pd
    (manifest 62aa93f4...).
  * Upload with `hf upload ... --revision candidate-8738b_pd`, the legacy
    publisher's own command pointed at the candidate's media_host/ (3,876
    LFS files processed; hf reported "Removing 101 file(s) from commit that
    have not changed").
  * READ-BACK OF EVERY FILE at commit e35b2c12: 7,676 of 7,676 matched by tree
    metadata (size plus LFS sha256, or git blob id for the JSON sidecars),
    and 7,676 of 7,676 by streaming each file's BYTES from resolve/<commit>
    and hashing them against release-manifest.json: 7,299,473,261 bytes in
    2,109 s, 0 failures. The revision also holds 103 files that are not in
    the candidate, inherited from main: 71_investigate_hit's 101 old media
    files, README.md and .gitattributes. The Coming-soon screen requests none.
  * TOOL BUG, FIXED. The upload run exited 1 at "Tag does not name the
    verified commit" AFTER the read-back passed and the tag was created.
    list_repo_refs reports an annotated tag's OBJECT id (8b402f85), not the
    commit it resolves to. The check now compares
    dataset_info(revision=tag).sha. The receipt was written by the read-only
    `resume-receipt` subcommand from that run's logged read-back line; it
    creates nothing and refuses unless the tag already resolves to the
    read-back commit and the branch is exactly one commit on main.
    An earlier attempt to add a second tag-creation path was refused by the
    permission system ("[Create Public Surface]") and was not retried.
  * PAGES TREE (publish_release_candidate.py pages): 199 files copied from the
    candidate and byte-checked against its manifest (76_ops and 77_embeddings
    are new). index.html differs from the candidate's only in: both media
    roots pinned to the commit; lesson_catalog.js and module_navigation.js
    keyed 20260915-8738b_pd, because their bytes changed; app_v2.js,
    styles.css and voice_catalog.js keeping their keys, because their bytes
    did not; and the static counts, now 76 available of 77. Left in place:
    71_investigate_hit's old poster and video, and the 540 tracked legacy
    audio files (325, pending the maintainer). docs_media_budget.py: published
    349.9 MiB of 700 (was 129.6), narration served from the pinned commit.
  * PLAYBACK AGAINST THE PUBLISHED MEDIA:
    verify_release_candidate.py <candidate> --published docs/source/_extra/tutorials
    exit 0, 90 PASS. The Pages tree is served locally as Pages would serve it;
    every one of the 152 media requests went to huggingface.co at the pinned
    commit, and anything else would have failed the run. The loaded Heart
    audio hashes equal the manifest, and the largest clock gap is 0.211 s.
    Report: release_candidate/published-media-browser-checks.json.
  * HOLD LIFTED (publish_release_candidate.py record, which refuses unless the
    receipt, the published playback report, the checkpoint and the Pages index
    all describe the same candidate and tree). checkpoint.json now reads
    release_hold false, media_uploaded true, pages_tree_ready true, published
    false (Pages is not deployed), plus media_revision {branch, tag, commit,
    root}. release-manifest.json is left exactly as built, still flagged held,
    because the browser evidence hashes it.

PINS OF THE HELD PUBLIC STATE, MOVED WITH IT
  * NARRATION_HOST (tools/docs_media_budget.py) and AUDIO_ROOT
    (tools/sample_tutorial_frames.py) now use the pinned commit.
  * verify_tutorial_live.py expects key 20260915-8738b_pd and 975 scenes.
    spacr/resources/tutorial_index.json was regenerated (71 modules); it had
    been stale since 5da4a6a95.
  * test_public_coming_soon.py pins the receipt's commit, one Coming soon
    screen and 76 ready lessons. A new routes test pins how the hold is lifted.
  * Decision 2: test_every_spoken_pypi_is_the_single_word_pypie ->
    test_every_spoken_pypi_is_the_reviewed_single_syllable_pype.
    test_spoken_pypi_is_exactly_one_continuous_pypie_token (the same 08-26 pin
    in test_tutorial_catalog_i18n.py) became
    test_spoken_pypi_is_the_reviewed_pype_form_in_every_spoken_locale. Both
    read pronunciation.py (2026-08-28-pype-v11). The English catalog carries
    no speech_text for these scenes, so English is gated in the sidecars;
    every translated narration language must carry the reviewed form.
  * Lesson 01's nightly checks: the 09-11 recording has eight scenes and
    names nightly only in scene 4, so both tests derive the scene list from
    English.
  * NOT A MAINTAINER DECISION, FLAGGED: test_localized_navigation_chrome_*
    pinned pre-refresh copy. That is the Feature Dictionary titles
    ("Diccionario de características" ...) and the two English repairs
    ("appropriate validation controls", "Import third-party images"). The
    approved candidate carries the 09-09 reviewed rewrites of those lessons:
    titles keep the literal screen name "Feature Dictionary" and add a
    translated subtitle, each equal to lessons/reviews/62_feature_dictionary.*;
    and 16_activation / 31_external_masks have new English equal to their
    lesson sources. The pins follow the reviewed sources. The retired
    mistranslation "Turn third-party images" is still banned.
  * Evidence re-bound to 8738b_pd with the lessons' own tools:
    checkpoint_models.py, checkpoint_ops.py and checkpoint_embeddings.py
    rewrote the four 2026-09-12_*_final_checks.json files.
    MAP IS NOT RE-BOUND. checkpoint_map.py needs checks/map-preservation-checks.json.
    check_map_preservation.py produces that report, but it proves a MAP-ONLY
    promotion and refuses a candidate that also changed 07/13. Map's 103
    media and web records are byte-identical between zveusu2j and 8738b_pd,
    but no honest preservation proof exists for this pair.
    tools/tutorials/tests/test_map_candidate.py therefore stays red: its
    evidence names zveusu2j. That suite is not in CI.

TESTS
  tests/test_tutorial_catalog_i18n.py + test_tutorial_release_audit.py +
  test_docs_media_budget.py: 40 passed. tools/tutorials/tests (20 files that
  read the changed trees): 265 passed, 1 failed (test_map_candidate, above).
  tests/qt/test_help_menu_tutorial_link.py: 8 passed.
  build_tutorial_index.py --check: up to date. The earlier refused re-run of
  the nightly test ("[Out-of-Place Publication]") ran after decision 1 and passes.

STILL OWED
  * THE PAGES DEPLOY, with the merge to main (the 1.5.0.8 merge, gated on
    CI). After it deploys, run tools/verify_tutorial_live.py --browser
    against the live site, and only then set published true.
  * 07_mask SCENE 10 RE-CAPTURE. The overlay-loop bug that scene 10 and its
    "STILL fails" prerequisite narrate is fixed in spacr/core.py, so both are
    now inaccurate. Re-capture, re-narrate, translation -> voices -> master ->
    rendition -> new candidate -> a NEW media revision (never reuse
    candidate-8738b_pd or this tag).
  * NATIVE-SPEAKER REVIEW of the new 07_mask translations (13 languages, 7 rows
    each), plus listening review (07_mask/en/bm_fable is ~0.9 LU softer).
  * Map evidence re-binding needs a preservation proof that allows other
    lessons' reviewed refreshes.
  * 05_home scene 3 (Embeddings in the Data band) and the Home/Performance
    question are unchanged from the notes above.
  * Rollback: resolve/main (003f4c8e88) and the pre-merge docs tree remain
    intact; reverting the Pages-tree commit restores the old site.

--------------------------------------------------------------------------------
2026-09-19 -- 09_ANNOTATE AND CLASSIFY RE-AUDITED; 05_HOME SCENE 3 DONE ON CPU;
MAP'S PRESERVATION PROOF EXISTS AND test_map_candidate IS GREEN
--------------------------------------------------------------------------------

Branch home/358-non-gpu, worktree
/tmp/claude-1000/-mnt-firecuda2-Claude-repo/ca197812-8400-4175-ac04-477538c4a051/scratchpad/wt-358-non-gpu,
from origin/nightly, 1.5.0.8. Nothing published, nothing uploaded, no GPU job
run. THE BASE MOVED TWICE while this was being written and the first draft of
this line named fa51b2e88, which was already wrong when it was committed: the
branch was rebased onto 2c072509e, then onto 5800dd4bf, and finally onto
e642b7d8b before it was pushed, which is where these commits sit. See the review note at the end of
this section for what landed in between and why none of it moves the audit.

Every screen fact below was read off a widget built from THIS worktree under an
isolated HOME/XDG_*, not from the code. The first four probes of the session
imported `spacr` from /mnt/firecuda2/Claude/repo/spacr instead -- `sys.path[0]`
is the SCRIPT's directory, not the working directory -- and were re-run with
PYTHONPATH pinned. Section 3f of the handoff is about exactly this and it still
caught me; the give-away was `spacr.qt.screens.annotate.__file__` in the probe's
own output, which is why it is printed there now.

WHAT 364 DID TO ANNOTATE TODAY, MEASURED ON BOTH SIDES
------------------------------------------------------

`8619dcb9c` (today, 17:04) renamed the factory's `img_size` to `crop_size`. The
same Annotate settings dialog, handed the shipped example
`annotate_settings.csv` (which carries BOTH `image_size,224` and `img_size,200`):

    at 8619dcb9c^   row label "Image size (px)"   value after Load test data: 224
    at fa51b2e88    row label "Crop size (px)"    value after Load test data: 200

`_ALSO_SPELT` accepts `img_size` and deliberately refuses `image_size`, because
`image_size` is the MODEL's input crop and is still live. So the example used to
draw its cells at the model's resolution and now draws them at 200.

  * 09_ANNOTATE SCENE 5 WAS WRONG AS OF TODAY and is FIXED in the source:
    "depends on the window and image size" -> "the window and the crop size".
    One sentence; it is the only narration change 09_annotate needs.
  * 09_ANNOTATE'S SETTINGS FRAMES ARE STALE: scene 6 (06_annotation_settings)
    and scene 13 (11_display_colour_settings) show "Image size (px)".
  * 09_ANNOTATE'S GRID FRAMES ARE STALE FOR A SECOND REASON: they were recorded
    with cells drawn at 224 px and the same route now draws them at 200, so the
    page fill differs. No NARRATED number is falsified -- scene 2's 2,341 crops
    is a dataset count and scene 5 quotes no per-page figure -- so this costs
    frames, not arithmetic.
  * 10_CLASSIFY_CV SCENE 7 IS NOT AFFECTED, and this is the one that looks like
    a hit and is not. "image size one hundred twenty-eight" is `image_size`,
    which survives the rename with its label "Image size" unchanged and sits in
    the "Computer Vision Data Source" category. `crop_size`/`img_size` is in NO
    settings category at all -- it exists only on Annotate's own dialog -- so it
    cannot appear on a Classify form. Two measurements disagreed and the lesson
    won.

09_ANNOTATE, EVERYTHING ELSE: NARRATION CURRENT, FRAMES FOUR VERSIONS BEHIND
----------------------------------------------------------------------------

Its capture is `captures/annotate_readable`, provenance version 1.5.0.4, commit
ed93b0e2a. The app is 1.5.0.8. Every frame carries a 1.5.0.4 footer.

Driving `AnnotateScreen()` at HEAD, every control the lesson names is present
and unrenamed: Open source..., Settings, Back, Next, Class counts, Coverage,
Retrain, Suggest..., Rounds, Train..., Generate annotation database...,
Console + AI, and the Annotator Agreement FoldButton on the masthead
(`FOLDED_APPS = ("agreement",)`, so scene 15 is complete). Train...'s menu is
still the two routes scene 16 narrates -- "On the images (CNN / Transformer)..."
and "On the measured features (XGBoost)...". The Load/Stream chooser scenes 2-3
describe is still there. `generate_annotation_dataset` still lives in
`spacr/annotation_dataset.py`, and AI still points at Preferences
("Preferences -> AI -> Providers..."). The keyboard legend has grown two entries
the narration does not enumerate (Space skip, Backspace back) and scene 9 says
"navigation", which covers them.

Diffing every user-visible string in `annotate.py` from the recording commit to
HEAD leaves exactly one label change, the `Crop size (px)` above.

  * NOT NAMED ANYWHERE IN THE LESSON, and this is a gap in the lesson rather
    than a staleness: Auto-annotate..., View in database, Annotate page,
    Clear page, Clear column, Skip to last annotated. Auto-annotate, View in
    database, Annotate page and Clear page all landed on 2026-08-06, a month
    BEFORE the recording, so nobody broke this -- it was authored short.
    FOR THE MAINTAINER: naming them costs new scenes, and a new scene changes
    the scene count, which `test_every_translated_tutorial_catalog_has_the_
    english_structure` pins across all fourteen catalogs. That is the same
    forty-translated-scene-sets bill the 2026-09-08 note prices out, for
    controls that mostly have their own lessons already (34_database for View
    in database). NOT DONE HERE, deliberately, and it is a decision rather than
    an oversight.

THE CLASSIFY LESSONS: NOTHING STALE IN THE NARRATION, FRAMES TWO VERSIONS BEHIND
--------------------------------------------------------------------------------

10_classify_cv (captures/classify_canonical_*, 1.5.0.6, 9ed867087),
11_classify_ml (captures/classify_ml_ten_percent_native, 1.5.0.6, 4351af7b9)
and 41_classify (captures/classify_main_*, 1.5.0.6, 9a676d2de). Their
frames are all 1.5.0.6 and the app is 1.5.0.8, so the version footers are stale.

CORRECTED AFTER REVIEW, and the correction is a small finding rather than a
wording fix. This note first said their prerequisites "all say Recorded with
spaCR 1.5.0.6". Two of the three do, in different words: 41_classify says
"Recorded with spaCR 1.5.0.6." and 10_classify_cv says "This recording uses
spaCR 1.5.0.6". 11_CLASSIFY_ML NAMES NO VERSION AT ALL -- grepping its whole
JSON for /1\.5\.[0-9.]+/ returns nothing -- so a reader of the ML lesson is
never told which release its frames are from, while a reader of the other two
is. That is a real gap the audit should have caught and did not. It is NOT
fixed here: adding the sentence changes a prerequisite string, which is a
narration-catalog change in thirteen languages, and it belongs in the same
re-composition pass as the stale frames rather than on its own. Whoever
re-composes 11_classify_ml adds the version sentence to its prerequisite in
the same edit, and lists it in the i18n owed.

Checked against the live tables rather than the code:

  * Every SETTING_LABELS entry the three lessons narrate is unchanged from each
    lesson's own recording commit to HEAD: Classifier family, Generate training
    dataset, Image size, Channel of interest, Classes, Test size, N estimators,
    N jobs, Plot, Model type, Cv group by, Epochs, Batch size, Train channels,
    Init weights, Augment, Random seed, Prune features, Cross validation,
    Batch correction, Top features, N repeats. Across the whole table, from
    those commits to HEAD, NO surviving key's label text moved at all -- the
    only movements are key additions and removals, `img_size` -> `crop_size`
    among them.
  * `classifier_family` still offers exactly "Computer Vision (Torch)" and
    "Tabular Machine Learning" (`_APP_COMBO_OPTIONS["classify_merged"]`).
  * `barcodes` and `Toxoplasma` were retired by the same commit today. Neither
    is named by any Classify or Annotate lesson, and neither is on the Map form
    -- so the recheck the 09-15 note queued for gui_04, gui_11 and gui_20 "if
    the held `barcodes` retirement lands" comes back clean.
  * Classify's fold strip is the five the lessons name, in the order they name
    them: Classifier Evaluation, Explain CV Model, Activation, Training Runs,
    Feature Explorer. 41_classify scene 10 says Feature Explorer opens "from the
    final icon" and it is last in `FOLDED_APPS`. Scene 7 calls it "Explain CV"
    where the button says "Explain CV Model"; that is a spoken short form, not a
    rename, and 05_home scene 7 gives the full name.
  * Classify's own Load test data chooser still offers Load and Stream, filed
    under "Plate Sources & Workflow" (EXAMPLE_DATA_SECTIONS["classify"]).
  * `app_screen.py` changed heavily since the recordings and renamed no chrome:
    no Run, Figures, Console, Essentials or search label moved.

NOTHING IN THE THREE CLASSIFY LESSONS NEEDS RE-NARRATION. They need frames.

05_HOME SCENE 3: DONE, INCLUDING THE CAPTURE, WHICH NEEDED NO GPU
------------------------------------------------------------------

The live Data band, read off the real MainWindow: Import, Embeddings, Run
Compare, Experiment Design, Power / Design, Dose-Response, QC. Seven tiles; the
tab itself now reads "Data  (7)". 45 registered modules, the same number the
09-15 note recorded.

The tab TEXT is the frame key -- `capture_refresh.py` names home frames
`f'{index:02d}_{tabs.tabText(index).lower()}'` -- so the count is not decoration
and "02_data  (6)" could not be edited on its own without breaking staging.

So the capture was re-run. It is pure GUI, no download, no model:

    capture_refresh.py --module home --stage $ST \
      --capture-name home_2026-09-19_bands --platform offscreen --timeout 600

21 seconds, 3.2 MB, eight frames at 3840x2160, provenance version 1.5.0.8
commit fa51b2e88. `02_data  (7).png` shows the seven tiles with Embeddings
second and an `embeddings` tile rect in frames.json.

05_home.json scene 3 is EDITED to match: visual "02_data  (7)", "Embeddings"
added to the narration after "Import", `embeddings` added to focus_modules in
band order, `77_embeddings` added to related_lessons. Proved rather than
asserted:

    stage_lesson.py lessons/05_home.json --capture-module home_2026-09-19_bands \
      --stage $ST --check-only    ->  Validated 05_home: 9 real capture scenes
    the same, against the OLD `home` capture  ->  KeyError: '02_data  (7)'

The second line is the control: the edit is genuinely bound to the new frames.

Because the new capture covers all nine scenes, staging 05_home against it makes
the WHOLE lesson current at 1.5.0.8 (its frames were 1.5.0.4, commit 04b23431f),
not just scene 3.

STILL OWED ON 05_HOME, AND IT IS GPU: scene 3's words changed, so that scene
needs translation -> 50 voices -> master -> rendition. Scenes 1, 2 and 4-9 keep
their narration and need re-composition only. Commands: as 07_mask, with
`--lessons 05_home` for translation and `--lessons 5` for voices.

  * 05_home scene 4 and scene 7 were re-checked against the new frames and are
    accurate: Make Masks does host model training and comparison
    (cellpose_all, curate, model_compare, model_zoo, napari_bridge,
    train_cellpose) and Graph Builder does host Plate Viewer and Small
    Multiples (plate_view, trellis).
  * Scene 8 says "In Mask, the Time and three-D switches ... Live opens the
    preview" and does not mention OPS. It is not wrong -- it never claims the
    row is complete -- and changing it would cost another narration render.
    Left alone, on purpose.

A TRAP THE NEXT READER WILL HIT: OPS IS NOT ON MASK'S FOLD STRIP
-----------------------------------------------------------------

The new `06_mask_host` frame lists exactly ONE fold button on the Mask masthead:
Timelapse. That looks like it contradicts the 09-15 note's "3D, Time, Live, OPS,
AI; confirmed on 1.5.0.7" and the 07_mask scene 1 edit made on its strength.

IT DOES NOT. `mask.PAGE_FOLDS = ("ops",)` and its docstring say so directly: a
page fold "is not on the strip: it is a switch in the actions row, beside Live"
(`b061fef43`, 2026-09-09). The action-row switches are not QAbstractButtons, so
they are absent from a frame's button list by construction. Both statements are
true and the 07_mask edit stands. Recorded because the evidence looks like a
contradiction and is not.

MAP'S PRESERVATION PROOF, AND test_map_candidate IS GREEN
----------------------------------------------------------

The 09-15 note left this: "Map evidence re-binding needs a preservation proof
that allows other lessons' reviewed refreshes", with
tools/tutorials/tests/test_map_candidate.py red because its evidence still named
zveusu2j.

`check_map_preservation.check` could not serve, and refused the pair three times
over -- on the 75->76 promotion counts, on 07_mask's changed media, and on
07_mask's changed catalog record. `check_refresh` is the second proof, beside it
rather than replacing it, and it makes the statement that is actually true of
this pair: MAP is untouched everywhere, and every remaining difference is
attributable to a NAMED lesson or to a shared index file whose own contents are
then compared entry by entry.

What it found on zveusu2j -> 8738b_pd, all measured:

    manifest paths                     identical sets, 7,875 each
    file records that differ           121
    Map's records                      103, of which 0 differ
    catalogs (14)                      only 07_mask's lesson record moved
    web/lesson_catalog.js              parsed; only 07_mask's entry moved
    web/module_navigation.js           parsed; only `source_commit` moved
    media retained byte-identical      7,723

    -> checks/map-preservation-checks.json, passed: true

13_REGRESSION IS WHY THE FIRST DESIGN WAS WRONG. An equality between "lessons
declared refreshed" and "lessons whose catalog record changed" refused the real
pair, because 13_regression was re-captured on 1.5.0.7 with its NARRATION
untouched: its media moved and its prose did not. The check now separates the
two and reports both (`refreshed_in_prose: [07_mask]`,
`refreshed_in_media_only: [13_regression]`). The honest declaration for this
pair is `--refreshed 07_mask 13_regression`, and the first run, with 07_mask
alone, is what caught it:

    ValueError: An unexplained file changed:
        media_host/13_regression/video/13_regression_silent.mp4

The stale-allow-list guard survives in the form that still means something: a
named lesson that moved NOWHERE is refused.

VERIFIED RATHER THAN ASSUMED, because an exit code is a claim about the checks:

  * `validate(include_hosted_media=True, require_browser=True)` really reads the
    bytes: instrumented, 7,878 digest calls, 7.67 GB hashed per candidate.
  * Three negative controls against the REAL pair, each patched in memory so
    nothing on disk moved: a changed Map media record -> "A Map media file
    changed: media_host/12_map_barcodes/audio/en/af_aoede.json"; a changed Map
    lesson record -> "Map itself changed; this is not a preservation proof"; an
    undeclared 08_measure change -> "Lessons changed that the refresh did not
    name: 08_measure". All three fired.
  * 13 unit tests on synthetic candidate trees:
    tools/tutorials/tests/test_map_preservation_across_a_refresh.py.

`checkpoint_map.py` then re-bound Map's evidence to 8738b_pd unchanged -- it
only ever asked for `passed is True` -- and rewrote
evidence/2026-09-13_map_barcodes_{final_checks,preservation}.json.
test_map_candidate.py's second test pinned `retained_media_files == 75 * 103`,
which was the OLD promotion's arithmetic and cannot survive a different proof;
it now pins the relationship instead (retained + changed == all media records,
`map_media_files == 103`, no changed path under 12_map_barcodes) plus the
declared refresh. BOTH ITS TESTS PASS.

ONE FILE WAS WRITTEN OUTSIDE THE REPOSITORY, and it is additive and reversible:
refresh_2026-09-09/release-candidate-8738b_pd/checks/map-preservation-checks.json,
which did not exist before. Delete it to undo. The Home capture added
refresh_2026-09-09/captures/home_2026-09-19_bands/ (3.2 MB, a new directory).
Nothing under release_candidate/ in the repository was regenerated; the manifest
digest still matches 8738b_pd exactly.

TESTS
  tools/tutorials/tests/test_map_candidate.py + the new preservation file +
  test_barcode_promotion.py + test_catalog_preservation.py: 35 passed.
  tests/test_tutorial_release_audit.py + test_tutorial_catalog_i18n.py +
  test_docs_media_budget.py: 40 passed, the same 40 the 09-15 note recorded.

WHAT THIS UNIT DID NOT DO
  * The 07_mask scene 10 re-capture. It is GPU and it is queued, with its exact
    commands and two things the item file did not know, at
    scratchpad/gpu_queue/358-non-gpu-1.md. Read that before starting it:
    `compose_mask_capture.py` currently REFUSES a successful overlay run
    (it asserts `accepted is not False`), and `live_preview.py` has changed in
    seven commits since the 09-15 preview, so the reuse rule is broken again and
    the Live preview must be re-recorded with it. The item file's instruction to
    run captures under `run_capture_guarded.sh` cannot be followed: NO FILE OF
    THAT NAME EXISTS anywhere in the repository or the workspace.
  * Re-composing 09_annotate and the three Classify lessons onto current frames.
    Their narration is correct as of today, so this is capture + compose +
    re-encode with NO translation and NO voice render. Not started.
  * Publishing, in any form.

--------------------------------------------------------------------------------
2026-09-19 -- PUBLISHING, WHICH IS THE MAINTAINER'S. THE EXACT STEPS.
--------------------------------------------------------------------------------

Nothing here was done. It is written out so that it is a sequence to follow
rather than a thing to work out again. It applies to the NEXT candidate, the one
that carries the refreshed 07_mask; candidate-8738b_pd is already uploaded and
its tag must not be reused.

  1. MERGE TO MAIN, which is what deploys Pages. `docs.yml` runs
     configure-pages and deploy-pages on a push to MAIN; a push to nightly
     builds and stops. This is the 1.5.0.8 merge and it is gated on CI.
  2. AFTER it deploys:
         python tools/verify_tutorial_live.py --browser
     against the live site, and ONLY THEN set `published: true` in
     release_candidate/checkpoint.json.
  3. FOR THE NEXT CANDIDATE, a NEW media revision. Never reuse the branch
     `candidate-8738b_pd` or the tag `tutorials-2026-09-15-8738b_pd`:
         hf upload einarolafsson/spacr-tutorials <candidate>/media_host \
            --repo-type dataset --revision candidate-<new id>
     then the read-back, then the tag. `list_repo_refs` reports an annotated
     tag's OBJECT id, not the commit; compare `dataset_info(revision=tag).sha`.
     That bug is already fixed in publish_release_candidate.py.
  4. ROLLBACK, unchanged: `resolve/main` is still 003f4c8e88 and the pre-merge
     docs tree is intact, so reverting the Pages-tree commit restores the old
     site.

ALSO STILL THE MAINTAINER'S, unchanged from 09-15: the native-speaker and
listening review of the 07_mask translations (13 languages, 7 rows each;
07_mask/en/bm_fable is about 0.9 LU softer), and whether Home should teach the
Performance selector again.

--------------------------------------------------------------------------------
2026-09-19 -- AFTER REVIEW, AND RE-CHECKED AGAINST THE SCREENS THAT CHANGED
LATER THE SAME DAY. PUSHED TO nightly.
--------------------------------------------------------------------------------

Rebased onto origin/nightly e642b7d8b and pushed. Everything in this section
was measured on 2026-09-19; the last rebase and the push fell just after
midnight, so git will date these commits 2026-09-20. Between 2c072509e (the base
the audit above was measured on) and e642b7d8b, seventeen commits landed, and
three of them touch screens this audit's lessons name: 419 points 4-6 rewrote
spacr/qt/screens/make_masks.py and spacr/qt/mask_engine.py, and 427 part B
rewrote spacr/qt/preferences.py, sound.py, sound_synth.py, sound_preferences.py
and widgets/ambient.py and added resonance.py. Neither is left to trust:

  * 05_home scene 4 -- "Tools contains Make Masks, Align and Stitch, Image
    UMAP, Gate Editor, and Graph Builder ... Make Masks hosts model training
    and comparison". `SECTION_TILE_ORDER[SECTION_TOOLS]` (spacr/qt/app.py) is
    still exactly `("make_masks", "align", "umap", "gate_editor",
    "graph_builder")`, in that order, and `git diff 2c072509e..HEAD --
    spacr/qt/screens/make_masks.py` does not touch a line of `FOLD_ORDER`,
    which still opens with `train_cellpose` and carries `model_compare`.
    419 grew what Make Masks DOES; it did not add or remove a Tools tile or a
    fold, so the scene's sentence is unchanged in truth as well as in words.
  * 09_annotate scene 20 -- "the provider configured in Preferences". DRIVEN,
    not read, on the rebased tree under an isolated HOME/XDG_* with
    QT_QPA_PLATFORM=offscreen: PreferencesDialog's tab bar is now
    `['General', 'Appearance', 'Theme', 'Animation', 'Performance', 'Modules',
    'Figures', 'Logging', 'AI', 'Sound']` -- 427's Sound tab is last, as it
    was asked to be, and the AI tab still carries the `Provider` row. The
    scene's sentence survives the new tab. Re-driven after the last rebase, on
    e642b7d8b, because 444 landed while this was being pushed: 444 rewrote
    spacr/qt/sound.py alone and the tab bar and the Provider row are the same
    on both sides of it. (The probe's own qt.conf landed in the isolated home;
    the real ~/.config/spacr/qt.conf still has its 23:27:13 mtime, from another
    session.)
  * The three Classify lessons name nothing in either commit's files: no
    "Make Masks", "Otsu", "Object detection", "Sound", "Preferences",
    "Resonance", "Music" or "theme" appears anywhere in their JSON.

THREE REVIEW FINDINGS FIXED, all of them in the proof rather than in a lesson:

  1. `check_refresh` excused any path under `web/catalog/` from the byte
     comparison and then opened only the fourteen files `CATALOGS` names. The
     manifest holds exactly those fourteen today, so nothing slipped through --
     but a fifteenth locale added to the folder and not to `CATALOGS` would
     have differed between two candidates and been excused unread. The excusal
     is now membership in `COMPARED_CATALOG_PATHS`, built from `CATALOGS`, and
     `test_a_catalog_locale_nobody_compares_is_not_excused` puts a
     `lessons_xx.json` in both manifests with different digests and asserts the
     refusal. `check()`, the Map-only promotion proof, is deliberately left
     alone: it ignores non-media files entirely, which is the same gap, older
     and wider, and it is not the proof this candidate runs. WORTH SOMEONE'S
     TIME LATER, not fixed here.
  2. `test_map_candidate`'s replacement for the removed `75 * 103` pin --
     `retained_media_files + changed media == all media records` -- was a
     tautology with respect to `check_refresh`, which partitions every path
     into exactly those two buckets. Replaced with the measured arithmetic of
     this pair: 7,828 media records, 7,723 retained, 105 changed, every changed
     media path owned by 07_mask or 13_regression. A different proof, or the
     same proof on a different pair, now fails it.
  3. The Classify paragraph claimed all three prerequisites say "Recorded with
     spaCR 1.5.0.6". Corrected in place above, along with the gap it hid:
     11_classify_ml names no version at all.

STILL OPEN, and nothing below changed today. GPU: the 07_mask scene 10 chain
(gpu_queue/358-non-gpu-1.md) and 05_home's translation and voice render for the
one scene whose words changed. NO GPU and NOT STARTED: re-composing 09_annotate
(frames at 1.5.0.4) and 10_classify_cv / 11_classify_ml / 41_classify (frames at
1.5.0.6) onto current frames -- narration correct as of today, so it is capture,
compose and re-encode, and /mnt/firecuda2 is at 100% with 7.1 GB free, so space
comes first. 11_classify_ml's missing version sentence goes into that same pass.
Publishing remains the maintainer's, with its exact steps in the section above.

2026-09-21 — CURRENT HOME CAPTURE, SHARED WITH 447 (CODEX)
--------------------------------------------------------

The combined dark/Blobs capture pass has begun. Four actual 3840x2160 Home
frames from spaCR 1.5.0.8 are under /tmp/spacr-tutorial-capture/production/
05_home/keyframes: overview, Performance, the opened Mask screen and real
Help-search results. The six-scene visual specification uses the captured
search result rectangle. Source/version, dimensions, hashes and appearance
receipts are retained in tools/tutorials/evidence/
2026-09-21_home_dark_blobs_capture.json. Home, Performance and search frames
were visually inspected. This is capture evidence, not a finished video.

The shared capture policy now requires an actually painted dark palette and
Blobs backdrop and refuses visible Qt text with personal/mounted paths.
Image pixels and external windows still need visual review. The authoring
capture no longer overwrites spacr.__version__ with a historical release.
Capture work uses /tmp because firecuda2 has only about 7 GiB free.

Home's four current categories, Performance explanation and Help search now
have source-bound tutorial wording in all thirteen non-English languages.
Only lessons 04/05 changed; the other 71 lessons are unchanged. The wording
was directly authored and checked, without claiming native-speaker or audio
review. The script/pronunciation suite passes 38 tests; the capture policy,
routes, visual selection and Performance selector pass 29 tests. The exact
"pype" pronunciation rule is tested, but the required new audio render and
listening review have not happened. No GPU job has started: the GPU remained
busy during continuous observation and never reached 20 idle minutes.

Core lesson re-captures, new narration/video assembly and publication remain
open. Existing acceptance manifests and live media were not promoted.

Same-day Make Masks follow-on: seven native 4K frames completed in
/tmp/spacr-masks-current/captures/readouts-05, on genuine copies of the first
two local Measure example arrays. The real Otsu magnifier returned one object
in the shown region, without changing image or label pixels. FEATURES opened
through its actual button and scanned the two ER images. They remain visibly
unassigned because their basenames do not match the default regex; this is
folder handoff evidence, not a measurement run or a finished FEATURES lesson.
The folder picker now starts in the neutral prepared root and has only that
root bookmarked. No home shortcut or repository path is used in that frame.
Receipt: tools/tutorials/evidence/2026-09-21_make_masks_dark_blobs_capture.json.
All source/copy array hashes match. Final narration, column-assignment/run
coverage and integration into the Make Masks lesson remain open.

2026-09-21 — CPU NARRATION AND A COMPOSED HOME REVIEW CANDIDATE
--------------------------------------------------------------------------------
The dedicated refresh environment can render Kokoro on CPU. Three fresh
English af_heart tracks (01, 04, 05) passed full decoding, exact narration
freshness, sentence timing, media identity and speech-activity verification.
The six-scene Home master is now composed from the native 4K dark/Blobs
captures and exact matching narration. It is 3840x2160, 30 fps, 92.833333 s;
the whole stream decodes without errors. Encoded frames from all six scenes
were sampled; the contact sheet and Performance scene were visually checked.
A local preview also contains the af_heart narration. It is a review candidate,
not a published or fully accepted lesson.

Evidence: tools/tutorials/evidence/2026-09-21-cpu-narration-home-master.json.
Artifacts: /tmp/spacr-tutorial-audio-20260921/production/05_home/video/.
The full supported voice/language matrix for 01/04/05 is now rendering under
a 6-GiB cap, CPU only. No GPU idle window has occurred. Listening/pronunciation,
browser/mobile checks, captions/matrix reconciliation and remaining captures
stay open; the rendered phoneme pˈIp is recorded for all three af_heart PyPI
sentences, which does not replace listening to their audio.

2026-09-21 — FEATURES ASSIGNMENT AND REAL MEASUREMENT CAPTURE (CODEX)
--------------------------------------------------------------------------------
The Make Masks follow-on now includes actual image/mask file pickers, both
assigned rows, visible one-worker/minimum-area/PNG-output choices, and a
completed Measure run. Fifteen native 4K frames are retained under
/tmp/spacr-masks-current/captures/readouts-features-08. The run measured all
114 labels in the two genuine ER fields; every saved cell's field identity,
area and mean raw intensity match the original masks and pixels. Source
images and labels are unchanged. The real Otsu magnifier also returned one
object. No GPU was used. Receipt and frame/source hashes:
tools/tutorials/evidence/2026-09-21_make_masks_features_measurement.json.

This is verified capture evidence, not a finished or published lesson. The
UI's stale regex-status caption after manual assignment remains visible and
is reported to its owner in 325. An underscore-containing plate name also
collapsed field keys; the accepted measurement uses the valid name maskdemo.
Narration/scene integration and reconciliation with incoming Make Masks UI
changes remain open, alongside the existing publication acceptance work.

2026-09-21 — CODEX: RECOVERY AFTER THE MACHINE RESTART
--------------------------------------------------------------------------------
The reboot removed /tmp's capture, measurement and narration artifacts and
ended their processes. The committed receipts above preserve past results,
but those temporary media are no longer available for assembly or delivery.
The capture implementation survives in Git. A persistent recovery checkout
now restores the current-release guard shared with 447: omit historical News,
reject other spaCR versions in visible Qt text, and retain image review.
19 capture-policy checks and the lesson 04 installer-route check pass anew.
The README now describes the actual verified FEATURES assignment/Measure
route. Final live-data recaptures, narration and publication remain open.

2026-09-21 — CODEX: HOME AND INSTALLER MEDIA REBUILT ON DURABLE STORAGE
--------------------------------------------------------------------------------
The new shared capture pass recorded Home, Help search, Preferences/Performance
and opened Mask at native 4K with dark/Blobs and no historical News panel.
Home and lesson 04 now have freshly composed 3840x2160, 30-fps silent masters
and narrated English af_heart review previews. Full decoding passes for both
masters and previews: Home 92.833333 s, installers 122.666667 s. Fresh 01/04/05
af_heart audio passes source freshness, media identity, timing and activity
checks. Home overview, Performance, Help search and lesson 04's Linux and
Performance keyframes were visually inspected.

Receipt: tools/tutorials/evidence/2026-09-21-post-reboot-home-installers.json.
Media persist under /mnt/firecuda2/Claude/repo/codex-artifacts-20260921;
the neutral capture path is only a symlink into that durable directory.
The full narration matrix continues on CPU. These are local review candidates:
audible pronunciation, captions/browser/mobile checks, remaining live-data
core lessons and final Make Masks FEATURES integration remain open. Nothing
was published to the live tutorial site.

2026-09-21 — CODEX: CURRENT ANNOTATE CAPTURE AND STAGING
--------------------------------------------------------------------------------
Captured 25 native 3840x2160 frames of real Annotate on 2341 exported example
crops, in dark mode with Blobs. Twenty lesson scenes are staged, with focus
rectangles taken from the actual dialogs. The real label save path proves
null -> class 1 -> null across Next/Back; all prior columns and pixels remain
unchanged. No training, generated biological label or AI response is claimed.
The private preparation helper relocates only image-path references, validates
every image and preserves every other value. Its tests cover the biological
rowID/SQLite row-identity distinction, external-image refusal and private-path
refusal. The picker and console now use neutral paths; the downloader's earlier
transcript is archived privately before the new annotation segment. The actual
folder dialog, crop grid and console were visually inspected. Receipt:
 tools/tutorials/evidence/2026-09-21-annotate-current-capture.json.
Narrated master rendering and language/voice/browser acceptance remain; no
live media was published. Annotate's English af_heart narration is rendering
on CPU in the durable annotate-verified staging tree.
Annotate's fresh af_heart narration and 353-second native 4K/30 master are now
rendered. Both silent and narrated videos fully decode. Audio source identity,
model/voice identity, timing and speech activity pass in the exact synthesis
environment (checking with the application environment correctly rejected its
different runtime fingerprint; no evidence was rewritten to bypass this).
Chromium passes actual player playback, chapter seeking/synchronization,
related links and 390-pixel mobile layout. Encoded frames at 60/310/320 seconds
were inspected, including the real neutral-path label-save console. The receipt
includes media hashes and browser evidence. Audible/full-language acceptance
and publication with the complete refreshed set still remain.

2026-09-21 — CODEX: CURRENT CLASSIFY OVERVIEW RECAPTURED
--------------------------------------------------------------------------------
Eleven native 3840x2160 frames show the current Classify CV/ML selector, five
nested tools and real Feature Explorer navigation in dark mode with Blobs.
Frame hashes, size, painted appearance and genuine GUI acceptance are checked;
selector/tooltip/nested-screen frames were visually inspected. Capture used
isolated state and no model/provider call. Receipt:
 tools/tutorials/evidence/2026-09-21-classify-overview-current-capture.json.
This completes only the overview's navigation capture. The actual CV/ML runs,
lesson composition, narration/captions/browser and audible acceptance remain.

2026-09-21 — CODEX: CURRENT CV RUN ACCEPTED AS A WORKFLOW EXAMPLE
--------------------------------------------------------------------------------
A fresh CPU run on the 64 unchanged example crops produced 27 native 4K dark/
Blobs frames. Both native train/test and train/validation audits pass with no
well/object/field/content overlap (one shared plate is explicit). Independent
checks verify all 32 test identities, labels/probability sums, accuracy 0.5 and
macro F1 1/3; accuracy equals the majority baseline, not useful-model evidence.
The original metadata database is copied byte for byte and rechecked. Two
loader workers are confirmed in the actual console. A private neutral HOME
keeps genuine journal paths safe to show; no console transcript was rewritten.
Picker/result/unsent-AI frames were visually checked. Receipt:
 tools/tutorials/evidence/2026-09-21-classify-cv-current-capture.json.
Lesson 10 now states current spaCR 1.5.0.8, the real two-worker setting and
preserved metadata. Fresh terminal preparation frames, full composition,
narration/captions/browser/mobile/listening and publication remain open.
The remaining ML recapture is still needed for the main Classify overview.

2026-09-21 — CODEX: CURRENT CV COMPOSITION AND ENGLISH MEDIA VERIFIED
Three real terminal preparation frames now accompany the accepted CV run in
12 staged scenes. The preparation was repeated after training into a separate
export; independent input proofs match the trained dataset. No model rerun or
pixel replacement is represented. The 254.7-second English af_heart review
and native 4K/30 silent master fully decode; narration freshness/timing checks
pass. Chromium desktop/mobile playback, chapter seek, synchronization, related
links and the downloadable helper pass. Capture/split checks: 23 passed.
Receipt: tools/tutorials/evidence/2026-09-21-classify-cv-current-capture.json.
Remaining: ML recapture, full language/voice/caption and listening acceptance,
then publication with the complete refreshed set. No live media was uploaded.
The linked preparation ZIP was also rebuilt from the exact recorded helper;
its README now matches the current two-worker run and preserved database.
The remaining English voices for current Annotate and CV are now rendering
sequentially on CPU, with private outputs and unchanged frozen narration.

2026-09-21 — CODEX: CURRENT TABULAR CLASSIFY CAPTURE VERIFIED
Thirty native 4K dark/Blobs frames now record the real ML workflow on a private
copy of the downloaded four-well dataset. All 2153 saved predictions and nine
holdout metrics are independently checked; 1035 held-out objects, no actual
well overlap, 188 unscored objects, accuracy 0.6202898551 versus majority
baseline 0.5632850242. The original database is unchanged. The labels remain
plate columns, not validated phenotypes. Source-picker confirmation clears
the genuine stale empty-source banner; an expanded native figure pane shows
all three charts without cropping. No pixels or warnings were rewritten.
The first attempt was rejected for code paths in warnings; current code is
mounted read-only at /tmp/spacr-code. The final recording is v4; earlier
attempts remain private. Lesson 11 now has 11 staged scenes and removes a
stale claim that test_split overrides test_size (the dispatcher uses setdefault).
English narration is rendering on CPU. Receipt:
 tools/tutorials/evidence/2026-09-21-classify-ml-current-capture.json.
Remaining: full media/locale/caption/listening acceptance, overview composition,
Mask recapture and complete-set publication. No media has been uploaded.
ML's 216.1-second English af_heart narration and native 4K/30 silent/review
masters now pass full decode and synthesis freshness/timing checks. Actual
Chromium desktop/mobile playback, chapter seek, synchronization and related
links pass. The encoded opening frame was visually checked; hashes and player
proof are in the current ML receipt. Full locale/caption/listening and final
publication acceptance still remain.

2026-09-21 — CODEX: CURRENT CLASSIFY OVERVIEW ASSEMBLY
The overview now composes its genuine current main-module navigation with
independently accepted current CV and ML footage. The composer accepts
explicit source directories and the actual expanded ML heatmap frame while
retaining acceptance, original-image hashes and navigation-only checks. No
model is rerun or screenshot rewritten. Eleven staged scenes use current
1.5.0.8 wording; five strict navigation tests pass. English af_heart narration
is complete at 235.9 seconds; video and player verification are in progress.
The remaining English voices for current Annotate, CV and ML are rendering
on CPU in private stages. Full languages, listening and publication remain.
Overview media acceptance now passes: both 235.9-second native 4K/30 videos
fully decode; source/model/voice freshness and audio timing/activity pass.
Actual Chromium desktop/mobile playback, seeking, synchronization and chapter
links pass. The receipt retains the original navigation and CV/ML provenance.
Full voice/language, listening and complete-set publication remain open.
The overview's remaining 23 English voices are now rendering on CPU against
its frozen verified stage, retaining the accepted af_heart track. Annotate,
CV and ML voice work continues separately; no full-matrix completion claimed.

2026-09-21 — CODEX: CURRENT MAP BARCODES CAPTURE AND INDEPENDENT COUNTS
Captured current 1.5.0.8 at eed895c2f with 20 native GUI frames plus two actual
terminal/figure frames. Fifteen staged scenes use real paired SRR33531217
reads and matching reference copies. Saved GUI output independently reconciles
10,000 pairs / 8,611 extracted / 7,657 mapped / 4,099 count rows; the separate
two-barcode API run reconciles 1,000 / 869 / 793 / 535. Original reference
copies and recorded API inputs retain their hashes. Three ambiguous guide
sequences remain unassigned, and the genuine warning is retained.
Actual scrolling makes all four guide and row orientation verdicts visible.
No app logic or warning text was replaced. The initial 233.2-second English
track exposed three noun-read pronunciations as red in the phoneme evidence;
exact sentence overrides repair these and preserve other lessons/languages.
Evidence: tools/tutorials/evidence/2026-09-21-map-barcodes-current-capture.json.
Final corrected media/player checks, the full language/voice/caption matrix,
listening acceptance and complete-set publication remain open.
The corrected English audio is complete at 233.2 seconds with a new media
hash; all three formerly wrong read-noun phoneme sequences now use reed.
Its video/player verification is rerunning. The remaining English voices
have started on CPU, retaining the corrected af_heart track. No publication.

2026-09-21 — CODEX: ANNOTATE ENGLISH MATRIX VERIFIED
All 24 English voice tracks for the current Annotate recording now pass
full AAC decode, exact synthesis freshness, sentence timing/activity and
peak checks. Receipt: tools/tutorials/evidence/2026-09-21-annotate-english-voices.json.
This is English technical acceptance; other spoken languages, captions,
audible acceptance and complete-set publication remain. The same capped
CPU queue has moved on to current Classify CV's remaining English voices.
Current Classify ML also has all 24 English voices verified, including
independent decoded true-peak measurements against the unchanged -1 dBFS
gate. The Annotate receipt now carries those numeric measurements as well.
Corrected Map silent/review masters pass full native 4K/30 decode and actual
Chromium desktop/mobile playback, seeking, synchronization and related links.
The corrected encoded row-verdict spotlight was visually inspected. Receipt
retains superseded first-render evidence and binds the new audio hash,
pronunciation phonemes, decoded peak and browser proof. No live publication.
Next capture: a fresh private Measure stage is prepared for the real example
download and native preview controls, using CPU/software graphics. No arrays
are preseeded and the recording has not yet been accepted.

2026-09-21 — CODEX: CURRENT MEASURE PREVIEW VERIFIED; FRESH BATCH RUNNING
The actual Load test data route downloaded and expanded all sixteen arrays
in an empty private stage. Nine native 4K frames at 7046d9485 show dark mode
and painted Blobs, with neutral paths. E02_1_1 has 27 candidates; channel
zero preserves their identities and restoring All channels restores pixels.
E02_9_1 has 15 candidates. Live and Normalize are restored off, all batch
settings match their starting values, and both source arrays retain hashes.
Receipt: tools/tutorials/evidence/2026-09-21-measure-current-preview.json.
The full sixteen-field normal-mode run is now recording on CPU with Plot
and one worker, under a 24-GiB cap (the historical measured peak was 17 GiB).
No previous database/output was seeded. The independent output verifier and
composition tool now accept explicit current paths and the actual preview
proof, retaining the older filter/propagation checks for historical footage.
34 pixel/preview tests pass. Full batch, lesson copy, media and acceptance
remain open; the preview receipt is not a completed-lesson verdict.
Classify overview's complete 24-track English matrix now passes full AAC
decode, exact synthesis freshness, sentence timing/activity and independent
true-peak measurements (highest observed -1.1 dBFS, unchanged -1 dBFS gate).
Receipt: tools/tutorials/evidence/2026-09-21-classify-overview-english-voices.json.
Other spoken languages, captions and listening acceptance remain open.
Annotate's prior thirteen language/caption reviews were checked against the
exact old English hash. Only scene 5's image-size wording changed to crop
size; all nineteen other English narrations and translated scenes are
retained. That sentence is corrected in every locale and the reviews are
bound to the actual current twenty-scene English lesson. All thirteen were
staged privately. The promoter now uses repository pronunciation helpers
rather than an older external authoring copy. The remaining 26 spoken
language/voice tracks are starting on CPU; no GPU or publication is involved.
Independent staging verification confirms all thirteen current Annotate
reviews, scene geometry/links and exact current speech inputs. Receipt:
tools/tutorials/evidence/2026-09-21-annotate-current-translations.json.
Eight promotion tests pass; the CPU multilingual render is live.

2026-09-21 — CODEX: MAP ENGLISH MATRIX, CURRENT CV TRANSLATIONS, MEASURE RETRY
All 24 current Map Barcodes English tracks pass full AAC decode, exact source
and synthesis-input freshness, sentence timing/activity and independent true
peak checks (highest -1.0 dBFS). Receipt: 2026-09-21-map-barcodes-english-voices.json.
All thirteen CV translation/caption reviews now bind current English 104932e5:
scene 7 says two loader workers; prerequisites state 1.5.0.8, n_jobs=2 and a
byte-identical original measurements database copy. Eleven other narrated
scenes are retained unchanged. Independent staging checks verify review and
catalog identity, current geometry/links and speech inputs. Receipt:
2026-09-21-classify-cv-current-translations.json. Its remaining 26 spoken
tracks are rendering on CPU under a 6-GiB cap; English render remains separate.
The first fresh Measure batch recording hit its 3600-second limit and opened
Stop's native confirmation dialog. After inspecting it, Finish current work
was selected; the capture exited 1. Its pipeline manifest reports success,
which is not an accepted recording or independent output verification. All
failed-capture evidence is retained (2026-09-21-measure-current-timeout.json).
A new empty private measure-current-v2 stage repeats the real download and
full 16-field/Plot run with a 10800-second limit, 32-GiB cap and one worker.
No old database/output was seeded and no application behavior was changed.
Full language/caption/player/listening acceptance and publication remain open.
Current CV English rendering has also finished: all 24 tracks pass full
technical verification and independent decoded peak measurement, highest
-1.0 dBFS. Receipt: 2026-09-21-classify-cv-english-voices.json. Its remaining
26 spoken-language tracks continue separately; no listening signoff inferred.

2026-09-21 — CODEX: CURRENT CLASSIFY OVERVIEW TRANSLATIONS
All thirteen translation/caption reviews now bind the verified current
English overview (f850fcd6). Scene 3 names example loading and source
confirmation; scene 4 describes the expanded plate heatmap actually shown.
Nine other narration scenes retain their accepted text. Prerequisites name
spaCR 1.5.0.8 and omit the removed old-parser sentence. Spanish's description
also drops "now"; German restores the instruction to inspect real results.
Independent verification checks all thirteen staged catalogs, source hashes,
retained scenes, current speech helpers and visual/related-lesson geometry.
Eight review-promotion tests pass (0.49 s). Receipt:
tools/tutorials/evidence/2026-09-21-classify-overview-current-translations.json.
The remaining 26 spoken tracks are rendering CPU-only with two threads and
a 6 GiB cap. No live publication or native/listening signoff is implied.

2026-09-21 — CODEX: CURRENT ML TRANSLATIONS AND VERIFIED MEASURE BATCH
ML's thirteen reviews now bind the eleven-scene recording. The new third
scene explicitly confirms the loaded project with Choose source data; the
opening names current Classify and the download is conditional. Prerequisites
use 1.5.0.8 and test_size=0.1 directly, removing the repaired normalization
workaround. Eight retained narration scenes preserve measured results,
actual held-out wells and scientific limitations. Independent staged/source,
speech/geometry and numeric-setting checks pass; eight promotion tests pass.
Receipt: 2026-09-21-classify-ml-current-translations.json. Remaining 26 voices
are rendering on CPU; native/listening and full player acceptance remain open.

The fresh Measure v2 capture finished normally. Independent read-only checks
verify 16/16 successful fields, 601 cell records/RGB224 crops, 622 nuclei, 373 pathogen
objects and 5409 pixel-derived area/intensity values, including the five
source-supported label merges. Source arrays match the preserved published
manifest. All 48 PDFs and 48 GUI figures verify, and collected settings are identical
before/after. The native dark/Blobs 4K frames contain neutral paths and an unsent
AI draft. The stale-results warning remains visible; these output checks do
not certify that warning or automatic handoff. No app behavior was changed.
Receipt: 2026-09-21-measure-current-batch.json. Verified current preview and
batch footage are composed into 13 scenes, English updated and staged, and the
af_heart review master is rendering. Older 47/48 queue and unverified prior-build
dialog/test-mode claims were removed. Full media/language/listening acceptance
and complete-set publication remain open. 37 measurement/mastering tests pass.

Overview Spanish ef_dora stopped after its decoded AAC peak reached -0.9 dBFS.
Added the established fingerprinted two-decibel fallback only for that track;
the -1 dBFS release limit is unchanged. The confirmed-terminal job has restarted;
no successful repaired-track claim is made until its actual output passes.

2026-09-21 — CODEX: CURRENT MEASURE ENGLISH MASTER AND BROWSER VERIFIED
The13-scene Measure review master finished:265.6seconds,native3840x2160 at
30fps. Silent and narrated videos fully decode; af_heart timings/activity/
freshness pass and decoded true peak is-1.5dBFS. Real browser checks pass for
English af_heart playback, seeking and authored scene links. Desktop/mobile
screenshots inspected and hashed in the batch evidence; no listening or full
language-matrix acceptance claimed. The remaining23English voices now render
onCPU, preserving the verified af_heart. Translations/captions and complete-set
publication remain open. The visible stale-output warning remains disclosed.

2026-09-21 — CODEX: MAP BARCODE REVIEWS MATCH THE CURRENT15SCENES
All13language/caption reviews now bind the verified current Map Barcodes
recording. Added guide and row orientation evidence: reverse-complement
support in read1 and stored orientation in read2, with low-rate alternatives
remaining indeterminate. Prerequisites name1.5.0.8 and the three ambiguous
guide sequences whose reads stay unassigned. Removed older-reference-table
comparisons. Twelve unchanged narration scenes preserve actual8611windows,
7657mappedreads,4099countrows, sample-specific limitations and GUI/API boundaries.
All13staged/source/speech/geometry checks pass, plus9bundle/promotion tests
(0.78s). Receipt:2026-09-21-map-current-translations.json. The remaining26
spoken tracks render CPU-only; no listening/native approval or publication.

2026-09-21 — CODEX: ANNOTATE CAPTURE NEEDS THE NEW OPENING FLOW
Integrated e04294be5: Load test data now opens the actual plate automatically.
The private Annotate recording's scene 4 still teaches an extra Open source
step, so it is no longer accepted as the current tutorial. Its remaining
CPU narration job was deliberately stopped (exit 143), preserving completed
files. Recapture the opening flow, update affected English/localized narration
and prerequisites, then regenerate and verify affected media. The independent
label/pixel-preservation evidence remains historical evidence for its recorded
source, not proof of the new opening flow. No tutorial media was published.

2026-09-21 — CODEX: ANNOTATE AUTO-OPEN RECAPTURE VERIFIED
The new capture passes against a30412e01 / 1.5.0.8. Load automatically opens
128 crops with the published infected labels before any extra source action.
For recording privacy the real picker then selects the identical dataset via
its neutral alias; the recorded Open source step is now explicitly optional.
No HOME change, app mock, personal-path frame or alternate dataset is used.
Full annotation tour passes: 2341 rows/PNGs, empty -> 1 -> empty persistence,
original database columns and every PNG independently rechecked, RGB/CMY/RGB,
current Agreement and training routes, unsent AI question. Source picker,
Stream explanation and final console images visually inspected.
Updated English scenes 3/4 and all13 source-bound translations; eighteen
narration scenes retained unchanged. Current prerequisites name1.5.0.8.
19 appearance/path tests pass11.96s;22 annotation/promotion/geometry tests
pass1.94s. Receipt:tools/tutorials/evidence/2026-09-21-annotate-auto-opening.json.
Fresh af_heart narration is rendering on CPU. Narrated master, full voices,
captions/player/listening acceptance and complete-set publication remain open.

2026-09-21 — CODEX: CURRENT CLASSIFY CV HAS ALL50 VOICES VERIFIED
All26 non-English tracks finished (CPU-only,220.5min) and independently pass
source freshness, timing, decoded activity, full decode and true-peak<=-1dBFS
checks with unchanged audio hashes. Together with24 previously verified
English voices, the current classify-cv-current-v4 stage now has50 technically
verified tracks. The initial verification wrapper selected one English lesson
against full translated catalogs; it correctly failed on lesson-id mismatch.
The corrected wrapper makes matching one-lesson copies for every language,
without modifying renderer catalogs/audio, then verifies all26 successfully.
Receipt:tools/tutorials/evidence/2026-09-21-classify-cv-current-other-voices.json.
This does not supply listening approval or publish the tutorial set.
Current Annotate's remaining English voices now render CPU-only; its new4K
master is undergoing full decode before browser/mobile playback acceptance.

2026-09-21 — CODEX: ANNOTATE CURRENT REVIEW MASTER VERIFIED
The new automatic-opening lesson's af_heart narration is356.8s, source/timing/
activity checked with decoded true peak-1.0dBFS. Both silent and narrated
masters fully decode at3840x2160/30fps. The repository player loads the exact
verified audio, exposes20 scenes and expected related links, and seeks with
audio/video clocks86.079/86.074s and no media error. Desktop/mobile screenshots
were visually inspected; hashes and browser checks are added to the existing
2026-09-21-annotate-auto-opening.json receipt. Remaining23 English and26 other
voices now render CPU-only against the new source, replacing the superseded
opening's tracks. Full voice/caption and listening acceptance plus complete-set
publication remain open. No tutorial media was published.

2026-09-22 — CODEX: GERMAN MEASURE REVIEW MATCHES CURRENT CAPTURE
Updated four narration scenes and prerequisite metadata to the current English
source b3810829. Nine translated scenes are preserved byte-for-byte. Removed
obsolete oversized-dialog/test-folder/export-limit descriptions and the old
47-of-48 figure claim; the lesson now describes all48 figures and keeps the
unvalidated stale-output warning/automatic handoff limits. Current source hash,
all thirteen staged captions, and all visual/focus/timing/link metadata pass
independent checks. Receipt:2026-09-22-measure-german-current.json. Twelve other
Measure reviews remain; this caption-only update does not alter the running
English narration inputs. No media publication or listening approval.

2026-09-22 — CODEX: ALL THIRTEEN MEASURE LANGUAGE REVIEWS CURRENT
Completed the twelve remaining reviews after German. All13 now match source
b3810829: four changed scenes and updated prerequisites; nine scenes retained
in every language. Independent verification checks actual source hashes,
metadata, captions, visual/focus/timing/related links and pronunciation helpers.
All48 saved/GUI figures are described; removed obsolete oversized-dialog,
test-folder and export-limit claims. Warning/automatic handoff limitations
remain explicit. Eight promotion tests pass0.74s. Receipt:
2026-09-22-measure-current-translations.json. NewCPU job14953 renders26other
language voices in measure-current-v2; running English inputs were not changed.
No listening approval/publication; final1.5.0.9 appearance/version review remains.

2026-09-22 — CODEX: CLASSIFY ML OTHER VOICES INDEPENDENTLY VERIFIED
CPU render26855 completed26tracks in241.7minutes. Independent verifier75434
finished0: all26 pass current-source freshness, timing, activity, full decode
and decoded true-peak threshold<=-1dBFS; file hashes unchanged. Receipt:
2026-09-22-classify-ml-current-other-voices.json. Listening, captions/player,
latest-release visual acceptance and complete-set publication remain open.

2026-09-22 — CODEX: OVERVIEW VOICES AND MEASURE ENGLISH VERIFIED
Classify Overview's 26 other-language tracks pass independent freshness,
timing, activity, full decode and peak checks, completing 50 technical tracks
with the earlier English set. Measure's 24 English tracks now pass those same
checks. The first Measure verification used the app Python environment and
correctly rejected mismatched tool/model identities; using the actual narration
environment passes without changing media or fingerprints. Receipts:
2026-09-22-classify-overview-current-other-voices.json and
2026-09-22-measure-english-voices.json. Listening/publication remain open.
Fresh Regression capture on 1.5.0.9 downloads four real paired tables and checks
nine settings searches without changing settings. Its analysis journal reports
success, but the guard refuses a progress frame containing the maintainer's
run-journal directory. No rejected screenshot is saved; no recording accepted.
Receipt:2026-09-22-regression-capture-path-rejection.json. The diagnostic log also
has a nonfatal exchangeability-report TypeError; scientific acceptance remains
open. Resolve the journal display path before recapturing; do not weaken privacy.

2026-09-22 — CODEX: CURRENT REGRESSION CAPTURE AND THIRTEEN REVIEWS
Resolved the path refusal using a neutral Linux account inside the recording's
private user/mount namespace. Host account files and application code are
unchanged; normal home lookup finds the staged profile. The actual journal now
prints a neutral path. Reusable CPU-only wrapper:run_neutral_capture.sh; shell
syntax and real help-command smoke checks pass, no HOME variable repurposed.
Current1.5.0.9 dark/Blobs capture completes26frames, nine settings searches and
all Runs/Results/Measurements/Cells plus p-values/Q-Q/Coefficients surfaces.
Independent saved-output checks confirm434guide/325gene rows, all759 empirical
P values and separate BH families, finite effects, five GUI figure hashes and
unchanged settings. Finished/results frames visually inspected. No database or
cell montage claim. Evidence:2026-09-22-regression-current-capture.json.
The private DEBUG log still records a nonfatal exchangeability-report TypeError.
The lesson now states that limitation and requires independent assessment of
permutation assumptions. All13 reviews updated for that scene and1.5.0.9,
20narration scenes retained; fresh-download frame replaces the old cached-load
frame. Independent source/staging/caption/geometry/link/pronunciation checks
pass; evidence:2026-09-22-regression-current-translations.json. First English
narration renders on CPU. Narrated master/full voices/player/listening and
complete-set publication remain open. Map's original renderer ended143 after
22tracks; resumable render73065 preserves completed tracks and finishes the rest.
Final capture-policy, staging, translation-promotion and acceptance tests:
63pass10.92s. Feature index and whitespace checks pass before the progress push.

2026-09-22 — CODEX: MAP BARCODE NARRATION MATRIX VERIFIED
The resumed current Map Barcodes renderer finishes its four remaining tracks
and reuses22 valid tracks. Independent source/freshness/timing/full-decode/
activity/true-peak checks pass all26 non-English tracks. Together with the
previous24 English tracks, all50 current tracks are technically verified.
Receipt:tools/tutorials/evidence/2026-09-22-map-current-other-voices.json.
No listening approval or complete-set publication is implied. Regression's
current English and non-English CPU narration continue; final visual/caption/
audible acceptance and the GPU-dependent Masks recapture remain open.

2026-09-22 — CODEX: CURRENT REGRESSION MASTER AND PLAYER VERIFIED
Both native3840x2160/30fps masters fully decode. Current en/af_heart narration
passes freshness/timing/activity and decoded true-peak checks. Actual Chromium
playback, chapter seeking,21scenes and all related-lesson links pass. Desktop
and390px-mobile screenshots inspected after the navigation layout settles.
The verifier now returns to the page top and waits for the mobile sidebar's
closing transition before capturing; the prior transient screenshots are kept
privately. No player/application changes. Capture receipt updated:
tools/tutorials/evidence/2026-09-22-regression-current-capture.json.
The exchangeability diagnostic limitation remains explicit. Full voice/caption
and listening acceptance plus complete-set publication remain open.

2026-09-22 — CODEX: MAKE MASKS CURRENT COMBINED CAPTURE VERIFIED
One 1.5.0.9 session captures Otsu magnifier, FEATURES assignment/measurement,
reversible editing, recrop and actual local Cellpose-SAM detection. All 40
frames are native 3840x2160 with dark/Blobs and neutral visible paths. The two
original Measure example arrays are unchanged. Independent verification checks
all 114 cell areas and mean raw intensities against original planes, distinct
field identities, child crop pixels and byte-identical archived parent image.
The CPU detection produces one object in a 332x267 crop, but visual inspection
shows it outlines the upper-left partial object instead of the existing central
cell mask. The private revised narration explicitly explains this mismatch;
it does not call the detection biologically correct. Its 27 scenes include six
new magnifier/FEATURES scenes and current Filter/Object detection labels.
Evidence:tools/tutorials/evidence/2026-09-22-make-masks-current-combined-capture.json.
Capture/input/policy checks:28 passed in5.04s. First English narration renders
on CPU; 13 translated reviews, complete voices, listening/player acceptance and
publication remain open. The canonical lesson/reviews are not replaced by a
partial private draft. Core Mask's GPU-dependent recapture remains separate.

2026-09-22 — CODEX: ANNOTATE ENGLISH MATRIX VERIFIED
The current auto-open Annotate renderer completes. Independent checks pass all
24 English voices: exact current source, timings, full decode, non-silent
activity and decoded true peak at or below -1dBFS. Receipt:
tools/tutorials/evidence/2026-09-22-annotate-current-english-voices.json.
Other-language rendering continues; listening and complete-set publication
remain open. No technical check is recorded as human listening approval.

2026-09-22 — CODEX: MAKE MASKS CURRENT LESSON AND ALL13 REVIEWS
Canonical lesson14 now has27 scenes, including six Otsu magnifier/FEATURES
scenes tied to the single current capture. All13 language reviews share the
exact new English source and preserve unchanged editing/recrop explanations.
They explain the visible stale regex counter,114 source-verified cell rows,
current Filter/Object detection labels,332x267 crop and the model's upper-left
partial-object result instead of the existing central-cell mask. Independent
review/staging checks preserve unrelated lessons, scene geometry, links and
pronunciation text.54 relevant tests pass3.22s. Evidence:
2026-09-22-make-masks-current-translations.json. First English af_heart renders
474.0s on CPU; native4K master/browser verification26837 now runs. Complete
voices, final listening/caption/player acceptance and publication remain open.

2026-09-22 — CODEX: THREE COMPLETE VOICE SETS / MAKE MASKS MASTER
Annotate and Measure's remaining 26 tracks and Regression's 24 English plus 26
other-language tracks independently pass source freshness, timing, full decode,
activity and decoded true peak checks. Each current lesson now has 50 technically
verified tracks. Four dated current-voices receipts are in tutorial evidence.
Make Masks silent/review masters pass full native 4K/30fps decode; af_heart audio
passes at -3.4dBFS. Browser playback, seek, 27 scenes and 10 related links pass;
desktop/mobile screenshots were inspected. Combined-capture receipt updated.
Make Masks English and other-language matrices are rendering on CPU. Listening,
remaining caption acceptance, Core Mask GPU capture and complete-set publication
remain open. No technical verification is treated as listening approval.

2026-09-22 — CODEX: LIVE PREVIEW TUTORIAL CONTRACT UPDATED
The real Mask tutorial test opens the deferred Live panel before checking that
Run and Run preview resolve to distinct buttons. Tutorial tests also wait for
real window-owned preview/usage jobs to retire. All 73 script tests pass in
75.74s without a lingering JobRunner report; retained Qt widgets still appear
in the harness shutdown report. Capture/publication status is unchanged.

2026-09-22 — CODEX: 472 SHARED WORKFLOW CONTRACTS
Four new tutorial authoring contracts under tools/tutorials/workflows/ are
now generated from spacr/resources/module_workflows.json, which also feeds
the API guide and nine interactive Home-starting pathway walkthroughs.
These contracts are not finished recordings or translated lesson catalogs;
the existing live-media acceptance requirements remain in force.

2026-09-22 — CODEX TAKEOVER: MAKE MASKS COMPLETE NARRATION MATRIX
Recovered the finished24 English and25 other-language tracks; rendered only
the missing Chinese zm_yunyang voice on CPU (371.1s,3.3min rendering). The
staged English lesson is byte-for-JSON equal to the current canonical27-scene
lesson. Independent full decode, current synthesis fingerprint, timing,
activity and decoded true-peak checks pass for all50 tracks (24 English and
26 others), each at or below -1dBFS. Source/media-hashed receipts:
2026-09-22-make-masks-current-english-voices.json and
2026-09-22-make-masks-current-other-voices.json in tutorial evidence.
No GPU used. Listening, final caption/player acceptance, Core Mask recapture,
the four new472 lessons and complete-set publication remain open.

2026-09-22 — CODEX:472 ENGLISH LESSON SCRIPTS
Four English source lessons78-81 now generate from the shared workflow map:
8,73,16 and10 scenes respectively. The long reference covers every mapped
module and every artifact location; focused lessons keep the relevant branches.
All scripts start at Home and link existing specialist lessons. Source/map/API
and staging checks pass89/89. This is authoring progress only: new recordings,
13-language reviews, voices, player/caption/listening acceptance and complete-set
publication remain open. They have not been inserted into the published course.

2026-09-23 — CODEX: READY VOICES CAN BE OFFERED PER LESSON
User explicitly prioritizes incremental public updates before every voice and
translation is complete. The authoring player now accepts narration_voices
per lesson, offering only declared, known voice IDs. Existing lessons without
this metadata keep their full voice catalog. A language with no ready audio
keeps its text language and offers Silent master. Eight Node-backed checks
pass; actual private Chromium playback verifies English Heart, Spanish silent
without missing-audio requests, and switching back to English. Evidence:
evidence/2026-09-23-incremental-narration-selector.json under tools/tutorials.
This is player preparation against previously recorded media, not acceptance
of those recordings against current source. The published player is unchanged;
reduced-voice candidate assembly, exact source/media checks and upload remain.

2026-09-24 — VERIFIED INCREMENTAL WALKTHROUGHS LIVE
Nightly8b8458b14 is publicly deployed with85routes/84playable tutorials.
47of81existing lessons now have revised user-walkthrough text and matching
published media; four new organism guides are also live. Source rewrites now
cover57of81existing lessons (70.4%); four follow-up English videos are being
rendered, while six earlier rewrites still need current captures.24existing
scripts remain to review. Retained captures are explicitly distinguished from
new runtime recordings.716old translation reviews now mismatch their revised
sources and stay registered pending replacement. See the per-lesson audit in
tools/tutorials/evidence/2026-09-23-user-walkthrough-review.json.

2026-09-24 — NATIVE ANNDATA / EMBEDDINGS AND FIVE MORE MODULE GUIDES
75of81 existing lesson scripts now follow the requested user-walkthrough style;
69 have matching accepted media, plus the four new organism guides. Six scripts
and six capture follow-ups remain, with the per-lesson list in the walkthrough
audit.950 old translation reviews are source-incompatible and registered;
English publication is not blocked by them.

Seven replacements (59,70,72,73,74,75,77) are accepted at immutable media
f0ded2a75c41f9eada9383b5aa970e1a6a7bcd65:1134/1134 hosted hashes matched,
84local and84hosted playback cases pass, both unavailable-route mutants fail
as expected, and eight final publication guards pass.525 compatible tracks
remain. Five lessons preserve53 byte-verified existing captures; AnnData and
Embeddings use new native GUI runs. All six AnnData exports passed independent
matrix checks; both real CPU embedding policies completed without crop
injection. No claims of new GPU execution or full locale review.

The previous eight-video batch is directly verified LIVE at1e5c408b6, with
11528generated English API entries and browser chapter seeks passing. This
new seven-video batch is accepted/prepared pending its nightly deployment;
see358_live_exploration_guides_2026-09-24.json for the last direct live proof.
