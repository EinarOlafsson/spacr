================================================================================
REDO EVERY CORE-MODULE TUTORIAL WITH LIVE DATA AND THE FOLDED-IN ACTIONS
================================================================================

Status:    OPEN, filed 2026-09-02. AFTER the version update, following the
           tooltip/API/translation sweep and README review already ordered.
Requested: 2026-09-02 — "the tutorials for all of the core moduals need to be
           redone. there is now live data for all core moduals that should be
           used in the tutorials, there are also several buttons that have been
           folded into the core modules that to be mentioned in the core
           modual tutorials. but for these we can retain the old tutorials and
           just reference other tutorials in the core module tutorials."
Owner:     the Codex tutorial session. The editable tutorial source and the
           derived documentation/media tree must not be edited concurrently by
           another session; instruction 48's ownership rule still applies.

WHY THIS IS NEW WORK
--------------------

Instruction 48 verified the previous tutorial release, but the application has
since changed its module structure. Core modules now expose downloadable live
test data and absorb actions that used to appear as separate module tiles. A
tutorial can therefore be technically polished and still teach a navigation
path, input source or module boundary the current application no longer has.

The Core tutorial is the current top-level workflow. A still-accurate older
specialist tutorial remains useful evidence and should be linked at the point
the folded action is introduced; it should not be deleted or narrated again
merely to make the Core tutorial self-contained.

WHAT TO DO
----------

1. Derive the Core-module inventory from the runtime module registry and Home
   grouping used by the running application. Do not copy a historical list
   from instruction 48 or hard-code a second list that can drift.

2. For every current Core module, rebuild its top-level tutorial against the
   running UI and current module name, category, settings layout, action row,
   output surfaces and completion state.

3. Use that module's real downloadable/live test dataset through the same
   control a user sees. Show the input arriving, run a bounded representative
   operation, and inspect a real output; a mocked path, static screenshot or
   source-only validation is not a live-data tutorial.

4. Name every action/button folded into the Core module and show where it now
   lives. When an existing specialist tutorial still explains the action
   accurately, retain it and link to it from the Core tutorial at that point
   instead of duplicating its full walkthrough.

5. Update references to renamed, merged or removed modules and controls. In
   particular, explain current test-data controls, live previews, GPU/device
   acceleration where the module uses it, and other capabilities added since
   the last tutorial release; do not present NVIDIA/CUDA as the only
   acceleration route.

6. Keep useful old tutorials. Remove one only when its workflow no longer
   exists or its surviving content is fully and more accurately replaced, and
   record that decision in this file before removing any published lesson.

7. Re-run instruction 48's production checks: current screenshots/video,
   highlight geometry and timing, concise narration, intelligible technical
   pronunciation, caption/narration synchronisation, mobile playback, media
   hashes/budgets and the live deployment verifier. The new lessons inherit
   the old quality bar rather than replacing it.

8. Publish only after the version-update hold is released and the intended
   application version is the one shown. Commit and push each completed Core
   tutorial or small coherent tutorial group so a crash cannot strand the
   whole rebuild.

HOW TO KNOW IT WORKED
---------------------

* A generated inventory test maps every current runtime Core module to exactly
  one current top-level tutorial and fails when a new Core module appears
  without one.
* Every Core tutorial downloads/loads its registered live dataset through the
  UI, completes a bounded real operation, and shows an inspectable real output.
* A generated folded-action inventory proves each action is mentioned in its
  owning Core tutorial and that every referenced specialist tutorial exists.
* Links are valid and no retained lesson points to a removed module, stale
  button name, obsolete settings category or old module hierarchy.
* Tutorials that use acceleration describe the current resolver and supported
  GPU backends accurately, and remain usable on CPU-only hardware.
* The repository tutorial/media tests, external tutorial suite, multilingual
  narration/caption checks, mobile player checks, hashes and media budget all
  pass against the exact published artifacts.
* A manual catalog-order review confirms the tutorial set teaches the current
  spaCR module structure and features rather than merely satisfying the
  inventory tests.

BOUNDARY
--------

This item records and later executes the tutorial rebuild. It does not weaken
the explicit hold on 1.5.0.5, move tutorials onto the pre-update list, or
authorise edits in the tutorial media tree by a second concurrent session.

--------------------------------------------------------------------------------
2026-09-08 -- WHY test_tutorial_release_audit CANNOT BE CLOSED AS A CODE FIX
--------------------------------------------------------------------------------

`test_every_module_and_fold_has_exactly_one_current_tutorial_route` is red and
will stay red until this instruction runs. Recorded here because from the
failure alone it reads like a missing dictionary entry, and it is not.

THREE ROUTES HAVE NO LESSON: `ops`, `regression_diagnostics`, `import_images`.
The assertion is an equality -- every registered module and every folded host
has exactly one lesson -- so a subset will not do and the three cannot simply
be excused.

WHAT ONE LESSON ACTUALLY COSTS, measured from the catalog:

  * `docs/source/_extra/tutorials/catalog/` holds fourteen files:
    `lessons_en` plus lessons in es, fr, hi, it, ja, pt-BR, zh-CN, and
    captions in da, de, is, ko, nb, sv.
  * `test_every_translated_tutorial_catalog_has_the_english_structure`
    requires every one of them to carry the same lesson ids, numbers,
    routes and SCENE COUNT as English. A lesson added to English alone
    fails that test instead of the one above it -- the count of red tests
    does not fall, it moves.
  * each scene carries `narration`, a `visual` key, `hold_after`, and
    `speech_text` with IPA for the reader -- see
    `test_every_spoken_pypi_is_the_single_word_pypie`, which pins a
    pronunciation across every narration language.
  * `voice_catalog.js` sits beside them; the README describes 73 lessons
    with 50 voices across eight languages.

So three lessons is roughly forty translated scene sets and their audio, not
three JSON objects. It needs the same checkpoints the API catalogs need and it
needs them for narration rather than tooltips.

FOR THE RELEASE CONVERSATION: this is not a gate anybody can close in an
afternoon, and it should be counted with 358 rather than with 288's failure
list. Writing the three lessons in English only would make the suite LOOK one
test better while breaking a different one and shipping a tutorial library
that is complete in one language and short in thirteen.

--------------------------------------------------------------------------------
2026-09-09 -- CODEX OWNS THE TUTORIALS. NEITHER CLAUDE SESSION TOUCHES THEM.
--------------------------------------------------------------------------------

The maintainer started a Codex session on the tutorials this morning and told
this session plainly not to touch them. That settles an ownership question
that had drifted: the other Claude session had taken the three missing
lessons an hour earlier, on the strength of owning the only machine with the
translation models. It has been told to drop it.

OUT OF BOUNDS for both Claude sessions until he says otherwise:

    docs/source/_extra/tutorials/**     lesson and caption catalogs
    spacr/qt/tutorial/**                the engine
    docs/tutorial/, docs/resources/tutorial/
    this instruction, and the tutorial half of 384

WHAT THIS DOES TO 288, and it is the reason this note is here rather than
only in a message: 288's LAST failing test is
`test_every_module_and_fold_has_exactly_one_current_tutorial_route`, and it
fails because ops, regression_diagnostics and import_images have no lesson.
That test now belongs to CODEX. 288 sits at one failure that neither Claude
session may fix, so nobody should read it as work either of them is about to
pick up.

WHAT CODEX INHERITS, so it is not re-derived: a lesson is FOURTEEN catalog
files, not one -- lessons in es, fr, hi, it, ja, pt-BR, zh-CN plus English,
and captions in da, de, is, ko, nb, sv -- and
`test_every_translated_tutorial_catalog_has_the_english_structure` requires
every one to carry the same lesson ids, numbers, routes and SCENE COUNT as
English. Writing the three in English alone makes the route test pass and
breaks the structure test. Each scene also carries `narration`, a `visual`
key, `hold_after`, and `speech_text` with IPA, pinned across languages by
`test_every_spoken_pypi_is_the_single_word_pypie`.

The translation models are at
/mnt/firecuda2/Claude/toxoplasma_projects/tutorials/project/translation_models
on the home machine -- nine opus pairs, m2m100_418M, madlad400-3b and 7b.

--------------------------------------------------------------------------------
2026-09-09 -- FOR CODEX: 109 COMMITS, AND THE BLOCKING TEST HAS NOT MOVED
--------------------------------------------------------------------------------

Written in the file rather than relayed, because two sessions have now tried
to pass this on and neither can confirm it arrived.

`test_every_module_and_fold_has_exactly_one_current_tutorial_route` reports
exactly what it reported this morning:

    Extra items in the right set: 'import_images', 'regression_diagnostics',
                                  'ops'

The work being done is careful -- Gate Editor in 13 locales with all 50
voices checked, a known-geometry stitch verified to exact pixel identities,
Converter frozen across 13 translations. But it is DEEPENING lessons that
already exist, and only one thing closes this test: three new entries in
`docs/source/_extra/tutorials/lesson_catalog.js`, each with an `app_key`
naming a live Home key -- or the exact `host_app_key` when the workflow is
reached through a fold.

This is 288's LAST failing test. Everything else in that instruction is
green.

>>> DO NOT WRITE THE `ops` LESSON YET.

The maintainer moved the OPS button from Align & Stitch to Mask Generation
today -- see 372, which is mid-implementation in another session. A lesson
written now would route to a host OPS is leaving, and the `host_app_key` it
records would be wrong the moment that lands.

`import_images` and `regression_diagnostics` have no such constraint and can
be written immediately.

TWO THINGS THAT WILL COST A RE-RECORDING IF THEY ARE NOT KNOWN FIRST:

  * SETTINGS RENAMES ARE COMING. Seven are approved and queued -- see 364's
    2026-09-09 entry for the table and the modules they touch. Every one
    changes a visible label, so a frame showing that row, or narration
    reading it aloud, is wrong from the moment they land. Regression and Map
    Barcodes are the affected tutorials.
  * RETIREMENTS ARE WORSE THAN RENAMES for a recording: the row disappears
    rather than changing its name.

Neither has landed. The renames wait on the session that owns the modules
that READ those keys.

--------------------------------------------------------------------------------
2026-09-09, LATER -- CODEX IS NOT CLOSED, AND OWNERSHIP RETURNS TO IT
--------------------------------------------------------------------------------

For about half an hour this instruction was handed to the other Claude
session on the grounds that Codex had been shut. It had not: what the
maintainer closed by accident was a session on the WORK machine. Codex runs
on the home machine and never stopped -- its newest tutorial commit is
17:17, after the handover was announced.

The other session has stood down and touched nothing. 358 IS CODEX'S.

WHY IT WAS NOT CAUGHT BY EITHER SESSION, recorded because it will recur:
neither Claude session can see Codex's process. Its COMMITS are the only
signal, and they are ambiguous by construction -- the median gap across its
last nineteen is four minutes, with two eleven-minute gaps already in that
history, so "stopped" and "a normal pause" look identical. A claim about
what a session IS is not a measurement of what it is DOING, and only the
maintainer could tell them apart.

>>> THE `ops` CONSTRAINT, which is the one thing Codex still needs from us:

DO NOT WRITE THE `ops` LESSON AGAINST ALIGN & STITCH. The maintainer moved
the OPS button to Mask Generation today (see 372), and the session
implementing that move is the one that will know which host and which fold
the lesson should name. A lesson written now records a `host_app_key` that
is wrong the moment 372 lands.

`import_images` and `regression_diagnostics` carry no such constraint and
are the two that close 288's last failing test.

--------------------------------------------------------------------------------
2026-09-11 -- THREE REDS THE FULL SWEEP LEFT IN CODEX'S HALF, UNTOUCHED
--------------------------------------------------------------------------------

A full-suite run of 2,754 files at `ae8bdb417` -- 61,904 passed, 28 distinct
failures -- left exactly three in tutorial-owned files. Recorded here rather
than fixed, because this file says 358 IS CODEX'S and because two of the
three are judgements about what a lesson claims, not typos.

  1. `tests/test_the_name_is_spaCR.py::test_no_file_mis_cases_the_name`

         tools/tutorials/authoring/tools/capture_install_web.py:331  SpaCR
         tools/tutorials/authoring/tools/translate_pre_app.py:264    SPACR
         tools/tutorials/authoring/tools/translate_pre_app.py:265    SPACR

     UPDATED 2026-09-11: judgement WAS required, and one of the three
     would have been a defect to "fix". `translate_pre_app.py:265` is

         text = re.sub(r"SPACR[A-Z]*TOKEN", "spaCR", text)

     a REGEX matching an uppercased placeholder the sentence model mangles,
     with the correct spelling already in the replacement. Lowercasing the
     pattern stops it matching, the placeholder is never restored, and every
     affected translation ships with a raw token in it. The naming rule now
     exempts an all-caps placeholder ending in TOKEN, so that line is
     settled and needs nothing from anybody.

     THE OTHER TWO ARE STILL CODEX'S AND STILL NEED A DECISION:

       * `translate_pre_app.py:264` -- prose, "The stable SPACR prefix and
         TOKEN suffix still identify it uniquely". It is naming the token's
         literal text, so the capitals are right and the sentence is what is
         wrong: a code quote around `SPACR` says so and satisfies the rule.

       * `capture_install_web.py:331` -- asserts that GitHub's release page
         renders "SpaCR <version>". If the page really says that, the
         assertion is correct and the PAGE is the mis-cased thing; if it
         says spaCR, the assertion has never matched and the capture has
         been reporting a miss quietly. Either way the fix is not in that
         line alone, and only somebody who can look at the page can say
         which.

  2. `tests/test_docs_media_budget.py::test_narration_is_the_stable_mobile
     _clock` expects `app_v2.js?v=20260910-example-downloads` and the
     player page carries `app_v2.js?v=20260910-plaque-example-zip`. One of
     the two is stale and only the session that changed the cache-buster
     knows which.

  3. `tests/test_tutorial_release_audit.py::test_every_module_and_fold_has
     _exactly_one_current_tutorial_route`.

THE CLAUDE SESSION'S HALF OF THAT RUN IS CLOSED. Of the 28, the ones that
were not tutorial-shaped are fixed: the caption ratchet, four docstring
ratchets, two segfaults, an abort, a teardown error, two stale message
expectations, the module denominator, an undeclared dependency, a phantom
`__all__` export, an assertion-free test, the French reviewed count, a dead
packaging exclusion, three archived installers, and the environment and
app-registry leaks. What is left is these three plus the API catalogs,
which are rebuilding.
