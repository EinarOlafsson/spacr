================================================================================
THE XFAIL BACKLOG IS FIFTEEN UNFIXED BUGS THE LEDGER CANNOT SEE
================================================================================

Status:    filed 2026-08-15. THE ORIGINAL FIFTEEN ARE ALL FIXED (RESULT
           2026-08-16). THE HEADER THAT USED TO STAND HERE -- "eleven of
           fifteen fixed ... four remain: B4, C2, C3, D2" -- WAS ALREADY WRONG
           WHEN IT WAS WRITTEN, and so is the WHAT IS LEFT section it points
           at: B4, C2 and D2 were fixed, and only C3 is correctly described
           there. Read RESULT 2026-08-16 before either of them.

           A SECOND backlog of nine grew afterwards; RE-AUDIT 2026-08-25
           records it. 2026-09-14: FOUR OF THOSE NINE ARE NOW FIXED AND THEIR
           MARKERS DELETED -- remote_execution, validate, hit_attribution and
           qt/widgets/sweep_runs. The other FIVE of the nine were fixed
           elsewhere between 2026-08-25 and today. See 2026-09-14 at the
           end for what survives, and for the two markers the re-audit never
           counted.

           Found while running the CI fast leg locally to pre-verify
           instruction 82.
Requested: 2026-08-15 - "please look through the instructions list then take a
           look at the state of the spacr code, do a sweep (not to deep) and
           tell me what you think."

--------------------------------------------------------------------------------
WHAT THE STATE IS
--------------------------------------------------------------------------------

The fast leg is green: 16345 passed, 8 skipped, 0 failed on py3.10.

It is also carrying FIFTEEN xfails, and every one of them is a written-up,
reproducible defect in shipped code. They pass CI because `xfail` means "known
to fail", so a suite full of them is indistinguishable from a suite with none.

This matters right now because `instructions/open/` was empty when this was
filed, and "0 open / 90 done" was being read as "nothing left". Fifteen real
bugs were sitting one directory away in a form no ledger counts.

An xfail is the correct way to RECORD a defect you have decided not to fix
yet. It is not a way to CLOSE one.

--------------------------------------------------------------------------------
WHY IT MATTERS
--------------------------------------------------------------------------------

The ledger and release summaries count open instructions, not expected test
failures. Without this record, known defects disappear from the release state
while the test command still exits successfully.

--------------------------------------------------------------------------------
THE DEFECTS
--------------------------------------------------------------------------------

GROUP A -- CELLPOSE 3 LEFTOVERS. Four sites pass `channels=` to
`CellposeModel.eval`. Cellpose 4.0.7 logs "channels deprecated in v4.0.1+"
and never reads the value. `spacr.model_compare.IGNORED_ARGUMENTS` already
lists 'channels' as exactly this no-op, and `spacr/object.py:1913` (the SAM
sibling) already omits it, so the correct shape is in the tree twice over.

    spacr/object.py:1251      generate_cellpose_masks   channels=channels
    spacr/object.py:1853      segment_cellpose          channels=[0, 1]
    spacr/submodules.py:421   test_cellpose_model       channels=[0, 0]
    spacr/submodules.py:621   apply_cellpose_model      channels=[0, 0]

Mechanical. Delete the argument; select the planes before the handover, as
object.py:1913 does. The remap that FEEDS the batch is still needed -- only
the kwarg is dead.

GROUP B -- align.py SILENTLY MIXES OR DROPS DATA. Five defects, two of them
verified by hand on 2026-08-15:

  B1. TWO WELLS BECOME ONE SITE  [VERIFIED]
      `_MERGED`'s (?P<plate>[^_]+) cannot match a plate token containing the
      separator, so the last-resort branch reads the WELL token as a channel:

          _parse_name('exp1_plate1_B07_001') -> ('','',1) as channel 1
          _parse_name('exp1_plate1_C08_001') -> ('','',1) as channel 8

      Both key to site ('','',1). `write_stack` composites well C08 into well
      B07's canvas as another plane, and every `align_coordinates` row carries
      an empty plateID/rowID/columnID, so nothing downstream can separate them
      again. Any plate named with an underscore contaminates across wells.

  B2. A 1536-WELL EXPORT KEEPS ONE TILE PER WELL  [VERIFIED]
      `_YOKOGAWA`'s (?P<well>[A-Za-z]?\d+) allows at most one letter, so
      'AA01' never matches; the trailing digits of the CHANNEL token become
      the field, and F001..F004 all key to the same site. Only the last file
      read survives. `spacr.schema.parse_well('AA01') == ('r27','c1')`, so the
      rest of spaCR handles 1536 plates -- only align's regex does not.

  B3. `scan_tiles(reference_channel=N)` is documented as "which channel drives
      registration" and stored as `Tile.channel`, but nothing in align.py ever
      reads `Tile.channel`. `estimate_offsets` uses its own parameter, default
      0. Choosing the reference channel at scan time silently does nothing.

  B4. `align_folder` applies `grid` to the whole folder and splits by well
      afterwards, so a per-well acquisition grid cannot be expressed: two
      wells acquired 2x2 are refused as "grid 2x2 has room for 4 tiles but 8
      were found". `scan_tiles` is called before `group_tiles`.

  B5. `estimate_offsets(max_open_tiles=1)` registers nothing. A pair needs
      both tiles open; `_ReaderCache` evicts and closes tile A while
      `_register_pair` still holds it. The AttributeError is swallowed into
      `PairResult.note`, every tile falls back to its stage position, and the
      run looks like a plate that simply would not register.

GROUP C -- schema.py KEY AND COLUMN HAZARDS. Four defects, one verified:

  C1. `canonicalise_columns` CREATES A COLLISION SQLITE REFUSES  [VERIFIED]

          canonicalise_columns(['row','rowid']) -> ['rowID','rowid']
          to_sql -> OperationalError: duplicate column name: rowid

      SQLite compares identifiers case-insensitively -- this is how a column
      named rowID once shadowed the implicit rowid and made a DELETE remove a
      whole table. The rename guard is exact while the collision it must avoid
      is case-folded. The fix must land in BOTH `schema.canonicalise_columns`
      and `utils.canonicalize_measurement_columns` in one commit, or the two
      frame canonicalisers disagree.

  C2. `compose_prcf(..., time="xy")` writes `p_r1_c1_f1_txy`, which
      `parse_prcf` refuses with "'txy' is not a field id". A preserved token
      round-trips in the FIELD slot but not the TIME slot.

  C3. `parse_field_stem` / `parse_object_stem` read a file name LEFT to right
      at fixed positions while `parse_prcf` reads right to left. So
      `parse_field_stem('my_plate_A01_3')` gives plateID='my', rowID='plate',
      and surplus components are dropped outright:
      `parse_field_stem('plate1_A01_3_junk') == parse_field_stem('plate1_A01_3')`.

  C4. `_sanitise_token` maps field 'a_b' and field 'a-b' onto one id, 'fa-b'.
      Two fields go in, one comes out -- via the escape hatch rather than via
      `_safe_int_convert`. Tier-2 path only, so QC-visible.

GROUP D -- RESOLUTION AND SHIPPED DATA. Two:

  D1. `generate_masks_from_imgs` discards the loader's `orig_dims`: on the
      normalize=True branch it overwrites them with the shapes of the ALREADY
      target-resized images, so "resize back to orig_dims" is a no-op and
      masks are written at target resolution instead of source.
      `identify_masks_finetune` keeps them and restores correctly.

  D2. THE SHIPPED DEFAULTS DO NOT RUN. `barcodes_grna.csv` contains three
      protospacers that each appear under two or three gene names, and
      `map_sequences_to_names` correctly refuses a reference with duplicate
      sequences, so `generate_barecode_mapping` raises ValueError on its first
      chunk with the shipped defaults and nothing else changed. Deciding which
      gene a shared protospacer counts for is a screen-design call, not a code
      change -- this one needs the maintainer, not a commit.

--------------------------------------------------------------------------------
WHAT TO DO
--------------------------------------------------------------------------------

In this order, because it is also increasing order of judgement required:

1. Group A. Delete four dead kwargs. Flip the four xfails to plain passing
   tests in the same commit -- an xfail that now passes is XPASS, which is
   not a green signal either.

2. C1. One commit touching both canonicalisers, with a test that asserts
   `to_sql` succeeds rather than asserting on the column list. Make the guard
   case-insensitive while still letting a column be renamed onto its own
   case-folded name.

3. B1 and B2. Both are cross-well or cross-field contamination and both are
   regex repairs with a clear correct answer. Prefer refusing an unparseable
   name over the last-resort branch that invents a channel from a well.

4. B3, B4, B5, C2, C3, C4, D1. Each needs a small design decision recorded in
   the commit message.

5. D2 is the maintainer's. Ask which gene each shared protospacer counts for,
   or ship a reference with the duplicates removed and say which were dropped.

--------------------------------------------------------------------------------
HOW TO KNOW IT WORKED
--------------------------------------------------------------------------------

`python -m pytest tests/ -m "not integration and not slow and not heavy and
not qt and not gpu and not network and not nas and not gui" -n 4 --dist
loadfile` reports ZERO xfailed and zero xpassed, or reports only xfails whose
reason names a maintainer decision that has been asked and not yet answered.

A defect that has been decided against is closed by DELETING the test and
recording the decision here, not by leaving an xfail behind to be recounted
by the next sweep.

--------------------------------------------------------------------------------
WHAT IS LEFT (2026-08-15, after eleven were fixed)
--------------------------------------------------------------------------------

C3 -- THE REPAIR STATED ABOVE IS WRONG. DO NOT DO IT.

  "Parse the stem right to left like every other key parser" was tried and
  reverted. Two things kill it:

  * ``_check_plate`` REFUSES a plate id containing the separator, because
    ``my_plate_r1_c1_f1`` cannot be split back. So right-to-left parsing
    produces a plateID the composer then rejects -- the property test's own
    expectation, ``plateID == 'my_plate'``, is unsatisfiable by design.
  * spacr/ml.py:3191 already reasoned this out and chose left-to-right
    deliberately: "a prc is a KEY, which has a fixed number of components and
    is therefore parseable right to left; this is a crop FILE NAME, whose
    grammar is <plate>_<well>_<field>[_<time>]_<object> -- a variable-length
    tail with no right anchor, so the well can only be found by counting from
    the left." A file name is not a key, and the xfail's premise that it
    should parse like one is the error.

  THE REAL REPAIR, named in that same comment and marked "NEEDS FOLLOW-UP IN
  spacr/schema.py": fix the WRITER, not the parser. Either make the crop
  writer percent-escape the separator in the plate component -- the escape
  table is now :data:`spacr.schema.KEY_ESCAPES`, shared and reversible, so
  this is a smaller job than it was -- or record the plate-id length in the
  name. Until then a plate id holding '_' shifts every position and the
  grammar carries nothing that can undo it.

  The injectivity half ('plate1_A01_3_junk' == 'plate1_A01_3') would be fixed
  by refusing a stem with surplus components, which is independent of the
  above and is the smaller, safer half if it is wanted on its own. It does
  change 4-part names from silently-wrong to raising, so it is a decision
  about existing pipelines rather than a free repair.

C2 -- unchanged, still the owner's call: making a trailing t-token count as a
  timepoint widens what "timelapse" means for every reader of every database.

B4 -- per-well acquisition grid. Real, and the fix is a restructure rather
  than a patch: scan_tiles assigns grid positions across the whole folder
  before align_folder ever calls group_tiles, so the per-well layout cannot
  be expressed. Group first, then lay each group out.

D2 -- the maintainer's, unchanged. Which gene does a shared protospacer count
  for?

--------------------------------------------------------------------------------
2026-08-15 - AN OPEN ASYMMETRY LEFT BY THE C3 ESCAPE. NEEDS A DECISION.
--------------------------------------------------------------------------------

C3 was fixed by escaping the plate id in the composer, which was the right
call. It left two spellings of one well key in the tree, and they are not
interchangeable:

    schema.compose_prc('exp1_plate2', 'rB', 'c3')  ->  exp1%5Fplate2_rB_c3
    ml.py's three hand-joins                        ->  exp1_plate2_rB_c3

Both parse. `_split_prc` now unescapes, so it reads either one correctly, and
`schema.parse_prcf` always did. Nothing is broken TODAY. But the two are
different strings for the same well, so anything that joins a key from one
source to a key from the other matches nothing, silently -- which is the
failure mode `prc` exists to prevent.

WHY IT WAS NOT JUST FIXED. Routing the hand-joins through an escaping
composer was implemented and reverted, because it changes the `prc` string
WRITTEN INTO DATABASES, and two tests assert the un-escaped form on purpose:

    test_an_underscored_plate_id_keeps_the_regression_qc_tables
    test_a_row_id_carrying_a_plate_whose_name_has_an_underscore

Those tests are not wrong; they encode the older contract, under which an
underscored plate is carried as a FOUR-component key and separated by the
row/column guard in `_split_prc`. The escape carries it as three. Both are
coherent designs. They cannot both be the stored format.

THE DECISION NEEDED, and it is the maintainer's because it is about data on
disk rather than about code:

  (a) ESCAPE EVERYWHERE. One spelling, three components always, and
      `_split_prc`'s four-component path becomes legacy-only. Databases
      written before this hold the old spelling, so it needs a migration or
      a reader that accepts both -- and `database_schema` already has the
      machinery for exactly that kind of rename.

  (b) HAND-JOIN EVERYWHERE. Revert the escape in `compose_prc`, keep the
      four-component key, and rely on the row/column guard. Cheaper today,
      but it is the design C3 was filed against: it cannot represent a plate
      whose name contains BOTH the separator and something that parses as a
      row/column pair.

(a) is the better end state and the more expensive one. Until it is chosen,
do not "tidy" the hand-joins -- that is the revert described above, and it
will look like a cleanup while changing what is written to disk.

--------------------------------------------------------------------------------
DECIDED 2026-08-16: (a). ESCAPE EVERYWHERE. DONE.
--------------------------------------------------------------------------------

The maintainer chose one spelling. `prc` is now always THREE components, and
a plate id holding the separator or a `%` is percent-escaped:

    exp1_plate2 / rB / c3  ->  exp1%5Fplate2_rB_c3

SEVEN hand-joins in ml.py -- not the three first found -- now go through
`_compose_prc_column`, the vectorised counterpart of `schema.compose_prc`.
Verified identical to it on canonical row/column tokens.

ROW AND COLUMN ARE STILL NOT CANONICALISED, deliberately. `compose_prc`
canonicalises them (`row_id('rB')` -> `'r470'`); the hand-joins never did, and
`_split_prc`'s docstring says why -- "a rewritten token would change the
identity rows are joined on". Only the PLATE escape changed.

OLD DATABASES STILL READ, which is what made (a) affordable: `_split_prc`
unescapes, and unescaping is a no-op on a key carrying no escape, so a
four-component legacy key parses exactly as before through the
`is_row_column_pair` guard. Accept both, write one.

ONE TEST CHANGED MEANING RATHER THAN BREAKING, and it is worth knowing:
`test_an_underscored_plate_id_keeps_the_regression_qc_tables` asserted that a
naive `prc.str.split('_')` RAISES on an underscored plate. It no longer does
-- the key is three components now, so the naive split succeeds and returns
the ESCAPED plate id. That is the improvement and the new trap at once, so
the test now asserts both halves: the naive splitter gets a plausible-looking
`exp1%5Fplate1` instead of an error, and only `_split_prc` gives back the
plate the user typed.

--------------------------------------------------------------------------------
RESULT 2026-08-16 - ALL FIFTEEN ARE FIXED. THIS FILE'S OWN STATUS WAS STALE.
--------------------------------------------------------------------------------

THE BACKLOG IS EMPTY. `grep -rn "mark\.xfail" tests/` returns nothing. The
only surviving occurrences of the word in tests/ are prose. There is no
`xfail_strict` in pytest.ini, setup.cfg, pyproject.toml or any conftest, so
the count is not being suppressed by configuration either. The HOW TO KNOW IT
WORKED bar -- "reports ZERO xfailed and zero xpassed" -- is met.

WHAT WAS REFUTED. Every claim in this file about what is LEFT. The header says
"ELEVEN OF FIFTEEN FIXED ... Four remain: B4, C2, C3, D2" and the WHAT IS LEFT
section argues at length about B4, C2 and D2. All three were already fixed
when that section was written or shortly after, each by a commit naming this
instruction. Only C3 is correctly recorded, in the appended DECIDED 2026-08-16
section.

Each of the fifteen was re-checked against the code -- reproduced or refuted
by running it, not by reading it:

  A   FIXED. No `channels=` at any of the four `.eval(` call sites; the two in
      object.py carry comments where the kwarg was.
  B1  FIXED. _parse_name('exp1_plate1_B07_001') -> plate 'exp1_plate1', well
      'B07'; C08 -> well 'C08'. Two sites, not one.
  B2  FIXED. _WELL_TOKEN allows two letters; 'AA01' parses, F001..F004 stay
      four distinct fields, and _well_ids('AA01') == parse_well('AA01').
  B3  FIXED. align.py:1569 reads tile.channel. scan_tiles(reference_channel=3)
      -> plan.reference_channel == 2, against 0 when unset.
  B4  FIXED, CONTRARY TO WHAT IS LEFT ABOVE. align_folder(grid=(2,2)) over two
      wells x 4 tiles returns 2 groups and does not raise. `default_settings`
      carries group_by_well: True (align.py:2479) and passes it into
      scan_tiles, which lays the grid out per well. Commit ec24d0c9. The
      "group first, then lay each group out" restructure this file called for
      is what was done. Calling scan_tiles(grid=(2,2)) DIRECTLY still raises,
      because its own group_by_well default is False -- that is the deliberate
      opt-out and has a test pinning it.
  B5  FIXED. _ReaderCache.MIN_OPEN = 2, so _ReaderCache(1).max_open == 2.
      estimate_offsets(max_open_tiles=1) now gives byte-identical output to
      max_open_tiles=8.
  C1  FIXED. canonicalise_columns(['row','rowid']) -> ['row','rowID'] and
      to_sql succeeds. utils.canonicalize_measurement_columns returns the
      identical list, so the two canonicalisers agree.
  C2  FIXED, CONTRARY TO WHAT IS LEFT ABOVE. compose_prcf(..., time='xy') ->
      p_r1_c1_f1_txy, and parse_prcf reads it back with timeID='txy'. No
      raise. Commit dc21cae0 names this instruction. The owner's call this
      file was waiting on was made.
  C3  FIXED, by the composer escape, exactly as the DECIDED section records.
      Both halves: parse_field_stem('my_plate_A01_3') now RAISES rather than
      silently returning plateID='my', and the writer escapes to
      my%5Fplate_A01_3, which parses back to 'my_plate'. The injectivity half
      is closed too -- 'plate1_A01_3_junk' raises ("expected exactly
      plate_well_field (3 parts), got 4") instead of equalling the 3-part
      parse. Commit 09a4a75d.
  C4  FIXED. _sanitise_token('a_b') -> 'a%5Fb', ('a-b') -> 'a-b'. No collision.
  D1  FIXED. The orig_dims recompute is gone from the normalize=True branch of
      generate_masks_from_imgs (spacr_cellpose.py:334-339) and survives only
      on the else branch; io.py:363 captures orig_dims right after imread,
      before the loader resizes.
  D2  FIXED, CONTRARY TO WHAT IS LEFT ABOVE, AND THE MAINTAINER DID DECIDE.
      The shipped barcodes_grna.csv (1377 rows) now holds zero duplicated
      sequences, and map_sequences_to_names accepts it. Commit cd5a8920,
      "data: drop ambiguous shared guides (instruction 100, D2)", removed 8
      rows. The decision was DROP, not reassign, and the three protospacers
      dropped were:
          GTCGCTAGGACATCCTCCAAG  TGGT1_241310_10 / _411210_10 / _411710_10
          GCCGGCGATAGAGCCCCGCCC  TGGT1_241310_2  / _411210_2  / _411710_2
          GCGATAGAGCCCCGCCCTGG   TGGT1_411210_3  / _411710_3
      Three genes were involved: TGGT1_241310, TGGT1_411210, TGGT1_411710.

EVERY ONE OF THE FIFTEEN HAS A REGRESSION TEST, which is the part that makes
"fixed" durable rather than incidental. 106 of them were run for this check
and all pass. Group A has one test per call site; B1-B5 are in
tests/test_cov_align_naming_and_solve.py; C1-C4 are hypothesis property tests
in tests/test_key_parsing_properties.py; D1 is
test_generate_normalize_resize_restores_source_resolution; D2 is
test_bundled_barcode_path_points_at_a_real_reference_csv, which asserts no
duplicated sequence in any of the three shipped references.

TWO STALE DOCSTRINGS FIXED HERE, both left behind when the markers came off,
and both of which described the defect as still present:

  * tests/test_key_parsing_properties.py said "They are xfail(strict=True) so
    that fixing one turns this file red" of four tests that carry no marker.
  * tests/test_coverage_fill_cellpose_gpu_funcs.py said "Here the mask is
    written at 16x16, so it cannot be overlaid on its own source image" while
    the assertion beneath it requires the 32x32 source.

A comment that still describes a fixed bug is the same failure mode this
instruction is about, one level down: it makes a repaired thing read as
broken, and the next person re-investigates it.

ONE THING THIS FILE ASKED FOR THAT WAS NOT DONE, recorded so it is not
mistaken for an oversight. Step 3 said "prefer refusing an unparseable name
over the last-resort branch that invents a channel from a well". The
last-resort branch in align.py (~932-941, returns plate='' and well='') still
exists. B1 and B2 no longer reach it, so nothing is contaminated by it today,
but the branch itself was not removed.

CLOSED. Fifteen of fifteen, zero xfails, every one with a test.

--------------------------------------------------------------------------------
2026-08-25 - RE-AUDIT: THE BACKLOG IS BACK. NINE, NOT ZERO.
--------------------------------------------------------------------------------

Run under this file's own marker selection over the ten files that still carry
`@pytest.mark.xfail(strict=True)`:

    -m "not integration and not slow and not heavy and not qt and not gpu
        and not network and not nas and not gui" -p no:randomly

    531 passed, 57 deselected, 9 xfailed

NOT ONE of the nine reasons names a maintainer decision that has been asked
and not yet answered, which is the only exemption this file grants. Every one
names a concrete unfixed defect with a file and a line:

  parameter_sweep.py   run_sweep's per-trial CSV write is not inside the
                       try/except OSError that guards the progress write
  hyperparam.py        builds SVC(probability=True); ml.py fits
                       CalibratedClassifierCV(SVC(...)), so the search tunes a
                       different estimator than the run
  qt/…/sweep runs      delete_runs_from_disk builds `keep` from the DECORATED
                       failure strings, so no plain folder path ever matches
  validate.py:167/239  a plugin alias is registered with '-' folded to '_' and
                       _normalize_app only strips and lowercases
  hit_attribution      candidate_rank / candidate_percentile are ranks OF
                       score_column, so include_original_score=False still
                       leaks the model output
  remote_execution     the timeout message names the requested timeout, not
                       the one that was waited
  model_check.py:174   imports KNOWN_MODELS from spacr.model_zoo, which never
                       exported it, so the ImportError branch always wins
  model_check.py:87    class_names raises ClassDefinitionError out of
                       check_model, documented at line 141 never to raise
  setting_animations   _animation_frames is defined twice (300 and 415); the
                       second shadows the first and drops its guarded imports

These are nine production bugs in nine modules, not one repair, so they are
reported rather than half-fixed here. The rule this file states still stands:
a defect decided against is closed by DELETING the test and recording the
decision, not by leaving an xfail for the next sweep to recount.

--------------------------------------------------------------------------------
2026-09-14 - FOUR OF THE RE-AUDIT'S NINE FIXED. TWO MARKERS LEFT IN THE
WHOLE TREE, NEITHER OF THEM ONE OF THE NINE.
--------------------------------------------------------------------------------

WHAT THIS FILE SAID TO DO, AND WHY IT WAS NOT WHAT WAS DONE. The task handed
out for this pass quoted the Status header -- "four remain: B4, C2, C3, D2".
That is the header this file's own RESULT 2026-08-16 section already refuted,
and it had been left standing for a month. The header is now rewritten. The
four actually carrying markers were four of the NINE from RE-AUDIT 2026-08-25,
in four different modules; the original fifteen were not touched and did not
need to be.

THE RE-AUDIT'S OWN COUNT IS ALSO NOW STALE, in both directions. Re-grepped
2026-09-14, `grep -rn "mark\.xfail" tests/`:

  * Of its nine, FOUR were already fixed and unmarked before today --
    parameter_sweep's per-trial CSV write, hyperparam's SVC/
    CalibratedClassifierCV mismatch, model_check.py:174's KNOWN_MODELS import
    and model_check.py:87's class_names raise. A fifth,
    setting_animations' duplicate `_animation_frames`, is likewise gone. So
    five of the nine closed between 2026-08-25 and today without this file
    hearing about it.
  * TWO markers it did not count exist:
      tests/test_analysis_submodules_real_data.py:153 -- an UPSTREAM cellpose
        defect (bsize=224 gives 28x28 against vit_sam.py:61's 32), carrying
        `@pytest.mark.gpu` and `@pytest.mark.slow`, so it is deselected by the
        marker expression this file's HOW TO KNOW IT WORKED specifies and was
        invisible to the audit that wrote "nine".
      tests/test_cov_w3_8_dnd.py:143 -- `_find_console` searches
        `reversed(visit_order + list(screens))`, so the registered screens are
        consulted before the most recently visited one. A real defect in
        spacr/qt, untouched here because it is outside this pass's files.

A COUNT TAKEN UNDER A MARKER EXPRESSION IS NOT A COUNT OF THE BACKLOG. That
is the same failure this instruction was filed about, one level down: the gpu
marker hid a real xfail from the audit whose whole purpose was to find them.
Any future re-audit of this file should grep, not run.

THE FOUR FIXED TODAY. Each marker was DELETED, not left to XPASS.

  1. remote_execution -- THE TIMEOUT SENTENCE NAMED A TIME NOBODY WAITED.
     `_run_command` passed `max(1.0, float(timeout))` to `subprocess.run` but
     formatted the message from the raw `timeout`, so a 0.1 s request that
     waited a whole second reported "timed out after 0.1s".
     FIX: the floored value is computed once into `waited`
     (spacr/remote_execution.py:96) and that is what both the call and the
     message use (line 113). The floor is documented in the docstring rather
     than left as a bare `max()`.
     TEST: test_a_timeout_message_names_the_time_that_was_actually_waited,
     marker removed, in tests/test_cov_w2_4_remote_execution.py.

  2. validate -- A PLUGIN'S HYPHENATED ALIAS WAS UNREACHABLE.
     Registration folds `-` to `_` (spacr/validate.py:146) while
     `_normalize_app` only stripped and lowercased, so the fold could only
     ever HIDE the spelling the plugin declared.
     FIX: `_normalize_app` (spacr/validate.py:192) tries the exact key first
     (line 208) and falls back to the folded one (line 210). TWO DECISIONS
     ARE RECORDED IN THE DOCSTRING because either could have gone the other
     way: the exact key wins so a table entry genuinely carrying a hyphen
     stays reachable, and an UNKNOWN key passes through in the spelling it
     arrived in rather than the folded one, because the fold exists to reach
     a registered alias and a miss should be reported in the words the caller
     typed.
     TEST: test_a_hyphenated_plugin_alias_resolves_by_the_spelling_it_declared,
     marker removed, in tests/test_cov_w2_3_validate.py.
     STALE LINE NUMBERS: the re-audit cites "validate.py:167/239". The real
     sites are 146 and 192. Both had drifted.

  3. hit_attribution -- THE SCORE OPT-OUT ADMITTED A PERFECT PROXY.
     `build_hit_cell_frame` derives `candidate_rank` and
     `candidate_percentile` from `score_column` ALONE, and neither name
     matches any entry in `_IDENTIFIER_HINTS`, so `include_original_score=
     False` excluded the score and then handed the model a monotone transform
     of it -- without the warning the explicit opt-in attaches. The file's own
     test_the_within_well_rank_of_the_score_is_a_perfect_proxy_for_it already
     asserted the within-well Spearman correlation is exactly 1.
     FIX: the derived columns are named in one place,
     `SCORE_DERIVED_COLUMNS` (spacr/hit_attribution.py:82), and
     `_default_features` treats them exactly as it treats the score itself --
     kept if and only if `include_score` (line 711).
     NOT DONE, DELIBERATELY: they are not added to `_IDENTIFIER_HINTS`. That
     list is matched as a SUBSTRING against every column name, and "rank" or
     "percentile" as substrings would silently drop real morphology features.
     An exact-name list is the narrower instrument.
     NOT DONE, DELIBERATELY (2): the leak guards in `fit_hit_attribution` and
     the candidate path are unchanged. They police an EXPLICIT
     `feature_columns=`, which is the caller's own choice, exactly as
     `score_column` itself is exempted there.
     SECOND CALL SITE FIXED FOR FREE: the candidate classifier's
     `_default_features(..., include_score=False)` was leaking the same two
     columns and now does not.
     TEST: test_the_default_features_exclude_every_transform_of_the_score,
     marker removed, in tests/test_cov_w2_5_hit_attribution.py.

  4. qt/widgets/sweep_runs -- A ROW VANISHED WHILE ITS FOLDER SURVIVED.
     `_delete_folders` appended failures as the pre-formatted sentence
     `f"{folder} ({why})"`, and `_deletion_finished` built `keep` out of those
     strings and compared them against absolute paths. No path ever matched,
     so EVERY row came off the table whether or not its folder went -- leaving
     a finished run on disk that only Reload could find again, under a status
     line saying the opposite.
     FIX: a failure is now a `(folder, why)` PAIR
     (spacr/qt/widgets/sweep_runs.py:237). `keep` is built from the folder
     half, normalised the same way the records are (line 1397), and the
     sentence is composed where it is shown by the new `_format_failures`
     (line 243). `_failed_folder` (line 259) accepts either shape, so a
     pre-formatted entry from an older build still yields a path.
     TEST: test_a_folder_that_will_not_delete_keeps_its_row, marker removed,
     in tests/test_cov_w2_5_qt_sweep_runs.py.

TEN GUARD TESTS WERE ADDED, in tests/test_xfail_backlog_item_100_repairs.py
and tests/test_xfail_backlog_item_100_sweep_runs.py -- two files rather than
one because the Qt half needs `importorskip("PySide6")`, and a module-level
skip put where it would cover the other three modules' guards is exactly the
"a suite full of them is indistinguishable from a suite with none" failure
this instruction is about. They were added because each of the four xfails
carried exactly ONE assertion and each could
be satisfied by a repair that is wrong in a different way. They pin what the
repair must NOT do: that the timeout sentence is not simply hardcoded to "1s";
that an unregistered hyphenated key is not rewritten, and that an exact
hyphenated entry still beats its folded twin; that the score transforms come
BACK when `include_original_score=True`, rather than being deleted outright;
that `SCORE_DERIVED_COLUMNS` still names the columns `build_hit_cell_frame`
actually writes; and that fixing the folder matching did not cost the words
that say WHICH folder would not go.

EVERY ONE OF THE FOURTEEN TESTS WAS PROVEN NON-VACUOUS by mutating the source
and watching it redden -- eleven mutations, each reddening exactly the test it
was aimed at and nothing else. Four of the eleven were re-introductions of the
original defect; the other seven were the plausible-but-wrong repairs listed
above.

WHAT IS STILL MARKED, and it is not nothing:

  tests/test_cov_w3_8_dnd.py:143       spacr/qt/.../dnd.py `_find_console`
                                       consults registered screens before the
                                       most recently visited one.
  tests/test_analysis_submodules_real_data.py:153
                                       upstream cellpose bsize; gpu+slow, so
                                       it never runs in the fast leg.

Neither is in this pass's blast radius. The dnd one is an ordinary unfixed
defect and should be picked up by whoever owns spacr/qt/dnd.py. The cellpose
one is the only marker in the tree that arguably EARNS its xfail under this
file's own rule -- it records a defect in a dependency, not in spaCR -- but
that is a judgement, not a decision anyone has recorded, and it should be
written down one way or the other rather than surviving on the gpu marker.

FIVE FAILURES IN THE VERIFICATION RUN ARE NOT FROM THIS WORK, and the check
that establishes that is worth recording rather than asserting. Running the
102 test files that import these four modules gives:

    5 failed, 1283 passed in 6:39

all five being repo-wide API-SURFACE COUNT RATCHETS --
test_public_docstrings_matches_reviewed_visible_coverage,
test_public_docstrings_exclude_the_exact_non_rendered_autoapi_boundary,
test_public_callable_inventory_is_source_derived_not_docstring_derived,
test_callable_boundary_is_cross_checked_with_i18n_extractor and
test_generated_constructor_ivar_reduction_is_exact_and_rendered.

Reverting all four source files to HEAD and re-running those five gives
BYTE-IDENTICAL numbers -- 10530 public docstrings, 8695 public callables, 43
required ivars, against reviewed baselines of 10478, 8651 and 38. So this
work contributes ZERO to every one of them; everything added here is private
(`_format_failures`, `_failed_folder`) or a module constant. The ivar failure
names `spacr.curation_queue.QueueItem`, a module that does not exist in HEAD
at all, which places the cause in another change in flight in the same tree.
The counts also MOVED between two runs twenty minutes apart (10526 -> 10530,
8699 -> 8695), which a single settled tree cannot do.

WHOEVER LANDS THOSE OTHER CHANGES OWNS THE RATCHET BUMP. It is named in the
assertion message: bump the count, say what it admitted, and regenerate with
tools/build_documentation_i18n.py.

--------------------------------------------------------------------------------
2026-09-14 - ADVERSARIAL REVIEW OF THE FOUR REPAIRS. ONE VACUOUS ASSERTION
FOUND AND CLOSED; NOTHING ELSE.
--------------------------------------------------------------------------------

All four repairs re-verified against the code rather than the summary above,
with eight independent mutations (the original defect re-introduced in each of
the four modules, plus fold-unconditionally, hardcode-"1s", drop-the-derived-
columns-outright, keep-built-from-the-whole-pair, and drop-the-reason-from-the-
sentence). Every one reddened exactly the test it was aimed at, and all four
source files were restored byte-identically afterwards (sha256 verified).

THE ONE CORRECTION. "Every one of the fourteen tests was proven non-vacuous"
was one test short.
`test_every_score_derived_column_is_one_build_hit_cell_frame_writes` iterated
`SCORE_DERIVED_COLUMNS` and asserted nothing else, so emptying that tuple --
the single mutation that reopens the leak while leaving the constant in place
-- made it pass on an empty loop. It now pins the membership of the tuple
before iterating it, and reddens on that mutation.

ALSO CHECKED, AND CLEAN. The five repo-wide ratchet failures are confirmed
foreign to this work by construction, not only by the revert: the extractor's
own `public_docstrings()` and `_additional_assignment_docs()` were called
directly and neither carries `spacr.hit_attribution.SCORE_DERIVED_COLUMNS` or
either new private function, so this change cannot move any of the three
counts. The docstring total also moved 10530 -> 10531 with nothing of this
item's touched, which is the concurrent-tree effect the section above records.
The ledger, suite-hygiene, `__all__` and module-documentation meta-suites all
pass (616 tests), and the 106 test files importing the four modules give 5
failed / 3866 passed -- the same five, and nothing else.

ONE RESIDUAL, OUT OF SCOPE AND NOT A DEFECT OF THIS CHANGE. `_normalize_app`
is not the only place a caller's app key meets `APP_ALIASES`. `ports.py:468`
(`module_ports`, whose docstring promises "every alias APP_ALIASES accepts"),
`ports.py:556` and `chaining.py:427` all still do a bare
`APP_ALIASES.get(key, key)`, so a plugin's hyphenated alias resolves through
validate and not through those three. Same defect, three call sites, in files
outside this pass.
