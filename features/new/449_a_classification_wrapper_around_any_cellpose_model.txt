================================================================================
A CLASSIFICATION WRAPPER AROUND ANY CELLPOSE-SAM MODEL: PARASITES PER VACUOLE,
AND WHAT EACH OBJECT ACTUALLY IS
================================================================================

Status:    OPEN, filed 2026-09-20. Not started. Four questions below need the
           maintainer before the classifier can be trained; the wrapper
           itself does not wait on them.

Requested: 2026-09-20 -- "it would be cool to have a cellpose model that
           detects toxo then detects how many nuclei are in each PV. so this
           would be 2 models (i already have the toxo pv detection model and
           the toxo from cellmask and hoechst), now i need to make a model
           that detects how many nuclei are in each pv. this should be a
           classefication modual that can take wrap arround any cellpose sam
           model and then cut out each pv and classify it as a 1,2,4,8,16,
           >16 PV. so for this we will need training data to train the
           classefication model. i also want a simmilar classification model
           wrapper that wrapps arround any cellpose sam model and classifies
           every object as a cell, nucleus, cut cell, cut nucleus, artifact.
           if the object is cut in half there should be an option to try to
           merge the two halfs. the code for this should be able to wrap
           cellpose so it takes a model and cellpose options, makes the mask
           then classifies the mask (should also be an option to remove
           artifacts from the mask) the code should alos be able to just take
           the output of cellpose and do the classefication."

WHAT THIS IS
--------------------------------------------------------------------------------

ONE SEAM, TWO HEADS. The seam is a wrapper that turns "segment" into
"segment, then say what each object is". It has two ways in, and the request
names both:

  1. MODEL IN: a Cellpose-SAM model and its options go in, the wrapper
     segments, then classifies every object of the mask it just made.
  2. MASK IN: a mask Cellpose already produced goes in and is classified as
     it stands. Nothing is re-segmented.

The heads are the classifiers that sit on that seam. Two are asked for:

  A. PARASITES PER VACUOLE. Each PV is cut out and put in one of
     1, 2, 4, 8, 16, >16. This is the replication read-out -- Toxoplasma
     divides by binary fission inside the vacuole, so those classes are the
     series that division produces rather than an arbitrary binning.
  B. WHAT THE OBJECT IS: cell, nucleus, cut cell, cut nucleus, artifact.
     With two options on top: try to merge the two halves of something cut
     in half, and remove what the head calls an artifact from the mask.

THE TWO MODELS THE FIRST HEAD SITS BEHIND ALREADY EXIST and are in the Model
Zoo: `cpsam_v2_toxo_r6` (PV segmentation) and the two cross-channel models,
`toxoplasma_from_cellmask_pv` and `toxoplasma_from_hoechst_pv`. This item
adds the SECOND model -- the classifier -- and the wrapper that runs the
pair as one operation.

WHAT ALREADY EXISTS AND SHOULD BE USED RATHER THAN REBUILT
--------------------------------------------------------------------------------

  * `spacr/crop_source.py::crop_object(array, mask, label, ...)` cuts one
    object out of a merged stack, either as its bounding box or with
    everything outside the object zeroed, and resizes to a fixed size. That
    IS "cut out each PV"; the classifier's input tensor is a batch of these.
  * `spacr/deep_spacr.py::train_model` trains a torch classifier on crops,
    and `spacr/classify.py`, `classifier_quality.py` and
    `classifier_evaluation.py` already score one. A new architecture is not
    needed for six classes of small crops.
  * `spacr/_segmentation_backends.py` is where a model becomes something
    with an `eval`. The wrapper should take THAT object rather than a
    Cellpose class, so it wraps Cellpose-SAM, Cellpose 3, DINOCell and
    SAMCell without knowing which it has -- items 404, 405, 423 and 446 made
    them all answer the same call.
  * `skimage.segmentation.clear_border` is already used in `utils.py` to
    drop objects touching the edge, and `measure.py`'s
    `merge_edge_pathogen_cells` already resolves a parasite straddling two
    cells. Both are prior art for the "cut" and "merge the halves" questions
    below, and whoever builds this should read them before inventing a third
    rule.

WHAT TO BUILD
--------------------------------------------------------------------------------

  1. THE WRAPPER. One module. Given (a) a segmentation backend and its
     options plus an image, or (b) an image and a mask, it produces the
     mask, the per-object crops, a class and a probability for every label,
     and a table keyed on label id. It must not change the mask unless
     asked: removing artifacts is an option, not the default, and the
     original labels are kept so nothing downstream loses object identity
     (`canonical_labels` in `mask_engine.py` says why that matters).
  2. THE TWO HEADS, as separate trained models behind one interface, so a
     third head can be added without touching the seam.
  3. ARTIFACT REMOVAL, as an option that returns a NEW mask and says how
     many objects it dropped, rather than editing in place silently.
  4. MERGING TWO HALVES, as an option, with the rule written down and a
     measurement of how often it is right. See the open question.
  5. TRAINING DATA. Neither head can be trained without labelled crops.
     What produces them, and who labels them, is the fourth question below.

HOW TO KNOW IT WORKED
--------------------------------------------------------------------------------

  * The same image, segmented then classified, and the resulting mask
    classified on its own, give identical per-object classes.
  * A run with artifact removal off leaves the mask byte-identical to the
    one the backend produced.
  * Object ids survive: a mask that went in with ids 1, 5, 9 comes out with
    1, 5, 9 and a class for each, not renumbered.
  * The PV head is scored against a held-out set of vacuoles that were
    counted by hand, reported per class -- a classifier that is right about
    1s and wrong about 16s is not "accurate", and the class sizes are
    wildly uneven in any real field.
  * The object head is scored the same way, with the artifact class
    reported separately: a false artifact is a deleted object.

OPEN QUESTIONS FOR THE MAINTAINER
--------------------------------------------------------------------------------

  1. WHAT DOES A VACUOLE OF THREE DO? The classes are the binary-fission
     series, but real fields hold vacuoles of 3, 5, 6 and 7 -- parasites
     divide asynchronously and some die. Nearest class, an "other" class,
     or a count regression rounded into these bins?
  2. WHAT DOES THE PV HEAD LOOK AT? The vacuole crop in one channel, or
     several? The nuclei it is counting are Hoechst; the vacuole boundary
     is not. If it needs both, the wrapper has to carry the channel map
     through, which is a different signature.
  3. WHAT IS A "CUT" OBJECT? Touching the image border, or split into two
     labels by the segmenter? And if halves are to be merged, are they two
     halves in the SAME field, or the same object across two fields --
     which needs the stitcher, not this.
  4. WHERE DOES THE TRAINING DATA COME FROM? Hand-annotated crops through
     the Annotate module, the curation queue, or a set you label outside
     spaCR? And how many per class -- the >16 class is rare, and a model
     trained on what a normal plate contains will hardly ever see one.

RELATED. Item 421 already cuts objects and measures them from Make Masks;
item 386 wants a label-free embedding for every object, which is the
unsupervised cousin of this; item 99 explains CV models and resolves hits
back to cells. The replication assay this head serves is the module that
would consume it.

--------------------------------------------------------------------------------
2026-09-20 -- THE FOUR QUESTIONS ANSWERED, AND WHAT THE DATA ACTUALLY IS
--------------------------------------------------------------------------------

THE ANSWERS, through the question prompt.

1. A VACUOLE OF THREE: "Predict the count, bin for display." The head
   predicts how many parasites it sees and 1/2/4/8/16/>16 is how the answer
   is REPORTED, not how it is trained. A 3 stays a 3 in the model and is
   shown in whichever bin the report asks for. Nothing is taught that a
   wrong answer is right, and the binning can change later without
   retraining.

2. WHAT IT LOOKS AT -- and the maintainer replaced the question with a
   design: "there should be different modes, count from hoechst, cell mask,
   or toxo staining (ds red or antibody). so we need to generate a dataset
   where we generate pngs with hoechst, parasite stainin, cell mask as the
   channels. annotate the pngs as the classes, then train the classifier on
   each channel individually so it can count based on any of the 3
   channels."

   So it is THREE HEADS, not one with three inputs: one trained on Hoechst,
   one on the parasite stain, one on CellMask, and the caller picks the mode
   its plate can serve. A plate with no CellMask still counts from Hoechst.
   The crops are generated once, as PNGs carrying all three channels, and
   annotated once; each head then trains on its own channel of the same
   annotated set, so the three heads share their labels and differ only in
   what they are shown.

3. WHAT "CUT" MEANS: both, told apart. An object touching the image border
   and an object the segmenter split into two labels are different classes,
   because only the second can be merged where it stands -- the first needs
   the neighbouring field. The merge option acts only on the second.

4. TRAINING DATA: the Annotate module on crops.

WHAT THE PV DATA IS, MEASURED 2026-09-20 RATHER THAN ASSUMED. The
maintainer asked whether the Toxo PV dataset carries those channels. It
does not.

  * `einarolafsson/toxoplasma-pv-segmentation-dataset` holds 556 image/mask
    pairs -- the set PV rounds 5 and 6 were trained on. One image was
    downloaded and read: shape (1998, 1998), uint16. ONE CHANNEL. The repo
    is 556 images, 556 masks, and three CSVs (`fields.csv`, `folds.csv`,
    `split.csv`); there is no second or third channel anywhere in it.
  * `fields.csv` keys every field by a stem of the form
    `4hMEFRNF213_20250117_122515__plate1_A03_6_1` -- experiment, plate,
    well, field and a trailing channel index. THE STEMS ARE THE WAY BACK:
    the other channels of the same fields exist in the original
    acquisitions, if those are still somewhere.
  * THEY ARE NOT ON THIS MACHINE. Every local mount (/mnt/firecuda2,
    /mnt/einar_12TB, /mnt/wd12tb, /mnt/wd4tb, /mnt/samsung) was searched
    five levels deep for `*4hMEFRNF*` and `*20250117*`: no acquisition
    folder.
  * WHAT IS LOCAL is `/mnt/wd4tb/toxo_channel_check/`, twelve PNGs from
    2026-09-02 -- four fields, each as Hoechst, PV and CellMask. That is a
    channel check, not a training set, but it is proof the three channels
    were acquired for this experiment.
  * THE NAS COULD NOT BE CHECKED. `/nas_mnt` answers a `stat` on its mount
    point and then `ls` returns "No such device": a stale NFS mount. This
    is exactly what hung a session earlier today; `tools/nas_guard.sh`
    returned instead of hanging, which is what it was written for. Whether
    the source plates are on the NAS is unknown until it is remounted.

WHAT THAT MEANS FOR THIS ITEM. The wrapper, the object head and the merge
and artifact options need none of this and can be built now. THE PV COUNT
HEADS CANNOT BE TRAINED until the three-channel crops exist, and producing
them needs the original acquisitions of those 556 fields -- or a fresh set
of fields acquired with all three stains. Everything else about the PV head
-- the count-not-class decision, the three modes, the per-channel training
-- is settled and written above.

--------------------------------------------------------------------------------
2026-09-20 -- THE TRAINING SET: WHERE THE CHANNELS ARE, AND WHERE THE STACKS GO
--------------------------------------------------------------------------------

THE MAINTAINER'S PLAN, given after the note above: "the toxoplasma cross
channel models datasets have all the info we need so just generate the crops
from those datasets. all we need is to merge the datasets again with toxo pv
mask, toxo channel, hoechst and cellmask togetwer in numpy stacks in a merged
folder then we can just pass it the images through the measure modual to
generate the data then i will annotate ... we will need to do one folder per
plate."

That is right about three of the four planes and `tools/build_pv_count_
dataset.py` now builds them. THE PARASITE STAIN IS NOT IN THOSE DATASETS,
and this was measured rather than assumed:
`cross-channel-toxoplasma-from-cellmask/images` is BYTE-IDENTICAL to
`cross-channel-nuclei-from-cellmask/images` -- same sha256 for the same stem
-- so both are the CellMask input. A cross-channel dataset exists precisely
to predict toxoplasma WITHOUT its own stain, so the stain was never in it.

WHAT THAT COSTS: two of the three counting heads can be trained from this
data -- count from Hoechst and count from CellMask. The parasite-stain head
needs the original acquisitions, which are not on any local mount and could
not be looked for on the NAS (see the note above).

WHAT THE STACK IS. Five planes, and the order is the contract because
Measure is told which plane is which by index:

    0  hoechst        cross-channel-cell-from-hoechst/images
    1  cellmask       cross-channel-nuclei-from-cellmask/images
    2  cell_mask      cross-channel-cell-from-hoechst/masks
    3  nucleus_mask   cross-channel-nuclei-from-cellmask/masks
    4  pv_mask        cross-channel-toxoplasma-from-cellmask/masks_pv

The cell and nucleus masks are in it because the SECOND head -- cell,
nucleus, cut cell, cut nucleus, artifact -- needs exactly those, and cutting
the same fields twice would be the only alternative.

WHERE IT LANDS: `<out>/<plate>/merged/<stem>.npy`, with `<out>` given on the
command line and a `fields.csv` beside each plate's folder. The first build
is under `/mnt/wd4tb/spacr_pv_counts/`. NOT on /mnt/firecuda2, which is at
100% with 7 GB free -- the repository itself lives there.

WHAT IS THERE, measured from the index:

    plate1      515 fields    6,642 vacuoles   20.6 GB
    plate2      750 fields   11,158 vacuoles   30.0 GB
    plate3      500 fields    5,073 vacuoles   20.0 GB
    plate4      500 fields    6,126 vacuoles   20.0 GB
    unplated    765 fields    8,754 vacuoles   30.6 GB

3,030 fields and 37,753 vacuoles in all, 121 GB as stacks and about as much
again downloaded. ONE PLATE IS ENOUGH TO START: plate1's 6,642 vacuoles are
already far more than anyone annotates by hand.

A FIELD MISSING ANY PLANE IS REFUSED rather than written with a zeroed one,
and the smoke run proved the guard: the toxo dataset has 3,030 fields and
the other two have 3,029, so `plate1_A01_13_1` has no Hoechst and was
skipped. A stack whose PV mask was silently empty would be measured as a
field with no vacuoles, which is a wrong number rather than a missing one.

VERIFIED on the first stack written: (1998, 1998, 5) uint16, 40 MB, with 292
cells, 306 nuclei and 62 vacuoles in that field.

THE MEASURE SETTINGS the tool prints at the end of a run:

    src                = '<out>/<plate>/merged'
    channels           = [0, 1]
    cell_mask_dim      = 2
    nucleus_mask_dim   = 3
    pathogen_mask_dim  = 4
    save_png           = True

Resumable: a stack already written is skipped and the downloads go through
the Hugging Face cache, which the first build put on
`/mnt/wd4tb/hf_cache` rather than in the maintainer's home.

--------------------------------------------------------------------------------
2026-09-20 -- A QUARTER OF THE FIELDS CANNOT BE STACKED, AND THE COUNTS ABOVE
              WERE WRONG
--------------------------------------------------------------------------------

The note above quotes 3,030 fields and 37,753 vacuoles from the PV dataset's
own index. THOSE ARE NOT THE NUMBERS THIS CAN PRODUCE, and the build found
it out rather than the plan: plate1 started skipping fields for "no hoechst"
far more often than the one-field difference between 3,030 and 3,029 could
explain.

MEASURED, by intersecting the three datasets' file listings:

    hoechst      3,029 fields
    cellmask     3,029 fields
    pv           3,030 fields
    IN ALL THREE 2,329 fields

So the two 3,029s are not the SAME 3,029. 701 fields -- 23% of the PV set --
are missing a plane somewhere, and cannot become a five-plane stack at all.

WHAT IS ACTUALLY THERE:

    plate1      395 usable fields   5,716 vacuoles   15.8 GB
    plate2      548 usable fields   8,767 vacuoles   21.9 GB
    plate3      417 usable fields   4,669 vacuoles   16.7 GB
    plate4      405 usable fields   5,476 vacuoles   16.2 GB
    unplated    564 usable fields   6,480 vacuoles   22.6 GB

2,329 fields and 31,108 vacuoles, 93 GB rather than 121 GB. Still far more
vacuoles than anyone annotates by hand, so nothing about the plan changes --
but a number that is wrong by 6,645 vacuoles is worth correcting rather than
leaving in a file somebody plans against later.

THE TOOL NOW ASKS FIRST. `fields_in_every_dataset()` intersects the three
listings once, up front, and the run works from that -- so a build neither
spends a lookup per missing field nor reports a plate's size as if every
field in the index could be built. `--list-plates` prints usable fields, not
indexed ones.

The refusal that found this is the one written in the note above: a field
missing any plane is refused rather than written with a zeroed one. It was
put there to stop a silently empty PV mask being measured as a field with no
vacuoles, and it caught a dataset-coverage gap instead.

--------------------------------------------------------------------------------
2026-09-20 -- THE STACKS HAD NAMES MEASURE COULD NOT READ, AND TWO PLATE1s
--------------------------------------------------------------------------------

The plan was "pass it the images through the measure modual". A trial run
over the first three stacks failed all three:

    spacr.schema.ObjectTableSchemaError: cell.prcf disagrees with its
    component identity columns; examples: [{'stored': 'error',
    'expected': 'error_error_error_error'}]

Every identity column read `error`, because `schema.parse_field_stem` could
make nothing of the dataset's own stem:

    CSA_screen__screen_20250124_133156__plate1_A02_1_1

It wants `<plate>_<well>_<field>` and it splits on underscores, so the
experiment and the acquisition in front of the plate are not extra context
-- they are extra fields, and the parse fails.

AND THE EXPERIMENT COULD NOT SIMPLY BE DROPPED, which is the part worth
recording. TWO DIFFERENT EXPERIMENTS IN THIS DATASET BOTH HAVE A `plate1`:
`CSA_screen` and `MTOC_Screen`. `plate1_A02_1` would have handed Measure
two different fields under one identity. The experiment is folded into the
plate id with a HYPHEN, which the parser keeps whole:

    CSAscreen-plate1_A02_1    -> plate=CSAscreen-plate1  r1  c2  f1
    MTOCScreen-plate1_G13_9   -> plate=MTOCScreen-plate1 r7 c13 f9

The folders follow the same rule, so a folder is one experiment's one
plate. THE 614 STACKS ALREADY BUILT WERE MIGRATED RATHER THAN REFETCHED,
and the migration is the evidence for the collision: what had been one
`plate1` folder came apart into 152 CSA fields and 243 MTOC ones.

PROVED END TO END on three renamed stacks, which is what the first trial
could not do:

    Progress: 3/3, operation_type: Measure and Crop
    Successfully completed run
    measurements.db: cell 200, nucleus 207, cytoplasm 200, pathogen 15,
                     png_list 15
    15 vacuole PNGs under data/.../pathogen_png/

`png_list` is the table Annotate reads, so that is the annotation set
arriving. Item 377's report fired in the same run without being asked for
-- `Infection report: .../measurements/infection_report.csv`, infection
rate 0.099 over 81 segmented host cells -- which is the first time that
code has run outside its own tests.

ONE THING SEEN AND NOT YET EXPLAINED: three fields yielded 15 vacuoles,
about 5 per field, where the index's own counts average 14.5 per field for
this plate. It may be these three fields, or it may be a filter in the
measure settings. Worth a look before the whole plate is cut, and recorded
here rather than discovered later.

--------------------------------------------------------------------------------
2026-09-20 -- THE DEFAULT SIZE FLOORS THREW AWAY 91% OF THE VACUOLES
--------------------------------------------------------------------------------

The note above recorded, unexplained, that three fields yielded 15 vacuoles
where the index averages 14.5 per field. Measured rather than left:

    PV objects in the three masks   162
    rows measured                    15

91% of them never reached the table. THE CAUSE IS `get_measure_crop_settings`
DOING EXACTLY WHAT IT IS FOR, on a job it was not written for:

    pathogen_min_size = 500 px   keeps 27 of 62 in one field
    cell_min_size    = 8000 px   keeps 89 of 292 in the same field

Those floors are right for the screening pipeline they were tuned on, where
a pathogen below 500 px is debris and a cell below 8,000 px is a fragment.
THEY ARE WRONG FOR THIS JOB, because the thing being cut out IS the small
object: the median vacuole in that field is 407 px and the median cell is
5,685 px, so both floors sit above the middle of their own distributions.
A vacuole holding ONE parasite -- class "1", the most common class there is
-- is the smallest thing on the field and the first thing a floor removes.

WITH THE FLOORS AT ZERO, same three fields, same everything else:

    rows measured   15 -> 153     (153 of the 162 in the masks, 94%)
    png_list        15 -> 153
    per field    ~19 s -> ~100 s at n_jobs=2

The nine that still do not appear are worth someone's attention eventually;
they are not worth holding the annotation set for.

WHAT THIS CHANGES ABOUT THE PLAN. At ~153 vacuoles per field, a WHOLE PLATE
IS NOT NEEDED and was never needed:

    20 fields  ~3,000 vacuoles      60 fields  ~9,200 vacuoles
    40 fields  ~6,100 vacuoles     395 fields ~60,000 vacuoles

Six classes do not need sixty thousand examples, and nobody annotates them.
Sixty fields of CSAscreen-plate1 are being cut now, at n_jobs=8 on this
32-core box, into

    /mnt/wd4tb/spacr_pv_crops/CSAscreen-plate1/

with the stacks symlinked rather than copied, so the crops cost their own
size and nothing more. The rest of the five-plate build still finishes --
it is the raw material for the OTHER head and for whatever second pass the
counting heads need -- but the annotation set does not wait for it.

THE SETTINGS THE CROP RUN USES, recorded because the next person will
otherwise inherit the defaults and the 91%:

    channels           [0, 1]
    cell_mask_dim      2      nucleus_mask_dim 3      pathogen_mask_dim 4
    crop_mode          ['pathogen']
    png_size           [224, 224]
    pathogen_min_size  0      cell_min_size    0
    nucleus_min_size   0      cytoplasm_min_size 0

--------------------------------------------------------------------------------
2026-09-20 -- THE ANNOTATION SET EXISTS: 8,882 VACUOLES
--------------------------------------------------------------------------------

    /mnt/wd4tb/spacr_pv_crops/MTOCScreen/          189 MB
        measurements/measurements.db               png_list 8,882 rows
        data/.../pathogen_png/                     8,882 PNGs

Cut from the 485 fields of MTOCScreen-plate1 and plate2 with the size
floors at zero. 87% of the 10,174 the index counts, which is the expected
yield once edge and parentless objects are gone -- the trial fields gave
94% and these two plates are denser.

WHY THE MTOC PLATES AND NOT THE ONES CUT FIRST. Sixty CSAscreen-plate1
fields were cut first, for no better reason than alphabetical order, and
gave 380 crops. The per-plate distributions say why that was the worst
choice available:

    MTOCScreen-plate2   242 fields   5,451 vacuoles   median 18 per field
    MTOCScreen-plate1   243 fields   4,723 vacuoles   median 15
    CSAscreen-plate1    152 fields     993 vacuoles   median  2

The CSA screen has a median of TWO vacuoles per field and 35 of its 152
fields have none at all. The MTOC screen is where the parasites are. The
380 CSA crops are kept -- they are valid and they are a different screen,
which is worth something to a classifier -- but they are not the set.

AND A NUMBER IN THE NOTE ABOVE WAS WRONG. It said ~153 vacuoles per field
and planned from it. That came from three trial fields that happened to be
rich; the real mean for that plate is 6.5 and the median 2. The same note's
"plate1: 395 fields, 5,716 vacuoles" was also pre-split -- that folder was
CSA, MTOC and THP1 mixed under one plate name, which is exactly what the
renaming pass separated. Both numbers are corrected here rather than left
for somebody to plan against.

WHAT IS LEFT ON THIS ITEM is the maintainer's: annotate those 8,882 crops
into the classes. Then the Hoechst head and the CellMask head can be
trained from the same annotated set, each on its own channel of the same
crops, which is the design he asked for. The parasite-stain head still has
no data anywhere reachable.
