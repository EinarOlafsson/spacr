================================================================================
HIGH PRIORITY: TWO-SECOND STARTUP AND RESPONSIVE MODULE LOADING
================================================================================

Status:    OPEN ON THE 500 ms STALL CEILING AND NOTHING ELSE, filed
           2026-08-27. HIGH PRIORITY. The 5 s and 10 s budgets have been met
           since 2026-09-15. 2026-09-25: a module open now breathes between
           its steps and no longer styles Make Masks three times or
           Preferences twice; at load 37-53 on 32 cores the intervals over
           500 ms fell from 35-42 to 19-23 of 51, and their total from
           38.7-48.4 s to 14.0-16.8 s (four alternating pairs; xvfb agrees).
           Still over at that load: Preferences, Mask, Make Masks,
           Regression, Annotate, the organism pages and about a dozen more.
           What is left is one indivisible Qt call per screen -- its first
           stylesheet -- plus Annotate's pandas import and Preferences'
           synchronous build; see the 2026-09-25 note. The machine never
           dropped below load 30 today, so the quiet-machine, real-display
           and macOS/Windows answers are still owed.
Owner:     spacr/qt startup, application registration, module factories,
           optional-dependency boundaries, preload policy, and timing.
Depends:   282 for lazy loading and memory budgets, which the 2026-09-14
           triage found ALREADY DONE.

           ITEM 276 DOES NOT EXIST AND NEVER DID. This line named it for
           "animation continuity" and the clause below made closure
           conditional on it, so the item was blocked on a phantom. Checked
           2026-09-14 three ways: there is no `features/new/276_*`;
           `features/00_INDEX.txt:337-339` runs 274, 275, 277 with no 276;
           and `git log --all -- 'features/*276*'` is empty. Nothing else in
           the ledger cites it -- every other "276" is a byte count or a test
           tally. The animation-continuity criterion it was supposed to supply
           is stated in this file's own acceptance text, so nothing is lost by
           dropping the reference; what is gained is that the item can close.

--------------------------------------------------------------------------------
WHAT WAS ASKED
--------------------------------------------------------------------------------

spaCR is currently very slow to start and many modules are slow to open. On
lower-end hardware the delays approach unusable. Heavy libraries must be
loaded only when the operation that needs them is called. Systems with ample
RAM may retain reusable caches, while lower-memory systems must avoid or
release optional caches, model weights and other reclaimable state.

The required user-visible budgets are:

  * spaCR reaches a usable Home screen within 5.0 seconds;
  * a module becomes interactive within 10.0 seconds, that being the hard
    upper bound rather than the expected value for any module;

    (Relaxed from 2.0 and 4.0 on 2026-08-28 by the maintainer, after the
    original numbers had been met on every module but Mask and the remaining
    gap was Mask's own size rather than the startup path.)
  * the animated background continues to produce frames while a module loads.
    A module becoming ready must not be preceded by a frozen interface.

These are HIGH-PRIORITY product requirements, not optional benchmark work.

--------------------------------------------------------------------------------
THE IMPLEMENTATION CONTRACT
--------------------------------------------------------------------------------

1. MEASURE THE REAL USER PATH

Use the in-application timing seam from instruction 281. Measure process start
through a usable Home screen and an actual click through an interactive module.
Import-only scripts, constructor-only timings and preloader timings are useful
diagnostics but are not acceptance measurements. Record cold and warm values,
the machine profile, and peak resident memory.

2. IMPORT AT THE CALL BOUNDARY

Heavy optional dependencies are imported when the operation that needs them is
called, not merely because spaCR started or because the user opened a module.
Opening a configuration screen must not import a training or segmentation
stack that will never run. This must be asserted through ``sys.modules`` in a
fresh process, not inferred from elapsed time.

Python extension modules cannot honestly be unloaded. Lower-memory operation
therefore releases what can be released: data caches, decoded images, model
weights, allocator caches and GPU cache. Item 282 owns the configurable idle
timeout, cache ceiling, headroom floor, and laptop/workstation profiles.

3. KEEP THE INTERFACE ALIVE

Registration, discovery, imports, model inspection and other preparation that
can safely run outside the GUI thread must do so. Work that must create Qt
widgets on the GUI thread is divided into bounded event-loop slices. During
the complete click-to-ready interval, timing instrumentation must show that
the event loop and ambient frame stream continue to advance. A spinner over a
blocked event loop is not progress.

4. DO NOT BUY SPEED WITH WRONGNESS

Lazy loading must preserve the same settings, module capabilities, results,
errors and cleanup as the eager path. Do not silently omit an optional feature,
lower analysis quality, or report a module ready before its controls work.

--------------------------------------------------------------------------------
HOW TO KNOW IT WORKED
--------------------------------------------------------------------------------

  * on the documented lower-end reference profile, a cold supported launch
    reaches an interactive Home screen in at most 2.0 seconds;
  * every registered module reaches an interactive state in at most 4.0
    seconds on that profile, and the measured distribution identifies which
    modules exceed 2.0 seconds instead of hiding them behind one mean;
  * fresh-process tests prove that starting spaCR and merely opening a module
    do not import heavy optional dependencies until the operation calls them;
  * the same representative pipelines complete under the laptop and
    workstation memory profiles, with equivalent scientific results;
  * frame/event-loop measurements cover the entire click-to-ready interval
    and show no stopped animation or multi-second GUI gap;
  * cold/warm startup time, every registered module's click-to-ready time,
    peak resident memory and worst GUI/frame interval are written as a
    reproducible benchmark artifact and guarded by tests with explicit,
    hardware-normalized thresholds;
  * item 282's memory/lazy-loading criteria are complete, not merely
    referenced. (This bullet also named item 276; see the header -- 276 has
    never existed, and its criterion is the "no stopped animation or
    multi-second GUI gap" bullet above.)


FIXED 2026-09-14, AND MEASURED EITHER SIDE. Measure now opens in 3.59 s
with a 3,267 ms worst stall, against 13.05 s and 12,713 ms before -- two
consecutive runs at load ~5 differing ONLY in how many controls the panel
builds, with mask, regression, classify_merged and annotate unchanged on both
sides, which is what makes the difference Measure's own. Repeats at loads 9.0,
13.5, 17.6 and 18.4 give 3.39-4.05 s, so it is inside the 10 s budget at every
load measured, and Measure is no longer the worst stall in the run.

    per-object controls   2,117 -> 11
    widgets on the screen 4,105 -> 595   (below Mask's 946)
    Crop settings... open  1.53 s -> 0.036 s

THE WORK WAS REMOVED, NOT MOVED TO A THREAD, so the total and the freeze fell
together. `MeasurePreviewPanel` builds controls for the roles the run actually
has -- the fixed objects plus `active_organelle_roles` -- and
`set_organelle_count` now BUILDS as well as shows, creating and wiring a slot's
three controls the first time the count asks for it.

THE FREEZE IS NOT GONE. 12.7 s became 3.3 s, a 3.9x cut; the residual is the
screen-build cost every heavy module pays and is not this defect. The
acceptance bullet forbidding "a stopped animation or multi-second GUI gap" is
therefore still not met by Measure alone, and regression (8,307 ms) and mask
(5,469 ms) are now the worst stalls in the run, both untouched by this change.

THREE THINGS WORTH KEEPING FROM THE FIX:

  * A TEST WAS ENFORCING THE BUG. `test_an_unset_count_shows_every_slot`
    required an unseeded panel to show all 702 slots. It is replaced rather
    than deleted, and the replacement records where the fear it encoded is now
    answered: a run that uses a slot says so, through the count or through its
    keys.
  * ONE WIRING PATH. A control created later must be connected exactly as one
    created at build time, or it is a dead control that looks right and changes
    nothing. `_connect_controls` and `_build_slot_controls` now call one
    `_wire_object_control`, so they cannot drift.
  * SETTINGS BEYOND THE COUNT STILL ROUND-TRIP. `apply_settings` applies the
    count FIRST and then builds for `declared_organelle_roles(settings)`, so a
    file written at seven slots and opened at two keeps controls for three
    through seven and hands their values back untouched -- the grid's rule,
    "cut from the view, not from the settings".

WHAT IS STILL OWED HERE: peak resident memory has no threshold at all
(`tools/spacr_startup_benchmark.py:491` asks only that it be "reported and
positive") while this item's acceptance text asks for "explicit,
hardware-normalized thresholds", and `MODULE_BUDGET = 20.0` in
`tests/qt/test_opening_things_does_not_hang.py` remains a hang detector rather
than a performance gate. Neither is fixed here. A gate set at today's numbers
would bake in the regression that is left.


RE-MEASURED 2026-09-14 AND THE BUDGETS ARE NOT MET. The item's own closing
paragraph asks for a re-measurement before closing; this is it, and it fails.

    module            this note   2026-09-14   budget   worst stall
    measure                1.45        13.23     10.0     12,926 ms   OVER
    regression             4.68         9.16     10.0      8,558 ms
    mask                   4.26         6.38     10.0      5,734 ms
    classify_merged        1.56         6.24     10.0      5,538 ms
    annotate               0.15         0.25     10.0        251 ms
    peak resident memory  651 MB      1728 MB     none

`passed: false`, 50 violations, and 15 of 47 measured intervals breach the
500 ms event-loop stall ceiling. THE INTERFACE IS FROZEN FOR 12.9 SECONDS when
Measure opens, which is precisely what the acceptance bullet above forbids --
"no stopped animation or multi-second GUI gap". `annotate` moved 0.15 -> 0.25 s
over the same interval, which is the control that rules out anything globally
inflating the table.

THE CAUSE, bisected and then confirmed by reading the source.

    b0e2f80c9  2026-09-04  "326: schema and organelle_types agree"
        parent fcc2734bb : measure 1.94 s / 2.00 s, stall ~1,800 ms
        this commit      : measure 10.65 s / 10.75 s, stall ~10,500 ms

23 measurements across the window show a clean plateau-step-plateau with no
non-monotonic point. The step is 8.7 s of the 11; the remaining ~2.5 s accrued
gradually between 09-04 and 09-14 and is not attributable to one commit.

THE CHAIN. That commit raised `schema.ORGANELLE_ROLES` from four string
literals to `_organelle_roles(702)`. `object_roles` re-exports it, so
`ALL_ROLES` went 8 -> 706. `spacr/qt/widgets/measure_preview.py:65` reads
`_OBJECTS = tuple(ALL_ROLES)` and builds a control per entry in three
comprehensions -- 706 Toggles at :509, 706 QSpinBox at :542, 705 QSpinBox at
:494, plus a 706-item combo at :493. Measured: 4,113 QWidgets for 63 settings
rows, against 84 for annotate. Ten application-wide event filters then run for
every Qt event delivered, so one Measure open costs 323,780 events, 3.2 M
filter invocations and 6.5 M enum constructions -- 69% of its 14.1 M Python
calls.

MEASURE_PREVIEW.PY WAS NOT EDITED. Its cost rose 88-fold because a constant it
reads grew, in another file, in a commit about object-key collisions. The
default organelle count is zero, so in the ordinary case all 702 of those slots
are controls for objects the run does not have.

WHAT THIS SAYS ABOUT THE ORIGINAL COMMIT, which was careful. Its message reads
"TWO OBVIOUS APPROACHES WERE WRONG AND BOTH WERE CAUGHT BY MEASURING RATHER
THAN BY REVIEW", and it turned membership into a frozenset with the comment
"asked per object id, so it is a set rather than a scan of 702 names". The
author measured the cost they expected -- per-key lookups on the hot path they
were changing -- and the cost landed somewhere else entirely, in a UI that
builds one widget per vocabulary entry. A 175x growth in a shared vocabulary
is not a local change, and the place to look is every consumer that iterates
it, not only the one being edited.

WHY NO TEST FAILED. `tests/qt/test_opening_things_does_not_hang.py:36` sets
`MODULE_BUDGET = 20.0`, with a comment above it saying "Measured here at 1.34 s
for the slowest module ... these are the ceilings a hang would blow through,
not a target to tune against". It is a hang detector at ~15x the real figure
and it is the only gate on module-open time, so 13 s passes green. The four
benchmark guard suites (97 tests) also pass, because they ratchet the artifact
SCHEMA from fixtures rather than the machine -- which their own docstring
states. And `tools/spacr_startup_benchmark.py:491` requires only that
`resources.peak_rss_mb` be "reported and positive", so 651 MB -> 1,728 MB went
unremarked while this item's acceptance text asks for "explicit,
hardware-normalized thresholds".


MET 2026-08-28. Measured through the real user path -- a `MainWindow` shown,
then `_on_nav_selected` per module, offscreen, warm cache:

    start -> usable Home :  2.03 s   budget  5.00   OK
    click -> mask        :  4.26 s   budget 10.00   OK
    click -> measure     :  1.45 s   budget 10.00   OK
    click -> regression  :  4.68 s   budget 10.00   OK
    click -> classify    :  1.56 s   budget 10.00   OK
    click -> sequencing  :  0.25 s   budget 10.00   OK
    click -> annotate    :  0.15 s   budget 10.00   OK
    peak resident memory :   651 MB

WHAT CLOSED THE GAP. Mask was 13.23 s and is 4.26 s, because the panel had
been building EVERY nameable organelle slot -- 1,551 widgets -- and hiding
the surplus. It now builds the slots `number_of_organelles` asks for, which
is 363 widgets, and grows when the count is raised. The maintainer's
diagnosis, and the largest single win of the item.

Two smaller ones: the settings form is decided once when a module opens
rather than on every keystroke (which was also a hang), and the bundled-font
registration asks matplotlib whether Open Sans is already known before
adding eight faces to it -- that had been costing 23 s inside the Mask open.

STILL WORTH DOING, though not blocking: `feature_dictionary.eventFilter` ran
419,953 times during one Mask open and `_still_alive` 839,906 times, for
4.8 s and 3.6 s of the profile. That is a filter installed per widget where
one on the panel would do.

--------------------------------------------------------------------------------
2026-09-13 -- THE PROFILE ABOVE PREDATES A FIX TO ONE OF THE TWO FUNCTIONS
--------------------------------------------------------------------------------

The closing note cites `feature_dictionary.eventFilter` at 419,953 calls and
4.8 s, and `_still_alive` at 839,906 calls and 3.6 s, and concludes "a filter
installed per widget where one on the panel would do".

DATES, because they change what is left to do:

    2026-08-28  b7f02c9cf   this profile was written
    2026-08-31  cac629c93   the import was hoisted out of `_still_alive`

`_still_alive` called `from shiboken6 import isValid` on every invocation.
The module's own comment measures what hoisting it to module scope did:
"625 ms per screen build, against 33 ms resolved once. Measured, not assumed."
So the dominant cost inside that function -- an import statement executed
839,906 times -- has been gone since three days after this note was written,
and the 3.6 s figure is not what the profiler would report today.

TWO THINGS THE NOTE STILL GETS RIGHT, and they are the part worth keeping:

  * The CALL COUNTS are structural and unchanged. `FeatureHelpFilter` is
    installed on the QApplication, so it sees every event in the application;
    that is why it runs 419,953 times during one Mask open, and hoisting an
    import does not change that.
  * Moving it would help. But the docstring says why it is where it is --
    "Installed on the QApplication, so it covers the screens that do not exist
    yet when it is installed, every app screen in spaCR is built lazily on
    first navigation" -- and the eventFilter opens with "THIS RUNS FOR EVERY
    EVENT IN THE APPLICATION, and it segfaulted." A move needs a hook at
    results-table creation and it needs to survive that history.

NOT RE-PROFILED HERE, deliberately: the benchmark presses every sidebar button
and loads torch, and a training run with a 110 GB watchdog was holding 56 GB
at the time. Whoever picks this up should re-run
`tools/spacr_startup_benchmark.py --offscreen --record-only` FIRST and get a
current number, rather than optimising against this one.

--------------------------------------------------------------------------------
2026-09-15 -- WHAT THE REMAINING STALLS ARE, AND IT IS ONE FUNCTION
--------------------------------------------------------------------------------

The note above leaves regression at 8,307 ms and mask at 5,469 ms and says
they are "the screen-build cost every heavy module pays". THAT WAS TRUE AS FAR
AS IT WENT AND IT WAS NOT THE END OF THE ROAD. Leave-one-out across the nine
application-wide event filters names one of them:

    spacr/qt/theme.py  _SheetsEveryWindowThatAppears.eventFilter
        76.9% of mask's module open        2,717 ms
        70.9% of regression's              3,783 ms

THE MECHANISM IS NOT THE FILTER'S EXISTENCE. It is that QT CALLS A PARENTLESS
WIDGET A WINDOW -- `QWidget::isWindow` is `window_flags & Qt::Window` and Qt
forces that flag on when the parent is null. So a `QSpinBox()` built on its own
line, one statement before `layout.addWidget(it)`, IS a window for as long as
it takes to reach the next statement, and a filter installed on the
QApplication hands it all ~49 KB of the composed sheet for a window it is
about to stop being.

    one offscreen registry sweep, 45 modules
    the whole filter                                   10,518 ms
    widgets that are windows only for want of a parent  3,817 ms

and OVER HALF OF THAT IS ONE MODULE SCREEN SHEETED THREE TIMES, because a
screen is built parentless and collects Polish events all through its own
construction: 11 x 7 ms while it is still empty, then 11 x 255 ms and
10 x 291 ms once it has grown ~1,500 widgets. The sheet is byte-identical all
three times; what makes the repeat cost 255 ms rather than 7 ms is the widgets
that arrived in between.

FIXED in "A parentless widget is not a window, and 3,817 ms says so", which
skips the Polish for a widget that is parentless, never shown, has no native
handle and whose windowType is Window -- and skips NOTHING else. Menus,
tooltips, dialogs and real windows all keep their Polish sheeting, and a
module page is sheeted on its Show because `_a_page_joined_the_stack` marks it
as a sheet target before it joins the stack.

IT ALSO FIXED A BUG NOBODY HAD FILED. Qt resolves QSS from the NEAREST
stylesheet first, so a copy of the sheet left on an intermediate widget beat a
more specific rule on the screen root -- and those copies carry the GLOBAL
sheet only, because `register_widget_qss` blocks live on the root alone. Every
widget a registered block named was therefore painted the plain `fg`. 31
widgets change colour with the copies gone, every one of them TO the colour
its own block asks for, and 42 move because a label sized by its own block is
a different height. No test held any of those colours in either direction;
one does now.

WHAT IS STILL OWED, unchanged from the note above and NOT closed by this:

  * THE CLOSE-OUT MEASUREMENT. This file's own rule is that a re-measurement
    decides, not an argument, and the numbers above are a sweep and a
    leave-one-out rather than the benchmark this item closes against. The
    run belongs AFTER the theme change lands, on a quiet machine, through
    `tools/spacr_startup_benchmark.py`. Until then this item stays open and
    the 2026-09-14 table above is the last word on the budgets.
  * peak resident memory still has no threshold (`spacr_startup_benchmark.py`
    asks only that it be "reported and positive").
  * `MODULE_BUDGET = 20.0` in `tests/qt/test_opening_things_does_not_hang.py`
    is still a hang detector rather than a performance gate.

  The last two are deliberate and stay deliberate: a gate set at today's
  numbers bakes in whatever regression is left.

--------------------------------------------------------------------------------
2026-09-15 -- THE CLOSE-OUT MEASUREMENT. BUDGETS MET, STALL CEILING NOT.
--------------------------------------------------------------------------------

The note above promised this run and said the item stays open until it
happens. It has happened. THE ITEM STILL DOES NOT CLOSE, and the reason is
specific rather than general.

WHAT WAS MEASURED, and under what conditions, because the conditions are the
first thing to attack in a performance claim. Another session runs Qt test
batches on this box continuously and I could not get a quiet machine at any
point tonight. So the wall-clock below is PAIRED BUT CONTAMINATED -- both arms
carried similar load, the deltas are indicative, THE ABSOLUTE NUMBERS ARE
INFLATED. I ran an uncontaminatable measurement beside it rather than pretend
otherwise.

  THE COUNT, which load cannot move. Sheet applications during five module
  opens, by wrapping `theme._sheet_one_window`:

                      sheetings   orphan   QSS handed out
      baseline run 1      3,483      353         240.6 MB
      baseline run 2      3,355      281         231.7 MB
      fixed    run 1      1,886        4         130.3 MB
      fixed    run 2      1,886        4         130.3 MB

      mask              645 ->   336     orphan 126 -> 1
      measure           323 ->   171     orphan  41 -> 1
      regression      1,581 ->   953     orphan  85 -> 1
      classify_merged   931 ->   424     orphan 100 -> 1

  44-46% OF ALL SHEET APPLICATIONS REMOVED, and the same share of the QSS
  bytes handed out. Skipping ~300 orphan sheetings removes ~1,500 in total,
  because each one re-polishes a subtree and generates more Polish events.

  AND THE VARIANCE IS ITSELF THE BUG, which is the part worth keeping. The
  baseline is NOT reproducible -- 3,483/353 then 3,355/281 from identical
  code, because how many Polish events land while a widget is still parentless
  depends on how the event loop interleaves with construction. The fixed arm
  is IDENTICAL TO THE BYTE across runs. The change removes a RACE, not only
  work: a number taken on the fixed code can be compared with another one, and
  a baseline number could not be compared with itself.

  THE WALL CLOCK, contaminated as described, one run each arm:

      module            open before -> after     worst stall before -> after
      regression          9.64 -> 6.88 s  -29%     8,932 -> 6,459 ms  -28%
      mask                6.64 -> 4.85 s  -27%     6,062 -> 4,514 ms  -26%
      classify_merged     6.50 -> 3.99 s  -39%     5,760 -> 3,591 ms  -38%
      measure             3.97 -> 2.59 s  -35%     3,612 -> 2,412 ms  -33%
      annotate            0.25 -> 0.28 s           253 ->   276 ms
      __home__            2.84 -> 3.05 s           464 ->   519 ms

  The last two moved the wrong way and ARE NOT REPORTED AS A REGRESSION:
  under that load, on one run each, a 55 ms move across the 500 ms line is
  noise. It is named only because it is the entire reason the violation count
  went 47 -> 50, and a reader comparing those two totals deserves to know
  that is all it was.

WHAT THIS SETTLES

  * THE BUDGETS IN THIS ITEM ARE MET, and met with room. 0 modules over
    budget in BOTH arms -- before the change as well as after -- with the
    worst module at 9.64 s against 10.0, and that measured UNDER ADVERSE
    LOAD. A quiet machine can only be faster. The 2026-09-14 table above,
    which had measure at 13.23 s and OVER, is superseded.

WHAT THIS DOES NOT SETTLE, AND WHY THE ITEM STAYS OPEN

  * THE 500 ms EVENT-LOOP STALL CEILING IS STILL BREACHED, and not
    marginally. regression stalls 6,459 ms after the change; the ceiling is
    500. That is thirteen times over, which no amount of load noise accounts
    for. The acceptance bullet forbidding "a stopped animation or multi-second
    GUI gap" is therefore STILL NOT MET, by regression, mask and
    classify_merged.

    The theme filter was 70-77% of those opens and this change removed the
    parentless-Polish share of it. What is left is the rest of the screen
    build, and it is a different piece of work from this one.

  * peak resident memory still has no threshold, and `MODULE_BUDGET = 20.0`
    in `tests/qt/test_opening_things_does_not_hang.py` is still a hang
    detector. Both deliberate: a gate set at tonight's numbers bakes in a
    13x stall.

SO: the item's own budgets pass and its own acceptance text does not. It stays
open on the stall ceiling alone, which is a smaller and much better-defined
thing than what it was open on this morning.

--------------------------------------------------------------------------------
2026-09-15 -- TWO FIXES ON THE STALL, AND A BROKEN INSTRUMENT FOUND ON THE WAY
--------------------------------------------------------------------------------

The item was left open on the 500 ms stall ceiling alone. These are the first
two things a PROFILE of a regression-screen open found, as opposed to a guess.
Neither is close to enough, and both were free.

THE QUADRATIC DUPLICATE CHECK. `settings._advanced_family_members` skipped
keys it had already filed with `key in found`, where `found` is a LIST. spaCR
files 37,165 setting keys across 706 object roles, so that list grows to
~4,900 entries and is scanned once per candidate: roughly TWELVE MILLION
string comparisons to file one family, three families per import.

  A set now answers the membership question. The list still decides the
  ORDER, which is the contract -- the Advanced panel lays keys out object then
  suffix, and swapping the list for a set would reshuffle every panel while
  passing a timing test.

      family                            before    after
      Image preprocessing (per object)  52.5 ms   2.9 ms
      Object filtration                 72.9 ms   2.5 ms
      Intensity handling                11.4 ms   1.6 ms

  `_regroup_advanced` was ALSO calling it twice per family -- once to learn
  which keys to empty out of the per-object categories, once to file them
  under the family headings -- and the second set of calls returned exactly
  what the first had found. Verified identical for all three families before
  the calls were merged, rather than argued from reading the code.

THE SETTINGS DEFAULT THAT IMPORTED PANDAS. `_outlier_criteria` reached four
(key, name) pairs through `spacr.outlier_filter`, which imports pandas at
module level: 211 ms on the MAIN THREAD while a settings panel was being laid
out, spent before anything had been run. The pairs now live in
`spacr/outlier_criteria.py`, which imports nothing but `typing`;
`outlier_filter` re-exports both names so every existing importer is unchanged.

  IT ALSO DELETED A SECOND COPY OF THE SAME FOUR PAIRS. `_outlier_criteria`
  carried a hand-written fallback for the case where the import failed,
  identical to the real `CRITERIA`, and nothing compared them -- so a
  criterion added to one and not the other would have gone missing from the
  panel with no test to notice.

MEASURED, interleaved arms, with `sys.path` pinned to the tree under test:

      import spacr.settings        413 ms -> 156 ms   (4 rounds each)
      open the Regression screen   1.53 s -> 1.33 s   (3 rounds each)

AND THE INSTRUMENT WAS WRONG FIRST, which is the part worth writing down.
The profiler and the timing harness lived in `/tmp` and were run as
`python /tmp/.../harness.py`. That puts `/tmp` on `sys.path[0]`, so
`import spacr` resolved to the INSTALLED package in the main checkout rather
than to the worktree, and every screen-open number taken that way measured
code the edits had never touched. It surfaced as a contradiction -- a direct
import 257 ms faster while the screen open was unchanged to the millisecond --
and a contradiction that sharp is a broken instrument, not a surprising
result. The harness now asserts `spacr.__file__` starts with the tree it was
given. ANY benchmark run from a script outside a worktree has this bug.

WHAT THIS DOES NOT DO: 205 ms is 13% of one module's open. The ceiling needs
13x on regression. The item stays open, on the same ceiling, for the same
reason -- what is left is the rest of the screen build, and the profile says
~1.0 s of the remaining 1.33 s is IMPORTS pulled in on the click path. That is
the next thing to look at, and it is a question about WHEN those imports
happen rather than about making them faster.

WHAT CHASING THE IMPORTS SHOWED, and it is the most useful thing here.

  After `_outlier_criteria` stopped importing pandas, the profile said
  `spacr.stream_dataset` imported it. After that was fixed, it said
  `spacr.settings_advisor`. After that, `spacr.cell_montage`. Each fix was
  correct and none of them stopped pandas loading, because a regression
  screen open imports 729 MODULES and several of them genuinely need it.

  THE MODULE COUNT DOES NOT MOVE: 729 before and after all three fixes. So
  what those commits bought is a LIBRARY property rather than a wall-clock
  one -- a settings default now answers a question about names and values
  without loading machinery, proved in a child interpreter by
  `tests/test_a_settings_default_does_not_import_the_world.py`. That matters
  for the headless paths, the CLI and the queue validator. It is not the
  answer to the stall.

  THE ANSWER TO THE STALL IS ABOUT THE 729, not about which module is
  unlucky enough to be first. Two directions, and the next session should
  pick one deliberately rather than resume whack-a-mole:

    1. IMPORT FEWER MODULES PER SCREEN. A regression screen should not need
       729 modules to draw a form. Most arrive through module-level imports
       in widgets the screen builds but the user has not touched.
    2. MOVE THE IMPORTS OFF THE CLICK. Python imports are thread-safe, and
       these are pure-Python and data modules with no Qt objects, so they can
       be warmed on a BACKGROUND thread after launch. This does not make the
       work smaller; it takes it off the main thread, which is exactly and
       only what the 500 ms ceiling measures.

  Direction 2 is the one that matches the acceptance text. The ceiling
  forbids a multi-second GUI gap, not a multi-second total.

  DO NOT MEASURE ANY OF THIS FROM A SCRIPT OUTSIDE THE WORKTREE. See the
  harness note above.

--------------------------------------------------------------------------------
2026-09-15 -- THE FIRST FIX THAT IS ABOUT A SCREEN RATHER THAN A LIBRARY
--------------------------------------------------------------------------------

THE PRELOADER CHANGES THE QUESTION. `_PipelinePreloader` already imports the
seven pipeline modules on a worker thread 1500 ms after launch. With it run
first, a Regression open imports 220 modules instead of 729 -- and the other
stall offenders import almost nothing at all: Mask 6 new modules, Measure 2,
Classify 7. Yet Mask still took 1.84 s and Measure 1.11 s. So after the
first screen, THE STALL IS WIDGET CONSTRUCTION, NOT IMPORTS, and every earlier
note in this item that pointed at imports was describing the first open only.

WHAT A MASK OPEN WAS DOING: `ast.parse`, 73 times, 0.71 s of a 2.05 s
profiled open. `fold_strip._host_declarations` reads each fold host's SOURCE
and parses it -- deliberately, because importing fifteen hosts would load
pandas and scipy before Home paints -- and nothing kept the answer.
`folded_fallback` asks `folded_modules` once per key, `folded_modules` walks
all fifteen hosts, so a fold strip asking about its keys re-parsed every host
file per key. Measured with the cache removed: asking about 8 keys parsed 112
files.

FIXED by caching `_host_declarations` per module name. A host's source cannot
change while the application runs. The cache lives in that function and NOT
in `folded_modules`, whose answer grows as modules register, and which two
tests call twice expecting a different second answer after they replace
`_host_declarations` -- replacing it bypasses the cache, which is what they
need, and they still pass.

      first `folded_modules()`    14 parses
      every call after            0 parses, identical answer
      open Mask                   1.43-1.86 s -> 1.02-1.24 s  (3 interleaved
                                  rounds, taken under suite load, so the range
                                  matters more than either end)

The guard COUNTS parses rather than timing them -- how many times is an
integer a loaded runner cannot change. Removing the cache lookup reddens two
of its three tests; the third checks the cached answer equals a cold read and
passes either way, correctly.

STILL OPEN. This is roughly a third off one screen; the ceiling needs Mask
under 500 ms of main-thread time. The method that found it -- run the
preloader first, then profile a SECOND screen by internal time -- is the one
to keep using, because it separates widget cost from import cost and the
import cost was hiding everything else.

NAMES CHANGED AFTER THE NOTES ABOVE WERE WRITTEN. The two pandas-free data
modules are now PRIVATE -- `spacr._outlier_criteria` and
`spacr._stream_selection` -- and listed in AUTOAPI_NON_RENDERED_MODULES. They
are data nothing outside the package imports, and a public module docstring is
a contract translated into nine languages. `outlier_filter` and
`stream_dataset` still re-export every name, and
`stream_dataset.coordinate_column` / `.settings_for_method` remain public
wrappers over the private module, so no caller moved.


--------------------------------------------------------------------------------
2026-09-19 -- THE STALL IS HALVED. WHAT WAS PAID FOR TWICE, AND WHAT IS LEFT.
--------------------------------------------------------------------------------

Decided: NO DECISION IS ON RECORD FOR THIS PASS, and no maintainer answer
specific to 284 reached it. The standing request it works to is the one
this item was filed under on 2026-08-27 and the sister item 380 quotes
verbatim from 2026-09-07 -- "optimized for smoothness and speed (on all
platforms) while not compromising functionality". Nothing below is quoted
as the maintainer's words, and the one lever that WOULD need his answer is
named at the end of this note rather than taken.

The 2026-09-15 close-out left this item open on the 500 ms event-loop stall
ceiling alone and said the rest was "the screen build". It is the screen
build, and half of it was the same screen being styled over and over before
anybody could see it.

WHAT WAS MEASURED FIRST, because the earlier notes name a cost and not a
mechanism. `QWidget.setStyleSheet` was wrapped for the length of one module
open, driven through `tools/spacr_startup_benchmark.py`'s own in-process
controller so the numbers come from the path this item closes against.
One Regression open, 1,385 widgets on the screen:

    _sync_page_palette, inside AppScreen.__init__       668 ms
    MainWindow._theme_screen, the late widget blocks    829 ms
    QStackedWidget.addWidget -- Qt repolishes a whole
      subtree whose root carries a sheet, so this one
      is not a setStyleSheet call and costs the same
      (2,771 StyleChange events, as many as the others)
    the page's Show, setting the identical text again   806 ms

FOUR REPOLISHES OF EVERY WIDGET ON THE SCREEN, AND ONLY THE LAST IS SEEN.
The same shape on Mask (three of the four at ~500 ms each) and on every
module with a settings panel. Counted rather than timed, because a count is
what a loaded machine cannot move: one Regression open delivered 139,963
events, of which 97,000 were PaletteChange, FontChange, StyleChange and
DynamicPropertyChange -- the repolishes themselves, multiplied by the ten
application-wide filters every one of them is handed to.

TWO CHANGES LANDED, both on branch `home/284-380-perf`:

  1. THE SHEET IS OWED AND PAID ONCE. A widget that is parentless, unshown
     and uncreated -- the same test the 2026-09-14 Polish skip uses -- has
     its own rule written down instead of applied and is marked as a sheet
     target; `ensure_widget_qss_applied` adds its blocks to that debt
     instead of restyling for them; `_sheet_one_window` pays it in one
     assignment at the page's first Polish or Show, which is where the
     design already sheeted it. The reparent into the stack then costs
     nothing: Qt does not restyle a widget with no sheet joining a parent
     with no sheet.

  2. FIVE APPLICATION-WIDE FILTERS NOW ASK THE CHEAP QUESTION FIRST. The
     dialog detacher imported QEvent and QDialog from PySide6 on every
     event in the process and read `event.type()` up to three times; the
     feature-help filter ran two liveness checks before it looked at the
     type (284 profiled that pair at 839,906 calls in one Mask open);
     glass and button_roles rebuilt a tuple of enum members per event.
     None of them changed which events they act on.

MEASURED AFTER, paired runs of `tools/spacr_startup_benchmark.py --offscreen
--runs 1 --record-only`, alternating trees on one machine at load ~3, two
runs per arm, against `origin/nightly` at `3f925f684`. Worst event-loop
stall per module, in ms:

    module            3f925f684        + sheeting   + filters
    regression          6632 6621      4517 4548    3680 4013
    mask                3664 3794      2311 2285    1920 2028
    classify_merged     3463 3540      2112 2175    1693 1735
    umap                2528 2504      1537 1541    1208 1301
    measure             2086 2176      1147 1225     961  984
    external_masks      1752 1805       969  947     742  818
    analyze_plaques     1488 1416      1116 1052     863  937
    recruitment         1330 1314       765  763     570  605
    invasion            1132 1107       662  664     526  535
    replication          972  975       553  573     468  489
    __preferences__     1226 1237      1240 1305    1062 1014
    __home__             229  238       230  232     231  254

    modules over the 500 ms ceiling      16      17      13
    total of those stalls, ms        30,089  20,880  15,182

Home is the control and does not move: nothing here reaches a screen that
is already on show. `annotate` and `replication` come under the ceiling;
`__preferences__` is not a module and is listed because the driver measures
it.

BEHAVIOUR IS THE SAME, AND IT IS CHECKED RATHER THAN ARGUED. All 45 module
screens were opened on both trees and every widget fingerprinted -- class,
objectName, geometry, eight palette roles, font family/size/weight/italic,
visibility, size hints, autoFillBackground, WA_StyledBackground -- plus the
screen root's entire stylesheet text. 44 screens match row for row.
Regression differs in 7 rows and every one is invisible: six are the scroll
arrows a QTabBar hides inside tab bars that have never been shown, whose
geometry it recomputes at the show it hides them for, and the seventh is a
widget inside a hidden scroll area that matches as soon as it is revealed.

`tests/qt/test_a_module_screen_is_sheeted_once_before_it_is_seen.py` is the
ratchet: the screen root receives ONE StyleChange before its first paint,
where it received four. It fails 4 == 1 on origin/nightly.

WHAT IS STILL OWED, and the first two are the same two as before:

  * THE 500 ms CEILING IS STILL BREACHED, by 13 of 47 measured intervals.
    Regression is 3.7 s, Mask 1.9 s, Classify 1.7 s. Halved is not met.
  * peak resident memory still has no threshold, and `MODULE_BUDGET = 20.0`
    in `tests/qt/test_opening_things_does_not_hang.py` is still a hang
    detector. Both stay deliberate: a gate at tonight's numbers bakes in
    what is left.
  * THE CLEAN-MACHINE CLOSE-OUT AND THE macOS/WINDOWS NUMBERS. Everything
    above was taken offscreen on Linux while another session ran test
    batches, so the absolutes are ceilings; the deltas are paired and the
    counts are load-proof.

WHAT THE NEXT LEVER IS, AND WHY IT IS NOT IN THIS BRANCH. After these two
changes, one Regression open spends its remaining style time here:

    sheeting ~500 popups at their Polish (the page's plots
      bring 351 parentless QMenus, pyqtgraph's ViewBoxMenu
      and its submenus among them)                          ~825 ms
    the one legitimate sheeting of the page itself          ~640 ms
    reparenting the settings form when the search strip is
      installed at first show (settings_search.install)     ~250 ms

The first needs a DESIGN DECISION and it contradicts a contract the
2026-09-14 work wrote down on purpose: `_SheetsEveryWindowThatAppears`
covers menus and tooltips at their `Polish`, and
`tests/qt/test_a_widget_mid_construction_is_not_a_window.py::
test_a_menu_is_still_sheeted_before_it_can_be_shown` pins it. A popup could
instead be sheeted from `QMenu.aboutToShow`, which Qt emits before it
measures the menu, so the geometry would be unchanged -- but a QComboBox's
popup container is a QFrame with no such signal, and a parented popup
inheriting its page's sheet instead of carrying its own would let the
page's registered blocks reach inside menus, which is a different picture.
Whoever takes it should decide between those two and say which.

--------------------------------------------------------------------------------
2026-09-20 -- THE LEVER IS PULLED: A MENU IS SHEETED WHEN IT IS ABOUT TO OPEN
--------------------------------------------------------------------------------

THE DECISION. Put to the maintainer through the question prompt with both
routes and what each costs, and answered: sheet menus at `aboutToShow`. The
other route -- letting a parented popup inherit its page's sheet -- would
also have covered a QComboBox's popup, which is a QFrame with no such
signal, but it would have let a page's registered blocks reach inside its
menus, and that is a change to the picture rather than to the speed.

WHAT CHANGED, all in spacr/qt/theme.py:
  `_sheets_itself_before_it_shows` is new. At a menu's `Polish` the filter
  connects its `aboutToShow` once, through a weak reference, and leaves the
  polish alone. Qt emits `aboutToShow` from `popup()` and `exec()` BEFORE it
  measures the menu, so a menu sheeted there has the geometry it would have
  had sheeted at its polish, and a menu nobody opens is never sheeted.
  `Show` REMAINS THE BELT TO THAT BRACE: a menu opened with `show()` emits
  no `aboutToShow` and is sheeted at its `Show` like any other window.

THE CONTRACT THAT MOVED, and it moved on purpose. `test_a_menu_is_still_
sheeted_before_it_can_be_shown` asserted `ensurePolished()` then sheeted.
What the test's NAME promises is unchanged -- the menu wears the sheet
before it can be shown -- so the test now drives the real property: not
sheeted at the polish, sheeted when `aboutToShow` fires, and not visible at
that moment. Three tests join it: a menu shown with `show()` is sheeted
anyway, five polishes connect once, and a menu nobody else holds is still
collectable, which is what the weak reference is for.

MEASURED, `tools/spacr_startup_benchmark.py --offscreen --runs 1
--record-only`, arms alternated on one machine, against origin/nightly
ff8a70135. Worst event-loop stall per module, in ms:

    module            base a  base b   mine a  mine b
    regression          4929    3722     2740    2775
    mask                2339    1779     1648    1702
    classify_merged     1643    1689     1578    1593
    umap                1201    1204     1129    1130
    measure              943     939      884     902
    external_masks       717     723      697     712
    __preferences__     1050    1039      906     924
    __home__             298     332      226     227

READ THIS CONSERVATIVELY. Comparing the WORST of the changed arm with the
BEST of the baseline, Regression is 947 ms better; against the baseline mean
it is about 1,565 ms. Either figure is at or above the ~825 ms this lever
was predicted to be worth, and the prediction was of sheeting cost alone.

THE SPREAD IS ITSELF EVIDENCE. The baseline's two Regression runs differ by
1,207 ms and the changed tree's differ by 35 ms. A cost that varies by a
second between runs of the same tree is work whose amount depends on what
Qt happened to polish; removing it removes the variance with it. No other
module's delta is clearly outside its own noise, which is what one would
expect from a change that only concerns pages carrying hundreds of popups.

WHAT IS LEFT ON 284: the two smaller costs named in the previous note --
the one legitimate sheeting of the page itself (~640 ms) and reparenting the
settings form when the search strip is installed at first show (~250 ms).
A QComboBox's popup is still sheeted at its polish, by the maintainer's
choice above.

--------------------------------------------------------------------------------
2026-09-21 -- FEWER WIDGETS AT OPEN: HIDDEN PANELS ARE BUILT WHEN FIRST SHOWN
--------------------------------------------------------------------------------

The lever the previous note left: build fewer widgets when a module opens.
Taken, on the five heaviest screens, by deferring the panels that sit in a
card that starts hidden. Nothing is removed; each panel is built, whole, the
first time its card is shown or anything asks for it.

WHAT WAS HIDDEN AT OPEN, counted with the page on screen in a real
MainWindow (every widget under the page, and which subtrees were not
visible):

    screen           widgets  hidden   the big hidden subtrees
    regression         1,455   1,192   Figures card 747 (results tabs 661:
                                       results panel 332, Measurements 214,
                                       Cells 79, Runs 29; figure pages 79)
    mask               1,124     956   Live preview 281, Timelapse fold's
                                       Track preview 130, settings sections
    classify_merged      857     769   settings sections 572, Hyperparameter
                                       search 133
    umap                 669     513   settings sections 210, Hyperparameter
                                       search 136, Figures 65
    measure              591     446   settings sections 246, Crop preview 120

WHAT IS DEFERRED NOW:
  * Regression: the results panel and everything built with it -- the Runs,
    Results, Measurements and Cells tabs, the figure grid, detail and
    volcano pages, the scan and sweep panels -- built when the Figures card
    is first shown or any of those attributes is used. The fold strip's
    extras (Hits tab, publication figure, one-family cut) wait for it
    instead of building it.
  * UMAP and Classify: the hyperparameter search panel, built when its
    switch first shows the card.
  * Mask: the Cellpose live preview, built when Live is first switched on;
    a src typed before then is loaded when it is built. The folded
    Timelapse Track preview (attached through preview_registry), built when
    its toggle first opens it.
  * Measure: the crop preview, built when Live is first switched on.
  Settings sections are NOT touched: rows are already built per category,
  and the hidden sections that remain are the next lever (below).

HOW, in one mechanism with one helper:
  * `widgets/card.py:_CardBuiltWhenShown` -- a Card that runs a builder
    once, from setVisible BEFORE the card becomes visible (no empty frame),
    with showEvent as the belt for a card revealed by its parent.
  * `AppScreen` owes a "part"; `_BuiltOnFirstUse` descriptors on the
    part's attributes (_results_panel, _results_tabs, _figure_grid, ...,
    _hyperparam, _live_preview, _measure_preview) build it on first read or
    assignment, so every one of the ~40 call sites and ~100 tests that read
    those names works unchanged. SHIBOKEN TRAP, recorded in the class
    docstring: a PySide wrapper's getattr reads the instance dict BEFORE a
    class data descriptor, so the values are stored under another key or
    the descriptor is never consulted again.
  * A late part gets what opening gave everything else, in the same order:
    the transparent-container sweep, then queued decorations (Hits tab),
    then ensurePolished, then the language pass and help-onto-caption move.
    Polish BEFORE translate matters: button_roles classifies by visible
    text at polish, and a Swedish "Kör förhandsgranskning" is not "Run".
  * Imports stay at the open (the panel modules, hit_list), so the widget
    QSS blocks they register reach every page's sheet exactly as before;
    only construction waits.
  * Teardown and pure questions never build: closeEvent, the rebuild-for-
    scale carry-over, and regression.project_path peek.

WIDGETS AT OPEN, same probe, same tree, before -> after:

    regression   1,455 ->   730
    mask         1,124 ->   723
    classify       857 ->   728
    umap           669 ->   537
    measure        591 ->   476

MEASURED, `tools/spacr_startup_benchmark.py --offscreen --runs 1
--record-only`, six runs per arm alternating base/changed on one machine,
base origin/nightly 92b0dcfc4. THE MACHINE WAS AT LOAD 23-36 ON 32 CORES
(other sessions' test batches), against ~3 for the earlier notes in this
file, so every absolute below is inflated and noisy; read the medians and
the conservative column only. "cons" is the changed arm's 75th percentile
minus the baseline's 25th: negative means better even read pessimistically.
Worst event-loop stall per module, ms:

    module            base med  lazy med   delta    cons   open s base->lazy
    regression            4770      2290   -2480   -1312    6.12 -> 4.29
    mask                  3577      2904    -673    -359    5.75 -> 4.69
    umap                  2621      1680    -942    -170    4.28 -> 2.54
    measure               1766      1431    -335     +20    2.40 -> 2.12
    classify_merged       2938      2802    -136   +1587    3.90 -> 4.03
    external_masks  (ctl) 1016      1138    +122    +470
    __preferences__ (ctl) 2076      2384    +308   +1209
    __home__        (ctl)  632       772    +140    +330

    intervals over 500 ms (median of six)   29.5 -> 29.0
    total of those stalls, ms (median)    42,357 -> 34,388

READ CONSERVATIVELY: the three controls, which this change cannot reach,
moved by +120 to +310 ms on the medians, so that is the noise floor here.
Regression's halving is far outside it and holds at the pessimistic
quantiles; Mask and UMAP hold too. Measure's -335 is inside the noise read
pessimistically, and Classify shows nothing -- expected, since only 130 of
its 857 widgets were deferred and its hidden mass is settings sections. The
over-ceiling COUNT does not move at this load, because at load ~30 almost
every module breaches 500 ms; the count needs a quiet machine to mean
anything.

BEHAVIOUR IS THE SAME, CHECKED RATHER THAN ARGUED. Fingerprint of every
widget (class, name, geometry, visibility, eight palette roles, font, size
hints, autoFill, WA_StyledBackground, text, tooltip, placeholder, tab texts,
dynamic properties, own stylesheet) on both trees, English and Swedish:
  * at open, every VISIBLE widget on all five screens is identical except
    the live RAM/CPU/GPU usage bars, and the page stylesheets are
    byte-identical;
  * after revealing the deferred parts, UMAP's search panel is identical;
    Classify differs only in three never-laid-out hidden widgets; Regression
    differs only in hidden widgets' geometry/size hints (unshown scroll
    bars, tab scroll arrows, a hidden table header) and an i18n bookkeeping
    property on one hidden combo whose displayed tooltip is identical;
  * Mask's live preview and Measure's crop preview match in visible layout
    and text. Measure's status line after priming with the placeholder src
    races a worker against a 250 ms timer ("Not a file or folder: path" vs
    "Load a merged array first."); the base shows the same race.
TWO DIFFERENCES ARE FIXES, and are said rather than hidden: Mask's folded
Track preview is now TRANSLATED -- it was inserted into the runtime splitter
after the language pass and no watcher reached it, so on a Swedish UI it
opened in English -- which also widens the runtime column by ~18 px in
Swedish; and Regression's Hits tab and results containers are swept like
the rest of the page.

WHAT BUILDS A DEFERRED PART, so nobody is surprised by a stall later: the
card being shown (results arriving, the Live / search / track switches),
any read of its attributes (a dropped results folder, a refit, the UMAP GPU
switch), and the run journal's workspace collection at the end of ANY run
while a Regression screen that never showed results is open -- kept on
purpose, because the journal records the panel's (default) state and
skipping it would change what the workspace file says.

TESTS: `tests/qt/test_a_hidden_panel_is_built_when_it_is_first_shown.py`
(19): not built at open, built once and whole on first show or first use,
fold extras wait, close does not build, translated in Swedish, Run keeps
its role under translation, attached previews build on first toggle. Eight
existing tests that counted the regression plots, tab bars, "View:" label
or UMAP search controls at construction now ask for the part first; the
public-callable parameter ratchet moves 18,311 -> 18,315 (four optional
keywords), and the signature digest move is proved by subtraction.

IS 284 DONE? NO, and not by this change alone. Its DONE-WHEN asks for no
multi-second GUI gap and every module under the 500 ms ceiling on the
reference profile. On this loaded machine Regression's worst stall is now a
median 2.3 s (4.8 s before), Mask 2.9 s, Classify 2.8 s, UMAP 1.7 s -- all
still over 0.5 s, and the ceiling is breached by ~29 intervals at load ~30
(13 at load ~3 before this change, not re-measured quietly). What remains,
in size order:
  * THE HIDDEN SETTINGS SECTIONS: Classify still opens with ~570 widgets in
    sections it hides (the folded modules' categories), Mask and Measure
    with ~250 each. They are settings rows, built when a category is
    first built, but the gated sections are built with it; deferring them
    touches the settings model and its collect()/apply paths and wants its
    own equivalence pass.
  * the per-widget polish of what remains visible (~500-700 widgets per
    screen), which the earlier notes showed cannot be moved, only shrunk;
  * a quiet-machine and real-display close-out, and the macOS/Windows
    numbers, which need those machines.

--------------------------------------------------------------------------------
2026-09-22 -- HIDDEN SETTINGS CATEGORIES WAIT OFF THE PAGE UNTIL OPENED
--------------------------------------------------------------------------------

The lever the 2026-09-21 note named next, asked for by the coordinator:
the settings sections a screen hides at open.

WHAT IS HIDDEN AT OPEN, and why it cannot simply be "not built". Counted
on a real MainWindow: Classify had 584 of its 596 settings widgets hidden
(every category but one, under the Essentials view), Mask 490 of 576 and
Measure 305 of 334 (collapsed bodies, plus beta categories hidden by the
maturity preference). About 60% of those widgets are the controls
themselves -- 352 of Classify's 584 -- and THE CONTROLS ARE THE SETTINGS
MODEL: `SettingsWidgets.collect()` reads them, `set_value_for_key` writes
them, the dependency and object-visibility rules are wired to their
signals, and recipes, CSV import and the drop handlers all go through
`_widgets`. Not building them means a second value store and every one of
those paths learning it. Measured before choosing, offscreen, one Classify
screen: the constructor is 379 ms, of which the model's control build is
209 ms and the category captions 51 ms; the page's stylesheet costs 178 ms
with everything on the page and 38 ms with the hidden bodies off it, and
removing only the captions moved it to 174 ms. So captions alone buy
nothing, controls cannot go, and the cost worth taking is the styling and
the per-widget passes over widgets nobody can see.

TRIED FIRST AND REJECTED, WITH NUMBERS: leave hidden widgets in place but
mark them unpolished (clear WA_WState_Polished) so the sheet skips them,
and polish them when their category opens. Fingerprints were identical,
but six alternating benchmark runs per arm made it WORSE -- Classify's
worst stall 1,678 -> 1,885 ms median, the over-ceiling total 17.6 s ->
21.3 s -- and an event count showed why: Qt still delivers Style, Palette
and Font changes down the whole subtree whether it polishes or not
(30,392 vs 29,662 events on a Classify open). Reverted.

WHAT SHIPPED: the body of every top-level category that is collapsed or
hidden is taken OUT of the page -- made parentless, not rebuilt -- by
`AppScreen._detach_what_the_form_hides`, which the window calls just
before the page is first shown (after maturity, the dimension switches
and the search strip's Essentials view have decided what is on the form).
`Section._attach_body` puts it back, at its old layout index, the moment
the category is expanded or an open category is shown (a per-section
Show filter, installed only while the body is away), and the screen then
runs the language pass and the help-onto-caption move over it, so a
category that was away while the language changed opens in the new one.
The same trial with bodies detached cut Classify's events 29,662 ->
18,869.

WHAT STILL REACHES A CATEGORY THAT IS AWAY, and how:
  * the run: the controls are untouched, so collect() returns every key,
    including values set while the body was away (tested);
  * recipes, CSV import, drops: all go through the model's `_widgets`;
  * the search strip: indexes rows from each section's own form, finds
    matches by key, and opening a match's section brings its body back
    (tested); its sub-heading roll-up walks `_logical_parent`, which
    answers a detached body's category;
  * the palette's reveal: `Section._holds` instead of isAncestorOf;
  * the empty-heading checks and the object rule's slot-heading scan:
    `_sections_below` sees through detached bodies;
  * the theme: a detached body is not a window, so per-window sheeting
    skips it and it wears the page's current sheet when it comes back;
  * teardown: a body away when its category dies goes with it.
Everything new is private, so the public API ratchets do not move (the
same seven fail on nightly c4d57c90a and here, with identical numbers).

WIDGETS UNDER THE PAGE AT OPEN, same probe, nightly -> this change:

    classify_merged    728 ->  172
    mask               723 ->  413
    measure            476 ->  243
    umap               537 ->  345
    regression         730 ->  550
    external_masks     498 ->  233
    analyze_plaques    519 ->  329

MEASURED, `tools/spacr_startup_benchmark.py --offscreen --runs 1
--record-only`, six runs per arm alternating nightly 64addaf2d and this
change, AT LOAD 34-36 ON 32 CORES (other sessions), so read pessimistically:
"cons" is the changed arm's 75th percentile minus nightly's 25th.

    module            nightly med  this med   delta    cons
    classify_merged          3585      1924   -1660    -224
    umap                     2512      2059    -453    +267
    mask                     3405      3062    -343    +422
    analyze_plaques          1981      1629    -352    +181
    external_masks           1473      1186    -287    +266
    measure                  1763      1487    -275    +171
    regression               2633      2764    +131    +596
    __preferences__ (ctl)    2564      2858    +294   +1208
    __home__        (ctl)     691       858    +167    +202

CLASSIFY IS THE ONE THAT CLEARS THE NOISE: its stall nearly halves and
stays better at the pessimistic quantiles (six of six paired runs better).
The others move the right way on the medians by about as much as the
controls moved the wrong way (+170 to +290), so they are reported, not
claimed. Regression is flat, as expected: its settings mostly stay on the
page. Over-ceiling count 35 -> 35.5 and total 50.0 s -> 48.1 s, both inside
the noise at this load.

BEHAVIOUR IS THE SAME, CHECKED: every visible settings widget
fingerprinted (class, name, geometry, visibility, eight palette roles,
font, size hints, text, tooltip, dynamic properties, own stylesheet) on
seven screens, both trees, English and Swedish, at open and again with
"All settings" and every category expanded -- identical, 0 differing rows
of 2,144. One Swedish run once showed a 21 px larger size hint on one
Classify chip strip (never its geometry); four repeats did not reproduce it.

TESTS: `tests/qt/test_a_hidden_category_stays_off_the_page_until_opened.py`
(13): a collapsed body leaves and an open one stays, expanding brings it
back styled, showing an open hidden category brings it back, a collapsed
one shown keeps its body away, it returns to its index, nested headings
and `_holds` see through it, the came-back hook fires, and on a real
Classify window: >200 widgets away, collect() has every key, a value set
while away is collected, search reveals an away setting, and a category
that comes back after a switch to Swedish is in Swedish. Two survey tests
that walk the page with findChildren now open each category first, or use
`_sections_below` -- the path a user takes, since a closed category's rows
are not reachable any other way.

284 STILL NOT DONE on these numbers: at load ~35 every heavy module is
still over 500 ms (Classify 1.9 s, UMAP 2.1 s, Mask 3.1 s, Regression
2.8 s), and at this load the ceiling cannot be judged at all -- Home, which
nothing here touches, is at 0.7-0.9 s. What remains is the styling and
construction of what IS visible, the settings model's control build
(~200 ms per heavy screen, which only a separate value store would move),
and a quiet-machine and real-display close-out plus macOS/Windows numbers.

--------------------------------------------------------------------------------
2026-09-22 -- A CLOSED CATEGORY IS BUILT WHEN IT IS OPENED; ITS VALUES ARE READ UNBUILT
--------------------------------------------------------------------------------

The lever the previous note left: "the settings model's control build
(~200 ms per heavy screen, which only a separate value store would move)".
Taken, but not with a second value store, for a reason that was measured.

MEASURED FIRST: WHAT IS THERE TO WIN, AND WHERE IT HIDES.
  * An upper bound, before any design: a throwaway arm that simply did not
    build the controls or rows of the categories a real window leaves
    closed (headings kept), six alternating benchmark runs per arm. Worst
    stall: Regression -418 ms (pessimistic -349), External masks -220
    (-165), Measure -189 (-78), UMAP -161 (-95), Classify -140 (-12); the
    controls (Home, Preferences, Annotate, Map barcodes) moved -34..+31.
    So it pays, if the controls can be kept from being built at all.
  * Warm control construction alone is 30-90 ms per module (Classify 92,
    Regression 81, External masks 75, Measure 70); the rest of the bound is
    rows, captions, and the passes and events over them.
  * A FIRST CUT THAT ONLY DEFERRED THE ROWS WAS WORSE (Regression +404 ms,
    Classify +217 median): the chaining strip calls collect() while the
    screen is built, collect() read every control, and reading a waiting
    control built it -- 90 of them in one batch, with none of the 25 ms
    breaths the panel build takes. Reverted; the controls had to be
    readable unbuilt.

WHY NOT A SEPARATE VALUE STORE. The control is what turns a declared value
into the one the run gets: a spin box turns 200 into 200.0 and rounds to six
places, a text box turns '' into None, UMAP's folder list turns 'path' into
[]. Over 886 settings on 22 modules, 48 read back differently from their
declared default. A store holding raw values would give the run different
settings; one holding normalised values would have to repeat each control's
normalisation and drift from it.

WHAT SHIPPED, in two halves:
  * THE MODEL (`settings_model.py`). `_widget_for` is split into
    `_route_control`, which decides the control and returns either a PLAN
    for one of the seven plain controls (toggle, combo, int / float / auto
    spin box, text, list text) or a builder for every other one, and
    `_build_plain`, which builds a plan. `_value_a_plain_control_holds(plan)`
    is what that control reads back, computed without building it. A test
    holds it to the built control for all 1,135 plain settings of all 59
    modules (0 differ; the one that did, a NaN default the spin box clamps
    to its maximum, is handled). `_widgets` becomes
    `_ControlsBuiltWhenAskedFor`, a MutableMapping with the dict's contract:
    `in`, `len`, iterating the keys answer for every setting; `m[key]` and
    `.get` build that one control; `.items()`/`.values()` build what is left
    in one batch; `built_items()` / `built()` never build. So every one of
    the ~90 production call sites and ~570 test lines that read
    `model._widgets` works unchanged -- a reader always gets a real control.
    Measured on the call sites: 262 references, most of them `.get(key)` or
    `key in`, so the mapping was the smaller safe change; seven sites were
    migrated because they copied or walked the whole map at open or close
    (below).
    Reading without building: `collect()`, `modified_keys()`, the
    dependency rules' current values, the object rule's values,
    `_current_setting` and `_setting_value` go through `_read_value`: the
    control if built, else its plan's value. WRITING BUILDS THE ONE CONTROL
    (`set_value_for_key` reads `_widgets.get`), deliberately: a written value
    has to pass through the control's clamping and matching for collect() to
    return what an eager panel would, and one control costs about a
    millisecond. Recipes, CSV import and drops therefore build only the
    controls they write, never a category.
    State passes (classifier family, training basis, UMAP reducer, analysis
    unit, dependency rules, exclusion editor, regression backend) touch only
    built controls and run again when a control they own arrives, so a
    control built late is greyed, locked or filled as it would have been.
    Only a screen asks for any of this (`categories_may_wait`); every model
    built for its values, every test that builds a `SettingsWidgets`, and
    every fold builds everything as before.
  * THE SCREEN (`app_screen.py`). A top-level category that starts closed --
    all but the example-data one, and not one the Essentials view opens as
    the search strip installs (a category built by being opened pays the
    opening passes on top of the build) -- is built as a heading only by
    `_build_a_waiting_heading`: title, maturity, blurb, its keys registered
    with the object rule, the search strip, the dimension switches and the
    palette. Its rows are laid out by `_open_a_waiting_heading`, through
    the same `_build_settings_section` every category takes, the first time
    it is expanded, revealed, asked for its rows, or asked whether a row is
    visible; then its sub-headings are recorded ahead of it, the category
    hints, surface sweep, maturity, dimension switches, object rule and
    search index reach the new rows, and the rows are polished before the
    language pass and the help move, in that order (`_translate_a_late_part`).
    `MainWindow._build_screen` turns this on for the screens it builds, via
    `spacr.qt.screens._categories_wait_to_be_opened` (not a class attribute:
    reaching AppScreen from `_build_screen` imported the module screens while
    Home was drawn). A control built before its row -- one nothing can read
    unbuilt, or one something read -- is taken off the page until its row is
    laid out; made with the form as its parent, it was being drawn at the
    form's top-left corner (caught by the fingerprint below).

CALL SITES CHANGED, because they walked or copied the whole map at open or
close: `chaining._widgets` and `prerun._widgets` (a `dict(...)` copy),
`_widget_key_index`, `_dimension_rows`, the search strip's `_build_index`,
`_shutdown_settings_widgets` (teardown built every control to shut it down),
and `_the_rows_moved` (iterating a row list forced every waiting caption).
Also, the palette and the search strip's `reveal` open a waiting category
first, and `_install_dimension_switches` counts waiting dimensional keys (it
offered no 3D/Time switch while the z and t rows waited, so the Volumetric
category showed).

ONE BEHAVIOUR FIX, needed to make "late equals eager" true: changing the
classifier family, the training basis or the UMAP reducer now re-runs the
dependency rules after re-greying. Before, the family pass re-enabled every
setting it owns, so `batch_column` came back live under
`batch_correction='none'` after a family change -- and a control built
after the change would have disagreed with one built before.

WIDGETS CREATED BY OPENING THE MODULE, real MainWindow, fresh home
(Essentials, as the benchmark), nightly ad01caeca -> this change, and
controls built at open:

    module            widgets created      controls built
    classify_merged      738 ->  335       101 -> 21 of 101
    mask                 742 ->  459       132 -> 54 of 132
    measure              486 ->  302        63 -> 17 of 63
    umap                 547 ->  384        58 -> 20 of 58
    regression           740 ->  601        77 -> 49 of 77
    external_masks       508 ->  319        68 -> 22 of 68
    analyze_plaques      529 ->  370        48 ->  6 of 48

MEASURED, `tools/spacr_startup_benchmark.py --offscreen --runs 1
--record-only`, six runs per arm alternating nightly ad01caeca and this
change, load 9-12 on 32 cores. "cons" = this change's 75th percentile minus
nightly's 25th. BOTH TREES BYTE-COMPILED FIRST: the benchmark runs with
PYTHONDONTWRITEBYTECODE=1, so a tree whose edited modules have no .pyc
recompiles them in every run -- it cost the changed arm ~0.8 s of startup and
+200 ms on Home, a module this change cannot reach, until it was found.
Earlier A/B notes in 284 and 380 compared a freshly edited tree against one
with caches and carry that bias against the change. Worst event-loop stall
per module, ms:

    module            nightly  this  delta  cons  paired  pairs better  open s
    classify_merged       877   697   -180   -73    -159     12/12       1.30 -> 0.89
    mask                 1385  1279   -105   +48    -134     10/12       2.04 -> 1.87
    umap                  961   820   -141    -5    -130     11/12       1.32 -> 1.08
    regression           1241  1141   -100   -14    -109     11/12       1.98 -> 1.78
    measure               721   573   -148   +83     -75     11/12       0.92 -> 0.73
    external_masks        526   441    -85    +8     -55     10/12       0.86 -> 0.72
    analyze_plaques       807   758    -48  +114      -7      7/12       0.99 -> 0.84
    __home__        (ctl) 379   399    +19   +90     +14      4/12
    __preferences__ (ctl)1196  1324   +128  +230      +5      4/12
    annotate        (ctl) 716   705    -11   +67      +1      6/12
    map_barcodes    (ctl) 566   635    +69  +165     +21      3/12

    intervals over 500 ms (median)        15.5 -> 14.5
    total of those stalls, ms (median)  13,723 -> 12,352

TWELVE RUNS PER ARM, pooled from two sessions of six (the second on the
pushed commit 48e431db4; the first on the same open path before the
dependency-order fix, which only acts on a user's change). The load fell
from ~10 to ~3 during the second session, which widens the unpaired
quantiles, so the PAIRED column is the one to read: the median of each
run's difference from the nightly run beside it. The controls, which this
change cannot reach, sit at +1..+21 ms paired and 3-6 of 12 pairs better --
that is the noise floor. Every settings-heavy module clears it: Classify
better in all twelve pairs, UMAP, Regression and Measure in eleven, Mask and
External masks in ten; Plaques (whose preview dominates) is flat. Opening a
module is 10-30% shorter on all seven.

BEHAVIOUR IS THE SAME, CHECKED: for seven modules (Classify, Mask, Measure,
UMAP, Regression, External masks, Plaques), in a real window, under both
Essentials and All settings: collect() with most controls unbuilt equals
collect() after every category is opened, equals a second window that built
everything at once (0 differing keys); every row of every category once
opened -- key, caption, row visibility, enabled, tooltip, field type --
equals the eager window's (0 differing sections); every widget visible at
open (class, name, text, geometry, enabled) equals the eager window's,
except the live CPU/RAM usage labels.

TESTS: `tests/qt/test_a_closed_category_builds_nothing_until_opened.py`
(14): the plan-value invariant over every plain setting of every module; an
eager model stays eager; the map answers membership without building and
builds on read; collect() and modified_keys() read unbuilt controls and
build nothing; a write builds only its control; on real windows, closed
categories have no rows, collect() and every row equal an eager window's
(Classify and Mask), a value written before the category opens is shown
when it does, the search strip and the palette reveal a setting in a waiting
category, a category opened after a switch to Swedish is in Swedish, the
family rule reaches controls built after it ran, the dimension switches are
offered for waiting rows, and closing builds nothing. Updated: the detach
tests (a closed category is now waiting OR detached), two survey tests that
count rows on a real window now open every category first (the path a user
takes), and the menu-source test reads `_route_control`, where the branch
moved.

IS 284 DONE? NO. Every heavy module is faster and still over the 500 ms
ceiling on this machine: Classify 0.7 s, Measure 0.6 s, UMAP 0.8 s,
Regression 1.1 s, Mask 1.3 s worst stall, at load ~10. The settings side
of the open is now mostly what the user can see; what remains is
the construction and polish of the visible page (runtime panel, previews,
the open categories), and the quiet-machine, real-display and
macOS/Windows close-out. Opening a waiting category costs its build on
that click (measured 10-170 ms per category at load ~10, Regression's Model
& Inference the largest); an idle-time prebuild would move that too, and is
not done.

--------------------------------------------------------------------------------
2026-09-22 -- THE CLOSED CATEGORIES ARE BUILT IN IDLE TIME, A FEW MS AT A TIME
--------------------------------------------------------------------------------

The cost the previous note moved, not removed: a category closed at open is
built on its first open, 36-200 ms on that click. Asked for by the
coordinator: build them beforehand while nobody is using the screen, in
slices well under a frame, stopping for input, through exactly the passes a
click-open runs.

WHAT SHIPPED
  * ONE CODE PATH, RUN IN STEPS. `_build_settings_section`,
    `_the_rows_moved` and `_translate_a_late_part` are now generators run to
    the end (`_run_to_the_end`), so a click runs the same code, in the same
    order, as before. `_waiting_heading_steps` is a waiting category's whole
    build as steps: one control, the greying passes (one per step), one row
    or sub-heading, the section explainer, then the hints, surface sweep,
    maturity, object rule, dimension switches, late captions, search index,
    search filter, each child's polish, each child's language pass, and the
    help move. A click on a category the idle build is half-way through
    finishes that build (the same generator); the heading counts as waiting
    until the last step.
  * `_IdlePrebuild` (app_screen.py) drives it: 400 ms after the screen is
    shown with no pointer movement it runs steps for up to 4 ms a slice (a
    step that alone took 2 ms ends its slice), then gives the loop back
    with a zero timer. While slices run it watches the application's
    events; any pointer, wheel, touch or key event pushes the next slice to
    400 ms after it. It stops when the screen is hidden or closed and
    uninstalls itself when done. It never expands, shows or hides anything
    outside category bodies (tested; for item 471's collapsible panes).
  * THE WATCH IS NOT ON WHILE IT WAITS. The first version kept an
    application-wide event filter installed from the screen's show: a
    Python call per event, and a module open delivers tens of thousands --
    it cost every module open ~10 ms in the benchmark (6 runs/arm: every
    module +5..+16 ms median, controls +2..+12). Now the wait ends by
    comparing the pointer's position; after a key the watch stays on so
    typing keeps it waiting. The benchmark A/B below is flat.
  * TWO WHOLE-PANEL COSTS THE SLICES EXPOSED, both fixed at the source
    because they are paid by clicks and channel commits too:
      - THE OBJECT RULE SHOWED EVERY ROW AND THE SEARCH STRIP HID MOST OF
        THEM AGAIN. Under Essentials (every module's default) that round
        trip was 68 ms of a 90 ms pass on Regression, run twice per
        category built. The rule now asks the screen which rows its filters
        will hide (`rows_the_screen_hides`: the search strip's
        `keys_it_hides` and the switched-off dimensions) and sets each row
        once. The forms are then invalidated once (`_lay_the_forms_out_again`)
        -- without that, Mask's filename-convention box stayed at its old
        height after a channel commit in Swedish (96 px instead of 114),
        because only the round trip had been re-laying its form out.
      - `essential_keys()` rebuilt the module's category layout on every
        search pass (16 ms on Regression); cached per settings set.
        `explainer_width()` rendered all 180 regression explainers to
        measure them (20 ms per Regression category build); cached per
        language.
  * ONE LONG-STANDING BUG FOUND BY THE EQUIVALENCE CHECK: a greyed
    setting's NAME never said why it was greyed. `_note_on_label` exists
    for exactly that, but a rule that greyed a field before its label
    existed left the reason on the field (`_PENDING_NOTE_PROPERTY`, "so it
    can be put on a label that does not exist yet") and nothing put it
    there, and the window's language pass (`refresh_api_tooltips`)
    rebuilt every label's help from its description and dropped any reason
    that had reached it. So whether a greyed name explained itself depended
    on the order categories happened to be built in. Now a new caption takes
    the pending reason (`_put_the_greyed_reason_on`) and the language pass
    keeps it (`_LABEL_NOTE_PROPERTY`). On nightly no greyed name said why;
    now all do -- e.g. Classify's batch_column: "batch_column is read only
    when batch_correction is enabled (currently 'none')".

FIRST OPEN OF A CLOSED CATEGORY, ms, All settings (under Essentials the
closed categories are hidden until All is chosen), median / max over every
closed category of the module, offscreen, load ~3:

    module            nightly ad01caeca    lazy e2f082bf8    idle-prebuilt
                      (built at open)      (built on click)
    mask                 33 /  47             55 /  83           24 / 26
    measure              36 /  58             64 / 106           25 / 28
    classify_merged      45 /  74             71 / 139           28 / 31
    regression           47 /  87             92 / 175           27 / 37
    umap                 32 /  40             49 /  88           23 / 28
    external_masks       34 /  65             49 /  94           26 / 32
    analyze_plaques      28 /  63             35 / 119           27 / 28

A prebuilt category opens faster than one built at open used to: its body
is on the page (hidden), where nightly's detached bodies paid a reparent and
restyle on the first open.

THE IDLE BUILD ITSELF, per module, offscreen, a 16 ms timer measuring how
late it fires; "moving" = pointer moved every 20 ms from 0.6 s after the
open (the build had started at 0.4 s), then 6 s idle:

                    slices   worst slice  idle: worst lateness     slices while   input delay
                             ms           (nightly | prebuild) ms  moving         max ms
    Essentials view
    mask               64      16.5          28 | 27                  0              0.5
    measure            59      25.2           9 | 55*                 0              1.9
    classify_merged   107      13.1           1 | 12                  0              1.4
    regression         45      11.3           3 | 12                  0              0.5
    umap               61      12.3          32 | 19                  0              2.3
    external_masks     57      23.8          27 | 11                  0              1.7
    analyze_plaques    37       9.1          51 | 37                  0              0.2
    All settings
    mask               89      10.6          44 | 18                  0              1.4
    measure            59      24.5          42 | 39                  0              1.9
    classify_merged    99      14.1          48 | 16                  0              0.3
    regression         97      20.3          28 | 23                  0              0.8
    umap               68       9.2          15 | 19                  0              1.5
    external_masks     70      23.5          30 | 21                  0              0.7
    analyze_plaques    38       9.4           3 | 26                  0              0.2

  (* the machine's load rose from ~3 to ~8 during that row; nightly, which
  runs no prebuild at all, shows 28-51 ms idle lateness on its own -- the
  gc tick, usage timers and the ambient backdrop -- so the idle column's
  worst values are that noise floor, not the build.)

READ PLAINLY: p95 slices are 7-9 ms. The worst slice is 20-25 ms on Measure,
External masks and Regression -- over the 16 ms target by up to 9 ms -- and
it is ONE indivisible Qt call: laying out one combo row (`addRow` plus the
maturity repolish, 12-20 ms for "Crop mode" / "Number of organelles") or
Regression's section explainer. No slice ran while the pointer moved, in any
module, and input waited at most 2.3 ms. So the worst a user moving the
mouse can meet is the one slice already running when they started, at most
about one frame; a 100 ms freeze from the prebuild did not occur in any run.

BENCHMARK, `tools/spacr_startup_benchmark.py --offscreen --runs 1
--record-only`, six alternating runs per arm, BOTH TREES BYTE-COMPILED,
nightly 954587cac vs this change, load 3.8-4.9. The benchmark leaves each
module within ~100 ms, before the 400 ms wait ends, so this measures what
the prebuild costs a module open (nothing), not what it saves:

    module            nightly med  this med  delta  cons
    regression             795       764     -31    +12
    mask                   831       811     -20    +30
    classify_merged        515       508      -8    +17
    umap                   588       570     -18    +14
    measure                423       408     -14    +19
    external_masks         308       302      -6     +1
    analyze_plaques        466       484     +17    +36
    __preferences__ (ctl)  937       915     -22    +16
    __home__        (ctl)  229       224      -5    +22
    over-500 count 6.5 -> 7.0, total 4,973 -> 5,258 ms (inside the noise)

THE .pyc BIAS, AGAIN, BECAUSE IT DECIDES A/B RESULTS: the benchmark runs
with PYTHONDONTWRITEBYTECODE=1, so a tree whose edited modules have no .pyc
recompiles them in every run (~0.8 s startup, ~+200 ms on Home). Every A/B
here byte-compiled both trees first (`python -m compileall -q spacr`); any
A/B in 284 or 380 older than today's lazy-category note compared a freshly
edited tree against a cached one and is biased against the change.

BEHAVIOUR IS THE SAME, CHECKED: on seven modules, Essentials and All,
English and Swedish, every visible settings widget after opening every
category (class, name, text, geometry, enabled, eight palette roles, font,
tooltip, size hint, dynamic properties) plus the search strip's index,
visible keys and count line, and collect(): prebuilt == click-opened ==
built-with-the-panel, 0 differences. Against nightly, over the same four
combinations (and again after a nucleus-channel commit on Mask), the only
differences are 158 greyed labels that now carry their reason (above) and,
on Mask and Measure under All settings, the row flag of the z/t rows inside
the Volumetric / 3D category the 3D switch is hiding: nightly left those row
flags visible inside the hidden category (the search strip re-showed them
after the switch hid them), this keeps them hidden, as the switch says.
Nothing on screen differs; switching 3D on shows them as before.

TESTS: `tests/qt/test_idle_time_builds_the_closed_categories.py` (12): the
idle build builds everything and is sliced; prebuilt equals click-opened
equals eager (rows, captions, help, colours, collect(), search state) on
Classify and Regression; opening a prebuilt category builds nothing; a click
mid-build finishes it; a category prebuilt after a switch to Swedish
matches a window built in Swedish; the pointer moving stops it (no slice in
2 s of movement) and idle resumes it; input mid-build waits; a hidden screen
builds nothing; no slice is a whole category; a greyed name says why after
the language pass and stops saying so when re-enabled; the prebuild changes
no visibility or expansion outside the categories; the object pass that
sets each row once ends where the round trip ended.

2026-09-22 — REGRESSION'S HIDDEN RESULTS NO LONGER IMPORT DATA LIBRARIES
The open-time results capability check imported MeasurementScanPanel and pandas
before its hidden card was needed. Those imports now run together at the start
of the deferred builder. Missing dependencies retain the figure-queue fallback,
without constructing a partial results panel or retrying on every show.
The full-window probe exposed a second path through Hit List's style registrar.
Its QSS still registers eagerly, but its data backend imports in the table job;
flag definitions load when displaying data. No captions or stylesheet changed.

At base 19cee264a, three fresh full-window Home -> Regression probes took
2052/1630/1559 ms; the final change took 1412/1461/1472 ms. Medians: 1630 ->
1461 ms (about 10%). Pandas was loaded in all three baseline probes and none
of the final probes. Both trees were byte-compiled. These are sequential,
offscreen measurements on a loaded machine, not a controlled release claim.
The 45-module benchmark still warms data libraries before Regression: its
stall changed 819 -> 785 ms; intervals over 500 ms changed 10 -> 9, with some
other screens crossing the threshold in either direction. Both runs FAIL the
stall requirement. Item 284 remains OPEN; this closes only the import slice.

100 distinct focused tests passed: 99 lazy-panel, fold, hit-list and capability
checks, plus a fresh-process check that an empty Hit List registers its QSS
without pandas or spacr.hits. The inline-error check also passed again after
moving backend loading into the job. Two new dependency tests failed before
the fix and pass after it, including the missing-dependency fallback.
Evidence: features/data/284_regression_imports_2026-09-22.json; private probe,
profiles and complete benchmark artifacts under the recorded workspace path.
First results use still pays imports/construction; no all-platform acceptance.

2026-09-22 — UMAP'S HIDDEN EXPLORER WAITS FOR ITS FIRST PAYLOAD
Opening UMAP no longer builds the hidden image explorer and its matplotlib
canvas. The existing deferred-part lifecycle builds it on the first embedding
payload or direct access. Switching Interactive before results, ordinary
figures without embeddings, and closing the screen leave it unbuilt. Once
created, the same explorer retains its payload across mode changes and later
results, applies the current language, and reads/writes current display settings.

Six alternating fresh-process Home -> UMAP probes at base be6c6e7a8 versus
this change: medians1626.90 ->1263.79ms (22.3% reduction). The screen builds
318 widgets instead of364; the explorer module stays unloaded. Both arms
used the same private settings, bundled fonts and byte-compiled source;
only app_screen differed. This is an offscreen diagnostic on this machine,
not cross-platform acceptance. First payload/direct access still pays the
construction cost, and the500ms open/stall target remains unmet.
Evidence: features/data/284_umap_explorer_2026-09-22.json. Item284 remains OPEN.
Validation: 115 focused tests passed again after restoring the change over
integrated d63dd13d5; fatal-error Ruff passes. Three new regression cases
exercise no-payload teardown, first-payload reuse, and late language/settings.

2026-09-22 — FOLD CONTROLS JOIN THE FIRST STYLING PASS
The stack watcher installed Classify's fold strip and FlowView section after
the first screen show, applying new QSS to an already styled page. MainWindow
now runs the same idempotent fold installer after construction, before its
theme pass and first show. The stack watcher remains for other entry paths.
Six alternating fresh-process real Home->Classify probes at3f907ea4f:
median986.48->912.30ms (7.5%); screen-root StyleChange events5->2 on every
pair; widget count186 in every run. Source was compiled, settings/fonts fixed,
no concurrent tests from this session during measurements. This is an
offscreen diagnostic on this loaded Linux machine, not release acceptance.
The ordering regression test now checks the production per-window theme;
its first version used the suite's application-wide QSS fixture, which does
not reproduce the production ordering. Existing fold behavior is preserved.
The500ms ceiling and other-platform evidence remain open. Receipt:
features/data/284_fold_controls_before_sheet_2026-09-22.json.

2026-09-23 — SEMANTIC ICONS INITIALIZE ONLY THEIR REQUESTED FONT
The first settings icon initialized all twelve QtAwesome font families;
spaCR's semantic mappings use only fa5s. iconset now uses QtAwesome's own
IconicFont renderer with that family's bundled metadata/checksum, cached per
QApplication. Already initialized global fonts are reused. Missing/changed
metadata, custom/system fonts, failed registration or checksum problems keep
the original API/fallback path. Invalidated application font IDs reload.
No artwork, public signature, caption, theme color or setting changed.

All semantic glyphs plus the unknown-name fallback are pixel-identical to
the original engine at 16/48 px, three themes, four button modes and both
states; normal small icons also have visible ink. 151 focused checks pass,
including font reset, failed/missing fonts and existing global-provider reuse.
The new helper has 33/33 statements and 14/14 branches covered; this is not
a whole-iconset coverage claim. No gate or baseline changed.

Fresh-process isolated first-icon creation AND rendering, excluding the
separately measured QtAwesome import: eight runs per arm, interleaved in
both orders. Median 89.36 -> 55.13 ms (38.3 percent lower). Font initialization
falls from twelve families to one. Import cost remains: medians 114/122 ms.

Real full-window Home->Classify measurements remain noisy. First six-pair
candidate round: median 1525.60 -> 1450.31 ms. Final code, reversed order:
1606.60 -> 1578.44 ms (1.8 percent); three pairs improve, three worsen.
Every run builds 186 widgets. These are fresh offscreen processes on this
loaded Linux machine, with fixed private preferences/fonts and byte-compiled
source. The baseline installs the exact prior icon function before window
construction; all other current source is shared. No concurrent tests or
other benchmarks ran from this session during either timing cohort.

Receipt: data/284_requested_icon_fonts_2026-09-23.json contains both complete
navigation cohorts, all isolated samples, source hashes and probe sources.
This establishes less icon setup work, not a reliable whole-navigation speed
gain or cross-platform acceptance. The 500 ms open/stall target remains OPEN.

2026-09-23 — RELEASE WHEEL RELAUNCH DIAGNOSTIC, NOT PERFORMANCE ACCEPTANCE
The actual1.5.1.0candidate wheel restarted into a new process, reached Home and
opened49/49registered modules in an offscreen private-home run. Its full raw
benchmark is retained in data/487_candidate_update_relaunch_2026-09-23.json.
Twenty intervals report the500msstall ceiling breached; worst1878.96ms. This
was a single capped CPU relaunch check on the shared host, not the paired native
release-performance protocol.284remains OPEN; no timing gate/baseline changed.

2026-09-24 — MOTILITY PLATE GROUPING NO LONGER IMPORTS ANALYSIS
Opening a plate imported spacr.timelapse just to parse merged filenames,
pulling Torch/utils/plotting into the scan worker. Cold plate-drop checks
showed410and459msGUI heartbeat gaps against the unchanged400msbudget.
Grouping now uses a standard-library-only shared parser; the existing
spacr.timelapse._parse_merged_filename entry point delegates to it, keeping
its signature and legacy grouping fields/numeric time ordering unchanged.
A fresh-process import guard fails before and passes after the extraction.

61preview/GC/import checks pass. Separate cold-window measurements: plate
5.627msworst gap/1002ticks, merged-array85.663ms/3294ticks, zero collections;
both pass. Five existing parser-compatibility tests pass. Timing windows now
pause the GUI GC timer as well as automatic GC and restore pending cleanup;
explicit sweeps still fail the accounting guard. No timing budget changed.
This is a bounded motility/load fix, not full284release-performance acceptance.

2026-09-25 — A MODULE OPEN BREATHES BETWEEN ITS STEPS; ITS DOUBLE WORK IS GONE
Re-measured on nightly a93f937d2 first (one offscreen run, load ~11): 13 of
51 intervals over 500 ms, 10,938 ms over the ceiling in total; worst Make
Masks 1,921, Preferences 1,276, Regression 1,007, Mask 1,004. Load then rose
to 30-90 on 32 cores for the rest of the day (other sessions), so every A/B
below alternates arms, byte-compiles both trees, and is read as paired.

HOW THE WORST GAPS WERE FOUND. A GUI-thread stack sampler (a daemon thread
reading sys._current_frames() every ~2 ms, weighted by elapsed time) ran
inside the real benchmark and kept the samples between the two heartbeat
beats that bounded each module's worst gap. cProfile is unusable for this
on Python 3.12: it charged worker-thread time (the catalogue fetch) to the
GUI thread.

WHAT THE GAPS WERE, AND WHAT CHANGED (d29d9254c, then the 150 ms threshold):
  * ONE FREEZE PER OPEN, BY CONSTRUCTION. _on_nav_selected ran build, theme,
    stack join, translation and first show back to back; Regression's worst
    gap began inside the screen factory and ended after the show. MainWindow
    now breathes between those steps, and AppScreen (settings -> runtime
    panel) and Make Masks (between its tool cards, before the view pane)
    breathe inside their constructors -- only during a window's open
    (spacr.qt.screens._a_window_is_opening_a_screen), only after a step has
    run 150 ms, user input held back, with a second pass when the first ran
    >= 100 ms (one pass can run the timers BEFORE the posted Polish that
    sheets the page). A navigation requested during a breath is posted and
    runs after the open returns. The Preparing card stays up until the
    screen is on show.
  * COLD ICONS IN THE FREEZE: 15-25 % of the worst gap on Mask, Classify,
    Regression and Toxoplasma was fold-strip and organism icons decoded,
    LANCZOS-downscaled and re-inked on the GUI thread (first run, or a new
    theme). iconset._warm_the_bundled_icons re-inks every bundled PNG on a
    worker thread 1.5 s after launch; each writer now has its own .part file.
  * THE WALKTHROUGH OVERLAY INSIDE THE SWITCH (80-150 ms of a first Mask or
    Analyze Plaques open): now offered 30 ms after currentChanged, checking
    again that the screen is still current and unseen.
  * THE LANGUAGE READ PER tr(): 5 % of Make Masks' gap was QSettings reads in
    get_language; the whole open runs inside ui_language_resolved_once.
  * MAKE MASKS WAS STYLED THREE TIMES. ensure_widget_qss_applied put a sheet
    on the empty root, every card was polished against it as it was added,
    and adding the body stack (a reparent into a sheeted parent) repolished
    the whole tree -- 575 ms on that one line at load 70 -- before the window
    sheet did it all again at the show. The root now registers as a sheet
    that waits for its show (the AppScreen mechanism): one pass remains.
  * PREFERENCES WAS STYLED TWICE. The glass filter appended its card rule
    with setStyleSheet after the dialog had the window sheet: a second full
    repolish, 20 % of the gap. theme._add_to_a_windows_own_rules records it
    as the dialog's own rule and puts it on with the sheet, once (sampled
    setStyleSheet time in the Preferences gap 1,827 -> 870 units, same load).

RESULTS, medians, tools/spacr_startup_benchmark.py --runs 1 --record-only;
base = an export of nightly a93f937d2, new = this branch. "stall" is the
worst event-loop gap, "open" the click-to-interactive time, both in ms.
Offscreen, 4 alternating pairs, load 37.5-52.8:

    module              stall base -> new     open base -> new
    __home__ (start)         602 ->  479          8478 -> 7357
    __preferences__         2869 -> 1972          2946 -> 2069
    mask                    1532 -> 1042          3619 -> 4075
    measure                 1224 ->  511          1698 -> 2393
    annotate                1058 ->  785          1176 -> 1394
    classify_merged         1410 ->  418          1763 -> 1534
    map_barcodes            1418 ->  514          2160 -> 2329
    regression              2454 ->  735          5378 -> 5202
    feature_explorer         878 ->  518           965 -> 1249
    external_masks          1202 ->  587          2440 -> 2352
    experiment_design       1203 ->  606          1249 -> 1651
    tabulate                 841 ->  547           893 -> 1273
    dose_response           1322 ->  630          1408 -> 1724
    make_masks              4254 ->  916          4355 -> 3099
    umap                    1433 ->  673          2046 -> 2307
    graph_builder            663 ->  546           745 -> 1071
    gate_editor             1241 ->  691          1322 -> 1660
    feature_dict             738 ->  579           841 -> 1048
    trellis                  840 ->  580           943 -> 1337
    toxoplasma              1579 ->  945          1647 -> 1799
    candida                 1002 ->  566          1016 -> 1286
    analyze_plaques         1942 ->  794          2069 -> 2337
    invasion                1020 ->  514          1498 -> 1825
    replication              997 ->  510          2704 -> 3448
    (the other 27 rows are under 500 ms on the new arm at this load)
    intervals over 500 ms 35-42 -> 19-23; over-500 total 38.7-48.4 s ->
    14.0-16.8 s. Under xvfb-run (real xcb windows, 2 pairs, load 34-43):
    36-39 -> 15-24 intervals, 39.9-44.7 s -> 11.6-18.3 s, same shape
    module by module. Every row: features/data/284_open_breaths_2026-09-25.json.

THE COST, STATED: A BREATH IS A FRAME. Several opens take longer at this
load (Mask +0.5 s, Measure +0.7 s, some light screens +0.1-0.4 s) because a
breath paints the whole window over the moving backdrop -- 70-250 ms a frame
at load 50 -- where the old open painted nothing until it was done. That is
the "animated background continues to produce frames while a module loads"
requirement being paid for. Breathing only after 150 ms of work is what
spares light screens (a 50 ms threshold cost visibly more; d29d9254c had
100 ms). Every open stays far inside the 10 s budget; Home is unaffected.

WHAT IS LEFT OVER 500 ms, AND WHY IT IS NOT A BREATH AWAY:
  * EACH SCREEN'S FIRST STYLESHEET is one setStyleSheet -- Qt restyling every
    widget on the page -- and cannot be sliced. Thread CPU time of that one
    call at load 70-80: Make Masks ~590 ms, Preferences ~550, Mask and
    Regression 360-420, Home 340-390, the organism pages 85-125 per pass.
    Lowering it means fewer widgets at open or a smaller sheet (theme / 380
    work), not scheduling.
  * ANNOTATE imports pandas (spacr.qt.linked_selection -> spacr.selection),
    64 % of its gap, because it is the first data screen the benchmark
    opens. Lazy would mean spacr.selection importing pandas per function.
  * PREFERENCES is built synchronously inside PreferencesDialog(); breathing
    there needs its own re-entrancy guard and was not attempted.
  * ANALYZE PLAQUES' preview card belongs to the session editing
    plaque_preview.py and was left alone.

NOT MEASURABLE HERE, AND NOT COUNTED AGAINST THE ITEM: a quiet-machine run
(this box sat at load 30-90 all day; the one load-11 baseline is unpaired),
the maintainer's real display with a compositor, and macOS/Windows.

TESTS: 331 related Qt tests pass (all-module smoke, every tile and fold
opens, Home navigation, sheeted-once, per-window sheets, glass, icon cache,
idle-time build, startup benchmark, Make Masks, translated-once,
Preferences), plus new ones in existing files: the icon warm-up fills both
caches off the GUI thread and two writers never share a .part file; the
walkthrough is offered after the switch and not for a module already left;
the glass rule joins the window sheet in one setStyleSheet and survives a
theme change; a navigation requested mid-open waits for it; a breath runs
only during an open and only when due. The public-docstring count
(test_api_i18n_extractor) and the documentation API inventory hash already
fail on nightly (11,724 vs 11,710 reviewed); this change adds no public
symbol (the new helpers are private for that reason).
IS 284 DONE? NO. The ceiling is still breached on this machine, and what
remains is the list above, not something only another platform can answer.
