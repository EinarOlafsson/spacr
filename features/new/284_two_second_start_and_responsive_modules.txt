================================================================================
HIGH PRIORITY: TWO-SECOND STARTUP AND RESPONSIVE MODULE LOADING
================================================================================

Status:    OPEN, filed 2026-08-27. HIGH PRIORITY.
Owner:     spacr/qt startup, application registration, module factories,
           optional-dependency boundaries, preload policy, and timing.
Depends:   282 for lazy loading and memory budgets, which the 2026-09-14
           triage found ALREADY DONE.

           ITEM 276 DOES NOT EXIST AND NEVER DID. This line named it for
           "animation continuity" and the clause below made closure
           conditional on it, so the item was blocked on a phantom. Checked
           2026-09-14 three ways: there is no `features/new/276_*`;
           `features/00_INDEX.txt:337-339` runs 274, 275, 277 with no 276;
           and `git log --all -- 'features/*276*'` is empty. Nothing else in
           the ledger cites it -- every other "276" is a byte count or a test
           tally. The animation-continuity criterion it was supposed to supply
           is stated in this file's own acceptance text, so nothing is lost by
           dropping the reference; what is gained is that the item can close.

--------------------------------------------------------------------------------
WHAT WAS ASKED
--------------------------------------------------------------------------------

spaCR is currently very slow to start and many modules are slow to open. On
lower-end hardware the delays approach unusable. Heavy libraries must be
loaded only when the operation that needs them is called. Systems with ample
RAM may retain reusable caches, while lower-memory systems must avoid or
release optional caches, model weights and other reclaimable state.

The required user-visible budgets are:

  * spaCR reaches a usable Home screen within 5.0 seconds;
  * a module becomes interactive within 10.0 seconds, that being the hard
    upper bound rather than the expected value for any module;

    (Relaxed from 2.0 and 4.0 on 2026-08-28 by the maintainer, after the
    original numbers had been met on every module but Mask and the remaining
    gap was Mask's own size rather than the startup path.)
  * the animated background continues to produce frames while a module loads.
    A module becoming ready must not be preceded by a frozen interface.

These are HIGH-PRIORITY product requirements, not optional benchmark work.

--------------------------------------------------------------------------------
THE IMPLEMENTATION CONTRACT
--------------------------------------------------------------------------------

1. MEASURE THE REAL USER PATH

Use the in-application timing seam from instruction 281. Measure process start
through a usable Home screen and an actual click through an interactive module.
Import-only scripts, constructor-only timings and preloader timings are useful
diagnostics but are not acceptance measurements. Record cold and warm values,
the machine profile, and peak resident memory.

2. IMPORT AT THE CALL BOUNDARY

Heavy optional dependencies are imported when the operation that needs them is
called, not merely because spaCR started or because the user opened a module.
Opening a configuration screen must not import a training or segmentation
stack that will never run. This must be asserted through ``sys.modules`` in a
fresh process, not inferred from elapsed time.

Python extension modules cannot honestly be unloaded. Lower-memory operation
therefore releases what can be released: data caches, decoded images, model
weights, allocator caches and GPU cache. Item 282 owns the configurable idle
timeout, cache ceiling, headroom floor, and laptop/workstation profiles.

3. KEEP THE INTERFACE ALIVE

Registration, discovery, imports, model inspection and other preparation that
can safely run outside the GUI thread must do so. Work that must create Qt
widgets on the GUI thread is divided into bounded event-loop slices. During
the complete click-to-ready interval, timing instrumentation must show that
the event loop and ambient frame stream continue to advance. A spinner over a
blocked event loop is not progress.

4. DO NOT BUY SPEED WITH WRONGNESS

Lazy loading must preserve the same settings, module capabilities, results,
errors and cleanup as the eager path. Do not silently omit an optional feature,
lower analysis quality, or report a module ready before its controls work.

--------------------------------------------------------------------------------
HOW TO KNOW IT WORKED
--------------------------------------------------------------------------------

  * on the documented lower-end reference profile, a cold supported launch
    reaches an interactive Home screen in at most 2.0 seconds;
  * every registered module reaches an interactive state in at most 4.0
    seconds on that profile, and the measured distribution identifies which
    modules exceed 2.0 seconds instead of hiding them behind one mean;
  * fresh-process tests prove that starting spaCR and merely opening a module
    do not import heavy optional dependencies until the operation calls them;
  * the same representative pipelines complete under the laptop and
    workstation memory profiles, with equivalent scientific results;
  * frame/event-loop measurements cover the entire click-to-ready interval
    and show no stopped animation or multi-second GUI gap;
  * cold/warm startup time, every registered module's click-to-ready time,
    peak resident memory and worst GUI/frame interval are written as a
    reproducible benchmark artifact and guarded by tests with explicit,
    hardware-normalized thresholds;
  * item 282's memory/lazy-loading criteria are complete, not merely
    referenced. (This bullet also named item 276; see the header -- 276 has
    never existed, and its criterion is the "no stopped animation or
    multi-second GUI gap" bullet above.)


FIXED 2026-09-14, AND MEASURED EITHER SIDE. Measure now opens in 3.59 s
with a 3,267 ms worst stall, against 13.05 s and 12,713 ms before -- two
consecutive runs at load ~5 differing ONLY in how many controls the panel
builds, with mask, regression, classify_merged and annotate unchanged on both
sides, which is what makes the difference Measure's own. Repeats at loads 9.0,
13.5, 17.6 and 18.4 give 3.39-4.05 s, so it is inside the 10 s budget at every
load measured, and Measure is no longer the worst stall in the run.

    per-object controls   2,117 -> 11
    widgets on the screen 4,105 -> 595   (below Mask's 946)
    Crop settings... open  1.53 s -> 0.036 s

THE WORK WAS REMOVED, NOT MOVED TO A THREAD, so the total and the freeze fell
together. `MeasurePreviewPanel` builds controls for the roles the run actually
has -- the fixed objects plus `active_organelle_roles` -- and
`set_organelle_count` now BUILDS as well as shows, creating and wiring a slot's
three controls the first time the count asks for it.

THE FREEZE IS NOT GONE. 12.7 s became 3.3 s, a 3.9x cut; the residual is the
screen-build cost every heavy module pays and is not this defect. The
acceptance bullet forbidding "a stopped animation or multi-second GUI gap" is
therefore still not met by Measure alone, and regression (8,307 ms) and mask
(5,469 ms) are now the worst stalls in the run, both untouched by this change.

THREE THINGS WORTH KEEPING FROM THE FIX:

  * A TEST WAS ENFORCING THE BUG. `test_an_unset_count_shows_every_slot`
    required an unseeded panel to show all 702 slots. It is replaced rather
    than deleted, and the replacement records where the fear it encoded is now
    answered: a run that uses a slot says so, through the count or through its
    keys.
  * ONE WIRING PATH. A control created later must be connected exactly as one
    created at build time, or it is a dead control that looks right and changes
    nothing. `_connect_controls` and `_build_slot_controls` now call one
    `_wire_object_control`, so they cannot drift.
  * SETTINGS BEYOND THE COUNT STILL ROUND-TRIP. `apply_settings` applies the
    count FIRST and then builds for `declared_organelle_roles(settings)`, so a
    file written at seven slots and opened at two keeps controls for three
    through seven and hands their values back untouched -- the grid's rule,
    "cut from the view, not from the settings".

WHAT IS STILL OWED HERE: peak resident memory has no threshold at all
(`tools/spacr_startup_benchmark.py:491` asks only that it be "reported and
positive") while this item's acceptance text asks for "explicit,
hardware-normalized thresholds", and `MODULE_BUDGET = 20.0` in
`tests/qt/test_opening_things_does_not_hang.py` remains a hang detector rather
than a performance gate. Neither is fixed here. A gate set at today's numbers
would bake in the regression that is left.


RE-MEASURED 2026-09-14 AND THE BUDGETS ARE NOT MET. The item's own closing
paragraph asks for a re-measurement before closing; this is it, and it fails.

    module            this note   2026-09-14   budget   worst stall
    measure                1.45        13.23     10.0     12,926 ms   OVER
    regression             4.68         9.16     10.0      8,558 ms
    mask                   4.26         6.38     10.0      5,734 ms
    classify_merged        1.56         6.24     10.0      5,538 ms
    annotate               0.15         0.25     10.0        251 ms
    peak resident memory  651 MB      1728 MB     none

`passed: false`, 50 violations, and 15 of 47 measured intervals breach the
500 ms event-loop stall ceiling. THE INTERFACE IS FROZEN FOR 12.9 SECONDS when
Measure opens, which is precisely what the acceptance bullet above forbids --
"no stopped animation or multi-second GUI gap". `annotate` moved 0.15 -> 0.25 s
over the same interval, which is the control that rules out anything globally
inflating the table.

THE CAUSE, bisected and then confirmed by reading the source.

    b0e2f80c9  2026-09-04  "326: schema and organelle_types agree"
        parent fcc2734bb : measure 1.94 s / 2.00 s, stall ~1,800 ms
        this commit      : measure 10.65 s / 10.75 s, stall ~10,500 ms

23 measurements across the window show a clean plateau-step-plateau with no
non-monotonic point. The step is 8.7 s of the 11; the remaining ~2.5 s accrued
gradually between 09-04 and 09-14 and is not attributable to one commit.

THE CHAIN. That commit raised `schema.ORGANELLE_ROLES` from four string
literals to `_organelle_roles(702)`. `object_roles` re-exports it, so
`ALL_ROLES` went 8 -> 706. `spacr/qt/widgets/measure_preview.py:65` reads
`_OBJECTS = tuple(ALL_ROLES)` and builds a control per entry in three
comprehensions -- 706 Toggles at :509, 706 QSpinBox at :542, 705 QSpinBox at
:494, plus a 706-item combo at :493. Measured: 4,113 QWidgets for 63 settings
rows, against 84 for annotate. Ten application-wide event filters then run for
every Qt event delivered, so one Measure open costs 323,780 events, 3.2 M
filter invocations and 6.5 M enum constructions -- 69% of its 14.1 M Python
calls.

MEASURE_PREVIEW.PY WAS NOT EDITED. Its cost rose 88-fold because a constant it
reads grew, in another file, in a commit about object-key collisions. The
default organelle count is zero, so in the ordinary case all 702 of those slots
are controls for objects the run does not have.

WHAT THIS SAYS ABOUT THE ORIGINAL COMMIT, which was careful. Its message reads
"TWO OBVIOUS APPROACHES WERE WRONG AND BOTH WERE CAUGHT BY MEASURING RATHER
THAN BY REVIEW", and it turned membership into a frozenset with the comment
"asked per object id, so it is a set rather than a scan of 702 names". The
author measured the cost they expected -- per-key lookups on the hot path they
were changing -- and the cost landed somewhere else entirely, in a UI that
builds one widget per vocabulary entry. A 175x growth in a shared vocabulary
is not a local change, and the place to look is every consumer that iterates
it, not only the one being edited.

WHY NO TEST FAILED. `tests/qt/test_opening_things_does_not_hang.py:36` sets
`MODULE_BUDGET = 20.0`, with a comment above it saying "Measured here at 1.34 s
for the slowest module ... these are the ceilings a hang would blow through,
not a target to tune against". It is a hang detector at ~15x the real figure
and it is the only gate on module-open time, so 13 s passes green. The four
benchmark guard suites (97 tests) also pass, because they ratchet the artifact
SCHEMA from fixtures rather than the machine -- which their own docstring
states. And `tools/spacr_startup_benchmark.py:491` requires only that
`resources.peak_rss_mb` be "reported and positive", so 651 MB -> 1,728 MB went
unremarked while this item's acceptance text asks for "explicit,
hardware-normalized thresholds".


MET 2026-08-28. Measured through the real user path -- a `MainWindow` shown,
then `_on_nav_selected` per module, offscreen, warm cache:

    start -> usable Home :  2.03 s   budget  5.00   OK
    click -> mask        :  4.26 s   budget 10.00   OK
    click -> measure     :  1.45 s   budget 10.00   OK
    click -> regression  :  4.68 s   budget 10.00   OK
    click -> classify    :  1.56 s   budget 10.00   OK
    click -> sequencing  :  0.25 s   budget 10.00   OK
    click -> annotate    :  0.15 s   budget 10.00   OK
    peak resident memory :   651 MB

WHAT CLOSED THE GAP. Mask was 13.23 s and is 4.26 s, because the panel had
been building EVERY nameable organelle slot -- 1,551 widgets -- and hiding
the surplus. It now builds the slots `number_of_organelles` asks for, which
is 363 widgets, and grows when the count is raised. The maintainer's
diagnosis, and the largest single win of the item.

Two smaller ones: the settings form is decided once when a module opens
rather than on every keystroke (which was also a hang), and the bundled-font
registration asks matplotlib whether Open Sans is already known before
adding eight faces to it -- that had been costing 23 s inside the Mask open.

STILL WORTH DOING, though not blocking: `feature_dictionary.eventFilter` ran
419,953 times during one Mask open and `_still_alive` 839,906 times, for
4.8 s and 3.6 s of the profile. That is a filter installed per widget where
one on the panel would do.

--------------------------------------------------------------------------------
2026-09-13 -- THE PROFILE ABOVE PREDATES A FIX TO ONE OF THE TWO FUNCTIONS
--------------------------------------------------------------------------------

The closing note cites `feature_dictionary.eventFilter` at 419,953 calls and
4.8 s, and `_still_alive` at 839,906 calls and 3.6 s, and concludes "a filter
installed per widget where one on the panel would do".

DATES, because they change what is left to do:

    2026-08-28  b7f02c9cf   this profile was written
    2026-08-31  cac629c93   the import was hoisted out of `_still_alive`

`_still_alive` called `from shiboken6 import isValid` on every invocation.
The module's own comment measures what hoisting it to module scope did:
"625 ms per screen build, against 33 ms resolved once. Measured, not assumed."
So the dominant cost inside that function -- an import statement executed
839,906 times -- has been gone since three days after this note was written,
and the 3.6 s figure is not what the profiler would report today.

TWO THINGS THE NOTE STILL GETS RIGHT, and they are the part worth keeping:

  * The CALL COUNTS are structural and unchanged. `FeatureHelpFilter` is
    installed on the QApplication, so it sees every event in the application;
    that is why it runs 419,953 times during one Mask open, and hoisting an
    import does not change that.
  * Moving it would help. But the docstring says why it is where it is --
    "Installed on the QApplication, so it covers the screens that do not exist
    yet when it is installed, every app screen in spaCR is built lazily on
    first navigation" -- and the eventFilter opens with "THIS RUNS FOR EVERY
    EVENT IN THE APPLICATION, and it segfaulted." A move needs a hook at
    results-table creation and it needs to survive that history.

NOT RE-PROFILED HERE, deliberately: the benchmark presses every sidebar button
and loads torch, and a training run with a 110 GB watchdog was holding 56 GB
at the time. Whoever picks this up should re-run
`tools/spacr_startup_benchmark.py --offscreen --record-only` FIRST and get a
current number, rather than optimising against this one.
