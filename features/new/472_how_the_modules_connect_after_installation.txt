================================================================================
AFTER INSTALLATION: HOW THE MODULES CONNECT -- TUTORIALS, WALKTHROUGHS AND API
THAT SAY WHAT FEEDS WHAT, AND A HOME SCREEN THAT SHOWS WHERE TO START
================================================================================

Status:    OPEN; English workflow map, guide and pathway implementation in progress.
Issue:     GitHub #130, "Guide what to do after installation" (momeara,
           2026-09-22).
Owners:    CODEX -- the tutorials, walkthroughs and API prose (parts 1-4), the
           lane that already owns items 358, 447 and 286.
           CLAUDE -- the Home screen part (part 5).

THE ISSUE, IN THE REPORTER'S WORDS
--------------------------------------------------------------------------------

    "After installing the package, looking at the homescreen I can't figure
    out what do."
    "I would like to open a sample project that is similar to a project I may
    have and see the platform in action."
    "When I look at the tiles, I can click them and nothing seems to happen."
    "When I go online looking for the documentation, I don't see guidance on
    what to do next."
    "I also looks at the interactive video tutorials and it also seemed be a
    mix of installation, capabilities, and technical details without a clear
    guide for workflow usage."

THE MAINTAINER'S REQUEST (2026-09-22), in his words
--------------------------------------------------------------------------------

    "the user is asking for a clearer path between modals. so there should be
    a Tutorial giving an overview of how the modulus are linked. that the mask
    generation outputs are used as inputs for the measurements, and
    measurement data base as input for classify machine learning and
    measurement image crops a (or streamed from masks) as input for computer
    vision, and classify modual outputs and Map barecodes as inputs for
    regression. and so on.

    this should be clearly stated in a new tutorial on spaCR screens, and
    another on Modual inports and outputs (covering all moduals and how they
    relate to each other), also make a tutorial on image analasys related
    moduals and how they relate to each other, and on sequensing elated
    moduals and how they relate to each other.

    links between moduals should also be discribed in the API, and in the
    walkthroughs, add a new walkthrough that covers what to start with on the
    home screen if doing a spaCR screen (make-->measure-->annotate-->classify
    and map barecodes-->regression), doing a high content image analasys
    experiment (mask--> measure--> several ), generating a mode (make
    masks-->train cellpose model), if you can think of any other common
    experiments and pathways through spacr discribe them to.

    the API, walkthroughs, and Tutorials should match each other."

WHAT TO BUILD
--------------------------------------------------------------------------------

0. ONE SOURCE OF TRUTH FOR THE LINKS. Before any prose: a single machine-
   readable map of every module's inputs and outputs -- what it reads (folder,
   file kind, database table/column) and what it writes -- from which the
   tutorials, the walkthroughs and the API pages are all written or checked,
   so the three "match each other" by construction and a test fails when a
   module's real inputs/outputs drift from the map. Start from what the code
   already declares (settings `src`/output folders, the tables each module
   writes to measurements.db, the preview registry, the Home registry's
   groups). The chain the maintainer names must come out of it verbatim:

     Mask generation -> masks + merged arrays
       -> Measure (reads the masks and images) -> measurements.db + crops
       -> Classify, machine learning (reads measurements.db)
       -> Classify, computer vision (reads the crops, or streams them from the
          masks)
       -> Annotate (labels crops in png_list) -> the classifiers' training data
     Map Barcodes (sequencing) -> per-well guide/gene counts
     Classify outputs + Map Barcodes outputs -> Regression -> hit list

1. TUTORIAL "spaCR screens": the pooled-screen story end to end -- what a
   spaCR screen is, and which module does each step, in order.
2. TUTORIAL "Module inputs and outputs": EVERY module, what it takes and what
   it produces, and which modules come before and after it (from part 0).
3. TUTORIAL "Image analysis modules and how they relate": Mask, Make Masks,
   Measure, Annotate, Classify, Image UMAP, Gate Editor, Align & Stitch, the
   assays (Plaque, Recruitment, Invasion, Replication), QC, Embeddings.
4. TUTORIAL "Sequencing modules and how they relate": Map Barcodes, OPS,
   Regression, Power / Design, Experiment Design, Dose-Response, Run Compare.
5. WALKTHROUGHS (the interactive ones), one per common pathway, each starting
   from the Home screen and saying which tile to press first:
     a. A spaCR screen: Mask -> Measure -> Annotate -> Classify, and
        Map Barcodes -> Regression.
     b. A high-content image analysis experiment: Mask -> Measure -> then
        Classify / Image UMAP / Gate Editor / Graph Builder / the assays.
     c. Training a segmentation model: Make Masks (curate) -> train a Cellpose
        model -> use it in Mask.
     d. OTHERS the maintainer asked us to think of -- candidates:
        - a parasite assay: Mask -> Plaque / Recruitment / Invasion /
          Replication;
        - an optical pooled screen: Align & Stitch -> OPS -> Regression;
        - importing someone else's data: Import -> Measure -> ...;
        - phenotype discovery without labels: Measure -> Embeddings /
          Image UMAP -> Gate Editor -> Annotate;
        - planning a screen before running it: Experiment Design ->
          Power / Design;
        - live-cell tracking: Mask (timelapse) -> Measure -> motility.
       Each named from the Home tiles as they read today.
6. API: each module's API page (and the Home tile's API link) states its
   inputs, its outputs, and the modules before and after it, generated from
   the part-0 map so it cannot disagree with the tutorials.
7. The three -- tutorials, walkthroughs, API -- say the same thing: a test
   (or the docs build) compares each one's stated links against the part-0
   map.

PART 5 -- THE HOME SCREEN (CLAUDE)
--------------------------------------------------------------------------------

   a. "I can click them and nothing seems to happen": reproduce a Home tile
      click on a fresh install (a module that opens slowly with no feedback,
      a single- vs double-click expectation, a tile that opens off screen or
      behind, an empty state that looks like nothing happened) and fix it so
      every tile click visibly does something at once.
   b. "Open a sample project similar to mine": a clear first-run entry on
      Home -- choose the kind of experiment (the pathways in 5a-d), get its
      example data (the existing test-data downloads) and land in its first
      module with the settings filled, with the walkthrough for that pathway
      one click away.

DONE WHEN
--------------------------------------------------------------------------------

  * The four tutorials and the pathway walkthroughs exist in every tutorial
    language the library carries, and the API pages state inputs, outputs and
    neighbours for every module.
  * One map of module inputs/outputs exists and a test fails if the
    tutorials, walkthroughs or API disagree with it or with the code.
  * A Home tile click always visibly does something, and a new user can start
    a sample project of their kind from Home.
  * Issue #130 is answered with links to the tutorials and the Home entry.

2026-09-22 — CODEX: IMPLEMENTATION STARTED
The shared resource path is spacr/resources/module_workflows.json. Source
inspection is reconciling the live module/fold/preview registries and actual
artifact readers/writers before prose generation. Pathway IDs and ownership
are announced in the coordination channel. No tutorial, translation or Home
completion is claimed yet.

2026-09-22 — CLAUDE: PART 5 IS BUILT
  a. A module that cannot open now SAYS SO. `MainWindow._on_nav_selected`
     let a build failure escape into the Qt slot, where Qt printed it to a
     terminal the user does not have and carried on -- the reporter's "I can
     click them and nothing seems to happen". It is caught, the module and
     the missing package are named in a dialog and the status bar, and no
     half-built screen is kept (tests/qt/test_a_tile_that_cannot_open_says_so.py).
  b. "START A SAMPLE PROJECT…" is the first thing in Home's side column. It
     asks which kind of experiment the person has, opens that pathway's first
     module and loads that module's own example data
     (spacr/qt/widgets/sample_project.py, tests/qt/test_home_starts_a_sample_project.py).
     THE PATHWAYS COME FROM CODEX'S MAP, spacr/resources/module_workflows.json
     (announced in the channel, part 0), read as {"pathways": [{id, title,
     summary, modules}]}; until that file lands, three fallback pathways are
     used -- the pooled screen, high-content imaging, and training a
     segmentation model. When the map lands the dialog follows it with no
     further change, so the dialog and the tutorials cannot drift.
     STILL OWED on part 5: the walkthrough for the chosen pathway one click
     away, which needs Codex's walkthroughs to exist.
2026-09-22 — CODEX: FIRST ENGLISH MAP / GUIDE / WALKTHROUGH IMPLEMENTATION
spacr/resources/module_workflows.json maps 71 registered/nested/mode routes
and nine pathways. Existing spacr.ports declarations are copied exactly and
checked against current code, alongside live names, parents, Home/API routes
and key saved-column/table literals. Explicit handoff notes distinguish
conditional inputs, Plaque's direct image route, and the OPS join/aggregation
needed before Regression. This checks declared contracts and selected source
anchors; it does not establish runtime execution of all 71 workflows.

build_module_workflows.py generates workflows.rst, per-API input/output and
neighbour includes, and four tutorial authoring contracts. Sphinx refuses
stale generated artifacts. Help -> Walkthroughs exposes all nine pathways;
show_walkthrough(window, 'pathway:<id>') begins on Home, navigates to each
module/host, and never starts analysis. Real visual inspection found and fixed
a competing delayed first-run tour. The map is included in wheel/sdist data.

These are English implementation checkpoints, not four finished narrated
lessons. All-language pathway wording, actual new tutorial media, broader
runtime I/O evidence and Claude's Home-map adapter/walkthrough link remain.
The API/runtime rebuild and completed-set tutorial publication remain open.
Validation for this slice: 121 focused map/navigation/first-run tests pass
in 24.51 s; the preceding ports/artifacts selection passed 75 existing checks.
A real Sphinx HTML build of the guide and all generated API include fragments
passes with warnings treated as errors. Real 1280x900 Home and Experiment
Design pathway cards were inspected; the first-run overlay no longer covers
the requested guide. No full application/API-docs build is claimed.

2026-09-22 — CODEX: FOUR ENGLISH LESSON SOURCES AND FULL DOCS BUILD
The workflow map now generates real English lesson-source JSON for78-81, not
only authoring contracts:8 pooled-screen scenes,73 module-reference scenes,
16 image-analysis scenes and10 sequencing scenes. All start from Home. The
reference covers all71 mapped routes and explains each shared artifact's
storage/tables/columns on first use; the shorter lessons keep their relevant
branches. These are scripts awaiting recordings and all13 translated reviews,
not published tutorials. No module tutorial identity was replaced.
Tests tie rendered narration to map changes, preserve both regression inputs,
check classifier alternatives and prove all artifact locations are explained.
89 workflow/staging tests pass in5.96s; the prior pathway selection had29 passes.
A full Sphinx9.1.0 build under8G at83281540b found one ambient.py timing-table
alignment error; removing the leading margin space fixed it without changing
words or numbers. The repeated full -W build succeeds. Independent HTML checks
find all71 module contracts on63 API pages, all9 pathway anchors and resolving
guide links. Four built-site constructor/helper checks pass;8 nested-helper
fixture checks pass with the independent GUI-map hook explicitly isolated.
The existing stale-English-manifest assertion remains a separate open API debt.
Receipt:features/data/472_full_docs_build_2026-09-22.json.

2026-09-22 — CODEX: NINE-LANGUAGE GUI PATHWAYS
All47 new pathway titles/actions/notes and the formatted Home introduction
now have source-bound reviews in the nine GUI languages:423 translated rows.
An independent verifier checks live sources, hashes, catalog values, candidate
gates and actual formatted tr() output, and proves unrelated rows unchanged.
Receipt:features/data/472_workflow_translations_2026-09-22.json. This is an
AI-assisted semantic/technical review, not native-speaker signoff. Image UMAP
stays verbatim under the protected product-name rule; other module references
include their current localized label where available.
The actual-overlay test caught a double-translation bug: pathway builder had
already translated each step before the shared tour translated it again.
The shared overlay now accepts already-translated text explicitly, and its
labels skip a second generic translation pass. Ordinary tours keep their
existing translation behavior.45 pathway/module-tour tests pass. Real Hindi,
Chinese and French long-note cards were visually inspected at1280x900; text
and controls fit. Their private captures are in workflow-authoring-20260922.
Additional tutorial languages da/nb/it/ja, four tutorial translations,
recordings and media acceptance remain open; no claim of complete472 yet.
Final validation:72 focused pathway/first-run/onboarding/runtime-syntax/English
manifest checks pass in42.14s. The localized path test also runs the application's
generic translation pass over each visible step, proving the approved wording
survives both rendering paths.

2026-09-22 — CODEX: HOME USES ALL NINE PATHWAYS AND OFFERS THE TOUR
Following Claude's dated handoff, the sample-project dialog now reads the
object-shaped shared map instead of falling back to three legacy routes.
Titles, first-step guidance, complete module order and first Home tile all
come from that map. Selecting a sample retains the existing example-data
loader and adds a persistent Walkthrough button to the status bar; one click
starts the selected route at Home. Subsequent samples reuse the button with
their own pathway ID. No analysis starts merely by opening the walkthrough.
The nine-language titles and steps use the already reviewed pathway text;
54 new translations cover the six Home captions. The chosen description wraps
below the list, including the complete Hindi training text. Themed Hindi,
German and French dialogs were rendered at640x520, and the real MainWindow
status control was inspected at1280x900. A font propagation issue found during
that check is covered by a regression: the status bar refits after Qt settles
the inherited font, so the button is not clipped.
57 combined Home/pathway/map/runtime checks passed, followed by38 Home/pathway
checks after the layout refinements. Independent source/hash/gate/live-tr and
unrelated-row checks cover54 captions plus nine updated473 magnifier tooltips.
Receipt:features/data/472_home_pathway_integration_2026-09-22.json.
This completes the map adapter and one-click link left in Claude's handoff.
The four additional tutorial languages, recordings and final media acceptance
are still open; this is not complete472 acceptance or a publication claim.

2026-09-22 — CODEX: ORGANISM HOME ROUTES IN THE SHARED MAP
The authoritative map remains spacr/resources/module_workflows.json. It now
covers74 modules,37 artifacts and nine common pathways. Three organism guides
have explicit GUI-only inputs/outputs; they do not produce measurements.
The four available assays open under Toxoplasma. Plasmodium and Candida's
proposed analyses remain Coming soon, not executable pipeline claims.
Regenerated workflow prose, API includes and tutorial contracts agree with
that map; lesson79 now has76 scenes. The changed parasite-pathway note has
nine source-bound translations, with its older reviews preserved verbatim.
README grids in English and nine translations and the API landing grid now
show the three organism pages; the four assay API anchors remain in the
folded-module list. New SVG-derived tiles were visually inspected and their
opacity is covered by a regression.71 distinct README/grid checks pass.
Recording, further tutorial languages, narration review and final media
acceptance are still open. This entry does not claim tutorial publication.

2026-09-22 — MAINTAINER CORRECTION: THE OPS SAMPLE STARTS ON MASK → OPS
--------------------------------------------------------------------------------
The maintainer correctly queried why Start a sample project → An optical
pooled screen opened Align & Stitch rather than Mask Generation's OPS tab.
The shared optical_screen pathway now starts with OPS, home_app mask, and
lists Align & Stitch only as optional preparation. Original sequencing cycles
remain required; the explicit identity join/aggregation before Regression
remains documented. The existing translated OPS action is retained.

SampleProjectDialog and offer_a_sample_project now use the first actual
module, rather than just its Home host. MainWindow resolves OPS to Mask,
selects the actions-row OPS page, and passes that form to the OPS example
loader. Reopening the example reuses and selects the same page, even after
switching to Mask's own tab. Saved OPS settings and seeded run records also
target the OPS form, so adding the host mapping cannot apply them to Mask.
No standalone OPS screen or Align screen is created by the sample route.

Generated workflows.rst and the79/81 workflow contracts were regenerated
from the shared JSON; all74 modules and9 pathways pass the generator check.
78 focused tests pass, including real MainWindow navigation and reopening,
OPS example settings and engine indexing of an eleven-cycle synthetic
sample, saved OPS state, the nine-language existing pathway captions and
chaining/restart regression checks. The old OPS placement test was updated
for the maintainer's September22 move of Live onto the preview card: OPS
remains next to Time in Actions. New optional-preparation note translation
and API source-binding refresh are handed to the other Codex lane.
No analysis, GPU job, example download or NAS access was performed.

2026-09-22 — CODEX: PYTHON-ONLY WORKFLOWS INCLUDED
The map now covers77 modules,43 artifacts and all9 Home pathways. The live
check covers APP_FUNCTIONS and declared PORTS as well as the GUI/folded
registries. Three previously omitted Python-only workflows are explicit:
endodyogeny (host-cell area-derived size proxy, not parasite count), direct
Cellpose inference (not training; saved TIFF masks need project import before
Measure) and pooled-screen simulation sweeps (synthetic planning estimates).
Their real callable entry points replace any Home/menu instruction. The API,
workflow reference and lessons79–81 are regenerated from the same contracts;
lesson79 now has79 scenes. Source literals and callable definitions are
checked without importing or running analysis pipelines.24 focused workflow
checks pass, including drift rejection and the scientific/data boundaries.
Tutorial translations, media and listening acceptance remain open.

2026-09-23 — CODEX: TWO WORKFLOW RECORDINGS AND OVERVIEW CAPTIONS
Lesson 78 has eight current native 4K scenes and all thirteen non-English
script reviews. Its shared 4K/30 master passes full decode. All thirteen
caption selections pass browser, seek, link and mobile checks. Visual review
found paragraph-sized Japanese/Hindi overlays; the shared player now splits
native punctuation without spaces and recognizes Unicode sentence starts.
Fourteen focused player tests pass, and Japanese/Hindi/Chinese mobile cues
were inspected. Every one of Heart's 52 recorded English sentence midpoints
shows the expected native caption. Narration rendering remains in progress.

Lesson 81 now has eleven map-bound native 4K scenes: eight actual GUI routes,
including Barcode QC under Map and OPS under Mask, plus the real Help search
and local reference for the Python-only simulation function. No analysis,
download or simulation was requested. Recording-specific splitter/table
adjustments make inputs readable; the bogus default count path is cleared
through its actual button, and OPS Actions is folded through its heading.
Other module lessons provide the worked data examples. Lessons 79/80 still
need recordings, and 81 still needs staging, translations and full media.
Receipt: tools/tutorials/evidence/2026-09-23-sequencing-workflow-capture.json.
No full tutorial-set or publication acceptance is claimed.

2026-09-23 — CODEX: THIRD WORKFLOW CAPTURE
Lesson80 now has18 current native4K scenes:14 actual GUI routes and two
source-checked local API references. The four assay routes use the Toxoplasma
organism's real tiles. Network isolation makes the real local Help fallback
consistent across both API entries. Visual inspection caught clipped Gate
Editor controls; dragging the actual splitter makes Filter/Columns readable.
49 named capture-policy, CLI and workflow-map checks pass; shell syntax passes.
Receipt: tools/tutorials/evidence/2026-09-23-image-workflow-capture.json.
No analysis/download was requested. Lesson79 still needs capture, and80/81
need translations and media. All50 lesson78 tracks now pass technical audio
checks, with per-voice browser checks continuing. No listening or full-set
publication acceptance is claimed.

2026-09-23 — CODEX: COMPLETE OVERVIEW TECHNICAL MATRIX; DATABASE ROUTES
Lesson78 now has50/50 current technical audio and actual Chromium voice
checks,13/13caption selections and52/52native English sentence midpoints.
All reports bind to the same player and unchanged media. Receipt:
tools/tutorials/evidence/2026-09-23-pooled-screen-overview-voices.json.
This does not claim auditory approval or complete-set publication.

Recording lesson79 exposed a route error: Database Browser is available
through Help, not a Home tile. Its nested Lineage and Tabulate instructions
now say so in both the generated reference/API inserts and English script.
51 focused map/capture/CLI checks pass. The shared map and other three
scripts are unchanged. The full79-scene capture is still in progress.

2026-09-23 — CODEX: SEQUENCING SCRIPT REVIEWS BEGIN
Lesson81 has German and French source-bound reviews for all11 scenes each.
Both preserve explicit optical joins, experimental controls and identities,
prospective planning versus measured results, and the simulation API's None
return, bounded max_workers/grid and manual transfer to planning tools.
Isolated promotion checks preserve scene/link structure and validate French
spoken-form rules. These are AI-assisted editorial reviews; full staging,
remaining11 languages, audio and listening acceptance remain open.

2026-09-23 — CODEX: ALL MODULE ROUTES CAPTURED; ENGLISH MEDIA STAGED
Lesson79 now has a successful native capture of all77 modules in79 lesson
scenes and99 total4K frames. All scenes were visually checked in contact
sheets. Home/fold routes, ten direct Help routes, Database Browser children,
three genuine offline API references, classifier modes, Timelapse and Sweep
controls are covered. Empty-input Cellpose All and Diagnostics guards are
recorded honestly; no analysis/download was run. The Hits visibility fix is
now verified in the recording.51 focused map/CLI/capture checks pass.
Receipt: tools/tutorials/evidence/2026-09-23-module-reference-capture.json.
Lessons79,80,81 were staged in order after78, without bypassing the identity
guard. English narration is rendering on CPU; no media publication claim.

2026-09-23 — CODEX: ALL SEQUENCING LANGUAGE SCRIPTS REVIEWED
Lesson81 has all13 source-pinned language reviews covering143 scenes.
Danish, Hindi, Icelandic, Italian, Japanese, Korean, Norwegian, Swedish and
Chinese complete the earlier German/French/Spanish/Portuguese set. Final
checks verify current source/map hashes, exact technical-token counts,
unchanged scene/link structure and isolated promotion/pronunciation rules.
Receipt: tools/tutorials/evidence/2026-09-23-sequencing-workflow-translations.json.
These are AI-assisted editorial reviews, not native-speaker or listening
sign-off. Coherent full-catalog promotion awaits79/80 translations; English
media is rendering, remaining26 non-English voices and full checks are owed.

2026-09-23 — CODEX: SHARED PHRASE TRANSLATION AND GERMAN WORKFLOW SET
The map generator now accepts a strict reviewed phrase lookup while leaving
all four English scripts byte-equivalent as parsed JSON. A238-phrase German
bank generates79/80 narration and215 GUI map captions from the same text.
German79/80/81 are promoted together into the coherent stage after78; missing
siblings, stale sources and mixed-language batches fail before any write.
49 generator/promotion checks passed; the updated phrase suite adds a tested
file-format plural rule (11checks) while rejecting TIFF-to-PNG substitutions.
All four German scripts/116scenes are source-bound and structurally verified.
Default English Heart tracks for79/80/81 are complete (2758.8s,652.7s,371.2s).
Their shared4K masters and remaining English voices are running CPU-only.
Receipt: tools/tutorials/evidence/2026-09-23-german-workflow-phrases.json.
No media/listening/publication acceptance is claimed from script completion.

2026-09-23 — CODEX: FRENCH SHARED PHRASES AND COMPLETE SCRIPT STAGING
The238-phrase French bank now generates79/80 and supplies215 GUI map sources.
All four French lessons are staged with current English/map hashes; scene
structure and unrelated lessons are preserved. Live tr, source hashes and the
complete French runtime/API review loaders pass. GUI source freshness is
4626/5017(92.2percent),209 newly fresh sources plus the preview caption update.
A narrow file-format grammar correction accepts TIFFs -> TIFF in prose while
preserving quoted literals and filenames and rejecting TIFF -> PNG.102focused
Help/preview/literal/generator/promotion checks pass. French narration is now
rendering CPU-only. Receipt2026-09-23-french-workflow-phrases.json records the
editorial/technical checks; native-speaker, listening and publication remain.

2026-09-23 — CODEX: SPANISH SHARED PHRASES AND SCRIPT STAGING
Spanish adds another238-phrase bank,97generated79/80scenes and215GUI map
sources, with209newly fresh sources. Existing reviewed terminology is retained;
ROI and other scientific/code identifiers survive translation. All four
Spanish workflow scripts are staged and verified against current sources,
including spoken-form rules and unchanged scene/link structure. Live tr and
both full review loaders pass; unrelated staged lessons/catalog rows remain.
Receipt2026-09-23-spanish-workflow-phrases.json. Spanish narration is rendering
CPU-only. Reviewed scene coverage for the four new workflows is538/1508
(35.7percent across13languages); this is script coverage, not listening or
publication acceptance. Ten shared phrase languages remain.

2026-09-23 — CODEX: SEVEN WORKFLOW LANGUAGE SETS AND FOUR VIDEO MASTERS
Portuguese, Swedish, Chinese and Italian shared phrase banks join German,
French and Spanish. Each new bank has 238 reviewed phrases and generates
97 scenes for lessons 79/80. All four workflow scripts are staged and checked
per language, preserving unrelated lessons, source/map hashes, scene links
and spoken-form rules. Chinese/Portuguese/Swedish also share 215 GUI captions.
Reviewed workflow scene coverage is now 926/1508 (61.4 percent across 13
languages). Danish, Norwegian, Japanese, Hindi, Korean and Icelandic shared
banks remain. Twenty-four generator/promotion tests pass.
All four 4K video masters are rendered. Lessons 79/80/81 pass full video
decoding, source-fresh default Heart audio checks and actual browser playback,
including 540/130/68 sentence-caption checks and desktop/mobile inspection.
Their separate default-media receipts are in tools/tutorials/evidence/.
The overview already has its complete technical voice/caption matrix; the
other three still need that full matrix. English, French, Spanish, Portuguese,
Chinese and Italian remaining voice tracks are rendering on the CPU in the
private staging directory. None of these receipts claims native-speaker,
listening or complete-set publication acceptance. The release hold remains.

2026-09-23 — CODEX: JAPANESE WORKFLOW TEXT AND FRENCH PLAYBACK
Japanese adds 238 source-bound phrases and 97 generated lesson scenes. All
four staged scripts retain source/map hashes, links, scene structure and
spoken-form rules; unrelated lessons are unchanged. Workflow scene coverage
is now 1023/1508 (67.8 percent). The generator/promotion cohort passes 24
checks. Japanese narration is rendering CPU-only. Hindi, Korean, Icelandic,
Danish and Norwegian shared phrase banks remain.
All three French ff_siwis tracks now pass full technical audio checks and
actual Chromium playback, caption, seek and lesson-link checks. Their receipt
records exact audio hashes and decoded peaks; it does not claim listening,
native-speaker or screenshot acceptance. Complete-set publication remains held.

2026-09-23 — CODEX: HINDI WORKFLOW TEXT AND ITALIAN MEDIA CHECKS
Hindi adds 238 shared phrases, 97 generated lesson scenes and 215 matching
GUI map captions. Existing reviewed terminology is retained; 209 GUI sources
become current. All four staged scripts preserve source/map hashes, scene
structure, links and spoken-form rules. Live tr and the full runtime review
loader pass. Hindi GUI freshness reaches4442/5017 (88.5percent); workflow
scene coverage reaches1120/1508 (74.3percent). Twenty-four generator/promotion
checks pass. Korean/Icelandic/Danish/Norwegian banks remain. Hindi narration
is rendering CPU-only. Its receipt explicitly leaves incoming API debt open.
All six Italian tracks for lessons79/80/81 pass full audio decoding, source
freshness, timing/activity/peak checks and actual Chromium playback, captions,
seeking and lesson links. Exact hashes and limitations are recorded in
2026-09-23-workflow-italian-media.json. No listening/native-speaker or complete
publication acceptance is claimed; the complete-set release hold remains.

2026-09-23 — CODEX: NORWEGIAN WORKFLOW CAPTIONS
Norwegian Bokmal adds238source-bound phrases and97generated lesson scenes.
All four staged scripts preserve the current map/source hashes and scene
links; unrelated lessons remain unchanged. All three new caption routes pass
actual Chromium playback/seek/text/link checks with the English Heart track.
Workflow translation coverage is1217/1508 (80.7percent). Twenty-four generator
and promotion checks pass. Korean, Icelandic and Danish phrase banks remain;
Danish is in private drafting. Norwegian is caption-only, with no GUI locale
or narration claim. Native-speaker and complete-set publication acceptance
remain open. Receipt2026-09-23-nb-workflow-phrases.json covers the text checks.

2026-09-23 — CODEX: DANISH TEXT AND SPANISH/PORTUGUESE PLAYBACK
Danish adds 238 source-bound phrases and 97 generated scenes. All four staged
scripts preserve the checkpoint's map/source hashes and scene structure;
three Danish caption routes pass actual Chromium playback with English Heart.
Eleven shared banks bring translation coverage to 1314/1508 (87.1 percent).
Twenty-four generator/promotion checks pass. Korean and Icelandic banks remain.
Spanish and Portuguese each have nine completed tracks with full audio checks
and matching browser playback/caption/seek/link checks; separate media receipts
record their exact hashes. No listening/native-speaker acceptance is claimed.
This evidence is bound to map 76b4473ba6a9e05ced1f897984b6868843f12ecb5268f7209a2888c31fa86984.
Incoming implementation c94a48cd7 expands pathway inputs and annotation routes;
its new map/API/GUI sources require a subsequent documentation and tutorial
refresh. These checkpoint percentages do not imply acceptance of that update.

2026-09-23 — CODEX: EXPANDED PIPELINE MAP SYNCHRONIZED
Eleven existing workflow phrase banks now contain242current phrases each.
Seven new or expanded phrases per language generate22current tutorial reviews;
33obsolete phrase records retain their original text. Shared GUI updates add
49matching phrases plus nine translated parasite-pathway titles. Together
with27secondary-control help translations, this checkpoint adds85GUI records.
The nine walkthroughs now explain external input choices and the persistent
pipeline details interface. All545rendered walkthrough links and eight new/
changed API anchors resolve;55workflow/generator and31translated-dialog/Help
checks pass. The source-bound receipt is411_pipeline_details_and_workflow_
refresh_2026-09-23.json. Text scene coverage remains1314/1508(87.1%). Korean
and Icelandic shared banks remain. Additional pipeline descriptions are being
reviewed separately. The frozen previous media stage is unchanged: lessons79
and80 need updated narration/master timing against this map; prior media
receipts do not certify the new scripts. Listening and complete-set publication
remain open. Incoming recruitment/TTA behavior needs its own tutorial update.
