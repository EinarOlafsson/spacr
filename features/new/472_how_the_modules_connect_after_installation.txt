================================================================================
AFTER INSTALLATION: HOW THE MODULES CONNECT -- TUTORIALS, WALKTHROUGHS AND API
THAT SAY WHAT FEEDS WHAT, AND A HOME SCREEN THAT SHOWS WHERE TO START
================================================================================

Status:    OPEN; English workflow map, guide and pathway implementation in progress.
Issue:     GitHub #130, "Guide what to do after installation" (momeara,
           2026-09-22).
Owners:    CODEX -- the tutorials, walkthroughs and API prose (parts 1-4), the
           lane that already owns items 358, 447 and 286.
           CLAUDE -- the Home screen part (part 5).

THE ISSUE, IN THE REPORTER'S WORDS
--------------------------------------------------------------------------------

    "After installing the package, looking at the homescreen I can't figure
    out what do."
    "I would like to open a sample project that is similar to a project I may
    have and see the platform in action."
    "When I look at the tiles, I can click them and nothing seems to happen."
    "When I go online looking for the documentation, I don't see guidance on
    what to do next."
    "I also looks at the interactive video tutorials and it also seemed be a
    mix of installation, capabilities, and technical details without a clear
    guide for workflow usage."

THE MAINTAINER'S REQUEST (2026-09-22), in his words
--------------------------------------------------------------------------------

    "the user is asking for a clearer path between modals. so there should be
    a Tutorial giving an overview of how the modulus are linked. that the mask
    generation outputs are used as inputs for the measurements, and
    measurement data base as input for classify machine learning and
    measurement image crops a (or streamed from masks) as input for computer
    vision, and classify modual outputs and Map barecodes as inputs for
    regression. and so on.

    this should be clearly stated in a new tutorial on spaCR screens, and
    another on Modual inports and outputs (covering all moduals and how they
    relate to each other), also make a tutorial on image analasys related
    moduals and how they relate to each other, and on sequensing elated
    moduals and how they relate to each other.

    links between moduals should also be discribed in the API, and in the
    walkthroughs, add a new walkthrough that covers what to start with on the
    home screen if doing a spaCR screen (make-->measure-->annotate-->classify
    and map barecodes-->regression), doing a high content image analasys
    experiment (mask--> measure--> several ), generating a mode (make
    masks-->train cellpose model), if you can think of any other common
    experiments and pathways through spacr discribe them to.

    the API, walkthroughs, and Tutorials should match each other."

WHAT TO BUILD
--------------------------------------------------------------------------------

0. ONE SOURCE OF TRUTH FOR THE LINKS. Before any prose: a single machine-
   readable map of every module's inputs and outputs -- what it reads (folder,
   file kind, database table/column) and what it writes -- from which the
   tutorials, the walkthroughs and the API pages are all written or checked,
   so the three "match each other" by construction and a test fails when a
   module's real inputs/outputs drift from the map. Start from what the code
   already declares (settings `src`/output folders, the tables each module
   writes to measurements.db, the preview registry, the Home registry's
   groups). The chain the maintainer names must come out of it verbatim:

     Mask generation -> masks + merged arrays
       -> Measure (reads the masks and images) -> measurements.db + crops
       -> Classify, machine learning (reads measurements.db)
       -> Classify, computer vision (reads the crops, or streams them from the
          masks)
       -> Annotate (labels crops in png_list) -> the classifiers' training data
     Map Barcodes (sequencing) -> per-well guide/gene counts
     Classify outputs + Map Barcodes outputs -> Regression -> hit list

1. TUTORIAL "spaCR screens": the pooled-screen story end to end -- what a
   spaCR screen is, and which module does each step, in order.
2. TUTORIAL "Module inputs and outputs": EVERY module, what it takes and what
   it produces, and which modules come before and after it (from part 0).
3. TUTORIAL "Image analysis modules and how they relate": Mask, Make Masks,
   Measure, Annotate, Classify, Image UMAP, Gate Editor, Align & Stitch, the
   assays (Plaque, Recruitment, Invasion, Replication), QC, Embeddings.
4. TUTORIAL "Sequencing modules and how they relate": Map Barcodes, OPS,
   Regression, Power / Design, Experiment Design, Dose-Response, Run Compare.
5. WALKTHROUGHS (the interactive ones), one per common pathway, each starting
   from the Home screen and saying which tile to press first:
     a. A spaCR screen: Mask -> Measure -> Annotate -> Classify, and
        Map Barcodes -> Regression.
     b. A high-content image analysis experiment: Mask -> Measure -> then
        Classify / Image UMAP / Gate Editor / Graph Builder / the assays.
     c. Training a segmentation model: Make Masks (curate) -> train a Cellpose
        model -> use it in Mask.
     d. OTHERS the maintainer asked us to think of -- candidates:
        - a parasite assay: Mask -> Plaque / Recruitment / Invasion /
          Replication;
        - an optical pooled screen: Align & Stitch -> OPS -> Regression;
        - importing someone else's data: Import -> Measure -> ...;
        - phenotype discovery without labels: Measure -> Embeddings /
          Image UMAP -> Gate Editor -> Annotate;
        - planning a screen before running it: Experiment Design ->
          Power / Design;
        - live-cell tracking: Mask (timelapse) -> Measure -> motility.
       Each named from the Home tiles as they read today.
6. API: each module's API page (and the Home tile's API link) states its
   inputs, its outputs, and the modules before and after it, generated from
   the part-0 map so it cannot disagree with the tutorials.
7. The three -- tutorials, walkthroughs, API -- say the same thing: a test
   (or the docs build) compares each one's stated links against the part-0
   map.

PART 5 -- THE HOME SCREEN (CLAUDE)
--------------------------------------------------------------------------------

   a. "I can click them and nothing seems to happen": reproduce a Home tile
      click on a fresh install (a module that opens slowly with no feedback,
      a single- vs double-click expectation, a tile that opens off screen or
      behind, an empty state that looks like nothing happened) and fix it so
      every tile click visibly does something at once.
   b. "Open a sample project similar to mine": a clear first-run entry on
      Home -- choose the kind of experiment (the pathways in 5a-d), get its
      example data (the existing test-data downloads) and land in its first
      module with the settings filled, with the walkthrough for that pathway
      one click away.

DONE WHEN
--------------------------------------------------------------------------------

  * The four tutorials and the pathway walkthroughs exist in every tutorial
    language the library carries, and the API pages state inputs, outputs and
    neighbours for every module.
  * One map of module inputs/outputs exists and a test fails if the
    tutorials, walkthroughs or API disagree with it or with the code.
  * A Home tile click always visibly does something, and a new user can start
    a sample project of their kind from Home.
  * Issue #130 is answered with links to the tutorials and the Home entry.

2026-09-22 — CODEX: IMPLEMENTATION STARTED
The shared resource path is spacr/resources/module_workflows.json. Source
inspection is reconciling the live module/fold/preview registries and actual
artifact readers/writers before prose generation. Pathway IDs and ownership
are announced in the coordination channel. No tutorial, translation or Home
completion is claimed yet.

2026-09-22 — CLAUDE: PART 5 IS BUILT
  a. A module that cannot open now SAYS SO. `MainWindow._on_nav_selected`
     let a build failure escape into the Qt slot, where Qt printed it to a
     terminal the user does not have and carried on -- the reporter's "I can
     click them and nothing seems to happen". It is caught, the module and
     the missing package are named in a dialog and the status bar, and no
     half-built screen is kept (tests/qt/test_a_tile_that_cannot_open_says_so.py).
  b. "START A SAMPLE PROJECT…" is the first thing in Home's side column. It
     asks which kind of experiment the person has, opens that pathway's first
     module and loads that module's own example data
     (spacr/qt/widgets/sample_project.py, tests/qt/test_home_starts_a_sample_project.py).
     THE PATHWAYS COME FROM CODEX'S MAP, spacr/resources/module_workflows.json
     (announced in the channel, part 0), read as {"pathways": [{id, title,
     summary, modules}]}; until that file lands, three fallback pathways are
     used -- the pooled screen, high-content imaging, and training a
     segmentation model. When the map lands the dialog follows it with no
     further change, so the dialog and the tutorials cannot drift.
     STILL OWED on part 5: the walkthrough for the chosen pathway one click
     away, which needs Codex's walkthroughs to exist.
2026-09-22 — CODEX: FIRST ENGLISH MAP / GUIDE / WALKTHROUGH IMPLEMENTATION
spacr/resources/module_workflows.json maps 71 registered/nested/mode routes
and nine pathways. Existing spacr.ports declarations are copied exactly and
checked against current code, alongside live names, parents, Home/API routes
and key saved-column/table literals. Explicit handoff notes distinguish
conditional inputs, Plaque's direct image route, and the OPS join/aggregation
needed before Regression. This checks declared contracts and selected source
anchors; it does not establish runtime execution of all 71 workflows.

build_module_workflows.py generates workflows.rst, per-API input/output and
neighbour includes, and four tutorial authoring contracts. Sphinx refuses
stale generated artifacts. Help -> Walkthroughs exposes all nine pathways;
show_walkthrough(window, 'pathway:<id>') begins on Home, navigates to each
module/host, and never starts analysis. Real visual inspection found and fixed
a competing delayed first-run tour. The map is included in wheel/sdist data.

These are English implementation checkpoints, not four finished narrated
lessons. All-language pathway wording, actual new tutorial media, broader
runtime I/O evidence and Claude's Home-map adapter/walkthrough link remain.
The API/runtime rebuild and completed-set tutorial publication remain open.
Validation for this slice: 121 focused map/navigation/first-run tests pass
in 24.51 s; the preceding ports/artifacts selection passed 75 existing checks.
A real Sphinx HTML build of the guide and all generated API include fragments
passes with warnings treated as errors. Real 1280x900 Home and Experiment
Design pathway cards were inspected; the first-run overlay no longer covers
the requested guide. No full application/API-docs build is claimed.

2026-09-22 — CODEX: FOUR ENGLISH LESSON SOURCES AND FULL DOCS BUILD
The workflow map now generates real English lesson-source JSON for78-81, not
only authoring contracts:8 pooled-screen scenes,73 module-reference scenes,
16 image-analysis scenes and10 sequencing scenes. All start from Home. The
reference covers all71 mapped routes and explains each shared artifact's
storage/tables/columns on first use; the shorter lessons keep their relevant
branches. These are scripts awaiting recordings and all13 translated reviews,
not published tutorials. No module tutorial identity was replaced.
Tests tie rendered narration to map changes, preserve both regression inputs,
check classifier alternatives and prove all artifact locations are explained.
89 workflow/staging tests pass in5.96s; the prior pathway selection had29 passes.
A full Sphinx9.1.0 build under8G at83281540b found one ambient.py timing-table
alignment error; removing the leading margin space fixed it without changing
words or numbers. The repeated full -W build succeeds. Independent HTML checks
find all71 module contracts on63 API pages, all9 pathway anchors and resolving
guide links. Four built-site constructor/helper checks pass;8 nested-helper
fixture checks pass with the independent GUI-map hook explicitly isolated.
The existing stale-English-manifest assertion remains a separate open API debt.
Receipt:features/data/472_full_docs_build_2026-09-22.json.

2026-09-22 — CODEX: NINE-LANGUAGE GUI PATHWAYS
All47 new pathway titles/actions/notes and the formatted Home introduction
now have source-bound reviews in the nine GUI languages:423 translated rows.
An independent verifier checks live sources, hashes, catalog values, candidate
gates and actual formatted tr() output, and proves unrelated rows unchanged.
Receipt:features/data/472_workflow_translations_2026-09-22.json. This is an
AI-assisted semantic/technical review, not native-speaker signoff. Image UMAP
stays verbatim under the protected product-name rule; other module references
include their current localized label where available.
The actual-overlay test caught a double-translation bug: pathway builder had
already translated each step before the shared tour translated it again.
The shared overlay now accepts already-translated text explicitly, and its
labels skip a second generic translation pass. Ordinary tours keep their
existing translation behavior.45 pathway/module-tour tests pass. Real Hindi,
Chinese and French long-note cards were visually inspected at1280x900; text
and controls fit. Their private captures are in workflow-authoring-20260922.
Additional tutorial languages da/nb/it/ja, four tutorial translations,
recordings and media acceptance remain open; no claim of complete472 yet.
Final validation:72 focused pathway/first-run/onboarding/runtime-syntax/English
manifest checks pass in42.14s. The localized path test also runs the application's
generic translation pass over each visible step, proving the approved wording
survives both rendering paths.

2026-09-22 — CODEX: HOME USES ALL NINE PATHWAYS AND OFFERS THE TOUR
Following Claude's dated handoff, the sample-project dialog now reads the
object-shaped shared map instead of falling back to three legacy routes.
Titles, first-step guidance, complete module order and first Home tile all
come from that map. Selecting a sample retains the existing example-data
loader and adds a persistent Walkthrough button to the status bar; one click
starts the selected route at Home. Subsequent samples reuse the button with
their own pathway ID. No analysis starts merely by opening the walkthrough.
The nine-language titles and steps use the already reviewed pathway text;
54 new translations cover the six Home captions. The chosen description wraps
below the list, including the complete Hindi training text. Themed Hindi,
German and French dialogs were rendered at640x520, and the real MainWindow
status control was inspected at1280x900. A font propagation issue found during
that check is covered by a regression: the status bar refits after Qt settles
the inherited font, so the button is not clipped.
57 combined Home/pathway/map/runtime checks passed, followed by38 Home/pathway
checks after the layout refinements. Independent source/hash/gate/live-tr and
unrelated-row checks cover54 captions plus nine updated473 magnifier tooltips.
Receipt:features/data/472_home_pathway_integration_2026-09-22.json.
This completes the map adapter and one-click link left in Claude's handoff.
The four additional tutorial languages, recordings and final media acceptance
are still open; this is not complete472 acceptance or a publication claim.

2026-09-22 — CODEX: ORGANISM HOME ROUTES IN THE SHARED MAP
The authoritative map remains spacr/resources/module_workflows.json. It now
covers74 modules,37 artifacts and nine common pathways. Three organism guides
have explicit GUI-only inputs/outputs; they do not produce measurements.
The four available assays open under Toxoplasma. Plasmodium and Candida's
proposed analyses remain Coming soon, not executable pipeline claims.
Regenerated workflow prose, API includes and tutorial contracts agree with
that map; lesson79 now has76 scenes. The changed parasite-pathway note has
nine source-bound translations, with its older reviews preserved verbatim.
README grids in English and nine translations and the API landing grid now
show the three organism pages; the four assay API anchors remain in the
folded-module list. New SVG-derived tiles were visually inspected and their
opacity is covered by a regression.71 distinct README/grid checks pass.
Recording, further tutorial languages, narration review and final media
acceptance are still open. This entry does not claim tutorial publication.
