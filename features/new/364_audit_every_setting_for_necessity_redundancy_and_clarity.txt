================================================================================
EVERY SETTING AUDITED: IS IT NEEDED, IS IT A DUPLICATE, IS ITS NAME HONEST
================================================================================

Status:    OPEN, BUT NOT FOR THE REASON THIS LINE USED TO GIVE. All three
           things the maintainer approved on 2026-09-02 are BUILT and tested
           -- the retired _size pair, the four renames, and the per-object
           table. This header went on claiming the table was unbuilt for two
           days after the body of this file recorded it landing; corrected
           2026-09-05, see the last section for the evidence.
           What keeps 364 open is the tail: the organelle preprocessing gap,
           two proposals waiting on the maintainer, and the legacy-migration
           hole the 2026-09-05 check found in the renames. Proposals are in
           instructions/data/364_mask_and_measure_settings_audit_2026-09-02.txt.
Requested: 2026-09-02, verbatim -- "look through each moduals settings and
           evaluate if any settings are unnecessary, redundant or are not
           intuitive. then propose to remove settings that are unnecessary,
           combine redundant settings, and change the names of non intuitive
           settings. this should be done for all modules, starting with core
           moduals, and all of their settings including any settings in live
           previews, hyperparameter searches or other sub modules. start with
           the mask and measure moduals. after you are done with the moduals
           go on to the preferences in spaCR and spaceout."
Owner:     spacr/settings.py, spacr/validate.py (RETIRED_SETTINGS),
           spacr/qt/screens/preferences.py, spacr/qt/spaceout.py.

PROPOSE, DO NOT DELETE. The maintainer asked to be shown proposals. Each
phase produces a TABLE for review, and nothing is removed or renamed until
that table comes back approved. A setting removed by surprise is a settings
file in the wild that stops loading.

--------------------------------------------------------------------------------
THE SIZE OF IT, MEASURED 2026-09-02
--------------------------------------------------------------------------------

  get_timelapse_settings (mask, the superset)     203
  get_perform_regression_default_settings          87
  get_measure_crop_settings                        65
  get_automated_motility_assay_default_settings    55
  get_train_test_model_settings                    50
  get_analyze_recruitment_default_settings         30
  get_default_generate_activation_map_settings     27
  get_identify_masks_finetune_default_settings     23
  get_analyze_plaque_settings                      22
  get_check_cellpose_models_default_settings       18
  get_train_cellpose_default_settings              16
  get_plot_data_from_csv_default_settings          13
  get_default_apply_cellpose_model_settings        10
  get_default_test_cellpose_model_settings          9
  get_map_barcodes_default_settings                 5
  get_analyze_reads_default_settings                3

Mask alone asks 203 questions. That is the number to hold in mind: a user
who has to answer, or decide not to answer, 203 questions before segmenting
anything has been handed the maintainer's problem instead of a tool.

--------------------------------------------------------------------------------
WHERE THE REDUNDANCY ACTUALLY IS
--------------------------------------------------------------------------------

78 of mask's 203 settings are the SAME 20 QUESTIONS asked once per object
type -- `cell_`, `nucleus_`, `pathogen_`, `organelle_`, `cytoplasm_`:

  CP_prob x4        FT x4              Signal_to_noise x3   area_multiplier x4
  background x3     channel x4         diameter x4          intensity_merge x4
  ... and 12 more shapes

This is the single biggest finding and it is NOT simply redundancy to
delete: a user genuinely may want a different diameter for a nucleus than
for a cell. But 20 shapes x N objects is a presentation problem being
solved by multiplying the vocabulary, and instruction 326 has already
established that the object count must be ARBITRARY -- so this multiplies
without bound as soon as 326 lands. The two items meet here.

  THE LIKELY SHAPE: one question per shape, answered per object in a small
  table or per-object tab, rather than N flat settings per shape. That is a
  presentation change with a settings-file migration behind it, not a
  rename. Propose it as such.

--------------------------------------------------------------------------------
WHAT THE FIRST AUDIT FOUND (mask + measure, 2026-09-02)
--------------------------------------------------------------------------------

NOTHING IS DEAD. All 268 settings across the two modules are read somewhere.
An audit hunting for removable settings would have reported "nothing to do"
and missed every real problem. The axes that matter are REDUNDANCY and
NAMING.

THE ONE THAT IS A BUG, NOT UNTIDINESS: organelle carries BOTH
`_min_size`/`_max_size` and `_min_area`/`_max_area`. They mean the same
thing, and they are read by DIFFERENT code -- the `_size` pair by the batch
organelle mask writer, the `_area` pair by the shared filter the Qt live
preview uses. So tuning the preview and then running the batch applies a
different filter, silently. cell, nucleus and pathogen have only the `_area`
pair; organelle alone answers the question twice.

FOUR RENAMES PROPOSED, all where the tooltip must translate the name before
it can explain the setting: `_FT` -> `_flow_threshold`, `_CP_prob` ->
`_cellprob_threshold`, `_Signal_to_noise` -> `_signal_to_noise`, and
`_min_object_area` -> `_min_split_area` (that last one is not style: it is
the floor below which an object is never SPLIT, and its current name reads
as a variant of `_min_area`, which it is not).

AND A FINDING FROM OUTSIDE THIS ITEM: 59 of the 706 settings in the
generated `setting_api_targets.py` map point at a function that reads no
settings at all -- 30 of them at one guide-permutation CSV writer. That map
is what a setting's "where is this used" API link follows, and instruction
368 proposes building a settings-flow tree on it. It has to be right first.

--------------------------------------------------------------------------------
THE FOUR JUDGEMENTS, AND WHAT EVIDENCE EACH NEEDS
--------------------------------------------------------------------------------

UNNECESSARY -- propose removal. The evidence is not opinion:
  * nothing reads it (grep the package, not just the module);
  * it is read but every caller passes the default;
  * it exposes an implementation detail the user cannot reason about;
  * it duplicates something spaCR can measure or infer.
  A setting that only ever takes its default is a question with one answer.

REDUNDANT -- propose combining. Two settings are redundant when no
combination of their values is meaningful that one setting could not
express. Say which combinations become unreachable, because that is the
cost.

NOT INTUITIVE -- propose a rename. The test is whether the name says what
the user is deciding, in the user's vocabulary. `pathogen_Signal_to_noise`
mixes snake_case with a capitalised term of art; `FT` is an abbreviation of
something; `intensity_merge` does not say what merges with what. Check the
TOOLTIP against the NAME: where the tooltip has to explain what the word
means before it can explain the setting, the word is wrong.

  ONLY 14 TOOLTIPS ARE DEFINED against ~600 settings across all modules.
  Any rename proposal should note where there is no tooltip at all, because
  an unnamed, undocumented setting is the worst case and is invisible in a
  count of "settings that need work".

--------------------------------------------------------------------------------
ORDER OF WORK, AS ASKED
--------------------------------------------------------------------------------

  1. Mask (203)          -- start here; the per-object families are here
  2. Measure (65)
  3. The rest of the core modules: Annotate, Classify, Map Barcodes,
     Regression (87)
  4. Sub-surfaces the maintainer named explicitly and which are easy to
     forget because they are not on the module's main page: LIVE PREVIEW
     settings, HYPERPARAMETER SEARCH settings (train_test_model 50,
     check_cellpose_models 18, identify_masks_finetune 23), and the assay
     sub-modules (motility 55, recruitment 30, plaques 22).
  5. spaCR Preferences
  6. spaceout Preferences

--------------------------------------------------------------------------------
TRAPS
--------------------------------------------------------------------------------

* A REMOVED SETTING GOES IN `RETIRED_SETTINGS` in `spacr/validate.py`, so an
  old settings file loads with a message instead of an error. `corrected_manders`
  is the worked example.
* A RENAMED SETTING IS A REMOVAL PLUS AN ADDITION as far as every saved
  settings file, published tutorial, notebook and reviewed translation
  catalog is concerned. Each rename stales: the notebooks' generated setting
  documentation, `docs/i18n/reviewed/runtime/**` records pinned to the old
  tooltip text, and the tutorial JSON. Budget for that; instruction 337's
  two-word default change staled two reviewed records on its own.
* `settings.categories` MUST list every key, and a key added there without
  being registered in `KEYS_ADDED_BY_REGROUP` fails two tests. This has
  already caught one change this week.
* Do not confuse "unused by the GUI" with "unused". Several settings exist
  for the headless `spacr-run` path and notebooks only.

--------------------------------------------------------------------------------
HOW IT WILL BE CHECKED
--------------------------------------------------------------------------------

* One proposal table per module, reviewed by the maintainer BEFORE any edit:
  setting, judgement, evidence, proposed action.
* After each approved batch: the module's settings count falls, every
  removal is in `RETIRED_SETTINGS`, every rename has a tooltip, and an old
  settings file still loads.
* No setting is left whose tooltip has to define its own name.


--------------------------------------------------------------------------------
DECIDED AND DONE, 2026-09-02
--------------------------------------------------------------------------------

RETIRED, as approved: `organelle_min_size` and `organelle_max_size`. Both are
in `RETIRED_SETTINGS` mapped to their `_area` counterpart, so an old settings
file loads with a message rather than an error. The batch mask writer now
reads the `_area` pair the Qt live preview already used, so tuning the
preview and pressing run finally apply the SAME filter. Mask went 203 -> 201.

  THE DEFAULT CARRIED OVER, NOT RESET. `organelle_min_area` is now 10 -- the
  value the batch run actually used through the retired `_size` setting --
  rather than the 0 the preview had. Real runs are unchanged; it is the
  preview that stops disagreeing with them.

  AND A SECOND COPY WAS FOUND WHILE DOING IT. The mask factory hand-wrote
  `organelle_min_area`/`organelle_max_area` as 0 beside
  `_set_organelle_defaults`, which owns them per organelle type and sets 10.
  That shadowing is what made the preview read 0 while the batch read 10, so
  it is the other half of the same bug and is removed.

RENAMED, all four approved: 90 settings across 26 organelle slots.

    <object>_FT               -> <object>_flow_threshold
    <object>_CP_prob          -> <object>_cellprob_threshold
    <object>_Signal_to_noise  -> <object>_signal_to_noise
    <object>_min_object_area  -> <object>_min_split_area

  Done as a SUFFIX substitution rather than 90 literal renames, because the
  organelle slots are generated and a literal list would miss the next one.
  497 occurrences across 60 files, plus 94 more for the retirement.

  THE TOOLTIPS NOW STATE THE REAL PER-APP DEFAULTS. The surviving
  `organelle_min_area` tooltip said "Default 0 (disabled)" while Mask's
  default is 10 -- a claim that was already wrong and would have stayed wrong
  silently. It now names both, which also removed two entries from the
  tooltip-drift ledger: a variant the tooltip itself explains is not drift.

THE PER-OBJECT TABLE'S MODEL IS BUILT: `spacr/object_settings_table.py`.
`to_table(settings)` reads a flat settings dict as {question: {object:
value}} and `from_table` writes it back. Mask's 113 per-object keys become 55
questions.

  AND THERE IS NO SETTINGS-FILE MIGRATION AFTER ALL, which is the important
  part. This file assumed one; there is none, because the table is a
  PRESENTATION and the stored keys do not change. `from_table(to_table(s), s)
  == s` for both Mask and Measure, so no saved settings file, notebook,
  tutorial or headless `spacr-run` has to move. Storing a nested structure
  instead would have been a file-format change -- the presentation was what
  was wrong, so only the presentation changes.

  `widen(table, 'organellec')` is the operation 326 needs: one call, one new
  column, and the number of QUESTIONS does not move. It starts the new
  organelle where the first one is rather than at a global default nobody
  chose. In the flat vocabulary the same act is twenty new settings that
  every consumer, tooltip and translation catalog has to learn.

  TWO THINGS THE MODEL GETS RIGHT THAT A GUI WOULD OTHERWISE GET WRONG.
  Object prefixes are matched LONGEST FIRST, or `organelleb_min_area` files
  under `organelle` and twenty-six slots collapse into one column -- which no
  assertion about totals would catch. And a question an object does not ask
  is ABSENT from its row rather than present as None: cytoplasm is derived,
  so it has no channel and no diameter, and that is a different fact from a
  diameter nobody has chosen yet.

STILL TO BUILD: the GUI that renders it, and the same treatment for the
remaining modules in this file's order.

--------------------------------------------------------------------------------
2026-09-02: THE TABLE IS BUILT -- MODEL AND VIEW
--------------------------------------------------------------------------------

`spacr/object_settings_table.py` was the model and drew nothing; the view is
now `spacr/qt/widgets/object_settings_grid.py`. One row per question, one
column per object, and the button that widens it.

WHAT IT COSTS THE SETTINGS FILE: nothing, and that is the whole design.
`to_table` reads a flat dict and `from_table` writes it back, so the stored
keys are unchanged and no settings file, notebook, tutorial or `spacr-run`
invocation migrates. `test_showing_a_settings_dict_changes_nothing_in_it`
round-trips Mask's own 201 defaults and checks the TYPE of every value as
well as its value.

TWO THINGS THE VIEW HAD TO GET RIGHT, both of which corrupt a settings file
rather than merely look wrong, and both now pinned:

  * A VALUE KEEPS ITS TYPE. A table hands back strings. `cell_background` is
    an int and `organelle_cellprob_threshold` is a float -- the SAME question
    with two types, which is exactly the case a single "cast to the row's
    type" rule gets wrong. Where the cell being replaced is None there is no
    type to copy, so the same question asked about another object is used
    instead: a diameter is a diameter whether it is a cell's or a nucleus's.
  * A QUESTION AN OBJECT DOES NOT ASK STAYS ABSENT. `cytoplasm` is derived --
    cell minus the rest -- so it has no channel, no diameter and no detection
    method. Those cells are not editable, because a value typed there invents
    a settings key nothing reads. They read as blank rather than as `auto`,
    which is a different claim: `auto` means "work it out" and is what an
    unset diameter says.

WHERE IT MEETS 326. Adding an organelle is `widen()` -- one column, seeded
from the first organelle's answers rather than from a global default nobody
chose. The question count does not move. In the flat vocabulary the same
operation was twenty new settings that every consumer, tooltip table and
translation catalog had to learn, which is why 326's ceiling could not be
raised before this existed. The column headers are NUMBERED ("Organelle 2"),
never lettered, per the maintainer on the same day.

The ceiling is refused out loud rather than hit silently: at 26 the button
says the slots are lettered and the alphabet is what runs out.

STILL TO DO: mounting the grid on Mask's own settings form in place of the
78 flat rows. The widget is finished and tested against Mask's real defaults
(`tests/qt/test_the_per_object_settings_are_one_table.py`, 13 tests), and
the mount is NOT a small edit. Read before starting it, because the seam
looks like a two-line change and is not:

  * THE SEAM IS `_split_rows_by_object` (settings_model.py:2219) and its one
    caller at 8312, which turns a family's rows into one sub-section per
    object. Replacing those children with a single grid section is the
    obvious move.
  * WHAT MAKES IT NOT OBVIOUS is what a "row" is by the time the screen has
    it. `AppScreen` reads each row as `(label, widget)` and then: derives the
    setting key from the widget (`_key_of_field`); decides from that key
    whether the run has an object for it and DEFERS the caption if not;
    hands the key list to `SettingsWidgets.remember_section_rows`; lets the
    settings search index the row by key; and `tests/qt/test_all_module_
    smoke.py` reads every entry as a labelled setting row. A QTableView
    holding 78 keys satisfies none of those five contracts.
  * SO THE REAL WORK is teaching the row machinery about a SPANNING
    COMPOSITE row that owns many keys -- what its key list is, how the
    search reaches a cell, what "hidden because the run has no organelle 3"
    means when the slot is a column rather than a row. That is a design
    change to the most-used screen in the application, and it is worth doing
    deliberately rather than in the same afternoon as the widget.
  * THE WIDGETS THEMSELVES SHOULD SURVIVE THE MOUNT. `collect()` reads
    `self._widgets`, and with it every coercion, canonicalisation and the
    organelle-slot rule in `_organelle_slots_worth_keeping`. The cheap and
    safe shape is therefore a grid that EDITS THE EXISTING WIDGETS and a
    flat row that is hidden rather than deleted -- not a grid that owns the
    values and a widget dict that has lost them.

REGRESSION IS AUDITED, 2026-09-02 -- see
instructions/data/364_regression_settings_audit_2026-09-02.txt. 88 settings,
nothing dead (the same scan that found nothing dead in mask and measure's
268), and six findings. The two worth the maintainer's attention first:

  * `Toxoplasma` and `annotation_source` ARE THE SAME REQUEST, and this is
    not an inference -- `spacr/ml.py`'s own reconciler says so in its
    docstring. Propose retiring the checkbox and keeping the field, with
    `Toxoplasma` accepted as a legacy spelling the way `toxo` already is.
    One case must survive: `Toxoplasma=False` with no field means NO
    annotation, and a careless migration would turn that into the default.
  * NINE SETTINGS SAY "CONTROL" AND MEAN FOUR DIFFERENT THINGS -- an
    identifier, a set of wells, non-targeting gRNAs, and an invasion-assay
    stain baseline. GitHub issues 91 and 92 are both this: a user putting
    well names into the identifier settings. `positive_control` also means a
    COLUMN VALUE in the machine-learning screen and a GENE/gRNA ID in gRNA
    regression -- one key, two meanings, chosen by which family runs.

CLASSIFY IS AUDITED TOO, 2026-09-02 -- see
instructions/data/364_classify_settings_audit_2026-09-02.txt. 114 settings,
nothing dead, seven findings. One of them should not wait for a rename batch:

  * `intermedeate_save`'s TOOLTIP IS FALSE. It tells the user "It currently
    has no effect ... those checkpoints are written regardless of this flag",
    and `spacr/io.py:_save_model` plainly branches on it -- `False` gives an
    empty threshold list, the comment beside it says so, `deep_spacr` passes
    the setting through, and four test files exercise `False`. So the control
    works and its own documentation tells everyone it does not. Anybody who
    wanted to stop the extra checkpoints read that and gave up. It is also
    the only misspelled key in the module.

The rest are the familiar shapes: `cross_validation` and
`cross_validation_enabled` are two switches for one question;
`gradient_accumulation` is a boolean that `gradient_accumulation_steps = 1`
already says; `normalize` and `normalization` are two letters apart and mean
different things at different stages; and `min_max` is the plate heatmap's
colour limits wearing the name of a normalisation method.

Annotate and Map Barcodes are the remaining core modules. Then preferences
and spaceout as the request asks.

ANNOTATE AND MAP BARCODES ARE AUDITED, 2026-09-02 -- see
instructions/data/364_annotate_and_map_barcodes_settings_audit_2026-09-02.txt.
That completes the six core modules. Annotate: 23 settings in its defaults
factory, 31 counting the screen's own table, 44 counting the Cells-tab
picture panel. Map Barcodes: 18 declared, 20 that the pipeline actually
reads, plus 5 in a factory nothing calls. Thirteen findings, and this pair
does NOT look like the first four.

  SOMETHING IS DEAD AT LAST, AND IT SAYS SO ITSELF. `barcodes` and `grna`
  are in `get_map_barcodes_default_settings`, which is called by nothing
  under spacr/. Their own tooltips state it -- "Nothing in the current code
  reads this key ... so setting it has no effect" -- and the grep confirms
  it: `settings['grna']` and `settings.get('grna')` appear nowhere. Both are
  still typed, tooltipped, categorised under Paths, and translated into all
  ten catalogs.

  WHICH MEANS THE "NOTHING IS DEAD" SCAN HAS A HOLE. The file-count scan the
  first four audits used says `grna` is named in 66 files and `barcodes` in
  46 -- every one of them `grna_csv`, `barcodes_column.csv` or the pandas
  column `df['grna']`. It is the same collision `test_dead_settings.py`
  already exempts `nc` and `pc` for. The four earlier "nothing is dead"
  results are probably still right -- those modules' keys are long and
  distinctive -- but they are worth re-checking with the stricter test
  (grep for `settings['<key>']`, not for the bare word) before they are
  relied on.

  AND THE ONE THAT COSTS A USER SOMETHING EVERY SESSION: ANNOTATE HAS TWO
  DEFAULT TABLES THAT DISAGREE ON 15 OF THE 21 KEYS THEY SHARE, TEN OF THEM
  SUBSTANTIVELY. `set_annotate_default_settings`
  is what every tooltip documents; the screen does
  `self._settings = AnnotateSettings()` (annotate.py:2237) and never consults
  it. `outline_sigma` is 4 in the factory and 1.0 on screen, and its tooltip
  says "Default 4." `crop_source` is the serious one: the factory ships
  'png' under a capitalised comment citing instructions 170 and 171 -- "This
  shipped 'auto', which takes the PNG folder whenever one exists ... and
  unaskable when a user wants the other" -- and the dataclass still starts at
  'auto'. The decision landed in the factory and the annotation screen never
  saw it.

  A RELATED ONE THAT IS PURE NAMING: `img_size` (factory, int) and
  `image_size` (screen, tuple) are one question, and the screen's own
  settings-CSV importer looks for `image_size` while the defaults factory
  writes `img_size` -- so a settings file made from spaCR's own defaults
  silently sets no image size.

The rest are the familiar shapes plus one new one. `threshold`'s tooltip
offers quantile codes 'q1'-'q9' that no code in the package parses, so acting
on it raises `ValueError` on a worker thread; the same tooltip pair claims
`>=`/`<=` where `_apply_threshold` is strictly `>`/`<`. `expected_end` is a
window LENGTH whose own docstring has to say "window *length*, not an end
coordinate" twice. `n_jobs`'s shared tooltip tells the Map Barcodes user that
-1 means every core, and `_chunk_worker_count` raises on anything below 1.
`image_type` / `object_array` / `object_type` are three names for "which
object", one per crop source, and the panel already copies one into another.
`cv_group_by` and `object_size` are the first two settings in five modules
that are genuinely UNNECESSARY -- offered by Annotate, never acted on by it.

  THE NEW SHAPE, and it is what let the rest happen: ANNOTATE IS NOT
  REGISTERED AS A SETTINGS MODULE. `settings_model.py:295` returns
  `{"src": "path to images"}` for it, and it is absent from
  `GUI_MODULE_DEFAULTS`, so `test_every_setting_a_module_offers_has_a_
  category` never runs for it. 28 of its 44 keys have no category, 28 have no
  `expected_types` entry -- which is exactly why `edge_image` still ships the
  STRING 'False', a truthy value for a checkbox -- and 7 are declared nowhere
  at all: `object_filters`, `outline_method`, `grid_rows`, `grid_cols`,
  `stored_channel_order`, `display_order`, `display_primaries`. Three of
  those are combo boxes on the form with hand-written setToolTip strings.
  Registering Annotate the way every other module is registered would have
  caught most of this file on its own.

Preferences and spaceout are next, as the request asks, followed by the
sub-surfaces it named: live previews, the hyperparameter searches, and the
assay sub-modules.

--------------------------------------------------------------------------------
2026-09-03: THE PREVIEWS / SEARCHES / ASSAYS AUDIT LANDED, AND ONE FINDING WAS
A LIVE REGRESSION
--------------------------------------------------------------------------------

`instructions/data/364_previews_searches_and_assays_settings_audit_2026-09-02
.txt`: 1,188 lines, 16 findings. Mask live preview 95 rows, Crop preview 27,
Timelapse 14, Motility 11; the three hyperparameter surfaces 23 / 18 / 50 plus
16 run controls; recruitment 30, invasion 37, replication 25, analyze_plaques
22.

INVASION AND REPLICATION ARE CLEAN. 62 settings between them, nothing dead,
no duplicate pair, no tooltip contradicting its code. Recorded as a result,
because "we looked and it was fine" is one.

TWO FINDINGS ACTED ON THE SAME NIGHT, both verified before and after:

  * THE MASK LIVE PREVIEW HAD STOPPED READING MASK. `b7ae412af` renamed four
    settings families by suffix substitution on names beginning with an
    underscore; three tables in `live_preview.py` spell them without one, so
    the substitution walked past them. The panel propagated
    `cell_flow_threshold` OUT and read `cell_FT` back IN. Fixed; a worktree
    at `b7ae412af^` proves the rename broke exactly three tests and the
    fourth was already red.

    I then swept the package for other tables keyed on a bare suffix. The
    remaining `FT` / `CP_prob` / `Signal_to_noise` are a DIFFERENT and
    intentional family -- the standalone apply/test-model submodules use
    unprefixed names, and `FT`'s own tooltip says it is "the counterpart of
    the Mask module's per-object <object>_flow_threshold". So no further
    breakage. It does leave two names for one quantity, which is a
    redundancy finding for this instruction rather than a bug.

  * `expected_types["background"] = str`, so spaCR's own untouched defaults
    failed spaCR's own pre-flight with severity ERROR on three modules, and
    `coerce_expected_types` preserved the string into a numpy comparison.
    Fixed to `(int, float)`.

ONE FINDING DELIBERATELY NOT ACTED ON TONIGHT, and the reason is the shape of
the fix rather than doubt about the finding:

  RECRUITMENT OFFERS THREE SETTINGS IT NEVER READS. Measured inside
  `analyze_recruitment`, its only consumer: `cell_mask_dim`,
  `nucleus_mask_dim` and `pathogen_mask_dim` have ZERO reads each, against
  four each for their `*_chann_dim` twins. The form interleaves the dead
  three with the live three in one section (`settings_model.py:1731-1735`)
  under a hint reading "A wrong index here measures the wrong compartment
  without complaining" -- so half that section does nothing and its tooltips
  describe `measure_crop`.

  WHY IT WAITS: the keys are shipped by `get_analyze_recruitment_default_
  settings` in `spacr/settings.py`, and `recruitment` is in
  `GUI_MODULE_DEFAULTS`, so `test_every_setting_a_module_offers_has_a_
  category` runs for it. Dropping them from the panel alone leaves three
  factory keys uncategorised; dropping them from the factory is a change to
  the other session's file with a blast radius I cannot measure end-to-end
  without running recruitment on real data. Both halves, together, with the
  module exercised -- not one half at one in the morning.

---

2026-09-03, overnight. THE GRID IS BOUND, AND THE RETIREMENT IS FINISHED.

THE MOUNT IS NO LONGER AN OPEN QUESTION. This item warned that mounting the
per-object grid on Mask's form is "a design change to the most-used screen"
and should not be done in the same afternoon as the widget. Reading the seam
properly showed the shape it proposed -- "a grid that EDITS THE EXISTING
WIDGETS" -- is cheap, because every primitive it needs already exists and is
proven in other work:

  * `SettingsWidgets.set_value_for_key` writes a value into the widget for a
    key and handles every widget class. Live Preview's "Propagate settings"
    already uses it.
  * `collect()` reads the other direction.
  * `_set_row_visible(key, False)` hides a row, LABEL AND ALL, and
    `_guard_hidden_rows` keeps it hidden against something else showing it --
    which is 364's "hidden rather than deleted" half, already built for the
    rule that hides an object whose channel names no plane.

`spacr/qt/widgets/object_grid_binding.py` is the write-through binding, with
11 tests. It is NOT mounted on Mask. What remains is genuinely small:

  1. Build the grid into Mask's form as one section.
  2. Seed it, and reseed when a settings file is applied.
  3. Hide the flat per-object rows through a set kept SEPARATE from
     `_hidden_by_the_run` -- that set is recomputed from scratch on every
     `refresh_object_visibility` pass, so grid-hidden rows put into it would
     be shown again on the next pass. Union the two in the pass.
  4. Decide whether the grid is on by default or behind a preference.

Only 4 is a question for the maintainer.

WHAT THE BINDING PINNED THAT IS WORTH KNOWING: reading both sides BEFORE
writing either is what protects values, not the reentrancy guard. The guard
only stops the table being rebuilt under the cursor. They were conflated in
the first draft of the test, which passed against the guard removed.

THE RETIREMENT IN b7ae412af HAD STOPPED HALFWAY, and it left three tests red
on nightly. Same cause as the live-preview aliases fixed last night: the
suffix substitution walked past every spelling that was not its literal.

  * `measure_crop` read `{role}_min_size` while the factory shipped none, so
    the organelle size filter never ran -- silently, because a missing key is
    falsy.
  * Measure's first organelle slot was seeded by hand and never given a size
    floor at all, while slots 2..n got one.
  * `_translate_legacy_setting_keys` never consulted RETIRED_SETTINGS, so
    `spacr-doctor` reported a rename for a file whose value the panel had
    just dropped. It now does, with a suffix rule for the generated slots,
    since the table names only the first one and a run can declare
    organellez.

Fixed in 2120c905f. The two stale test lines were left by the same
substitution; the legacy-CSV one now pins the migration rather than asserting
the pre-migration names.

SEPARATELY: `docs/setting_consumers.json` was stale at HEAD -- every "where
is this setting read?" link pointed at line numbers 14 to 36 lines out.
Confirmed not mine by regenerating at clean HEAD in a worktree. Regenerated
in 1f39dfaf0, on its own so the churn did not bury the fix.

THREE ORGANELLE SETTINGS EXIST IN THE CODE AND NOWHERE ELSE. Found by
sweeping `measure.py` and `io.py` for keys the code reads that no factory
ships -- the generalisation of tonight's `{role}_min_size` bug.

`io.py:1686-1691` reads `organelle_background`, `organelle_signal_to_noise`
and `remove_background_organelle` as optional overrides of the generic
values, with an explicit fallback, under the comment "use organelle-specific
settings when present, otherwise the generic defaults above". No factory
ships any of the three and no form offers them, so the only way to reach them
is to hand-edit a settings CSV.

The other three objects have all three keys each. So an organelle is the one
object whose background subtraction cannot be tuned, while
`organelle_signal_to_noise` MULTIPLIES a background the user cannot see --
and 326 makes organelles first-class and arbitrary in number.

NOT DONE, and the reason is the same as the recruitment finding above: the
keys belong to `spacr/settings.py`, and the fix is only correct if the
shipped defaults equal what the fallback currently produces, which has to be
read out of the calling frame rather than guessed. Behaviour-neutral is
achievable here -- it is a question of confirming the generic value, not of
choosing a new one.

`measure.py` swept clean after tonight's fix. The sweep is worth keeping as a
test -- it is the check that would have caught the retirement stopping
halfway -- but NOT as written: a naive regex over `settings['x']` also
matches inside docstrings (`io.py:3494` names two keys in prose) and matches
WRITES (`io.py:7557` assigns `nr_classes`). Both were false positives here.
It needs to read the AST and count reads only, and to be seeded with the
metadata/annotation keys (`metadata_rules`, `annotation_columns`,
`annotation_values`, `metadata_type_by`) which come from a settings family
outside the per-module factories rather than from nowhere.

---

2026-09-03, 06:20. THE MOUNT IS DONE, and the one question this file said
needed the maintainer is answered by making it a preference.

`Preferences -> Appearance -> "Per-object settings as a table"`, OFF by
default. Clear, `_mount_the_object_grid` returns before touching anything and
the form is exactly what it was. Set, the 78 per-object rows are hidden and
the grid takes their place.

WHAT MADE IT SAFE, and both were found by reading rather than guessing:

  * `Section.add_row` records the pair in `_row_widgets`, and the module
    smoke test takes every entry there to BE a labelled setting with its own
    `settingKey` and linked API help. `add_prose_row` exists for exactly that
    distinction and is what the grid goes in through. This was the blocker I
    had recorded as a reason to wait; it turned out to be a solved problem
    with a method already written for it.
  * `refresh_object_visibility` recomputes its hidden set from scratch on
    every pass, so the grid's keys live in `_hidden_by_the_grid` and are
    unioned in. In `_hidden_by_the_run` they would have reappeared the first
    time a channel was typed -- which is the trap this file predicted.

MEASURED, NOT ASSERTED: `collect()` returns the same keys AND the same values
with the grid on and off, so a settings file written either way is the same
file. The module smoke test, the caption contract, the settings search and
the object-gating suites were all re-run with the grid FORCED ON -- 221 tests
-- because "off by default" is only safe if on is also correct.

WHAT IS LEFT ON THIS ITEM: nothing that is mine. The recruitment finding and
the organelle preprocessing gap above both need `spacr/settings.py`, which is
the other session's file, and both are recorded with the measurement that
found them.

--------------------------------------------------------------------------------
2026-09-04 -- THE RECRUITMENT FINDING IS ACTED ON, AND ITS BLOCKER IS GONE
--------------------------------------------------------------------------------

This file closed with "nothing that is mine": the recruitment finding needed
`spacr/settings.py`, which belonged to the other session under 325. That
session has been retired and the maintainer confirmed ownership of every lane,
so the blocker was administrative rather than technical.

RE-MEASURED BEFORE ACTING, because a finding two days old is a claim:

    reads inside analyze_recruitment, its only consumer
      cell_mask_dim       0        cell_chann_dim       4
      nucleus_mask_dim    0        nucleus_chann_dim    4
      pathogen_mask_dim   0        pathogen_chann_dim   4

    references anywhere in spacr/submodules.py: none of the three

Both halves together, which is what this file said it was waiting for: the
rows come off the form and the keys off
`get_analyze_recruitment_default_settings`.

THE ONE JUDGEMENT THIS NEEDED, and it is the trap 375 records for the same
table: NOT `RETIRED_SETTINGS`. Those three keys are declared by three
factories -- recruitment, `get_measure_crop_settings` and
`set_default_plot_merge_settings` -- and are read in the latter two, where
they name a plane of the merged stack. RETIRED_SETTINGS warns against exactly
this case in its own comment, because naming a key there that `spacr.settings`
still declares warns a user off a setting that works. The retirement is
therefore scoped to recruitment and nothing global changed.

THE HEADING FOLLOWED THE ROWS. "Mask & Channel Mapping" over a section with no
mask rows is the same dishonesty in smaller type, and its blurb promised a
mask index the module does not use. Renamed to "Channel Mapping", and the
existing per-app blurb was rewritten rather than a new shared entry added --
one CHANGED string across nine locales instead of nine new ones, which matters
while 306 is already carrying 2,646 owed translations.

276 passed, 1 skipped across the recruitment, settings-category and
retired-key selection. The skip is a missing pypdf and predates this.

WHAT IS LEFT ON 364 NOW: the organelle preprocessing gap recorded above, and
the remaining smaller modules in this file's own order -- motility 55,
train_test_model 50, activation 27, identify_masks_finetune 23, plaques 22 and
the rest. The two core modules the maintainer named first are done.

--------------------------------------------------------------------------------
2026-09-04 -- MOTILITY AUDITED (55 settings). ONE DEFECT, AND IT IS THE
              CLASSIFY SHAPE AGAIN
--------------------------------------------------------------------------------

See instructions/data/364_motility_settings_audit_2026-09-04.txt. PROPOSALS
only; nothing in the module was changed.

  * NOTHING IS DEAD, and the near-miss is worth recording. Two of the 55 are
    never referenced in `timelapse.py`, which owns the assay. One of them,
    `motility_analysis`, is read twice in `spacr/object.py` as the gate that
    decides whether the assay runs at all -- it reads as dead from the module
    that consumes it and removing it would have taken away the module's own
    switch. Checked before proposing, which is the whole method.

  * `infection_pca_n_clusters` IS the defect, and it is classify's finding 0
    again: not a dead setting but a live one whose own tooltip says it does
    nothing -- "Not currently honoured: the pca/umap/tsne QC always runs
    KMeans with exactly two clusters". Verified in the code rather than taken
    from the tooltip: four KMeans calls, every one hardcoding n_clusters=2,
    and the key read nowhere. It is offered all the same -- factory, type map
    and rendered form row.

    It is also, unlike the recruitment keys, declared by ONLY this factory, so
    it is a genuine candidate for RETIRED_SETTINGS rather than a scoped
    removal. Recommended end: remove it, because a binary infected/uninfected
    call has no cluster count to choose.

  * NO REDUNDANT PAIRS, which is worth saying after mask's 78-of-203. The
    twelve `infection_xgb_*` settings are one classifier's twelve
    hyperparameters, each read once by the model it configures.

  * TWO NAMES RECORDED AND NOT PROPOSED: `zscore_thresh` names the statistic
    rather than the decision, and `straightness_filter` removes tracks rather
    than flagging them. Both are one-line renames with a migration each, not
    worth a settings-file change on their own; recorded so they ride along if
    this module is touched.

REMAINING IN THIS FILE'S ORDER: train_test_model 42, activation 27,
identify_masks_finetune 23, plaques 22, check_cellpose_models 18,
train_cellpose 16, plot_data_from_csv 13, apply_cellpose_model 10,
test_cellpose_model 9, map_barcodes 5, analyze_reads 3. Plus the organelle
preprocessing gap already recorded above.

--------------------------------------------------------------------------------
2026-09-04 -- THE REMAINING ELEVEN MODULES SWEPT. THE MODULE PHASE IS COMPLETE.
--------------------------------------------------------------------------------

The request was "this should be done for all modules, starting with core
moduals". The core modules were audited on 2026-09-02 and motility earlier
tonight; this is the rest of the list, swept in one pass for the two questions
a machine can answer honestly -- is any key never read, and does any tooltip
admit the setting has no effect.

    train_test_model                42 settings    clean
    analyze_recruitment             28 settings    clean (after tonight's fix)
    generate_activation_map         27 settings    clean
    identify_masks_finetune         23 settings    clean
    analyze_plaque                  22 settings    clean
    check_cellpose_models           18 settings    clean
    train_cellpose                  16 settings    clean
    plot_data_from_csv              13 settings    clean
    apply_cellpose_model            10 settings    clean
    test_cellpose_model              9 settings    clean
    map_barcodes                     5 settings    the known dead factory
    analyze_reads                    3 settings    clean

NOTHING NEW WAS FOUND, and that is the result rather than an absence of one.
Ten of the twelve are clean on both questions. The eleventh, map_barcodes, is
the dead factory the 2026-09-02 annotate audit already recorded -- checked
here against that file rather than re-derived, because re-deriving a known
finding and presenting it as new is how a list grows without moving.

  Re-confirmed only because it is cheap: `get_map_barcodes_default_settings`
  is defined at settings.py:1975 and called nowhere. Its `barcodes` and `grna`
  tooltips say so themselves, and the live equivalents `row_csv`,
  `column_csv` and `grna_csv` carry 59 references each.

SO THE MODULE PHASE OF THIS INSTRUCTION IS DONE. Every settings factory in
`spacr/settings.py` has now been audited for dead keys, redundant pairs and
dishonest names. Two defects came out of the whole sweep and both are the same
shape -- a live setting whose own description says it does nothing:

    map_barcodes   `barcodes`, `grna`      known since 2026-09-02
    motility       `infection_pca_n_clusters`   found 2026-09-04

WHAT REMAINS ON 364 is no longer a sweep: the two proposals above wait on the
maintainer, the organelle preprocessing gap is recorded above, and the two
motility renames are parked until that module is touched for another reason.
The preferences and spaceout half was audited on 2026-09-02.


================================================================================
2026-09-04  THE PER-OBJECT TABLE: THREE DEFECTS, ALL MINE, ALL FROM ONE CHANGE
================================================================================

The table was asked to show only the settings "common between all objects".
That was implemented literally, and literally was wrong in three ways that
took a user report and two tests to find. Recording them together because
they are one mistake wearing three faces: I picked a rule that was easy to
state instead of measuring what the rule would select.

FACE ONE -- "ALL" EMPTIED MEASURE COMPLETELY.
Reported as "in measure there are way more per object settings than are shown
in the per object settings pannel, e.g. mask dim, max size". Measured:

    measure     mask_dim   cell, nucleus, pathogen, organelle   (not cytoplasm)
                min_size   cell, nucleus, pathogen, organelle
                max_size   three of the five
                min_area   one
                type       one

Cytoplasm is DERIVED -- it is what is left of a cell once the nucleus and
pathogen are taken out -- so it does not answer most questions an object
answers. Requiring all five objects therefore selected NOTHING, and Measure's
table was empty rather than short. A blank cell already reads as "this object
does not ask", and the model draws it as not-editable, so a question three
objects share was always a perfectly good row.

FACE TWO -- "MORE THAN ONE COLUMN" MADE THE ROWS DEPEND ON THE COLUMN COUNT.
The obvious repair. It is wrong because a new organelle is SEEDED FROM THE
ONE BEFORE IT, so every question only organelle A asked suddenly has two
askers and qualifies. Pressing Add went from 20 rows to 54. Caught by
`test_one_more_organelle_is_one_more_column`, which asserts the question
count does not move -- a test that existed for a different reason and paid
for itself here.

THE RULE THAT HOLDS is KINDS, not columns: every organelle counts once
between them. A question is per-object because DIFFERENT KINDS of object ask
it, and two organelles are the same kind asked twice. `_kind_of` in
`object_settings_grid.py`.

FACE THREE -- THE TABLE HAS TO EARN ITS PLACE, and the filter is not what
decides that. Asked whether per-object settings belong in regression and
classify. Measured:

    mask/timelapse   20 questions x 3 objects      a table
    measure           3 questions x 4 objects      a table
    regression        2 questions x 2 objects      NOT a table
    classify          0                            NOT a table

Regression shares `area_outlier_mads` and `intensity_outlier_mads` between
cell and nucleus. That is genuinely per-object and genuinely four cells, and
replacing a four-row form with a table, a header, an Add button and a resize
grip is a heavier control than the thing it replaces. `MIN_GRID_QUESTIONS`
in `app_screen.py` is that threshold.

THE LESSON, which is the same one 306 recorded from the other direction:
measure what a predicate SELECTS, against the real dictionary, before
believing the sentence that describes it. "Common to all objects" and
"shared between objects" sound like the same rule and differ by every
per-object setting Measure has.

--------------------------------------------------------------------------------
ALSO FIXED, same day, same area
--------------------------------------------------------------------------------

THE SWITCH WAS READ ONCE, WHILE THE PANEL WAS BEING BUILT. So turning the
per-object table on did nothing to a module already open, and turning it off
left the table on screen with the rows it speaks for still hidden. Reported
as "if you have per object table off and open a modual then turn it on it
dosnt go on, same if you have it on". `apply_object_grid_preference` now
mounts or unmounts on demand and Preferences broadcasts to every open screen.
The off direction is the one worth guarding: the flat rows are HIDDEN while
the grid speaks for them, so a table that vanished without releasing them
would leave those settings in no table and no form.

THE BINDING WAS ONE-WAY. The grid wrote through to the widgets -- that is
what makes it safe and it was well tested -- but nothing told the grid when a
widget moved, so a value changed anywhere else (a preset, a settings file, the
Live Preview, a flat row the table does not claim) left the table showing the
old answer while `collect()` returned the new one. Two answers to the same
question is the exact failure the binding exists to prevent. `follow_the_form`
connects the widgets behind the cells; it writes ONE CELL rather than
reseeding, because a reseed is 30 ms on Mask and 100 ms at ten organelles and
would run per keystroke and steal focus from the cell being typed into.

THE CEILING TEST HUNG. 326 raised MAX_ORGANELLES from 26 to 702, and
`test_the_ceiling_is_said_rather_than_hit_silently` pressed Add until it
refused -- 702 full table rebuilds. `next_organelle` also rescanned from slot
1 on every call, which made the walk quadratic. Both fixed: the scan starts
from the slots in use, and the test seeds at the ceiling instead of walking
to it. Its docstring was also still claiming twenty-six was the alphabet
running out, which 326 had made false.

================================================================================
2026-09-05  "IS THE TABLE BUILT?"  IT IS. THE STATUS HEADER WAS LYING.
================================================================================

Asked whether to build the per-object table, the maintainer answered "i am
confused, this is already built!" He is right, and the disagreement was
entirely inside this file: the header said the table was "the only part not
yet built" while four sections of the body below it described building it,
mounting it, and then fixing three defects in it. A status line is the only
part of an instruction anybody reads before deciding what to do next, and
this one would have bought a second implementation of a finished feature.

THE HEADER WAS NEVER UPDATED WHEN THE WORK LANDED. Each session appended a
dated section -- which is the right habit -- and none of them went back to
the top. So the file grew more accurate and its summary grew more wrong at
the same time.

WHAT WAS ACTUALLY CHECKED, in the code rather than in this file's own prose,
because a file that was wrong once is not evidence for itself:

  THE PER-OBJECT TABLE -- BUILT, all four layers.
    spacr/object_settings_table.py            model: to_table / from_table /
                                              widen
    spacr/qt/widgets/object_settings_grid.py  the view, with _kind_of
    spacr/qt/widgets/object_grid_binding.py   write-through to the widgets
    spacr/qt/screens/app_screen.py:2413       _mount_the_object_grid, and
                                              apply_object_grid_preference at
                                              2492 for the live switch
    spacr/qt/preferences.py                   "Per-object settings as a
                                              table", the preference that
                                              gates it

  THE RETIRED _size PAIR -- DONE. `organelle_min_size` and
  `organelle_max_size` map to their `_area` counterparts in
  RETIRED_SETTINGS (spacr/validate.py:1033-1034), with the comment that says
  why. Neither name is read anywhere in spacr/ any more; the six remaining
  hits are comments explaining the retirement, plus the animation manifest.

  THE FOUR RENAMES -- DONE in the settings themselves. `_FT`, `_CP_prob`,
  `_Signal_to_noise` and `_min_object_area` have zero occurrences in
  spacr/settings.py; the new spellings carry 17, 17, 18 and 21. The surviving
  bare `FT` is the standalone apply/test-model family this file already
  recorded as intentional and separate (settings.py:4356 spells that out in
  the tooltip).

VERIFIED: 96 passed, 1 skipped.
  QT_QPA_PLATFORM=offscreen tools/run_capped.sh 6G python -m pytest \
    -p no:randomly -q tests/qt/test_the_per_object_settings_are_one_table.py \
    tests/qt/test_the_object_grid_can_be_switched_on.py \
    tests/test_a_retired_setting_says_so.py
The skip is a settings dict in which every object asks for a channel, so the
absent-cell case has nothing to assert on.

--------------------------------------------------------------------------------
AND THE CHECK FOUND ONE THING GENUINELY MISSING: THE RENAMES NEVER GOT THEIR
MIGRATION
--------------------------------------------------------------------------------

This file's own TRAPS section says a rename is a removal plus an addition as
far as every saved settings file is concerned, and that a removed setting
goes in `RETIRED_SETTINGS` so an old file loads with a message instead of
silence. The `_size` retirement did that. The four renames did not:

    RETIRED_SETTINGS.get("cell_FT")                -> None
    RETIRED_SETTINGS.get("cell_CP_prob")           -> None
    RETIRED_SETTINGS.get("cell_min_object_area")   -> None
    RETIRED_SETTINGS.get("cell_Signal_to_noise")   -> None

MEASURED, not inferred, by handing all four to the pre-flight check as a
settings dict. Only ONE of them says anything, and it is the one that says it
by accident:

    cell_Signal_to_noise   warning -- "did you mean 'cell_signal_to_noise'?"
    cell_FT                nothing
    cell_CP_prob           nothing
    cell_min_object_area   nothing

`cell_Signal_to_noise` is caught by the FUZZY matcher, because the rename was
only a change of case and the old name lands within matching distance of the
new one. The other three renames changed the word, so nothing is left within
distance -- which is precisely the hole RETIRED_SETTINGS' own comment says it
exists to fill, quoted there as `upscale` / `upscale_factor`.

THE CONSEQUENCE IS THE SILENT ONE. `_translate_legacy_setting_keys`
(app_screen.py:750) migrates a loaded dict by consulting RETIRED_SETTINGS, so
a settings file written before b7ae412af has its flow threshold and its
cellprob threshold dropped on the floor, the module default used instead, and
nothing said. That is the same failure mode as the live-preview aliases fixed
on 2026-09-03 -- the suffix substitution walked past the places that spell the
key differently -- reaching one more place.

NOT FIXED HERE, because this was a bookkeeping pass and the fix is not a
one-liner. Whoever takes it should know two things first:

  * IT IS A SUFFIX RULE, NOT FOUR ENTRIES. The organelle slots are generated
    and a run may declare `organellez`, so a literal table migrates the slots
    it happens to name and misses the rest -- the trap `_surviving_name_of`
    already solves for the `_size` pair, and the same shape works here.
    `cell`, `nucleus`, `pathogen` and `cytoplasm` are literal; the organelle
    slots are not.
  * `test_a_retired_setting_says_so.py:72` FAILS ANY KEY THAT IS IN BOTH
    RETIRED_SETTINGS AND `expected_types`. The four old names are gone from
    expected_types, so they are clear -- but check that again before adding,
    because the check is what stops a retirement warning a user off a setting
    that still works.


--------------------------------------------------------------------------------
2026-09-06 — THE OPS SETTINGS JOIN THE TABLE, TO THE SAME STANDARD
--------------------------------------------------------------------------------

The 58 OPS tooltips written on 2026-09-05 were prose without the house shape,
and five quality gates said so. The work session hit 26 failures across 23
settings while working its own lane and left them alone; they are rewritten
here, because they are mine.

THE HOUSE SHAPE IS `(type) - what it does, what changes when you alter it,
and the default.` Every one of the 600-odd existing tooltips keeps it, and
`test_every_tooltip_opens_with_its_type` exists so the table reads as one
voice rather than as however many people wrote it. All 58 now do.

WHAT "SAYS WHAT CHANGES" MEANS IN PRACTICE, since the gate rejects prose that
only names the setting again:

    before   "Scale tiles down before matching, for speed."
    after    "... THE MOST COMMON CAUSE OF A RUN THAT FINDS NOTHING: at 0.5 a
             256 px tile becomes 128 px and the detector has almost no corners
             left, so every pair is skipped. Raise it to 1.0 if pairs are
             being skipped. Default 0.5."

TWO PRE-EXISTING FAILURES FIXED ON THE WAY PAST, neither of them OPS:

  * `regression_panel_manifest` was 644 characters against a 600 ceiling.
    Trimmed to 401 with nothing lost -- it had been explaining its own
    implementation, naming two internal functions a user cannot call.
  * The unit-name census asserted `6 + 4 * MAX_ORGANELLES` and found one
    more. The extra is `cellpose_diameter`, which entered the census the
    moment the OPS settings gained tooltips: it ends in `_diameter`, so it is
    a genuine SEVENTH base unit-bearing setting rather than a per-organelle
    one. Its tooltip does say pixels, so only the arithmetic was stale. Base
    is now 7, with the reason written beside it.

  tooltip suite   15 failures -> 6, none of the six caused here


--------------------------------------------------------------------------------
2026-09-08 -- PLAQUES AUDITED (22 settings). ONE OF THEM IS NOT AN AUDIT
              FINDING AT ALL
--------------------------------------------------------------------------------

See instructions/data/364_plaque_settings_audit_2026-09-08.txt. PROPOSALS
only; nothing in the module was changed.

  FINDING 0 IS A BUG, NOT A SETTINGS OPINION, and it is why plaques jumped the
  queue ahead of train_test_model (50) and activation (27). `analyze_plaques`
  cannot run from its own declared settings: `identify_masks_finetune` reads
  seven keys that neither `get_analyze_plaque_settings` declares nor
  `analyze_plaques` adds, and one of them -- `normalize` -- is read
  unconditionally inside the per-batch loop at `spacr_cellpose.py:237`. With
  `masks=True`, which is the DEFAULT, the module raises `KeyError:
  'normalize'` on the first image.

  THE FUNCTION HAS THE SAME BUG WRITTEN IN ITS OWN COMMENT three lines above
  that loop -- "there is no settings['dst'] key, so the old settings['dst']
  raised KeyError on this no-images path". One instance was found and fixed in
  place; seven more were left, and fixing one instance of a class is what made
  the rest look handled.

  AND THE ONLY TEST THAT DRIVES `analyze_plaques` PASSES `masks=False`, which
  is the branch that skips the whole mask step. The covered path is the one
  the user does not take. Whatever fix is chosen, that test should be repeated
  with `masks=True`.

  NOT DRIVEN TO THE TRACEBACK, and the audit says so rather than implying it:
  reaching the KeyError needs a real Cellpose checkpoint, which this did not
  download. What was executed reaches model loading, which is strictly before
  the loop, so the claim rests on control flow rather than on a stack trace.

  The rest is ordinary audit material: `Signal_to_noise` and `CP_prob` are the
  two names that do not match the house style, four of the 22 settings are
  inert unless `well_detection` is on, and `resize` is implied by the two
  target-size settings beside it.

WHAT IS LEFT ON 364 AFTER THIS: the organelle preprocessing gap, and
train_test_model (50), activation (27), identify_masks_finetune (23) and the
rest of this file's own order. `identify_masks_finetune`'s own settings are
now worth auditing FIRST of those, given what its caller turned up.

--------------------------------------------------------------------------------
2026-09-08 — GENERALISING THE PLAQUE FINDING, AND WHY THE RESULT IS NOT FILED
--------------------------------------------------------------------------------

The plaque finding was "can this module run from its own declared settings",
answered by hand. That question applies to every module, so it was worth
trying to ask it mechanically: parse each entry point and the functions it
hands `settings` to, collect every `settings['key']` LOAD, subtract the keys
the entry itself assigns, and subtract what `resolve_default_settings`
declares.

  THE HARNESS IS NOT TRUSTWORTHY YET AND ITS OUTPUT IS THEREFORE NOT A LIST
  OF DEFECTS. It fails its own control case: `analyze_plaques`, the module
  the question came from and whose seven missing keys are verified by hand
  and by control flow, does NOT appear in its output. The callee resolution
  still does not reach `identify_masks_finetune` through the function-local
  `from .spacr_cellpose import ...`.

  A tool that cannot reproduce the finding it was built from cannot be used
  to find new ones. Same rule that retired the "333 of 333 tiles placed"
  stitch result in 372 PART 6-A: a number that looks like success while the
  control says otherwise is not a result.

WHAT IT DID PRINT, recorded as CANDIDATES FOR A HUMAN TO CHECK and explicitly
not as findings. Three rounds of correction were needed to get even this far
-- `inspect.cleandoc` on source rather than `textwrap.dedent`, counting
`ast.Store` subscripts as reads, and a wrong factory-function name that made
everything come back clean -- so treat each of these as "worth ten minutes",
nothing more:

    classify        extract_channels
    endodyogeny     20 keys, including cell_types, um_per_px, treatments
    mask            timelapse
    simulation      max_workers
    train_cellpose  src
    train_only      24 keys, including epochs, learning_rate, batch_size
    umap            col_to_compare, mix, neg, pos

  `train_only` and `endodyogeny` are the two worth looking at first, on
  volume alone: a module reading two dozen keys its factory does not declare
  is either delegating to something with its own settings contract -- which
  is fine and is probably what both are doing -- or is the plaque bug again.
  Deciding which needs someone to read them, which is the part a parser
  cannot do.

  `mask`'s `timelapse` is the cheapest to check and the most alarming if
  real: mask is the first module in the pipeline.

THE PLAQUE FINDING ITSELF IS UNAFFECTED and stands on its own evidence:
`resolve_default_settings('analyze_plaques')` returns 22 keys, none of them
the seven, checked again today.

--------------------------------------------------------------------------------
2026-09-09 -- ALL SEVEN CANDIDATES CHECKED. NONE IS THE PLAQUE BUG.
--------------------------------------------------------------------------------

The candidate list above was explicitly "worth ten minutes, nothing more".
Each was given its ten minutes. Every one is benign, and three of them were
never a question -- which is a fact about the parser, not about the modules.

    classify        extract_channels   RETIRED, and migrated on read
    endodyogeny     20 keys            NOT AN APP
    mask            timelapse          defaults applied before the bare reads
    simulation      max_workers        NOT AN APP
    train_cellpose  src                supplied by the runner, never a panel row
    train_only      24 keys            NOT AN APP
    umap            col_to_compare +3  popped ON PURPOSE, and it says so

THE THREE NON-APPS ARE THE FINDING ABOUT THE INSTRUMENT. `endodyogeny`,
`simulation` and `train_only` have `tile=False` -- they are not registered
apps -- so `resolve_default_settings` falls through to its `{'src': ...}`
default and returns ONE key. The parser compared "keys the module reads"
against that one key and reported two dozen missing. `train_only` and
`endodyogeny` were flagged above as "the two worth looking at first, on
volume alone", and the volume was the fallback.

MASK's timelapse was the one flagged as "cheapest to check and most alarming
if real", because mask is the first module in the pipeline and three of the
reads are bare subscripts -- `settings['timelapse']` at core.py:426, 554 and
612, which raise KeyError on a dict without the key. They cannot:
`set_default_settings_preprocess_generate_masks` runs at core.py:364, inside
the per-source loop and above all three. `settings.setdefault('timelapse',
False)` is settings.py:586. The panel not offering it is deliberate -- it is
the mode switch that chooses `preprocess_generate_masks_timelapse` -- and the
value is always present by the time it is read.

UMAP's four are popped by name in `resolve_default_settings`, with the reason
written beside them: `col_to_compare`, `pos`, `neg` and `mix` describe one
lab's c1/c2/c3 plate convention, are kept as API-compatible backend defaults,
and are replaced in the UI by `exclude_rows`, which works from the columns in
the user's own database. `embedding_by_controls` and `exclude_conditions` go
with them.

CLASSIFY's `extract_channels` is retired. `deep_spacr_defaults` moves its
value to `train_channels` when the new key is unset -- so a settings file in
the wild keeps meaning what it meant -- and then pops it.

WHY THIS IS WORTH THE SPACE. The plaque finding was real and this list was
generated to find more like it. It found none, and the honest conclusion is
that the plaque bug was a single defect rather than the visible end of a
class. A candidate list that comes back empty is a result, and leaving it
unchecked would have left seven open questions reading as seven suspected
bugs.

--------------------------------------------------------------------------------
2026-09-09 -- THE MAINTAINER APPROVED ALL THREE GROUPS
--------------------------------------------------------------------------------

Asked with the eight audit files summarised into one list, he approved:

  1. the silent-divergence bugs   organelle_min_size/_min_area and its two
                                  siblings, plus gradient_accumulation
  2. the eleven renames
  3. the retirements

And added one instruction that is not about settings at all:

    "any changes you make that impact the tutorials please tell codex via the
     instructions list or in any other way you can. so codex knows to redo
     those later."

>>> FOR CODEX: SETTINGS RENAMES ARE LANDING, AND THEY INVALIDATE RECORDINGS.

Every rename below changes a VISIBLE LABEL on a settings panel. Any tutorial
frame showing one of these rows shows a name that will no longer exist, and
any narration that reads the label aloud is wrong from the moment it lands.
This is written here rather than only messaged because the ledger is the
thing a later session reads.

    <object>_FT               -> <object>_flow_threshold
    <object>_CP_prob          -> <object>_cellprob_threshold
    <object>_Signal_to_noise  -> <object>_signal_to_noise
    <object>_min_object_area  -> <object>_min_split_area
    positive_control          -> positive_control_id
    negative_control          -> negative_control_id
    controls                  -> nontargeting_control_grnas
    control_wells             -> stain_baseline_wells
    min_n                     -> min_observations_per_hit
    min_cell_count            -> min_cells_per_well
    expected_end              -> window_length

`<object>` expands over cell, nucleus, pathogen, cytoplasm and the organelle
slots, so the first four are many rows rather than four.

WHICH MODULES' TUTORIALS ARE AFFECTED: Mask and Measure (the four per-object
names), Regression (the six control and threshold names), Map Barcodes
(`expected_end`). A tutorial that never opens a settings panel is untouched.

THE RETIREMENTS remove rows entirely rather than renaming them --
`barcodes`, `grna`, `Toxoplasma`, `cell_mask_dim`, `nucleus_mask_dim`,
`pathogen_mask_dim`, `prefs/space_variant`, `prefs/space_seed`, and
`img_size` folded into `image_size`. A frame showing one of those shows a
control that is gone.

Nothing is applied yet as this note is written. Each group will be committed
separately so the tutorial impact can be read off one commit rather than
reconstructed from a range.


--------------------------------------------------------------------------------
THE REGRESSION READERS ARE TAKEN, AND THE ORDER MATTERS. 2026-09-09.
--------------------------------------------------------------------------------

RECORDED IN THE FILE BECAUSE THE CHANNEL CLOSED. The work session and I
agreed this split over the session bridge and both of its sessions have since
ended; a decision that lives only in a chat neither party can re-read is not
a decision. So it is here, in the file whoever picks this up will open.

THE SPLIT. The declaration side -- `spacr/settings.py`'s tables and the Qt
labels -- was the work session's. THE READER SIDE IS TAKEN BY THE SESSION
THAT OWNS `spacr/ml.py` AND FRIENDS: ml.py, hyperparam.py, sequencing.py,
batch_correction.py and classify_classes.py, for all seven renames

    positive_control  -> positive_control_id
    negative_control  -> negative_control_id
    controls          -> nontargeting_control_grnas
    control_wells     -> stain_baseline_wells
    min_n             -> min_observations_per_hit
    min_cell_count    -> min_cells_per_well
    expected_end      -> window_length

and for the retirements that read into those files: `grna`, `Toxoplasma`,
the three `*_mask_dim` keys and `img_size`.

BEFORE THE ORDER: FIND THE WRITERS, NOT ONLY THE READERS.

A settings key is not only read. It is BUILT -- by the demo generator, by
the notebook generator, by fixtures, by anything that hands a dict to a
factory -- and a rename that updates every reader and no writer produces a
settings dict that the pipeline's own pre-flight refuses. `qt/synthetic.py`
did exactly that on the first rename: the Map Barcodes demo went on naming
`expected_end` and failed its own validation, and it was the TESTS that
found it, because the survey had grepped for reads.

    grep for the key in every file, not for `settings[` and `.get(`

This is a step and not a caveat. A reader-side survey cannot see it, and
the failure it produces looks like the rename being wrong rather than
incomplete.

THE ORDER, AND IT IS NOT INTERCHANGEABLE:

  1. READERS FIRST, accepting BOTH spellings.
  2. Then the old declaration is retired, AND `validate.RETIRED_SETTINGS`
     gains the pair IN THE SAME STEP -- not in step 1.
  3. Then the readers drop the old spelling.

At no point does a settings file that worked yesterday stop working. THE
OTHER ORDER IS THE BUG THIS INSTRUCTION EXISTS TO REMOVE: land the
declaration first and every settings file in the wild names a key nothing
reads -- silently, because a missing key is a default and a default is not
an error.

  STEP 2 IS WHERE `RETIRED_SETTINGS` GOES, AND THE LIST WAS INCOMPLETE
  UNTIL 2026-09-09. At step 1 the old name is still LIVE -- still in
  `expected_types`, still produced by the factory -- and a retired entry
  naming a live key is exactly what
  `test_no_retired_name_is_also_a_live_setting` exists to refuse. This is
  not reasoning: `gradient_accumulation` was retired earlier the same day
  with its `expected_types` row and two category entries left behind, and
  three tests caught it within a minute. The correction is written into
  the ordered list rather than beside it, because the next person will
  read the list.

--------------------------------------------------------------------------------
2026-09-09 -- ONE SESSION HOLDS ALL THREE STEPS NOW, AND THAT CHANGES THE SHAPE
--------------------------------------------------------------------------------

The split above was written for two sessions. It is one now -- the reader
side was handed over rather than left at step 0, because "I am saturated;
364 would sit at step 0 until tomorrow if it stayed with me, and it has sat
there long enough."

WITH ONE SESSION HOLDING BOTH ENDS, STEPS 1 AND 3 COLLAPSE INTO ONE
COMMIT, and the back-compatibility comes from a FOLD in the settings
factory rather than from readers that understand two spellings:

    the factory `setdefault`s the NEW name, and folds an OLD one it finds
    into it before anything reads either

which is the mechanism `_fold_gradient_accumulation` already uses. A
settings file naming the old key still works -- it is folded before any
reader sees it -- and the readers only ever know one spelling. The
three-step order stays correct for a two-session split and is kept above
for that case.

  ONE RENAME AT A TIME, AND THE FIRST ONE PROVES THE MECHANISM. Measured
  before starting: each of the seven touches 16 to 29 files under `spacr/`
  and 4 to 43 under `tests/`, and two of the old names -- `controls` and
  `min_n` -- are common words that will match text having nothing to do
  with the setting. Seven at once is one commit nobody can review and one
  mistake nobody can bisect.

AND CODEX IS TOLD BEFORE, NOT AFTER. The maintainer's words on 2026-09-09
were "any changes you make that impact the tutorials please tell codex via
the instructions list or in any other way you can. so codex knows to redo
those later". Every one of these seven changes a VISIBLE LABEL, so the
rename table goes into 358 in the same commit that lands the readers.

ONE CONTRACT WORTH HAVING HERE, from the API catalog work: THE PRODUCT-NAME
RULE IS CASE SENSITIVE. `NONE` shouted in prose is not the literal `None`,
and a translated label that writes the literal ADDS a protected token the
source never had. That is the same shape as this instruction's
silent-divergence bugs -- two spellings, one meaning, and only one of them is
the contract -- and it will reach `stain_baseline_wells` and
`nontargeting_control_grnas` the moment they gain translated labels.


--------------------------------------------------------------------------------
2026-09-09 -- EIGHT SETTINGS THE PACKAGE READS NOWHERE, FOUND BY 383's AUDIT
--------------------------------------------------------------------------------

Instruction 383 audited the 23 API links that still land on a bare module
page and found that eight of them are not a link problem at all:

    denoise            load_path_regex     mask_array
    normalization      normalization_scope normalize_plots
    save_to_db         visualize

EACH HAS EXACTLY ONE CONSUMER, AND IT IS ITS OWN DEFAULTS SETTER. In
`docs/setting_consumers.json` every one of the eight has a single row, and
in every case the reader is `spacr.settings`'s own `set_default_*`
function putting the default in. Nothing in the package then reads the
value back.

THE DYNAMIC READS WERE CHECKED, because "nothing reads it" is the one
claim that looks identical whether it is true or the detector is broken --
383 met that twice and says so. Every mention of all eight was read by
hand:

  * `classify.FAMILY_SETTINGS` and `stream_dataset.METHOD_SETTINGS` name
    several of them. Neither is a read: both are the tables that decide
    which controls to GREY, so a name there means "this family would use
    it", not "this code consumes it".
  * `timelapse._prepare_for_tracking(mask_array)` and
    `utils.load_image_paths(c, visualize)` take parameters of the same
    name; neither is called with the setting, and the first takes a label
    array rather than a flag.
  * `align.py`'s `normalization` is a local loop variable over
    ('phase', None) -- phase correlation's own option, not this setting.
  * `visualize`'s only match is a COMMENT: `# 'cell', not
    settings['visualize']`.

WHAT TO DO WITH THEM IS THIS INSTRUCTION'S QUESTION, not 383's, and it is
not the same answer for all eight. Some are probably genuinely dead and
should be retired the way `gradient_accumulation` and the `_size` pair
were. Others may be a FEATURE THAT WAS NEVER WIRED -- `denoise` and
`normalization` both name real capabilities spaCR has -- in which case the
control is a promise the pipeline does not keep, which is worse than a
dead setting rather than better.

  EACH NEEDS ITS OWN VERDICT AND THE MAINTAINER'S FOR ANY THAT ARE
  FEATURES. Retiring a control that was meant to work would remove the
  evidence that it was.

ALSO FOUND, AND FIXED: `classify.FAMILY_SETTINGS["cv"]` still named
`gradient_accumulation`, which this instruction retired earlier the same
day. A greying table naming a key that no longer exists greys nothing and
reads as though the control is still there.


--------------------------------------------------------------------------------
2026-09-09 -- THREE OF THE SEVEN RENAMES ARE BUILT, ON A BRANCH. WHAT EACH COST.
--------------------------------------------------------------------------------

    expected_end     -> window_length              DONE
    min_n            -> min_observations_per_hit   DONE
    min_cell_count   -> min_cells_per_well         DONE
    control_wells    -> stain_baseline_wells       HELD, see 357-Q6
    positive_control -> positive_control_id        not started
    negative_control -> negative_control_id        not started
    controls         -> nontargeting_control_grnas not started

ON A BRANCH (`364-renames`) AND NOT ON NIGHTLY. A setting rename adds a
`SETTING_LABELS` and a `SETTING_TOOLTIPS` key in nine RUNTIME catalogs and
orphans the old pair. Those are built by `tools/build_i18n_catalogs.py` --
a DIFFERENT pipeline from the API catalogs under `docs/`, which the other
session is close to making green after five days red. `docs.yml` runs both
audits, so landing these on nightly now trades one green audit for two red
ones. They land together when that dispatch is away, and one runtime
rebuild covers all of them.

WHAT THE FIRST THREE TAUGHT, and it is not what the survey predicted.

  1. THE WRITERS MATTER AS MUCH AS THE READERS. `spacr/qt/synthetic.py`
     BUILDS demo settings, and it went on naming `expected_end` after the
     key was renamed -- so the Map Barcodes demo produced a settings dict
     its own pre-flight refused. Three test files parse the demo's window
     with that key too. A survey that greps for reads is half a survey;
     this one was found by the tests.
  2. THE CATEGORY MAP IS IN TWO PLACES. `settings.categories` and
     `settings_model`'s per-module ordered lists are separate, and
     updating only the first put the renamed key under "Additional
     Settings" -- a heading nobody chose, which is exactly what that
     ordered list exists to prevent.
  3. THE NOTEBOOKS CARRY THE KEYS AS LITERALS.
     `tools/build_notebook_settings.py` regenerates them and a test
     compares the committed copies against what it writes, so a rename
     that skips them is caught -- but only by that one test.
  4. A COMMON WORD IS A TRAP AND THE TRAP IS REAL.
     `regression_summary.py` uses `min_cell_count` as an internal FIELD
     NAME as well as reading the setting, and `regression_qc.py` uses it
     as a report key that has nothing to do with the setting. A blanket
     replace renamed the field and broke the mapping that names it. The
     label a user reads follows the rename; the internal wiring does not.
  5. AND A BLANKET REPLACE OVER `tests/` HIT A NON-UTF8 FILE and stopped
     part way, leaving the suite half-renamed and 28 tests red for a
     reason that had nothing to do with the rename. Skip what cannot be
     decoded rather than letting one `.pyc` truncate the pass.

WHAT IS STILL RED ON THE BRANCH, ON PURPOSE:
`tests/qt/test_the_chrome_captions_have_catalog_rows.py` reads
`sv.SETTING_LABELS['min_observations_per_hit']`, which does not exist until
the runtime catalogs are rebuilt. That is the branch saying what it is
waiting for.

--------------------------------------------------------------------------------
2026-09-09 -- WHY THE LAST THREE RENAMES ARE NOT STARTED, WITH THE SURVEY DONE
--------------------------------------------------------------------------------

    positive_control -> positive_control_id
    negative_control -> negative_control_id
    controls         -> nontargeting_control_grnas

THESE THREE ARE NOT LIKE THE FIRST THREE, and the difference is not size.
`expected_end`, `min_n` and `min_cell_count` are distinctive strings: a
grep for them finds the setting and almost nothing else, which is what made
a careful blanket replace safe once the two traps were known.

`controls` IS A COMMON WORD DOING FOUR JOBS. 38 occurrences outside the
generated catalogs, and they are not one thing:

    spacr/core.py:1650             the SETTING, read into annotate_conditions
    spacr/regression_summary.py    the SETTING, read through _setting()
    spacr/baseline.py:32           CONTROLS = "controls", a constant naming
                                   a column or a payload field
    spacr/parameter_sweep.py:834   "controls": dict(controls or {}) -- a
                                   sweep payload key
    spacr/sweep_child.py:103       the same payload, read back
    spacr/regression_annotation.py a "needs" token in a dependency
                                   declaration, three sites

Only the first two are the setting. A blanket replace renames a payload
field and a dependency token, and the failure that produces looks like the
rename being wrong -- which is exactly the shape the `min_cell_count` pass
already met once in `regression_summary` and `regression_qc`.

`positive_control` and `negative_control` are 50 and 49 mentions each, and
carry the same hazard in a milder form: they appear as column values, as
plot labels and as the setting.

  SO EACH OF THE THREE WANTS ITS OCCURRENCES CLASSIFIED ONE AT A TIME, not
  a regex. That is an hour of judgement per name, and judgement is the
  thing that degrades at the end of a long session. Stopping with three
  landed and proven is better than landing a fourth with one site wrong,
  because a wrong site here is silent: a payload key that no longer
  matches, or a setting read that returns the default.

THE SURVEY IS DONE AND IS WRITTEN ABOVE so the next pass starts from the
classification rather than from the grep. The mechanism is proven, the two
traps are recorded as steps, and the branch is one `git checkout` away.


--------------------------------------------------------------------------------
2026-09-09 -- THE EIGHT UNREAD SETTINGS ARE RETIRED, AND TWO OF THEM LIED
--------------------------------------------------------------------------------

357-Q4, answered: RETIRE ALL EIGHT. Broader than the entry recommended --
it proposed keeping `denoise` and `normalization` pending a decision about
whether they were features never wired -- and the maintainer took the whole
set.

    denoise            load_path_regex     mask_array
    normalization      normalization_scope normalize_plots
    save_to_db         visualize

Each had exactly one consumer in the generated map and in every case it was
the setting's own `set_default_*` function. Nothing read the value back.

TWO OF THEM DOCUMENTED A JOB THEY DID NOT DO, which is worse than a dead
control, and finding it is the argument for having asked rather than
retiring quietly:

  * `mask_array` said it named the labelled plane for the 'array' stream
    method. `stream_dataset` takes that plane from `object_array` -- for
    BOTH methods -- so the route's own documented input was never read.
    `picture_settings` even WROTE it: a panel naming a plane by index sent
    `mask_array` an integer that nothing consulted, and the value was
    silently the object type's default instead.
  * `load_path_regex` said it selected already-exported crops, and carried
    a dependency RULE explaining when it did not apply. Both described a
    control nothing read in either branch.

WHAT CAME OUT WITH THEM, because a retirement is not just four table rows:

    the eight setdefaults, expected_types rows, tooltips and categories
    the "Output & Database" SECTION, whose only member was `save_to_db`
    its SECTION_HINTS paragraph, which described a choice the run did
      not offer
    `load_path_regex`'s dependency rule
    two entries in `classify.FAMILY_SETTINGS` and one in
      `stream_dataset.METHOD_SETTINGS` -- greying tables naming settings
      that no longer exist grey nothing
    two VERIFIED_TOOLTIP_FACTS waivers whose own text said "Legacy
      compatibility" and "do not read", which is the audit recording that
      a control did nothing rather than the control being fixed

AND FIVE PINNED COUNTS MOVED, each named rather than nudged: the tooltip
census 679 -> 671, the witnessed inapplicable defaults 49 -> 48 and their
distinct keys 35 -> 34, and -- from the `control_wells` split in the same
batch -- the default variants 47 -> 46 with its repaired-tooltip reason and
repaired fact, 23 -> 22 apiece. Three of those five are one cause seen from
three sides, which is why all three are kept: a change that moved only one
would be a change nobody had understood.

AND THE PROSE THAT STILL NAMED THEM. `stream_method`'s tooltip told the
user the 'array' route required `mask_array`; it now says what the route
actually reads. Four comment passages in `settings.py` explained category
membership by naming `save_to_db` and `load_path_regex` as live settings.
A comment that describes a key nothing defines is the same defect as the
key itself, one indirection later.

`tests/qt/test_cov_app_screen.py` used `denoise` nine times as its
stand-in for "a boolean on the mask screen" -- in the widget-construction
census, in three CSV round trips, and in the crash-report test. It now
uses `plot`, which is a live boolean whose default is False, so the
import assertions still observe a value CHANGING rather than agreeing
with a default.

STILL OUTSTANDING: the runtime i18n catalogs (`spacr/qt/i18n_catalogs/*.py`)
carry a label and a tooltip for each of the eight in all languages. They
are GENERATED -- `tools/build_i18n_catalogs.py` -- and the rebuild is held
until the other session's catalog audit is green, so the stale entries go
when it runs, not by hand.

A STEP THE SURVEY WAS MISSING, AND THE THIRD TIME IN ONE NIGHT A RULE WAS
RIGHT ABOUT THE FAMILY AND BLIND TO THE INSTANCE. A blanket replace of a
setting's name also hits every identifier that merely SPELLS it the same.
`control_wells` caught two: a FUNCTION in `gene_measurement_compare` that
derives the control wells from the count table, and
`app_screen._migrate_control_wells`, which is the migration for the very
trio being renamed -- so the rename broke the code whose job was to carry
old files across it. `min_cell_count` caught a third, an internal field
name in `regression_summary`.

So the survey has a step before the replace: list every occurrence and
mark each as THE SETTING, A FUNCTION OR FIELD OF THE SAME NAME, or THE
MIGRATION FOR THIS SETTING. Only the first is renamed. The other two are
not exceptions to the rule -- they are what the rule cannot see, and the
migration is the one that stings, because it is the code that exists to
protect the user from the rename.


--------------------------------------------------------------------------------
2026-09-09 -- THE CLASSIFICATION FOR THE TWO CONTROL IDENTIFIERS, SITE BY SITE
--------------------------------------------------------------------------------

The section above says each of the last three names wants its occurrences
classified one at a time rather than by regex, and calls that an hour of
judgement. Here is that hour spent for two of the three. The pass that runs
the rename starts from this table; it does not start from a grep.

    positive_control -> positive_control_id
    negative_control -> negative_control_id

WHAT WAS COUNTED. 130 and 141 occurrences in `spacr/` including the
generated i18n catalogs, which are not edited by hand. Excluding those, and
excluding the DIFFERENT settings that merely begin with the same word --
`positive_control_wells`, `negative_control_wells`, and the `_rank` and
`_df` locals -- what is left is the list below. Plus 149 occurrences across
46 files in `tests/`, which follow whatever the source does.

  THE SETTING. These are renamed.

    settings.py            two setdefault pairs (1547/1548, 2505/2506),
                           the expected_types rows (3519/3520), the two
                           tooltips (4666, 4688), and the "Plate Layout &
                           Controls" category membership
    settings.py:1504       the merged module POPS the trio; the popped
                           names are the setting
    settings_model.py      three category lists (1192, 1568, 2495)
    settings_flow_index.py two entries (583, 831)
    setting_api_targets.py four entries (405, 661, 1231, 1334)
    training_basis.py:81   the trio, as the basis's own list
    classify_classes.py    53 and 373, the trio and the class translation
    well_spec.py           WELL_SETTINGS, the "may contain wells" list
    hyperparam.py:3450-1   settings.get with the 'c2'/'c1' defaults
    trial_metrics.py:216-7 settings.get, keyed by role
    regression_summary.py  _setting(run.settings, ...)
    ml.py                  every settings.get / settings[...] read:
                           435, 2051-2, 6808-9, 7016-7, 9172, 9641-2,
                           10435, 10578-81, 10611, 10627-8
    parameter_sweep.py     the Qt screen's base.get at 491
    app_screen.py:10656    the trio inside the migration

  NOT THE SETTING, AND EACH ONE BREAKS SOMETHING DIFFERENT IF RENAMED.

    batch_correction.py:635   THE WORST OF THEM. `"negative_control"` here
                              is an ALIAS FOR A BATCH-CORRECTION METHOD --
                              `batch_correction=negative_control` resolves
                              to `control_center`. It is a user-facing
                              VALUE, not a key, so renaming it silently
                              removes a documented spelling from a settings
                              file and the run raises "Unknown
                              batch_correction=" on a file that used to
                              work.
    plate_layout.py:49-50     ROLE_POSITIVE / ROLE_NEGATIVE, a Qt WIDGET
                              ROLE token that spells the setting. Line 471
                              maps role to setting on ONE LINE, so a
                              regex sees the same string twice on that line
                              meaning two different things.
    experiment_design.py      299 and 304, a stylesheet selector
                              `QLabel[spacrWellRole="negative_control"]`.
                              Renaming it detaches the rule from the widget
                              and the wells lose their colour, with no
                              error anywhere.
    ml.py:10744-11510         THE PUBLIC PARAMETER NAMES. `_resolve_controls`
                              and `generate_ml_scores` take
                              `positive_control='c2'` and
                              `negative_control='c1'` as ARGUMENTS, and the
                              whole body -- roughly 60 sites, including a
                              commented-out block -- is those locals. The
                              call sites already pass them as `pc=`/`nc=`
                              in four places, so the setting name and the
                              kwarg are ALREADY allowed to differ.
    prose                     docstrings, error text and comments in
                              classifier_evaluation, settings_advisor,
                              control_names, cell_montage, ml and
                              app_screen. These say the setting's name to a
                              user, so they follow the rename -- but they
                              follow it as PROSE, one read at a time, not as
                              a token.

THE PARAMETER QUESTION IS THE ONE REAL DECISION and it is not mechanical.
`generate_ml_scores` is public API. Renaming the setting without renaming
the parameter leaves a user reading `positive_control_id` in the panel and
`positive_control=` in the signature; renaming both is a breaking change to
a public function, in a module that is not this session's lane. The four
`pc=`/`nc=` call sites are the evidence that the two names are already
decoupled, which argues for renaming the SETTING ONLY and saying so in the
tooltip.

WHAT THIS LEAVES. `controls -> nontargeting_control_grnas` is still
unclassified, and it is the hardest of the three: the word does four jobs
and appears 1,593 times across the package, the overwhelming majority of
them the ordinary English word in comments and in widget names. Its
classification is a separate sitting.


--------------------------------------------------------------------------------
2026-09-09 -- AND THE CLASSIFICATION FOR `controls`, THE HARDEST OF THE THREE
--------------------------------------------------------------------------------

    controls -> nontargeting_control_grnas

The section above said this one's classification was a separate sitting.
It is done, and the count that made it look impossible was the wrong count.
1,593 occurrences of the word `controls` in `spacr/` is the ORDINARY
ENGLISH WORD in comments, docstrings and widget names. The occurrences that
a rename could touch are the ones where it is written as a STRING or read
as a key, and there are 41 of those outside the generated catalogs. That is
a table, not a wall.

  THE SETTING. These are renamed.

    settings.py:2528       setdefault('controls', ['000000'])
    settings.py:3801       the expected_types row
    settings.py:4965       the tooltip -- which already says the true
                           meaning, "Non-targeting control gRNA
                           identifiers", and is the argument for the name
    settings.py:5344       "Plate Layout & Controls" membership
    settings_model.py:1570 the category list
    settings_flow_index.py:181
    setting_api_targets.py 133 and 974
    object_roles.py:105    EXACT_LABELS["controls"] = "Control gRNA/Gene",
                           a label override KEYED BY THE SETTING. Its own
                           comment already explains the confusion the
                           rename removes: "`controls` names guide or gene
                           identifiers, whereas the neighbouring control
                           settings name wells."
    core.py:1650           settings['controls']
    regression_summary.py:1922
    ml.py                  6810, 7018, 7411, 7413, 9173, and the prose at
                           7286 and 7403
    settings_advisor.py    477, 841, 844, 851 -- the advisor's question key
                           IS the setting name

  NOT THE SETTING. Four families, and the first is bigger than the setting.

    THE PANEL KEY. "controls" names the control-separation figure, and it
    travels through six files as a key:
        figures/panels.py      577, 593, 621, 738, and SHEET_ORDER at 746
        figures/fast_render.py 35 and 263
        regression_results.py  _PANEL_TABS at 1392, and 3024
        app_screen.py          the live-tile entry at 8667 and
                               _LIVE_TILE_WIDGETS at 8685
    Eleven sites, more than the setting has in any single file. Renaming
    them detaches a figure from its tab and its live tile, and the failure
    is a blank panel rather than an exception.

    THE SWEEP PAYLOAD. parameter_sweep.py:834 writes `"controls":
    dict(controls or {})` and sweep_child.py:103 reads it back. The two
    halves are in different processes, so a rename applied to one and not
    the other is a sweep that silently runs with no controls.

    THE DEPENDENCY TOKEN. regression_annotation.py declares
    `needs=("controls", "features")` at 192, tests it at 395, and
    documents it at 127. This is the token a step declares, not a setting
    read.

    THE COLUMN CONSTANT. baseline.py:32, `CONTROLS = "controls"`, and
    submodules.py:5313, `'controls': controls` in a results payload.

SO ALL THREE ARE NOW CLASSIFIED and the rename is a mechanical pass over a
list rather than a judgement call per line. What it still needs is a
sitting with the tests: 46 files and 149 occurrences follow the two control
identifiers, and `controls` has its own test surface on top of that. That
is the work, and it is the kind that should start at the beginning of a
session rather than the end of one.


--------------------------------------------------------------------------------
2026-09-10 -- RENAMES 5 AND 6, AND A BUG IN THE FIRST FOUR THAT THE SIXTH FOUND
--------------------------------------------------------------------------------

    positive_control -> positive_control_id
    negative_control -> negative_control_id

Done from the classification written above rather than from a grep, and it
was worth the hour: the four false friends it named are all still there and
none of them was renamed.

THE SETTING ONLY. `generate_ml_scores` and `_resolve_controls` keep their
PUBLIC parameter names. The evidence for that decision is in the code
already -- four call sites pass them as `pc=`/`nc=`, so the setting name and
the kwarg have been decoupled for a long time -- and renaming a public
signature is a break this rename does not need to make. The tooltips say
which is which.

WHAT WAS LEFT ALONE, each for its own reason:
  * `batch_correction.py`'s `"negative_control"` alias, which resolves to
    `control_center`. A user-facing VALUE, not a key.
  * `plate_layout.ROLE_POSITIVE` / `ROLE_NEGATIVE`, Qt widget-role tokens.
    Line 471 maps role to setting and now shows the two apart, with a
    comment saying why they were ever the same string.
  * `experiment_design`'s `QLabel[spacrWellRole="negative_control"]`
    stylesheet selectors.
  * every bare identifier in `ml.py` -- about sixty sites, including a
    commented-out block. The rename touched only QUOTED occurrences, which
    is what made "the setting but not the parameter" a mechanical rule
    rather than a judgement per line.

AND THE RENAME ATE ITS OWN TABLE ONCE. The quoted-occurrence pass ran over
`settings.py` after `RENAMED_SETTINGS` had gained
`"positive_control": "positive_control_id"`, and rewrote the KEY as well --
leaving `"positive_control_id": "positive_control_id"`, a rename from a
name to itself, which folds nothing and reads as correct. Caught by asking
the fold to move a value and watching it not. The trap this file already
describes, sprung on the file that describes it.

--------------------------------------------------------------------------------
THE BUG: THE FOLD HAD ONE CALL SITE, SO THREE RENAMES DROPPED THE USER'S VALUE
--------------------------------------------------------------------------------

`_fold_renamed_settings` is what makes a rename safe: it moves an old key's
VALUE onto the new name before any `setdefault` runs. Measured while wiring
the two above:

    grep -n "_fold_renamed_settings" spacr/settings.py
      1638: def _fold_renamed_settings(settings):
      6824:     _fold_renamed_settings(settings)      <- one caller

One. `set_default_generate_barecode_mapping`, which owns `window_length`.
So `min_observations_per_hit`, `min_cells_per_well`, `stain_baseline_wells`
and `analysis_excluded_wells` were never folded at all: their factories
filled the default first, and a settings file naming the old key kept
running and lost its number in silence. That is the one failure a rename
exists to prevent, and it was in the tree for a day.

WHY THE TEST DID NOT CATCH IT, and this is the transferable part.
`test_the_fold_runs_before_the_defaults_are_filled_in` is the right test and
it was written the first day. It walks a TABLE -- `FACTORIES` -- naming the
factory each renamed setting belongs to, and that table had ONE ENTRY. A
rename missing from the table was never asked the question. The test passed,
every time, on one of four renames.

    A TABLE-DRIVEN TEST IS ONLY AS COMPLETE AS ITS TABLE, and nothing was
    checking the table against the thing it describes.

FIXED IN THREE PARTS:
  * the fold is called at the top of `get_perform_regression_default_settings`,
    `set_default_analyze_screen` and `set_analyze_invasion_defaults` as well
    (`set_default_classify` inherits it by calling the second);
  * `FACTORIES` is complete, seven entries;
  * `test_every_rename_names_the_factory_that_declares_it` fails when a
    rename is added with no entry, so the table cannot go stale in the
    direction that hides the bug.

TWO SMALLER CORRECTIONS TO THE SAME TEST, both found by running it:
  * the factory lookup asked "which old name maps EXACTLY to this string",
    which raises StopIteration on both halves of a SPLIT, since
    `control_wells` maps to a tuple. It asks "which old name leads here".
  * it compared the folded value against the default and required them to
    differ, which cannot be asked of a setting whose default is None --
    `stain_baseline_wells` is one, and the assertion could only say "no
    default to compare against" about the very case it existed to check. It
    now sends a probe the default cannot be and asserts the probe arrives.

--------------------------------------------------------------------------------
2026-09-10 -- RENAME 7 OF 7. ALL SEVEN ARE DONE.
--------------------------------------------------------------------------------

    controls -> nontargeting_control_grnas

Done from the classification written on 2026-09-09, which said 41 string
sites rather than the 1,593 a raw word count suggests, and named four
false-friend families that must not move. None of them did: the
control-separation PANEL KEY through figures/panels, fast_render,
regression_results and app_screen; the SWEEP PAYLOAD whose two halves live
in different processes; `regression_annotation`'s `needs=("controls", ...)`
DEPENDENCY TOKEN; and `baseline.CONTROLS`, a column constant.

THE SETTING'S OWN CODE ARGUED FOR THIS RENAME BEFORE ANYONE DID.
`object_roles.EXACT_LABELS` carried a label OVERRIDE for `controls`, and
its comment is the case in full: "`controls` names guide or gene
identifiers, whereas the neighbouring control settings name wells. Use an
explicit label so the two concepts are not confused in settings forms."
A setting that needs an override to be understood is a setting whose name
is wrong.

DONE AS A RENAME ONLY, WITH NO REWORDING, and that was the other session's
advice rather than mine. The runtime reviewed-translation migration matches
on EXACT source text and re-points when the destination is unique -- so a
rename alone carries the human's review across, while a rename plus a
reworded tooltip drops it. Two commits, not one, if the wording ever needs
to change.

AND THE GUARD WRITTEN THIS MORNING CAUGHT ITS OWN SUCCESSOR.
`test_every_rename_names_the_factory_that_declares_it` failed the moment
`controls` was renamed without a `FACTORIES` entry -- which is exactly the
hole that let three earlier renames reach the tree with no fold at all,
silently dropping the user's value. It was written hours earlier for
precisely this and fired on the first real case.

ONE THING THE RULE DID NOT COVER, found by running the suite: the
quoted-occurrences-only rule is right for SOURCE and incomplete for TESTS.
A helper like `_run(tmp_path, controls=None)` or
`_settings(positive_control=PC_GUIDE)` passes the setting as a KEYWORD
ARGUMENT into `**overrides`, where it becomes a dict key without ever being
quoted. Four call sites across two files. Worth adding to the survey step:
after the quoted pass, grep for the bare name as a keyword argument.

3,459 tests pass across the affected slice.

--------------------------------------------------------------------------------
WHAT IS LEFT IN 364
--------------------------------------------------------------------------------

The RUNTIME i18n catalogs, and they are the other session's to rebuild --
they have said so and will do it once for all fifteen changed keys rather
than twice. Until then `spacr/qt/i18n_catalogs/*.py` carry rows for the old
names and none for the new ones, so those settings fall back to English in
all nine languages, and `docs.yml` cannot publish.

Nothing else. Seven renames, one split, eight retirements, the fold bug and
its guard, and the classification that made the last three safe.

--------------------------------------------------------------------------------
2026-09-10 -- A RENAME STRANDS EVERY GENERATED TABLE KEYED BY SETTING NAME
--------------------------------------------------------------------------------

FOUR OF THEM WERE FOUND STALE, days after the renames landed, and the way
each was found is the point: none of them raised, none of them broke a
run, and each stayed internally consistent while describing settings that
no longer exist. Only a test comparing two artefacts could see it.

  1. `Notebooks/*.ipynb` -- eight of nine still documented
     `positive_control`, `negative_control` and `controls`. Regenerated by
     `tools/build_notebook_settings.py`.

     AND THE GENERATOR ITSELF WAS KEYED BY THE OLD NAMES. Its per-entry-point
     map of which settings a CLI run requires said `positive_control` /
     `negative_control`, so it matched nothing and wrote "optional" for
     both -- when a Classify (ML) run REQUIRES them unless
     `annotation_column` is set. The documentation did not merely name a
     retired setting, it gave the wrong answer about a live one.

  2. `docs/source/_generated/settings_flow.rst` -- 1,109 sections.
  3. `spacr/qt/screens/settings_flow_index.py` -- the index for the above.
     Both written by `tools/settings_flow.py --rst`, which writes them
     together; regenerating one without the other is what
     `test_the_flow_index_agrees_with_the_page_it_was_written_from`
     exists to catch.

     THIS ONE WAS ABOUT TO BE PUBLISHED. `settings_flow` is in the
     documentation toctree, and the home session had already dispatched
     `docs.yml` when the staleness was reported; the run was cancelled
     mid-build. The site would have carried 1,109 sections built from
     renamed settings.

  4. `spacr/qt/screens/setting_api_targets.py`, from
     `tools/build_setting_consumer_map.py` -- four settings had NO API
     link at all (`invasion/stain_baseline_wells`,
     `map_barcodes/window_length`, `regression/min_cells_per_well`,
     `regression/min_observations_per_hit`), so the tooltip's "API" link
     went nowhere. Regenerated to 819 per-module rows over 611 settings
     across 56 app modules.

THE CHECKLIST FOR THE NEXT RENAME, and it belongs in the survey step
beside "grep the bare name as a kwarg":

    tools/build_notebook_settings.py     -> Notebooks/*.ipynb
    tools/settings_flow.py --rst         -> settings_flow.rst AND
                                            settings_flow_index.py
    tools/build_setting_consumer_map.py  -> setting_api_targets.py
    tools/build_i18n_catalogs.py         -> spacr/qt/i18n_catalogs/*.py
    tools/build_documentation_i18n.py    -> docs/source/_static/i18n/api/

RUN ALL OF THEM, then run the tests that compare pairs of them. A FIFTH
GENERATED TABLE KEYED BY SETTING NAME JOINS THIS LIST -- that is the rule,
not the specific five, because the failure is structural: a rename reaches
the source, every generated file stays internally consistent, nothing
raises, and the only thing that can see the gap is a comparison.

--------------------------------------------------------------------------------
2026-09-12 -- THE MISSING MIGRATION IS BUILT, AND IT WAS THREE DEFECTS RATHER
THAN THE ONE THIS FILE RECORDED
--------------------------------------------------------------------------------

Commit 15fa72737 on nightly. The 2026-09-05 entry above named the hole
correctly and understated it twice over.

WHAT THIS FILE SAID: four renames never got their RETIRED_SETTINGS migration,
so `_translate_legacy_setting_keys` drops a pre-b7ae412af file's flow and
cellprob thresholds. Correct, and measured again here through the real entry
point rather than the helper:

    set_default_settings_preprocess_generate_masks({'cell_FT': 0.42})
        -> cell_flow_threshold == 100      the default, not 0.42

WHAT IT MISSED, FIRST. There are TWO tables, not one, and they answer
different questions. `validate.RETIRED_SETTINGS` (45 entries) decides what is
SAID; `settings.RENAMED_SETTINGS` (7) decides what is DONE. Nothing checked
they agreed. So there are two classes of failure and this file described only
the quieter one:

    CLASS 1  recorded, never performed -- the doctor says "renamed to X" and
             the run uses the default anyway. minimum_cell_count,
             organelle_min_size, organelle_max_size, redunction_method.
    CLASS 2  recorded NOWHERE -- no migration and no warning at all. The four
             b7ae412af families. Strictly worse: class 1 at least tells the
             user.

    An hour was spent disagreeing with the other session about whether class 2
    was real, because checking RETIRED_SETTINGS for a flow key and finding
    nothing reads as "no such defect". The absence IS the defect: the value is
    moved by RENAMED_SETTINGS, and absence from BOTH is the silent case.
    Settled by running it rather than by arguing.

WHAT IT MISSED, SECOND, AND THIS IS THE LARGEST PART.
`set_default_settings_preprocess_generate_masks` NEVER CALLED THE FOLD AT ALL.
`_fold_renamed_settings` ran in four default-setters and none of them was the
mask pipeline -- so both tables could have been perfect and generate-masks
would still have lost every value. One line, and most of the fix.

THE SUFFIX RULE HAD TO LEAVE THE Qt PANEL. `_ORGANELLE_SLOT_KEY` there was the
only code in spaCR that knew a rename can belong to a role FAMILY, which is
why `organelleq_min_size` was migrated by the panel, ignored by the run and
unmentioned by the doctor -- three answers to one question. Now one resolver,
`settings.surviving_setting_name`, and the regex is deleted rather than
generalised.

MERGING THE TABLES WAS THE OBVIOUS DESIGN AND IT IS WRONG, which a four-way
design panel established by measurement rather than taste. RETIRED_SETTINGS is
a SUPERSET: 33 withdrawals and one SEMANTIC migration as well as renames. The
Qt panel already read it directly, which is merging in miniature, and:

    _translate_legacy_setting_keys({'gradient_accumulation': False})
        -> {'gradient_accumulation_steps': False}        int() of that is 0

A step count of zero is not a state the code has; `steps = 1` is the off
state. That bug was shipped, and generalising the "clean" design would have
spread it to every consumer. Fixed here; `SEMANTIC_FOLDS` now DECLARES the
exemption, because absence is a fact with no guard.

THE ROLE SCOPE IS DERIVED, NOT WRITTEN DOWN. A suffix rule fires only where
the old key is not live and the terminal new key is. That gate does real work:
seven `_size` keys are still live -- cell/nucleus/pathogen `_min_size` and
`_max_size`, plus `cytoplasm_min_size` -- and they mean something DIFFERENT
from their `_area` neighbours (`cell_min_size` filters at MEASUREMENT time,
`cell_min_area` at SEGMENTATION time). A blanket `_size -> _area` eats all
seven. The RETIRED_SETTINGS comment claiming "cell, nucleus and pathogen only
ever had `_area`" is stale and should not be trusted by the next reader.

IT IS A BEHAVIOUR CHANGE ON HISTORICAL DATA, and that is the point rather than
a side effect. Any pre-2026-09-02 file carrying one of these keys STARTS
SETTING CONTROLS IT HAS BEEN SILENTLY IGNORING -- a file that read
`cell_FT=0.42` as 100 yesterday reads it as 0.42 today and the masks change.
So the fold now says what it did, one INFO line per migration naming the old
key, the new key and the value, because the common case is not a collision but
one old key taking effect for the first time.

  tests/test_the_two_settings_tables_agree.py -- 42 assertions, each written
  as a thing that must be IMPOSSIBLE. Verified by reverting each half in
  memory: without the literals the agreement check names the ones it should,
  without the suffix table neither `cell_FT` nor `organelleq_min_size` moves.
  476 passed across settings, validate, doctor and app_screen after rebasing
  onto 0f3c6dace.

ONE THING THE TESTS TAUGHT ME MID-WRITE: `organellea` IS NOT A ROLE. The slots
run `organelle`, `organelleaa`, `organelleab`, ... with no single-letter
spelling, so parametrising on it turned twelve assertions into twelve skips
that read as passes. `test_every_sample_role_is_real` now fails instead.

STILL OPEN in 364: the organelle preprocessing gap, the two proposals waiting
on the maintainer, and the generated-table checklist, which 391 will exercise.

--------------------------------------------------------------------------------
2026-09-12 -- THE ORGANELLE PREPROCESSING GAP, FOUND AND MEASURED. IT IS THREE
SETTINGS THE CODE READS AND NOTHING DECLARES.
--------------------------------------------------------------------------------

This file names "the organelle preprocessing gap" SEVEN times, every one of
them as "recorded above", and never once describes it. Nothing in
`features/data/364_mask_and_measure_settings_audit_2026-09-02.txt` describes it
either. So it was found again from the code rather than recovered from prose,
and it is written down properly here so the next reader does not repeat that.

    `spacr/io.py:1765-1771`, the per-channel preprocessing loop:

        background        = settings.get('organelle_background', background)
        signal_threshold  = settings.get('organelle_signal_to_noise', ...) * background
        remove_background = settings.get('remove_background_organelle', remove_background)

    ALL THREE ARE ABSENT FROM `expected_types`. Measured:

        remove_background_cell        declared: True
        cell_background               declared: True
        remove_background_organelle   declared: False
        organelle_background          declared: False
        organelle_signal_to_noise     declared: False

WHAT THAT COSTS. Every `.get` has a fallback, so nothing raises and nothing is
logged -- the organelle channel simply keeps the generic defaults set at the
top of the loop, `settings.get('background', 100)` and
`settings.get('remove_background', False)`. So:

  * ORGANELLE IS THE ONLY OBJECT CHANNEL THAT CANNOT HAVE ITS BACKGROUND
    REMOVED. cell, nucleus and pathogen each have a declared
    `remove_background_<role>` with a GUI row and a tooltip;
    `remove_background_pathogen` even defaults to True. The organelle channel
    is off and there is no declared setting that turns it on.
  * IT IS UNREACHABLE FROM THE INTERFACE. An undeclared setting has no type,
    no default, no tooltip, no category and no row, so it cannot be set in the
    GUI and never appears in a settings file the GUI writes.
  * AND THE DOCTOR ACTIVELY MISDIRECTS ANYONE WHO WORKS THE KEY OUT:

        'remove_background_organelle' is not a spaCR setting;
            did you mean 'remove_background_cell'?

    Following that advice changes the CELL channel's preprocessing instead of
    the organelle's. That is the same class of defect 391 found and suppressed
    for `<role>_intensity_threshold_method`: a wrong suggestion is FOLLOWED,
    where silence at least gets investigated. The other two are accepted
    silently, which is the quieter half of the same problem.

NOT FIXED HERE, AND THE REASON IS SEQUENCING RATHER THAN DIFFICULTY. The fix is
to declare the three -- default, type, tooltip, category -- which is a new
translatable string in ten catalogs and nine locales. The other session is
mid-rebuild for an unrelated merge and we have agreed to batch catalog-touching
work rather than pay forty minutes a commit. This is queued for that batch.

WHOEVER TAKES IT SHOULD CHECK ONE THING FIRST, because it decides the defaults:
`remove_background_pathogen` is True and `remove_background_cell` is False, so
the roles do not agree with each other and there is no obvious value to copy.
The honest default is whatever the organelle channel has EFFECTIVELY been doing
since the fallback was written -- which is False -- so declaring it False
changes no existing run, and any other value is a behaviour change that belongs
in the release notes.

--------------------------------------------------------------------------------
2026-09-13 -- THE ORGANELLE PREPROCESSING FIX REACHES ONE SLOT OF 702
--------------------------------------------------------------------------------

Found while auditing what the runtime catalog rebuild added. It added ten
settings, and eight of them are this item's:

    organelle_background       organelle_signal_to_noise
    organelleb_background      organelleb_signal_to_noise
    organellec_background      organellec_signal_to_noise
    organelled_background      organelled_signal_to_noise
    remove_background_organelle                     (slot one only)

TWO FACTS, AND THE SECOND IS THE BIGGER ONE.

FIRST, THE ASYMMETRY I INTRODUCED. `<slot>_background` and
`<slot>_signal_to_noise` were declared inside `_set_organelle_defaults`, so
every organelle slot gets them. `remove_background_organelle` was deliberately
left in the GENERIC block, because its role resolves to None and moving it
would make `_count_implied_by_the_slots` infer an organelle that is not there
-- the regression this item already records. The consequence was not thought
through at the time: slots b..zz now have a background floor and a
signal-to-noise anchor and NO toggle to apply them with. Slot one has all
three.

    organelle    background=True  signal_to_noise=True  remove_background=True
    organelleb   background=True  signal_to_noise=True  remove_background=False
    organellec   background=True  signal_to_noise=True  remove_background=False

SECOND, IT DOES NOT MATTER YET, AND THAT IS THE ACTUAL FINDING. `io.py`'s
channel-preprocessing branch (io.py:1746-1771) handles exactly four channels:
nucleus, cell, pathogen, and `organelle_channel`. It reads NO `organelleb_*`
key at all -- zero occurrences in the file. So all 701 organelle slots past the
first are declared with 52 settings each, carry tooltips, appear in the API
catalogs, and are never reached by the code that would use them.

This is the same shape as the gap this item was filed about, one level up. That
gap was "io.py reads three keys nothing declares". This one is "the settings
declare 701 slots that io.py cannot reach".

NOT FIXED HERE, DELIBERATELY. Adding `remove_background_organelleb` would
complete a symmetry into a branch that does not exist, which is tidier and no
more useful. The real question is whether io.py's four-branch structure should
become a loop over resolved object roles -- and that is a change to the
preprocessing path, not a settings audit. Recorded so the next person does not
mistake the missing toggle for the whole of it.

