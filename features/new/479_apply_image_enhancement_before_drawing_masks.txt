APPLY IMAGE ENHANCEMENT BEFORE DRAWING MASKS

Status: IMPLEMENTED — validated 2026-09-22; see verification notes below.
Owner: implementation; API/settings descriptions and translations coordinated with the documentation lane.

REQUEST
Put Compare and Apply at the bottom of Image enhancement. Apply toggles blue when active and can be switched off.

ACCEPTANCE
Selected enhancement settings remain configurable and comparable while inactive. Apply enables the chain for the displayed image and subsequent detection/mask creation; switching off restores unenhanced input without deleting settings or existing masks. Pending workers must not publish obsolete results.

Validate real UI interaction and affected pixel/mask behavior with focused tests.

2026-09-22 — IMPLEMENTED: Compare and blue checkable Apply share the bottom
row. Apply defaults off; configured settings remain available to Compare.
Display and automatic detection use only the active chain; original source
pixels and existing masks survive toggling. Request keys include the active
chain, protecting previews against obsolete worker results. Manual wand
flooding still uses original pixel intensities, as before; this is a separate
manual editing tool. 209 focused checks pass, including provenance and
histogram consistency. Reviewed the loaded-field rendering.

2026-09-22 — WAND FOLLOW-UP: supersedes the manual-Wand exception above.
Wand now reads the ready applied picture when Apply is on, with relative
intensity tolerance recalculated on it and absolute tolerance in the source
dtype's units. Pending/failed enhancement cannot silently flood original
pixels and creates no edit/history step. Failed work is reported; corrected
settings or reapplying can retry. Obsolete worker results cannot strand a
later Apply request. Ledger records input kind, inversion, normalization and
only the preprocessing steps used; detector morphology/splitting are not
claimed as manual Wand operations.126 focused checks pass;71 enhancement/
method checks pass after provenance additions. Original pixels stay intact.
Receipt for477–482: features/data/477_482_mask_editor_tools_2026-09-22.json.

2026-09-23 — COMPARE RESPONSIVENESS AND NORMALIZATION FOLLOW-UP
Compare previously called the enhancement chain synchronously on Qt's event
thread. It now opens immediately and processes a captured image/settings
snapshot on a serial worker. Cancel closes the comparison and suppresses its
result without waiting for a native filter already executing. A newer request
replaces pending work; closing the screen suppresses delivery. Late successes
and failures cannot overwrite a different field/settings comparison.
Compare also now applies whole-field detection normalization before cropping;
it previously named that step in the caption without actually performing it.
73 focused checks passed, covering timer responsiveness during blocked work,
cancel, obsolete success/error, newest-request replacement, real enhancement
output, linked zoom, source preservation and normalization equivalence.
This is readiness work for489/490, not their implementation.

SEE ALSO (filed 2026-09-24): item 508 (the same chain and deconvolution in
Mask generation, plus log and Gaussian transforms) and item 509 (inferring the
optics, and collapsing the advanced settings).
