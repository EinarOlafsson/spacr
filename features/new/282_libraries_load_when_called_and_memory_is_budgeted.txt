================================================================================
LIBRARIES LOAD WHEN CALLED, AND THE USER SETS THE MEMORY BUDGET
================================================================================

Status:    CLOSED 2026-09-19 BY THE MAINTAINER'S DECISION. The one thing
           still owed below, an opinion on the design, was put to him through
           the question prompt on 2026-09-19. He chose the option relayed
           as "the 'MEASURED, AND IT DECIDES THE SHAPE' section stands as the
           design opinion" (relayed wording, not his own words; see the
           2026-09-19 note at the foot). The work itself was already done
           (the 2026-09-15 note at the foot).
           Nothing is owed. The history of this line follows.

           WAS: PARTLY DONE, corrected 2026-09-13. "NOT STARTED" was wrong:
           the file carries "DONE 2026-08-28." with the lazy-import result
           asserted through `sys.modules` rather than timed.

           THAT "STILL OPEN" IS ITSELF STALE, corrected 2026-09-15 -- see
           the note at the foot. The caches DO consult them now:
           `resource_cleanup.sweep_memory_budget` applies both policies and
           `install_budget_sweep` is wired from seven call sites including
           the figure live-cache. 43 tests pass across the three budget
           files.

           STILL OWED, and it is the only thing: the maintainer wanted an
           OPINION on the design as well as the work, and no note in this
           file gives one. Originally filed 2026-08-27.
Owner:     spacr/qt/app.py (the preloader), and whatever cache layer this
           grows.
Requested: 2026-08-27.

--------------------------------------------------------------------------------
WHAT WAS ASKED
--------------------------------------------------------------------------------

"can you make it so that libraries get loaded when they are needed not when
 starting the application or eaven starting the modual, but when the library
 is called? then there should be a setting for how long the library is
 loaded for, so on week machines that which is not needed can be unloaded as
 soon as it is not needed and on stronger machines the machine can hold more
 in memory ... an option for the time a library is kept in memory while it is
 not being actively used, and an option for how long something can be in disk
 and ram cach while not being used before it gets removed, and how much ram
 and cach the system wants to keep open for new processes ... I want the user
 to be able controll these things so spacr can function on a laptop, server,
 and workstation with a wide range of hardware specs. also in the tooltips
 for the different modes should be system configuration recomendations for
 each level."

--------------------------------------------------------------------------------
MEASURED, AND IT DECIDES THE SHAPE
--------------------------------------------------------------------------------

A PYTHON MODULE CANNOT BE UNLOADED. Measured on this machine:

    baseline                     12 MB
    after `import torch`        489 MB   (+477)
    after deleting every torch entry from sys.modules and gc.collect()
                                489 MB   (0 returned)
    torch .so regions still mapped afterwards: 63

That is not a spaCR limitation and no setting can change it. C extensions
register types, allocate static buffers and map shared objects; CPython has
never supported unloading them. So "a setting for how long the library is
kept in memory" cannot be built for the libraries that actually cost
anything -- torch is 477 MB of the problem.

WHAT IS REAL, AND WHAT THE SETTINGS SHOULD GOVERN INSTEAD:

  * DEFERRING the import until first use -- real, and the honest form of
    "load when called". A session that never opens Classify never pays the
    477 MB. This is the part worth building.
  * GPU memory -- `torch.cuda.empty_cache()` returns cached allocations.
    Real, and on a shared GPU it matters more than RAM.
  * MODEL WEIGHTS held after a run -- real. Droppable and reloadable.
  * DATA CACHES: merged frames, image caches, crop caches. Real, and the
    natural home for an idle timeout and a size ceiling.
  * A HEADROOM FLOOR -- "keep this much free for new processes" -- is the
    right control and the maintainer is right that it comes FIRST: the other
    limits are meaningless until something says when to apply them.

So the request survives almost intact; only the word "library" has to become
"cache, weights and GPU memory", because that is what can actually be
returned.

--------------------------------------------------------------------------------
HOW TO KNOW IT WORKED
--------------------------------------------------------------------------------

  * a session that never opens a deep-learning module never imports torch,
    and that is asserted by `sys.modules` and not by a timing;
  * the idle timeout, the cache ceiling and the headroom floor are settings,
    and each one's tooltip says what hardware it suits -- the maintainer
    asked for "system configuration recomendations for each level" and that
    is the difference between a number and a decision;
  * a laptop profile and a workstation profile both run the same pipeline to
    completion, with the laptop trading speed for footprint;
  * NOTHING CLAIMS TO UNLOAD A LIBRARY. If a setting is named for that, it
    is lying, and the measurement above is why.


DONE 2026-08-28.

  * A session that never opens a deep-learning module never imports torch,
    asserted through `sys.modules` after Home and after Mask, Measure and
    Regression -- not by a timing. Peak resident memory for that session:
    533 MB, against the 489 MB a bare `import torch` costs by itself.

  * The three settings are on the Performance tab, under the level they take
    their suggestions from: KEEP FREE (the headroom floor), DROP UNUSED
    AFTER (the idle timeout) and CACHE CEILING. The floor is first, because
    the other two say what may be kept and it says when keeping it stops
    being acceptable.

  * Each tooltip quotes what every level suggests AND the hardware it suits
    -- "Laptop (8 GB or less, or running on battery): 1024 MB" -- which is
    the difference between a number and a decision. The recommendations are
    monotonic along the level order, which a test asserts.

  * NOTHING CLAIMS TO UNLOAD A LIBRARY, and the idle-timeout tooltip says so
    outright, because that is exactly what was asked for and exactly what
    cannot be done. A test asserts the sentence is there.

  * `what_to_drop` applies two reasons in order: anything idle longer than
    the timeout goes because nothing is using it, then if what remains is
    still over the ceiling the least recently used go until it fits. An
    entry is never dropped for being large alone -- size decides the ORDER
    of a trim, idleness decides whether one happens. Headroom that cannot be
    measured drops nothing: a cache that cannot be shown to be a problem is
    not dropped on suspicion.

STILL TO WIRE: `what_to_drop` and `headroom_is_short` are the policy and
they are tested, but the existing caches do not consult them yet. That is
`spacr/qt/resource_cleanup.py`'s sweep and the figure live-cache, and it is
mechanical -- the decisions the item was about are made and recorded here.

--------------------------------------------------------------------------------
2026-09-15 -- THE CACHES DO CONSULT THE BUDGET. ONLY THE OPINION IS OWED.
--------------------------------------------------------------------------------

The status line said `what_to_drop` and `headroom_is_short` "are written and
tested but the existing caches do not consult them yet". Checked against the
code rather than the header, which is this ledger's standing lesson:

  * `resource_cleanup.sweep_memory_budget` (:619) calls
    `memory_budget.what_to_drop` (:665) and re-measures
    `headroom_is_short` between pressure evictions, as its own docstring
    describes. It excludes in-use entries before either policy runs, counts
    their bytes against ONE process-wide ceiling rather than applying the
    ceiling per cache, and handles the CUDA allocator alongside.
  * `install_budget_sweep` is called from SEVEN sites --
    `crops.py:916`, `annotate_engine.py:56`, `crop_thumbs.py:133`,
    `timelapse_preview.py:130`, `timelapse_movie.py:83`,
    `figure_queue.py:566`, and `resource_cleanup.py:1676` itself. The figure
    live-cache, named in the stale line as not consulting the budget, is
    among them.
  * 43 tests pass: `tests/qt/test_live_cache_budget_wiring.py`,
    `test_the_memory_budget_drops_what_nothing_is_using.py`,
    `test_cov_r4_memory_budget.py`. They include a real thumbnail cache
    sharing one pool, a headroom re-check after each eviction, and a
    selected figure surviving an idle sweep.

SO THE MECHANICAL HALF IS DONE. What remains is the half that was never
mechanical: the maintainer asked for an opinion on the design, and this file
has recorded decisions without ever answering that. THAT IS NOT A TASK A
SESSION SHOULD QUIETLY CLOSE -- an opinion nobody asked for is worth nothing,
and one written to empty a checklist is worth less. It stays open and stays
named.


--------------------------------------------------------------------------------
2026-09-19 — CLOSED: THE MEASURED SECTION IS THE OPINION
--------------------------------------------------------------------------------

The note above kept this open for one thing, and said a session should not
close it by writing an opinion nobody asked for. So it was asked. Put to the
maintainer through the question prompt on 2026-09-19, the answer was: "the
'MEASURED, AND IT DECIDES THE SHAPE' section stands as the design opinion".
The quoted wording is the answer as the session that asked recorded it
and relayed it to this one; the prompt itself is not in the repository.

What that section commits the design to, so a reader does not have to
re-derive it:

  * A Python library cannot be unloaded. `import torch` cost 477 MB and
    deleting it from sys.modules returned 0 MB, with 63 .so regions still
    mapped. No setting is named as if it could.
  * "Load when called" means DEFERRING the import to first use, and that is
    what shipped: a session that never opens a deep-learning module never
    imports torch, asserted through `sys.modules`.
  * What CAN be returned is caches, model weights and GPU memory, so those
    are what the idle timeout and the cache ceiling govern, and the headroom
    floor comes first because it decides when the other two apply.

Checked again today that the mechanism the opinion describes is the one in
the tree: `sweep_memory_budget` is at spacr/qt/resource_cleanup.py:619, and
`install_budget_sweep` appears in seven modules (crops.py,
qt/annotate_engine.py, qt/crop_thumbs.py, qt/resource_cleanup.py,
qt/widgets/figure_queue.py, qt/widgets/timelapse_movie.py,
qt/widgets/timelapse_preview.py). No test was run for this note.
