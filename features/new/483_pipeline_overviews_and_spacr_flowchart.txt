PIPELINE OVERVIEWS AND THE SPACR MODULE FLOWCHART

Requested: 2026-09-22 by the maintainer.
Status: IMPLEMENTATION COMPLETE; translation/API/tutorial handoff pending in
        the documentation lane. Not a claim that the entire two-list goal is done.

Requirements
- Rename Home's Start a sample project button to Pipeline overviews.
- The overview window has an80percent opaque background, with opaque text.
- Each pathway row is a flowchart showing modules, inputs, outputs and links.
- Help has spaCR flowchart: every module is a network node, with directed
  output/input connections. Module and edge explanations appear in a fixed
  text box below the network, including on hover and keyboard selection.

Implementation
spacr/qt/widgets/workflow_diagram.py consumes the existing shared
spacr/resources/module_workflows.json; no alternate topology file.
spacr/qt/widgets/sample_project.py renders all9pathways as graph rows.
spacr/qt/widgets/home.py labels its existing button Pipeline overviews.
spacr/qt/app.py installs the Help action and reuses a single nonmodal window.

The full map includes all74modules and all49documented handoffs. Solid
arrows describe those documented handoffs. Optional dashed arrows match
artifact output/input types; their bottom-panel explanations explicitly
require checking schema, identities and settings. They are not claims that
all files of the same general type can be loaded directly. The connection
picker reveals a selected candidate arrow even when candidates were hidden.
Pathways follow the map's explicit after dependencies, preserving branches:
Map Barcodes is independent of image classification before Regression.
OPS starts in Mask's OPS page; generic Align & Stitch is not inserted.

Diagrams support drag-to-pan and Ctrl+wheel zoom. The full network also has
module/connection selectors and zoom/Fit controls. Hovering changes ink and
fixed-panel text without moving nodes or resizing rows. All input/output
locations are available in the bottom panel; long node summaries use ellipsis.
Start example remains an explicit action, retaining example loading and the
existing one-click walkthrough. Browsing/hovering never launches analysis.
The window paints an80percent-alpha rounded background, not window opacity:
text and controls stay fully opaque.

Validation
Focused Qt checks cover real node/edge hovering and clicks, stable geometry,
all map nodes/handoffs, candidate type matches, branch direction, Help entry
reuse, background pixel alpha204/255, existing nine-locale route titles and
summaries, and real OPS example routing. Dark/light renders inspected in
/mnt/firecuda2/Claude/implementation-20260922/pipeline-overviews/.
Final test count and documentation handoff recorded in the shared protocol.

Follow-up, same day
The maintainer requested API links and structured input/output columns.
Node explanations now link the mapped API module using Help's localized URL
builder. Edge explanations link BOTH endpoint APIs. Links open in a browser.
The network uses12dependency columns instead of a uniform grid: all49solid
handoffs point left-to-right; Map Barcodes shares Classify's column on a
separate row before Regression. Main pipeline nodes take the first row in
their columns. Independent sources sit near their consuming branch, terminal
consumers spread over later columns, and other modules use their artifact
types for placement. No node overlaps.35focused UI/routing/menu tests pass,
including column order, branch convergence and localized API URLs.

INTEGRATED DOCUMENTATION MAP, 2026-09-23
The documentation lane's expanded shared map now has77modules,52documented
handoffs and9pathways. All documented arrows still point left-to-right;
Mask -> Measure -> Annotate -> Classify -> Regression remains ordered, and
Map Barcodes still occupies Classify's column on a separate row. Localized
module and endpoint API links, stable hover geometry and node nonoverlap pass
against the integrated map.43combined graph, sample-route and secondary-editor
checks pass after rebase. Flowchart API descriptions and control translations
from the other lane are retained; broader catalog/tutorial work remains open.

ROW ORDER REFINEMENT, 2026-09-23
Within the existing dependency columns, two forward/backward passes now
order each branch near its connected modules. Main pooled-screen steps remain
first in their columns; Classify and Map Barcodes keep their shared column
and separate rows. No captions, API destinations or handoff contracts changed.
Independent parallel branches align instead of crossing, in both view modes.

The integrated77-module/52-handoff map's sampled cubic-arrow crossing pairs
fell62->33 in the compact full network and56->13 in the expanded layout.
These are transverse crossings, excluding shared endpoints and collinear
overlaps, measured at65samples per curve with0.01scene-unit tolerance.
This is a diagnostic, not an optimum: coincident routes and arrows through
intermediate columns remain. A stroke-intersection count including overlaps
did not improve in compact mode (112->114), so it is not claimed to do so.
Offscreen1800x1050render inspected; fixed hover/details and localized links
still pass. Geometry and measurement method are recorded in
features/data/483_neighbor_row_order_2026-09-23.json.
Validation:53flowchart, sample-project and shared workflow-map checks pass;
fatal-error Ruff and whitespace checks pass. Row-order regression cases
exercise crossed independent branches in both modes and repeatability; the
integrated graph also asserts the main pipeline's common top row.

POPUP AND PIPELINE DETAIL FOLLOW-UP, 2026-09-23
Implemented the maintainer's expanded request. Popup background dragging now
includes passive labels and containers inside scroll areas; sliders, editors,
selection views, diagram panning and divider handles retain their gestures.
Resize edges/corners show blue directional pointers. Diagram windows paint
one rounded80percent surface instead of stacking the global glass card.
Arrow shafts stop at the center of the triangle base, with a horizontal final
segment and flat caps. Independent branches align near their consumers.

The lower Pipeline overviews pane now retains the selected pipeline title,
description, every module/input card and every connection card. Module cards
end with localized API links. Selection changes a blue perimeter without
rebuilding cards, changing geometry or scrolling. Cards fill the available
width; a one-pixel blue splitter with Qt's expanded grab area adjusts height.
Default window1280x1000, diagram/details approximately380/520pixels; long
pipelines retain scrolling because all explanations remain available.

Nine pathways now declare external inputs. Added existing-image/mask Import
routes, Gate Editor/Image UMAP annotation alternatives, single/paired FASTQ
inputs, and independent recruitment/invasion/replication/plaque assay branches.
Display-only input nodes never replace a sample project's first runnable step.

118focused Qtchecks pass,9translation checks excluded from that follow-up run
because the new assay title is awaiting the documentation lane's catalogs.
Those9checks were run first and failed on the new title, not waived. Shared
workflow schema and live routes validate. Pooled and parasite renders were
inspected at1280x1000. Native compositor movement remains desktop-dependent;
offscreen tests cover fallback dragging, slider ownership and blue pointers.

2026-09-23 — USER FOLLOW-UP: CONTINUOUS TEXT AND DENSER PRESENTATION
Restored individual diagram nodes' input/output information. Continuous prose
now joins each module's guidance, input/output inventory, locations and final
API link; connections use the same inline treatment. No paragraph/bullet/line-
break fragmentation inside explanation cards or network detail tooltips.
Removed vertical stretching that separated cards from the top of their lane.
The pipeline title and description share a continuous header. External inputs
retain their dependency columns beside their consumers.
A single cubic tangent meets each arrowhead at its base center, replacing the
previous straight shoulder. Persistent selection borders and thin blue splitter
remain. Focused validation across the changed UI/schema paths: 1096 passed;
rendered pipeline and dictionary views inspected. Documentation handoff in 325.

2026-09-23 — SELECT A PIPELINE FROM ITS WHOLE ROW
A left click anywhere in a pipeline row selects it and refreshes its detail
cards: title, margins, empty diagram space, nodes, edges and scrollbar areas.
The event is observed without swallowing it, so diagram panning and item
selection retain their normal behavior. Five click-target regressions pass.
