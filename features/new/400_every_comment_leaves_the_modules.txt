================================================================================
EVERY COMMENT LEAVES THE MODULES AND THE REASONS MOVE TO docs/notes
================================================================================

Status:    BLOCKED 2026-09-13 -- both branches are unreachable from this
           machine and from origin; see the 09-13 note. Two rival versions
           exist and one must be discarded -- see BEWARE.
Requested: 2026-09-12 -- "you have left an extreemely high number of notes
           throughout many of the modual py files. If there is valuble
           information there i would like that to be retained. but if possible
           i would like to remove all of these # comments and move this
           information some place different."
Owner:     Claude session.

--------------------------------------------------------------------------------
WHAT THE STATE IS
--------------------------------------------------------------------------------

`tools/extract_source_notes.py` classifies every comment in the package,
moves the ones worth keeping to `docs/notes/<same path>.md`, and strips them
from the source. It is a dry run unless `--execute`, and it writes
`--discard-log` so a real run throws nothing away irrecoverably.

THE SAFETY PROPERTY IS AST EQUALITY, AND IT IS NOT SKIPPABLE. Every file is
compared `ast.dump(ast.parse(before)) == ast.dump(ast.parse(after))` before
it is written, and a file whose AST moved is refused. A comment strip that
changes behaviour is the only way this task can do harm, so it is the one
thing the tool will not do.

Current measurement: 512 modules stripped, 491 notes files written, ~50,500
comment lines removed. The 71 `#` lines that remain in `spacr/` are all
inside string literals -- QSS selectors (`#Sidebar`), a GLSL `#version 120`,
and `# BEFORE` / `# AFTER` examples inside docstrings.

--------------------------------------------------------------------------------
BEWARE: TWO VERSIONS, ONE MUST DIE
--------------------------------------------------------------------------------

  branch salvage/comment-extraction-original   commit 1d93e5947
      The first run. 514 files. Was on a DETACHED HEAD on no branch for
      several days, one `git worktree prune` from being lost, which is why
      it is branched now. Its base is 54 commits behind nightly.

  branch feat/comment-extraction-rebuilt       commit 5947cb375
      CANONICAL. Same tool re-run against current code rather than rebasing
      the original across 514 conflicted files. Also un-ignores `docs/notes`
      in `.gitignore` with a reason, instead of the original's `git add -f`,
      which left 491 files tracked-but-ignored and invisible to `git status`.

LANDING BOTH WOULD DOUBLE-APPLY. Land the rebuilt one; delete the salvage
branch once it is merged.

--------------------------------------------------------------------------------
WHAT TO DO
--------------------------------------------------------------------------------

1. DONE 2026-09-13, and the answer is that the strip is not the cause. A
   full-suite run on the stripped tree showed 15 failures -- 7 errors in
   `tests/flowview/test_the_panel_is_a_box_you_can_resize.py`, plus
   `tests/qt/test_ambient_wiring.py`, `tests/qt/test_console_thread_safety.py`
   and `tests/qt/test_cov_app_screen.py`. Run as their own four files on the
   SAME stripped tree, all four are green:

       225 passed, 1 warning in 313.06s

   So these are the family item 03 is about -- Qt tests that pass alone and
   fail after certain other files have run -- and not the comment strip,
   which does not touch a test file. Do not chase them here; 03 is where
   that work belongs.

   NOT the explanation, though it was the first guess: `-p no:randomly` on
   the suite run did not reorder anything, because `pytest-randomly` is not
   installed in this environment. The flag was a no-op.
2. Rebase `feat/comment-extraction-rebuilt` onto nightly and re-run.
3. Merge, then delete `salvage/comment-extraction-original`.

--------------------------------------------------------------------------------
HOW TO KNOW IT WORKED
--------------------------------------------------------------------------------

Not a full-suite run in one process. That was tried and is not survivable:
`tests/conftest.py` enforces a 6 GB ceiling with `os._exit(3)`, and the run
reached 5.3 GB at 9 % after 80 minutes, projecting ~14 hours. Use
`tools/run_pytest_batches.py` the way CI does, or raise
`SPACR_TEST_MEMORY_GB` deliberately.

    python tools/extract_source_notes.py          # dry run, expect no changes

A clean dry run on a merged tree means nothing was missed.

--------------------------------------------------------------------------------
2026-09-13 -- NEITHER BRANCH IS ON THIS MACHINE, AND NEITHER IS THE TOOL
--------------------------------------------------------------------------------

Steps 2 and 3 above cannot be performed from this checkout, and the reason is
worse than a missing branch.

SEARCHED, not assumed:

    git cat-file -e 5947cb375   (CANONICAL, feat/comment-extraction-rebuilt)
        NOT IN THIS REPOSITORY
    git cat-file -e 1d93e5947   (salvage/comment-extraction-original)
        NOT IN THIS REPOSITORY

and this worktree shares `/home/carruthers/Documents/repo/spacr/.git` with the
main checkout, so "not here" means not in the object store either copy uses.
Also checked and also absent:

    git ls-remote --heads origin | grep comment-extraction    nothing
    git reflog --all | grep -i comment.extraction             0 mentions
    every other .git under mnt3, /home/carruthers, and the
      session scratchpad, probed with `cat-file -e`           no hits
    tools/extract_source_notes.py                             absent
    docs/notes/                                               absent

SO THE DELIVERABLE AND THE TOOL THAT MADE IT ARE BOTH GONE FROM HERE: 512
modules stripped, 491 notes files, ~50,500 comment lines moved, and the AST
equality check that made it safe.

  THIS FILE PREDICTED IT. The BEWARE section says the original "was on a
  DETACHED HEAD on no branch for several days, one `git worktree prune` from
  being lost, which is why it is branched now". Branching it locally was the
  right instinct and was not enough, because NEITHER BRANCH WAS EVER PUSHED.
  A local branch in a repository somebody prunes is a detached HEAD with a
  nicer name.

WHAT IS NOT CLAIMED. The maintainer works on two machines and syncs between
them, so this work may exist on the other one. "Gone from this machine and
from origin" is the measured claim; "lost" is not, and a `git ls-remote` from
the other machine settles it in a second.

WHAT TO DO, in order:

  1. CHECK THE OTHER MACHINE for `feat/comment-extraction-rebuilt`. If it is
     there, PUSH IT before doing anything else with it.
  2. If it is not, the work is redoable -- the tool was ~a day and the run is
     mechanical -- but it has now been redone once already (the rebuilt branch
     exists BECAUSE rebasing the original across 514 conflicted files was
     judged worse than re-running the tool). Redoing it a third time without
     pushing would be the same mistake twice.
  3. Whatever survives, PUSH THE BRANCH FIRST and land second. Nothing about
     this item is safe to hold only locally.

STATUS CHANGED from IN PROGRESS to BLOCKED: the steps it lists operate on
commits that are not reachable from here.

--------------------------------------------------------------------------------
LANDED 2026-09-13 -- AND THE "BLOCKED" NOTE ON THIS ITEM WAS RIGHT AT THE TIME
--------------------------------------------------------------------------------

Commit d0334c103 on nightly: 492 modules stripped, 491 notes under
docs/notes, tools/extract_source_notes.py included.

6e72b1611 recorded this item as BLOCKED because neither branch nor the tool
existed on that machine. That was accurate and worth recording: both
branches were LOCAL-ONLY to the other checkout, which is exactly the hazard
the item itself warned about -- a 50,000-line deletion on a detached HEAD is
one `git worktree prune` from gone. Nothing was lost, but only because it
was branched in time.

The tool and the notes are now on origin, so the two steps that could not
run there can run anywhere. The salvage branch
`salvage/comment-extraction-original` (1d93e5947) is superseded and can be
deleted; the landed commit was regenerated from current source, four times
over, rather than rebased from it.

VERIFIED BEFORE THE PUSH, through `run_pytest_batches.py` with CI's own
invocation: 7 failures, every one of which fails identically on UNSTRIPPED
nightly or is a load artefact. Zero caused by the strip.

