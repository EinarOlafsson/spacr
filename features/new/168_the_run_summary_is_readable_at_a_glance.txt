================================================================================
THE RUN SUMMARY IS READABLE AT A GLANCE
================================================================================

Status:    filed 2026-08-19.
Requested: 2026-08-19, having read one in full -- "not very accessable make it
           more acessable and easier to read and overview"

THE CONTENT IS RIGHT AND THE SHAPE IS WRONG. Nothing in the summary is
inaccurate and very little of it is unnecessary: the "not applicable" lines
each say WHY, which is the property that makes a permutation run legible at
all, and they were written deliberately. The problem is that a reader opening
it cannot find the three numbers they came for.

Measured on the maintainer's own run: 108 lines, of which the answer to "did
this work and what did it call" is 6.

WHAT TO BUILD

A. A VERDICT AT THE TOP, before anything else. What was fitted, how many
   coefficients were called, at what alpha, and whether the positive control
   came out where it should. Six lines that a reader can stop after.

B. THE LONG-FORM BELOW IT, UNCHANGED. Every section that is there now stays
   exactly as it is -- this is an addition, not a rewrite. A summary that
   dropped the reasoning to be shorter would have traded the thing that makes
   it worth reading.

C. THE "NOT APPLICABLE" BLOCK COLLAPSES TO ONE LINE by default. Nine
   consecutive not-applicables with a paragraph each is the bulk of what makes
   this hard to scan, and they all say the same thing: this run fitted no
   model. Say that once, name the nine, and keep every full explanation
   available -- in the file, under a heading, not deleted.

D. THE PANEL FOLDS IT. The Summary tab shows the verdict expanded and each
   section collapsed, with the section headings as the outline. The file on
   disk stays plain text and stays readable in a terminal, because it is a
   run artefact before it is a widget.

HOW TO KNOW IT WORKED

* The first screen of the Summary tab answers "did it work, what did it call,
  is the control where it should be" with no scrolling.
* Every sentence that is in today's summary is still reachable.
* `model_summary.txt` still opens in a terminal and still reads top to bottom.

--------------------------------------------------------------------------------
2026-08-19 -- A, B AND C SHIPPED. D (the panel folds it) is not done.
--------------------------------------------------------------------------------

A. THE ANSWER FIRST. Six lines -- regression type, inference, wells,
   coefficients called, alpha, positive control rank -- then "Everything below
   is how that was arrived at." Every line is QUOTED VERBATIM from the section
   it came from, so there is no second place for a number to be wrong and
   nothing can drift between the headline and its source.

B. THE LONG FORM IS UNCHANGED. Nothing was deleted. The reasoning is what
   makes a permutation run legible at all, and a summary that dropped it to be
   shorter would have traded away the thing worth reading.

C. THE NOT-APPLICABLE BLOCK COLLAPSES. One line where they stood, NAMING them,
   with every word kept under NOT APPLICABLE, AND WHY at the end -- and there,
   fields sharing a reason share its explanation. Measured on the maintainer's
   own run: eleven deferred fields carrying SIX distinct explanations, two of
   them printed three times each. Six paragraphs where two would do, and that
   repetition is most of what "not very accessable" was about.

   A joined label longer than the label column gets its own line: the first
   version squeezed the explanation into whatever was left and it came out one
   word wide.

STILL OWED
  * D. The Summary tab does not fold the sections. The file is right; the
    panel still shows it as one scroll.

--------------------------------------------------------------------------------
2026-08-19 (later) -- D SHIPPED. THE INSTRUCTION IS CLOSED.
--------------------------------------------------------------------------------

`spacr.qt.widgets.folding_summary.FoldingSummaryView` -- a drop-in for the
QPlainTextEdit the Summary tab used (setPlainText, toPlainText, isReadOnly,
font), so nothing that fills or reads the tab changed. THE ANSWER open, every
other section folded, the headings as the outline.

PARSED FROM THE TEXT rather than built from `RunSummary`, which looks like
the wrong way round when the structure is right there. The tab does not
always have it: what it shows may be the run's OWN file read back from beside
a results table loaded from disk, a file written by another version of spaCR,
or the statsmodels summary, which has no spaCR sections at all. Parsing
serves all three and keeps the FILE as the source of truth rather than making
the widget a second renderer that can disagree with it.

A heading is a line whose successor is a rule of - or = THE SAME LENGTH, and
that last clause is the whole parser: statsmodels draws rows of dashes all
over its output, and without the length check every line above one becomes a
heading and the tab folds itself into nonsense. Text with no headings is
shown whole.

The file on disk is unchanged and still reads top to bottom in a terminal.
