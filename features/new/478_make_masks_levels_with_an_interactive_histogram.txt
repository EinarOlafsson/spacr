MAKE MASKS LEVELS WITH AN INTERACTIVE HISTOGRAM

Status: IMPLEMENTED — validated 2026-09-22; see verification notes below.
Owner: implementation; API/settings descriptions and translations coordinated with the documentation lane.

REQUEST
Add a Levels graph beside Normalize in Make Masks, letting the user threshold the image histogram.

ACCEPTANCE
Show the actual image histogram with adjustable level/threshold controls, immediate visual feedback and reset. Define explicitly which controls affect display and detector input, preserve original measurements, and document every new setting.

Validate real UI interaction and affected pixel/mask behavior with focused tests.

2026-09-22 — IMPLEMENTED: Levels beside the display percentiles opens a
modeless full-field histogram. Drag black/white markers or enter intensities;
cutoffs map to the existing percentile settings and clip/stretch the display.
Reset restores the full range. Detect on the normalized image is synchronized
with the panel and explicitly opts detectors into the same levels, before
any active enhancement. This is a levels stretch, not a binary mask edit.
Worker-built sorted intensities give exact interpolated percentile positions;
constant/nonfinite fields are explained. Changing field/inversion closes the
old editor. No source pixels or existing labels change.52 focused checks
pass in14.73s; all4 new interaction checks pass again after the layout fix.
Reviewed the themed560x520 dialog: graph and controls no longer overlap.
