494 — RETIRE DEMOS IN FAVOR OF TEST DATA AND WALKTHROUGHS
=======================================================
Status: DONE for the runtime 2026-09-25 (built 2026-09-23, re-checked). The
leftover Demos references are all in the docs lane's files (see the last note).

User requests removal of Demos from Help and all references to that navigation
destination. Modules now offer Load test data; Home offers Pipeline overviews
and the matching walkthroughs. Remove menu actions and the source-empty prompt
that directed users to Demos. First-run tour describes test data/walkthroughs.

Internal synthetic generators are still used by unit tests and recording
fixtures. Their presence does not make Demos a user-facing product feature.
The documentation lane must retire the tutorial scripts' obsolete menu steps
and published instructions/translations. No completion claim for that work yet.

153focused menu, form-registration, preview and registry tests pass;4existing
non-settings-screen cases skip. Host–Pathogen Live now has the same toggle
type as other Live controls and stays at the bottom Actions heading.

Download follow-up2026-09-23: the shared progress title and CLI Mask dataset
summary now say test data. Download errors no longer send offline users to
the retired Demos menu; they offer cached test data or local images and
explain reconnecting for Load test data.47focused download checks pass.

2026-09-23 — UNUSED HANDLERS REMOVED
Deleted the retired menu's folder/generation/download dialogs, end-to-end
handler, menu-label resolver, status-tip refresh and empty-state menu opener.
The Home media generator no longer draws Demos. Internal recording/test
fixture generation and settings application remain; they have no menu entry.
Removed tests specific to the deleted controls and changed the old end-to-end
menu assertion to assert absence. Active examples, settings import, module
registration, language-error handling and menu checks remain covered:
176pass/4expected non-settings-screen skips, plus the corrected menu-absence
check passes separately (177passes combined);327retained coverage tests collect.
Documentation lane owns the remaining published tutorial/media/API/catalog
references, explicitly handed off with the list of removed methods.

2026-09-25 — RUNTIME CLOSED; WHAT THE DOCS LANE STILL HOLDS
Re-checked on nightly a93f937d2 (offscreen): tests/qt/test_demo_menu.py and
the Demos cases of tests/qt/test_walkthrough.py, 30 passed, including
test_the_removed_demos_menu_is_not_offered_by_the_walkthrough. No
user-facing string, menu or prompt in spacr/ reaches a Demos menu. What is
left is inert and belongs to the docs lane (tutorials, API notes,
translations), so it is listed rather than edited here:
  * spacr/qt/tutorial/scripts.py -- the "Demos" special cases in _find_menu
    and _menu_target and the unused _open_demos_menu (no tutorial step
    calls it; tests/qt/test_cov_r4_tutorial_scripts.py and
    tests/test_cov_10_tutorial_scripts.py cover them and go with them);
  * the "Demos", "Demos menu" and "Open Demos menu" rows in
    spacr/qt/i18n.py and the translation-correctness exemption for "Demos";
  * docs/notes/spacr/qt/{tutorial/scripts,app,screens/app_screen,
    synthetic}.md and the reviewed API translation JSONs that still name it.
