================================================================================
THE GUI FITS A LAPTOP: A WHOLE-GUI SCALE, AND EVERYTHING COLLAPSES
================================================================================

Status:    OPEN, filed 2026-09-22. Not started.
Requested: 2026-09-22, by the maintainer, in these words:

           "the scale of spacr works great on a 4k monitor, but on a laptop at
           a lower resolution it there is often not enough spact to show
           everything. This is esspecially true for live preview in mask
           generatin, measure, image UMAP. so we need to add a setting that
           will scale all the GUI elements to preferences. we already have
           font scale but this dosnt effect the scale of many gui elements so
           add scale GUI as a setting from 10% to 200% default 100%. also for
           each live preview. appart from this scale slider, it would be nice
           if live preview wuld ocupy the entire screen (vertically) so if
           console, system and the buttons section below system were all auto
           collapsed (we need to add the ability to collapse the section under
           the systems container). it would also be cool if we could collapse
           the settings to the left. same should be true of the figures,
           whenever figures appear, consol, system, and buttons auto collapse
           and the setting s collapse to the left. then if the user wants they
           should be able to collapse the figures pannel and the live preview
           pannel and uncollapse anything at any time. also any sections inside
           the figures or live preview containers should also be colapseable.
           really whenever possible evey GUI element be it a figure or a table,
           should be collapseable in every modual, to acomodate smaller
           screens. and whenever anything is collapsed it should auto loch to
           the bottom of the container it is in (now for example when i
           collapse the console it collapses to the middle of the container.)"

Owner:     unassigned. Touches the app shell and nearly every screen: it is a
           cross-cutting item, and whoever builds it should land it in slices
           (the scale, the shell's collapse behaviour, then screen by screen).

WHAT IS THERE TODAY (read on nightly e2f082bf8)
-----------------------------------------------

* A FONT scale exists and nothing else scales: spacr/qt/preferences.py
  FONT_SCALE_MIN = 0.10 and FONT_SCALE_MAX = 2.00 (~540-541), clamped at
  ~3625 and ~3631, with its slider at ~5504. No whole-GUI scale exists
  anywhere in spacr/qt (no QT_SCALE_FACTOR, no scale-factor preference), which
  is why widget sizes, paddings, icons and fixed pixel sizes stay put while the
  text shrinks.
* The console already collapses its own topic sections
  (spacr/qt/widgets/console_panel.py collapse_section ~1647, toggle_section
  ~1662; sections start expanded, ~1665), but the console panel itself, the
  System container and the button section under System do not auto-collapse,
  and the button section cannot collapse at all.
* The live previews the maintainer names are spacr/qt/widgets/live_preview.py
  (Mask), measure_preview.py (Measure) and the image-UMAP views
  (umap_explorer.py, umap_search_viewer.py, annotation_umap_tab.py); the same
  shape recurs in motility_preview.py, plaque_preview.py and
  timelapse_preview.py. The figures panel is figure_grid.py /
  figure_grid_view.py / figure_queue.py. Screen layout is in
  spacr/qt/screens/app_screen.py.
* Item 417 (the magnifier's second round) made Make Masks' settings
  categories collapsible; that is the nearest existing pattern to reuse.

WHAT TO BUILD
-------------

1. "GUI scale" in Preferences, beside font scale: 10% to 200%, default 100%,
   scaling every GUI element -- widget sizes, spacing, icons, fixed pixel sizes
   -- not only text. Each live preview gets its own scale slider as well.
2. When a live preview opens, it takes the whole screen height: the console,
   the System container and the button section below System auto-collapse, and
   the settings collapse to the left. The button section needs a collapse
   control it does not have today.
3. The same when figures appear: console, System and the buttons auto-collapse
   and the settings collapse to the left.
4. The user can collapse the figures panel and the live preview panel, and can
   expand anything again at any time. Auto-collapse never removes the user's
   ability to undo it.
5. Every section inside the figures and live preview containers collapses.
6. Wherever possible, every GUI element -- a figure, a table -- is
   collapsible, in every module.
7. A collapsed element locks to the BOTTOM of its container. Today the console
   collapses to the middle of its container; that is the bug this point fixes.

WHAT TO BE CAREFUL WITH
-----------------------

* A true whole-GUI scale in Qt is either a startup-time scale factor (needs a
  restart to change) or every size routed through one scaled helper (live, but
  every hard-coded pixel size must go through it). Decide which before
  building, and say in the setting which it is. The font scale and the GUI
  scale must compose, not fight: 50% GUI at 200% font is a real combination.
* Auto-collapse must not fight the user: once they expand something by hand,
  it stays expanded for that view until they leave it.
* 10% is very small. The screen must stay usable and recoverable there (the
  same survival test item 413 added for the 10% font floor).
* New captions (GUI scale, the collapse controls) are translated strings in
  nine languages: list them for the catalog lane rather than regenerating
  spacr/qt/i18n_catalogs.

DONE WHEN
---------

* At GUI scale 50% on a laptop-sized window, Mask live preview, Measure live
  preview and image UMAP show everything, and 100% looks as it does today.
* Opening a live preview or figures collapses console, System, the buttons
  and the settings, and each can be expanded again.
* Every figure and table in every module can be collapsed.
* A collapsed element sits at the bottom of its container, the console
  included.

--------------------------------------------------------------------------------
2026-09-22 — ADDED BY THE MAINTAINER: DRAG AN EDGE TO RESIZE
--------------------------------------------------------------------------------

In his words:

    "to the same feature whenever possible make the container expandable or
    shrinkable by draging the edges."

WHAT IS THERE TODAY: drag-to-resize already exists in places -- 67
QSplitter constructions across 51 files under spacr/qt (among them
layer_viewer.py, screens/agreement.py, screens/align.py, screens/annotate.py, screens/app_screen.py, screens/batch.py) -- but only partly in the app shell
itself (spacr/qt/screens/app_screen.py): the settings column, the console, System and the
button section below it, the live preview and the figures panel are not
resized by dragging their edges.

WHAT TO BUILD: wherever a container can sensibly change size, the user can
drag its edge to grow or shrink it, with collapse (points 2-7 above) as the
limit of shrinking rather than a separate mechanism. A QSplitter already gives
both -- a draggable handle and collapsing a pane to zero -- so the two halves
of this item can share one mechanism.

DONE WHEN: the settings column, console, System, the buttons section, the live
preview and the figures panel can each be resized by dragging an edge, the
sizes survive leaving and returning to a module, and dragging never fights an
auto-collapse the user has already undone.

--------------------------------------------------------------------------------
2026-09-22 — SLICE A LANDED: THE GUI SCALE AND THE PREVIEW SCALE (point 1)
--------------------------------------------------------------------------------

Slice A is point 1 only. Points 2-7 and the drag-to-resize addendum are other
slices and are not touched here.

THE MECHANISM, DECIDED BY AN AUDIT. A live scale would have to route every
hard-coded pixel size through one helper. Counted on nightly across the 328
modules under spacr/qt: 470 setContentsMargins, 489 setSpacing, 179
setMinimumWidth/Height, 67 setMaximumWidth/Height, 74 setFixed*, 30
setMinimumSize, 56 QSize(...), 64 resize(...), 6 setIconSize -- about 1,420
geometry calls in 181 files -- plus 486 "px" literals in style-sheet text in
64 files (177 of them in theme.py). Too invasive, and every size added later
would have to remember the helper. So the GUI scale is a HYBRID:

* GUI scale (Preferences, beside Font scale; 10-200 %, default 100 %) is a
  startup-time Qt scale factor. spacr/qt/gui_scale.py writes it into
  QT_SCALE_FACTOR before the QApplication exists (called from launch() in
  spacr/qt/app.py), so Qt scales every logical pixel: widget sizes, fixed
  sizes, margins, spacing, QSS paddings, icons, pictures, text. A change
  needs a restart; the row says so, shows "N% after a restart (now M%)" and
  offers Restart now, which saves the dialog and restarts through the same
  record Force restart writes (the module and its settings come back; a run
  in progress is asked about first). At 100 % nothing is written, so a
  default launch is byte-for-byte today's.
* Font scale ("Zoom") stays live, on top. They multiply: 50 % GUI at 200 %
  font gives half-size widgets with text at its usual size on screen
  (tested in a child process: body text 26 logical px = 13 device px, a
  100 px box = 50 device px).
* A user's own QT_SCALE_FACTOR is kept (SPACR_GUI_SCALE_BASE) and a restart
  does not compound it. SPACR_GUI_SCALE=1 overrides the stored value for one
  launch; safespacr reads it as its default, 100 %.
* The window keeps its physical size at any GUI scale
  (fit_window_to_gui_scale), so the room gained goes to the content.
* matplotlib's toolbar icon engine multiplies the device ratio in twice;
  below a ratio of 1 the pan/zoom icons drew 3 px tall at 50 %.
  gui_scale.mend_matplotlib_icons() holds the engine's ratio at >= 1 (only
  when running below 100 %); measured 11 px, and no change at ratio 1 or 2.

EACH PREVIEW'S OWN SCALE, live. spacr/qt/widgets/preview_scale.py (re-exported
from preview_controls.py) is one implementation used by Mask
(live_preview.py), Measure (measure_preview.py), Plaque, Timelapse, Motility
and the three image-UMAP views (umap_explorer.py, umap_search_viewer.py,
annotation_umap_tab.py). A 10-200 % slider scales, inside that preview only:
style-sheet sizes it inherits and its widgets set (font sizes, paddings,
margins, min/max sizes, radii; border widths left alone), min/max/fixed
widget sizes, layout margins and spacing, button icon sizes, and through a
hook what the panel draws itself (Measure's crop thumbnails, the UMAP
figure's dpi). Bases are remembered per widget so it never compounds, a size
the code sets later becomes the new base, and at 100 % nothing is touched at
all. Saved per preview under prefs/preview_scale/<name>. The slider sits in
the preview card's title row beside Refresh (placed on first show) -- in the
toolbar row it raised the column's minimum and the settings column lost
68 px at 100 %. It is exempt from its own scale; double-click its value for
100 %.

SURVIVAL AT 10 %. Ctrl+Alt+0 (free in shortcuts.py; Cmd+Option+0 is not a
macOS-reserved combination) puts GUI scale, font scale and every preview
scale back to 100 % and asks to restart with Restart now as the DEFAULT
button, so Ctrl+Alt+0 then Enter recovers without reading anything. Tested
on a real MainWindow built at 10 % in a child process (the binding exists,
Preferences still opens with the row at 10, Enter restarts). Honest limit:
at 10 % Preferences itself is a ~60 px postage stamp on a 1366x768 screen --
reachable, not usable by mouse; the keyboard route is the way back.

CHECKED. At GUI 50 % on 1366x768 and 1280x800 (offscreen), Mask live
preview, Measure crop preview and image UMAP show every control, the image
canvases and the figure. At 100 % the screens pixel-compare against nightly
at ec09c3608 (slice B landed): Home identical (0 px); Mask, Measure and
image UMAP differ by 683 px each, all inside the new slider's own box.
Tests: tests/qt/test_the_gui_scale_fits_a_laptop.py (23). The shipped-module
ratchet moves 606 -> 608 for the two new modules.

NEW CAPTIONS for the catalog lane (not translated here): "GUI scale",
"Restart now", "Later", "Restart spaCR?", "GUI scale reset",
"{wanted}% after a restart (now {running}%)",
"GUI scale is back to 100 %. It takes effect when spaCR restarts.",
"A run is still going. Restarting stops it; the module and its settings come
back, the run does not.", "Reset GUI scale and font scale to 100%", the GUI
scale tooltip (preferences.GUI_SCALE_TIP), the Restart now tooltip, and the
preview-scale tooltip (preview_scale.PREVIEW_SCALE_TIP).

LEFT FOR OTHER SLICES / NOTED. With a saved preview scale below 100 % the
body splitter's first allocation follows the smaller minimum and gives the
settings column more room -- the app-shell sizing is slice B's. The Mask
"MIP" toggle paints its own text and does not follow the preview scale.
tests/test_i18n_coverage_audit.py::test_written_review_scope_matches_current_source_bound_evidence
pins docstring counts and moves with any new docstring; the catalog lane
owns that report.

--------------------------------------------------------------------------------
2026-09-22 — SLICE B LANDED: THE SHELL COLLAPSES AND EVERY EDGE DRAGS
(points 2, 3, 4, 7 and the DRAG AN EDGE addendum)
--------------------------------------------------------------------------------

Commits 01ac8a80b (the mechanism), ec09c3608 (the shell), e37f9e347 (the
module at 100 % branch coverage). Points 5-6 (every section, figure and table
inside every screen) are slice C, and slice C builds on the primitive below.

ONE MECHANISM. spacr/qt/widgets/collapsible_splitter.py. A QSplitter already
gives a draggable handle and a child that can be taken to zero, so collapse is
the limit of dragging, not a second system.

* CollapsibleSplitter(orientation, parent=None, *, persist_key="") -- a
  QSplitter whose children are named panes:
    add_pane(widget, name, *, folder=None, mode=None, stretch=1, extent=0,
             minimum=None, focus=False, fold_key="", index=None) -> Pane
    pane(name), panes(), is_collapsed(name),
    set_collapsed(name, collapsed, *, by_user=False), toggle_pane(name),
    rebalance(), extents(); signal pane_toggled(name, collapsed, by_user).
  Pane kinds:
    HEADER (give it a Folder from widgets/foldable.make_foldable) -- folds
      to its heading, and the heading is LOCKED TO THE BOTTOM of the room the
      pane has (a stretch goes in above it). A folded HEADER pane is held at
      its heading's height by a maximum, so no window resize or drag hands it
      a gap. When nothing that stretches is left open, the first folded
      heading takes the spare room, so every folded heading stacks at the
      bottom of the container.
    EDGE (mode=EDGE) -- folds to nothing; the handle beside it is the strip:
      it carries an arrow, a click on it folds or restores the pane, a drag
      resizes it. fold_key remembers the user's fold across restarts.
    plain (no folder, no mode) -- resizes, never folds.
  Dragging an open pane past its minimum collapses it (Qt's own collapse,
  turned into the pane's fold). Sizes are remembered per pane NAME (not
  index) under persist_key in the preferences store (layout/panes/<key>), so
  adding a pane to a screen does not scramble the old layout. A drag never
  moves anything the user did not move; a width change re-fits only
  fixed-size panes that wrap (the Run row), since no handle is held then.
  A widget that is later wrapped in a container put in its place (the
  settings search strip does this) hands its registration to the container.
* FocusCollapse(parent) -- watch(widget) a FOCUS widget, target(splitter,
  name) a pane; while any focus widget is shown the targets fold, and when
  the last is hidden the ones it folded open again. begin_view()/end_view()
  and is_pinned(splitter, name). It reads hidden flags only: nothing is
  shown, measured or built to register it.
* lock_folded_to_bottom(container, folder, orientation=Qt.Vertical) -- the
  bottom-lock on its own, for a fold that is not in a splitter (a section
  in a scroll area). This is the piece slice C applies to sections.
* Folder.set_shut(shut, *, by_user=True), Folder.toggle(*, by_user=True),
  Folder.add_listener(cb(shut, by_user)), Folder.last_change_by_user. A fold
  made on the user's behalf (by_user=False) is never persisted.

FOR SLICE C: to make a panel collapsible, give it a heading and
make_foldable(heading, body, name=..., persist_key="<module>/<panel>"); if its
siblings should trade height with it by dragging, put them in a
CollapsibleSplitter and add_pane each with its folder; otherwise call
lock_folded_to_bottom(panel, folder). Card(title=..., foldable=True,
fold_key=...) already gives a Folder on the title.

THE SHELL (spacr/qt/screens/app_screen.py).
* The runtime column is ONE vertical CollapsibleSplitter, top to bottom:
  Figures, the module's preview (Mask/Plaque Live preview, Measure Crop
  preview, Timelapse Track preview, Motility preview) or its Parameter sweep
  / Hyperparameter search card, Console, System, and the new buttons section
  ("Actions": the Run row, whatever is mounted above it -- chaining strip,
  QC banner, diameter panel, map-barcodes strips -- and the two hover-help
  strips). Every one of them resizes by its edge and folds by its title; the
  buttons section has a heading it did not have before. A folded one is its
  heading at the bottom: the console no longer folds to the middle.
* The settings column is an EDGE pane of the body splitter: click its handle
  (or drag it to nothing) and it collapses to the LEFT; click or drag again
  and it comes back at its old width. Ctrl+F opens it first if it is folded.
* Keys: runtime sizes under "<module>::runtime", the column under
  "<module>::body"; folds under "<module>/Console", "/System", "/Actions",
  "/Settings" (Console and System keep their existing keys).
* The four "above the Run row" inserters (prerun, chaining, preview_registry,
  map_barcodes) now follow the Run row into its section; a preview mounted
  by preview_registry is adopted (adopt_runtime_pane) as a focus pane.

AUTO-COLLAPSE (points 2-4).
* FOCUS panes: the figures card and every preview card, including registry
  previews. When one is shown -- a Live switch turned on, the first figure of
  a run, the image-UMAP explorer -- Console, System, the buttons section and
  the settings column fold, and the focus takes the height. When the last is
  hidden, whatever the auto-collapse folded opens again; what the user had
  folded stays folded.
* The user can fold the figures panel and the preview by their titles, and
  open anything at any time. A focus pane the user folded opens again when it
  is next shown (switching Live on means "show me the preview").
* NEVER FIGHTS THE USER. A target the user opens by hand WHILE a focus is
  shown -- clicking its heading or its handle, or dragging it open -- is
  PINNED: no later auto-collapse folds it again during the view. Opening a
  pane when no focus is shown overrules nothing and is not a pin.
* THE VIEW is one visit to one module screen: from the screen's showEvent to
  its hideEvent (going to another module or screen). Spontaneous events
  (minimising the window) do not end it. Returning to the module starts a new
  view: pins are forgotten and, if a preview or figures are still showing,
  the surroundings fold again.

KNOWN CONSEQUENCE. The Live / 3D / Time / OPS / AI switches live in the Run
row, so while a preview has the height they are behind the folded "Actions"
strip, one click away. Moving the preview switch into the preview card's
title row would keep it in reach; left for the maintainer to decide.

SCREENSHOTS (offscreen, scratchpad b471shots/): 1366x768 and 1920x1080 of
Mask with the console folded (locked to the bottom, directly above System),
Mask with Live preview open (auto-collapsed; the preview has the height),
the same after the console was reopened by hand, and Measure with a figure
showing (settings folded left, console/System/Actions folded).

TESTS. tests/qt/test_a_collapsed_pane_locks_to_the_bottom.py (the mechanism:
bottom lock, drag-to-collapse, the settings handle, persistence by name, the
never-fight rules, views), tests/qt/test_the_shell_collapses_and_resizes.py
(Mask and a registry preview end to end, Ctrl+F, a lazy panel stays
unbuilt), tests/qt/test_cov_collapsible_splitter.py (every branch). Updated:
test_cov_app_screen (pane order), test_preview_registry, test_prerun,
test_chaining_gui (the Run row's section).

NEW CAPTIONS for the catalog lane (not translated here): "Actions";
"Click to hide {name}, or drag to resize it."; "Click to show {name} again,
or drag to resize it."

--------------------------------------------------------------------------------
2026-09-22 — SLICE A, SECOND ROUND: THE GUI SCALE IS LIVE, AND EVERY SCALE
CHANGE ASKS KEEP OR REVERT
--------------------------------------------------------------------------------

In the maintainer's words:

    "the GUI scale is only adjusted after startup. please make this work with
    out restarting. and if either font or GUI scale are modified the user
    should be prompted with a Keep or Revert popup."

WHAT CHANGED. The startup Qt scale factor of the section above is gone. The
GUI scale is now applied live by a scaling layer over Qt's own setters
(spacr/qt/gui_scale.py, install_scaling_layer, called from launch() before
the first widget exists). No restart path is left in Preferences.

HOW THE LAYER WORKS, and why it is not 1,420 edits. It replaces the setters
themselves on the Qt classes -- QWidget's setMinimum/Maximum/Fixed
Width/Height/Size and setStyleSheet, the layouts' setContentsMargins,
setSpacing, setHorizontal/VerticalSpacing and addSpacing, every setIconSize
in QtWidgets, QSplitter.setSizes -- and each replacement:

  * remembers on the object the value the code asked for, its size at 100 %;
  * hands Qt that value times the current scale;
  * answers the matching getter in 100 % units, so the theme's sheet
    digests, slice B's persisted splitter sizes and any code that reads a
    size and sets it again keep working in one unit and never compound.

set_gui_scale_live() then walks every live widget and layout, puts the
remembered values back at the new scale, rebuilds what a sheet cannot reach
(refresh_theme: the window chrome and the Home tiles' painted pixmaps) and
asks the windows to lay out again. Widgets built later are scaled as they
are built, so the lazily built panels (284/380) and the settings categories
that wait off the page need nothing of their own.

STYLE SHEETS. scale_qss_text rewrites only the values of size properties --
font-size, font, padding, margin, min/max width and height, width, height,
spacing, border radii -- in place, by regex, leaving selectors, colours,
borders and url(...) data untouched. The theme's sheet goes through the
same replacement, which is how font scale and GUI scale compose: a font
size is base x font scale x GUI scale. A user's own QT_SCALE_FACTOR is not
touched and still multiplies on the outside.

WHAT ELSE FOLLOWS. Matplotlib figure dpi (base x GUI scale x that preview's
own scale), matplotlib toolbar icons and any other icon size the style
supplies rather than the code, Measure's crop thumbnails, and the preview
sliders of the first round, which compose with it.

WHAT STILL DOES NOT FOLLOW, named rather than hidden: text and shapes a
widget paints at fixed pixel coordinates, pixmaps scaled to a number a
widget computed, setFont with an explicit size, move/setGeometry positions,
header section sizes, a Python sizeHint override that returns a constant,
style metrics the theme's sheet does not restate, and pyqtgraph's axis
text. A size computed from font metrics is scaled once by the font and
again by the setter; the layer keeps a base that looks like its own scaled
value (within 6 %) rather than re-basing, which is what stopped the window
chrome coming back half size after a round trip.

KEEP OR REVERT. change_scales() applies the new GUI and font scale at once
and opens "Keep these settings?" without blocking, counting down 15 s.
Revert, Esc, closing the window and the end of the countdown all put the
old values back; Keep leaves them. The question is exempt from the scale
(EXEMPT property on its window) and states its own text sizes, so it stays
readable at 10 % GUI and 10 % font -- which is the recovery that matters,
and the Z + wheel gesture now ends with the same question. Ctrl+Alt+0
remains the backup and no longer restarts anything.

CHECKED. tests/qt/test_the_gui_scale_fits_a_laptop.py (27) covers a live
change reaching widgets already built and widgets built afterwards, the
zero-drift round trip 100 -> 50 -> 170 -> 10 -> 100 on every recorded size,
the re-measuring widget that must not re-base, the exempt window, the
composition of the two scales, the sheet rewrite leaving borders and data
URIs alone, the countdown's auto-revert, Keep, Revert, Esc, the readable
question at 10 %, the Preferences slider applying live and asking, the
reset key, and the figure dpi following both scales. 919 tests across the
preferences, zoom, theme, shell, preview and figure suites pass beside it.

Screenshots at 1366x768, offscreen: mask/measure/umap at live100, live50
and live100b (one process, scale changed under a running window) and the
Keep question at 50 %. Going back to 100 % is pixel-identical on all three
screens except 380 px in the window's own corner marks, which refresh_theme
rebuilds two pixels lower -- the same rebuild a Zoom change has always
done. At 100 % the screens are pixel-identical to nightly apart from the
live RAM/GPU meters and a status message.

--------------------------------------------------------------------------------
2026-09-22 — SLICE C LANDED: EVERYTHING COLLAPSES (points 5 and 6, and the
DRAG AN EDGE addendum inside the screens)
--------------------------------------------------------------------------------

Slice C is points 5 and 6: every SECTION inside the figures panel and inside
every live preview, and then -- wherever possible -- every figure and every
table in every module. It adds no second mechanism: everything below is slice
B's CollapsibleSplitter, FocusCollapse and Folder.

WHAT SLICE C ADDED TO THE MECHANISM (spacr/qt/widgets/collapsible_splitter.py)

* FoldSection(body, "Name", persist_key="<module>/<Section>", actions=...)
  -- the one way a section inside a screen gets a fold control, so every one
  of them looks alike: the arrow and the name above the body, a click folds
  it, and a folded section is its heading locked to the BOTTOM of its room
  (lock_folded_to_bottom, now idempotent per container and folder).
  THE BODY'S OWN VISIBILITY STAYS ITS OWNER'S: the fold hides an inner
  holder, never the body, so unfolding can never show a panel its owner hid
  or build one that is built on first show (items 284/380). The other way
  round, the section follows its body: while the owner keeps the body hidden
  the whole section is hidden with it, read from the hidden flag alone --
  nothing is shown, measured or built to find out.
* CollapsibleSplitter.add_section(widget, "Name", ...) -- the same section as
  a pane, so it also trades room with its neighbours by dragging, and a drag
  past its minimum folds it. Sizes are remembered per pane NAME under the
  splitter's persist_key, exactly as in slice B; only a fold the USER makes
  is stored.
* fold_card(card, persist_key=...) -- the same fold on a titled Card.
* add_pane(..., hint="...") -- an EDGE pane can carry its own sentence in the
  handle's tooltip (the image-UMAP divider keeps "the points do not move").
* A splitter that has never been shown no longer shares out a size it does
  not have: the first show lays it out from the remembered sizes.
* The heading wears QLabel#FoldHeading (theme.py): the body font, 600.

WHICH CONTROL WHERE. A section with a name of its own gets a HEADING (click
it to fold; folded, it is a strip at the bottom of its room). A side column
with no name of its own -- the settings column in slice B, Gate Editor's
Filter/Search tabs, the figure queue's thumbnail strip, image UMAP's sidebar,
Regression's results beside its figure pages, Plaque's well picture -- is an
EDGE pane: the handle beside it carries the arrow, a click folds or restores
it and a drag resizes it. Both are the same splitter.

THE INVENTORY. One row per figure, table or section per module: whether it
now FOLDS, whether it now DRAGS (an edge it shares with a neighbour), and
how. HOW is one of
  section   a FoldSection registered as a splitter pane (add_section): it
            folds by its heading and trades room with its neighbours
  fold only a FoldSection with no sibling to trade with
  EDGE      an EDGE pane: the handle beside it carries the arrow
  card      fold_card on a titled Card
  already   a fold that existed before this slice (item 417's settings
            categories, the fold strips, CollapsibleSection, the figure
            grid's own section headers, the console's topic sections)
  pane      a plain splitter pane: drags, never folds
  no [x]    not folded, for the reason in the note below the block

THE PREVIEWS AND THE FIGURES (the maintainer's own list)

  module                element                      fold drag how
  --------------------------------------------------------------------------
  live_preview (Mask)   image-set table              yes  yes  section
                        image area (both canvases)   yes  yes  section
                        pixel and compare strip      yes  yes  section
                        pick row, action row         no   no   no [a]
  measure_preview       crop grid                    yes  no   already [b]
  plaque_preview        pictures (image tabs)        yes  yes  section
                        well picture beside them     yes  yes  EDGE
                        wells / plaques tables       yes  yes  section [c]
                        legend prompt, notices       no   no   no [a]
  timelapse_preview     settings groups              yes  yes  section
                        movie and scrub              yes  yes  section
                        track-quality read-out       yes  yes  section
  motility_preview      settings groups              yes  yes  section
                        plot                         yes  yes  section
                        summary                      yes  yes  section
  umap_explorer         sidebar (crop, form, status) yes  yes  EDGE [d]
                        the chart                    yes  yes  already [b]
  umap_search_viewer    the 2D/3D view               yes  yes  section [e]
  annotation_umap_tab   purity plot                  yes  yes  section
                        score table                  yes  yes  section
                        report                       yes  yes  section
  figure_queue          thumbnail strip              yes  yes  EDGE [f]
                        the figure itself            yes  yes  already [b]
  figure_grid_view      per-run figure sections      yes  no   already
  figure_grid           the search grid              yes  yes  section [e]
  regression_results    volcano plot                 yes  yes  section
                        coefficient table            yes  yes  section
                        guide-agreement plot         yes  yes  section
                        guide support table          yes  yes  section
                        the other diagnostic tabs    no   no   no [g]
  cell_montage_view     crops                        yes  yes  section
                        caption                      yes  yes  section
  gene_panel            gene record                  yes  yes  section
                        what is known                yes  yes  section
  app_screen/Regression results beside the figures   yes  yes  EDGE
                        gene tile under the volcano  yes  yes  EDGE [h]
                        a second run opened beside   no   yes  pane

  * [a] a toolbar, a picker row or a prompt that belongs to the picture
    beside it: folding it hides the controls that drive the figure and frees
    one line.
  * [b] the panel's own card title is the fold; a second heading over the
    same thing would be two controls for one fold.
  * [c] hidden with its tabs in Plaque mode, so there is no empty heading.
  * [d] the display settings' sidebar width is kept for when it opens again
    rather than forcing it open.
  * [e] no sections of its own; folded where Hyperparameter search mounts it.
  * [f] its 160 px fixed width became a draggable, remembered one.
  * [g] one plot per tab: the tab bar already hides all but one, and folding
    a tab's only child leaves an empty tab.
  * [h] it still starts shut, as it did -- but the strip that opens it is
    now always there.

THE SCREENS, A TO F

  module                element                      fold drag how
  --------------------------------------------------------------------------
  activation            (no UI of its own)           --   --   [i]
  agreement             annotation-column list       yes  yes  section
                        pairwise-kappa table         yes  yes  section
                        confusion matrix             yes  yes  section
                        disagreeing rows             yes  yes  section
                        crop preview                 yes  yes  section
                        top block / review block     no   yes  pane
  align                 tile-layout figure           yes  yes  section
                        plan report                  yes  yes  section
  annotate              crop grid                    yes  yes  section
                        Console + AI side panel      yes  yes  section [j]
                        legend, toolbar, switches    no   no   no [a]
                        report / settings dialogs    no   no   no [k]
  batch                 job queue                    yes  yes  section
                        validation problems          yes  yes  section [l]
                        job log                      yes  yes  section
  classifier_evaluation the six result tabs          yes  no   fold only
                        confusion table              yes  yes  section
                        error inspector              yes  yes  section
  classify              fold strip, categories       yes  no   already
  control_chart         chart canvas                 yes  yes  section
                        report                       yes  yes  section
                        rule violations              yes  yes  section
                        left control column          no   yes  pane
  convert               conversion plan table        yes  yes  section
                        run summary                  yes  yes  section [l]
  curate                layer viewer                 yes  yes  section
                        Brush / Tracks tools         yes  yes  section
  data_manager          Usage/Prune/Archive tabs     yes  no   fold only
                        "can be deleted" table       yes  yes  section
                        kept table                   yes  yes  section
                        delete confirmation          no   no   no [k]
  db_browser            table list                   yes  yes  section
                        table preview and paging     yes  yes  section
                        filter row, SQL line         no   no   no [a]
  distributed_jobs      job table                    yes  yes  section
                        details and log              yes  yes  section
                        "Submission" card            no   no   no [a]
  dose_response         curve canvas                 yes  yes  section
                        fit table                    yes  yes  section
                        report                       yes  yes  section
  embeddings            crop preview stack           yes  no   fold only
  experiment_design     condition table              yes  yes  section [l]
                        plate map                    yes  yes  section
                        findings                     yes  no   fold only [m]
  feature_explorer      ranking table                yes  yes  section
                        distribution figure          yes  yes  section
                        Filter / Columns side tabs   yes  yes  section
  foreign               column mapping               yes  yes  section
                        import report                yes  yes  section [l]

  * [i] it builds the generic settings screen; the shell's own panes are
    slice B's.
  * [j] it stays hidden behind its switch, folding never shows it, and its
    heading actions (Copy console, File as issue) stay reachable while
    folded. It also reopens at the height it was dragged to.
  * [k] a dialog: nothing inside it takes the room a fold would free.
  * [l] a fixed maximum height became a draggable, remembered size, so the
    handle beside it does something (Batch 140 px, Convert 140 px, Foreign
    190 px, Experiment Design's table 220 px).
  * [m] kept out of the splitter so a remembered size can never clip a list
    that has grown since.

THE SCREENS, G TO P

  module                element                      fold drag how
  --------------------------------------------------------------------------
  gate_editor           graph canvas                 yes  yes  section
                        gate table                   yes  yes  section
                        Filter / Search side tabs    yes  yes  EDGE [n]
                        gate console                 yes  yes  EDGE [o]
                        Filter / Columns forms       no   no   no [a]
  graph_builder         column shelf                 yes  yes  section
                        graph canvas                 yes  yes  section
                        local data filter            yes  yes  section
  pca                   feature shelf                yes  yes  section
                        scree plot                   yes  no   fold only
                        scores canvas and report     yes  yes  section
                        filter                       yes  yes  section
  outliers              result tabs                  yes  yes  section
                        scan column                  yes  yes  section
  model_compare         parameters table             yes  yes  section
                        per-field table and summary  yes  yes  section
                        mask previews A / B          yes  yes  section [p]
  model_explanation     Explain-CV result tabs       yes  no   fold only
                        Investigate-hit tabs         yes  no   fold only
  map_barcodes          findings table               yes  yes  section
                        reads view                   yes  yes  section
                        proposal notes               yes  yes  section
                        "Find barcodes" card         yes  yes  card [q]
                        fold strip, categories       yes  no   already
  parameter_sweep       settings column              yes  yes  EDGE
                        trials table                 yes  yes  section
                        regression results           yes  yes  section
                        figure queue                 yes  yes  section [r]
  hyperparam            trial table                  yes  yes  section
                        search preview               yes  yes  section
                        search settings dialog       no   no   no [k]
  image_scatter         point cloud                  yes  yes  section
                        crop preview column          yes  yes  section
  lineage               object tree                  yes  yes  section
                        unattached children          yes  yes  section
  model_zoo             model table                  yes  no   fold only
                        benchmark table              yes  yes  section
                        mask preview                 yes  yes  section
                        detail read-out              no   no   no [a]
  measure_inputs        "the files" table card       yes  yes  card
                        settings scroll area         yes  yes  already
  methods_export        draft tabs                   yes  no   fold only
  hit_list              hit table                    yes  no   fold only
                        filter strip                 no   no   no [a]
  image_import          proposed-layout table        yes  yes  section
                        questions table              yes  yes  section
                        scan report                  yes  yes  section
  import_workbench      file table                   yes  yes  section
                        folder preview               yes  yes  section
  image_umap            (fold-strip builders only)   yes  no   already [s]
  make_masks, mask,     settings categories          yes  yes  already [t]
  measure

  * [n] an EDGE pane rather than a heading: a heading above the tab strip
    would break the one-row layout the maintainer asked for, and
    align_side_panel still holds (a test pins both).
  * [o] it starts collapsed, as it did at width 0 -- now with a strip that
    opens it.
  * [p] the two sides drag apart inside it and carry their own captions.
  * [q] the card's 560 px floor is dropped while it is folded, so the fold
    frees real room; the card still stays hidden behind its toggle.
  * [r] it stays hidden until its owner shows it.
  * [s] its views are Image Scatter's and PCA's, both above.
  * [t] item 417's categories and the fold strips; the settings column
    itself is slice B's EDGE pane.

THE SCREENS, P TO Z

  module                element                      fold drag how
  --------------------------------------------------------------------------
  pipeline_graph        the graph                    yes  yes  section
                        step details                 yes  yes  section
  plate_view            plate map                    yes  yes  section
                        edge-effect report           yes  yes  section
  power                 power vs cells per well      yes  yes  section
                        power vs wells               yes  yes  section
                        power table                  yes  yes  section
                        caveats                      yes  no   fold only
                        design form                  no   yes  pane [a]
  profiler              inputs                       yes  yes  section
                        timing curve                 yes  yes  section
                        held values                  no   yes  pane [a]
  project_browser       project table                yes  yes  section
                        project details              yes  yes  section
                        search-root list, toolbar    no   no   no [a]
  qc_dashboard          every QC card                yes  no   fold only [u]
                        source row, verdict line     no   no   no [a]
  queue                 queue table                  yes  no   fold only
  regression (screen)   fold strip and its pages     yes  no   already [v]
  report                sections found               yes  no   fold only
  run_compare           comparison tabs              yes  no   fold only
  run_history           runs table                   yes  yes  section
                        run details                  yes  yes  section
  tabulate              pivot                        yes  yes  section
                        graph                        yes  yes  section
                        filter                       yes  yes  section
  pivot_builder         field shelf                  yes  yes  section
                        pivot table                  yes  yes  section
  train_cellpose        its two module tabs          no   no   no [w]
  train_compare         runs list                    yes  yes  section
                        training curves              yes  yes  section
                        settings diff                yes  yes  section
  trellis               channel shelf                yes  yes  section
                        trellis canvas               yes  yes  section
                        filter / columns side        yes  yes  section
  volcano               volcano plot                 yes  yes  section
                        selected point               yes  yes  section
                        style controls (56 fields)   yes  yes  already [x]

  * [u] each card folds to its heading where it stands in the column, its
    verdict chip kept in the heading, so a folded card still says pass or
    fail. The cards are a scroll column: there is no sibling to trade width
    with.
  * [v] the results themselves are the figures panel's, above.
  * [w] each of its two tabs IS a module screen with slice B's own shell
    folds; a fold here would hide a whole module.
  * [x] its categories are widgets/section.py Sections and already fold; the
    column itself now drags.

WHAT IS NOT COLLAPSIBLE, AND WHY ("wherever possible")

* FORMS AND TOOLBARS. A row of buttons, a picker row, a filter strip, a
  settings form, a one-line status or hint: folding these hides the controls
  that drive the figure beside them and frees a line of height. They keep
  their old behaviour; where such a column shares an edge it is still a plain
  pane, so it drags.
* DIALOGS. Gate settings, crop settings, aggregation rules, the UMAP search
  settings, the plaque overlay, the delete confirmation: a dialog is sized to
  its content and nothing inside it takes the room a fold would free.
* A CONTAINER'S ONLY CHILD. Where a screen's whole body is one tabbed panel
  or one grid, the section folds but does not drag -- there is no sibling to
  trade with -- and where that one child is already inside a titled card, the
  card's own title is the fold (Measure's crop grid, the figure queue's
  picture): a second heading over the same thing would be two controls for
  one fold.
* ONE FIGURE PER TAB. Regression's diagnostic tabs (Effect rank, p-values,
  Q-Q, Controls, Residuals, Influence, Summary, ...) hold a single plot each.
  The tab bar already hides them all but one, and folding a tab's only child
  leaves an empty tab.
* A GROWING LIST THAT MUST NOT BE CLIPPED. Experiment Design's findings folds
  but stays out of its splitter, so a remembered size can never cut a list
  that grew since it was stored.

TESTS. One file per family, all offscreen and capped:
  tests/qt/test_every_section_folds_to_the_bottom.py        (the mechanism)
  tests/qt/test_471c_previews_sections_fold.py              (the previews)
  tests/qt/test_471c_figures_sections_fold.py               (figures, results)
  tests/qt/test_471c_screens_a_to_f_sections_fold.py        (16 screens)
  tests/qt/test_471c_screens_g_to_p_sections_fold.py        (18 screens)
  tests/qt/test_471c_screens_p_to_z_sections_fold.py        (16 screens)
Each family asserts the same three things the maintainer asked for: every
named figure/table folds by its heading, a folded heading is at the BOTTOM of
its room (and folding a whole column stacks the headings there), and a size
the user dragged comes back when the screen is rebuilt. Each family also
pins the 284/380 rule: a hidden or deferred panel -- Mask's live preview,
Regression's results, Annotate's console, the sweep's figure queue, Map
Barcodes' search card -- is neither built nor shown by any of the folding.
Existing tests updated where a splitter child is now a section or an EDGE
pane: test_preview_source_selectors, test_annotation_umap_plot,
test_umap_figure_container_drag, test_the_umap_divider_moves (by the new
handle tooltip), test_gate_editing.
Pre-existing failures on nightly, unchanged by this slice and confirmed on
the commit before it: the four caption ratchets in
tests/qt/test_i18n_caption_ratchet.py, the four docstring-inventory ratchets
in tests/test_docstring_correctness.py, tests/test_api_i18n_extractor.py (2),
tests/test_documentation_i18n.py (4), test_import_test_data_chooser (2, no
pylibCZIrw here). The catalog and docstring lanes own those.

SCREENSHOTS (offscreen, 1366x768, scratchpad c471shots/): Mask with the live
preview open and then with its table, its pixel strip and its images folded
(the three headings stacked at the bottom of the preview); Plaque in Figure
mode and with its pictures folded; Regression's results beside its figure
pages and then collapsed to the left; the results panel with the volcano
folded; image UMAP with its sidebar open and collapsed; and, in a real
window, Gate Editor, Tabulate, Model Compare, Power, Annotate and Database
Browser, each open and with every section folded.

NEW CAPTIONS for the catalog lane (not translated here). Every one is a
section heading or a pane name; the Folder translates the name it is given,
so they are English in the source. The two new tooltip lines are
"Click to hide {name}." and "Click to show {name} again." (an EDGE pane that
carries its own hint), beside slice B's two.

  the previews and the figures: "Image sets", "Images", "Pixel info",
    "Pictures", "Well", "Wells and plaques", "Preview settings", "Movie",
    "Track quality", "Plot", "Summary", "Sidebar", "Chart", "Purity plot",
    "Scores", "Report", "Results", "Figure list", "Figure", "Figure pages",
    "Volcano plot", "Coefficient table", "Model", "Guide agreement",
    "Guide support table", "Cells", "Caption", "Gene record",
    "Known about this gene", "Gene".
  screens A-F: "Annotation columns", "Pairwise agreement",
    "Confusion matrix", "Disagreeing rows", "Crop preview", "Agreement",
    "Review", "Tile layout", "Plan report", "Crops", "Console + AI",
    "Job queue", "Validation problems", "Job log", "Evaluation results",
    "Confusion table", "Error inspector", "Control chart",
    "Rule violations", "Controls", "Conversion plan", "Image",
    "Curation tools", "Project data", "Can be deleted", "Kept, and why",
    "Tables", "Table preview", "Jobs", "Job details and log",
    "Dose-response curves", "Fit results", "Conditions", "Plate map",
    "Findings", "Feature ranking", "Distributions", "Filter and columns",
    "Explorer", "Column mapping", "Import report", "Output".
  screens G-P: "Graph", "Gate table", "Filter and search", "Console",
    "Columns", "Filter", "Features", "Scree plot", "Scan", "Mask previews",
    "Model A", "Model B", "Per-field comparison",
    "Parameters that reached each model", "Reads", "Proposal", "Trials",
    "Regression results", "Figures", "Sweep settings", "Search preview",
    "Point cloud", "Crop", "Objects", "Unattached children", "Models",
    "Benchmark", "Mask preview", "Draft", "Hits", "Proposed layout",
    "Questions", "Files", "Folders", "The files".
  screens P-Z: "Pipeline graph", "Details", "Edge-effect report",
    "Power vs cells per well", "Power vs wells", "Power table", "Caveats",
    "Inputs", "Curve", "Projects", "Project details", "Queue",
    "Sections found in this folder:", "Comparison", "Runs", "Run details",
    "Pivot", "Pivot fields", "Pivot table", "Curves", "Settings diff",
    "Channels", "Trellis", "Selected point", plus each QC card's own title,
    which was already a translated string.

WHAT A HEADING COSTS. Every section adds one heading row to its screen's
minimum height. Measured at 1366 px wide, before and after: Tabulate
1085 -> 1144, Power 817 -> 889, Train Compare 562 -> 602, Profiler
584 -> 601, Volcano 514 -> 552, Run History 379 -> 419. Tabulate and Power
were already taller than a 768 px screen before this slice; folding is now
the way down from that, but the heading row is what it costs.

THE OPEN QUESTION FROM SLICE B IS STILL OPEN. The Live / 3D / Time / OPS / AI
switches stay in the Run row inside the folded "Actions" strip. Slice C did
not move them: where that switch lives is the maintainer's call.

DONE WHEN, checked

* "Every figure and table in every module can be collapsed" -- yes, with the
  exceptions listed above (forms, toolbars, dialogs, and a tab that holds one
  plot), each named in the inventory with its reason.
* "A collapsed element sits at the bottom of its container, the console
  included" -- yes: every section uses lock_folded_to_bottom, and the tests
  measure the folded heading's position rather than trusting it.
* The two shell lines (a preview or figures collapsing console/System/the
  buttons/the settings, and each expandable again) were slice B's and still
  hold, re-run here.
* The GUI-scale line is slice A's.
* The DRAG AN EDGE addendum: the shell's containers were slice B; inside the
  screens, every section that shares an edge with another now drags, with the
  fold as the limit of shrinking.
So 471's DONE WHEN is met. What is left is not a missing fold but taste: a
handful of one-figure tabs and forms that are deliberately not foldable, and
slice B's open question about the Live switch.

2026-09-22 — Regression Figures overlap repaired
The handoff's known Regression exception is now fixed. At1366x768 the
Figures widget enforced560px while Console began at469px, painting91px
beyond its allocation. Regression's body now has a frameless scroll area;
its contents retain their natural minimum while the outer card can shrink.
The fold target is the scroll area, so folding hides the whole body; lazy
results construction and the missing-dependency figure-queue fallback remain.
Measured actual MainWindow after repair: Figures463px, Console begins469px,
System496px, Actions558px within579px runtime area. New real-window checks
cover1366x600 and1366x768, no sibling overlap, scrolling to both ends,
reopening Console, folding/reopening Figures and deferred construction.
Receipt: features/data/471_regression_figures_fit_2026-09-22.json.

2026-09-23 — EXPERIMENT DESIGN CONDITION TABLE AT LARGE FONTS
The tutorial lane's native4K capture exposed clipped Replicates/Role columns.
Reproduced at100/150/200percent font: columns stayed100px while the Replicates
header wanted113/159/198px. Content sizing alone fixed headers but Qt's table
item padding still removed16px width and8px height from embedded role boxes.
Columns1/2 and rows now size to contents; a role-column delegate uses the whole
cell, retaining each dropdown's own padding. Condition names retain stretch.
At150percent the role box is199x41px against a199x40hint; the rendered table
shows complete headers and role values.39checks pass18.39s including live font
changes, added conditions, unchanged values and existing design/export behavior.

A separate OPS action-row overlap reported in the capture did NOT reproduce
in a settled actual3840x2160MainWindow at150percent font. Its buttons fit and
have disjoint rectangles inside their row. No AppScreen change or claim of
resolving the native-capture observation. No cross-platform acceptance claim.
Receipt:471_experiment_design_readable_conditions_2026-09-23.json.
