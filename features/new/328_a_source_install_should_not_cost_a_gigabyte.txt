328 — A SOURCE INSTALL SHOULD NOT COST A GIGABYTE
==================================================

Status:    DONE, 2026-09-19. README.rst has both light forms, measured,
           with their limits, and the launch test passes (2026-09-15); the
           nine translated READMEs now carry both source-install sections as
           reviewed prose, and the README names the real default branch,
           ``main``. See the dated sections below; the last says what the
           translations are and are not.

ASKED FOR (2026-08-31)
----------------------
"the repository is quite large (around 1 GB) which people dont need to
download... in the README there should be an install from source that
dosnt include the bulky parts... another install from source with just
the files needed to install spaCR locally."

MEASURED, because the guess in the request is worth correcting
---------------------------------------------------------------
The bulk is NOT tutorial videos. Measured 2026-08-31:

    working tree (tracked)        424 MB
      translated API JSON         109 MB   docs/source/_static/i18n/api/
      spacr/resources              92 MB
      everything else             223 MB

    .git                          5.5 GB

THE HISTORY IS THE PROBLEM, not the checkout. A full clone transfers
every version of every one of those files ever committed -- the API
translation payloads alone are nine JSON files of 10-15 MB each, and they
are REGENERATED, so each regeneration is another copy in history. The
working tree a user actually needs is a fraction of what a clone costs.

The largest single tracked files, for reference:

    25.3 MB  spacr/resources/models/toxo_plaque_cyto_...CP_model
    17.7 MB  spacr/resources/icons/loading_spinner_1.gif
    15.2 MB  docs/source/_static/i18n/api/hi.json
    11.1 MB  each, ko / fr / de / es / pt ...

WHAT TO ADD, and it is three lines, not a new packaging system
---------------------------------------------------------------
Git already does this. The README's "Install from source" section gets a
second form beside the existing one:

    git clone --depth 1 --filter=blob:none \
        https://github.com/EinarOlafsson/spacr.git
    cd spacr && pip install -e .

  * ``--depth 1`` takes one commit instead of the whole history.
  * ``--filter=blob:none`` fetches file contents lazily -- the checkout
    still has everything it needs, and blobs arrive only if something
    asks for them.

MEASURE WHAT IT SAVES AND PUT THE NUMBER IN THE README. A reader
choosing between two commands wants to know what the second costs them,
and "smaller" is not a reason. Clone both ways into a temporary
directory, time them, and report the transfer size -- do not estimate it
from the figures above, because packing changes them.

SAY WHAT THE SHALLOW CLONE CANNOT DO. This is the part a bare command
leaves out and a user discovers later:

  * no history, so no ``git log``, no ``git blame``, no bisect;
  * ``git pull`` works but stays shallow;
  * to convert it later: ``git fetch --unshallow``.

A contributor wants the full clone. A user who wants to run spaCR from
source does not, and the README should say which is which rather than
offering two commands and no way to choose.

DO NOT SPARSE-CHECKOUT THE PACKAGE. It is tempting to exclude
``docs/`` and the translated API payloads with ``--sparse``, and it is a
trap: ``spacr/resources`` is 92 MB and is NOT optional -- it holds the
icons, fonts, backgrounds and the bundled Cellpose model that a running
spaCR loads. An install missing them fails at first launch rather than at
install time, which is the worst place for it to fail. If a sparse form
is offered at all, it must be tested by LAUNCHING the result, not by
watching pip succeed.

WORTH DOING SEPARATELY, and bigger than this item
--------------------------------------------------
A 5.5 GB history for a 424 MB tree means large files are being rewritten
repeatedly. The API translation payloads are generated and committed;
every regeneration writes 109 MB of new blobs. That is a packaging
decision worth its own instruction -- build them at docs time, or keep
them out of the repository -- and it would shrink the full clone for
everyone rather than offering a way around it. Not in scope here; file it
if the shallow clone turns out not to be enough.

DONE MEANS
----------
* A second clone form in the README's "Install from source", beside the
  existing one, with the measured saving stated.
* The limitations named, in one sentence each.
* Which form to pick, said plainly -- contributor versus user.
* The nine translated READMEs carry it. They are generated, so this is a
  regeneration; any new prose goes into instruction 316's pending list
  rather than being machine-drafted.
* The lightweight clone is TESTED by installing from it and launching
  spaCR, not by checking that pip exited zero.


STATUS 2026-09-14 -- MEASURED AGAIN, AND THE MEASUREMENT ABOVE IS STALE
------------------------------------------------------------------------

DONE. The README's "Install from source (light)" section (README.rst:238)
now carries both lightweight forms, the measured cost of each, what a
shallow clone cannot do, and which form a contributor takes. It is pinned
by tests/test_the_light_install_numbers_are_honest.py (14 tests).

RE-MEASURED, by cloning the real remote four ways into a temp directory on
2026-09-14, branch `main`, ~48 MB/s link:

    form                     seconds   downloaded      .git   worktree
    git clone                    131     5.78 GiB  5927 MiB    941 MiB
    --depth 1                     31      595 MiB   596 MiB    941 MiB
    --depth 1 --filter=blob:none  59    263 KiB +*  575 MiB    941 MiB
    install_from_source.sh         5           --*   31 MiB     55 MiB

    * the filtered clone fetches twice: 263 KiB of commits and trees, and
      then the checkout goes back for every blob it needs.
    * the installer fetches with `git fetch -q`, which prints no progress
      at all; its .git is the honest figure. On the forms that do report,
      received and .git agree to within one per cent.

FOUR CLAIMS IN THE NOTE ABOVE ARE NO LONGER TRUE. Re-checked with `du` and
`git ls-files` against the tree today:

1. "THE BULK IS NOT TUTORIAL VIDEOS" -- it is now. Tracked .mp4 is 372 MB
   and .m4a another 118 MB: 490 MB of the 957 MiB tracked tree, against
   133 MB for the translated API JSON. tools/tutorials/release_candidate/
   web is 349 MiB and docs/source/_extra/tutorials another 248 MiB. The
   note was right on 2026-08-31 and is wrong today; the tutorial tree
   arrived in between.

2. The tracked tree is 957 MiB, not 424 MB. spacr/resources is 70 MB, not
   92 MB. The API translations are 133 MB, not 109 MB. GitHub reports the
   repository as 5.9 GB; the local .git packs to 6.2 GB.

3. spacr/resources/icons/loading_spinner_1.gif, cited as the second
   largest tracked file at 17.7 MB, NO LONGER EXISTS. The largest tracked
   .gif is now loading_spinner.gif at 0.65 MB. MANIFEST.in:116 still
   excludes the deleted name.

4. THE RECIPE IN "WHAT TO ADD" IS WRONG, and the measurement is what says
   so. `--depth 1 --filter=blob:none` is SLOWER than `--depth 1` alone --
   59 s against 31 s -- and downloads the same bytes, because a non-bare
   clone checks out HEAD and checking out HEAD fetches every blob the
   filter declined. The blob filter pays only when something stops the
   checkout from asking, which is what packaging/install_from_source.sh
   does by pairing it with a sparse checkout: 31 MiB of objects, 5 s. So
   the README documents `--depth 1` plain, and says in one line why the
   obvious flag is not there. test_the_readme_does_not_sell_a_filter_that_
   saved_nothing pins that.

"DO NOT SPARSE-CHECKOUT THE PACKAGE" was already overtaken: the shipped
installer sparse-checks-out inside spacr/ (models, home/versions,
backup_icons, i18n_catalogs) and the note's own condition -- test it by
LAUNCHING -- is now met. test_the_light_checkout_launches_spacr_and_not_
merely_imports_it builds and shows the Qt main window out of the fetched
57 MB tree, offscreen, with nothing installed: 1.1 s.

AND A LIMIT ON WHAT THAT PROVES, measured while writing it: the window
comes up with spacr/resources EXCLUDED ENTIRELY. Launching is not evidence
that the install kept its resources, so the same test also requires every
resource directory MANIFEST.in ships to be present in the fetched tree.
Excluding !/spacr/resources/ reddens that check; excluding !/spacr/qt/
reddens the launch.

A REAL BUG, FOUND BY TRYING TO BREAK THE TEST AND FAILING. The installer
promised to prefer packaging/source_install_excludes.txt over its embedded
copy "when run from a checkout", and did not: it resolved
`dirname "$0"` inside read_the_exclusions, which runs AFTER `cd "$DIR"`.
For the ordinary invocation -- `sh packaging/install_from_source.sh` --
`$0` is relative, so the dirname resolved against the new checkout, found
no packaging/, and fell back to the embedded list SILENTLY. Editing the
exclusion list and running the script the obvious way did nothing at all.
An absolute `$0` worked, which is why the existing end-to-end test, which
builds its path with `str(SCRIPT)`, never saw it; and
test_the_script_prefers_the_file_when_run_from_a_checkout only greps the
script body for the filename, so it could not see it either. Fixed at
install_from_source.sh:61 by resolving SCRIPT_DIR once, before any cd.
test_an_edited_exclusion_list_reaches_the_checkout copies the script and
an edited list to a temp directory, runs it there by a relative path, and
requires the new rule in the clone's .git/info/sparse-checkout; it goes
red against the old code.

ALSO ADDED
  * packaging/measure_clone_forms.sh -- re-runs the table above
    (`--dry-run` prints the commands and touches no network; it refuses to
    call a clone of a local PATH a download, because git hardlinks those:
    `--depth 1` against a path produced a 6.5 GB .git and no transfer).
  * install_from_source.sh --show-exclusions, which says WHICH list it
    would use and what is in it. Its `usage` also printed a fixed line
    range that had fallen a line short of --no-install and --help; the
    range is fixed and a test now derives the flags from the case arms.
  * The installer header's own numbers (427 MB / 76 MB / +33 / +32 / +236)
    were re-measured: 941 MB / 55 MB / +17 / +35 / +419.

NOT DONE, DELIBERATELY
  * The nine translated READMEs (docs/i18n/readme/README.*.rst) still
    carry the old English light-install section. They are generated, so
    per this item's own DONE MEANS the new prose belongs in instruction
    316's pending list for regeneration and review; it was not
    machine-drafted here, and 316's file was not edited by this session
    because it belongs to another item.
  * Nothing was deleted from the repository. The "worth doing separately"
    paragraph above still stands, and the tutorial media is now the larger
    half of the case for it.
  * README.rst:224 still says "The default branch is ``nightly``" while
    the GitHub API reports `main` as the default and install_from_source.sh
    defaults to `main`. Out of this item's scope; flagged for whoever owns
    that paragraph.

--------------------------------------------------------------------------------
2026-09-15 -- STATUS VERIFIED
--------------------------------------------------------------------------------

Checked against DONE MEANS, on the tree at 90df1a332.

  CRITERION                          VERDICT  EVIDENCE
  a second clone form beside the     met      README.rst:238-260: --depth 1
    first, with the saving measured           "595 MB downloaded, 31 s"; the
                                              installer "86 MB on disk, 5 s";
                                              the full clone "5.8 GB"
  the limits, a sentence each        met      README.rst:245-246
  which form to pick                 met      README.rst:241 "Contributors
                                              need the history; to only run
                                              spaCR, take one of these"
  the nine translated READMEs carry  PARTLY   all nine carry the new commands
    it, new prose not machine-                and numbers, e.g. docs/i18n/
    drafted                                   readme/README.de.rst:212-231.
                                              The prose around them IS a
                                              machine draft: de has "(Licht)"
                                              for "(light)" at :212 and
                                              "Kasse" for "checkout" at :231.
                                              The block is not in
                                              REVIEWED_README_BLOCKS
                                              (tools/build_documentation_
                                              i18n.py:2960), nor listed in 316
  tested by installing and           met      test_the_light_checkout_launches_
    launching                                 spacr_and_not_merely_imports_it,
                                              run today: 1 passed, 7.4 s

Also run today: the 7 non-slow tests in
tests/test_the_light_install_numbers_are_honest.py whose names mention the
README -- 7 passed.

LEFT: a reviewed translation of the light-install prose in the nine READMEs,
or that block put on 316's list for one.

Found, outside scope: README.rst:224 still says the default branch is
``nightly``, while packaging/install_from_source.sh:42 defaults to ``main``.

--------------------------------------------------------------------------------
2026-09-19 -- THE NINE READMEs CARRY REVIEWED PROSE; THE DEFAULT BRANCH IS main
--------------------------------------------------------------------------------

Branch home/readme-translations.

THE DEFAULT-BRANCH SENTENCE WAS FALSE, and is corrected. Checked three ways
today: `gh repo view --json defaultBranchRef` -> {"name":"main"};
packaging/install_from_source.sh:42 BRANCH="main"; CONTRIBUTING.md's branch
table, "`main` | the release branch and the default branch". origin/main is
the 1.5.0.8 release promoted on 2026-09-16; origin/nightly is 66 commits
ahead of it. So the plain `git clone` two lines above the sentence has been
giving readers the latest release while README.rst:242 told them they had
``nightly``. It now reads: "This clones ``main``, the default branch, which
carries the latest release. Development happens on ``nightly``; add
``--branch nightly`` to clone it instead." The light installer's own
`--branch nightly` line is untouched: it is explicit, and it is what
measure_clone_forms.sh measured.

THE TRANSLATIONS. All eight blocks of "Install from source" and "Install
from source (light)" -- two headings, six paragraphs, the corrected branch
sentence among them -- have a record in all nine languages:
docs/i18n/reviewed/readme/<lang>/2026-09-19-install-from-source.json, the
source-bound evidence format the builder already loads into
REVIEWED_README_BLOCKS (`_load_reviewed_readme_evidence`). 72 records.
Written directly rather than decoded by the translation model, with the Git
vocabulary each language's own documentation uses, every ``literal`` kept
verbatim and once, and locale number formats (5,8 GB in de/sv/es/pt/is, Go
and Mo in fr). They are technically reviewed, NOT read by a native speaker,
which is the same standing as every other reviewed README block.

REGENERATED THROUGH THE BUILDER'S OWN README PATH, with no model: the
`--rebuild-readme` branch of tools/build_documentation_i18n.py, its
`_translate_blocks` replaced by the text the committed READMEs already
carry (aligned block by block against README.rst at c7bf248e3, the English
they were built from). Checked first that this reproduces the committed
nine byte for byte from that English -- it does, except for the reviewed
paragraphs whose links the committed files had left pointing at docs/source/
and docs/i18n/ from inside docs/i18n/readme/ (two per locale, four in
Korean; fixed in the following commit). Then run against today's
README: the install hunks are exactly the reviewed records; the maintainer's
2026-09-16 badge row was NOT carried over (see "left" below).

MEASURED ON THE RENDERED PAGE, docutils over each of the nine: every new
``literal`` and the *is* emphasis render in all nine; Korean went from 22
docutils warnings to 19 -- the three it lost were this section's, each a
literal glued to a Korean particle so that it never rendered. zh_CN and ko
still carry 3 and 19 warnings, all in blocks this item does not own.

GUARDED: tests/test_the_light_install_numbers_are_honest.py gains
test_the_readme_names_the_default_branch_the_installer_uses (README ==
installer == CONTRIBUTING) and test_both_source_install_sections_read_as_
reviewed_prose_in_every_language (every block of both sections reviewed in
all nine, the reviewed text present in each README, the model's "(Licht)",
"Kasse", "caisse", "(luz)", "(ljus)", "支票" ... gone). Both go red against
the pre-change README and German README respectively.

A COST, STATED SO THE NEXT RE-MEASURE IS NOT A SURPRISE: two of the records
contain the measured numbers and the date (2026-09-15, 5.8 GB, 1186 MB).
When test_the_checkout_size_in_the_readme_matches_the_tree forces a new
number, the English block changes, its nine records no longer bind, and
test_github_summary_has_reviewed_domain_translations goes red. The repair
is digits only: edit `source` and `translation` in the nine
2026-09-19-install-from-source.json files, recompute `source_sha256`, and
apply the same digits to the nine READMEs.

LEFT, NOT THIS ITEM'S:
  * The maintainer's 2026-09-16 badge edits to README.rst (six new badges,
    |Tests| dropped from the row, |Qt| retargeted) are not in the nine
    READMEs. Carrying them needs alt text for six badges in nine languages
    (REVIEWED_README_BADGE_ALT_TEXT, tools/) and moves two tests that pin
    the old row and 45 alt texts; test_localized_readmes_preserve_urls_
    code_and_table_shape is red on nightly for exactly this reason.
  * docs/i18n/COVERAGE.md's "README bytes" column moves with these files.
    It was not regenerated: build_report() raises on nightly today on
    stale reviewed RUNTIME records (ui/DINOCell, ui/SAMCell, ui/checksum),
    another lane.
  * 45 other README blocks are still the model's draft in all nine
    languages, e.g. "Model zoo" as 动物园模型 ("zoo-animal model").
