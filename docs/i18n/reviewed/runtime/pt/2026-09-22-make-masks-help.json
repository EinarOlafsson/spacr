{
  "schema": 1,
  "language": "pt",
  "records": [
    {
      "table": "ui",
      "key": "Invert the picture and the pixels used for detection, so dark objects become bright. The field is normalized to 0..1, then each pixel becomes 1 minus itself. Hover pixel intensity follows the inversion; object mean intensity and Filter thresholds use the original loaded values. The loaded image data and existing mask are unchanged. To swap foreground and background in a finished mask, use 'Swap object and background' in Object operations.",
      "source": "Invert the picture and the pixels used for detection, so dark objects become bright. The field is normalized to 0..1, then each pixel becomes 1 minus itself. Hover pixel intensity follows the inversion; object mean intensity and Filter thresholds use the original loaded values. The loaded image data and existing mask are unchanged. To swap foreground and background in a finished mask, use 'Swap object and background' in Object operations.",
      "source_sha256": "ed765dd4155a043c1c41c950c183818902c0ed756293b746528907c66bbdd612",
      "translation": "Inverte a imagem e os pixels usados na detecção, tornando claros os objetos escuros. O campo é normalizado para 0..1 e cada pixel passa a ser 1 menos seu valor. A intensidade do pixel sob o cursor acompanha a inversão; a intensidade média dos objetos e os limiares de Filtro usam os valores originais carregados. Os dados de imagem carregados e a máscara existente não mudam. Para trocar o primeiro plano e o fundo de uma máscara pronta, use «Trocar objeto e fundo» ('Swap object and background') em Operações de objectos."
    },
    {
      "table": "ui",
      "key": "Invert image is on: the picture and detection use the INVERTED image. Hover pixel intensity follows the inversion; object mean intensity and Filter thresholds use the original loaded values. The loaded image data and existing mask are unchanged.",
      "source": "Invert image is on: the picture and detection use the INVERTED image. Hover pixel intensity follows the inversion; object mean intensity and Filter thresholds use the original loaded values. The loaded image data and existing mask are unchanged.",
      "source_sha256": "442f48373af356249b2681ce360c695bb1e258d823306192dd1f27ab5390671d",
      "translation": "Inverter imagem está ativado: a visualização e a detecção usam a imagem INVERTIDA. A intensidade do pixel sob o cursor acompanha a inversão; a intensidade média dos objetos e os limiares de Filtro usam os valores originais carregados. Os dados de imagem carregados e a máscara existente não mudam."
    }
  ],
  "superseded_records": [
    {
      "record": {
        "table": "ui",
        "key": "Show the picture as a negative and detect on that same negative: the field is normalized to 0..1 and each pixel becomes 1 minus itself, so its darkest pixel is its brightest. That is what lets Otsu and the Live magnifier, which look for bright objects, take DARK ones — a dark object on a pale brightfield reads the way a fluorescent one does, to the eye and to the detector alike. THE MASKS ARE MADE FROM THE INVERTED IMAGE while this is on, and a warning stays above the image saying so. The hover readout, the Filter category and the mask you save go on reading the image's real values, so nothing you measure changes. This is not 'Swap object and background' in Object operations, which flips a finished MASK and does nothing to the picture.",
        "source": "Show the picture as a negative and detect on that same negative: the field is normalized to 0..1 and each pixel becomes 1 minus itself, so its darkest pixel is its brightest. That is what lets Otsu and the Live magnifier, which look for bright objects, take DARK ones — a dark object on a pale brightfield reads the way a fluorescent one does, to the eye and to the detector alike. THE MASKS ARE MADE FROM THE INVERTED IMAGE while this is on, and a warning stays above the image saying so. The hover readout, the Filter category and the mask you save go on reading the image's real values, so nothing you measure changes. This is not 'Swap object and background' in Object operations, which flips a finished MASK and does nothing to the picture.",
        "source_sha256": "d71fbae27cb6a2958c1a5ab5e73fa5f140525522c63333c78a2186bae8264789",
        "translation": "Mostra a imagem como um negativo e detecta nesse mesmo negativo: o campo é normalizado para 0..1 e cada pixel passa a valer 1 menos seu valor original, de modo que o mais escuro se torna o mais claro. Isso permite que Otsu e a lupa ao vivo, que procuram objetos claros, detectem objetos ESCUROS: um objeto escuro sobre um fundo claro de campo claro passa a se parecer com um objeto fluorescente, tanto para os olhos quanto para o detector. AS MÁSCARAS SÃO CRIADAS A PARTIR DA IMAGEM INVERTIDA enquanto esta opção está ativa, e um aviso acima da imagem informa isso. A leitura sob o ponteiro, a categoria Filtro e a máscara salva continuam usando os valores reais da imagem, portanto as medições não mudam. Isso é diferente de trocar objeto e fundo ('Swap object and background') nas operações de objetos, que inverte uma MÁSCARA pronta sem alterar a imagem."
      },
      "superseded_on": "2026-09-22",
      "reason": "Item 435 corrected inversion help: hover pixels follow inversion, object means and Filter use original loaded values. Current source and target replace this preserved historical record."
    }
  ]
}
