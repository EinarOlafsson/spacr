{
  "schema": 1,
  "language": "pt",
  "records": [
    {
      "label": "spacr.ops_accel#4",
      "source_sha256": "6b7bee0d9d406e5763c5c3576999cd6bad2266f9bc6ba7e1df8b71f4c5ce52ba",
      "source": "AND IT NEVER ASSUMES THE CARD IS FREE. A shared GPU is the common case -- this one also runs structure prediction -- so `ops_gpu=False` refuses a device that exists, and a backend that raises at runtime (an out-of-memory, a driver that went away) falls through to the next rather than failing the plate.",
      "context": "And it never assumes the card is free. A shared GPU is the common case -- this one also runs structure prediction -- so `ops_gpu=False` refuses a device that exists, and a backend that raises at runtime (an out-of-memory, a driver that went away) falls through to the next rather than failing the plate.",
      "translation": "E NUNCA PRESSUPÕE QUE A PLACA GRÁFICA ESTEJA LIVRE. Uma GPU partilhada é o caso comum -- nesta também corre previsão de estrutura -- por isso `ops_gpu=False` recusa um dispositivo que existe, e um backend que falha em tempo de execução (falta de memória, um controlador que desapareceu) passa para o seguinte em vez de fazer falhar a microplaca."
    }
  ]
}
