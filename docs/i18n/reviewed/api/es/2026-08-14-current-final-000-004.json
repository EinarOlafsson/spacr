{
  "schema": 1,
  "language": "es",
  "records": [
    {
      "label": "spacr.power_model.prepare_model_data#7",
      "source_sha256": "1f0b9b2723569a57592215e5ff0d64e6661a4e628f5ac0c266c37042b8b0b595",
      "source": "if the ``gene x well`` grid is incomplete, ``False`` (the default) raises, because a missing pair is ambiguous — zero reads, or not measured?  ``True`` fills the missing pairs with zeros for every count column.",
      "context": "if the ``gene x well`` grid is incomplete, ``False`` (the default) raises, because a missing pair is ambiguous — zero reads, or not measured?  ``True`` fills the missing pairs with zeros for every count column.",
      "translation": "Si la cuadrícula ``gene x well`` está incompleta, ``False`` (el valor predeterminado) genera una excepción, porque un par ausente es ambiguo — ¿cero lecturas o no se midió? ``True`` rellena con ceros los pares ausentes de cada columna de conteo."
    },
    {
      "label": "spacr.power_model.scan_parameters#2",
      "source_sha256": "caaddd5ba419c9c299f816ee984f8812017f22186dc0692869c50cf7f710c631",
      "source": "**A point that fails is reported as failed.**  A fit that raises lands as ``status=\"failed\"`` with the exception text in ``error``; a fit that runs but does not meet its convergence criterion lands as ``status=\"not_converged\"``.  Both carry ``NaN`` metrics.  This is the whole point: a sweep that backfilled 0.5 for its broken points would plot as a design sitting at chance, and \"this design cannot find its hits\" and \"this fit crashed\" are conclusions with opposite consequences.",
      "context": "**A point that fails is reported as failed.**  A fit that raises lands as ``status=\"failed\"`` with the exception text in ``error``; a fit that executes but does not meet its convergence criterion lands as ``status=\"not_converged\"``.  Both carry ``NaN`` metrics.  This is the whole point: a sweep that backfilled 0.5 for its broken points would plot as a design sitting at chance, and \"this design cannot find its hits\" and \"this fit crashed\" are conclusions with opposite consequences.",
      "translation": "**Un punto que falla se registra como fallido.** Un ajuste que genera una excepción termina con ``status=\"failed\"`` y el texto de la excepción en ``error``; un ajuste que se ejecuta pero no satisface su criterio de convergencia termina con ``status=\"not_converged\"``. Ambos llevan métricas ``NaN``. Esa es la finalidad: un barrido que asignara 0.5 a sus puntos defectuosos se representaría como un diseño al nivel del azar, pero \"este diseño no puede encontrar sus aciertos\" y \"this fit crashed\" son conclusiones con consecuencias opuestas."
    },
    {
      "label": "spacr.qt.screens.db_browser#5",
      "source_sha256": "3016617123d996dc0a88daff1456d3be0f5ab3c8c3a6894a37f58d51d01533b6",
      "source": "**Keyset paging, not OFFSET.** Each chunk asks for ``rowid > <last rowid seen>`` and ``ORDER BY rowid``. ``LIMIT ? OFFSET ?`` makes SQLite walk (and throw away) every skipped row, so chunk 500 of a 400 k-row table would cost 500× chunk 1 — the \"fast\" version would get slower the further you scrolled. Only tables with no usable key (views, composite-primary-key ``WITHOUT ROWID`` tables) fall back to ``OFFSET``, and those are small by construction.",
      "context": "**Use keyset paging instead of OFFSET.** Fetch each chunk with ``rowid > <last rowid seen>`` and ``ORDER BY rowid``. ``LIMIT ? OFFSET ?`` makes SQLite scan every skipped row, so chunk 500 costs about 500× as much as chunk 1 and the \"fast\" version gets slower. Small views and composite-key ``WITHOUT ROWID`` tables have no usable row key and use ``OFFSET`` as a fallback.",
      "translation": "**Paginación por clave, no OFFSET.** Cada bloque solicita ``rowid > <last rowid seen>`` y ``ORDER BY rowid``. ``LIMIT ? OFFSET ?`` hace que SQLite recorra (y descarte) cada fila omitida, por lo que el bloque 500 de una tabla de 400 k filas costaría 500× lo que cuesta el bloque 1 — la versión \"fast\" se volvería más lenta cuanto más se avanzara. Solo las tablas sin una clave utilizable (vistas y tablas ``WITHOUT ROWID`` con clave primaria compuesta) recurren a ``OFFSET``, y por construcción son pequeñas."
    },
    {
      "label": "spacr.runctx#2",
      "source_sha256": "5f176ea81ee615430f3dd8f2640b17afe533e2fa86d81c8fbb10e2fb25ac9228",
      "source": "**One run id, on every log line and every output.** A run used to be untraceable. The log said a field failed; the registry said an artifact existed; nothing connected them, so \"show me everything from the run that produced this file\" had no answer. :func:`run_context` mints one id, stamps it onto every :class:`logging.LogRecord` created anywhere in the process, writes a per-run JSONL log that :func:`read_run_log` reads back, and hands the *same* id to :func:`spacr.artifacts.register_run_outputs`. A log line and an output can therefore be joined on ``run_id``, which is the whole point.",
      "context": "**One processing-session id, on every log line and every output.** A processing session used to be untraceable. The log said a field failed; the registry said an artifact existed; nothing connected them, so \"show me everything from the processing session that produced this file\" had no answer. :func:`run_context` mints one id, stamps it onto every :class:`logging.LogRecord` created anywhere in the process, writes a per processing session JSONL log that :func:`read_run_log` reads back, and hands the *same* id to :func:`spacr.artifacts.register_run_outputs`. A log line and an output can therefore be joined on ``run_id``, which is the whole point.",
      "translation": "**Un identificador de sesión de procesamiento en cada línea de registro y en cada salida.** Antes, una sesión de procesamiento no podía rastrearse. El registro indicaba que un campo había fallado; el registro de artefactos indicaba que existía un artefacto; nada los conectaba, de modo que \"muéstrame todo lo correspondiente a la sesión de procesamiento que produjo este archivo\" no tenía respuesta. :func:`run_context` crea un identificador, lo estampa en cada :class:`logging.LogRecord` creado en cualquier parte del proceso, escribe un registro JSONL por sesión de procesamiento que :func:`read_run_log` vuelve a leer y entrega el *mismo* identificador a :func:`spacr.artifacts.register_run_outputs`. Así, una línea de registro y una salida se pueden relacionar mediante ``run_id``; esa es toda la finalidad."
    }
  ]
}
